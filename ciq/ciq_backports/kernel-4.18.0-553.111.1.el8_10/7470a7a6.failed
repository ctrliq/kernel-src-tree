macvlan: fix possible UAF in macvlan_forward_source()

jira KERNEL-762
cve CVE-2026-23001
Rebuild_History Non-Buildable kernel-4.18.0-553.111.1.el8_10
commit-author Eric Dumazet <edumazet@google.com>
commit 7470a7a63dc162f07c26dbf960e41ee1e248d80e
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-553.111.1.el8_10/7470a7a6.failed

Add RCU protection on (struct macvlan_source_entry)->vlan.

Whenever macvlan_hash_del_source() is called, we must clear
entry->vlan pointer before RCU grace period starts.

This allows macvlan_forward_source() to skip over
entries queued for freeing.

Note that macvlan_dev are already RCU protected, as they
are embedded in a standard netdev (netdev_priv(ndev)).

Fixes: 79cf79abce71 ("macvlan: add source mode")
	Reported-by: syzbot+7182fbe91e58602ec1fe@syzkaller.appspotmail.com
https: //lore.kernel.org/netdev/695fb1e8.050a0220.1c677c.039f.GAE@google.com/T/#u
	Signed-off-by: Eric Dumazet <edumazet@google.com>
Link: https://patch.msgid.link/20260108133651.1130486-1-edumazet@google.com
	Signed-off-by: Jakub Kicinski <kuba@kernel.org>
(cherry picked from commit 7470a7a63dc162f07c26dbf960e41ee1e248d80e)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/macvlan.c
diff --cc drivers/net/macvlan.c
index cd792205a606,b4df7e184791..000000000000
--- a/drivers/net/macvlan.c
+++ b/drivers/net/macvlan.c
@@@ -392,20 -386,14 +393,26 @@@ err
  static void macvlan_flush_sources(struct macvlan_port *port,
  				  struct macvlan_dev *vlan)
  {
 -	struct macvlan_source_entry *entry;
 -	struct hlist_node *next;
  	int i;
  
++<<<<<<< HEAD
 +	for (i = 0; i < MACVLAN_HASH_SIZE; i++) {
 +		struct hlist_node *h, *n;
++=======
+ 	hash_for_each_safe(port->vlan_source_hash, i, next, entry, hlist)
+ 		if (rcu_access_pointer(entry->vlan) == vlan)
+ 			macvlan_hash_del_source(entry);
++>>>>>>> 7470a7a63dc1 (macvlan: fix possible UAF in macvlan_forward_source())
 +
 +		hlist_for_each_safe(h, n, &port->vlan_source_hash[i]) {
 +			struct macvlan_source_entry *entry;
  
 +			entry = hlist_entry(h, struct macvlan_source_entry,
 +					    hlist);
 +			if (entry->vlan == vlan)
 +				macvlan_hash_del_source(entry);
 +		}
 +	}
  	vlan->macaddr_count = 0;
  }
  
@@@ -442,11 -430,22 +449,24 @@@ static void macvlan_forward_source(stru
  	struct macvlan_source_entry *entry;
  	u32 idx = macvlan_eth_hash(addr);
  	struct hlist_head *h = &port->vlan_source_hash[idx];
 -	bool consume = false;
  
  	hlist_for_each_entry_rcu(entry, h, hlist) {
++<<<<<<< HEAD
 +		if (ether_addr_equal_64bits(entry->addr, addr))
 +			macvlan_forward_source_one(skb, entry->vlan);
++=======
+ 		if (ether_addr_equal_64bits(entry->addr, addr)) {
+ 			struct macvlan_dev *vlan = rcu_dereference(entry->vlan);
+ 
+ 			if (!vlan)
+ 				continue;
+ 
+ 			if (vlan->flags & MACVLAN_FLAG_NODST)
+ 				consume = true;
+ 			macvlan_forward_source_one(skb, vlan);
+ 		}
++>>>>>>> 7470a7a63dc1 (macvlan: fix possible UAF in macvlan_forward_source())
  	}
 -
 -	return consume;
  }
  
  /* called under rcu_read_lock() from netif_receive_skb */
* Unmerged path drivers/net/macvlan.c
