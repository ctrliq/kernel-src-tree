ALSA: usb-audio: remove some dead code

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-1127.el7
commit-author Dan Carpenter <dan.carpenter@oracle.com>
commit b39e077fcb283dd96dd251a3abeba585402c61fe
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-1127.el7/b39e077f.failed

We recently cleaned up the error handling in commit 52c3e317a857 ("ALSA:
usb-audio: Unify the release of usb_mixer_elem_info objects") but
accidentally left this stray return.

Fixes: 52c3e317a857 ("ALSA: usb-audio: Unify the release of usb_mixer_elem_info objects")
	Signed-off-by: Dan Carpenter <dan.carpenter@oracle.com>
	Signed-off-by: Takashi Iwai <tiwai@suse.de>
(cherry picked from commit b39e077fcb283dd96dd251a3abeba585402c61fe)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	sound/usb/mixer.c
diff --cc sound/usb/mixer.c
index cc763d8f862a,3fd1d1749edf..000000000000
--- a/sound/usb/mixer.c
+++ b/sound/usb/mixer.c
@@@ -2662,9 -2649,8 +2662,14 @@@ static int parse_audio_selector_unit(st
  	kctl = snd_ctl_new1(&mixer_selectunit_ctl, cval);
  	if (! kctl) {
  		usb_audio_err(state->chip, "cannot malloc kcontrol\n");
++<<<<<<< HEAD
 +		kfree(namelist);
 +		kfree(cval);
 +		return -ENOMEM;
++=======
+ 		err = -ENOMEM;
+ 		goto error_name;
++>>>>>>> b39e077fcb28 (ALSA: usb-audio: remove some dead code)
  	}
  	kctl->private_value = (unsigned long)namelist;
  	kctl->private_free = usb_mixer_selector_elem_free;
* Unmerged path sound/usb/mixer.c
