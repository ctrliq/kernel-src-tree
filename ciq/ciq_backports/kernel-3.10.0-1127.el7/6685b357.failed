percpu: stop leaking bitmap metadata blocks

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-1127.el7
commit-author Mike Rapoport <rppt@linux.vnet.ibm.com>
commit 6685b357363bfe295e3ae73665014db4aed62c58
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-1127.el7/6685b357.failed

The commit ca460b3c9627 ("percpu: introduce bitmap metadata blocks")
introduced bitmap metadata blocks. These metadata blocks are allocated
whenever a new chunk is created, but they are never freed. Fix it.

Fixes: ca460b3c9627 ("percpu: introduce bitmap metadata blocks")
	Signed-off-by: Mike Rapoport <rppt@linux.vnet.ibm.com>
	Cc: stable@vger.kernel.org
	Signed-off-by: Dennis Zhou <dennis@kernel.org>
(cherry picked from commit 6685b357363bfe295e3ae73665014db4aed62c58)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	mm/percpu.c
diff --cc mm/percpu.c
index abecedcf6b94,4b90682623e9..000000000000
--- a/mm/percpu.c
+++ b/mm/percpu.c
@@@ -753,8 -1212,10 +753,15 @@@ static void pcpu_free_chunk(struct pcpu
  {
  	if (!chunk)
  		return;
++<<<<<<< HEAD
 +	pcpu_mem_free(chunk->map, chunk->map_alloc * sizeof(chunk->map[0]));
 +	pcpu_mem_free(chunk, pcpu_chunk_struct_size);
++=======
+ 	pcpu_mem_free(chunk->md_blocks);
+ 	pcpu_mem_free(chunk->bound_map);
+ 	pcpu_mem_free(chunk->alloc_map);
+ 	pcpu_mem_free(chunk);
++>>>>>>> 6685b357363b (percpu: stop leaking bitmap metadata blocks)
  }
  
  /**
* Unmerged path mm/percpu.c
