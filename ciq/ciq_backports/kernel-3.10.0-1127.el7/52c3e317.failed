ALSA: usb-audio: Unify the release of usb_mixer_elem_info objects

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-1127.el7
commit-author Takashi Iwai <tiwai@suse.de>
commit 52c3e317a857091fd746e15179a637f32be4d337
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-1127.el7/52c3e317.failed

Instead of the direct kfree() calls, introduce a new local helper to
release the usb_mixer_elem_info object.  This will be extended to do
more than a single kfree() in the later patches.

Also, use the standard goto instead of multiple calls in
parse_audio_selector_unit() error paths.

	Signed-off-by: Takashi Iwai <tiwai@suse.de>
(cherry picked from commit 52c3e317a857091fd746e15179a637f32be4d337)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	sound/usb/mixer.c
diff --cc sound/usb/mixer.c
index ba1f28d57b75,277660fd6e0a..000000000000
--- a/sound/usb/mixer.c
+++ b/sound/usb/mixer.c
@@@ -2722,8 -2709,8 +2724,13 @@@ static int parse_audio_selector_unit(st
  	kctl = snd_ctl_new1(&mixer_selectunit_ctl, cval);
  	if (! kctl) {
  		usb_audio_err(state->chip, "cannot malloc kcontrol\n");
++<<<<<<< HEAD
 +		kfree(namelist);
 +		kfree(cval);
++=======
+ 		err = -ENOMEM;
+ 		goto error_name;
++>>>>>>> 52c3e317a857 (ALSA: usb-audio: Unify the release of usb_mixer_elem_info objects)
  		return -ENOMEM;
  	}
  	kctl->private_value = (unsigned long)namelist;
* Unmerged path sound/usb/mixer.c
