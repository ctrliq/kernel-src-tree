net/mlx5: Improve naming of pci function vectors

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-537.el8
commit-author Eli Cohen <elic@nvidia.com>
commit 8bebfd767909eb884a7b529fb7a263e40c99627b
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-537.el8/8bebfd76.failed

The variable pf_vec is used to denote the number of vectors required for
the pci function's own use. To avoid confusion interpreting pf as
physical function, change the name to pcif_vec.

Same reasoning goes for pf_pool which is really pci function pool.

	Signed-off-by: Eli Cohen <elic@nvidia.com>
	Reviewed-by: Shay Drory <shayd@nvidia.com>
	Signed-off-by: Saeed Mahameed <saeedm@nvidia.com>
	Reviewed-by: Jacob Keller <jacob.e.keller@intel.com>
(cherry picked from commit 8bebfd767909eb884a7b529fb7a263e40c99627b)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/mellanox/mlx5/core/pci_irq.c
diff --cc drivers/net/ethernet/mellanox/mlx5/core/pci_irq.c
index 75dab94f4c94,c8e2b1ac7fe5..000000000000
--- a/drivers/net/ethernet/mellanox/mlx5/core/pci_irq.c
+++ b/drivers/net/ethernet/mellanox/mlx5/core/pci_irq.c
@@@ -427,8 -426,8 +427,13 @@@ struct mlx5_irq *mlx5_irq_request(struc
  	struct mlx5_irq_pool *pool;
  	struct mlx5_irq *irq;
  
++<<<<<<< HEAD
 +	pool = irq_table->pf_pool;
 +	irq = irq_pool_request_vector(pool, vecidx, affinity);
++=======
+ 	pool = irq_table->pcif_pool;
+ 	irq = irq_pool_request_vector(pool, vecidx, af_desc);
++>>>>>>> 8bebfd767909 (net/mlx5: Improve naming of pci function vectors)
  	if (IS_ERR(irq))
  		return irq;
  	mlx5_core_dbg(dev, "irq %u mapped to cpu %*pbl, %u EQs on this irq\n",
* Unmerged path drivers/net/ethernet/mellanox/mlx5/core/pci_irq.c
