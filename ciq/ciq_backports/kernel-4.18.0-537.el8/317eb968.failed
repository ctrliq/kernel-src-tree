netfilter: nft_set_pipapo: skip inactive elements during set walk

jira LE-1907
cve CVE-2023-6817
Rebuild_History Non-Buildable kernel-4.18.0-537.el8
commit-author Florian Westphal <fw@strlen.de>
commit 317eb9685095678f2c9f5a8189de698c5354316a
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-537.el8/317eb968.failed

Otherwise set elements can be deactivated twice which will cause a crash.

	Reported-by: Xingyuan Mo <hdthky0@gmail.com>
Fixes: 3c4287f62044 ("nf_tables: Add set type for arbitrary concatenation of ranges")
	Signed-off-by: Florian Westphal <fw@strlen.de>
	Signed-off-by: Pablo Neira Ayuso <pablo@netfilter.org>
(cherry picked from commit 317eb9685095678f2c9f5a8189de698c5354316a)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/netfilter/nft_set_pipapo.c
diff --cc net/netfilter/nft_set_pipapo.c
index 53a2931a570c,7252fcdae349..000000000000
--- a/net/netfilter/nft_set_pipapo.c
+++ b/net/netfilter/nft_set_pipapo.c
@@@ -1933,9 -2043,10 +1933,16 @@@ static void nft_pipapo_walk(const struc
  
  		e = f->mt[r].e;
  
++<<<<<<< HEAD
 +		elem.priv = e;
 +
 +		iter->err = iter->fn(ctx, set, iter, &elem);
++=======
+ 		if (!nft_set_elem_active(&e->ext, iter->genmask))
+ 			goto cont;
+ 
+ 		iter->err = iter->fn(ctx, set, iter, &e->priv);
++>>>>>>> 317eb9685095 (netfilter: nft_set_pipapo: skip inactive elements during set walk)
  		if (iter->err < 0)
  			goto out;
  
* Unmerged path net/netfilter/nft_set_pipapo.c
