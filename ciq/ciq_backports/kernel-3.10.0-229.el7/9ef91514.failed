audit: correct AUDIT_GET_FEATURE return message type

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-229.el7
commit-author Richard Guy Briggs <rgb@redhat.com>
commit 9ef91514774a140e468f99d73d7593521e6d25dc
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-229.el7/9ef91514.failed

When an AUDIT_GET_FEATURE message is sent from userspace to the kernel, it
should reply with a message tagged as an AUDIT_GET_FEATURE type with a struct
audit_feature.  The current reply is a message tagged as an AUDIT_GET
type with a struct audit_feature.

This appears to have been a cut-and-paste-eo in commit b0fed40.

	Reported-by: Steve Grubb <sgrubb@redhat.com>
	Signed-off-by: Richard Guy Briggs <rgb@redhat.com>
(cherry picked from commit 9ef91514774a140e468f99d73d7593521e6d25dc)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	kernel/audit.c
diff --cc kernel/audit.c
index 1c66a0cdea72,3a80abb6eaa1..000000000000
--- a/kernel/audit.c
+++ b/kernel/audit.c
@@@ -663,8 -724,7 +663,12 @@@ static int audit_get_feature(struct sk_
  
  	seq = nlmsg_hdr(skb)->nlmsg_seq;
  
++<<<<<<< HEAD
 +	audit_send_reply(NETLINK_CB(skb).portid, seq, AUDIT_GET, 0, 0,
 +			 &af, sizeof(af));
++=======
+ 	audit_send_reply(skb, seq, AUDIT_GET_FEATURE, 0, 0, &af, sizeof(af));
++>>>>>>> 9ef91514774a (audit: correct AUDIT_GET_FEATURE return message type)
  
  	return 0;
  }
* Unmerged path kernel/audit.c
