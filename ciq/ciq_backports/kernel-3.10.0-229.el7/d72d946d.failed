KVM: async_pf: kill the unnecessary use_mm/unuse_mm async_pf_execute()

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-229.el7
Rebuild_CHGLOG: - [virt] kvm/async_pf: kill the unnecessary use_mm/unuse_mm async_pf_execute() (Paolo Bonzini) [1116936]
Rebuild_FUZZ: 97.84%
commit-author Oleg Nesterov <oleg@redhat.com>
commit d72d946d0b649b79709b99b9d5cb7269fff8afaa
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-229.el7/d72d946d.failed

async_pf_execute() has no reasons to adopt apf->mm, gup(current, mm)
should work just fine even if current has another or NULL ->mm.

Recently kvm_async_page_present_sync() was added insedie the "use_mm"
section, but it seems that it doesn't need current->mm too.

	Signed-off-by: Oleg Nesterov <oleg@redhat.com>
	Reviewed-by: Andrea Arcangeli <aarcange@redhat.com>
	Signed-off-by: Paolo Bonzini <pbonzini@redhat.com>
(cherry picked from commit d72d946d0b649b79709b99b9d5cb7269fff8afaa)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	virt/kvm/async_pf.c
diff --cc virt/kvm/async_pf.c
index 7580aa1dec24,0ced4f31bc65..000000000000
--- a/virt/kvm/async_pf.c
+++ b/virt/kvm/async_pf.c
@@@ -65,11 -80,10 +65,14 @@@ static void async_pf_execute(struct wor
  
  	might_sleep();
  
- 	use_mm(mm);
  	down_read(&mm->mmap_sem);
 -	get_user_pages(current, mm, addr, 1, 1, 0, NULL, NULL);
 +	get_user_pages(NULL, mm, addr, 1, 1, 0, NULL, NULL);
  	up_read(&mm->mmap_sem);
++<<<<<<< HEAD
 +	unuse_mm(mm);
++=======
+ 	kvm_async_page_present_sync(vcpu, apf);
++>>>>>>> d72d946d0b64 (KVM: async_pf: kill the unnecessary use_mm/unuse_mm async_pf_execute())
  
  	spin_lock(&vcpu->async_pf.lock);
  	list_add_tail(&apf->link, &vcpu->async_pf.done);
* Unmerged path virt/kvm/async_pf.c
