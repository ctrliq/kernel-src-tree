prepend_path() needs to reinitialize dentry/vfsmount/mnt on restarts

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-229.el7
Rebuild_CHGLOG: - [fs] dcache: prepend_path() needs to reinitialize dentry/vfsmount/mnt on restarts (Mikulas Patocka) [1092746]
Rebuild_FUZZ: 94.44%
commit-author Al Viro <viro@zeniv.linux.org.uk>
commit ede4cebce16f5643c61aedd6d88d9070a1d23a68
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-229.el7/ede4cebc.failed

... and equivalent is needed in 3.12; it's broken there as well

	Signed-off-by: Al Viro <viro@zeniv.linux.org.uk>
(cherry picked from commit ede4cebce16f5643c61aedd6d88d9070a1d23a68)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/dcache.c
diff --cc fs/dcache.c
index d5dc4567dfd8,a9dd384c5e80..000000000000
--- a/fs/dcache.c
+++ b/fs/dcache.c
@@@ -2718,11 -2912,11 +2718,11 @@@ static int prepend_path(const struct pa
  			const struct path *root,
  			char **buffer, int *buflen)
  {
- 	struct dentry *dentry = path->dentry;
- 	struct vfsmount *vfsmnt = path->mnt;
- 	struct mount *mnt = real_mount(vfsmnt);
+ 	struct dentry *dentry;
+ 	struct vfsmount *vfsmnt;
+ 	struct mount *mnt;
  	int error = 0;
 -	unsigned seq, m_seq = 0;
 +	unsigned seq = 0;
  	char *bptr;
  	int blen;
  
@@@ -2730,6 -2928,10 +2730,13 @@@
  restart:
  	bptr = *buffer;
  	blen = *buflen;
++<<<<<<< HEAD
++=======
+ 	error = 0;
+ 	dentry = path->dentry;
+ 	vfsmnt = path->mnt;
+ 	mnt = real_mount(vfsmnt);
++>>>>>>> ede4cebce16f (prepend_path() needs to reinitialize dentry/vfsmount/mnt on restarts)
  	read_seqbegin_or_lock(&rename_lock, &seq);
  	while (dentry != root->dentry || vfsmnt != root->mnt) {
  		struct dentry * parent;
* Unmerged path fs/dcache.c
