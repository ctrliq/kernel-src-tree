libceph: ceph-msgr workqueue needs a resque worker

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-229.el7
Rebuild_CHGLOG: - [net] ceph: ceph-msgr workqueue needs a resque worker (Ilya Dryomov) [1165232]
Rebuild_FUZZ: 96.91%
commit-author Ilya Dryomov <idryomov@redhat.com>
commit f9865f06f7f18c6661c88d0511f05c48612319cc
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-229.el7/f9865f06.failed

Commit f363e45fd118 ("net/ceph: make ceph_msgr_wq non-reentrant")
effectively removed WQ_MEM_RECLAIM flag from ceph_msgr_wq.  This is
wrong - libceph is very much a memory reclaim path, so restore it.

	Cc: stable@vger.kernel.org # needs backporting for < 3.12
	Signed-off-by: Ilya Dryomov <idryomov@redhat.com>
	Tested-by: Micha Krause <micha@krausam.de>
	Reviewed-by: Sage Weil <sage@redhat.com>
(cherry picked from commit f9865f06f7f18c6661c88d0511f05c48612319cc)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/ceph/messenger.c
diff --cc net/ceph/messenger.c
index c202c92b8a45,559c9f619c20..000000000000
--- a/net/ceph/messenger.c
+++ b/net/ceph/messenger.c
@@@ -292,7 -292,11 +292,15 @@@ int ceph_msgr_init(void
  	if (ceph_msgr_slab_init())
  		return -ENOMEM;
  
++<<<<<<< HEAD
 +	ceph_msgr_wq = alloc_workqueue("ceph-msgr", WQ_NON_REENTRANT, 0);
++=======
+ 	/*
+ 	 * The number of active work items is limited by the number of
+ 	 * connections, so leave @max_active at default.
+ 	 */
+ 	ceph_msgr_wq = alloc_workqueue("ceph-msgr", WQ_MEM_RECLAIM, 0);
++>>>>>>> f9865f06f7f1 (libceph: ceph-msgr workqueue needs a resque worker)
  	if (ceph_msgr_wq)
  		return 0;
  
* Unmerged path net/ceph/messenger.c
