GFS2: Remove gfs2_quota_change_host structure

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-229.el7
commit-author Steven Whitehouse <swhiteho@redhat.com>
commit 7aed98fb1dfbd3f6d7b78647ff2956de2fcc4150
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-229.el7/7aed98fb.failed

There is only one place this is used, when reading in the quota
changes at mount time. It is not really required and much
simpler to just convert the fields from the on-disk structure
as required.

There should be no functional change as a result of this patch.

	Signed-off-by: Steven Whitehouse <swhiteho@redhat.com>
(cherry picked from commit 7aed98fb1dfbd3f6d7b78647ff2956de2fcc4150)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/gfs2/quota.c
diff --cc fs/gfs2/quota.c
index 679b366e073c,1b6b3675ee1d..000000000000
--- a/fs/gfs2/quota.c
+++ b/fs/gfs2/quota.c
@@@ -66,17 -67,11 +66,23 @@@
  #include "inode.h"
  #include "util.h"
  
++<<<<<<< HEAD
 +struct gfs2_quota_change_host {
 +	u64 qc_change;
 +	u32 qc_flags; /* GFS2_QCF_... */
 +	struct kqid qc_id;
 +};
 +
 +static LIST_HEAD(qd_lru_list);
 +static atomic_t qd_lru_count = ATOMIC_INIT(0);
 +static DEFINE_SPINLOCK(qd_lru_lock);
++=======
+ /* Lock order: qd_lock -> qd->lockref.lock -> lru lock */
+ static DEFINE_SPINLOCK(qd_lock);
+ struct list_lru gfs2_qd_lru;
++>>>>>>> 7aed98fb1dfb (GFS2: Remove gfs2_quota_change_host structure)
  
 -static void gfs2_qd_dispose(struct list_head *list)
 +int gfs2_shrink_qd_memory(struct shrinker *shrink, struct shrink_control *sc)
  {
  	struct gfs2_quota_data *qd;
  	struct gfs2_sbd *sdp;
* Unmerged path fs/gfs2/quota.c
