perf tools: Allow to close dso fd in case of open failure

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-229.el7
Rebuild_CHGLOG: - [tools] perf: Allow to close dso fd in case of open failure (Jiri Olsa) [1134356]
Rebuild_FUZZ: 94.44%
commit-author Jiri Olsa <jolsa@kernel.org>
commit a08cae03f430b971afa508a32662dc476d42d8cb
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-229.el7/a08cae03.failed

Adding do_open function that tries to close opened
dso objects in case we fail to open the dso due to
to crossing the allowed RLIMIT_NOFILE limit.

	Acked-by: Namhyung Kim <namhyung@kernel.org>
	Cc: Arnaldo Carvalho de Melo <acme@kernel.org>
	Cc: Corey Ashford <cjashfor@linux.vnet.ibm.com>
	Cc: David Ahern <dsahern@gmail.com>
	Cc: Frederic Weisbecker <fweisbec@gmail.com>
	Cc: Ingo Molnar <mingo@kernel.org>
	Cc: Jean Pihet <jean.pihet@linaro.org>
	Cc: Namhyung Kim <namhyung@kernel.org>
	Cc: Paul Mackerras <paulus@samba.org>
	Cc: Peter Zijlstra <a.p.zijlstra@chello.nl>
Link: http://lkml.kernel.org/r/1401892622-30848-9-git-send-email-jolsa@kernel.org
	Signed-off-by: Jiri Olsa <jolsa@kernel.org>
(cherry picked from commit a08cae03f430b971afa508a32662dc476d42d8cb)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	tools/perf/util/dso.c
diff --cc tools/perf/util/dso.c
index ffd3d5ede878,c30752c7eebb..000000000000
--- a/tools/perf/util/dso.c
+++ b/tools/perf/util/dso.c
@@@ -135,13 -139,53 +135,58 @@@ int dso__binary_type_file(struct dso *d
  	return ret;
  }
  
++<<<<<<< HEAD
 +static int open_dso(struct dso *dso, struct machine *machine)
++=======
+ /*
+  * Global list of open DSOs and the counter.
+  */
+ static LIST_HEAD(dso__data_open);
+ static long dso__data_open_cnt;
+ 
+ static void dso__list_add(struct dso *dso)
+ {
+ 	list_add_tail(&dso->data.open_entry, &dso__data_open);
+ 	dso__data_open_cnt++;
+ }
+ 
+ static void dso__list_del(struct dso *dso)
+ {
+ 	list_del(&dso->data.open_entry);
+ 	WARN_ONCE(dso__data_open_cnt <= 0,
+ 		  "DSO data fd counter out of bounds.");
+ 	dso__data_open_cnt--;
+ }
+ 
+ static void close_first_dso(void);
+ 
+ static int do_open(char *name)
+ {
+ 	int fd;
+ 
+ 	do {
+ 		fd = open(name, O_RDONLY);
+ 		if (fd >= 0)
+ 			return fd;
+ 
+ 		pr_debug("dso open failed, mmap: %s\n", strerror(errno));
+ 		if (!dso__data_open_cnt || errno != EMFILE)
+ 			break;
+ 
+ 		close_first_dso();
+ 	} while (1);
+ 
+ 	return -1;
+ }
+ 
+ static int __open_dso(struct dso *dso, struct machine *machine)
++>>>>>>> a08cae03f430 (perf tools: Allow to close dso fd in case of open failure)
  {
 +	char *root_dir = (char *) "";
 +	char *name;
  	int fd;
 -	char *root_dir = (char *)"";
 -	char *name = malloc(PATH_MAX);
  
 +	name = malloc(PATH_MAX);
  	if (!name)
  		return -ENOMEM;
  
* Unmerged path tools/perf/util/dso.c
