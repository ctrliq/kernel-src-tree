mm: convert use of typedef ctl_table to struct ctl_table

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-229.el7
Rebuild_CHGLOG: - [md] Convert use of typedef ctl_table to struct ctl_table (Jes Sorensen) [1085530]
Rebuild_FUZZ: 96.30%
commit-author Joe Perches <joe@perches.com>
commit cccad5b983d2b0aa453879591ac4ab1c54ff9db6
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-229.el7/cccad5b9.failed

This typedef is unnecessary and should just be removed.

	Signed-off-by: Joe Perches <joe@perches.com>
	Signed-off-by: Andrew Morton <akpm@linux-foundation.org>
	Signed-off-by: Linus Torvalds <torvalds@linux-foundation.org>
(cherry picked from commit cccad5b983d2b0aa453879591ac4ab1c54ff9db6)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	mm/page_alloc.c
diff --cc mm/page_alloc.c
index 48fe47b7829c,4f59fa29eda8..000000000000
--- a/mm/page_alloc.c
+++ b/mm/page_alloc.c
@@@ -5590,7 -5805,7 +5590,11 @@@ module_init(init_per_zone_wmark_min
   *	that we can call two helper functions whenever min_free_kbytes
   *	changes.
   */
++<<<<<<< HEAD
 +int min_free_kbytes_sysctl_handler(ctl_table *table, int write, 
++=======
+ int min_free_kbytes_sysctl_handler(struct ctl_table *table, int write,
++>>>>>>> cccad5b983d2 (mm: convert use of typedef ctl_table to struct ctl_table)
  	void __user *buffer, size_t *length, loff_t *ppos)
  {
  	int rc;
@@@ -5659,11 -5874,10 +5663,15 @@@ int lowmem_reserve_ratio_sysctl_handler
  
  /*
   * percpu_pagelist_fraction - changes the pcp->high for each zone on each
 - * cpu.  It is the fraction of total pages in each zone that a hot per cpu
 - * pagelist can have before it gets flushed back to buddy allocator.
 + * cpu.  It is the fraction of total pages in each zone that a hot per cpu pagelist
 + * can have before it gets flushed back to buddy allocator.
   */
++<<<<<<< HEAD
 +
 +int percpu_pagelist_fraction_sysctl_handler(ctl_table *table, int write,
++=======
+ int percpu_pagelist_fraction_sysctl_handler(struct ctl_table *table, int write,
++>>>>>>> cccad5b983d2 (mm: convert use of typedef ctl_table to struct ctl_table)
  	void __user *buffer, size_t *length, loff_t *ppos)
  {
  	struct zone *zone;
diff --git a/mm/page-writeback.c b/mm/page-writeback.c
index 4c5ec2af3e9d..df3cc9ff7b47 100644
--- a/mm/page-writeback.c
+++ b/mm/page-writeback.c
@@ -1682,7 +1682,7 @@ void throttle_vm_writeout(gfp_t gfp_mask)
 /*
  * sysctl handler for /proc/sys/vm/dirty_writeback_centisecs
  */
-int dirty_writeback_centisecs_handler(ctl_table *table, int write,
+int dirty_writeback_centisecs_handler(struct ctl_table *table, int write,
 	void __user *buffer, size_t *length, loff_t *ppos)
 {
 	proc_dointvec(table, write, buffer, length, ppos);
* Unmerged path mm/page_alloc.c
