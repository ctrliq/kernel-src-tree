iommu/vt-d: Rename 'hwdev' variables to 'dev' now that that's the norm

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-229.el7
Rebuild_CHGLOG: - [iommu] vt-d: Rename 'hwdev' variables to 'dev' now that that's the norm (Myron Stowe) [1129880 1087643]
Rebuild_FUZZ: 95.52%
commit-author David Woodhouse <David.Woodhouse@intel.com>
commit 5040a918bd109a1903e7babac817325620939fa4
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-229.el7/5040a918.failed

	Signed-off-by: David Woodhouse <David.Woodhouse@intel.com>
(cherry picked from commit 5040a918bd109a1903e7babac817325620939fa4)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/iommu/intel-iommu.c
diff --cc drivers/iommu/intel-iommu.c
index bd20ea6166e6,60f8ceeb06e4..000000000000
--- a/drivers/iommu/intel-iommu.c
+++ b/drivers/iommu/intel-iommu.c
@@@ -2859,10 -2968,10 +2859,14 @@@ static dma_addr_t __intel_map_single(st
  
  	BUG_ON(dir == DMA_NONE);
  
- 	if (iommu_no_mapping(hwdev))
+ 	if (iommu_no_mapping(dev))
  		return paddr;
  
++<<<<<<< HEAD
 +	domain = get_valid_domain_for_dev(pdev);
++=======
+ 	domain = get_valid_domain_for_dev(dev);
++>>>>>>> 5040a918bd10 (iommu/vt-d: Rename 'hwdev' variables to 'dev' now that that's the norm)
  	if (!domain)
  		return 0;
  
@@@ -3098,11 -3208,12 +3102,11 @@@ static void intel_unmap_sg(struct devic
  	unsigned long start_pfn, last_pfn;
  	struct iova *iova;
  	struct intel_iommu *iommu;
 -	struct page *freelist;
  
- 	if (iommu_no_mapping(hwdev))
+ 	if (iommu_no_mapping(dev))
  		return;
  
- 	domain = find_domain(hwdev);
+ 	domain = find_domain(dev);
  	BUG_ON(!domain);
  
  	iommu = domain_get_iommu(domain);
@@@ -3163,10 -3271,10 +3167,14 @@@ static int intel_map_sg(struct device *
  	struct intel_iommu *iommu;
  
  	BUG_ON(dir == DMA_NONE);
- 	if (iommu_no_mapping(hwdev))
- 		return intel_nontranslate_map_sg(hwdev, sglist, nelems, dir);
+ 	if (iommu_no_mapping(dev))
+ 		return intel_nontranslate_map_sg(dev, sglist, nelems, dir);
  
++<<<<<<< HEAD
 +	domain = get_valid_domain_for_dev(pdev);
++=======
+ 	domain = get_valid_domain_for_dev(dev);
++>>>>>>> 5040a918bd10 (iommu/vt-d: Rename 'hwdev' variables to 'dev' now that that's the norm)
  	if (!domain)
  		return 0;
  
* Unmerged path drivers/iommu/intel-iommu.c
