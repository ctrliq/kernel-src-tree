drm/i915: resume MST after reading back hw state

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-229.el7
Rebuild_CHGLOG: - [drm] i915: resume MST after reading back hw state (Rob Clark) [1173317]
Rebuild_FUZZ: 95.65%
commit-author Dave Airlie <airlied@redhat.com>
commit e7d6f7d708290da1b7c92f533444b042c79412e0
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-229.el7/e7d6f7d7.failed

Otherwise the MST resume paths can hit DPMS paths
which hit state checker paths, which hit WARN_ON,
because the state checker is inconsistent with the
hw.

This fixes a bunch of WARN_ON's on resume after
undocking.

	Signed-off-by: Dave Airlie <airlied@redhat.com>
	Reviewed-by: Daniel Vetter <daniel.vetter@ffwll.ch>
	Cc: stable@vger.kernel.org
	Signed-off-by: Jani Nikula <jani.nikula@intel.com>
(cherry picked from commit e7d6f7d708290da1b7c92f533444b042c79412e0)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/gpu/drm/i915/i915_drv.c
diff --cc drivers/gpu/drm/i915/i915_drv.c
index 2ad27880cd04,f990ab4c3efb..000000000000
--- a/drivers/gpu/drm/i915/i915_drv.c
+++ b/drivers/gpu/drm/i915/i915_drv.c
@@@ -600,6 -701,11 +600,14 @@@ static int __i915_drm_thaw(struct drm_d
  
  		intel_modeset_init_hw(dev);
  
++<<<<<<< HEAD
++=======
+ 		spin_lock_irq(&dev_priv->irq_lock);
+ 		if (dev_priv->display.hpd_irq_setup)
+ 			dev_priv->display.hpd_irq_setup(dev);
+ 		spin_unlock_irq(&dev_priv->irq_lock);
+ 
++>>>>>>> e7d6f7d70829 (drm/i915: resume MST after reading back hw state)
  		drm_modeset_lock_all(dev);
  		intel_modeset_setup_hw_state(dev, true);
  		drm_modeset_unlock_all(dev);
* Unmerged path drivers/gpu/drm/i915/i915_drv.c
