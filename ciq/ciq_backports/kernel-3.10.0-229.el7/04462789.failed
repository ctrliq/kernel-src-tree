nfs: get rid of duplicate dprintk

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-229.el7
commit-author Weston Andros Adamson <dros@primarydata.com>
commit 0446278999e5b622b309179ba392efcb5d8b8a46
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-229.el7/04462789.failed

This was introduced by a merge error with my recent pgio patchset.

	Signed-off-by: Weston Andros Adamson <dros@primarydata.com>
	Signed-off-by: Trond Myklebust <trond.myklebust@primarydata.com>
(cherry picked from commit 0446278999e5b622b309179ba392efcb5d8b8a46)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/nfs/read.c
diff --cc fs/nfs/read.c
index d58a7ae6152b,beff2769c5c5..000000000000
--- a/fs/nfs/read.c
+++ b/fs/nfs/read.c
@@@ -166,16 -105,16 +166,25 @@@ static void nfs_readpage_release(struc
  {
  	struct inode *d_inode = req->wb_context->dentry->d_inode;
  
 -	dprintk("NFS: read done (%s/%llu %d@%lld)\n", d_inode->i_sb->s_id,
 -		(unsigned long long)NFS_FILEID(d_inode), req->wb_bytes,
 -		(long long)req_offset(req));
 +	if (PageUptodate(req->wb_page))
 +		nfs_readpage_to_fscache(d_inode, req->wb_page, 0);
  
++<<<<<<< HEAD
 +	unlock_page(req->wb_page);
 +
 +	dprintk("NFS: read done (%s/%Lu %d@%Ld)\n",
 +			req->wb_context->dentry->d_inode->i_sb->s_id,
 +			(unsigned long long)NFS_FILEID(req->wb_context->dentry->d_inode),
 +			req->wb_bytes,
 +			(long long)req_offset(req));
++=======
+ 	if (nfs_page_group_sync_on_bit(req, PG_UNLOCKPAGE)) {
+ 		if (PageUptodate(req->wb_page))
+ 			nfs_readpage_to_fscache(d_inode, req->wb_page, 0);
+ 
+ 		unlock_page(req->wb_page);
+ 	}
++>>>>>>> 0446278999e5 (nfs: get rid of duplicate dprintk)
  	nfs_release_request(req);
  }
  
* Unmerged path fs/nfs/read.c
