hpsa: make hpsa_init_one return -ENOMEM if allocation of h->lockup_detected fails

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-229.el7
commit-author Stephen M. Cameron <scameron@beardog.cce.hp.com>
commit 2a5ac32653786871f03a22ecd524617cf53e1b53
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-229.el7/2a5ac326.failed

	Signed-off-by: Stephen M. Cameron <scameron@beardog.cce.hp.com>
	Reviewed-by: Joe Handzik <joseph.t.handzik@hp.com>
	Signed-off-by: Christoph Hellwig <hch@lst.de>
(cherry picked from commit 2a5ac32653786871f03a22ecd524617cf53e1b53)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/scsi/hpsa.c
diff --cc drivers/scsi/hpsa.c
index 7620db05b902,1d284730d66b..000000000000
--- a/drivers/scsi/hpsa.c
+++ b/drivers/scsi/hpsa.c
@@@ -6926,6 -6992,15 +6926,18 @@@ reinit_after_soft_reset
  	spin_lock_init(&h->offline_device_lock);
  	spin_lock_init(&h->scan_lock);
  	spin_lock_init(&h->passthru_count_lock);
++<<<<<<< HEAD
++=======
+ 
+ 	/* Allocate and clear per-cpu variable lockup_detected */
+ 	h->lockup_detected = alloc_percpu(u32);
+ 	if (!h->lockup_detected) {
+ 		rc = -ENOMEM;
+ 		goto clean1;
+ 	}
+ 	set_lockup_detected_for_all_cpus(h, 0);
+ 
++>>>>>>> 2a5ac3265378 (hpsa: make hpsa_init_one return -ENOMEM if allocation of h->lockup_detected fails)
  	rc = hpsa_pci_init(h);
  	if (rc != 0)
  		goto clean1;
* Unmerged path drivers/scsi/hpsa.c
