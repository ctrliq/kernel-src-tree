gfs2: Withdraw immediately in gfs2_trans_add_meta

jira KERNEL-1613
Rebuild_History Non-Buildable kernel-5.14.0-687.49.1.el9_8
commit-author Andreas Gruenbacher <agruenba@redhat.com>
commit 3a88edc1657da9a847041ea994e66e26db9578d2
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-5.14.0-687.49.1.el9_8/3a88edc1.failed

We can now withdraw while the log is locked.

	Signed-off-by: Andreas Gruenbacher <agruenba@redhat.com>
(cherry picked from commit 3a88edc1657da9a847041ea994e66e26db9578d2)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/gfs2/trans.c
diff --cc fs/gfs2/trans.c
index 694b2ece6b16,1cce8e0f85da..000000000000
--- a/fs/gfs2/trans.c
+++ b/fs/gfs2/trans.c
@@@ -287,9 -304,7 +286,13 @@@ void gfs2_trans_add_meta(struct gfs2_gl
  	list_add(&bd->bd_list, &tr->tr_buf);
  	tr->tr_num_buf_new++;
  out_unlock:
++<<<<<<< HEAD
 +	spin_unlock(&sdp->sd_log_lock);
 +	if (withdraw)
 +		gfs2_assert_withdraw(sdp, 0);
++=======
+ 	gfs2_log_unlock(sdp);
++>>>>>>> 3a88edc1657d (gfs2: Withdraw immediately in gfs2_trans_add_meta)
  out:
  	unlock_buffer(bh);
  }
* Unmerged path fs/gfs2/trans.c
