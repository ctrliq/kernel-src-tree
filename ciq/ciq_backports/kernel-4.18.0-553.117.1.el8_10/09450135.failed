nfs: properly protect nfs_direct_req fields

jira KERNEL-814
Rebuild_History Non-Buildable kernel-4.18.0-553.117.1.el8_10
commit-author Josef Bacik <josef@toxicpanda.com>
commit 094501358e7a165071673e754c3925683683057f
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-553.117.1.el8_10/09450135.failed

We protect accesses to the nfs_direct_req fields with the dreq->lock
ever where except nfs_direct_commit_complete.  This isn't a huge deal,
but it does lead to confusion, and we could potentially end up setting
NFS_ODIRECT_RESCHED_WRITES in one thread where we've had an error in
another.  Clean this up to properly protect ->error and ->flags in the
commit completion path.

	Signed-off-by: Josef Bacik <josef@toxicpanda.com>
	Signed-off-by: Trond Myklebust <trond.myklebust@hammerspace.com>
(cherry picked from commit 094501358e7a165071673e754c3925683683057f)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/nfs/direct.c
diff --cc fs/nfs/direct.c
index 82ea49f259c5,befcc167e25f..000000000000
--- a/fs/nfs/direct.c
+++ b/fs/nfs/direct.c
@@@ -617,6 -604,9 +617,12 @@@ static void nfs_direct_commit_complete(
  	struct nfs_page *req;
  	int status = data->task.tk_status;
  
++<<<<<<< HEAD
++=======
+ 	trace_nfs_direct_commit_complete(dreq);
+ 
+ 	spin_lock(&dreq->lock);
++>>>>>>> 094501358e7a (nfs: properly protect nfs_direct_req fields)
  	if (status < 0) {
  		/* Errors in commit are fatal */
  		dreq->error = status;
@@@ -632,8 -621,16 +639,21 @@@
  	while (!list_empty(&data->pages)) {
  		req = nfs_list_entry(data->pages.next);
  		nfs_list_remove_request(req);
++<<<<<<< HEAD
 +		if (status >= 0 && !nfs_write_match_verf(verf, req)) {
 +			dreq->flags = NFS_ODIRECT_RESCHED_WRITES;
++=======
+ 		if (status < 0) {
+ 			spin_lock(&dreq->lock);
+ 			nfs_direct_truncate_request(dreq, req);
+ 			spin_unlock(&dreq->lock);
+ 			nfs_release_request(req);
+ 		} else if (!nfs_write_match_verf(verf, req)) {
+ 			spin_lock(&dreq->lock);
+ 			if (dreq->flags == 0)
+ 				dreq->flags = NFS_ODIRECT_RESCHED_WRITES;
+ 			spin_unlock(&dreq->lock);
++>>>>>>> 094501358e7a (nfs: properly protect nfs_direct_req fields)
  			/*
  			 * Despite the reboot, the write was successful,
  			 * so reset wb_nio.
* Unmerged path fs/nfs/direct.c
