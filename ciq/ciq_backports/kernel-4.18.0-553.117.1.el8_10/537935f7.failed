NFS/pNFS: Set the connect timeout for the pNFS flexfiles driver

jira KERNEL-814
Rebuild_History Non-Buildable kernel-4.18.0-553.117.1.el8_10
commit-author Trond Myklebust <trond.myklebust@hammerspace.com>
commit 537935f72eb28a3dd0097386f06402e25e66359a
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-553.117.1.el8_10/537935f7.failed

Ensure that the connect timeout for the pNFS flexfiles driver is of the
same order as the I/O timeout, so that we can fail over quickly when
trying to read from a data server that is down.

	Signed-off-by: Trond Myklebust <trond.myklebust@hammerspace.com>
	Signed-off-by: Anna Schumaker <Anna.Schumaker@Netapp.com>
(cherry picked from commit 537935f72eb28a3dd0097386f06402e25e66359a)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/nfs/client.c
#	fs/nfs/internal.h
#	fs/nfs/nfs3client.c
diff --cc fs/nfs/client.c
index 75ae576066e9,44eca51b2808..000000000000
--- a/fs/nfs/client.c
+++ b/fs/nfs/client.c
@@@ -518,6 -516,9 +518,12 @@@ int nfs_create_rpc_client(struct nfs_cl
  		.version	= clp->rpc_ops->version,
  		.authflavor	= flavor,
  		.cred		= cl_init->cred,
++<<<<<<< HEAD
++=======
+ 		.xprtsec	= cl_init->xprtsec,
+ 		.connect_timeout = cl_init->connect_timeout,
+ 		.reconnect_timeout = cl_init->reconnect_timeout,
++>>>>>>> 537935f72eb2 (NFS/pNFS: Set the connect timeout for the pNFS flexfiles driver)
  	};
  
  	if (test_bit(NFS_CS_DISCRTRY, &clp->cl_flags))
diff --cc fs/nfs/internal.h
index 417bf90e9fe6,9c9cf764f600..000000000000
--- a/fs/nfs/internal.h
+++ b/fs/nfs/internal.h
@@@ -81,6 -81,9 +81,12 @@@ struct nfs_client_initdata 
  	struct net *net;
  	const struct rpc_timeout *timeparms;
  	const struct cred *cred;
++<<<<<<< HEAD
++=======
+ 	struct xprtsec_parms xprtsec;
+ 	unsigned long connect_timeout;
+ 	unsigned long reconnect_timeout;
++>>>>>>> 537935f72eb2 (NFS/pNFS: Set the connect timeout for the pNFS flexfiles driver)
  };
  
  /*
diff --cc fs/nfs/nfs3client.c
index 17a2e97780ce,674c012868b1..000000000000
--- a/fs/nfs/nfs3client.c
+++ b/fs/nfs/nfs3client.c
@@@ -92,6 -98,9 +93,12 @@@ struct nfs_client *nfs3_set_ds_client(s
  		.net = mds_clp->cl_net,
  		.timeparms = &ds_timeout,
  		.cred = mds_srv->cred,
++<<<<<<< HEAD
++=======
+ 		.xprtsec = mds_clp->cl_xprtsec,
+ 		.connect_timeout = connect_timeout,
+ 		.reconnect_timeout = connect_timeout,
++>>>>>>> 537935f72eb2 (NFS/pNFS: Set the connect timeout for the pNFS flexfiles driver)
  	};
  	struct nfs_client *clp;
  	char buf[INET6_ADDRSTRLEN + 1];
* Unmerged path fs/nfs/client.c
* Unmerged path fs/nfs/internal.h
* Unmerged path fs/nfs/nfs3client.c
diff --git a/fs/nfs/pnfs_nfs.c b/fs/nfs/pnfs_nfs.c
index 098274daca24..ca1331377b1f 100644
--- a/fs/nfs/pnfs_nfs.c
+++ b/fs/nfs/pnfs_nfs.c
@@ -851,6 +851,7 @@ static int _nfs4_pnfs_v3_ds_connect(struct nfs_server *mds_srv,
 {
 	struct nfs_client *clp = ERR_PTR(-EIO);
 	struct nfs4_pnfs_ds_addr *da;
+	unsigned long connect_timeout = timeo * (retrans + 1) * HZ / 10;
 	int status = 0;
 
 	dprintk("--> %s DS %s\n", __func__, ds->ds_remotestr);
@@ -869,6 +870,8 @@ static int _nfs4_pnfs_v3_ds_connect(struct nfs_server *mds_srv,
 				.dstaddr = (struct sockaddr *)&da->da_addr,
 				.addrlen = da->da_addrlen,
 				.servername = clp->cl_hostname,
+				.connect_timeout = connect_timeout,
+				.reconnect_timeout = connect_timeout,
 			};
 
 			if (da->da_transport != clp->cl_proto)
