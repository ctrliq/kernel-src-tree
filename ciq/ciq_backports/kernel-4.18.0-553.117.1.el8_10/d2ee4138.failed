SUNRPC: Allow specification of TCP client connect timeout at setup

jira KERNEL-814
Rebuild_History Non-Buildable kernel-4.18.0-553.117.1.el8_10
commit-author Trond Myklebust <trond.myklebust@hammerspace.com>
commit d2ee413884cdbdfcfc1560526615519311a47d33
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-553.117.1.el8_10/d2ee4138.failed

When we create a TCP transport, the connect timeout parameters are
currently fixed to be 90s. This is problematic in the pNFS flexfiles
case, where we may have multiple mirrors, and we would like to fail over
quickly to the next mirror if a data server is down.

This patch adds the ability to specify the connection parameters at RPC
client creation time.

	Signed-off-by: Trond Myklebust <trond.myklebust@hammerspace.com>
	Signed-off-by: Anna Schumaker <Anna.Schumaker@Netapp.com>
(cherry picked from commit d2ee413884cdbdfcfc1560526615519311a47d33)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	include/linux/sunrpc/clnt.h
#	include/linux/sunrpc/xprt.h
#	net/sunrpc/clnt.c
diff --cc include/linux/sunrpc/clnt.h
index 8fcd08c62084,af7358277f1c..000000000000
--- a/include/linux/sunrpc/clnt.h
+++ b/include/linux/sunrpc/clnt.h
@@@ -140,6 -147,9 +140,12 @@@ struct rpc_create_args 
  	struct svc_xprt		*bc_xprt;	/* NFSv4.1 backchannel */
  	const struct cred	*cred;
  	unsigned int		max_connect;
++<<<<<<< HEAD
++=======
+ 	struct xprtsec_parms	xprtsec;
+ 	unsigned long		connect_timeout;
+ 	unsigned long		reconnect_timeout;
++>>>>>>> d2ee413884cd (SUNRPC: Allow specification of TCP client connect timeout at setup)
  };
  
  struct rpc_add_xprt_test {
diff --cc include/linux/sunrpc/xprt.h
index 9e93b83f5412,4ecc89301eb7..000000000000
--- a/include/linux/sunrpc/xprt.h
+++ b/include/linux/sunrpc/xprt.h
@@@ -332,6 -350,9 +332,12 @@@ struct xprt_create 
  	struct svc_xprt		*bc_xprt;	/* NFSv4.1 backchannel */
  	struct rpc_xprt_switch	*bc_xps;
  	unsigned int		flags;
++<<<<<<< HEAD
++=======
+ 	struct xprtsec_parms	xprtsec;
+ 	unsigned long		connect_timeout;
+ 	unsigned long		reconnect_timeout;
++>>>>>>> d2ee413884cd (SUNRPC: Allow specification of TCP client connect timeout at setup)
  };
  
  struct xprt_class {
diff --cc net/sunrpc/clnt.c
index a44209ef8f52,06df08b0ee9e..000000000000
--- a/net/sunrpc/clnt.c
+++ b/net/sunrpc/clnt.c
@@@ -534,6 -533,9 +534,12 @@@ struct rpc_clnt *rpc_create(struct rpc_
  		.addrlen = args->addrsize,
  		.servername = args->servername,
  		.bc_xprt = args->bc_xprt,
++<<<<<<< HEAD
++=======
+ 		.xprtsec = args->xprtsec,
+ 		.connect_timeout = args->connect_timeout,
+ 		.reconnect_timeout = args->reconnect_timeout,
++>>>>>>> d2ee413884cd (SUNRPC: Allow specification of TCP client connect timeout at setup)
  	};
  	char servername[48];
  	struct rpc_clnt *clnt;
* Unmerged path include/linux/sunrpc/clnt.h
* Unmerged path include/linux/sunrpc/xprt.h
* Unmerged path net/sunrpc/clnt.c
diff --git a/net/sunrpc/xprtsock.c b/net/sunrpc/xprtsock.c
index 41866e518f19..c5bb2b2b32e5 100644
--- a/net/sunrpc/xprtsock.c
+++ b/net/sunrpc/xprtsock.c
@@ -2217,8 +2217,6 @@ static void xs_tcp_set_connect_timeout(struct rpc_xprt *xprt,
 		unsigned long reconnect_timeout)
 {
 	struct sock_xprt *transport = container_of(xprt, struct sock_xprt, xprt);
-	struct rpc_timeout to;
-	unsigned long initval;
 
 	spin_lock(&xprt->transport_lock);
 	if (reconnect_timeout < xprt->max_reconnect_timeout)
@@ -3019,8 +3017,13 @@ static struct rpc_xprt *xs_setup_tcp(struct xprt_create *args)
 	xprt->timeout = &xs_tcp_default_timeout;
 
 	xprt->max_reconnect_timeout = xprt->timeout->to_maxval;
+	if (args->reconnect_timeout)
+		xprt->max_reconnect_timeout = args->reconnect_timeout;
+
 	xprt->connect_timeout = xprt->timeout->to_initval *
 		(xprt->timeout->to_retries + 1);
+	if (args->connect_timeout)
+		xs_tcp_do_set_connect_timeout(xprt, args->connect_timeout);
 
 	INIT_WORK(&transport->recv_worker, xs_stream_data_receive_workfn);
 	INIT_WORK(&transport->error_worker, xs_error_handle);
