tcp: use this_cpu_read(*X) instead of *this_cpu_ptr(X)

jira KERNEL-814
Rebuild_History Non-Buildable kernel-4.18.0-553.115.1.el8_10
commit-author Eric Dumazet <edumazet@google.com>
commit 5472c3c6a5f0573a609f77adce8ed1bd54233c7a
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-553.115.1.el8_10/5472c3c6.failed

this_cpu_read(*X) is slightly faster than *this_cpu_ptr(X)

	Signed-off-by: Eric Dumazet <edumazet@google.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 5472c3c6a5f0573a609f77adce8ed1bd54233c7a)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/ipv4/tcp_ipv4.c
diff --cc net/ipv4/tcp_ipv4.c
index f9a3862ce853,59b7edd8719c..000000000000
--- a/net/ipv4/tcp_ipv4.c
+++ b/net/ipv4/tcp_ipv4.c
@@@ -802,13 -771,10 +802,18 @@@ static void tcp_v4_send_reset(const str
  	arg.tos = ip_hdr(skb)->tos;
  	arg.uid = sock_net_uid(net, sk && sk_fullsock(sk) ? sk : NULL);
  	local_bh_disable();
++<<<<<<< HEAD
 +	ctl_sk = *this_cpu_ptr(net->ipv4.tcp_sk);
 +	if (sk) {
++=======
+ 	ctl_sk = this_cpu_read(*net->ipv4.tcp_sk);
+ 	if (sk)
++>>>>>>> 5472c3c6a5f0 (tcp: use this_cpu_read(*X) instead of *this_cpu_ptr(X))
  		ctl_sk->sk_mark = (sk->sk_state == TCP_TIME_WAIT) ?
  				   inet_twsk(sk)->tw_mark : sk->sk_mark;
 +		txhash = (sk->sk_state == TCP_TIME_WAIT) ?
 +			 inet_twsk(sk)->tw_txhash : sk->sk_txhash;
 +	}
  	ip_send_unicast_reply(ctl_sk,
  			      skb, &TCP_SKB_CB(skb)->header.h4.opt,
  			      ip_hdr(skb)->saddr, ip_hdr(skb)->daddr,
* Unmerged path net/ipv4/tcp_ipv4.c
