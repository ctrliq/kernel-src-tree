x86/microcode/amd: Use correct per CPU ucode_cpu_info

jira KERNEL-814
Rebuild_History Non-Buildable kernel-4.18.0-553.115.1.el8_10
commit-author Thomas Gleixner <tglx@linutronix.de>
commit ecfd41089348fa4cc767dc588367e9fdf8cb6b9d
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-553.115.1.el8_10/ecfd4108.failed

find_blobs_in_containers() is invoked on every CPU but overwrites
unconditionally ucode_cpu_info of CPU0.

Fix this by using the proper CPU data and move the assignment into the
call site apply_ucode_from_containers() so that the function can be
reused.

	Signed-off-by: Thomas Gleixner <tglx@linutronix.de>
	Signed-off-by: Borislav Petkov (AMD) <bp@alien8.de>
Link: https://lore.kernel.org/r/20231010150702.433454320@linutronix.de
(cherry picked from commit ecfd41089348fa4cc767dc588367e9fdf8cb6b9d)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	arch/x86/kernel/cpu/microcode/amd.c
diff --cc arch/x86/kernel/cpu/microcode/amd.c
index 26b29916f12b,56d8bd6b9fe9..000000000000
--- a/arch/x86/kernel/cpu/microcode/amd.c
+++ b/arch/x86/kernel/cpu/microcode/amd.c
@@@ -475,27 -498,11 +475,30 @@@ static bool get_builtin_microcode(struc
  
  static void find_blobs_in_containers(unsigned int cpuid_1_eax, struct cpio_data *ret)
  {
 +	struct ucode_cpu_info *uci;
  	struct cpio_data cp;
 +	const char *path;
 +	bool use_pa;
 +
 +	if (IS_ENABLED(CONFIG_X86_32)) {
 +		uci	= (struct ucode_cpu_info *)__pa_nodebug(ucode_cpu_info);
 +		path	= (const char *)__pa_nodebug(ucode_path);
 +		use_pa	= true;
 +	} else {
 +		uci     = ucode_cpu_info;
 +		path	= ucode_path;
 +		use_pa	= false;
 +	}
  
  	if (!get_builtin_microcode(&cp, x86_family(cpuid_1_eax)))
 -		cp = find_microcode_in_initrd(ucode_path);
 +		cp = find_microcode_in_initrd(path, use_pa);
 +
++<<<<<<< HEAD
 +	/* Needed in load_microcode_amd() */
 +	uci->cpu_sig.sig = cpuid_1_eax;
  
++=======
++>>>>>>> ecfd41089348 (x86/microcode/amd: Use correct per CPU ucode_cpu_info)
  	*ret = cp;
  }
  
* Unmerged path arch/x86/kernel/cpu/microcode/amd.c
