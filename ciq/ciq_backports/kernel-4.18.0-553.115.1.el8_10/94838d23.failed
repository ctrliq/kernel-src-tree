x86/microcode/AMD: Use the family,model,stepping encoded in the patch ID

jira KERNEL-814
Rebuild_History Non-Buildable kernel-4.18.0-553.115.1.el8_10
commit-author Borislav Petkov <bp@alien8.de>
commit 94838d230a6c835ced1bad06b8759e0a5f19c1d3
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-553.115.1.el8_10/94838d23.failed

On Zen and newer, the family, model and stepping is part of the
microcode patch ID so that the equivalence table the driver has been
using, is not needed anymore.

So switch the driver to use that from now on.

The equivalence table in the microcode blob should still remain in case
there's need to pass some additional information to the kernel loader.

	Signed-off-by: Borislav Petkov (AMD) <bp@alien8.de>
Link: https://lore.kernel.org/r/20240725112037.GBZqI1BbUk1KMlOJ_D@fat_crate.local
(cherry picked from commit 94838d230a6c835ced1bad06b8759e0a5f19c1d3)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	arch/x86/kernel/cpu/microcode/amd.c
diff --cc arch/x86/kernel/cpu/microcode/amd.c
index 26b29916f12b,1f5d36f92b8a..000000000000
--- a/arch/x86/kernel/cpu/microcode/amd.c
+++ b/arch/x86/kernel/cpu/microcode/amd.c
@@@ -116,15 -212,17 +172,19 @@@ static bool verify_equivalence_table(co
  	const u32 *hdr = (const u32 *)buf;
  	u32 cont_type, equiv_tbl_len;
  
 -	if (!verify_container(buf, buf_size))
 +	if (!verify_container(buf, buf_size, early))
  		return false;
  
+ 	/* Zen and newer do not need an equivalence table. */
+ 	if (x86_family(bsp_cpuid_1_eax) >= 0x17)
+ 		return true;
+ 
  	cont_type = hdr[1];
  	if (cont_type != UCODE_EQUIV_CPU_TABLE_TYPE) {
 -		pr_debug("Wrong microcode container equivalence table type: %u.\n",
 -			 cont_type);
 +		if (!early)
 +			pr_debug("Wrong microcode container equivalence table type: %u.\n",
 +			       cont_type);
 +
  		return false;
  	}
  
@@@ -229,9 -319,9 +290,14 @@@ static unsigned int __verify_patch_size
   * positive: patch is not for this family, skip it
   * 0: success
   */
++<<<<<<< HEAD
 +static int
 +verify_patch(u8 family, const u8 *buf, size_t buf_size, u32 *patch_size, bool early)
++=======
+ static int verify_patch(const u8 *buf, size_t buf_size, u32 *patch_size)
++>>>>>>> 94838d230a6c (x86/microcode/AMD: Use the family,model,stepping encoded in the patch ID)
  {
+ 	u8 family = x86_family(bsp_cpuid_1_eax);
  	struct microcode_header_amd *mc_hdr;
  	unsigned int ret;
  	u32 sh_psize;
@@@ -259,10 -347,9 +325,10 @@@
  		return -1;
  	}
  
- 	ret = __verify_patch_size(family, sh_psize, buf_size);
+ 	ret = __verify_patch_size(sh_psize, buf_size);
  	if (!ret) {
 -		pr_debug("Per-family patch size mismatch.\n");
 +		if (!early)
 +			pr_debug("Per-family patch size mismatch.\n");
  		return -1;
  	}
  
@@@ -325,7 -420,7 +400,11 @@@ static size_t parse_container(u8 *ucode
  		u32 patch_size;
  		int ret;
  
++<<<<<<< HEAD
 +		ret = verify_patch(x86_family(desc->cpuid_1_eax), buf, size, &patch_size, true);
++=======
+ 		ret = verify_patch(buf, size, &patch_size);
++>>>>>>> 94838d230a6c (x86/microcode/AMD: Use the family,model,stepping encoded in the patch ID)
  		if (ret < 0) {
  			/*
  			 * Patch verification failed, skip to the next container, if
@@@ -413,21 -509,12 +493,28 @@@ static int __apply_microcode_amd(struc
   *
   * Returns true if container found (sets @desc), false otherwise.
   */
++<<<<<<< HEAD
 +static bool early_apply_microcode(u32 cpuid_1_eax, void *ucode, size_t size)
++=======
+ static bool early_apply_microcode(u32 old_rev, void *ucode, size_t size)
++>>>>>>> 94838d230a6c (x86/microcode/AMD: Use the family,model,stepping encoded in the patch ID)
  {
  	struct cont_desc desc = { 0 };
  	struct microcode_amd *mc;
 +	u32 rev, dummy, *new_rev;
  	bool ret = false;
  
++<<<<<<< HEAD
 +#ifdef CONFIG_X86_32
 +	new_rev = (u32 *)__pa_nodebug(&ucode_new_rev);
 +#else
 +	new_rev = &ucode_new_rev;
 +#endif
 +
 +	desc.cpuid_1_eax = cpuid_1_eax;
 +
++=======
++>>>>>>> 94838d230a6c (x86/microcode/AMD: Use the family,model,stepping encoded in the patch ID)
  	scan_containers(ucode, size, &desc);
  
  	mc = desc.mc;
@@@ -441,20 -526,16 +528,25 @@@
  	 * changes even if the revision of the other SMT thread is already
  	 * up-to-date.
  	 */
 -	if (old_rev > mc->hdr.patch_id)
 +	if (rev > mc->hdr.patch_id)
  		return ret;
  
 -	return !__apply_microcode_amd(mc);
 +	if (!__apply_microcode_amd(mc)) {
 +		*new_rev = mc->hdr.patch_id;
 +		ret      = true;
 +	}
 +
 +	return ret;
  }
  
++<<<<<<< HEAD
 +static bool get_builtin_microcode(struct cpio_data *cp, unsigned int family)
++=======
+ static bool get_builtin_microcode(struct cpio_data *cp)
++>>>>>>> 94838d230a6c (x86/microcode/AMD: Use the family,model,stepping encoded in the patch ID)
  {
  	char fw_name[36] = "amd-ucode/microcode_amd.bin";
+ 	u8 family = x86_family(bsp_cpuid_1_eax);
  	struct firmware fw;
  
  	if (IS_ENABLED(CONFIG_X86_32))
@@@ -473,46 -554,34 +565,71 @@@
  	return false;
  }
  
++<<<<<<< HEAD
 +static void find_blobs_in_containers(unsigned int cpuid_1_eax, struct cpio_data *ret)
++=======
+ static void __init find_blobs_in_containers(struct cpio_data *ret)
++>>>>>>> 94838d230a6c (x86/microcode/AMD: Use the family,model,stepping encoded in the patch ID)
  {
 +	struct ucode_cpu_info *uci;
  	struct cpio_data cp;
 +	const char *path;
 +	bool use_pa;
 +
 +	if (IS_ENABLED(CONFIG_X86_32)) {
 +		uci	= (struct ucode_cpu_info *)__pa_nodebug(ucode_cpu_info);
 +		path	= (const char *)__pa_nodebug(ucode_path);
 +		use_pa	= true;
 +	} else {
 +		uci     = ucode_cpu_info;
 +		path	= ucode_path;
 +		use_pa	= false;
 +	}
  
++<<<<<<< HEAD
 +	if (!get_builtin_microcode(&cp, x86_family(cpuid_1_eax)))
 +		cp = find_microcode_in_initrd(path, use_pa);
 +
 +	/* Needed in load_microcode_amd() */
 +	uci->cpu_sig.sig = cpuid_1_eax;
++=======
+ 	if (!get_builtin_microcode(&cp))
+ 		cp = find_microcode_in_initrd(ucode_path);
++>>>>>>> 94838d230a6c (x86/microcode/AMD: Use the family,model,stepping encoded in the patch ID)
  
  	*ret = cp;
  }
  
 -void __init load_ucode_amd_bsp(struct early_load_data *ed, unsigned int cpuid_1_eax)
 +static void apply_ucode_from_containers(unsigned int cpuid_1_eax)
  {
  	struct cpio_data cp = { };
++<<<<<<< HEAD
++=======
+ 	u32 dummy;
+ 
+ 	bsp_cpuid_1_eax = cpuid_1_eax;
  
- 	find_blobs_in_containers(cpuid_1_eax, &cp);
+ 	native_rdmsr(MSR_AMD64_PATCH_LEVEL, ed->old_rev, dummy);
+ 
+ 	/* Needed in load_microcode_amd() */
+ 	ucode_cpu_info[0].cpu_sig.sig = cpuid_1_eax;
++>>>>>>> 94838d230a6c (x86/microcode/AMD: Use the family,model,stepping encoded in the patch ID)
+ 
+ 	find_blobs_in_containers(&cp);
  	if (!(cp.data && cp.size))
  		return;
  
++<<<<<<< HEAD
 +	early_apply_microcode(cpuid_1_eax, cp.data, cp.size);
 +}
 +
 +void load_ucode_amd_early(unsigned int cpuid_1_eax)
 +{
 +	return apply_ucode_from_containers(cpuid_1_eax);
++=======
+ 	if (early_apply_microcode(ed->old_rev, cp.data, cp.size))
+ 		native_rdmsr(MSR_AMD64_PATCH_LEVEL, ed->new_rev, dummy);
++>>>>>>> 94838d230a6c (x86/microcode/AMD: Use the family,model,stepping encoded in the patch ID)
  }
  
  static enum ucode_state load_microcode_amd(u8 family, const u8 *data, size_t size);
@@@ -523,7 -594,10 +640,14 @@@ int __init save_microcode_in_initrd_amd
  	enum ucode_state ret;
  	struct cpio_data cp;
  
++<<<<<<< HEAD
 +	cp = find_microcode_in_initrd(ucode_path, false);
++=======
+ 	if (dis_ucode_ldr || c->x86_vendor != X86_VENDOR_AMD || c->x86 < 0x10)
+ 		return 0;
+ 
+ 	find_blobs_in_containers(&cp);
++>>>>>>> 94838d230a6c (x86/microcode/AMD: Use the family,model,stepping encoded in the patch ID)
  	if (!(cp.data && cp.size))
  		return -EINVAL;
  
@@@ -539,13 -611,25 +661,30 @@@
  
  	return 0;
  }
 -early_initcall(save_microcode_in_initrd);
 +
 +static u16 __find_equiv_id(unsigned int cpu)
 +{
 +	struct ucode_cpu_info *uci = ucode_cpu_info + cpu;
 +	return find_equiv_id(&equiv_table, uci->cpu_sig.sig);
 +}
  
+ static inline bool patch_cpus_equivalent(struct ucode_patch *p, struct ucode_patch *n)
+ {
+ 	/* Zen and newer hardcode the f/m/s in the patch ID */
+         if (x86_family(bsp_cpuid_1_eax) >= 0x17) {
+ 		union cpuid_1_eax p_cid = ucode_rev_to_cpuid(p->patch_id);
+ 		union cpuid_1_eax n_cid = ucode_rev_to_cpuid(n->patch_id);
+ 
+ 		/* Zap stepping */
+ 		p_cid.stepping = 0;
+ 		n_cid.stepping = 0;
+ 
+ 		return p_cid.full == n_cid.full;
+ 	} else {
+ 		return p->equiv_cpu == n->equiv_cpu;
+ 	}
+ }
+ 
  /*
   * a small, trivial cache of per-family ucode patches
   */
@@@ -595,13 -701,23 +756,31 @@@ static void free_cache(void
  
  static struct ucode_patch *find_patch(unsigned int cpu)
  {
++<<<<<<< HEAD
 +	u16 equiv_id;
 +
 +	equiv_id = __find_equiv_id(cpu);
 +	if (!equiv_id)
 +		return NULL;
++=======
+ 	struct ucode_cpu_info *uci = ucode_cpu_info + cpu;
+ 	u32 rev, dummy __always_unused;
+ 	u16 equiv_id;
+ 
+ 	/* fetch rev if not populated yet: */
+ 	if (!uci->cpu_sig.rev) {
+ 		rdmsr(MSR_AMD64_PATCH_LEVEL, rev, dummy);
+ 		uci->cpu_sig.rev = rev;
+ 	}
++>>>>>>> 94838d230a6c (x86/microcode/AMD: Use the family,model,stepping encoded in the patch ID)
+ 
+ 	if (x86_family(bsp_cpuid_1_eax) < 0x17) {
+ 		equiv_id = find_equiv_id(&equiv_table, uci->cpu_sig.sig);
+ 		if (!equiv_id)
+ 			return NULL;
+ 	}
  
- 	return cache_find_patch(equiv_id);
+ 	return cache_find_patch(uci, equiv_id);
  }
  
  void reload_ucode_amd(unsigned int cpu)
@@@ -749,7 -875,7 +936,11 @@@ static int verify_and_add_patch(u8 fami
  	u16 proc_id;
  	int ret;
  
++<<<<<<< HEAD
 +	ret = verify_patch(family, fw, leftover, patch_size, false);
++=======
+ 	ret = verify_patch(fw, leftover, patch_size);
++>>>>>>> 94838d230a6c (x86/microcode/AMD: Use the family,model,stepping encoded in the patch ID)
  	if (ret)
  		return ret;
  
* Unmerged path arch/x86/kernel/cpu/microcode/amd.c
