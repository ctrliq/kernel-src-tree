net/mlx5e: Release skb in case of failure in tc update skb

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-301.1.el8
commit-author Maor Dickman <maord@nvidia.com>
commit a34ffec8af8ff1c730697a99e09ec7b74a3423b6
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-301.1.el8/a34ffec8.failed

In case of failure in tc update skb the packet is dropped
without freeing the skb.

Fixed by freeing the skb in case failure in tc update skb.

Fixes: d6d27782864f ("net/mlx5: E-Switch, Restore chain id on miss")
Fixes: c75690972228 ("net/mlx5e: Add tc chains offload support for nic flows")
	Signed-off-by: Maor Dickman <maord@nvidia.com>
	Reviewed-by: Roi Dayan <roid@nvidia.com>
	Signed-off-by: Saeed Mahameed <saeedm@nvidia.com>
(cherry picked from commit a34ffec8af8ff1c730697a99e09ec7b74a3423b6)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/mellanox/mlx5/core/en_rx.c
diff --cc drivers/net/ethernet/mellanox/mlx5/core/en_rx.c
index 6cadb4feae48,ca4b55839a8a..000000000000
--- a/drivers/net/ethernet/mellanox/mlx5/core/en_rx.c
+++ b/drivers/net/ethernet/mellanox/mlx5/core/en_rx.c
@@@ -1253,6 -1260,13 +1253,16 @@@ static void mlx5e_handle_rx_cqe(struct 
  	}
  
  	mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
++<<<<<<< HEAD
++=======
+ 
+ 	if (mlx5e_cqe_regb_chain(cqe))
+ 		if (!mlx5e_tc_update_skb(cqe, skb)) {
+ 			dev_kfree_skb_any(skb);
+ 			goto free_wqe;
+ 		}
+ 
++>>>>>>> a34ffec8af8f (net/mlx5e: Release skb in case of failure in tc update skb)
  	napi_gro_receive(rq->cq.napi, skb);
  
  free_wqe:
@@@ -1514,6 -1532,13 +1528,16 @@@ static void mlx5e_handle_rx_cqe_mpwrq(s
  		goto mpwrq_cqe_out;
  
  	mlx5e_complete_rx_cqe(rq, cqe, cqe_bcnt, skb);
++<<<<<<< HEAD
++=======
+ 
+ 	if (mlx5e_cqe_regb_chain(cqe))
+ 		if (!mlx5e_tc_update_skb(cqe, skb)) {
+ 			dev_kfree_skb_any(skb);
+ 			goto mpwrq_cqe_out;
+ 		}
+ 
++>>>>>>> a34ffec8af8f (net/mlx5e: Release skb in case of failure in tc update skb)
  	napi_gro_receive(rq->cq.napi, skb);
  
  mpwrq_cqe_out:
* Unmerged path drivers/net/ethernet/mellanox/mlx5/core/en_rx.c
