ASoC: amd: acp: clean up some inconsistent indentings

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-536.el8
commit-author Yang Li <yang.lee@linux.alibaba.com>
commit 6d2a87ddd9c2488732ca422476a9417ca312f75a
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-536.el8/6d2a87dd.failed

sound/soc/amd/acp/acp-rembrandt.c:283 rmb_pcm_resume() warn: inconsistent indenting

	Reported-by: Abaci Robot <abaci@linux.alibaba.com>
Closes: https://bugzilla.openanolis.cn/show_bug.cgi?id=5863
	Signed-off-by: Yang Li <yang.lee@linux.alibaba.com>
Link: https://lore.kernel.org/r/20230713065106.21564-1-yang.lee@linux.alibaba.com
	Signed-off-by: Mark Brown <broonie@kernel.org>
(cherry picked from commit 6d2a87ddd9c2488732ca422476a9417ca312f75a)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	sound/soc/amd/acp/acp-rembrandt.c
diff --cc sound/soc/amd/acp/acp-rembrandt.c
index 76d4f1bcb565,21e67ed956d1..000000000000
--- a/sound/soc/amd/acp/acp-rembrandt.c
+++ b/sound/soc/amd/acp/acp-rembrandt.c
@@@ -247,14 -252,48 +247,50 @@@ static int rembrandt_audio_remove(struc
  
  	acp_disable_interrupts(adata);
  	acp_platform_unregister(dev);
 -	pm_runtime_disable(&pdev->dev);
 +	return 0;
  }
  
++<<<<<<< HEAD
++=======
+ static int __maybe_unused rmb_pcm_resume(struct device *dev)
+ {
+ 	struct acp_dev_data *adata = dev_get_drvdata(dev);
+ 	struct acp_stream *stream;
+ 	struct snd_pcm_substream *substream;
+ 	snd_pcm_uframes_t buf_in_frames;
+ 	u64 buf_size;
+ 
+ 	acp6x_master_clock_generate(dev);
+ 	spin_lock(&adata->acp_lock);
+ 	list_for_each_entry(stream, &adata->stream_list, list) {
+ 		if (stream) {
+ 			substream = stream->substream;
+ 			if (substream && substream->runtime) {
+ 				buf_in_frames = (substream->runtime->buffer_size);
+ 				buf_size = frames_to_bytes(substream->runtime, buf_in_frames);
+ 				config_pte_for_stream(adata, stream);
+ 				config_acp_dma(adata, stream, buf_size);
+ 				if (stream->dai_id)
+ 					restore_acp_i2s_params(substream, adata, stream);
+ 				else
+ 					restore_acp_pdm_params(substream, adata);
+ 			}
+ 		}
+ 	}
+ 	spin_unlock(&adata->acp_lock);
+ 	return 0;
+ }
+ 
+ static const struct dev_pm_ops rmb_dma_pm_ops = {
+ 	SET_SYSTEM_SLEEP_PM_OPS(NULL, rmb_pcm_resume)
+ };
+ 
++>>>>>>> 6d2a87ddd9c2 (ASoC: amd: acp: clean up some inconsistent indentings)
  static struct platform_driver rembrandt_driver = {
  	.probe = rembrandt_audio_probe,
 -	.remove_new = rembrandt_audio_remove,
 +	.remove = rembrandt_audio_remove,
  	.driver = {
  		.name = "acp_asoc_rembrandt",
 -		.pm = &rmb_dma_pm_ops,
  	},
  };
  
* Unmerged path sound/soc/amd/acp/acp-rembrandt.c
