cifs: call cifs_reconnect when a connection is marked

jira LE-4669
Rebuild_History Non-Buildable kernel-4.18.0-553.82.1.el8_10
commit-author Shyam Prasad N <sprasad@microsoft.com>
commit a81da65fbae6436e1e2f415532b8aacc3274d840
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-553.82.1.el8_10/a81da65f.failed

In cifsd thread, we should continue to call cifs_reconnect
whenever server->tcpStatus is marked as CifsNeedReconnect.
This was inexplicably removed by one of my recent commits.
Fixing that here.

Fixes: a05885ce13bd ("cifs: fix the connection state transitions with multichannel")
	Signed-off-by: Shyam Prasad N <sprasad@microsoft.com>
	Signed-off-by: Steve French <stfrench@microsoft.com>
(cherry picked from commit a81da65fbae6436e1e2f415532b8aacc3274d840)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/cifs/connect.c
diff --cc fs/cifs/connect.c
index 7aabd597027d,5b4733eb42c7..000000000000
--- a/fs/cifs/connect.c
+++ b/fs/cifs/connect.c
@@@ -613,13 -631,18 +613,18 @@@ cifs_readv_from_socket(struct TCP_Serve
  		else
  			length = sock_recvmsg(server->ssocket, smb_msg, 0);
  
 -		spin_lock(&cifs_tcp_ses_lock);
 -		if (server->tcpStatus == CifsExiting) {
 -			spin_unlock(&cifs_tcp_ses_lock);
 +		if (server->tcpStatus == CifsExiting)
  			return -ESHUTDOWN;
 -		}
  
  		if (server->tcpStatus == CifsNeedReconnect) {
++<<<<<<< HEAD
 +			cifs_reconnect(server);
++=======
+ 			spin_unlock(&cifs_tcp_ses_lock);
+ 			cifs_reconnect(server, false);
++>>>>>>> a81da65fbae6 (cifs: call cifs_reconnect when a connection is marked)
  			return -ECONNABORTED;
  		}
 -		spin_unlock(&cifs_tcp_ses_lock);
  
  		if (length == -ERESTARTSYS ||
  		    length == -EAGAIN ||
* Unmerged path fs/cifs/connect.c
