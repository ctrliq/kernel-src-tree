cifs: remove unused variable ses_selected

jira LE-4669
Rebuild_History Non-Buildable kernel-4.18.0-553.82.1.el8_10
commit-author Muhammad Usama Anjum <usama.anjum@collabora.com>
commit 8a409cda978e212661b8c032e1b08b3b0b0f9d36
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-553.82.1.el8_10/8a409cda.failed

ses_selected is being declared and set at several places. It is not
being used. Remove it.

	Signed-off-by: Muhammad Usama Anjum <usama.anjum@collabora.com>
	Signed-off-by: Steve French <stfrench@microsoft.com>
(cherry picked from commit 8a409cda978e212661b8c032e1b08b3b0b0f9d36)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/cifs/smb2pdu.c
diff --cc fs/cifs/smb2pdu.c
index fdd9bd57531e,202ddbafe939..000000000000
--- a/fs/cifs/smb2pdu.c
+++ b/fs/cifs/smb2pdu.c
@@@ -3794,22 -3790,30 +3794,35 @@@ void smb2_reconnect_server(struct work_
  {
  	struct TCP_Server_Info *server = container_of(work,
  					struct TCP_Server_Info, reconnect.work);
 -	struct TCP_Server_Info *pserver;
 -	struct cifs_ses *ses, *ses2;
 +	struct cifs_ses *ses;
  	struct cifs_tcon *tcon, *tcon2;
++<<<<<<< HEAD
 +	struct list_head tmp_list;
 +	int tcon_exist = false;
++=======
+ 	struct list_head tmp_list, tmp_ses_list;
+ 	bool tcon_exist = false, ses_exist = false;
+ 	bool tcon_selected = false;
++>>>>>>> 8a409cda978e (cifs: remove unused variable ses_selected)
  	int rc;
 -	bool resched = false;
 +	int resched = false;
  
 -	/* If server is a channel, select the primary channel */
 -	pserver = CIFS_SERVER_IS_CHAN(server) ? server->primary_server : server;
  
  	/* Prevent simultaneous reconnects that can corrupt tcon->rlist list */
 -	mutex_lock(&pserver->reconnect_mutex);
 +	mutex_lock(&server->reconnect_mutex);
  
  	INIT_LIST_HEAD(&tmp_list);
 -	INIT_LIST_HEAD(&tmp_ses_list);
 -	cifs_dbg(FYI, "Reconnecting tcons and channels\n");
 +	cifs_dbg(FYI, "Need negotiate, reconnecting tcons\n");
  
  	spin_lock(&cifs_tcp_ses_lock);
++<<<<<<< HEAD
 +	list_for_each_entry(ses, &server->smb_ses_list, smb_ses_list) {
++=======
+ 	list_for_each_entry(ses, &pserver->smb_ses_list, smb_ses_list) {
+ 
+ 		tcon_selected = false;
+ 
++>>>>>>> 8a409cda978e (cifs: remove unused variable ses_selected)
  		list_for_each_entry(tcon, &ses->tcon_list, tcon_list) {
  			if (tcon->need_reconnect || tcon->need_reopen_files) {
  				tcon->tc_count++;
@@@ -3823,9 -3827,21 +3836,24 @@@
  		 */
  		if (ses->tcon_ipc && ses->tcon_ipc->need_reconnect) {
  			list_add_tail(&ses->tcon_ipc->rlist, &tmp_list);
 -			tcon_selected = tcon_exist = true;
 +			tcon_exist = true;
  			ses->ses_count++;
  		}
++<<<<<<< HEAD
++=======
+ 		/*
+ 		 * handle the case where channel needs to reconnect
+ 		 * binding session, but tcon is healthy (some other channel
+ 		 * is active)
+ 		 */
+ 		spin_lock(&ses->chan_lock);
+ 		if (!tcon_selected && cifs_chan_needs_reconnect(ses, server)) {
+ 			list_add_tail(&ses->rlist, &tmp_ses_list);
+ 			ses_exist = true;
+ 			ses->ses_count++;
+ 		}
+ 		spin_unlock(&ses->chan_lock);
++>>>>>>> 8a409cda978e (cifs: remove unused variable ses_selected)
  	}
  	/*
  	 * Get the reference to server struct to be sure that the last call of
* Unmerged path fs/cifs/smb2pdu.c
