security: remove unneeded subdir-$(CONFIG_...)

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-425.3.1.el8
commit-author Masahiro Yamada <masahiroy@kernel.org>
commit e052826ff1a685a5c3a7033e907213de8433253e
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-425.3.1.el8/e052826f.failed

All of these are unneeded. The directories to descend are specified
by obj-$(CONFIG_...).

	Signed-off-by: Masahiro Yamada <masahiroy@kernel.org>
(cherry picked from commit e052826ff1a685a5c3a7033e907213de8433253e)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	security/Makefile
diff --cc security/Makefile
index 68882214c6ee,18121f8f85cd..000000000000
--- a/security/Makefile
+++ b/security/Makefile
@@@ -4,13 -4,6 +4,16 @@@
  #
  
  obj-$(CONFIG_KEYS)			+= keys/
++<<<<<<< HEAD
 +subdir-$(CONFIG_SECURITY_SELINUX)	+= selinux
 +subdir-$(CONFIG_SECURITY_SMACK)		+= smack
 +subdir-$(CONFIG_SECURITY_TOMOYO)        += tomoyo
 +subdir-$(CONFIG_SECURITY_APPARMOR)	+= apparmor
 +subdir-$(CONFIG_SECURITY_YAMA)		+= yama
 +subdir-$(CONFIG_SECURITY_LOADPIN)	+= loadpin
 +subdir-$(CONFIG_BPF_LSM)		+= bpf
++=======
++>>>>>>> e052826ff1a6 (security: remove unneeded subdir-$(CONFIG_...))
  
  # always enable default capabilities
  obj-y					+= commoncap.o
@@@ -26,12 -19,11 +29,11 @@@ obj-$(CONFIG_SECURITY_TOMOYO)		+= tomoy
  obj-$(CONFIG_SECURITY_APPARMOR)		+= apparmor/
  obj-$(CONFIG_SECURITY_YAMA)		+= yama/
  obj-$(CONFIG_SECURITY_LOADPIN)		+= loadpin/
 -obj-$(CONFIG_SECURITY_SAFESETID)       += safesetid/
 -obj-$(CONFIG_SECURITY_LOCKDOWN_LSM)	+= lockdown/
 -obj-$(CONFIG_CGROUPS)			+= device_cgroup.o
 +obj-$(CONFIG_CGROUP_DEVICE)		+= device_cgroup.o
  obj-$(CONFIG_BPF_LSM)			+= bpf/
 -obj-$(CONFIG_SECURITY_LANDLOCK)		+= landlock/
  
  # Object integrity file lists
- subdir-$(CONFIG_INTEGRITY)		+= integrity
  obj-$(CONFIG_INTEGRITY)			+= integrity/
 +
 +# Allow the kernel to be locked down
 +obj-$(CONFIG_LOCK_DOWN_KERNEL)		+= lock_down.o
* Unmerged path security/Makefile
