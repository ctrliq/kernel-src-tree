netfilter: conntrack: remove pr_debug callsites from tcp tracker

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-425.3.1.el8
commit-author Florian Westphal <fw@strlen.de>
commit f74360d3440ccf3bb10178f2805498c835c3ed5d
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-425.3.1.el8/f74360d3.failed

They are either obsolete or useless.

Those in the normal processing path cannot be enabled on a production
system; they generate too much noise.

One pr_debug call resides in an error path and does provide useful info,
merge it with the existing nf_log_invalid().

	Signed-off-by: Florian Westphal <fw@strlen.de>
	Signed-off-by: Pablo Neira Ayuso <pablo@netfilter.org>
(cherry picked from commit f74360d3440ccf3bb10178f2805498c835c3ed5d)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/netfilter/nf_conntrack_proto_tcp.c
diff --cc net/netfilter/nf_conntrack_proto_tcp.c
index f9bb6bbd54e2,a63b51dceaf2..000000000000
--- a/net/netfilter/nf_conntrack_proto_tcp.c
+++ b/net/netfilter/nf_conntrack_proto_tcp.c
@@@ -1032,10 -987,11 +987,15 @@@ int nf_conntrack_tcp_packet(struct nf_c
  		}
  
  		/* Invalid packet */
- 		pr_debug("nf_ct_tcp: Invalid dir=%i index=%u ostate=%u\n",
- 			 dir, get_conntrack_index(th), old_state);
  		spin_unlock_bh(&ct->lock);
++<<<<<<< HEAD
 +		nf_ct_l4proto_log_invalid(skb, ct, "invalid state");
++=======
+ 		nf_ct_l4proto_log_invalid(skb, ct, state,
+ 					  "packet (index %d) in dir %d invalid, state %s",
+ 					  index, dir,
+ 					  tcp_conntrack_names[old_state]);
++>>>>>>> f74360d3440c (netfilter: conntrack: remove pr_debug callsites from tcp tracker)
  		return -NF_ACCEPT;
  	case TCP_CONNTRACK_TIME_WAIT:
  		/* RFC5961 compliance cause stack to send "challenge-ACK"
* Unmerged path net/netfilter/nf_conntrack_proto_tcp.c
