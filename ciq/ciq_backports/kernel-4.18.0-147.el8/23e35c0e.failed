drm/cma-helper: Fix drm_gem_cma_free_object()

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-147.el8
commit-author Noralf Trønnes <noralf@tronnes.org>
commit 23e35c0eb81a23d40c079a7eb187fc321fa1deb7
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-147.el8/23e35c0e.failed

The logic for freeing an imported buffer with a virtual address is
broken. It will free the buffer instead of unmapping the dma buf.
Fix by reversing the if ladder and first check if the buffer is imported.

Fixes: b9068cde51ee ("drm/cma-helper: Add DRM_GEM_CMA_VMAP_DRIVER_OPS")
	Cc: stable@vger.kernel.org
	Reported-by: "Li, Tingqian" <tingqian.li@intel.com>
	Signed-off-by: Noralf Trønnes <noralf@tronnes.org>
	Reviewed-by: Oleksandr Andrushchenko <oleksandr_andrushchenko@epam.com>
Link: https://patchwork.freedesktop.org/patch/msgid/20190426124753.53722-1-noralf@tronnes.org
(cherry picked from commit 23e35c0eb81a23d40c079a7eb187fc321fa1deb7)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/gpu/drm/drm_gem_cma_helper.c
diff --cc drivers/gpu/drm/drm_gem_cma_helper.c
index 80a5115c3846,e01ceed09e67..000000000000
--- a/drivers/gpu/drm/drm_gem_cma_helper.c
+++ b/drivers/gpu/drm/drm_gem_cma_helper.c
@@@ -185,11 -186,13 +185,20 @@@ void drm_gem_cma_free_object(struct drm
  
  	cma_obj = to_drm_gem_cma_obj(gem_obj);
  
++<<<<<<< HEAD
 +	if (cma_obj->vaddr) {
 +		dma_free_wc(gem_obj->dev->dev, cma_obj->base.size,
 +			    cma_obj->vaddr, cma_obj->paddr);
 +	} else if (gem_obj->import_attach) {
++=======
+ 	if (gem_obj->import_attach) {
+ 		if (cma_obj->vaddr)
+ 			dma_buf_vunmap(gem_obj->import_attach->dmabuf, cma_obj->vaddr);
++>>>>>>> 23e35c0eb81a (drm/cma-helper: Fix drm_gem_cma_free_object())
  		drm_prime_gem_destroy(gem_obj, cma_obj->sgt);
+ 	} else if (cma_obj->vaddr) {
+ 		dma_free_wc(gem_obj->dev->dev, cma_obj->base.size,
+ 			    cma_obj->vaddr, cma_obj->paddr);
  	}
  
  	drm_gem_object_release(gem_obj);
* Unmerged path drivers/gpu/drm/drm_gem_cma_helper.c
