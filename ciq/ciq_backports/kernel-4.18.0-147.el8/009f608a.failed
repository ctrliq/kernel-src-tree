signal/arm64: Remove arm64_force_sig_info

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-147.el8
commit-author Eric W. Biederman <ebiederm@xmission.com>
commit 009f608ab20a25d01a07e9e75e7d246e81252eb8
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-147.el8/009f608a.failed

The function has no more callers so remove it.

	Reviewed-by: Catalin Marinas <catalin.marinas@arm.com>
	Tested-by: Catalin Marinas <catalin.marinas@arm.com>
	Signed-off-by: "Eric W. Biederman" <ebiederm@xmission.com>
(cherry picked from commit 009f608ab20a25d01a07e9e75e7d246e81252eb8)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	arch/arm64/include/asm/traps.h
#	arch/arm64/kernel/traps.c
diff --cc arch/arm64/include/asm/traps.h
index 08e99901edbc,d32b8bd440af..000000000000
--- a/arch/arm64/include/asm/traps.h
+++ b/arch/arm64/include/asm/traps.h
@@@ -38,7 -38,7 +38,11 @@@ void unregister_undef_hook(struct undef
  void force_signal_inject(int signal, int code, unsigned long address);
  void arm64_notify_segfault(unsigned long addr);
  void arm64_force_sig_fault(int signo, int code, void __user *addr, const char *str);
++<<<<<<< HEAD
 +void arm64_force_sig_info(struct siginfo *info, const char *str);
++=======
+ void arm64_force_sig_mceerr(int code, void __user *addr, short lsb, const char *str);
++>>>>>>> 009f608ab20a (signal/arm64: Remove arm64_force_sig_info)
  
  /*
   * Move regs->pc to next instruction and do necessary setup before it
diff --cc arch/arm64/kernel/traps.c
index 4efd47b623f4,de67818258cd..000000000000
--- a/arch/arm64/kernel/traps.c
+++ b/arch/arm64/kernel/traps.c
@@@ -255,10 -255,11 +255,18 @@@ void arm64_force_sig_fault(int signo, i
  	force_sig_fault(signo, code, addr, current);
  }
  
++<<<<<<< HEAD
 +void arm64_force_sig_info(struct siginfo *info, const char *str)
 +{
 +	arm64_show_signal(info->si_signo, str);
 +	force_sig_info(info->si_signo, info, current);
++=======
+ void arm64_force_sig_mceerr(int code, void __user *addr, short lsb,
+ 			    const char *str)
+ {
+ 	arm64_show_signal(SIGBUS, str);
+ 	force_sig_mceerr(code, addr, lsb, current);
++>>>>>>> 009f608ab20a (signal/arm64: Remove arm64_force_sig_info)
  }
  
  void arm64_notify_die(const char *str, struct pt_regs *regs,
* Unmerged path arch/arm64/include/asm/traps.h
* Unmerged path arch/arm64/kernel/traps.c
