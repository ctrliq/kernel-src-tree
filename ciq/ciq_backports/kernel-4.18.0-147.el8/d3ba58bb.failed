ALSA: hda/realtek - Support low power consumption for ALC295

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-147.el8
commit-author Kailang Yang <kailang@realtek.com>
commit d3ba58bb895915f7f9105e0844441d2ca7d83340
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-147.el8/d3ba58bb.failed

Enter to close more power control widgets at suspend.
Remove hp_pin check. Add the default pin 0x21 as headphone.
Supported low power consumption, it must do depop procedure when
headset jack was plugged or unplugged.
So, alc225_init() and alc225_shutup() must run delay when headset
jack was plugged or unplugged.
If depop procedure not run with delay, it will have a chance to let
power consumption raise high.

[ A few compile fixes by tiwai ]

Fixes: 8983eb602af5 ("ALSA: hda/realtek - Move to ACT_INIT state")
	Signed-off-by: Kailang Yang <kailang@realtek.com>
	Signed-off-by: Takashi Iwai <tiwai@suse.de>
(cherry picked from commit d3ba58bb895915f7f9105e0844441d2ca7d83340)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	sound/pci/hda/patch_realtek.c
diff --cc sound/pci/hda/patch_realtek.c
index 686e0ed48813,e733d323f1a7..000000000000
--- a/sound/pci/hda/patch_realtek.c
+++ b/sound/pci/hda/patch_realtek.c
@@@ -5526,9 -5537,13 +5537,19 @@@ static void alc_fixup_headset_jack(stru
  static void alc295_fixup_chromebook(struct hda_codec *codec,
  				    const struct hda_fixup *fix, int action)
  {
++<<<<<<< HEAD
++
++	switch (action) {
++	case HDA_FIXUP_ACT_PRE_PROBE:
++=======
+ 	struct alc_spec *spec = codec->spec;
  
  	switch (action) {
  	case HDA_FIXUP_ACT_PRE_PROBE:
+ 		spec->ultra_low_power = true;
+ 		break;
+ 	case HDA_FIXUP_ACT_INIT:
++>>>>>>> d3ba58bb8959 (ALSA: hda/realtek - Support low power consumption for ALC295)
  		switch (codec->core.vendor_id) {
  		case 0x10ec0295:
  			alc_update_coef_idx(codec, 0x4a, 0x8000, 1 << 15); /* Reset HP JD */
* Unmerged path sound/pci/hda/patch_realtek.c
