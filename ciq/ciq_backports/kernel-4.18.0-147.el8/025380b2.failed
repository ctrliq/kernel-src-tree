net/mlx5e: Use single argument for the esw representor build params helper

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-147.el8
commit-author Or Gerlitz <ogerlitz@mellanox.com>
commit 025380b20dc2a3b92a36394799157a63a2fe5814
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-147.el8/025380b2.failed

This is prep step towards adding dedicated uplink representor.

The patch doesn't change any functionality.

	Signed-off-by: Or Gerlitz <ogerlitz@mellanox.com>
	Signed-off-by: Saeed Mahameed <saeedm@mellanox.com>
(cherry picked from commit 025380b20dc2a3b92a36394799157a63a2fe5814)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/mellanox/mlx5/core/en_rep.c
diff --cc drivers/net/ethernet/mellanox/mlx5/core/en_rep.c
index 58cddf0a96a7,996a03de9171..000000000000
--- a/drivers/net/ethernet/mellanox/mlx5/core/en_rep.c
+++ b/drivers/net/ethernet/mellanox/mlx5/core/en_rep.c
@@@ -1091,9 -1205,12 +1091,17 @@@ static const struct net_device_ops mlx5
  	.ndo_change_mtu          = mlx5e_change_rep_mtu,
  };
  
++<<<<<<< HEAD
 +static void mlx5e_build_rep_params(struct mlx5_core_dev *mdev,
 +				   struct mlx5e_params *params, u16 mtu)
++=======
+ static void mlx5e_build_rep_params(struct net_device *netdev)
++>>>>>>> 025380b20dc2 (net/mlx5e: Use single argument for the esw representor build params helper)
  {
+ 	struct mlx5e_priv *priv = netdev_priv(netdev);
+ 	struct mlx5_core_dev *mdev = priv->mdev;
+ 	struct mlx5e_params *params;
+ 
  	u8 cq_period_mode = MLX5_CAP_GEN(mdev, cq_period_start_from_cqe) ?
  					 MLX5_CQ_PERIOD_MODE_START_FROM_CQE :
  					 MLX5_CQ_PERIOD_MODE_START_FROM_EQE;
@@@ -1112,6 -1230,9 +1121,12 @@@
  	params->num_tc                = 1;
  
  	mlx5_query_min_inline(mdev, &params->tx_min_inline_mode);
++<<<<<<< HEAD
++=======
+ 
+ 	/* RSS */
+ 	mlx5e_build_rss_params(&priv->rss_params, params->num_channels);
++>>>>>>> 025380b20dc2 (net/mlx5e: Use single argument for the esw representor build params helper)
  }
  
  static void mlx5e_build_rep_netdev(struct net_device *netdev)
@@@ -1160,9 -1281,11 +1175,16 @@@ static int mlx5e_init_rep(struct mlx5_c
  	if (err)
  		return err;
  
 +	priv->channels.params.num_channels = MLX5E_REP_PARAMS_DEF_NUM_CHANNELS;
  
++<<<<<<< HEAD
 +	mlx5e_build_rep_params(mdev, &priv->channels.params, netdev->mtu);
++=======
+ 	priv->channels.params.num_channels =
+ 				mlx5e_get_netdev_max_channels(netdev);
+ 
+ 	mlx5e_build_rep_params(netdev);
++>>>>>>> 025380b20dc2 (net/mlx5e: Use single argument for the esw representor build params helper)
  	mlx5e_build_rep_netdev(netdev);
  
  	mlx5e_timestamp_init(priv);
* Unmerged path drivers/net/ethernet/mellanox/mlx5/core/en_rep.c
