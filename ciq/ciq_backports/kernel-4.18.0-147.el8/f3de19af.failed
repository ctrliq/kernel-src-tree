net/tls: remove unused function tls_sw_sendpage_locked

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-147.el8
Rebuild_CHGLOG: - [net] tls: remove unused function tls_sw_sendpage_locked (Sabrina Dubroca) [1711821]
Rebuild_FUZZ: 96.15%
commit-author YueHaibing <yuehaibing@huawei.com>
commit f3de19af0f5b18dcbb6d445db02120656ebf9cfb
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-147.el8/f3de19af.failed

There are no in-tree callers.

	Signed-off-by: YueHaibing <yuehaibing@huawei.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit f3de19af0f5b18dcbb6d445db02120656ebf9cfb)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/tls/tls_sw.c
diff --cc net/tls/tls_sw.c
index 3f443983a6b3,c4755388ed03..000000000000
--- a/net/tls/tls_sw.c
+++ b/net/tls/tls_sw.c
@@@ -1042,11 -1139,21 +1042,25 @@@ wait_for_memory
  		}
  	}
  sendpage_end:
 -	ret = sk_stream_error(sk, flags, ret);
 -	return copied ? copied : ret;
 -}
 +	if (orig_size > size)
 +		ret = orig_size - size;
 +	else
 +		ret = sk_stream_error(sk, flags, ret);
  
++<<<<<<< HEAD
++=======
+ int tls_sw_sendpage(struct sock *sk, struct page *page,
+ 		    int offset, size_t size, int flags)
+ {
+ 	int ret;
+ 
+ 	if (flags & ~(MSG_MORE | MSG_DONTWAIT | MSG_NOSIGNAL |
+ 		      MSG_SENDPAGE_NOTLAST | MSG_SENDPAGE_NOPOLICY))
+ 		return -ENOTSUPP;
+ 
+ 	lock_sock(sk);
+ 	ret = tls_sw_do_sendpage(sk, page, offset, size, flags);
++>>>>>>> f3de19af0f5b (net/tls: remove unused function tls_sw_sendpage_locked)
  	release_sock(sk);
  	return ret;
  }
* Unmerged path net/tls/tls_sw.c
