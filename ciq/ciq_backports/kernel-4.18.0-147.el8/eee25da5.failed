crypto: sparc - Forbid 2-key 3DES in FIPS mode

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-147.el8
commit-author Herbert Xu <herbert@gondor.apana.org.au>
commit eee25da50baaa32790dbe82d60754936ceb82042
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-147.el8/eee25da5.failed

This patch forbids the use of 2-key 3DES (K1 == K3) in FIPS mode.

	Signed-off-by: Herbert Xu <herbert@gondor.apana.org.au>
(cherry picked from commit eee25da50baaa32790dbe82d60754936ceb82042)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	arch/sparc/crypto/des_glue.c
diff --cc arch/sparc/crypto/des_glue.c
index 61af794aa2d3,453a4cf5492a..000000000000
--- a/arch/sparc/crypto/des_glue.c
+++ b/arch/sparc/crypto/des_glue.c
@@@ -206,13 -205,11 +205,20 @@@ static int des3_ede_set_key(struct cryp
  	u64 k1[DES_EXPKEY_WORDS / 2];
  	u64 k2[DES_EXPKEY_WORDS / 2];
  	u64 k3[DES_EXPKEY_WORDS / 2];
+ 	int err;
  
++<<<<<<< HEAD
 +	if (unlikely(!((K[0] ^ K[2]) | (K[1] ^ K[3])) ||
 +		     !((K[2] ^ K[4]) | (K[3] ^ K[5]))) &&
 +		     (*flags & CRYPTO_TFM_REQ_WEAK_KEY)) {
 +		*flags |= CRYPTO_TFM_RES_WEAK_KEY;
 +		return -EINVAL;
 +	}
++=======
+ 	err = __des3_verify_key(flags, key);
+ 	if (unlikely(err))
+ 		return err;
++>>>>>>> eee25da50baa (crypto: sparc - Forbid 2-key 3DES in FIPS mode)
  
  	des_sparc64_key_expand((const u32 *)key, k1);
  	key += DES_KEY_SIZE;
* Unmerged path arch/sparc/crypto/des_glue.c
