drm: Expose "FB_DAMAGE_CLIPS" property to atomic aware user-space only

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-147.el8
commit-author Deepak Rawat <drawat@vmware.com>
commit c8f005684c98f4d9942baec13ad98054dbf312a0
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-147.el8/c8f00568.failed

Plane property "FB_DAMAGE_CLIPS" can only be used by atomic aware
user-space, so no point exposing it otherwise.

	Cc: <stable@vger.kernel.org>
	Signed-off-by: Deepak Rawat <drawat@vmware.com>
	Reviewed-by: Daniel Vetter <daniel.vetter@ffwll.ch>
Fixes: d3b21767821e ("drm: Add a new plane property to send damage during plane update")
Link: https://patchwork.freedesktop.org/patch/msgid/20190415172814.9840-1-drawat@vmware.com
(cherry picked from commit c8f005684c98f4d9942baec13ad98054dbf312a0)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/gpu/drm/drm_mode_config.c
diff --cc drivers/gpu/drm/drm_mode_config.c
index e5c653357024,1a346ae1599d..000000000000
--- a/drivers/gpu/drm/drm_mode_config.c
+++ b/drivers/gpu/drm/drm_mode_config.c
@@@ -293,6 -297,13 +293,16 @@@ static int drm_mode_create_standard_pro
  		return -ENOMEM;
  	dev->mode_config.prop_crtc_id = prop;
  
++<<<<<<< HEAD
++=======
+ 	prop = drm_property_create(dev,
+ 			DRM_MODE_PROP_ATOMIC | DRM_MODE_PROP_BLOB,
+ 			"FB_DAMAGE_CLIPS", 0);
+ 	if (!prop)
+ 		return -ENOMEM;
+ 	dev->mode_config.prop_fb_damage_clips = prop;
+ 
++>>>>>>> c8f005684c98 (drm: Expose "FB_DAMAGE_CLIPS" property to atomic aware user-space only)
  	prop = drm_property_create_bool(dev, DRM_MODE_PROP_ATOMIC,
  			"ACTIVE");
  	if (!prop)
* Unmerged path drivers/gpu/drm/drm_mode_config.c
