Fix access_ok() fallout for sparc32 and powerpc

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-147.el8
commit-author Linus Torvalds <torvalds@linux-foundation.org>
commit 4caf4ebfe4cf0ea262eb9e829bb254a6a6d58acc
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-147.el8/4caf4ebf.failed

These two architectures actually had an intentional use of the 'type'
argument to access_ok() just to avoid warnings.

I had actually noticed the powerpc one, but forgot to then fix it up.
And I missed the sparc32 case entirely.

This is hopefully all of it.

	Reported-by: Mathieu Malaterre <malat@debian.org>
	Reported-by: Guenter Roeck <linux@roeck-us.net>
Fixes: 96d4f267e40f ("Remove 'type' argument from access_ok() function")
	Signed-off-by: Linus Torvalds <torvalds@linux-foundation.org>
(cherry picked from commit 4caf4ebfe4cf0ea262eb9e829bb254a6a6d58acc)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	arch/powerpc/include/asm/uaccess.h
#	arch/sparc/include/asm/uaccess_32.h
diff --cc arch/powerpc/include/asm/uaccess.h
index 95430c1f5d78,e3a731793ea2..000000000000
--- a/arch/powerpc/include/asm/uaccess.h
+++ b/arch/powerpc/include/asm/uaccess.h
@@@ -63,8 -62,8 +63,13 @@@ static inline int __access_ok(unsigned 
  
  #endif
  
++<<<<<<< HEAD
 +#define access_ok(type, addr, size)		\
 +	(__chk_user_ptr(addr), (void)(type),		\
++=======
+ #define access_ok(addr, size)		\
+ 	(__chk_user_ptr(addr),		\
++>>>>>>> 4caf4ebfe4cf (Fix access_ok() fallout for sparc32 and powerpc)
  	 __access_ok((__force unsigned long)(addr), (size), get_fs()))
  
  /*
diff --cc arch/sparc/include/asm/uaccess_32.h
index de71c65b99f0,5153798051fb..000000000000
--- a/arch/sparc/include/asm/uaccess_32.h
+++ b/arch/sparc/include/asm/uaccess_32.h
@@@ -39,8 -39,7 +39,12 @@@
  #define __user_ok(addr, size) ({ (void)(size); (addr) < STACK_TOP; })
  #define __kernel_ok (uaccess_kernel())
  #define __access_ok(addr, size) (__user_ok((addr) & get_fs().seg, (size)))
++<<<<<<< HEAD
 +#define access_ok(type, addr, size) \
 +	({ (void)(type); __access_ok((unsigned long)(addr), size); })
++=======
+ #define access_ok(addr, size) __access_ok((unsigned long)(addr), size)
++>>>>>>> 4caf4ebfe4cf (Fix access_ok() fallout for sparc32 and powerpc)
  
  /*
   * The exception table consists of pairs of addresses: the first is the
* Unmerged path arch/powerpc/include/asm/uaccess.h
* Unmerged path arch/sparc/include/asm/uaccess_32.h
