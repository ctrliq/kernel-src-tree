nvme: cache controller attributes

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-147.el8
commit-author Sagi Grimberg <sagi@grimberg.me>
commit 3e53ba38a9404434a8b57683825279f4305b5a76
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-147.el8/3e53ba38.failed

We get the controller attributes in identify, cache them as we'll need
them for traffic based keep alive support.

	Reviewed-by: Chaitanya Kulkarni <chaitanya.kulkarni@wdc.com>
	Signed-off-by: Sagi Grimberg <sagi@grimberg.me>
	Signed-off-by: Christoph Hellwig <hch@lst.de>
	Signed-off-by: Jens Axboe <axboe@kernel.dk>
(cherry picked from commit 3e53ba38a9404434a8b57683825279f4305b5a76)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/nvme/host/core.c
diff --cc drivers/nvme/host/core.c
index 49d67447cdcc,9de6244a345c..000000000000
--- a/drivers/nvme/host/core.c
+++ b/drivers/nvme/host/core.c
@@@ -2419,6 -2408,8 +2419,11 @@@ int nvme_init_identify(struct nvme_ctr
  	nvme_set_queue_limits(ctrl, ctrl->admin_q);
  	ctrl->sgls = le32_to_cpu(id->sgls);
  	ctrl->kas = le16_to_cpu(id->kas);
++<<<<<<< HEAD
++=======
+ 	ctrl->max_namespaces = le32_to_cpu(id->mnan);
+ 	ctrl->ctratt = le32_to_cpu(id->ctratt);
++>>>>>>> 3e53ba38a940 (nvme: cache controller attributes)
  
  	if (id->rtd3e) {
  		/* us -> s */
* Unmerged path drivers/nvme/host/core.c
diff --git a/drivers/nvme/host/nvme.h b/drivers/nvme/host/nvme.h
index 5e9fbf4bca79..002a215623bf 100644
--- a/drivers/nvme/host/nvme.h
+++ b/drivers/nvme/host/nvme.h
@@ -193,6 +193,7 @@ struct nvme_ctrl {
 	u8 apsta;
 	u32 oaes;
 	u32 aen_result;
+	u32 ctratt;
 	unsigned int shutdown_timeout;
 	unsigned int kato;
 	bool subsystem;
