ipv6: add inet6_fill_args

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-147.el8
commit-author Christian Brauner <christian@brauner.io>
commit 203651b665f72ba765fc4721842a471f8e9e5739
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-147.el8/203651b6.failed

inet6_fill_if{addr,mcaddr, acaddr}() already took 6 arguments which
meant the 7th argument would need to be pushed onto the stack on x86.
Add a new struct inet6_fill_args which holds common information passed
to inet6_fill_if{addr,mcaddr, acaddr}() and shortens the functions to
three pointer arguments.

	Signed-off-by: Christian Brauner <christian@brauner.io>
	Cc: Kirill Tkhai <ktkhai@virtuozzo.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 203651b665f72ba765fc4721842a471f8e9e5739)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/ipv6/addrconf.c
diff --cc net/ipv6/addrconf.c
index 75a2cc3a2edb,d4733160e6b7..000000000000
--- a/net/ipv6/addrconf.c
+++ b/net/ipv6/addrconf.c
@@@ -4950,13 -4961,9 +4968,19 @@@ static int in6_dump_addrs(struct inet6_
  
  		/* unicast address incl. temp addr */
  		list_for_each_entry(ifa, &idev->addr_list, if_list) {
++<<<<<<< HEAD
 +			if (ip_idx < s_ip_idx)
 +				goto next;
 +			err = inet6_fill_ifaddr(skb, ifa,
 +						NETLINK_CB(cb->skb).portid,
 +						cb->nlh->nlmsg_seq,
 +						RTM_NEWADDR,
 +						NLM_F_MULTI, netnsid);
++=======
+ 			if (++ip_idx < s_ip_idx)
+ 				continue;
+ 			err = inet6_fill_ifaddr(skb, ifa, &fillargs);
++>>>>>>> 203651b665f7 (ipv6: add inet6_fill_args)
  			if (err < 0)
  				break;
  			nl_dump_check_consistent(cb, nlmsg_hdr(skb));
* Unmerged path net/ipv6/addrconf.c
