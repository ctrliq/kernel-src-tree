dma-buf: Remove leftover [un]map_atomic comments

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-147.el8
commit-author Andrew F. Davis <afd@ti.com>
commit 67b886d290052dbf2bcfc876a5ae41a5fe461edf
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-147.el8/67b886d2.failed

The map_atomic/unmap_atomic callbacks have been removed, remove
the related comments.

Fixes: f664a5269542 ("dma-buf: remove kmap_atomic interface")
	Signed-off-by: Andrew F. Davis <afd@ti.com>
	Signed-off-by: Sumit Semwal <sumit.semwal@linaro.org>
Link: https://patchwork.freedesktop.org/patch/msgid/20190321200957.16938-1-afd@ti.com
(cherry picked from commit 67b886d290052dbf2bcfc876a5ae41a5fe461edf)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	include/linux/dma-buf.h
diff --cc include/linux/dma-buf.h
index bee40907be84,e4a8dab2bc54..000000000000
--- a/include/linux/dma-buf.h
+++ b/include/linux/dma-buf.h
@@@ -39,12 -39,7 +39,16 @@@ struct dma_buf_attachment
  
  /**
   * struct dma_buf_ops - operations possible on struct dma_buf
++<<<<<<< HEAD
 + * @map_atomic: maps a page from the buffer into kernel address
 + *		space, users may not block until the subsequent unmap call.
 + *		This callback must not sleep.
 + * @unmap_atomic: [optional] unmaps a atomically mapped page from the buffer.
 + *		  This Callback must not sleep.
 + * @map: maps a page from the buffer into kernel address space.
++=======
+  * @map: [optional] maps a page from the buffer into kernel address space.
++>>>>>>> 67b886d29005 (dma-buf: Remove leftover [un]map_atomic comments)
   * @unmap: [optional] unmaps a page from the buffer.
   * @vmap: [optional] creates a virtual mapping for the buffer into kernel
   *	  address space. Same restrictions as for vmap and friends apply.
* Unmerged path include/linux/dma-buf.h
