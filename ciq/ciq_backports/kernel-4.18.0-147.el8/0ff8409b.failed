nfp: flower: remove double new line

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-147.el8
commit-author Jakub Kicinski <jakub.kicinski@netronome.com>
commit 0ff8409b521b7b315bd69c4f1fbff024dc6219f6
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-147.el8/0ff8409b.failed

Recent cls_flower offload rewrite added a double new line.

	Signed-off-by: Jakub Kicinski <jakub.kicinski@netronome.com>
	Reviewed-by: Quentin Monnet <quentin.monnet@netronome.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 0ff8409b521b7b315bd69c4f1fbff024dc6219f6)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/netronome/nfp/flower/offload.c
diff --cc drivers/net/ethernet/netronome/nfp/flower/offload.c
index a689d0c87ffa,450d7296fd57..000000000000
--- a/drivers/net/ethernet/netronome/nfp/flower/offload.c
+++ b/drivers/net/ethernet/netronome/nfp/flower/offload.c
@@@ -190,33 -183,18 +190,38 @@@ nfp_flower_calculate_key_layers(struct 
  			return -EOPNOTSUPP;
  
  		/* These fields are already verified as used. */
 -		flow_rule_match_enc_ipv4_addrs(rule, &ipv4_addrs);
 -		if (ipv4_addrs.mask->dst != cpu_to_be32(~0))
 +		mask_ipv4 =
 +			skb_flow_dissector_target(flow->dissector,
 +						  FLOW_DISSECTOR_KEY_ENC_IPV4_ADDRS,
 +						  flow->mask);
 +		if (mask_ipv4->dst != cpu_to_be32(~0))
  			return -EOPNOTSUPP;
  
++<<<<<<< HEAD
 +		mask_enc_ports =
 +			skb_flow_dissector_target(flow->dissector,
 +						  FLOW_DISSECTOR_KEY_ENC_PORTS,
 +						  flow->mask);
 +		enc_ports =
 +			skb_flow_dissector_target(flow->dissector,
 +						  FLOW_DISSECTOR_KEY_ENC_PORTS,
 +						  flow->key);
 +
 +		if (mask_enc_ports->dst != cpu_to_be16(~0))
++=======
+ 		flow_rule_match_enc_ports(rule, &enc_ports);
+ 		if (enc_ports.mask->dst != cpu_to_be16(~0))
++>>>>>>> 0ff8409b521b (nfp: flower: remove double new line)
  			return -EOPNOTSUPP;
  
 -		if (flow_rule_match_key(rule, FLOW_DISSECTOR_KEY_ENC_OPTS))
 -			flow_rule_match_enc_opts(rule, &enc_op);
 +		if (dissector_uses_key(flow->dissector,
 +				       FLOW_DISSECTOR_KEY_ENC_OPTS)) {
 +			enc_op = skb_flow_dissector_target(flow->dissector,
 +							   FLOW_DISSECTOR_KEY_ENC_OPTS,
 +							   flow->key);
 +		}
  
 -		switch (enc_ports.key->dst) {
 +		switch (enc_ports->dst) {
  		case htons(NFP_FL_VXLAN_PORT):
  			*tun_type = NFP_FL_TUNNEL_VXLAN;
  			key_layer |= NFP_FLOWER_LAYER_VXLAN;
* Unmerged path drivers/net/ethernet/netronome/nfp/flower/offload.c
