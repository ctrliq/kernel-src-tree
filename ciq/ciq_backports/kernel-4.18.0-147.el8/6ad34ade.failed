drm/amd/display: CS_TFM_1D only applied post EOTF

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-147.el8
commit-author Krunoslav Kovac <Krunoslav.Kovac@amd.com>
commit 6ad34adeaec5b56a5ba90e90099cabf1c1fe9dd2
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-147.el8/6ad34ade.failed

[Why]
There's some unnecessary mem allocation for CS_TFM_ID. What's worse, it
depends on LUT size and since it's 4K for CS_TFM_1D, it is 16x bigger
than in regular case when it's actually needed. This leads to some
crashes in stress conditions.

[How]
Skip ramp combining designed for RGB256 and DXGI gamma with CS_TFM_1D.

	Signed-off-by: Krunoslav Kovac <Krunoslav.Kovac@amd.com>
	Reviewed-by: Aric Cyr <Aric.Cyr@amd.com>
	Acked-by: Leo Li <sunpeng.li@amd.com>
	Signed-off-by: Alex Deucher <alexander.deucher@amd.com>
(cherry picked from commit 6ad34adeaec5b56a5ba90e90099cabf1c1fe9dd2)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/gpu/drm/amd/display/modules/color/color_gamma.c
diff --cc drivers/gpu/drm/amd/display/modules/color/color_gamma.c
index eee0dfad6962,89a65e1d8317..000000000000
--- a/drivers/gpu/drm/amd/display/modules/color/color_gamma.c
+++ b/drivers/gpu/drm/amd/display/modules/color/color_gamma.c
@@@ -1274,11 -1576,34 +1274,17 @@@ bool mod_color_calculate_regamma_params
  
  	output_tf->type = TF_TYPE_DISTRIBUTED_POINTS;
  
++<<<<<<< HEAD
 +	rgb_user = kvcalloc(ramp->num_entries + _EXTRA_POINTS,
++=======
+ 	if (ramp && ramp->type != GAMMA_CS_TFM_1D &&
+ 			(mapUserRamp || ramp->type != GAMMA_RGB_256)) {
+ 		rgb_user = kvcalloc(ramp->num_entries + _EXTRA_POINTS,
++>>>>>>> 6ad34adeaec5 (drm/amd/display: CS_TFM_1D only applied post EOTF)
  			    sizeof(*rgb_user),
  			    GFP_KERNEL);
 -		if (!rgb_user)
 -			goto rgb_user_alloc_fail;
 -
 -		axis_x = kvcalloc(ramp->num_entries + 3, sizeof(*axis_x),
 -				GFP_KERNEL);
 -		if (!axis_x)
 -			goto axis_x_alloc_fail;
 -
 -		dividers.divider1 = dc_fixpt_from_fraction(3, 2);
 -		dividers.divider2 = dc_fixpt_from_int(2);
 -		dividers.divider3 = dc_fixpt_from_fraction(5, 2);
 -
 -		build_evenly_distributed_points(
 -				axis_x,
 -				ramp->num_entries,
 -				dividers);
 -
 -		if (ramp->type == GAMMA_RGB_256 && mapUserRamp)
 -			scale_gamma(rgb_user, ramp, dividers);
 -		else if (ramp->type == GAMMA_RGB_FLOAT_1024)
 -			scale_gamma_dx(rgb_user, ramp, dividers);
 -	}
 -
 +	if (!rgb_user)
 +		goto rgb_user_alloc_fail;
  	rgb_regamma = kvcalloc(MAX_HW_POINTS + _EXTRA_POINTS,
  			       sizeof(*rgb_regamma),
  			       GFP_KERNEL);
* Unmerged path drivers/gpu/drm/amd/display/modules/color/color_gamma.c
