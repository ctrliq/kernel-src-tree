net: usb: ax88179_178a: move priv to driver_priv

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-540.el8
commit-author Justin Chen <justinpopo6@gmail.com>
commit 2bcbd3d8a7b4525cdb741fe82330edb6f5452c7f
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-540.el8/2bcbd3d8.failed

We need more space to save WoL context. So lets allocate memory
for ax88179_data instead of using struct usbnet data field which
only supports 5 words. We continue to use the struct usbnet data
field for multicast filters. However since we no longer have the
private data stored there, we can shift it to the beginning.

	Signed-off-by: Justin Chen <justinpopo6@gmail.com>
	Signed-off-by: Jakub Kicinski <kuba@kernel.org>
(cherry picked from commit 2bcbd3d8a7b4525cdb741fe82330edb6f5452c7f)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/usb/ax88179_178a.c
diff --cc drivers/net/usb/ax88179_178a.c
index a5749233f0e8,d0cd9867cae7..000000000000
--- a/drivers/net/usb/ax88179_178a.c
+++ b/drivers/net/usb/ax88179_178a.c
@@@ -182,7 -170,7 +182,11 @@@ struct ax88179_data 
  	u8  eee_enabled;
  	u8  eee_active;
  	u16 rxctl;
++<<<<<<< HEAD
 +	u16 reserved;
++=======
+ 	u8 in_pm;
++>>>>>>> 2bcbd3d8a7b4 (net: usb: ax88179_178a: move priv to driver_priv)
  };
  
  struct ax88179_int_data {
@@@ -199,8 -187,22 +203,25 @@@ static const struct 
  	{7, 0xcc, 0x4c, 0x18, 8},
  };
  
++<<<<<<< HEAD
++=======
+ static void ax88179_set_pm_mode(struct usbnet *dev, bool pm_mode)
+ {
+ 	struct ax88179_data *ax179_data = dev->driver_priv;
+ 
+ 	ax179_data->in_pm = pm_mode;
+ }
+ 
+ static int ax88179_in_pm(struct usbnet *dev)
+ {
+ 	struct ax88179_data *ax179_data = dev->driver_priv;
+ 
+ 	return ax179_data->in_pm;
+ }
+ 
++>>>>>>> 2bcbd3d8a7b4 (net: usb: ax88179_178a: move priv to driver_priv)
  static int __ax88179_read_cmd(struct usbnet *dev, u8 cmd, u16 value, u16 index,
 -			      u16 size, void *data)
 +			      u16 size, void *data, int in_pm)
  {
  	int ret;
  	int (*fn)(struct usbnet *, u8, u8, u16, u16, void *, u16);
* Unmerged path drivers/net/usb/ax88179_178a.c
