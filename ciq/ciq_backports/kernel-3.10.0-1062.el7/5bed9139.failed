ALSA: rawmidi: Tidy up coding styles

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-1062.el7
commit-author Takashi Iwai <tiwai@suse.de>
commit 5bed9139727f3bad06c9444bd092336a59397e9d
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-1062.el7/5bed9139.failed

Just minor coding style fixes like removal of superfluous white space,
adding missing blank lines, etc.  No actual code changes at all.

	Signed-off-by: Takashi Iwai <tiwai@suse.de>
(cherry picked from commit 5bed9139727f3bad06c9444bd092336a59397e9d)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	sound/core/rawmidi.c
diff --cc sound/core/rawmidi.c
index edf6ff52bbea,14dec13ecde9..000000000000
--- a/sound/core/rawmidi.c
+++ b/sound/core/rawmidi.c
@@@ -368,9 -373,9 +373,9 @@@ static int snd_rawmidi_open(struct inod
  	int err;
  	struct snd_rawmidi *rmidi;
  	struct snd_rawmidi_file *rawmidi_file = NULL;
 -	wait_queue_entry_t wait;
 +	wait_queue_t wait;
  
- 	if ((file->f_flags & O_APPEND) && !(file->f_flags & O_NONBLOCK)) 
+ 	if ((file->f_flags & O_APPEND) && !(file->f_flags & O_NONBLOCK))
  		return -EINVAL;		/* invalid combination */
  
  	err = nonseekable_open(inode, file);
@@@ -1019,7 -1029,8 +1029,12 @@@ static ssize_t snd_rawmidi_read(struct 
  	while (count > 0) {
  		spin_lock_irq(&runtime->lock);
  		while (!snd_rawmidi_ready(substream)) {
++<<<<<<< HEAD
 +			wait_queue_t wait;
++=======
+ 			wait_queue_entry_t wait;
+ 
++>>>>>>> 5bed9139727f (ALSA: rawmidi: Tidy up coding styles)
  			if ((file->f_flags & O_NONBLOCK) != 0 || result > 0) {
  				spin_unlock_irq(&runtime->lock);
  				return result > 0 ? result : -EAGAIN;
@@@ -1323,7 -1334,8 +1338,12 @@@ static ssize_t snd_rawmidi_write(struc
  	while (count > 0) {
  		spin_lock_irq(&runtime->lock);
  		while (!snd_rawmidi_ready_append(substream, count)) {
++<<<<<<< HEAD
 +			wait_queue_t wait;
++=======
+ 			wait_queue_entry_t wait;
+ 
++>>>>>>> 5bed9139727f (ALSA: rawmidi: Tidy up coding styles)
  			if (file->f_flags & O_NONBLOCK) {
  				spin_unlock_irq(&runtime->lock);
  				return result > 0 ? result : -EAGAIN;
@@@ -1355,8 -1367,9 +1375,9 @@@
  	if (file->f_flags & O_DSYNC) {
  		spin_lock_irq(&runtime->lock);
  		while (runtime->avail != runtime->buffer_size) {
 -			wait_queue_entry_t wait;
 +			wait_queue_t wait;
  			unsigned int last_avail = runtime->avail;
+ 
  			init_waitqueue_entry(&wait, current);
  			add_wait_queue(&runtime->sleep, &wait);
  			set_current_state(TASK_INTERRUPTIBLE);
@@@ -1374,7 -1387,7 +1395,11 @@@
  	return result;
  }
  
++<<<<<<< HEAD
 +static unsigned int snd_rawmidi_poll(struct file *file, poll_table * wait)
++=======
+ static __poll_t snd_rawmidi_poll(struct file *file, poll_table *wait)
++>>>>>>> 5bed9139727f (ALSA: rawmidi: Tidy up coding styles)
  {
  	struct snd_rawmidi_file *rfile;
  	struct snd_rawmidi_runtime *runtime;
* Unmerged path sound/core/rawmidi.c
