pktgen: remove one sparse error

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-1062.el7
commit-author Eric Dumazet <edumazet@google.com>
commit d4969581452be430135ba5040c5b6c9023c1d461
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-1062.el7/d4969581.failed

net/core/pktgen.c:2672:43: warning: incorrect type in assignment (different base types)
net/core/pktgen.c:2672:43:    expected unsigned short [unsigned] [short] [usertype] <noident>
net/core/pktgen.c:2672:43:    got restricted __be16 [usertype] protocol

Let's use proper struct ethhdr instead of hard coding everything.

	Signed-off-by: Eric Dumazet <edumazet@google.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit d4969581452be430135ba5040c5b6c9023c1d461)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/core/pktgen.c
diff --cc net/core/pktgen.c
index ae8340dc91c8,d93cbc5715f4..000000000000
--- a/net/core/pktgen.c
+++ b/net/core/pktgen.c
@@@ -2661,9 -2667,9 +2661,15 @@@ static int process_ipsec(struct pktgen_
  				goto err;
  			}
  			/* restore ll */
++<<<<<<< HEAD
 +			eth = skb_push(skb, ETH_HLEN);
 +			memcpy(eth, pkt_dev->hh, 12);
 +			*(u16 *) &eth[12] = protocol;
++=======
+ 			eth = (struct ethhdr *)skb_push(skb, ETH_HLEN);
+ 			memcpy(eth, pkt_dev->hh, 2 * ETH_ALEN);
+ 			eth->h_proto = protocol;
++>>>>>>> d4969581452b (pktgen: remove one sparse error)
  
  			/* Update IPv4 header len as well as checksum value */
  			iph = ip_hdr(skb);
* Unmerged path net/core/pktgen.c
