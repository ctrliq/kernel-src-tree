ext4: Set flags on quota files directly

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-1062.el7
commit-author Jan Kara <jack@suse.cz>
commit 957153fce8d226612de05938ca18101b7d608328
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-1062.el7/957153fc.failed

Currently immutable and noatime flags on quota files are set by quota
code which requires us to copy inode->i_flags to our on disk version of
quota flags in GETFLAGS ioctl and ext4_do_update_inode(). Move to
setting / clearing these on-disk flags directly to save that copying.

	Signed-off-by: Jan Kara <jack@suse.cz>
(cherry picked from commit 957153fce8d226612de05938ca18101b7d608328)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/ext4/super.c
diff --cc fs/ext4/super.c
index fd0252f29e8f,6af08bcc6f40..000000000000
--- a/fs/ext4/super.c
+++ b/fs/ext4/super.c
@@@ -1063,11 -1241,7 +1085,15 @@@ static int ext4_release_dquot(struct dq
  static int ext4_mark_dquot_dirty(struct dquot *dquot);
  static int ext4_write_info(struct super_block *sb, int type);
  static int ext4_quota_on(struct super_block *sb, int type, int format_id,
++<<<<<<< HEAD
 +			 struct path *path);
 +static int ext4_quota_on_sysfile(struct super_block *sb, int type,
 +				 int format_id);
 +static int ext4_quota_off(struct super_block *sb, int type);
 +static int ext4_quota_off_sysfile(struct super_block *sb, int type);
++=======
+ 			 const struct path *path);
++>>>>>>> 957153fce8d2 (ext4: Set flags on quota files directly)
  static int ext4_quota_on_mount(struct super_block *sb, int type);
  static ssize_t ext4_quota_read(struct super_block *sb, int type, char *data,
  			       size_t len, loff_t off);
@@@ -5544,11 -5481,17 +5593,22 @@@ static int ext4_quota_off(struct super_
  	 * start looking at them */
  	handle = ext4_journal_start(inode, EXT4_HT_QUOTA, 1);
  	if (IS_ERR(handle))
++<<<<<<< HEAD
 +		goto out;
 +	inode->i_mtime = inode->i_ctime = CURRENT_TIME;
++=======
+ 		goto out_unlock;
+ 	EXT4_I(inode)->i_flags &= ~(EXT4_NOATIME_FL | EXT4_IMMUTABLE_FL);
+ 	inode_set_flags(inode, 0, S_NOATIME | S_IMMUTABLE);
+ 	inode->i_mtime = inode->i_ctime = current_time(inode);
++>>>>>>> 957153fce8d2 (ext4: Set flags on quota files directly)
  	ext4_mark_inode_dirty(handle, inode);
  	ext4_journal_stop(handle);
- 
+ out_unlock:
+ 	inode_unlock(inode);
+ out_put:
+ 	iput(inode);
+ 	return err;
  out:
  	return dquot_quota_off(sb, type);
  }
* Unmerged path fs/ext4/super.c
