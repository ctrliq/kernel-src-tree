ipmi_ssif: Remove usecount handling

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-1062.el7
commit-author Corey Minyard <cminyard@mvista.com>
commit f0258c95304e7712143bddd3a2af53bc9e549a7b
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-1062.el7/f0258c95.failed

Now that we can handle hot remove there is no need for usecounts
for interfaces.

	Signed-off-by: Corey Minyard <cminyard@mvista.com>
(cherry picked from commit f0258c95304e7712143bddd3a2af53bc9e549a7b)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/char/ipmi/ipmi_ssif.c
diff --cc drivers/char/ipmi/ipmi_ssif.c
index 26de0e60ffb6,8c72f271d4b4..000000000000
--- a/drivers/char/ipmi/ipmi_ssif.c
+++ b/drivers/char/ipmi/ipmi_ssif.c
@@@ -1102,27 -1094,8 +1102,32 @@@ static void request_events(void *send_i
  	}
  }
  
++<<<<<<< HEAD
 +static int inc_usecount(void *send_info)
 +{
 +	struct ssif_info *ssif_info = send_info;
 +
 +	if (!i2c_get_adapter(i2c_adapter_id(ssif_info->client->adapter)))
 +		return -ENODEV;
 +
 +	i2c_use_client(ssif_info->client);
 +	return 0;
 +}
 +
 +static void dec_usecount(void *send_info)
 +{
 +	struct ssif_info *ssif_info = send_info;
 +
 +	i2c_release_client(ssif_info->client);
 +	i2c_put_adapter(ssif_info->client->adapter);
 +}
 +
 +static int ssif_start_processing(void *send_info,
 +				 ipmi_smi_t intf)
++=======
+ static int ssif_start_processing(void            *send_info,
+ 				 struct ipmi_smi *intf)
++>>>>>>> f0258c95304e (ipmi_ssif: Remove usecount handling)
  {
  	struct ssif_info *ssif_info = send_info;
  
* Unmerged path drivers/char/ipmi/ipmi_ssif.c
