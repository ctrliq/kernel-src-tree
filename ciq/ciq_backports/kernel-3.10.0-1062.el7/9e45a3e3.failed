tcp: apply Kern's check on RTTs used for congestion control

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-1062.el7
commit-author Yuchung Cheng <ycheng@google.com>
commit 9e45a3e36b363cc4c79c70f2b4f994e66543a219
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-1062.el7/9e45a3e3.failed

Currently ca_seq_rtt_us does not use Kern's check. Fix that by
checking if any packet acked is a retransmit, for both RTT used
for RTT estimation and congestion control.

Fixes: 5b08e47ca ("tcp: prefer packet timing to TS-ECR for RTT")
	Signed-off-by: Yuchung Cheng <ycheng@google.com>
	Signed-off-by: Neal Cardwell <ncardwell@google.com>
	Signed-off-by: Eric Dumazet <edumazet@google.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 9e45a3e36b363cc4c79c70f2b4f994e66543a219)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/ipv4/tcp_input.c
diff --cc net/ipv4/tcp_input.c
index 153152d4fc37,62ee71efd1ce..000000000000
--- a/net/ipv4/tcp_input.c
+++ b/net/ipv4/tcp_input.c
@@@ -3153,9 -3166,13 +3150,13 @@@ static int tcp_clean_rtx_queue(struct s
  		flag |= FLAG_SACK_RENEGING;
  
  	skb_mstamp_get(&now);
++<<<<<<< HEAD
 +	if (first_ackt.v64) {
++=======
+ 	if (likely(first_ackt.v64) && !(flag & FLAG_RETRANS_DATA_ACKED)) {
++>>>>>>> 9e45a3e36b36 (tcp: apply Kern's check on RTTs used for congestion control)
  		seq_rtt_us = skb_mstamp_us_delta(&now, &first_ackt);
 -		ca_rtt_us = skb_mstamp_us_delta(&now, &last_ackt);
 -	}
 -	if (sack->first_sackt.v64) {
 -		sack_rtt_us = skb_mstamp_us_delta(&now, &sack->first_sackt);
 -		ca_rtt_us = skb_mstamp_us_delta(&now, &sack->last_sackt);
 +		ca_seq_rtt_us = skb_mstamp_us_delta(&now, &last_ackt);
  	}
  
  	rtt_update = tcp_ack_update_rtt(sk, flag, seq_rtt_us, sack_rtt_us);
* Unmerged path net/ipv4/tcp_input.c
