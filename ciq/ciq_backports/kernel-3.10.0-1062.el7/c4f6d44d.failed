crypto: chelsio - cleanup:send addr as value in function argument

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-1062.el7
Rebuild_CHGLOG: - [crypto] chelsio - cleanup:send addr as value in function argument (Arjun Vynipadath) [1642432]
Rebuild_FUZZ: 93.44%
commit-author Harsh Jain <harsh@chelsio.com>
commit c4f6d44d774eff382b6fc79a9fe1ff376b5ac6d7
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-1062.el7/c4f6d44d.failed

Send dma address as value to function arguments instead of pointer.

	Signed-off-by: Harsh Jain <harsh@chelsio.com>
	Signed-off-by: Herbert Xu <herbert@gondor.apana.org.au>
(cherry picked from commit c4f6d44d774eff382b6fc79a9fe1ff376b5ac6d7)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/crypto/chelsio/chcr_algo.c
diff --cc drivers/crypto/chelsio/chcr_algo.c
index 8a76dcf1a215,f94364b097dc..000000000000
--- a/drivers/crypto/chelsio/chcr_algo.c
+++ b/drivers/crypto/chelsio/chcr_algo.c
@@@ -2506,11 -2481,9 +2506,17 @@@ void chcr_add_aead_src_ent(struct aead_
  		ulptx_walk_init(&ulp_walk, ulptx);
  		if (reqctx->b0_len)
  			ulptx_walk_add_page(&ulp_walk, reqctx->b0_len,
++<<<<<<< HEAD
 +					    &reqctx->b0_dma);
 +		ulptx_walk_add_sg(&ulp_walk, req->assoc, assoclen, 0);
 +		ulptx_walk_add_page(&ulp_walk, IV, &reqctx->iv_dma);
 +		ulptx_walk_add_sg(&ulp_walk, req->src, req->cryptlen,
 +				  0);
++=======
+ 					    reqctx->b0_dma);
+ 		ulptx_walk_add_sg(&ulp_walk, req->src, req->cryptlen +
+ 				  req->assoclen,  0);
++>>>>>>> c4f6d44d774e (crypto: chelsio - cleanup:send addr as value in function argument)
  		ulptx_walk_end(&ulp_walk);
  	}
  }
@@@ -2527,13 -2499,12 +2533,19 @@@ void chcr_add_aead_dst_ent(struct aead_
  	u32 temp;
  
  	dsgl_walk_init(&dsgl_walk, phys_cpl);
++<<<<<<< HEAD
 +	if (reqctx->b0_len)
 +		dsgl_walk_add_page(&dsgl_walk, reqctx->b0_len, &reqctx->b0_dma);
 +	dsgl_walk_add_sg(&dsgl_walk, req->assoc, assoclen, 0);
 +	dsgl_walk_add_page(&dsgl_walk, IV, &reqctx->iv_dma);
 +	temp = req->cryptlen + (reqctx->op ? -authsize : authsize);
++=======
+ 	dsgl_walk_add_page(&dsgl_walk, IV + reqctx->b0_len, reqctx->iv_dma);
+ 	temp = req->assoclen + req->cryptlen +
+ 		(reqctx->op ? -authsize : authsize);
++>>>>>>> c4f6d44d774e (crypto: chelsio - cleanup:send addr as value in function argument)
  	dsgl_walk_add_sg(&dsgl_walk, req->dst, temp, 0);
 -	dsgl_walk_end(&dsgl_walk, qid, ctx->pci_chan_id);
 +	dsgl_walk_end(&dsgl_walk, qid);
  }
  
  void chcr_add_cipher_src_ent(struct ablkcipher_request *req,
* Unmerged path drivers/crypto/chelsio/chcr_algo.c
