xfs: remove a duplicate assignment in xfs_bmap_add_extent_delay_real

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-1062.el7
commit-author Christoph Hellwig <hch@lst.de>
commit bf99971c8200fcb3e16d880194f5d559aca09576
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-1062.el7/bf99971c.failed

	Reported-by: Brian Foster <bfoster@redhat.com>
	Signed-off-by: Christoph Hellwig <hch@lst.de>
	Reviewed-by: Brian Foster <bfoster@redhat.com>
	Reviewed-by: Darrick J. Wong <darrick.wong@oracle.com>
	Signed-off-by: Darrick J. Wong <darrick.wong@oracle.com>
(cherry picked from commit bf99971c8200fcb3e16d880194f5d559aca09576)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/xfs/libxfs/xfs_bmap.c
diff --cc fs/xfs/libxfs/xfs_bmap.c
index 905742f2f1f9,e1d61face277..000000000000
--- a/fs/xfs/libxfs/xfs_bmap.c
+++ b/fs/xfs/libxfs/xfs_bmap.c
@@@ -1786,16 -1757,19 +1786,30 @@@ xfs_bmap_add_extent_delay_real
  		 * Filling in the first part of a previous delayed allocation.
  		 * The left neighbor is contiguous.
  		 */
 -		old = LEFT;
 +		trace_xfs_bmap_pre_update(bma->ip, bma->idx - 1, state, _THIS_IP_);
 +		xfs_bmbt_set_blockcount(xfs_iext_get_ext(ifp, bma->idx - 1),
 +			LEFT.br_blockcount + new->br_blockcount);
 +		xfs_bmbt_set_startoff(ep,
 +			PREV.br_startoff + new->br_blockcount);
 +		trace_xfs_bmap_post_update(bma->ip, bma->idx - 1, state, _THIS_IP_);
 +
  		temp = PREV.br_blockcount - new->br_blockcount;
++<<<<<<< HEAD
 +		trace_xfs_bmap_pre_update(bma->ip, bma->idx, state, _THIS_IP_);
 +		xfs_bmbt_set_blockcount(ep, temp);
++=======
+ 		da_new = XFS_FILBLKS_MIN(xfs_bmap_worst_indlen(bma->ip, temp),
+ 				startblockval(PREV.br_startblock));
+ 
+ 		LEFT.br_blockcount += new->br_blockcount;
+ 		xfs_iext_update_extent(bma->ip, state, bma->idx - 1, &LEFT);
+ 
+ 		PREV.br_blockcount = temp;
+ 		PREV.br_startoff += new->br_blockcount;
+ 		PREV.br_startblock = nullstartblock(da_new);
+ 		xfs_iext_update_extent(bma->ip, state, bma->idx, &PREV);
+ 
++>>>>>>> bf99971c8200 (xfs: remove a duplicate assignment in xfs_bmap_add_extent_delay_real)
  		if (bma->cur == NULL)
  			rval = XFS_ILOG_DEXT;
  		else {
* Unmerged path fs/xfs/libxfs/xfs_bmap.c
