fm10k: don't protect fm10k_queue_mac_request by fm10k_host_mbx_ready

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-1062.el7
commit-author Jacob Keller <jacob.e.keller@intel.com>
commit 0a3e92de1be42b5dbed4b81f835740d3f58c5430
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-1062.el7/0a3e92de.failed

We don't actually need to check if the host mbx is ready when queuing
MAC requests, because these are not handled by a special queue which
queues up requests until the mailbox is capable of handling them.

Pull these requests outside the fm10k_host_mbx_ready() check, as it is
not necessary.

	Signed-off-by: Jacob Keller <jacob.e.keller@intel.com>
	Tested-by: Krishneil Singh <krishneil.k.singh@intel.com>
	Signed-off-by: Jeff Kirsher <jeffrey.t.kirsher@intel.com>
(cherry picked from commit 0a3e92de1be42b5dbed4b81f835740d3f58c5430)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/intel/fm10k/fm10k_netdev.c
diff --cc drivers/net/ethernet/intel/fm10k/fm10k_netdev.c
index d2c9568f1f34,929f538d28bc..000000000000
--- a/drivers/net/ethernet/intel/fm10k/fm10k_netdev.c
+++ b/drivers/net/ethernet/intel/fm10k/fm10k_netdev.c
@@@ -1528,12 -1537,12 +1528,19 @@@ static void *fm10k_dfwd_add_station(str
  
  	glort = l2_accel->dglort + 1 + i;
  
- 	if (fm10k_host_mbx_ready(interface)) {
+ 	if (fm10k_host_mbx_ready(interface))
  		hw->mac.ops.update_xcast_mode(hw, glort,
++<<<<<<< HEAD
 +					      FM10K_XCAST_MODE_MULTI);
 +		fm10k_queue_mac_request(interface, glort, sdev->dev_addr,
 +					hw->mac.default_vid, true);
 +	}
++=======
+ 					      FM10K_XCAST_MODE_NONE);
+ 
+ 	fm10k_queue_mac_request(interface, glort, sdev->dev_addr,
+ 				hw->mac.default_vid, true);
++>>>>>>> 0a3e92de1be4 (fm10k: don't protect fm10k_queue_mac_request by fm10k_host_mbx_ready)
  
  	for (vid = fm10k_find_next_vlan(interface, 0);
  	     vid < VLAN_N_VID;
* Unmerged path drivers/net/ethernet/intel/fm10k/fm10k_netdev.c
