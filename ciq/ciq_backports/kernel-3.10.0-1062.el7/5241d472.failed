fs/userfaultfd.c: remove redundant pointer uwq

jira LE-1907
cve CVE-2018-18397
Rebuild_History Non-Buildable kernel-3.10.0-1062.el7
Rebuild_CHGLOG: - [fs] userfaultfd.c: remove redundant pointer uwq (Andrea Arcangeli) [1640519] {CVE-2018-18397}
Rebuild_FUZZ: 96.63%
commit-author Colin Ian King <colin.king@canonical.com>
commit 5241d4727479aad77af50b80757c38268bfa4560
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-1062.el7/5241d472.failed

Pointer uwq is being assigned but is never used hence it is redundant
and can be removed.

Cleans up clang warning:
  warning: variable 'uwq' set but not used [-Wunused-but-set-variable]

Link: http://lkml.kernel.org/r/20180717090802.18357-1-colin.king@canonical.com
	Signed-off-by: Colin Ian King <colin.king@canonical.com>
	Reviewed-by: Andrew Morton <akpm@linux-foundation.org>
	Signed-off-by: Andrew Morton <akpm@linux-foundation.org>
	Signed-off-by: Linus Torvalds <torvalds@linux-foundation.org>
(cherry picked from commit 5241d4727479aad77af50b80757c38268bfa4560)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/userfaultfd.c
diff --cc fs/userfaultfd.c
index 461ed05b3a6e,15c265d450bf..000000000000
--- a/fs/userfaultfd.c
+++ b/fs/userfaultfd.c
@@@ -1840,21 -1845,18 +1840,33 @@@ static long userfaultfd_ioctl(struct fi
  }
  
  #ifdef CONFIG_PROC_FS
 -static void userfaultfd_show_fdinfo(struct seq_file *m, struct file *f)
 +static int userfaultfd_show_fdinfo(struct seq_file *m, struct file *f)
  {
  	struct userfaultfd_ctx *ctx = f->private_data;
++<<<<<<< HEAD
 +	wait_queue_t *wq;
 +	struct userfaultfd_wait_queue *uwq;
 +	unsigned long pending = 0, total = 0;
 +
 +	spin_lock(&ctx->fault_pending_wqh.lock);
 +	list_for_each_entry(wq, &ctx->fault_pending_wqh.task_list, task_list) {
 +		uwq = container_of(wq, struct userfaultfd_wait_queue, wq);
 +		pending++;
 +		total++;
 +	}
 +	list_for_each_entry(wq, &ctx->fault_wqh.task_list, task_list) {
 +		uwq = container_of(wq, struct userfaultfd_wait_queue, wq);
++=======
+ 	wait_queue_entry_t *wq;
+ 	unsigned long pending = 0, total = 0;
+ 
+ 	spin_lock(&ctx->fault_pending_wqh.lock);
+ 	list_for_each_entry(wq, &ctx->fault_pending_wqh.head, entry) {
+ 		pending++;
+ 		total++;
+ 	}
+ 	list_for_each_entry(wq, &ctx->fault_wqh.head, entry) {
++>>>>>>> 5241d4727479 (fs/userfaultfd.c: remove redundant pointer uwq)
  		total++;
  	}
  	spin_unlock(&ctx->fault_pending_wqh.lock);
* Unmerged path fs/userfaultfd.c
