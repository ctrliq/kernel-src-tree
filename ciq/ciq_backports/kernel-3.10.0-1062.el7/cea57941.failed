ext4: add missing brelse() in set_flexbg_block_bitmap()'s error path

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-1062.el7
commit-author Vasily Averin <vvs@virtuozzo.com>
commit cea5794122125bf67559906a0762186cf417099c
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-1062.el7/cea57941.failed

Fixes: 33afdcc5402d ("ext4: add a function which sets up group blocks ...")
	Cc: stable@kernel.org # 3.3
	Signed-off-by: Vasily Averin <vvs@virtuozzo.com>
	Signed-off-by: Theodore Ts'o <tytso@mit.edu>
(cherry picked from commit cea5794122125bf67559906a0762186cf417099c)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/ext4/resize.c
diff --cc fs/ext4/resize.c
index 102d23d9a5a0,0a4dc6217e78..000000000000
--- a/fs/ext4/resize.c
+++ b/fs/ext4/resize.c
@@@ -442,16 -459,18 +442,24 @@@ static int set_flexbg_block_bitmap(stru
  
  		BUFFER_TRACE(bh, "get_write_access");
  		err = ext4_journal_get_write_access(handle, bh);
- 		if (err)
+ 		if (err) {
+ 			brelse(bh);
  			return err;
++<<<<<<< HEAD
 +		ext4_debug("mark block bitmap %#04llx (+%llu/%u)\n", block,
 +			   block - start, count2);
 +		ext4_set_bits(bh->b_data, block - start, count2);
++=======
+ 		}
+ 		ext4_debug("mark block bitmap %#04llx (+%llu/%u)\n",
+ 			   first_cluster, first_cluster - start, count2);
+ 		ext4_set_bits(bh->b_data, first_cluster - start, count2);
++>>>>>>> cea579412212 (ext4: add missing brelse() in set_flexbg_block_bitmap()'s error path)
  
  		err = ext4_handle_dirty_metadata(handle, NULL, bh);
+ 		brelse(bh);
  		if (unlikely(err))
  			return err;
- 		brelse(bh);
  	}
  
  	return 0;
* Unmerged path fs/ext4/resize.c
