net/mlx5e: Err if asked to mirror a goto chain tc eswitch rule

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-1062.el7
Rebuild_CHGLOG: - [netdrv] mlx5e: Err if asked to mirror a goto chain tc eswitch rule (Alaa Hleihel) [1642498]
Rebuild_FUZZ: 96.67%
commit-author Or Gerlitz <ogerlitz@mellanox.com>
commit e88afe759a49f8d51d6cc5fc89d65b4a5768703f
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-1062.el7/e88afe75.failed

Currently we are not supporting this and not err-ing on that either.

For now, just err if asked to do that.

Fixes: bf07aa730a04 ('net/mlx5e: Support offloading tc priorities and chains for eswitch flows')
	Signed-off-by: Or Gerlitz <ogerlitz@mellanox.com>
	Reported-by: Eli Britstein <elibr@mellanox.com>
	Reviewed-by: Eli Britstein <elibr@mellanox.com>
	Signed-off-by: Saeed Mahameed <saeedm@mellanox.com>
(cherry picked from commit e88afe759a49f8d51d6cc5fc89d65b4a5768703f)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/mellanox/mlx5/core/en_tc.c
diff --cc drivers/net/ethernet/mellanox/mlx5/core/en_tc.c
index 0b52a07ef281,1f01c905a042..000000000000
--- a/drivers/net/ethernet/mellanox/mlx5/core/en_tc.c
+++ b/drivers/net/ethernet/mellanox/mlx5/core/en_tc.c
@@@ -2673,16 -2950,50 +2673,56 @@@ static int parse_tc_fdb_actions(struct 
  		}
  
  		if (is_tcf_tunnel_release(a)) {
++<<<<<<< HEAD
 +			attr->action |= MLX5_FLOW_CONTEXT_ACTION_DECAP;
++=======
+ 			action |= MLX5_FLOW_CONTEXT_ACTION_DECAP;
+ 			continue;
+ 		}
+ 
+ 		if (is_tcf_gact_goto_chain(a)) {
+ 			u32 dest_chain = tcf_gact_goto_chain_index(a);
+ 			u32 max_chain = mlx5_eswitch_get_chain_range(esw);
+ 
+ 			if (dest_chain <= attr->chain) {
+ 				NL_SET_ERR_MSG(extack, "Goto earlier chain isn't supported");
+ 				return -EOPNOTSUPP;
+ 			}
+ 			if (dest_chain > max_chain) {
+ 				NL_SET_ERR_MSG(extack, "Requested destination chain is out of supported range");
+ 				return -EOPNOTSUPP;
+ 			}
+ 			action |= MLX5_FLOW_CONTEXT_ACTION_COUNT;
+ 			attr->dest_chain = dest_chain;
+ 
++>>>>>>> e88afe759a49 (net/mlx5e: Err if asked to mirror a goto chain tc eswitch rule)
  			continue;
  		}
  
  		return -EINVAL;
  	}
  
 -	attr->action = action;
 -	if (!actions_match_supported(priv, exts, parse_attr, flow, extack))
 +	if (!actions_match_supported(priv, exts, parse_attr, flow))
  		return -EOPNOTSUPP;
  
++<<<<<<< HEAD
++=======
+ 	if (attr->dest_chain) {
+ 		if (attr->action & MLX5_FLOW_CONTEXT_ACTION_FWD_DEST) {
+ 			NL_SET_ERR_MSG(extack, "Mirroring goto chain rules isn't supported");
+ 			return -EOPNOTSUPP;
+ 		}
+ 		attr->action |= MLX5_FLOW_CONTEXT_ACTION_FWD_DEST;
+ 	}
+ 
+ 	if (attr->mirror_count > 0 && !mlx5_esw_has_fwd_fdb(priv->mdev)) {
+ 		NL_SET_ERR_MSG_MOD(extack,
+ 				   "current firmware doesn't support split rule for port mirroring");
+ 		netdev_warn_once(priv->netdev, "current firmware doesn't support split rule for port mirroring\n");
+ 		return -EOPNOTSUPP;
+ 	}
+ 
++>>>>>>> e88afe759a49 (net/mlx5e: Err if asked to mirror a goto chain tc eswitch rule)
  	return 0;
  }
  
* Unmerged path drivers/net/ethernet/mellanox/mlx5/core/en_tc.c
