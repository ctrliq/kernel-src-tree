r8169: remove rtl_wol_pll_power_down

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-1062.el7
commit-author Heiner Kallweit <hkallweit1@gmail.com>
commit 5ea25b125289ed1d77da736a5d511a7d01f4bfb6
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-1062.el7/5ea25b12.failed

rtl_wol_pll_power_down() is used in only one place and removing it
makes the code simpler and better readable.

	Signed-off-by: Heiner Kallweit <hkallweit1@gmail.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 5ea25b125289ed1d77da736a5d511a7d01f4bfb6)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/realtek/r8169.c
diff --cc drivers/net/ethernet/realtek/r8169.c
index f80daa5053d5,e8a112149a62..000000000000
--- a/drivers/net/ethernet/realtek/r8169.c
+++ b/drivers/net/ethernet/realtek/r8169.c
@@@ -4333,55 -4247,6 +4333,58 @@@ static void rtl_wol_suspend_quirk(struc
  	}
  }
  
++<<<<<<< HEAD
 +static bool rtl_wol_pll_power_down(struct rtl8169_private *tp)
 +{
 +	if (!netif_running(tp->dev) || !__rtl8169_get_wol(tp))
 +		return false;
 +
 +	rtl_speed_down(tp);
 +	rtl_wol_suspend_quirk(tp);
 +
 +	return true;
 +}
 +
 +static void r8168_phy_power_up(struct rtl8169_private *tp)
 +{
 +	rtl_writephy(tp, 0x1f, 0x0000);
 +	switch (tp->mac_version) {
 +	case RTL_GIGA_MAC_VER_11:
 +	case RTL_GIGA_MAC_VER_12:
 +	case RTL_GIGA_MAC_VER_17 ... RTL_GIGA_MAC_VER_28:
 +	case RTL_GIGA_MAC_VER_31:
 +		rtl_writephy(tp, 0x0e, 0x0000);
 +		break;
 +	default:
 +		break;
 +	}
 +	rtl_writephy(tp, MII_BMCR, BMCR_ANENABLE);
 +}
 +
 +static void r8168_phy_power_down(struct rtl8169_private *tp)
 +{
 +	rtl_writephy(tp, 0x1f, 0x0000);
 +	switch (tp->mac_version) {
 +	case RTL_GIGA_MAC_VER_32:
 +	case RTL_GIGA_MAC_VER_33:
 +	case RTL_GIGA_MAC_VER_40:
 +	case RTL_GIGA_MAC_VER_41:
 +		rtl_writephy(tp, MII_BMCR, BMCR_ANENABLE | BMCR_PDOWN);
 +		break;
 +
 +	case RTL_GIGA_MAC_VER_11:
 +	case RTL_GIGA_MAC_VER_12:
 +	case RTL_GIGA_MAC_VER_17 ... RTL_GIGA_MAC_VER_28:
 +	case RTL_GIGA_MAC_VER_31:
 +		rtl_writephy(tp, 0x0e, 0x0200);
 +	default:
 +		rtl_writephy(tp, MII_BMCR, BMCR_PDOWN);
 +		break;
 +	}
 +}
 +
++=======
++>>>>>>> 5ea25b125289 (r8169: remove rtl_wol_pll_power_down)
  static void r8168_pll_power_down(struct rtl8169_private *tp)
  {
  	if (r8168_check_dash(tp))
@@@ -4391,11 -4256,12 +4394,14 @@@
  	    tp->mac_version == RTL_GIGA_MAC_VER_33)
  		rtl_ephy_write(tp, 0x19, 0xff64);
  
- 	if (rtl_wol_pll_power_down(tp))
+ 	if (device_may_wakeup(tp_to_dev(tp))) {
+ 		phy_speed_down(tp->phydev, false);
+ 		rtl_wol_suspend_quirk(tp);
  		return;
+ 	}
  
 +	r8168_phy_power_down(tp);
 +
  	switch (tp->mac_version) {
  	case RTL_GIGA_MAC_VER_25 ... RTL_GIGA_MAC_VER_33:
  	case RTL_GIGA_MAC_VER_37:
* Unmerged path drivers/net/ethernet/realtek/r8169.c
