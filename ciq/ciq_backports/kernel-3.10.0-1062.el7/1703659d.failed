scsi: qla2xxx: don't allow negative thresholds

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-1062.el7
Rebuild_CHGLOG: - [scsi] qla2xxx: don't allow negative thresholds (Himanshu Madhani) [1628301]
Rebuild_FUZZ: 93.02%
commit-author Dan Carpenter <dan.carpenter@oracle.com>
commit 1703659dada8a5bfe9c31db6436792cbca1e26ea
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-1062.el7/1703659d.failed

We shouldn't allow negative thresholds.  I don't know what it would do
but it can't be good.

Fixes: 8b4673ba3a1b ("scsi: qla2xxx: Add support for ZIO6 interrupt threshold")
	Signed-off-by: Dan Carpenter <dan.carpenter@oracle.com>
	Acked-by: Himanshu Madhani <himanshu.madhani@cavium.com>
	Signed-off-by: Martin K. Petersen <martin.petersen@oracle.com>
(cherry picked from commit 1703659dada8a5bfe9c31db6436792cbca1e26ea)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/scsi/qla2xxx/qla_attr.c
diff --cc drivers/scsi/qla2xxx/qla_attr.c
index 6fff0cceac31,b28f159fdaee..000000000000
--- a/drivers/scsi/qla2xxx/qla_attr.c
+++ b/drivers/scsi/qla2xxx/qla_attr.c
@@@ -1142,6 -1208,34 +1142,37 @@@ qla2x00_zio_timer_store(struct device *
  }
  
  static ssize_t
++<<<<<<< HEAD
++=======
+ qla_zio_threshold_show(struct device *dev, struct device_attribute *attr,
+ 		       char *buf)
+ {
+ 	scsi_qla_host_t *vha = shost_priv(class_to_shost(dev));
+ 
+ 	return scnprintf(buf, PAGE_SIZE, "%d exchanges\n",
+ 	    vha->hw->last_zio_threshold);
+ }
+ 
+ static ssize_t
+ qla_zio_threshold_store(struct device *dev, struct device_attribute *attr,
+     const char *buf, size_t count)
+ {
+ 	scsi_qla_host_t *vha = shost_priv(class_to_shost(dev));
+ 	int val = 0;
+ 
+ 	if (vha->hw->zio_mode != QLA_ZIO_MODE_6)
+ 		return -EINVAL;
+ 	if (sscanf(buf, "%d", &val) != 1)
+ 		return -EINVAL;
+ 	if (val < 0 || val > 256)
+ 		return -ERANGE;
+ 
+ 	atomic_set(&vha->hw->zio_threshold, val);
+ 	return strlen(buf);
+ }
+ 
+ static ssize_t
++>>>>>>> 1703659dada8 (scsi: qla2xxx: don't allow negative thresholds)
  qla2x00_beacon_show(struct device *dev, struct device_attribute *attr,
  		    char *buf)
  {
* Unmerged path drivers/scsi/qla2xxx/qla_attr.c
