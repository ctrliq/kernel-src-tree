net: phy: add helper phy_config_aneg

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-1062.el7
Rebuild_CHGLOG: - [netdrv] phy: add helper phy_config_aneg (Ivan Vecera) [1655590]
Rebuild_FUZZ: 92.54%
commit-author Heiner Kallweit <hkallweit1@gmail.com>
commit 762995807059dec20c926617415e0cdd52fd44e9
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-1062.el7/76299580.failed

This functionality will also be needed in subsequent patches of this
series, therefore factor it out to a helper.

	Signed-off-by: Heiner Kallweit <hkallweit1@gmail.com>
	Reviewed-by: Andrew Lunn <andrew@lunn.ch>
	Reviewed-by: Florian Fainelli <f.fainelli@gmail.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 762995807059dec20c926617415e0cdd52fd44e9)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/phy/phy.c
diff --cc drivers/net/phy/phy.c
index 9e5ca8eeda8a,c4aa360dedff..000000000000
--- a/drivers/net/phy/phy.c
+++ b/drivers/net/phy/phy.c
@@@ -461,9 -467,18 +461,17 @@@ int phy_mii_ioctl(struct phy_device *ph
  }
  EXPORT_SYMBOL(phy_mii_ioctl);
  
+ static int phy_config_aneg(struct phy_device *phydev)
+ {
+ 	if (phydev->drv->config_aneg)
+ 		return phydev->drv->config_aneg(phydev);
+ 	else
+ 		return genphy_config_aneg(phydev);
+ }
+ 
  /**
 - * phy_start_aneg_priv - start auto-negotiation for this PHY device
 + * phy_start_aneg - start auto-negotiation for this PHY device
   * @phydev: the phy_device struct
 - * @sync: indicate whether we should wait for the workqueue cancelation
   *
   * Description: Sanitizes the settings (if we're not autonegotiating
   *   them), and then calls the driver's config_aneg function.
@@@ -482,7 -501,7 +490,11 @@@ int phy_start_aneg(struct phy_device *p
  	/* Invalidate LP advertising flags */
  	phydev->lp_advertising = 0;
  
++<<<<<<< HEAD
 +	err = phydev->drv->config_aneg(phydev);
++=======
+ 	err = phy_config_aneg(phydev);
++>>>>>>> 762995807059 (net: phy: add helper phy_config_aneg)
  	if (err < 0)
  		goto out_unlock;
  
* Unmerged path drivers/net/phy/phy.c
