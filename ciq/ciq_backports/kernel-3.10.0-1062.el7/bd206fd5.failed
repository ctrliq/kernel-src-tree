net/mlx5e: Use u8 instead of int for LRO number of segments

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-1062.el7
Rebuild_CHGLOG: - [netdrv] mlx5e: Use u8 instead of int for LRO number of segments (Alaa Hleihel) [1642498]
Rebuild_FUZZ: 96.49%
commit-author Tariq Toukan <tariqt@mellanox.com>
commit bd206fd52e039870027d23d6de9f432272fc61d5
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-1062.el7/bd206fd5.failed

Range of LRO number of segments fits in u8.
Also, bring initialization and declaration together to
save code.

	Signed-off-by: Tariq Toukan <tariqt@mellanox.com>
	Signed-off-by: Saeed Mahameed <saeedm@mellanox.com>
(cherry picked from commit bd206fd52e039870027d23d6de9f432272fc61d5)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/mellanox/mlx5/core/en_rx.c
diff --cc drivers/net/ethernet/mellanox/mlx5/core/en_rx.c
index 29331bf7aa1d,e10422b845ca..000000000000
--- a/drivers/net/ethernet/mellanox/mlx5/core/en_rx.c
+++ b/drivers/net/ethernet/mellanox/mlx5/core/en_rx.c
@@@ -698,12 -681,10 +698,14 @@@ static inline void mlx5e_build_rx_skb(s
  				      struct mlx5e_rq *rq,
  				      struct sk_buff *skb)
  {
++<<<<<<< HEAD
 +	struct mlx5e_rq_stats *stats = rq->stats;
++=======
+ 	u8 lro_num_seg = be32_to_cpu(cqe->srqn) >> 24;
++>>>>>>> bd206fd52e03 (net/mlx5e: Use u8 instead of int for LRO number of segments)
  	struct net_device *netdev = rq->netdev;
- 	int lro_num_seg;
  
  	skb->mac_len = ETH_HLEN;
- 	lro_num_seg = be32_to_cpu(cqe->srqn) >> 24;
  	if (lro_num_seg > 1) {
  		mlx5e_lro_update_hdr(skb, cqe, cqe_bcnt);
  		skb_shinfo(skb)->gso_size = DIV_ROUND_UP(cqe_bcnt, lro_num_seg);
* Unmerged path drivers/net/ethernet/mellanox/mlx5/core/en_rx.c
