RDMA/cma: Initialize resource type in __rdma_create_id()

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-1062.el7
commit-author Parav Pandit <parav@mellanox.com>
commit e11fef9f8d442e6929dce6c80dacea6466e348b0
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-1062.el7/e11fef9f.failed

Currently rdma_cm_id's resource tracking fields such as owner task and
kern_name and other non resource tracking fields are initialized in
in single function __rdma_create_id().

Therefore, initialize rdma_cm_id's resource type also in same init
function.

	Signed-off-by: Parav Pandit <parav@mellanox.com>
	Reviewed-by: Daniel Jurgens <danielj@mellanox.com>
	Signed-off-by: Leon Romanovsky <leonro@mellanox.com>
	Signed-off-by: Jason Gunthorpe <jgg@mellanox.com>
(cherry picked from commit e11fef9f8d442e6929dce6c80dacea6466e348b0)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/infiniband/core/cma.c
diff --cc drivers/infiniband/core/cma.c
index 640a4508f496,714fff27d2e3..000000000000
--- a/drivers/infiniband/core/cma.c
+++ b/drivers/infiniband/core/cma.c
@@@ -495,6 -495,7 +495,10 @@@ static void _cma_attach_to_dev(struct r
  	id_priv->id.route.addr.dev_addr.transport =
  		rdma_node_get_transport(cma_dev->device->node_type);
  	list_add_tail(&id_priv->list, &cma_dev->id_list);
++<<<<<<< HEAD
++=======
+ 	rdma_restrack_add(&id_priv->res);
++>>>>>>> e11fef9f8d44 (RDMA/cma: Initialize resource type in __rdma_create_id())
  }
  
  static void cma_attach_to_dev(struct rdma_id_private *id_priv,
@@@ -789,7 -790,11 +793,15 @@@ struct rdma_cm_id *rdma_create_id(struc
  	if (!id_priv)
  		return ERR_PTR(-ENOMEM);
  
++<<<<<<< HEAD
 +	id_priv->owner = task_pid_nr(current);
++=======
+ 	if (caller)
+ 		id_priv->res.kern_name = caller;
+ 	else
+ 		rdma_restrack_set_task(&id_priv->res, current);
+ 	id_priv->res.type = RDMA_RESTRACK_CM_ID;
++>>>>>>> e11fef9f8d44 (RDMA/cma: Initialize resource type in __rdma_create_id())
  	id_priv->state = RDMA_CM_IDLE;
  	id_priv->id.context = context;
  	id_priv->id.event_handler = event_handler;
* Unmerged path drivers/infiniband/core/cma.c
