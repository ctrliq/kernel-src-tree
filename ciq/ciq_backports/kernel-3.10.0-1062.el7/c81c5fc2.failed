ipmi:devintf: Clean up some coding style issues

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-1062.el7
commit-author Corey Minyard <cminyard@mvista.com>
commit c81c5fc2128e3a596900859f10e294e30bc49b24
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-1062.el7/c81c5fc2.failed

	Signed-off-by: Corey Minyard <cminyard@mvista.com>
(cherry picked from commit c81c5fc2128e3a596900859f10e294e30bc49b24)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/char/ipmi/ipmi_devintf.c
diff --cc drivers/char/ipmi/ipmi_devintf.c
index 5bc04673465f,f862838e2c10..000000000000
--- a/drivers/char/ipmi/ipmi_devintf.c
+++ b/drivers/char/ipmi/ipmi_devintf.c
@@@ -75,21 -55,21 +75,26 @@@ static void file_receive_handler(struc
  		kill_fasync(&priv->fasync_queue, SIGIO, POLL_IN);
  	}
  
- 	spin_unlock_irqrestore(&(priv->recv_msg_lock), flags);
+ 	spin_unlock_irqrestore(&priv->recv_msg_lock, flags);
  }
  
 -static __poll_t ipmi_poll(struct file *file, poll_table *wait)
 +static unsigned int ipmi_poll(struct file *file, poll_table *wait)
  {
  	struct ipmi_file_private *priv = file->private_data;
 -	__poll_t             mask = 0;
 +	unsigned int             mask = 0;
  	unsigned long            flags;
  
  	poll_wait(file, &priv->wait, wait);
  
  	spin_lock_irqsave(&priv->recv_msg_lock, flags);
  
++<<<<<<< HEAD
 +	if (!list_empty(&(priv->recv_msgs)))
 +		mask |= (POLLIN | POLLRDNORM);
++=======
+ 	if (!list_empty(&priv->recv_msgs))
+ 		mask |= (EPOLLIN | EPOLLRDNORM);
++>>>>>>> c81c5fc2128e (ipmi:devintf: Clean up some coding style issues)
  
  	spin_unlock_irqrestore(&priv->recv_msg_lock, flags);
  
* Unmerged path drivers/char/ipmi/ipmi_devintf.c
