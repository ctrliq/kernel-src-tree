usb: roles: Fix potential NULL dereference in intel_xhci_usb_probe()

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-1062.el7
Rebuild_CHGLOG: - [usb] roles: Fix potential NULL dereference in intel_xhci_usb_probe() (Torez Smith) [1657401]
Rebuild_FUZZ: 96.18%
commit-author Wei Yongjun <weiyongjun1@huawei.com>
commit 548f4726a10568c208a7a9d676d66470e9e99aa3
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-1062.el7/548f4726.failed

platform_get_resource() may fail and return NULL, so we should
better check it's return value to avoid a NULL pointer dereference
a bit later in the code.

This is detected by Coccinelle semantic patch.

@@
expression pdev, res, n, t, e, e1, e2;
@@

res = platform_get_resource(pdev, t, n);
+ if (!res)
+   return -EINVAL;
... when != res == NULL
e = devm_ioremap_nocache(e1, res->start, e2);

	Signed-off-by: Wei Yongjun <weiyongjun1@huawei.com>
	Signed-off-by: Greg Kroah-Hartman <gregkh@linuxfoundation.org>
(cherry picked from commit 548f4726a10568c208a7a9d676d66470e9e99aa3)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/usb/roles/intel-xhci-usb-role-switch.c
* Unmerged path drivers/usb/roles/intel-xhci-usb-role-switch.c
* Unmerged path drivers/usb/roles/intel-xhci-usb-role-switch.c
