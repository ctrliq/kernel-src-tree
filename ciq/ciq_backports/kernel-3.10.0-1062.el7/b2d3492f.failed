scsi: bnx2fc: Fix error handling in probe()

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-1062.el7
Rebuild_CHGLOG: - [scsi] bnx2fc: Fix error handling in probe() (Maurizio Lombardi) [1643544]
Rebuild_FUZZ: 92.50%
commit-author Dan Carpenter <dan.carpenter@oracle.com>
commit b2d3492fc591b1fb46b81d79ca1fc44cac6ae0ae
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-1062.el7/b2d3492f.failed

There are two issues here.  First if cmgr->hba is not set early enough then
it leads to a NULL dereference.  Second if we don't completely initialize
cmgr->io_bdt_pool[] then we end up dereferencing uninitialized pointers.

Fixes: 853e2bd2103a ("[SCSI] bnx2fc: Broadcom FCoE offload driver")
	Signed-off-by: Dan Carpenter <dan.carpenter@oracle.com>
	Signed-off-by: Martin K. Petersen <martin.petersen@oracle.com>
(cherry picked from commit b2d3492fc591b1fb46b81d79ca1fc44cac6ae0ae)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/scsi/bnx2fc/bnx2fc_io.c
diff --cc drivers/scsi/bnx2fc/bnx2fc_io.c
index 5dc3d3b54416,bc9f2a2365f4..000000000000
--- a/drivers/scsi/bnx2fc/bnx2fc_io.c
+++ b/drivers/scsi/bnx2fc/bnx2fc_io.c
@@@ -240,8 -240,9 +240,14 @@@ struct bnx2fc_cmd_mgr *bnx2fc_cmd_mgr_a
  		return NULL;
  	}
  
++<<<<<<< HEAD
 +	cmgr->free_list = kzalloc(sizeof(*cmgr->free_list) *
 +				  arr_sz, GFP_KERNEL);
++=======
+ 	cmgr->hba = hba;
+ 	cmgr->free_list = kcalloc(arr_sz, sizeof(*cmgr->free_list),
+ 				  GFP_KERNEL);
++>>>>>>> b2d3492fc591 (scsi: bnx2fc: Fix error handling in probe())
  	if (!cmgr->free_list) {
  		printk(KERN_ERR PFX "failed to alloc free_list\n");
  		goto mem_err;
* Unmerged path drivers/scsi/bnx2fc/bnx2fc_io.c
