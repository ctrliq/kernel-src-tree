crypto: chelsio - clean up various indentation issues

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-1062.el7
Rebuild_CHGLOG: - [crypto] chelsio - clean up various indentation issues (Arjun Vynipadath) [1642432]
Rebuild_FUZZ: 91.84%
commit-author Colin Ian King <colin.king@canonical.com>
commit fc6176a240ae93850be445f355c1dba769fe8467
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-1062.el7/fc6176a2.failed

Trivial fix to clean up varous indentation issue

	Signed-off-by: Colin Ian King <colin.king@canonical.com>
	Signed-off-by: Herbert Xu <herbert@gondor.apana.org.au>
(cherry picked from commit fc6176a240ae93850be445f355c1dba769fe8467)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/crypto/chelsio/chcr_algo.c
diff --cc drivers/crypto/chelsio/chcr_algo.c
index 8a76dcf1a215,497c57803de7..000000000000
--- a/drivers/crypto/chelsio/chcr_algo.c
+++ b/drivers/crypto/chelsio/chcr_algo.c
@@@ -1308,12 -1306,13 +1308,12 @@@ static int chcr_aes_decrypt(struct ablk
  
  	if (unlikely(cxgb4_is_crypto_q_full(u_ctx->lldi.ports[0],
  					    c_ctx(tfm)->tx_qidx))) {
 -		isfull = 1;
  		if (!(req->base.flags & CRYPTO_TFM_REQ_MAY_BACKLOG))
 -			return -ENOSPC;
 +			return -EBUSY;
  	}
  
- 	 err = process_cipher(req, u_ctx->lldi.rxq_ids[c_ctx(tfm)->rx_qidx],
- 			      &skb, CHCR_DECRYPT_OP);
+ 	err = process_cipher(req, u_ctx->lldi.rxq_ids[c_ctx(tfm)->rx_qidx],
+ 			     &skb, CHCR_DECRYPT_OP);
  	if (err || !skb)
  		return err;
  	skb->dev = u_ctx->lldi.ports[0];
@@@ -2245,7 -2249,7 +2245,11 @@@ static int chcr_aead_fallback(struct ae
  				  req->base.complete, req->base.data);
  	aead_request_set_crypt(subreq, req->src, req->dst, req->cryptlen,
  				 req->iv);
++<<<<<<< HEAD
 +	aead_request_set_assoc(subreq, req->assoc, req->assoclen);
++=======
+ 	aead_request_set_ad(subreq, req->assoclen);
++>>>>>>> fc6176a240ae (crypto: chelsio - clean up various indentation issues)
  	return op_type ? crypto_aead_decrypt(subreq) :
  		crypto_aead_encrypt(subreq);
  }
* Unmerged path drivers/crypto/chelsio/chcr_algo.c
