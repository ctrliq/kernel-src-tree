perf map: Use map->prot in place of type==MAP__FUNCTION

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-1062.el7
commit-author Arnaldo Carvalho de Melo <acme@redhat.com>
commit d183b2614fcd6d0a10c792891fd09bb254594ad4
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-1062.el7/d183b261.failed

Equivalent, one step more in ditching enum map_type.

	Cc: Adrian Hunter <adrian.hunter@intel.com>
	Cc: David Ahern <dsahern@gmail.com>
	Cc: Jiri Olsa <jolsa@kernel.org>
	Cc: Namhyung Kim <namhyung@kernel.org>
	Cc: Wang Nan <wangnan0@huawei.com>
Link: https://lkml.kernel.org/n/tip-mrjjc87a4tpf896j5u4sql4e@git.kernel.org
	Signed-off-by: Arnaldo Carvalho de Melo <acme@redhat.com>
(cherry picked from commit d183b2614fcd6d0a10c792891fd09bb254594ad4)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	tools/perf/util/map.c
diff --cc tools/perf/util/map.c
index f4cc5261e8a7,a164e1ed5462..000000000000
--- a/tools/perf/util/map.c
+++ b/tools/perf/util/map.c
@@@ -167,9 -171,11 +167,15 @@@ struct map *map__new(struct machine *ma
  		map->ino_generation = ino_gen;
  		map->prot = prot;
  		map->flags = flags;
 -		nsi = nsinfo__get(thread->nsinfo);
  
++<<<<<<< HEAD
 +		if ((anon || no_dso) && type == MAP__FUNCTION) {
 +			snprintf(newfilename, sizeof(newfilename), "/tmp/perf-%d.map", pid);
++=======
+ 		if ((anon || no_dso) && nsi && (prot & PROT_EXEC)) {
+ 			snprintf(newfilename, sizeof(newfilename),
+ 				 "/tmp/perf-%d.map", nsi->pid);
++>>>>>>> d183b2614fcd (perf map: Use map->prot in place of type==MAP__FUNCTION)
  			filename = newfilename;
  		}
  
@@@ -197,9 -213,10 +203,9 @@@
  			 * functions still return NULL, and we avoid the
  			 * unnecessary map__load warning.
  			 */
- 			if (type != MAP__FUNCTION)
+ 			if (!(prot & PROT_EXEC))
  				dso__set_loaded(dso, map->type);
  		}
 -		dso->nsinfo = nsi;
  		dso__put(dso);
  	}
  	return map;
* Unmerged path tools/perf/util/map.c
