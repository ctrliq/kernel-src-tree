ALSA: hda/ca0132 - make pci_iounmap() call conditional

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-1062.el7
commit-author Arnd Bergmann <arnd@arndb.de>
commit 1e73359a24fad529b0794515b46cbfff99e5fbe6
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-1062.el7/1e73359a.failed

When building without CONFIG_PCI, we can (depending on the architecture)
get a link failure:

ERROR: "pci_iounmap" [sound/pci/hda/snd-hda-codec-ca0132.ko] undefined!

Adding a compile-time check for PCI gets it to work correctly on
32-bit ARM.

Fixes: d99501b8575d ("ALSA: hda/ca0132 - Call pci_iounmap() instead of iounmap()")
	Signed-off-by: Arnd Bergmann <arnd@arndb.de>
	Signed-off-by: Takashi Iwai <tiwai@suse.de>
(cherry picked from commit 1e73359a24fad529b0794515b46cbfff99e5fbe6)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	sound/pci/hda/patch_ca0132.c
diff --cc sound/pci/hda/patch_ca0132.c
index 86e3e7952188,e5bdbc245682..000000000000
--- a/sound/pci/hda/patch_ca0132.c
+++ b/sound/pci/hda/patch_ca0132.c
@@@ -4893,6 -8451,18 +4893,21 @@@ static void ca0132_free(struct hda_code
  	ca0132_exit_chip(codec);
  
  	snd_hda_power_down(codec);
++<<<<<<< HEAD
++=======
+ 	if (IS_ENABLED(CONFIG_PCI) && spec->mem_base)
+ 		pci_iounmap(codec->bus->pci, spec->mem_base);
+ 	kfree(spec->spec_init_verbs);
+ 	kfree(codec->spec);
+ }
+ 
+ static void dbpro_free(struct hda_codec *codec)
+ {
+ 	struct ca0132_spec *spec = codec->spec;
+ 
+ 	zxr_dbpro_power_state_shutdown(codec);
+ 
++>>>>>>> 1e73359a24fa (ALSA: hda/ca0132 - make pci_iounmap() call conditional)
  	kfree(spec->spec_init_verbs);
  	kfree(codec->spec);
  }
* Unmerged path sound/pci/hda/patch_ca0132.c
