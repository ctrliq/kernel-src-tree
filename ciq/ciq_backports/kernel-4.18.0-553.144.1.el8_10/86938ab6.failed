KVM: x86/mmu: Stop passing "direct" to mmu_alloc_root()

jira KERNEL-1335
Rebuild_History Non-Buildable kernel-4.18.0-553.144.1.el8_10
commit-author David Matlack <dmatlack@google.com>
commit 86938ab6925b8fe174ca6abf397e6ea9d3c054a4
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-553.144.1.el8_10/86938ab6.failed

The "direct" argument is vcpu->arch.mmu->root_role.direct,
because unlike non-root page tables, it's impossible to have
a direct root in an indirect MMU.  So just use that.

	Suggested-by: Lai Jiangshan <jiangshanlai@gmail.com>
	Signed-off-by: David Matlack <dmatlack@google.com>
Message-Id: <20220516232138.1783324-4-dmatlack@google.com>
	Signed-off-by: Paolo Bonzini <pbonzini@redhat.com>
(cherry picked from commit 86938ab6925b8fe174ca6abf397e6ea9d3c054a4)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	arch/x86/kvm/mmu/mmu.c
diff --cc arch/x86/kvm/mmu/mmu.c
index 1588d5212210,2e30398fe59f..000000000000
--- a/arch/x86/kvm/mmu/mmu.c
+++ b/arch/x86/kvm/mmu/mmu.c
@@@ -3377,9 -3409,9 +3378,9 @@@ static int mmu_alloc_direct_roots(struc
  			WARN_ON_ONCE(IS_VALID_PAE_ROOT(mmu->pae_root[i]));
  
  			root = mmu_alloc_root(vcpu, i << (30 - PAGE_SHIFT),
- 					      i << 30, PT32_ROOT_LEVEL, true);
+ 					      i << 30, PT32_ROOT_LEVEL);
  			mmu->pae_root[i] = root | PT_PRESENT_MASK |
 -					   shadow_me_value;
 +					   shadow_me_mask;
  		}
  		mmu->root.hpa = __pa(mmu->pae_root);
  	} else {
@@@ -3499,9 -3531,9 +3500,13 @@@ static int mmu_alloc_shadow_roots(struc
  	 * Do we shadow a long mode page table? If so we need to
  	 * write-protect the guests page table root.
  	 */
 -	if (mmu->cpu_role.base.level >= PT64_ROOT_4LEVEL) {
 +	if (mmu->root_level >= PT64_ROOT_4LEVEL) {
  		root = mmu_alloc_root(vcpu, root_gfn, 0,
++<<<<<<< HEAD
 +				      mmu->shadow_root_level, false);
++=======
+ 				      mmu->root_role.level);
++>>>>>>> 86938ab6925b (KVM: x86/mmu: Stop passing "direct" to mmu_alloc_root())
  		mmu->root.hpa = root;
  		goto set_root_pgd;
  	}
* Unmerged path arch/x86/kvm/mmu/mmu.c
