mt76: mt7921: remove unnecessary variable

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-315.el8
commit-author Nigel Christian <nigel.l.christian@gmail.com>
commit c2fa8edcca3b87c1d5cd6b9de00cb649abd636f5
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-315.el8/c2fa8edc.failed

In mt7921_pm_set() the variable "ret" is initialized to zero
and then returned. Remove it and return zero.

	Signed-off-by: Nigel Christian <nigel.l.christian@gmail.com>
	Signed-off-by: Felix Fietkau <nbd@nbd.name>
(cherry picked from commit c2fa8edcca3b87c1d5cd6b9de00cb649abd636f5)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/wireless/mediatek/mt76/mt7921/debugfs.c
diff --cc drivers/net/wireless/mediatek/mt76/mt7921/debugfs.c
index 9cdeb14cc933,4bc3dd99bd14..000000000000
--- a/drivers/net/wireless/mediatek/mt76/mt7921/debugfs.c
+++ b/drivers/net/wireless/mediatek/mt76/mt7921/debugfs.c
@@@ -159,6 -159,73 +159,76 @@@ mt7921_queues_read(struct seq_file *s, 
  	return 0;
  }
  
++<<<<<<< HEAD
++=======
+ static int
+ mt7921_pm_set(void *data, u64 val)
+ {
+ 	struct mt7921_dev *dev = data;
+ 	struct mt76_phy *mphy = dev->phy.mt76;
+ 
+ 	mt7921_mutex_acquire(dev);
+ 
+ 	dev->pm.enable = val;
+ 
+ 	ieee80211_iterate_active_interfaces(mphy->hw,
+ 					    IEEE80211_IFACE_ITER_RESUME_ALL,
+ 					    mt7921_pm_interface_iter, mphy->priv);
+ 	mt7921_mutex_release(dev);
+ 
+ 	return 0;
+ }
+ 
+ static int
+ mt7921_pm_get(void *data, u64 *val)
+ {
+ 	struct mt7921_dev *dev = data;
+ 
+ 	*val = dev->pm.enable;
+ 
+ 	return 0;
+ }
+ 
+ DEFINE_DEBUGFS_ATTRIBUTE(fops_pm, mt7921_pm_get, mt7921_pm_set, "%lld\n");
+ 
+ static int
+ mt7921_pm_idle_timeout_set(void *data, u64 val)
+ {
+ 	struct mt7921_dev *dev = data;
+ 
+ 	dev->pm.idle_timeout = msecs_to_jiffies(val);
+ 
+ 	return 0;
+ }
+ 
+ static int
+ mt7921_pm_idle_timeout_get(void *data, u64 *val)
+ {
+ 	struct mt7921_dev *dev = data;
+ 
+ 	*val = jiffies_to_msecs(dev->pm.idle_timeout);
+ 
+ 	return 0;
+ }
+ 
+ DEFINE_DEBUGFS_ATTRIBUTE(fops_pm_idle_timeout, mt7921_pm_idle_timeout_get,
+ 			 mt7921_pm_idle_timeout_set, "%lld\n");
+ 
+ static int mt7921_config(void *data, u64 val)
+ {
+ 	struct mt7921_dev *dev = data;
+ 	int ret;
+ 
+ 	mt7921_mutex_acquire(dev);
+ 	ret = mt76_connac_mcu_chip_config(&dev->mt76);
+ 	mt7921_mutex_release(dev);
+ 
+ 	return ret;
+ }
+ 
+ DEFINE_DEBUGFS_ATTRIBUTE(fops_config, NULL, mt7921_config, "%lld\n");
+ 
++>>>>>>> c2fa8edcca3b (mt76: mt7921: remove unnecessary variable)
  int mt7921_init_debugfs(struct mt7921_dev *dev)
  {
  	struct dentry *dir;
* Unmerged path drivers/net/wireless/mediatek/mt76/mt7921/debugfs.c
