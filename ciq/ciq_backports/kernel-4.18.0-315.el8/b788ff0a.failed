net: ena: fix inaccurate print type

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-315.el8
commit-author Yixing Liu <liuyixing1@huawei.com>
commit b788ff0a7d7dc04da4c938d56cbe96c7fa261983
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-315.el8/b788ff0a.failed

Use "%u" to replace "hu%".

	Signed-off-by: Yixing Liu <liuyixing1@huawei.com>
	Signed-off-by: Weihang Li <liweihang@huawei.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit b788ff0a7d7dc04da4c938d56cbe96c7fa261983)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/amazon/ena/ena_com.c
diff --cc drivers/net/ethernet/amazon/ena/ena_com.c
index d02aeb5b8e86,764852ead1d6..000000000000
--- a/drivers/net/ethernet/amazon/ena/ena_com.c
+++ b/drivers/net/ethernet/amazon/ena/ena_com.c
@@@ -833,9 -862,10 +833,16 @@@ static u32 ena_com_reg_bar_read32(struc
  	}
  
  	if (unlikely(i == timeout)) {
++<<<<<<< HEAD
 +		pr_err("reading reg failed for timeout. expected: req id[%hu] offset[%hu] actual: req id[%hu] offset[%hu]\n",
 +		       mmio_read->seq_num, offset, read_resp->req_id,
 +		       read_resp->reg_off);
++=======
+ 		netdev_err(ena_dev->net_device,
+ 			   "Reading reg failed for timeout. expected: req id[%u] offset[%u] actual: req id[%u] offset[%u]\n",
+ 			   mmio_read->seq_num, offset, read_resp->req_id,
+ 			   read_resp->reg_off);
++>>>>>>> b788ff0a7d7d (net: ena: fix inaccurate print type)
  		ret = ENA_MMIO_READ_TIMEOUT;
  		goto err;
  	}
@@@ -2314,8 -2395,9 +2321,14 @@@ int ena_com_fill_hash_function(struct e
  	case ENA_ADMIN_TOEPLITZ:
  		if (key) {
  			if (key_len != sizeof(hash_key->key)) {
++<<<<<<< HEAD
 +				pr_err("key len (%hu) doesn't equal the supported size (%zu)\n",
 +				       key_len, sizeof(hash_key->key));
++=======
+ 				netdev_err(ena_dev->net_device,
+ 					   "key len (%u) doesn't equal the supported size (%zu)\n",
+ 					   key_len, sizeof(hash_key->key));
++>>>>>>> b788ff0a7d7d (net: ena: fix inaccurate print type)
  				return -EINVAL;
  			}
  			memcpy(hash_key->key, key, key_len);
* Unmerged path drivers/net/ethernet/amazon/ena/ena_com.c
