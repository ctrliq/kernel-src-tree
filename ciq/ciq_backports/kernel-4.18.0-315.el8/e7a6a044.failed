mt76: testmode: move mtd part to mt76_dev

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-315.el8
commit-author Shayne Chen <shayne.chen@mediatek.com>
commit e7a6a044f9b9a3df3652aed8b91fa479ffad4475
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-315.el8/e7a6a044.failed

Move testmode mtd variables to mt76_dev, since they are the same on
each phy.

	Signed-off-by: Shayne Chen <shayne.chen@mediatek.com>
	Acked-by: Ryder Lee <ryder.lee@mediatek.com>
	Signed-off-by: Felix Fietkau <nbd@nbd.name>
(cherry picked from commit e7a6a044f9b9a3df3652aed8b91fa479ffad4475)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/wireless/mediatek/mt76/eeprom.c
#	drivers/net/wireless/mediatek/mt76/mt76.h
diff --cc drivers/net/wireless/mediatek/mt76/eeprom.c
index 3044e0069991,665b54c5c8ae..000000000000
--- a/drivers/net/wireless/mediatek/mt76/eeprom.c
+++ b/drivers/net/wireless/mediatek/mt76/eeprom.c
@@@ -75,8 -75,8 +75,13 @@@ mt76_get_of_eeprom(struct mt76_dev *dev
  	}
  
  #ifdef CONFIG_NL80211_TESTMODE
++<<<<<<< HEAD
 +	dev->test.mtd_name = devm_kstrdup(dev->dev, part, GFP_KERNEL);
 +	dev->test.mtd_offset = offset;
++=======
+ 	dev->test_mtd.name = devm_kstrdup(dev->dev, part, GFP_KERNEL);
+ 	dev->test_mtd.offset = offset;
++>>>>>>> e7a6a044f9b9 (mt76: testmode: move mtd part to mt76_dev)
  #endif
  
  out_put_node:
diff --cc drivers/net/wireless/mediatek/mt76/mt76.h
index b1381101ffac,10034c21f812..000000000000
--- a/drivers/net/wireless/mediatek/mt76/mt76.h
+++ b/drivers/net/wireless/mediatek/mt76/mt76.h
@@@ -655,9 -662,11 +652,16 @@@ struct mt76_dev 
  
  #ifdef CONFIG_NL80211_TESTMODE
  	const struct mt76_testmode_ops *test_ops;
++<<<<<<< HEAD
 +	struct mt76_testmode_data test;
++=======
+ 	struct {
+ 		const char *name;
+ 		u32 offset;
+ 	} test_mtd;
++>>>>>>> e7a6a044f9b9 (mt76: testmode: move mtd part to mt76_dev)
  #endif
 +
  	struct workqueue_struct *wq;
  
  	union {
* Unmerged path drivers/net/wireless/mediatek/mt76/eeprom.c
* Unmerged path drivers/net/wireless/mediatek/mt76/mt76.h
diff --git a/drivers/net/wireless/mediatek/mt76/testmode.c b/drivers/net/wireless/mediatek/mt76/testmode.c
index d1b171697e28..94ae967d5e81 100644
--- a/drivers/net/wireless/mediatek/mt76/testmode.c
+++ b/drivers/net/wireless/mediatek/mt76/testmode.c
@@ -471,9 +471,9 @@ int mt76_testmode_dump(struct ieee80211_hw *hw, struct sk_buff *msg,
 	if (nla_put_u32(msg, MT76_TM_ATTR_STATE, td->state))
 		goto out;
 
-	if (td->mtd_name &&
-	    (nla_put_string(msg, MT76_TM_ATTR_MTD_PART, td->mtd_name) ||
-	     nla_put_u32(msg, MT76_TM_ATTR_MTD_OFFSET, td->mtd_offset)))
+	if (dev->test_mtd.name &&
+	    (nla_put_string(msg, MT76_TM_ATTR_MTD_PART, dev->test_mtd.name) ||
+	     nla_put_u32(msg, MT76_TM_ATTR_MTD_OFFSET, dev->test_mtd.offset)))
 		goto out;
 
 	if (nla_put_u32(msg, MT76_TM_ATTR_TX_COUNT, td->tx_count) ||
