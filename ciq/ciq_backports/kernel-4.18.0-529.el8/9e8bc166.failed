ACPI: thermal: Drop redundant parens from expressions

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-529.el8
commit-author Rafael J. Wysocki <rafael.j.wysocki@intel.com>
commit 9e8bc16626a007108f2c3496cd48f0eda4d09045
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-529.el8/9e8bc166.failed

Some expressions in the ACPI thermal driver contain redundant
parentheses.  Drop them.

No functional impact.

	Signed-off-by: Rafael J. Wysocki <rafael.j.wysocki@intel.com>
	Acked-by: Daniel Lezcano <daniel.lezcano@linaro.org>
(cherry picked from commit 9e8bc16626a007108f2c3496cd48f0eda4d09045)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/acpi/thermal.c
diff --cc drivers/acpi/thermal.c
index 2e962e067ffa,6dc1d54808b8..000000000000
--- a/drivers/acpi/thermal.c
+++ b/drivers/acpi/thermal.c
@@@ -345,7 -326,7 +345,11 @@@ static int acpi_thermal_trips_update(st
  
  	/* Passive (optional) */
  	if (((flag & ACPI_TRIPS_PASSIVE) && tz->trips.passive.flags.valid) ||
++<<<<<<< HEAD
 +		(flag == ACPI_TRIPS_INIT)) {
++=======
+ 	    flag == ACPI_TRIPS_INIT) {
++>>>>>>> 9e8bc16626a0 (ACPI: thermal: Drop redundant parens from expressions)
  		valid = tz->trips.passive.flags.valid;
  		if (psv == -1) {
  			status = AE_SUPPORT;
@@@ -418,10 -399,10 +422,15 @@@
  		if (act == -1)
  			break; /* disable all active trip points */
  
++<<<<<<< HEAD
 +		if ((flag == ACPI_TRIPS_INIT) || ((flag & ACPI_TRIPS_ACTIVE) &&
 +			tz->trips.active[i].flags.valid)) {
++=======
+ 		if (flag == ACPI_TRIPS_INIT || ((flag & ACPI_TRIPS_ACTIVE) &&
+ 		    tz->trips.active[i].flags.valid)) {
++>>>>>>> 9e8bc16626a0 (ACPI: thermal: Drop redundant parens from expressions)
  			status = acpi_evaluate_integer(tz->device->handle,
 -						       name, NULL, &tmp);
 +							name, NULL, &tmp);
  			if (ACPI_FAILURE(status)) {
  				tz->trips.active[i].flags.valid = 0;
  				if (i == 0)
@@@ -673,9 -654,8 +682,14 @@@ static int thermal_get_trend(struct the
  	 * tz->temperature has already been updated by generic thermal layer,
  	 * before this callback being invoked
  	 */
++<<<<<<< HEAD
 +	i = (tz->trips.passive.tc1 * (tz->temperature - tz->last_temperature))
 +		+ (tz->trips.passive.tc2
 +		* (tz->temperature - tz->trips.passive.temperature));
++=======
+ 	i = tz->trips.passive.tc1 * (tz->temperature - tz->last_temperature) +
+ 	    tz->trips.passive.tc2 * (tz->temperature - tz->trips.passive.temperature);
++>>>>>>> 9e8bc16626a0 (ACPI: thermal: Drop redundant parens from expressions)
  
  	if (i > 0)
  		*trend = THERMAL_TREND_RAISING;
* Unmerged path drivers/acpi/thermal.c
