ACPI: platform: Use sizeof(*pointer) instead of sizeof(type)

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-529.el8
commit-author Andy Shevchenko <andriy.shevchenko@linux.intel.com>
commit 895a4d6ce17b377316ebe7e7090efc1ce9ffd1fe
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-529.el8/895a4d6c.failed

It is preferred to use sizeof(*pointer) instead of sizeof(type).
The type of the variable can change and one needs not change
the former (unlike the latter). No functional change intended.

	Signed-off-by: Andy Shevchenko <andriy.shevchenko@linux.intel.com>
	Signed-off-by: Rafael J. Wysocki <rafael.j.wysocki@intel.com>
(cherry picked from commit 895a4d6ce17b377316ebe7e7090efc1ce9ffd1fe)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/acpi/acpi_platform.c
diff --cc drivers/acpi/acpi_platform.c
index 7b59abf00f5c,5e757b53476a..000000000000
--- a/drivers/acpi/acpi_platform.c
+++ b/drivers/acpi/acpi_platform.c
@@@ -116,13 -113,11 +116,18 @@@ struct platform_device *acpi_create_pla
  
  	INIT_LIST_HEAD(&resource_list);
  	count = acpi_dev_get_resources(adev, &resource_list, NULL, NULL);
 -	if (count < 0)
 +	if (count < 0) {
  		return NULL;
++<<<<<<< HEAD
 +	} else if (count > 0) {
 +		resources = kcalloc(count, sizeof(struct resource),
 +				    GFP_KERNEL);
++=======
+ 	if (count > 0) {
+ 		resources = kcalloc(count, sizeof(*resources), GFP_KERNEL);
++>>>>>>> 895a4d6ce17b (ACPI: platform: Use sizeof(*pointer) instead of sizeof(type))
  		if (!resources) {
 +			dev_err(&adev->dev, "No memory for resources\n");
  			acpi_dev_free_resource_list(&resource_list);
  			return ERR_PTR(-ENOMEM);
  		}
* Unmerged path drivers/acpi/acpi_platform.c
