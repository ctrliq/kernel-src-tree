fs: factor out common code in fget() and fget_raw()

jira LE-1907
cve CVE-2021-4083
Rebuild_History Non-Buildable kernel-3.10.0-1160.62.1.el7
commit-author Oleg Nesterov <oleg@redhat.com>
commit 1deb46e2562561255c34075825fd00f22a858bb3
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-1160.62.1.el7/1deb46e2.failed

Apart from FMODE_PATH check fget() and fget_raw() are identical,
shift the code into the new simple helper, __fget(fd, mask). Saves
160 bytes.

	Signed-off-by: Oleg Nesterov <oleg@redhat.com>
	Signed-off-by: Al Viro <viro@zeniv.linux.org.uk>
(cherry picked from commit 1deb46e2562561255c34075825fd00f22a858bb3)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/file.c
diff --cc fs/file.c
index 44bd634b636a,4ed58a32575e..000000000000
--- a/fs/file.c
+++ b/fs/file.c
@@@ -727,7 -646,8 +727,12 @@@ static struct file *__fget(unsigned in
  	file = fcheck_files(files, fd);
  	if (file) {
  		/* File object ref couldn't be taken */
++<<<<<<< HEAD
 +		if (file->f_mode & FMODE_PATH || !get_file_rcu(file))
++=======
+ 		if ((file->f_mode & mask) ||
+ 		    !atomic_long_inc_not_zero(&file->f_count))
++>>>>>>> 1deb46e25625 (fs: factor out common code in fget() and fget_raw())
  			file = NULL;
  	}
  	rcu_read_unlock();
* Unmerged path fs/file.c
