smb: client: fix potential OOB in cifs_dump_detail()

jira LE-1907
cve CVE-2023-6610
Rebuild_History Non-Buildable kernel-4.18.0-538.el8
commit-author Paulo Alcantara <pc@manguebit.com>
commit b50492b05fd02887b46aef079592207fb5c97a4c
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-538.el8/b50492b0.failed

Validate SMB message with ->check_message() before calling
->calc_smb_size().

	Signed-off-by: Paulo Alcantara (SUSE) <pc@manguebit.com>
	Cc: stable@vger.kernel.org
	Signed-off-by: Steve French <stfrench@microsoft.com>
(cherry picked from commit b50492b05fd02887b46aef079592207fb5c97a4c)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/cifs/cifs_debug.c
diff --cc fs/cifs/cifs_debug.c
index eb4f3a982dfa,60027f5aebe8..000000000000
--- a/fs/cifs/cifs_debug.c
+++ b/fs/cifs/cifs_debug.c
@@@ -51,13 -38,15 +51,23 @@@ cifs_dump_mem(char *label, void *data, 
  void cifs_dump_detail(void *buf, struct TCP_Server_Info *server)
  {
  #ifdef CONFIG_CIFS_DEBUG2
 -	struct smb_hdr *smb = buf;
 -
 +	struct smb_hdr *smb = (struct smb_hdr *)buf;
 +
++<<<<<<< HEAD:fs/cifs/cifs_debug.c
 +	cifs_dbg(VFS, "Cmd: %d Err: 0x%x Flags: 0x%x Flgs2: 0x%x Mid: %d Pid: %d\n",
 +		 smb->Command, smb->Status.CifsError,
 +		 smb->Flags, smb->Flags2, smb->Mid, smb->Pid);
 +	cifs_dbg(VFS, "smb buf %p len %u\n", smb,
 +		 server->ops->calc_smb_size(smb, server));
++=======
+ 	cifs_dbg(VFS, "Cmd: %d Err: 0x%x Flags: 0x%x Flgs2: 0x%x Mid: %d Pid: %d Wct: %d\n",
+ 		 smb->Command, smb->Status.CifsError, smb->Flags,
+ 		 smb->Flags2, smb->Mid, smb->Pid, smb->WordCount);
+ 	if (!server->ops->check_message(buf, server->total_read, server)) {
+ 		cifs_dbg(VFS, "smb buf %p len %u\n", smb,
+ 			 server->ops->calc_smb_size(smb));
+ 	}
++>>>>>>> b50492b05fd0 (smb: client: fix potential OOB in cifs_dump_detail()):fs/smb/client/cifs_debug.c
  #endif /* CONFIG_CIFS_DEBUG2 */
  }
  
* Unmerged path fs/cifs/cifs_debug.c
