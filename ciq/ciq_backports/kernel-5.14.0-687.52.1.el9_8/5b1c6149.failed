smb: client: let smbd_post_send_negotiate_req() use smbd_post_send()

jira KERNEL-1677
Rebuild_History Non-Buildable kernel-5.14.0-687.52.1.el9_8
commit-author Stefan Metzmacher <metze@samba.org>
commit 5b1c6149657af840a02885135c700ab42e6aa322
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-5.14.0-687.52.1.el9_8/5b1c6149.failed

The server has similar logic and it makes sure that
request->wr is used instead of a stack struct ib_send_wr send_wr.

This makes sure send_done can see request->wr.send_flags
as the next commit will check for IB_SEND_SIGNALED

	Cc: <stable@vger.kernel.org> # 6.18.x
	Cc: Steve French <smfrench@gmail.com>
	Cc: Tom Talpey <tom@talpey.com>
	Cc: Long Li <longli@microsoft.com>
	Cc: Namjae Jeon <linkinjeon@kernel.org>
	Cc: linux-cifs@vger.kernel.org
	Cc: samba-technical@lists.samba.org
	Signed-off-by: Stefan Metzmacher <metze@samba.org>
	Signed-off-by: Steve French <stfrench@microsoft.com>
(cherry picked from commit 5b1c6149657af840a02885135c700ab42e6aa322)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/smb/client/smbdirect.c
diff --cc fs/smb/client/smbdirect.c
index 3d6f9a549b6e,88fefb901c27..000000000000
--- a/fs/smb/client/smbdirect.c
+++ b/fs/smb/client/smbdirect.c
@@@ -953,8 -1025,7 +957,12 @@@ out1
  static int smbd_post_send_negotiate_req(struct smbdirect_socket *sc)
  {
  	struct smbdirect_socket_parameters *sp = &sc->parameters;
++<<<<<<< HEAD
 +	struct ib_send_wr send_wr;
 +	int rc = -ENOMEM;
++=======
+ 	int rc;
++>>>>>>> 5b1c6149657a (smb: client: let smbd_post_send_negotiate_req() use smbd_post_send())
  	struct smbdirect_send_io *request;
  	struct smbdirect_negotiate_req *packet;
  
@@@ -1008,16 -1059,11 +998,21 @@@
  	if (!rc)
  		return 0;
  
++<<<<<<< HEAD
 +	/* if we reach here, post send failed */
 +	log_rdma_send(ERR, "ib_post_send failed rc=%d\n", rc);
 +	atomic_dec(&sc->send_io.pending.count);
 +	ib_dma_unmap_single(sc->ib.dev, request->sge[0].addr,
 +		request->sge[0].length, DMA_TO_DEVICE);
 +
 +	smbd_disconnect_rdma_connection(sc);
++=======
+ 	if (rc == -EAGAIN)
+ 		rc = -EIO;
++>>>>>>> 5b1c6149657a (smb: client: let smbd_post_send_negotiate_req() use smbd_post_send())
  
  dma_mapping_failed:
 -	smbd_free_send_io(request);
 +	mempool_free(request, sc->send_io.mem.pool);
  	return rc;
  }
  
* Unmerged path fs/smb/client/smbdirect.c
