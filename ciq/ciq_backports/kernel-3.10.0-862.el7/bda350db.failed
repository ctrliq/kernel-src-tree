iommu/amd: Remove align-parameter from __map_single()

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-862.el7
Rebuild_CHGLOG: - [iommu] amd: Remove align-parameter from __map_single() (Jerry Snitselaar) [1411581]
Rebuild_FUZZ: 94.00%
commit-author Joerg Roedel <jroedel@suse.de>
commit bda350dbdbc1ad8655ece0ec3d41bebc3ee7a77b
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-862.el7/bda350db.failed

This parameter is not required anymore because the
iova-allocations are always aligned to its size.

	Signed-off-by: Joerg Roedel <jroedel@suse.de>
(cherry picked from commit bda350dbdbc1ad8655ece0ec3d41bebc3ee7a77b)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/iommu/amd_iommu.c
diff --cc drivers/iommu/amd_iommu.c
index c9d79e07ea12,d218e35ed3e3..000000000000
--- a/drivers/iommu/amd_iommu.c
+++ b/drivers/iommu/amd_iommu.c
@@@ -2479,12 -2173,7 +2477,16 @@@ static dma_addr_t __map_single(struct d
  	pages = iommu_num_pages(paddr, size, PAGE_SIZE);
  	paddr &= PAGE_MASK;
  
++<<<<<<< HEAD
 +	if (align)
 +		align_mask = (1UL << get_order(size)) - 1;
 +
 +	address = dma_ops_alloc_addresses(dev, dma_dom, pages, align_mask,
 +					  dma_mask);
 +
++=======
+ 	address = dma_ops_alloc_iova(dev, dma_dom, pages, dma_mask);
++>>>>>>> bda350dbdbc1 (iommu/amd: Remove align-parameter from __map_single())
  	if (address == DMA_ERROR_CODE)
  		goto out;
  
* Unmerged path drivers/iommu/amd_iommu.c
