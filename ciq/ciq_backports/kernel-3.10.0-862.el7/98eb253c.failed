vxlan: remove vni zero check and drop for COLLECT_METADATA

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-862.el7
commit-author Roopa Prabhu <roopa@cumulusnetworks.com>
commit 98eb253cbde7e860fad83fa759058269ca69916d
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-862.el7/98eb253c.failed

This patch drops the vni zero check for COLLECT_METADATA mode.
It is not really needed, vni zero is a valid vni.

Fixes: 3ad7a4b141eb ("vxlan: support fdb and learning in COLLECT_METADATA mode"
	Reported-by: Joe Stringer <joe@ovn.org>
	Signed-off-by: Roopa Prabhu <roopa@cumulusnetworks.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 98eb253cbde7e860fad83fa759058269ca69916d)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/vxlan.c
diff --cc drivers/net/vxlan.c
index 2ed0f4915f35,4e27c5b09600..000000000000
--- a/drivers/net/vxlan.c
+++ b/drivers/net/vxlan.c
@@@ -1292,7 -1331,9 +1292,13 @@@ static int vxlan_rcv(struct sock *sk, s
  	if (!vs)
  		goto drop;
  
++<<<<<<< HEAD
 +	vxlan = vxlan_vs_find_vni(vs, vxlan_vni(vxlan_hdr(skb)->vx_vni));
++=======
+ 	vni = vxlan_vni(vxlan_hdr(skb)->vx_vni);
+ 
+ 	vxlan = vxlan_vs_find_vni(vs, vni);
++>>>>>>> 98eb253cbde7 (vxlan: remove vni zero check and drop for COLLECT_METADATA)
  	if (!vxlan)
  		goto drop;
  
* Unmerged path drivers/net/vxlan.c
