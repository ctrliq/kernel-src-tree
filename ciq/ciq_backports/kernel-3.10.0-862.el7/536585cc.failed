NFSv4: Don't check file access when reclaiming state

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-862.el7
commit-author Trond Myklebust <trond.myklebust@primarydata.com>
commit 536585ccf95d34d64e2855f72608095854b8fc0e
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-862.el7/536585cc.failed

If we're reclaiming state after a reboot, or as part of returning a
delegation, we don't need to check access modes again.

	Signed-off-by: Trond Myklebust <trond.myklebust@primarydata.com>
(cherry picked from commit 536585ccf95d34d64e2855f72608095854b8fc0e)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/nfs/nfs4proc.c
diff --cc fs/nfs/nfs4proc.c
index 3b8bf7e61088,b582df89c083..000000000000
--- a/fs/nfs/nfs4proc.c
+++ b/fs/nfs/nfs4proc.c
@@@ -1216,7 -1221,7 +1216,11 @@@ static struct nfs4_opendata *nfs4_opend
  	atomic_inc(&sp->so_count);
  	p->o_arg.open_flags = flags;
  	p->o_arg.fmode = fmode & (FMODE_READ|FMODE_WRITE);
++<<<<<<< HEAD
 +	p->o_arg.umask = current_umask();
++=======
+ 	p->o_arg.claim = nfs4_map_atomic_open_claim(server, claim);
++>>>>>>> 536585ccf95d (NFSv4: Don't check file access when reclaiming state)
  	p->o_arg.share_access = nfs4_map_atomic_open_share(server,
  			fmode, flags);
  	/* don't put an ACCESS op in OPEN compound if O_EXCL, because ACCESS
* Unmerged path fs/nfs/nfs4proc.c
