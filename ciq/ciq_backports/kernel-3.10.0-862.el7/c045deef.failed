net/mlx5e: Use kernel types instead of uint*_t in ethtool callbacks

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-862.el7
Rebuild_CHGLOG: - [netdrv] mlx5e: Use kernel types instead of uint*_t in ethtool callbacks (Kamal Heib) [1456694]
Rebuild_FUZZ: 96.92%
commit-author Gal Pressman <galp@mellanox.com>
commit c045deef64f389f40f135c71b2ded3405c989fba
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-862.el7/c045deef.failed

Fix checkpatch errors:
CHECK:PREFER_KERNEL_TYPES: Prefer kernel type 'u32' over 'uint32_t'

	Signed-off-by: Gal Pressman <galp@mellanox.com>
	Signed-off-by: Saeed Mahameed <saeedm@mellanox.com>
(cherry picked from commit c045deef64f389f40f135c71b2ded3405c989fba)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/mellanox/mlx5/core/en_ethtool.c
#	drivers/net/ethernet/mellanox/mlx5/core/ipoib/ethtool.c
diff --cc drivers/net/ethernet/mellanox/mlx5/core/en_ethtool.c
index a12add73652e,c30cf6b4736f..000000000000
--- a/drivers/net/ethernet/mellanox/mlx5/core/en_ethtool.c
+++ b/drivers/net/ethernet/mellanox/mlx5/core/en_ethtool.c
@@@ -195,7 -199,14 +195,18 @@@ static int mlx5e_get_sset_count(struct 
  	}
  }
  
++<<<<<<< HEAD
 +static void mlx5e_fill_stats_strings(struct mlx5e_priv *priv, uint8_t *data)
++=======
+ static int mlx5e_get_sset_count(struct net_device *dev, int sset)
+ {
+ 	struct mlx5e_priv *priv = netdev_priv(dev);
+ 
+ 	return mlx5e_ethtool_get_sset_count(priv, sset);
+ }
+ 
+ static void mlx5e_fill_stats_strings(struct mlx5e_priv *priv, u8 *data)
++>>>>>>> c045deef64f3 (net/mlx5e: Use kernel types instead of uint*_t in ethtool callbacks)
  {
  	int i, j, tc, prio, idx = 0;
  	unsigned long pfc_combined;
@@@ -287,13 -305,11 +298,17 @@@
  			for (j = 0; j < NUM_SQ_STATS; j++)
  				sprintf(data + (idx++) * ETH_GSTRING_LEN,
  					sq_stats_desc[j].format,
 -					priv->channel_tc2txq[i][tc]);
 +					priv->channeltc_to_txq_map[i][tc]);
  }
  
++<<<<<<< HEAD
 +static void mlx5e_get_strings(struct net_device *dev,
 +			      uint32_t stringset, uint8_t *data)
++=======
+ void mlx5e_ethtool_get_strings(struct mlx5e_priv *priv, u32 stringset, u8 *data)
++>>>>>>> c045deef64f3 (net/mlx5e: Use kernel types instead of uint*_t in ethtool callbacks)
  {
 +	struct mlx5e_priv *priv = netdev_priv(dev);
  	int i;
  
  	switch (stringset) {
@@@ -314,10 -330,17 +329,14 @@@
  	}
  }
  
++<<<<<<< HEAD
 +static void mlx5e_get_ethtool_stats(struct net_device *dev,
 +				    struct ethtool_stats *stats, u64 *data)
++=======
+ static void mlx5e_get_strings(struct net_device *dev, u32 stringset, u8 *data)
++>>>>>>> c045deef64f3 (net/mlx5e: Use kernel types instead of uint*_t in ethtool callbacks)
  {
  	struct mlx5e_priv *priv = netdev_priv(dev);
 -
 -	mlx5e_ethtool_get_strings(priv, stringset, data);
 -}
 -
 -void mlx5e_ethtool_get_ethtool_stats(struct mlx5e_priv *priv,
 -				     struct ethtool_stats *stats, u64 *data)
 -{
 -	struct mlx5e_channels *channels;
  	struct mlx5_priv *mlx5_priv;
  	int i, j, tc, prio, idx = 0;
  	unsigned long pfc_combined;
* Unmerged path drivers/net/ethernet/mellanox/mlx5/core/ipoib/ethtool.c
* Unmerged path drivers/net/ethernet/mellanox/mlx5/core/en_ethtool.c
* Unmerged path drivers/net/ethernet/mellanox/mlx5/core/ipoib/ethtool.c
