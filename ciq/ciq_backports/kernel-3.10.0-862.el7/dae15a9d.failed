ALSA: x86: Move dma_mask debug print into intel_hdmi_lpe_audio.c

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-862.el7
commit-author Takashi Iwai <tiwai@suse.de>
commit dae15a9d960e513d5b60c00c306cddd87465f1c4
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-862.el7/dae15a9d.failed

It belongs to the right place.

And, remove a few sanity checks (e.g. NULL card) and debug prints as
well.

	Signed-off-by: Takashi Iwai <tiwai@suse.de>
(cherry picked from commit dae15a9d960e513d5b60c00c306cddd87465f1c4)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	sound/x86/intel_hdmi_lpe_audio.c
diff --cc sound/x86/intel_hdmi_lpe_audio.c
index 1d23ff38e96f,6f823d4278bb..000000000000
--- a/sound/x86/intel_hdmi_lpe_audio.c
+++ b/sound/x86/intel_hdmi_lpe_audio.c
@@@ -348,11 -314,8 +348,16 @@@ static int hdmi_lpe_audio_probe(struct 
  		{}
  	};
  
++<<<<<<< HEAD
 +	dev_dbg(&hlpe_pdev->dev, "Enter %s\n", __func__);
 +
 +	/*TBD:remove globals*/
 +	hlpe_pdev = pdev;
 +	hlpe_state = hdmi_connector_status_disconnected;
++=======
+ 	dev_dbg(&pdev->dev, "Enter %s\n", __func__);
+ 	dev_dbg(&pdev->dev, "dma_mask: %p\n", pdev->dev.dma_mask);
++>>>>>>> dae15a9d960e (ALSA: x86: Move dma_mask debug print into intel_hdmi_lpe_audio.c)
  
  	/* get resources */
  	irq = platform_get_irq(pdev, 0);
diff --git a/sound/x86/intel_hdmi_audio.c b/sound/x86/intel_hdmi_audio.c
index f2f54691cb43..dbbdbeb415a5 100644
--- a/sound/x86/intel_hdmi_audio.c
+++ b/sound/x86/intel_hdmi_audio.c
@@ -1595,8 +1595,6 @@ int hdmi_audio_probe(struct platform_device *devptr,
 
 	pr_debug("Enter %s\n", __func__);
 
-	pr_debug("hdmi_audio_probe dma_mask: %p\n", devptr->dev.dma_mask);
-
 	/* allocate memory for saving internal context and working */
 	intelhaddata = kzalloc(sizeof(*intelhaddata), GFP_KERNEL);
 	if (!intelhaddata)
@@ -1648,15 +1646,6 @@ int hdmi_audio_probe(struct platform_device *devptr,
 	retval = snd_pcm_lib_preallocate_pages_for_all(pcm,
 			SNDRV_DMA_TYPE_DEV, NULL,
 			HAD_MAX_BUFFER, HAD_MAX_BUFFER);
-
-	if (card->dev == NULL)
-		pr_debug("card->dev is NULL!!!!! Should not be this case\n");
-	else if (card->dev->dma_mask == NULL)
-		pr_debug("hdmi_audio_probe dma_mask is NULL!!!!!\n");
-	else
-		pr_debug("hdmi_audio_probe dma_mask is : %p\n",
-				card->dev->dma_mask);
-
 	if (retval)
 		goto err;
 
* Unmerged path sound/x86/intel_hdmi_lpe_audio.c
