qede: Update receive statistic once per NAPI

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-862.el7
commit-author Mintz, Yuval <Yuval.Mintz@cavium.com>
commit 10a0176e4e6eb6243c4b1c55e50372e03139d592
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-862.el7/10a0176e.failed

Currently, each time an ingress packet is passed to networking stack
the driver increments a per-queue SW statistic.
As we want to have additional fields in the first cache-line of the
Rx-queue struct, change flow so this statistic would be updated once per
NAPI run. We will later push the statistic to a different cache line.

	Signed-off-by: Yuval Mintz <Yuval.Mintz@cavium.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 10a0176e4e6eb6243c4b1c55e50372e03139d592)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/qlogic/qede/qede_fp.c
diff --cc drivers/net/ethernet/qlogic/qede/qede_fp.c
index 798f7f869b6b,c77e6972ab48..000000000000
--- a/drivers/net/ethernet/qlogic/qede/qede_fp.c
+++ b/drivers/net/ethernet/qlogic/qede/qede_fp.c
@@@ -1099,6 -1225,10 +1098,13 @@@ static int qede_rx_process_cqe(struct q
  	len = le16_to_cpu(fp_cqe->len_on_first_bd);
  	pad = fp_cqe->placement_offset;
  
++<<<<<<< HEAD
++=======
+ 	/* Run eBPF program if one is attached */
+ 	if (xdp_prog)
+ 		if (!qede_rx_xdp(edev, fp, rxq, xdp_prog, bd, fp_cqe))
+ 			return 0;
++>>>>>>> 10a0176e4e6e (qede: Update receive statistic once per NAPI)
  
  	/* If this is an error packet then drop it */
  	flags = cqe->fast_path_regular.pars_flags.flags;
* Unmerged path drivers/net/ethernet/qlogic/qede/qede_fp.c
