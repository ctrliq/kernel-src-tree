params: improve standard definitions

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-862.el7
commit-author Felipe Contreras <felipe.contreras@gmail.com>
commit 88a88b320a9068294aaa2841464e4809af2ff454
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-862.el7/88a88b32.failed

We are repeating the functionality of kstrtol in param_set_long, and the
same for kstrtoint. We can get rid of the extra code by using the right
functions.

	Signed-off-by: Felipe Contreras <felipe.contreras@gmail.com>
	Signed-off-by: Rusty Russell <rusty@rustcorp.com.au>
(cherry picked from commit 88a88b320a9068294aaa2841464e4809af2ff454)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	kernel/params.c
diff --cc kernel/params.c
index e9578f496fbc,b00142e7f3ba..000000000000
--- a/kernel/params.c
+++ b/kernel/params.c
@@@ -200,13 -246,13 +193,23 @@@ int parse_args(const char *doing
  	EXPORT_SYMBOL(param_ops_##name)
  
  
++<<<<<<< HEAD
 +STANDARD_PARAM_DEF(byte, unsigned char, "%hhu", unsigned long, strict_strtoul);
 +STANDARD_PARAM_DEF(short, short, "%hi", long, strict_strtol);
 +STANDARD_PARAM_DEF(ushort, unsigned short, "%hu", unsigned long, strict_strtoul);
 +STANDARD_PARAM_DEF(int, int, "%i", long, strict_strtol);
 +STANDARD_PARAM_DEF(uint, unsigned int, "%u", unsigned long, strict_strtoul);
 +STANDARD_PARAM_DEF(long, long, "%li", long, strict_strtol);
 +STANDARD_PARAM_DEF(ulong, unsigned long, "%lu", unsigned long, strict_strtoul);
++=======
+ STANDARD_PARAM_DEF(byte, unsigned char, "%hhu", kstrtou8);
+ STANDARD_PARAM_DEF(short, short, "%hi", kstrtos16);
+ STANDARD_PARAM_DEF(ushort, unsigned short, "%hu", kstrtou16);
+ STANDARD_PARAM_DEF(int, int, "%i", kstrtoint);
+ STANDARD_PARAM_DEF(uint, unsigned int, "%u", kstrtouint);
+ STANDARD_PARAM_DEF(long, long, "%li", kstrtol);
+ STANDARD_PARAM_DEF(ulong, unsigned long, "%lu", kstrtoul);
++>>>>>>> 88a88b320a90 (params: improve standard definitions)
  
  int param_set_charp(const char *val, const struct kernel_param *kp)
  {
* Unmerged path kernel/params.c
