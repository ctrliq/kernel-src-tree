kvm: x86: make function static to avoid compiling warning

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-862.el7
commit-author Jiang Biao <jiang.biao2@zte.com.cn>
commit 33365e7a454fd40d30d11bf0d9147e059b22e099
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-862.el7/33365e7a.failed

vmx_arm_hv_timer is only used in vmx.c, and should be static to
avoid compiling warning when with -Wmissing-prototypes option.

	Signed-off-by: Jiang Biao <jiang.biao2@zte.com.cn>
	Signed-off-by: Paolo Bonzini <pbonzini@redhat.com>
(cherry picked from commit 33365e7a454fd40d30d11bf0d9147e059b22e099)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	arch/x86/kvm/vmx.c
diff --cc arch/x86/kvm/vmx.c
index 04ec8bdd2903,f81a8e44ae52..000000000000
--- a/arch/x86/kvm/vmx.c
+++ b/arch/x86/kvm/vmx.c
@@@ -8515,6 -8709,26 +8515,29 @@@ static void atomic_switch_perf_msrs(str
  					msrs[i].host);
  }
  
++<<<<<<< HEAD
++=======
+ static void vmx_arm_hv_timer(struct kvm_vcpu *vcpu)
+ {
+ 	struct vcpu_vmx *vmx = to_vmx(vcpu);
+ 	u64 tscl;
+ 	u32 delta_tsc;
+ 
+ 	if (vmx->hv_deadline_tsc == -1)
+ 		return;
+ 
+ 	tscl = rdtsc();
+ 	if (vmx->hv_deadline_tsc > tscl)
+ 		/* sure to be 32 bit only because checked on set_hv_timer */
+ 		delta_tsc = (u32)((vmx->hv_deadline_tsc - tscl) >>
+ 			cpu_preemption_timer_multi);
+ 	else
+ 		delta_tsc = 0;
+ 
+ 	vmcs_write32(VMX_PREEMPTION_TIMER_VALUE, delta_tsc);
+ }
+ 
++>>>>>>> 33365e7a454f (kvm: x86: make function static to avoid compiling warning)
  static void __noclone vmx_vcpu_run(struct kvm_vcpu *vcpu)
  {
  	struct vcpu_vmx *vmx = to_vmx(vcpu);
* Unmerged path arch/x86/kvm/vmx.c
