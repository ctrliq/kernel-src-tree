ibmvnic: Convert vnic server reported statistics to cpu endian

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-862.el7
commit-author John Allen <jallen@linux.vnet.ibm.com>
commit 52da5c114a884f8245dca5d3a69e1f45c759ba31
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-862.el7/52da5c11.failed

The vnic server reports the statistics buffer in big endian format and must
be converted to cpu endian in order to be displayed correctly on little
endian lpars.

	Signed-off-by: John Allen <jallen@linux.vnet.ibm.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 52da5c114a884f8245dca5d3a69e1f45c759ba31)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/ibm/ibmvnic.c
diff --cc drivers/net/ethernet/ibm/ibmvnic.c
index e2daa771f3cf,ea0ff2841a1f..000000000000
--- a/drivers/net/ethernet/ibm/ibmvnic.c
+++ b/drivers/net/ethernet/ibm/ibmvnic.c
@@@ -1234,7 -1807,26 +1234,30 @@@ static void ibmvnic_get_ethtool_stats(s
  	wait_for_completion(&adapter->stats_done);
  
  	for (i = 0; i < ARRAY_SIZE(ibmvnic_stats); i++)
++<<<<<<< HEAD
 +		data[i] = IBMVNIC_GET_STAT(adapter, ibmvnic_stats[i].offset);
++=======
+ 		data[i] = be64_to_cpu(IBMVNIC_GET_STAT(adapter,
+ 						ibmvnic_stats[i].offset));
+ 
+ 	for (j = 0; j < adapter->req_tx_queues; j++) {
+ 		data[i] = adapter->tx_stats_buffers[j].packets;
+ 		i++;
+ 		data[i] = adapter->tx_stats_buffers[j].bytes;
+ 		i++;
+ 		data[i] = adapter->tx_stats_buffers[j].dropped_packets;
+ 		i++;
+ 	}
+ 
+ 	for (j = 0; j < adapter->req_rx_queues; j++) {
+ 		data[i] = adapter->rx_stats_buffers[j].packets;
+ 		i++;
+ 		data[i] = adapter->rx_stats_buffers[j].bytes;
+ 		i++;
+ 		data[i] = adapter->rx_stats_buffers[j].interrupts;
+ 		i++;
+ 	}
++>>>>>>> 52da5c114a88 (ibmvnic: Convert vnic server reported statistics to cpu endian)
  }
  
  static const struct ethtool_ops ibmvnic_ethtool_ops = {
* Unmerged path drivers/net/ethernet/ibm/ibmvnic.c
