sch_dsmark: use correct func name in print messages

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-862.el7
commit-author Yang Yingliang <yangyingliang@huawei.com>
commit c76f2a2c4c348e2de94b7ba10fcd3640a35d04fa
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-862.el7/c76f2a2c.failed

In dsmark_drop(), the function name printed by pr_debug
is "dsmark_reset", correct it to "dsmark_drop" by using
__func__ .

BTW, replace the other function names with __func__ .

	Signed-off-by: Yang Yingliang <yangyingliang@huawei.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit c76f2a2c4c348e2de94b7ba10fcd3640a35d04fa)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/sched/sch_dsmark.c
diff --cc net/sched/sch_dsmark.c
index aa6f17b4c579,49d6ef338b55..000000000000
--- a/net/sched/sch_dsmark.c
+++ b/net/sched/sch_dsmark.c
@@@ -193,10 -200,10 +194,10 @@@ static int dsmark_enqueue(struct sk_buf
  	struct dsmark_qdisc_data *p = qdisc_priv(sch);
  	int err;
  
- 	pr_debug("dsmark_enqueue(skb %p,sch %p,[qdisc %p])\n", skb, sch, p);
+ 	pr_debug("%s(skb %p,sch %p,[qdisc %p])\n", __func__, skb, sch, p);
  
  	if (p->set_tc_index) {
 -		switch (skb->protocol) {
 +		switch (tc_skb_protocol(skb)) {
  		case htons(ETH_P_IP):
  			if (skb_cow_head(skb, sizeof(struct iphdr)))
  				goto drop;
@@@ -301,7 -305,7 +302,11 @@@ static struct sk_buff *dsmark_dequeue(s
  		 */
  		if (p->mask[index] != 0xff || p->value[index])
  			pr_warn("%s: unsupported protocol %d\n",
++<<<<<<< HEAD
 +				__func__, ntohs(tc_skb_protocol(skb)));
++=======
+ 				__func__, ntohs(skb->protocol));
++>>>>>>> c76f2a2c4c34 (sch_dsmark: use correct func name in print messages)
  		break;
  	}
  
@@@ -392,9 -396,8 +397,9 @@@ static void dsmark_reset(struct Qdisc *
  {
  	struct dsmark_qdisc_data *p = qdisc_priv(sch);
  
- 	pr_debug("dsmark_reset(sch %p,[qdisc %p])\n", sch, p);
+ 	pr_debug("%s(sch %p,[qdisc %p])\n", __func__, sch, p);
  	qdisc_reset(p->q);
 +	sch->qstats.backlog = 0;
  	sch->q.qlen = 0;
  }
  
* Unmerged path net/sched/sch_dsmark.c
