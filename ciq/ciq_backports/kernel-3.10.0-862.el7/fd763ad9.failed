netvsc: remove bogus rtnl_unlock

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-862.el7
commit-author stephen hemminger <stephen@networkplumber.org>
commit fd763ad96ae5a4aa76ce00b4090b6044c63c18ed
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-862.el7/fd763ad9.failed

Remove accidental rtnl_unlock from earlier testing.

Fixes: 3962981f4822 ("netvsc: add rtnl annotations in rndis")
	Signed-off-by: Stephen Hemminger <sthemmin@microsoft.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit fd763ad96ae5a4aa76ce00b4090b6044c63c18ed)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/hyperv/netvsc_drv.c
diff --cc drivers/net/hyperv/netvsc_drv.c
index d4dc9c189b85,70b7cc37103c..000000000000
--- a/drivers/net/hyperv/netvsc_drv.c
+++ b/drivers/net/hyperv/netvsc_drv.c
@@@ -1541,10 -1540,23 +1541,22 @@@ static int netvsc_probe(struct hv_devic
  	}
  	memcpy(net->dev_addr, device_info.mac_adr, ETH_ALEN);
  
 -	/* hw_features computed in rndis_filter_device_add */
 -	net->features = net->hw_features |
 -		NETIF_F_HIGHDMA | NETIF_F_SG |
 -		NETIF_F_HW_VLAN_CTAG_TX | NETIF_F_HW_VLAN_CTAG_RX;
 -	net->vlan_features = net->features;
 -
 +	nvdev = net_device_ctx->nvdev;
  	netif_set_real_num_tx_queues(net, nvdev->num_chn);
  	netif_set_real_num_rx_queues(net, nvdev->num_chn);
++<<<<<<< HEAD
 +	netif_set_gso_max_size(net, NETVSC_GSO_MAX_SIZE);
++=======
+ 
+ 	netdev_lockdep_set_classes(net);
+ 
+ 	/* MTU range: 68 - 1500 or 65521 */
+ 	net->min_mtu = NETVSC_MTU_MIN;
+ 	if (nvdev->nvsp_version >= NVSP_PROTOCOL_VERSION_2)
+ 		net->max_mtu = NETVSC_MTU - ETH_HLEN;
+ 	else
+ 		net->max_mtu = ETH_DATA_LEN;
++>>>>>>> fd763ad96ae5 (netvsc: remove bogus rtnl_unlock)
  
  	ret = register_netdev(net);
  	if (ret != 0) {
* Unmerged path drivers/net/hyperv/netvsc_drv.c
