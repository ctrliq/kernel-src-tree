net_sched: remove an unnecessary list_del()

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-862.el7
commit-author WANG Cong <xiyou.wangcong@gmail.com>
commit 824a7e8863b3eb283343f891b11a782b4ec0d0de
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-862.el7/824a7e88.failed

This list_del() for tc action is not needed actually,
because we only use this list to chain bulk operations,
therefore should not be carried for latter operations.

Fixes: ec0595cc4495 ("net_sched: get rid of struct tcf_common")
	Cc: Jamal Hadi Salim <jhs@mojatatu.com>
	Signed-off-by: Cong Wang <xiyou.wangcong@gmail.com>
	Acked-by: Jamal Hadi Salim <jhs@mojatatu.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 824a7e8863b3eb283343f891b11a782b4ec0d0de)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/sched/act_api.c
diff --cc net/sched/act_api.c
index 9d43de892233,b4c7be38b632..000000000000
--- a/net/sched/act_api.c
+++ b/net/sched/act_api.c
@@@ -67,15 -56,15 +67,23 @@@ int __tcf_hash_release(struct tc_actio
  
  	if (p) {
  		if (bind)
 -			p->tcfa_bindcnt--;
 -		else if (strict && p->tcfa_bindcnt > 0)
 +			p->tcfc_bindcnt--;
 +		else if (strict && p->tcfc_bindcnt > 0)
  			return -EPERM;
  
++<<<<<<< HEAD
 +		p->tcfc_refcnt--;
 +		if (p->tcfc_bindcnt <= 0 && p->tcfc_refcnt <= 0) {
 +			if (a->ops->cleanup)
 +				a->ops->cleanup(a, bind);
 +			tcf_hash_destroy(a);
++=======
+ 		p->tcfa_refcnt--;
+ 		if (p->tcfa_bindcnt <= 0 && p->tcfa_refcnt <= 0) {
+ 			if (p->ops->cleanup)
+ 				p->ops->cleanup(p, bind);
+ 			tcf_hash_destroy(p->hinfo, p);
++>>>>>>> 824a7e8863b3 (net_sched: remove an unnecessary list_del())
  			ret = ACT_P_DELETED;
  		}
  	}
* Unmerged path net/sched/act_api.c
