net/mlx5e: Fix setup TC ndo

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-862.el7
Rebuild_CHGLOG: - [netdrv] mlx5e: Fix setup TC ndo (Don Dutile) [1456694 1499362]
Rebuild_FUZZ: 92.00%
commit-author Saeed Mahameed <saeedm@mellanox.com>
commit 20b6a1c78280dbeb45214c463cf9cbccb3665146
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-862.el7/20b6a1c7.failed

Fail-safe support patches introduced a trivial bug,
setup tc callback is doing a wrong check of the netdevice state,
the fix is simply to invert the condition.

Fixes: 6f9485af4020 ("net/mlx5e: Fail safe tc setup")
	Signed-off-by: Saeed Mahameed <saeedm@mellanox.com>
(cherry picked from commit 20b6a1c78280dbeb45214c463cf9cbccb3665146)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/mellanox/mlx5/core/en_main.c
diff --cc drivers/net/ethernet/mellanox/mlx5/core/en_main.c
index fc92406a15c4,41cd22a223dc..000000000000
--- a/drivers/net/ethernet/mellanox/mlx5/core/en_main.c
+++ b/drivers/net/ethernet/mellanox/mlx5/core/en_main.c
@@@ -2593,17 -2973,21 +2593,24 @@@ static int mlx5e_setup_tc(struct net_de
  
  	mutex_lock(&priv->state_lock);
  
 -	new_channels.params = priv->channels.params;
 -	new_channels.params.num_tc = tc ? tc : 1;
 +	was_opened = test_bit(MLX5E_STATE_OPENED, &priv->state);
 +	if (was_opened)
 +		mlx5e_close_locked(priv->netdev);
  
++<<<<<<< HEAD
 +	priv->params.num_tc = tc ? tc : 1;
++=======
+ 	if (!test_bit(MLX5E_STATE_OPENED, &priv->state)) {
+ 		priv->channels.params = new_channels.params;
+ 		goto out;
+ 	}
++>>>>>>> 20b6a1c78280 (net/mlx5e: Fix setup TC ndo)
  
 -	err = mlx5e_open_channels(priv, &new_channels);
 -	if (err)
 -		goto out;
 +	if (was_opened)
 +		err = mlx5e_open_locked(priv->netdev);
  
 -	mlx5e_switch_priv_channels(priv, &new_channels, NULL);
 -out:
  	mutex_unlock(&priv->state_lock);
 +
  	return err;
  }
  
* Unmerged path drivers/net/ethernet/mellanox/mlx5/core/en_main.c
