net: report right mtu value in error message

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-862.el7
Rebuild_CHGLOG: - [net] report right mtu value in error message (Ivan Vecera) [1480542]
Rebuild_FUZZ: 93.98%
commit-author Jakub Kicinski <jakub.kicinski@netronome.com>
commit a0e65de71527477b100afe1e9ab2667d70f94d9e
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-862.el7/a0e65de7.failed

Check is for max_mtu but message reports min_mtu.

	Signed-off-by: Jakub Kicinski <jakub.kicinski@netronome.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit a0e65de71527477b100afe1e9ab2667d70f94d9e)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/core/dev.c
diff --cc net/core/dev.c
index 0c9178ce691d,352e98129601..000000000000
--- a/net/core/dev.c
+++ b/net/core/dev.c
@@@ -6556,9 -6363,18 +6556,19 @@@ int dev_set_mtu(struct net_device *dev
  	if (new_mtu == dev->mtu)
  		return 0;
  
 -	/* MTU must be positive, and in range */
 -	if (new_mtu < 0 || new_mtu < dev->min_mtu) {
 -		net_err_ratelimited("%s: Invalid MTU %d requested, hw min %d\n",
 -				    dev->name, new_mtu, dev->min_mtu);
 +	/*	MTU must be positive.	 */
 +	if (new_mtu < 0)
  		return -EINVAL;
++<<<<<<< HEAD
++=======
+ 	}
+ 
+ 	if (dev->max_mtu > 0 && new_mtu > dev->max_mtu) {
+ 		net_err_ratelimited("%s: Invalid MTU %d requested, hw max %d\n",
+ 				    dev->name, new_mtu, dev->max_mtu);
+ 		return -EINVAL;
+ 	}
++>>>>>>> a0e65de71527 (net: report right mtu value in error message)
  
  	if (!netif_device_present(dev))
  		return -ENODEV;
* Unmerged path net/core/dev.c
