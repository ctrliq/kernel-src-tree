dm raid: use rs_is_raid*()

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-862.el7
commit-author Heinz Mauelshagen <heinzm@redhat.com>
commit 552aa679f265743163fb440c61370a9c51f66c81
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-862.el7/552aa679.failed

Cleanup, no functional change.

	Signed-off-by: Heinz Mauelshagen <heinzm@redhat.com>
	Signed-off-by: Mike Snitzer <snitzer@redhat.com>
(cherry picked from commit 552aa679f265743163fb440c61370a9c51f66c81)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/md/dm-raid.c
diff --cc drivers/md/dm-raid.c
index 94db8c87956d,7d7dc1723180..000000000000
--- a/drivers/md/dm-raid.c
+++ b/drivers/md/dm-raid.c
@@@ -2886,11 -2974,10 +2886,18 @@@ static void configure_discard_support(s
  	bool raid456;
  	struct dm_target *ti = rs->ti;
  
++<<<<<<< HEAD
 +	/* Assume discards not supported until after checks below. */
 +	ti->discards_supported = false;
 +
 +	/* RAID level 4,5,6 require discard_zeroes_data for data integrity! */
 +	raid456 = (rs->md.level == 4 || rs->md.level == 5 || rs->md.level == 6);
++=======
+ 	/*
+ 	 * XXX: RAID level 4,5,6 require zeroing for safety.
+ 	 */
+ 	raid456 = rs_is_raid456(rs);
++>>>>>>> 552aa679f265 (dm raid: use rs_is_raid*())
  
  	for (i = 0; i < rs->raid_disks; i++) {
  		struct request_queue *q;
* Unmerged path drivers/md/dm-raid.c
