hv_netvsc: Fix rndis_filter_close error during netvsc_remove

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-862.el7
commit-author Haiyang Zhang <haiyangz@microsoft.com>
commit c6f71c418fcc46f59fc87ac93ce9336c12d7898b
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-862.el7/c6f71c41.failed

We now remove rndis filter before unregister_netdev(), which calls
device close. It involves closing rndis filter already removed.

This patch fixes this error.

	Signed-off-by: Haiyang Zhang <haiyangz@microsoft.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit c6f71c418fcc46f59fc87ac93ce9336c12d7898b)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/hyperv/netvsc_drv.c
diff --cc drivers/net/hyperv/netvsc_drv.c
index 5103dca8a92e,fac44c5c8d0d..000000000000
--- a/drivers/net/hyperv/netvsc_drv.c
+++ b/drivers/net/hyperv/netvsc_drv.c
@@@ -130,15 -116,19 +130,29 @@@ static int netvsc_open(struct net_devic
  static int netvsc_close(struct net_device *net)
  {
  	struct net_device_context *net_device_ctx = netdev_priv(net);
++<<<<<<< HEAD
 +	struct netvsc_device *nvdev = net_device_ctx->nvdev;
 +	int ret;
++=======
+ 	struct net_device *vf_netdev
+ 		= rtnl_dereference(net_device_ctx->vf_netdev);
+ 	struct netvsc_device *nvdev = rtnl_dereference(net_device_ctx->nvdev);
+ 	int ret = 0;
++>>>>>>> c6f71c418fcc (hv_netvsc: Fix rndis_filter_close error during netvsc_remove)
  	u32 aread, i, msec = 10, retry = 0, retry_max = 20;
  	struct vmbus_channel *chn;
  
  	netif_tx_disable(net);
  
++<<<<<<< HEAD
 +	/* Make sure netvsc_set_multicast_list doesn't re-enable filter! */
 +	cancel_work_sync(&net_device_ctx->work);
++=======
+ 	/* No need to close rndis filter if it is removed already */
+ 	if (!nvdev)
+ 		goto out;
+ 
++>>>>>>> c6f71c418fcc (hv_netvsc: Fix rndis_filter_close error during netvsc_remove)
  	ret = rndis_filter_close(nvdev);
  	if (ret != 0) {
  		netdev_err(net, "unable to close device (ret %d).\n", ret);
@@@ -177,6 -167,10 +191,13 @@@
  		ret = -ETIMEDOUT;
  	}
  
++<<<<<<< HEAD
++=======
+ out:
+ 	if (vf_netdev)
+ 		dev_close(vf_netdev);
+ 
++>>>>>>> c6f71c418fcc (hv_netvsc: Fix rndis_filter_close error during netvsc_remove)
  	return ret;
  }
  
* Unmerged path drivers/net/hyperv/netvsc_drv.c
