net_sched: always reset qdisc backlog in qdisc_reset()

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-862.el7
commit-author Konstantin Khlebnikov <khlebnikov@yandex-team.ru>
commit c8e1812960eeae42e2183154927028511c4bc566
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-862.el7/c8e18129.failed

SKB stored in qdisc->gso_skb also counted into backlog.

Some qdiscs don't reset backlog to zero in ->reset(),
for example sfq just dequeue and free all queued skb.

	Signed-off-by: Konstantin Khlebnikov <khlebnikov@yandex-team.ru>
Fixes: 2ccccf5fb43f ("net_sched: update hierarchical backlog too")
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit c8e1812960eeae42e2183154927028511c4bc566)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/sched/sch_generic.c
diff --cc net/sched/sch_generic.c
index 57cbfa322683,bf8c81e07c70..000000000000
--- a/net/sched/sch_generic.c
+++ b/net/sched/sch_generic.c
@@@ -654,8 -680,12 +654,13 @@@ void qdisc_reset(struct Qdisc *qdisc
  	if (qdisc->gso_skb) {
  		kfree_skb_list(qdisc->gso_skb);
  		qdisc->gso_skb = NULL;
 +		qdisc->q.qlen = 0;
  	}
++<<<<<<< HEAD
++=======
+ 	qdisc->q.qlen = 0;
+ 	qdisc->qstats.backlog = 0;
++>>>>>>> c8e1812960ee (net_sched: always reset qdisc backlog in qdisc_reset())
  }
  EXPORT_SYMBOL(qdisc_reset);
  
* Unmerged path net/sched/sch_generic.c
