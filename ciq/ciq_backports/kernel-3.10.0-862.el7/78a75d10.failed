dm raid: small cleanup and remove unsed "struct raid_set" member

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-862.el7
Rebuild_CHGLOG: - [md] dm raid: small cleanup and remove unsed struct raid_set member (Heinz Mauelshagen) [1388632]
Rebuild_FUZZ: 98.41%
commit-author Heinz Mauelshagen <heinzm@redhat.com>
commit 78a75d10ef869f4fae70f9b86afce28eb1922529
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-862.el7/78a75d10.failed

Move raid_resume()'s setting of 'rw' and 'in_sync' to just prior to
mddev_resume().

Also, remove unused 'bitmap_loaded' member from "struct raid_set".

No functional changes.

	Signed-off-by: Heinz Mauelshagen <heinzm@redhat.com>
	Signed-off-by: Mike Snitzer <snitzer@redhat.com>
(cherry picked from commit 78a75d10ef869f4fae70f9b86afce28eb1922529)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/md/dm-raid.c
diff --cc drivers/md/dm-raid.c
index a5a315efdf86,1069e617e727..000000000000
--- a/drivers/md/dm-raid.c
+++ b/drivers/md/dm-raid.c
@@@ -3877,26 -3983,13 +3873,33 @@@ static void raid_resume(struct dm_targe
  			clear_bit(MD_RECOVERY_FROZEN, &mddev->recovery);
  	}
  
++<<<<<<< HEAD
 +	if (test_and_clear_bit(RT_FLAG_RS_SUSPENDED, &rs->runtime_flags))
++=======
+ 	if (test_and_clear_bit(RT_FLAG_RS_SUSPENDED, &rs->runtime_flags)) {
+ 		mddev_lock_nointr(mddev);
+ 		mddev->ro = 0;
+ 		mddev->in_sync = 0;
++>>>>>>> 78a75d10ef86 (dm raid: small cleanup and remove unsed "struct raid_set" member)
  		mddev_resume(mddev);
 -		mddev_unlock(mddev);
 -	}
 +}
 +
 +static int raid_merge(struct dm_target *ti, struct bvec_merge_data *bvm,
 +		      struct bio_vec *biovec, int max_size)
 +{
 +	struct raid_set *rs = ti->private;
 +	struct md_personality *pers = rs->md.pers;
 +
 +	if (pers && pers->mergeable_bvec)
 +		return min(max_size, pers->mergeable_bvec(&rs->md, bvm, biovec));
 +
 +	/*
 +	 * In case we can't request the personality because
 +	 * the raid set is not running yet
 +	 *
 +	 * -> return safe minimum
 +	 */
 +	return rs->md.chunk_sectors;
  }
  
  static struct target_type raid_target = {
* Unmerged path drivers/md/dm-raid.c
