ALSA: hda - Minor code refactoring for Intel HDMI codec parsers

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-862.el7
commit-author Takashi Iwai <tiwai@suse.de>
commit 43f6c8d97bbdc05858b3d64e4c683821ce46c2db
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-862.el7/43f6c8d9.failed

No functional change, just a slight reduction of lines.

	Signed-off-by: Takashi Iwai <tiwai@suse.de>
(cherry picked from commit 43f6c8d97bbdc05858b3d64e4c683821ce46c2db)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	sound/pci/hda/patch_hdmi.c
diff --cc sound/pci/hda/patch_hdmi.c
index 0d7955eec201,8704ea43381e..000000000000
--- a/sound/pci/hda/patch_hdmi.c
+++ b/sound/pci/hda/patch_hdmi.c
@@@ -2604,35 -2601,12 +2604,41 @@@ static int patch_i915_byt_hdmi(struct h
  /* Intel IronLake, SandyBridge and IvyBridge; with eld notifier */
  static int patch_i915_cpt_hdmi(struct hda_codec *codec)
  {
- 	struct hdmi_spec *spec;
  	int err;
  
++<<<<<<< HEAD
 +	/* no i915 component should have been bound before this */
 +	if (WARN_ON(codec->bus->core.audio_component))
 +		return -EBUSY;
 +
 +	err = alloc_generic_hdmi(codec);
 +	if (err < 0)
 +		return err;
 +	spec = codec->spec;
 +
 +	/* Try to bind with i915 now */
 +	err = snd_hdac_i915_init(&codec->bus->core);
 +	if (err < 0)
 +		goto error;
 +	spec->i915_bound = true;
 +
 +	err = hdmi_parse_codec(codec);
 +	if (err < 0)
 +		goto error;
 +
 +	generic_hdmi_init_per_pins(codec);
 +	register_i915_notifier(codec);
 +	return 0;
 +
 + error:
 +	generic_spec_free(codec);
 +	return err;
++=======
+ 	err = alloc_intel_hdmi(codec);
+ 	if (err < 0)
+ 		return err;
+ 	return parse_intel_hdmi(codec);
++>>>>>>> 43f6c8d97bbd (ALSA: hda - Minor code refactoring for Intel HDMI codec parsers)
  }
  
  /*
* Unmerged path sound/pci/hda/patch_hdmi.c
