nvme-pci: rename to nvme_pci_configure_admin_queue

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-862.el7
Rebuild_CHGLOG: - [nvme] pci: rename to nvme_pci_configure_admin_queue (David Milburn) [1457880 1456486 1454365]
Rebuild_FUZZ: 94.74%
commit-author Sagi Grimberg <sagi@grimberg.me>
commit 01ad0990467eaa17ae17db7376a4f02739f558c0
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-862.el7/01ad0990.failed

we are going to need the name for the core routine...

	Reviewed-by: Johannes Thumshirn <jthumshirn@suse.de>
	Reviewed-by: Max Gurtovoy <maxg@mellanox.com>
	Reviewed-by: Christoph Hellwig <hch@lst.de>
	Signed-off-by: Sagi Grimberg <sagi@grimberg.me>
(cherry picked from commit 01ad0990467eaa17ae17db7376a4f02739f558c0)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/nvme/host/pci.c
diff --cc drivers/nvme/host/pci.c
index bdee6de6e811,eb729ff70e7d..000000000000
--- a/drivers/nvme/host/pci.c
+++ b/drivers/nvme/host/pci.c
@@@ -1103,7 -1357,33 +1103,37 @@@ static int nvme_alloc_admin_tags(struc
  	return 0;
  }
  
++<<<<<<< HEAD
 +static int nvme_configure_admin_queue(struct nvme_dev *dev)
++=======
+ static unsigned long db_bar_size(struct nvme_dev *dev, unsigned nr_io_queues)
+ {
+ 	return NVME_REG_DBS + ((nr_io_queues + 1) * 8 * dev->db_stride);
+ }
+ 
+ static int nvme_remap_bar(struct nvme_dev *dev, unsigned long size)
+ {
+ 	struct pci_dev *pdev = to_pci_dev(dev->dev);
+ 
+ 	if (size <= dev->bar_mapped_size)
+ 		return 0;
+ 	if (size > pci_resource_len(pdev, 0))
+ 		return -ENOMEM;
+ 	if (dev->bar)
+ 		iounmap(dev->bar);
+ 	dev->bar = ioremap(pci_resource_start(pdev, 0), size);
+ 	if (!dev->bar) {
+ 		dev->bar_mapped_size = 0;
+ 		return -ENOMEM;
+ 	}
+ 	dev->bar_mapped_size = size;
+ 	dev->dbs = dev->bar + NVME_REG_DBS;
+ 
+ 	return 0;
+ }
+ 
+ static int nvme_pci_configure_admin_queue(struct nvme_dev *dev)
++>>>>>>> 01ad0990467e (nvme-pci: rename to nvme_pci_configure_admin_queue)
  {
  	int result;
  	u32 aqa;
* Unmerged path drivers/nvme/host/pci.c
