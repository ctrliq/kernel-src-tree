skbuff: make skb_put_zero() return void

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-862.el7
commit-author Johannes Berg <johannes.berg@intel.com>
commit 83ad357dee467f63574de35752bc40033deab30e
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-862.el7/83ad357d.failed

It's nicer to return void, since then there's no need to
cast to any structures. Currently none of the users have
a cast, but a number of future conversions do.

	Signed-off-by: Johannes Berg <johannes.berg@intel.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 83ad357dee467f63574de35752bc40033deab30e)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	include/linux/skbuff.h
diff --cc include/linux/skbuff.h
index 28aefdf34562,01ea64d0783a..000000000000
--- a/include/linux/skbuff.h
+++ b/include/linux/skbuff.h
@@@ -1928,6 -1904,15 +1928,18 @@@ static inline unsigned char *__skb_put(
  	return tmp;
  }
  
++<<<<<<< HEAD
++=======
+ static inline void *skb_put_zero(struct sk_buff *skb, unsigned int len)
+ {
+ 	void *tmp = skb_put(skb, len);
+ 
+ 	memset(tmp, 0, len);
+ 
+ 	return tmp;
+ }
+ 
++>>>>>>> 83ad357dee46 (skbuff: make skb_put_zero() return void)
  unsigned char *skb_push(struct sk_buff *skb, unsigned int len);
  static inline unsigned char *__skb_push(struct sk_buff *skb, unsigned int len)
  {
* Unmerged path include/linux/skbuff.h
