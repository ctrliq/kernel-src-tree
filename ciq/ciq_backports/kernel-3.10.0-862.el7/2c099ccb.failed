net: sched: choke: remove some dead code

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-862.el7
Rebuild_CHGLOG: - [net] sched: choke: remove some dead code (Ivan Vecera) [1445420]
Rebuild_FUZZ: 93.33%
commit-author Dan Carpenter <dan.carpenter@oracle.com>
commit 2c099ccbd093ffce056987a227fcc96e6a77b101
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-862.el7/2c099ccb.failed

We accidentally left this dead code behind after commit 5952fde10c35
("net: sched: choke: remove dead filter classify code").

	Signed-off-by: Dan Carpenter <dan.carpenter@oracle.com>
	Reviewed-by: Jiri Pirko <jiri@mellanox.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 2c099ccbd093ffce056987a227fcc96e6a77b101)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/sched/sch_choke.c
diff --cc net/sched/sch_choke.c
index f71e148f3849,593183a5b5b5..000000000000
--- a/net/sched/sch_choke.c
+++ b/net/sched/sch_choke.c
@@@ -218,9 -220,9 +218,8 @@@ static bool choke_match_random(const st
  	return choke_match_flow(oskb, nskb);
  }
  
 -static int choke_enqueue(struct sk_buff *skb, struct Qdisc *sch,
 -			 struct sk_buff **to_free)
 +static int choke_enqueue(struct sk_buff *skb, struct Qdisc *sch)
  {
- 	int ret = NET_XMIT_SUCCESS | __NET_XMIT_BYPASS;
  	struct choke_sched_data *q = qdisc_priv(sch);
  	const struct red_parms *p = &q->parms;
  
@@@ -282,16 -284,11 +281,19 @@@
  	}
  
  	q->stats.pdrop++;
 -	return qdisc_drop(skb, sch, to_free);
 +	return qdisc_drop(skb, sch);
  
  congestion_drop:
 -	qdisc_drop(skb, sch, to_free);
 +	qdisc_drop(skb, sch);
  	return NET_XMIT_CN;
++<<<<<<< HEAD
 +
 +	if (ret & __NET_XMIT_BYPASS)
 +		qdisc_qstats_drop(sch);
 +	kfree_skb(skb);
 +	return ret;
++=======
++>>>>>>> 2c099ccbd093 (net: sched: choke: remove some dead code)
  }
  
  static struct sk_buff *choke_dequeue(struct Qdisc *sch)
* Unmerged path net/sched/sch_choke.c
