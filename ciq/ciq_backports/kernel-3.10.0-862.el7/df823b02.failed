net sched actions: Use proper root attribute table for actions

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-862.el7
Rebuild_CHGLOG: - [net] sched: actions: Use proper root attribute table for actions (Ivan Vecera) [1445420]
Rebuild_FUZZ: 95.87%
commit-author Jamal Hadi Salim <jhs@mojatatu.com>
commit df823b02970172f3e4003063699e333295b9b32d
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-862.el7/df823b02.failed

Bug fix for an issue which has been around for about a decade.
We got away with it because the enumeration was larger than needed.

Fixes: 7ba699c604ab ("[NET_SCHED]: Convert actions from rtnetlink to new netlink API")
	Suggested-by: Jiri Pirko <jiri@mellanox.com>
	Reviewed-by: Simon Horman <simon.horman@netronome.com>
	Signed-off-by: Jamal Hadi Salim <jhs@mojatatu.com>
	Reviewed-by: Jiri Pirko <jiri@mellanox.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit df823b02970172f3e4003063699e333295b9b32d)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/sched/act_api.c
diff --cc net/sched/act_api.c
index f6d266c05d33,848370e2fcca..000000000000
--- a/net/sched/act_api.c
+++ b/net/sched/act_api.c
@@@ -1056,10 -1068,11 +1056,10 @@@ static int tcf_action_add(struct net *n
  	return tcf_add_notify(net, n, &actions, portid);
  }
  
 -static int tc_ctl_action(struct sk_buff *skb, struct nlmsghdr *n,
 -			 struct netlink_ext_ack *extack)
 +static int tc_ctl_action(struct sk_buff *skb, struct nlmsghdr *n)
  {
  	struct net *net = sock_net(skb->sk);
- 	struct nlattr *tca[TCA_ACT_MAX + 1];
+ 	struct nlattr *tca[TCAA_MAX + 1];
  	u32 portid = skb ? NETLINK_CB(skb).portid : 0;
  	int ret = 0, ovr = 0;
  
@@@ -1067,7 -1080,8 +1067,12 @@@
  	    !netlink_capable(skb, CAP_NET_ADMIN))
  		return -EPERM;
  
++<<<<<<< HEAD
 +	ret = nlmsg_parse(n, sizeof(struct tcamsg), tca, TCA_ACT_MAX, NULL);
++=======
+ 	ret = nlmsg_parse(n, sizeof(struct tcamsg), tca, TCAA_MAX, NULL,
+ 			  extack);
++>>>>>>> df823b029701 (net sched actions: Use proper root attribute table for actions)
  	if (ret < 0)
  		return ret;
  
* Unmerged path net/sched/act_api.c
