net: sfc: remove redundant variable start

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-862.el7
Rebuild_CHGLOG: - [netdrv] sfc: remove redundant variable start (Jarod Wilson) [1479400]
Rebuild_FUZZ: 93.51%
commit-author Colin Ian King <colin.king@canonical.com>
commit 492d070f2495d4b200124ed44a35ab3d8f74ac93
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-862.el7/492d070f.failed

Variable start is assigned but never read hence it is redundant
and can be removed. Cleans up clang warning:

drivers/net/ethernet/sfc/ptp.c:655:2: warning: Value stored to 'start'
is never read

	Signed-off-by: Colin Ian King <colin.king@canonical.com>
	Acked-by: Bert Kenward <bkenward@solarflare.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 492d070f2495d4b200124ed44a35ab3d8f74ac93)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/sfc/ptp.c
diff --cc drivers/net/ethernet/sfc/ptp.c
index a218e5c113aa,4f54245df0ec..000000000000
--- a/drivers/net/ethernet/sfc/ptp.c
+++ b/drivers/net/ethernet/sfc/ptp.c
@@@ -646,20 -646,18 +646,22 @@@ static void efx_ptp_send_times(struct e
  			       struct pps_event_time *last_time)
  {
  	struct pps_event_time now;
 -	struct timespec64 limit;
 +	struct timespec limit;
  	struct efx_ptp_data *ptp = efx->ptp_data;
++<<<<<<< HEAD
 +	struct timespec start;
++=======
++>>>>>>> 492d070f2495 (net: sfc: remove redundant variable start)
  	int *mc_running = ptp->start.addr;
  
  	pps_get_ts(&now);
- 	start = now.ts_real;
  	limit = now.ts_real;
 -	timespec64_add_ns(&limit, SYNCHRONISE_PERIOD_NS);
 +	timespec_add_ns(&limit, SYNCHRONISE_PERIOD_NS);
  
  	/* Write host time for specified period or until MC is done */
 -	while ((timespec64_compare(&now.ts_real, &limit) < 0) &&
 +	while ((timespec_compare(&now.ts_real, &limit) < 0) &&
  	       ACCESS_ONCE(*mc_running)) {
 -		struct timespec64 update_time;
 +		struct timespec update_time;
  		unsigned int host_time;
  
  		/* Don't update continuously to avoid saturating the PCIe bus */
* Unmerged path drivers/net/ethernet/sfc/ptp.c
