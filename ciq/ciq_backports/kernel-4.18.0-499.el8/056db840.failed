ASoC: SOF: ipc4/intel: Fix spelling mistake "schduler" -> "scheduler"

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-499.el8
commit-author Colin Ian King <colin.i.king@gmail.com>
commit 056db840115653659d86a3931a78b3c504edb2cc
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-499.el8/056db840.failed

There are two spelling mistakes in dev_warn messages. Fix them.

	Signed-off-by: Colin Ian King <colin.i.king@gmail.com>
Link: https://lore.kernel.org/r/20230323090531.67679-1-colin.i.king@gmail.com
	Signed-off-by: Mark Brown <broonie@kernel.org>
(cherry picked from commit 056db840115653659d86a3931a78b3c504edb2cc)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	sound/soc/sof/ipc4-topology.c
diff --cc sound/soc/sof/ipc4-topology.c
index 537221b37f67,de7213237b27..000000000000
--- a/sound/soc/sof/ipc4-topology.c
+++ b/sound/soc/sof/ipc4-topology.c
@@@ -1921,6 -1989,12 +1921,15 @@@ static int sof_ipc4_widget_setup(struc
  	case snd_soc_dapm_scheduler:
  		pipeline = swidget->private;
  
++<<<<<<< HEAD
++=======
+ 		if (pipeline->use_chain_dma) {
+ 			dev_warn(sdev->dev, "use_chain_dma set for scheduler %s",
+ 				 swidget->widget->name);
+ 			return 0;
+ 		}
+ 
++>>>>>>> 056db8401156 (ASoC: SOF: ipc4/intel: Fix spelling mistake "schduler" -> "scheduler")
  		dev_dbg(sdev->dev, "pipeline: %d memory pages: %d\n", swidget->pipeline_id,
  			pipeline->mem_usage);
  
@@@ -2066,6 -2148,13 +2075,16 @@@ static int sof_ipc4_widget_free(struct 
  		struct sof_ipc4_msg msg = {{ 0 }};
  		u32 header;
  
++<<<<<<< HEAD
++=======
+ 		if (pipeline->use_chain_dma) {
+ 			dev_warn(sdev->dev, "use_chain_dma set for scheduler %s",
+ 				 swidget->widget->name);
+ 			mutex_unlock(&ipc4_data->pipeline_state_mutex);
+ 			return 0;
+ 		}
+ 
++>>>>>>> 056db8401156 (ASoC: SOF: ipc4/intel: Fix spelling mistake "schduler" -> "scheduler")
  		header = SOF_IPC4_GLB_PIPE_INSTANCE_ID(swidget->instance_id);
  		header |= SOF_IPC4_MSG_TYPE_SET(SOF_IPC4_GLB_DELETE_PIPELINE);
  		header |= SOF_IPC4_MSG_DIR(SOF_IPC4_MSG_REQUEST);
* Unmerged path sound/soc/sof/ipc4-topology.c
