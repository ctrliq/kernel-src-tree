ASoC: SOF: Fix compilation when HDA_AUDIO_CODEC config is disabled

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-499.el8
commit-author Cezary Rojewski <cezary.rojewski@intel.com>
commit 1cda83e42bf66beb06bf61c7a78951ec0c028898
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-499.el8/1cda83e4.failed

hda_codec_device_init() expects three parameters, not two.

Fixes: 3fd63658caed ("ASoC: Intel: Drop hdac_ext usage for codec device creation")
	Reported-by: kernel test robot <lkp@intel.com>
	Signed-off-by: Cezary Rojewski <cezary.rojewski@intel.com>
	Acked-by: Mark Brown <broonie@kernel.org>
Link: https://lore.kernel.org/r/20220819124740.3564862-1-cezary.rojewski@intel.com
	Signed-off-by: Takashi Iwai <tiwai@suse.de>
(cherry picked from commit 1cda83e42bf66beb06bf61c7a78951ec0c028898)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	sound/soc/sof/intel/hda-codec.c
diff --cc sound/soc/sof/intel/hda-codec.c
index 46149e4094ed,1e9afc48394c..000000000000
--- a/sound/soc/sof/intel/hda-codec.c
+++ b/sound/soc/sof/intel/hda-codec.c
@@@ -211,15 -209,12 +211,20 @@@ static int hda_codec_probe(struct snd_s
  
  out:
  	if (ret < 0) {
 -		snd_hdac_device_unregister(&codec->core);
 -		put_device(&codec->core.dev);
 +		snd_hdac_device_unregister(hdev);
 +		put_device(&hdev->dev);
  	}
  #else
++<<<<<<< HEAD
 +	hdev = devm_kzalloc(sdev->dev, sizeof(*hdev), GFP_KERNEL);
 +	if (!hdev)
 +		return -ENOMEM;
 +
 +	ret = snd_hdac_ext_bus_device_init(&hbus->core, address, hdev, HDA_DEV_ASOC);
++=======
+ 	codec = hda_codec_device_init(&hbus->core, address, HDA_DEV_ASOC);
+ 	ret = PTR_ERR_OR_ZERO(codec);
++>>>>>>> 1cda83e42bf6 (ASoC: SOF: Fix compilation when HDA_AUDIO_CODEC config is disabled)
  #endif
  
  	return ret;
* Unmerged path sound/soc/sof/intel/hda-codec.c
