mlxsw: core: Fix use-after-free calling devl_unlock() in mlxsw_core_bus_device_unregister()

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-477.15.1.el8_8
commit-author Jiri Pirko <jiri@nvidia.com>
commit 1b5995e370e4024e7bb7408247363e8e67f0393d
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-477.15.1.el8_8/1b5995e3.failed

Do devl_unlock() before freeing the devlink in
mlxsw_core_bus_device_unregister() function.

	Reported-by: Ido Schimmel <idosch@nvidia.com>
Fixes: 72a4c8c94efa ("mlxsw: convert driver to use unlocked devlink API during init/fini")
	Signed-off-by: Jiri Pirko <jiri@nvidia.com>
	Reviewed-by: Ido Schimmel <idosch@nvidia.com>
Link: https://lore.kernel.org/r/20220721142424.3975704-1-jiri@resnulli.us
	Signed-off-by: Jakub Kicinski <kuba@kernel.org>
(cherry picked from commit 1b5995e370e4024e7bb7408247363e8e67f0393d)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/mellanox/mlxsw/core.c
diff --cc drivers/net/ethernet/mellanox/mlxsw/core.c
index 49fab4a59db8,1b61bc8f59a2..000000000000
--- a/drivers/net/ethernet/mellanox/mlxsw/core.c
+++ b/drivers/net/ethernet/mellanox/mlxsw/core.c
@@@ -2096,16 -2293,19 +2096,28 @@@ void mlxsw_core_bus_device_unregister(s
  	kfree(mlxsw_core->lag.mapping);
  	mlxsw_ports_fini(mlxsw_core, reload);
  	if (!reload)
 -		devl_resources_unregister(devlink);
 +		devlink_resources_unregister(devlink);
  	mlxsw_core->bus->fini(mlxsw_core->bus_priv);
++<<<<<<< HEAD
 +	if (!reload)
 +		devlink_free(devlink);
++=======
+ 	if (!reload) {
+ 		devl_unlock(devlink);
+ 		devlink_free(devlink);
+ 	}
++>>>>>>> 1b5995e370e4 (mlxsw: core: Fix use-after-free calling devl_unlock() in mlxsw_core_bus_device_unregister())
  
  	return;
  
  reload_fail_deinit:
  	mlxsw_core_params_unregister(mlxsw_core);
++<<<<<<< HEAD
 +	devlink_resources_unregister(devlink);
++=======
+ 	devl_resources_unregister(devlink);
+ 	devl_unlock(devlink);
++>>>>>>> 1b5995e370e4 (mlxsw: core: Fix use-after-free calling devl_unlock() in mlxsw_core_bus_device_unregister())
  	devlink_free(devlink);
  }
  EXPORT_SYMBOL(mlxsw_core_bus_device_unregister);
* Unmerged path drivers/net/ethernet/mellanox/mlxsw/core.c
