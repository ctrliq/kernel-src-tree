netfilter: cttimeout: remove set but not used variable 'l3num'

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-240.el8
commit-author YueHaibing <yuehaibing@huawei.com>
commit ea9cf2a55a7bb84ba862a0fc13044a4311c9bbfe
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-240.el8/ea9cf2a5.failed

Fixes gcc '-Wunused-but-set-variable' warning:

net/netfilter/nfnetlink_cttimeout.c: In function 'cttimeout_default_set':
net/netfilter/nfnetlink_cttimeout.c:353:8: warning:
 variable 'l3num' set but not used [-Wunused-but-set-variable]

It not used any more after
commit dd2934a95701 ("netfilter: conntrack: remove l3->l4 mapping information")

	Signed-off-by: YueHaibing <yuehaibing@huawei.com>
	Signed-off-by: Pablo Neira Ayuso <pablo@netfilter.org>
(cherry picked from commit ea9cf2a55a7bb84ba862a0fc13044a4311c9bbfe)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/netfilter/nfnetlink_cttimeout.c
diff --cc net/netfilter/nfnetlink_cttimeout.c
index 6720dbb70442,e7a50af1b3d6..000000000000
--- a/net/netfilter/nfnetlink_cttimeout.c
+++ b/net/netfilter/nfnetlink_cttimeout.c
@@@ -361,8 -350,6 +361,11 @@@ static int cttimeout_default_set(struc
  				 struct netlink_ext_ack *extack)
  {
  	const struct nf_conntrack_l4proto *l4proto;
++<<<<<<< HEAD
 +	unsigned int *timeouts;
 +	__u16 l3num;
++=======
++>>>>>>> ea9cf2a55a7b (netfilter: cttimeout: remove set but not used variable 'l3num')
  	__u8 l4num;
  	int ret;
  
@@@ -371,9 -358,8 +374,8 @@@
  	    !cda[CTA_TIMEOUT_DATA])
  		return -EINVAL;
  
- 	l3num = ntohs(nla_get_be16(cda[CTA_TIMEOUT_L3PROTO]));
  	l4num = nla_get_u8(cda[CTA_TIMEOUT_L4PROTO]);
 -	l4proto = nf_ct_l4proto_find_get(l4num);
 +	l4proto = nf_ct_l4proto_find_get(l3num, l4num);
  
  	/* This protocol is not supported, skip. */
  	if (l4proto->l4proto != l4num) {
* Unmerged path net/netfilter/nfnetlink_cttimeout.c
