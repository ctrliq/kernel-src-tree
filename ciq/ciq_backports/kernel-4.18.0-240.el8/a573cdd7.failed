arm64: rename variables used to calculate ZONE_DMA32's size

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-240.el8
commit-author Nicolas Saenz Julienne <nsaenzjulienne@suse.de>
commit a573cdd7973dedd87e62196c400332896bb236c8
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-240.el8/a573cdd7.failed

Let the name indicate that they are used to calculate ZONE_DMA32's size
as opposed to ZONE_DMA.

	Signed-off-by: Nicolas Saenz Julienne <nsaenzjulienne@suse.de>
	Reviewed-by: Catalin Marinas <catalin.marinas@arm.com>
	Signed-off-by: Catalin Marinas <catalin.marinas@arm.com>
(cherry picked from commit a573cdd7973dedd87e62196c400332896bb236c8)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	arch/arm64/mm/init.c
diff --cc arch/arm64/mm/init.c
index 5acf8fbf4557,8e9bc64c5878..000000000000
--- a/arch/arm64/mm/init.c
+++ b/arch/arm64/mm/init.c
@@@ -60,25 -48,15 +60,37 @@@
   * that cannot be mistaken for a real physical address.
   */
  s64 memstart_addr __ro_after_init = -1;
++<<<<<<< HEAD
 +phys_addr_t arm64_dma_phys_limit __ro_after_init;
++=======
+ EXPORT_SYMBOL(memstart_addr);
+ 
+ s64 physvirt_offset __ro_after_init;
+ EXPORT_SYMBOL(physvirt_offset);
+ 
+ struct page *vmemmap __ro_after_init;
+ EXPORT_SYMBOL(vmemmap);
+ 
+ phys_addr_t arm64_dma32_phys_limit __ro_after_init;
++>>>>>>> a573cdd7973d (arm64: rename variables used to calculate ZONE_DMA32's size)
 +
 +#ifdef CONFIG_BLK_DEV_INITRD
 +static int __init early_initrd(char *p)
 +{
 +	unsigned long start, size;
 +	char *endp;
 +
 +	start = memparse(p, &endp);
 +	if (*endp == ',') {
 +		size = memparse(endp + 1, NULL);
 +
 +		initrd_start = start;
 +		initrd_end = start + size;
 +	}
 +	return 0;
 +}
 +early_param("initrd", early_initrd);
 +#endif
  
  #ifdef CONFIG_KEXEC_CORE
  /*
* Unmerged path arch/arm64/mm/init.c
