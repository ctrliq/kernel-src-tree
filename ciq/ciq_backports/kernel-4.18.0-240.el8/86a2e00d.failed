ice: remove unnecessary check

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-240.el8
commit-author Bruce Allan <bruce.w.allan@intel.com>
commit 86a2e00d20bd4f7fd0efd1cccd8d5a5d7270b640
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-240.el8/86a2e00d.failed

The variable status cannot be zero due to a prior check of it; remove this
check.

	Signed-off-by: Bruce Allan <bruce.w.allan@intel.com>
	Tested-by: Andrew Bowers <andrewx.bowers@intel.com>
	Signed-off-by: Jeff Kirsher <jeffrey.t.kirsher@intel.com>
(cherry picked from commit 86a2e00d20bd4f7fd0efd1cccd8d5a5d7270b640)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/intel/ice/ice_main.c
diff --cc drivers/net/ethernet/intel/ice/ice_main.c
index 40fd98d4ad25,c3e5c4334e26..000000000000
--- a/drivers/net/ethernet/intel/ice/ice_main.c
+++ b/drivers/net/ethernet/intel/ice/ice_main.c
@@@ -161,9 -149,9 +161,15 @@@ unregister
  	/* We aren't useful with no MAC filters, so unregister if we
  	 * had an error
  	 */
++<<<<<<< HEAD
 +	if (status && vsi->netdev->reg_state == NETREG_REGISTERED) {
 +		dev_err(ice_pf_to_dev(pf), "Could not add MAC filters error %d. Unregistering device\n",
 +			status);
++=======
+ 	if (vsi->netdev->reg_state == NETREG_REGISTERED) {
+ 		dev_err(ice_pf_to_dev(pf), "Could not add MAC filters error %s. Unregistering device\n",
+ 			ice_stat_str(status));
++>>>>>>> 86a2e00d20bd (ice: remove unnecessary check)
  		unregister_netdev(vsi->netdev);
  		free_netdev(vsi->netdev);
  		vsi->netdev = NULL;
* Unmerged path drivers/net/ethernet/intel/ice/ice_main.c
