gpiolib: add support for biasing output lines

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-240.el8
commit-author Kent Gibson <warthog618@gmail.com>
commit 2821ae5f3033fb4140855ffd728c28fb42ba2bb9
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-240.el8/2821ae5f.failed

Allow pull up/down bias to be set on output lines.
Use case is for open source or open drain applications where
internal pull up/down may conflict with external biasing.

	Signed-off-by: Kent Gibson <warthog618@gmail.com>
	Signed-off-by: Bartosz Golaszewski <bgolaszewski@baylibre.com>
(cherry picked from commit 2821ae5f3033fb4140855ffd728c28fb42ba2bb9)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/gpio/gpiolib.c
diff --cc drivers/gpio/gpiolib.c
index 07b822ae02a4,be159f56ff45..000000000000
--- a/drivers/gpio/gpiolib.c
+++ b/drivers/gpio/gpiolib.c
@@@ -560,6 -556,22 +560,25 @@@ static int linehandle_create(struct gpi
  	     (lflags & GPIOHANDLE_REQUEST_OPEN_SOURCE)))
  		return -EINVAL;
  
++<<<<<<< HEAD
++=======
+ 	/* Bias flags only allowed for input or output mode. */
+ 	if (!((lflags & GPIOHANDLE_REQUEST_INPUT) ||
+ 	      (lflags & GPIOHANDLE_REQUEST_OUTPUT)) &&
+ 	    ((lflags & GPIOHANDLE_REQUEST_BIAS_DISABLE) ||
+ 	     (lflags & GPIOHANDLE_REQUEST_BIAS_PULL_UP) ||
+ 	     (lflags & GPIOHANDLE_REQUEST_BIAS_PULL_DOWN)))
+ 		return -EINVAL;
+ 
+ 	/* Only one bias flag can be set. */
+ 	if (((lflags & GPIOHANDLE_REQUEST_BIAS_DISABLE) &&
+ 	     (lflags & (GPIOHANDLE_REQUEST_BIAS_PULL_DOWN |
+ 			GPIOHANDLE_REQUEST_BIAS_PULL_UP))) ||
+ 	    ((lflags & GPIOHANDLE_REQUEST_BIAS_PULL_DOWN) &&
+ 	     (lflags & GPIOHANDLE_REQUEST_BIAS_PULL_UP)))
+ 		return -EINVAL;
+ 
++>>>>>>> 2821ae5f3033 (gpiolib: add support for biasing output lines)
  	lh = kzalloc(sizeof(*lh), GFP_KERNEL);
  	if (!lh)
  		return -ENOMEM;
@@@ -2790,7 -3170,21 +2809,10 @@@ int gpiod_direction_output(struct gpio_
  	}
  
  set_output_value:
+ 	ret = gpio_set_bias(gc, desc);
+ 	if (ret)
+ 		return ret;
  	return gpiod_direction_output_raw_commit(desc, value);
 -
 -set_output_flag:
 -	/*
 -	 * When emulating open-source or open-drain functionalities by not
 -	 * actively driving the line (setting mode to input) we still need to
 -	 * set the IS_OUT flag or otherwise we won't be able to set the line
 -	 * value anymore.
 -	 */
 -	if (ret == 0)
 -		set_bit(FLAG_IS_OUT, &desc->flags);
 -	return ret;
  }
  EXPORT_SYMBOL_GPL(gpiod_direction_output);
  
* Unmerged path drivers/gpio/gpiolib.c
