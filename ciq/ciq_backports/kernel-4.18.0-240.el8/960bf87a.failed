scsi: libiscsi: Use scsi_[gs]et_resid() where appropriate

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-240.el8
Rebuild_CHGLOG: - [scsi] scsi: libiscsi: Use scsi_et_resid() where appropriate (Chris Leech) [1830140]
Rebuild_FUZZ: 96.36%
commit-author Bart Van Assche <bvanassche@acm.org>
commit 960bf87a4fbe46bec1e69fcb6868f43c3c3c9456
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-240.el8/960bf87a.failed

This patch does not change any functionality.

	Cc: Lee Duncan <lduncan@suse.com>
	Cc: Chris Leech <cleech@redhat.com>
	Signed-off-by: Bart Van Assche <bvanassche@acm.org>
	Reviewed-by: Christoph Hellwig <hch@lst.de>
	Signed-off-by: Martin K. Petersen <martin.petersen@oracle.com>
(cherry picked from commit 960bf87a4fbe46bec1e69fcb6868f43c3c3c9456)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/scsi/libiscsi.c
diff --cc drivers/scsi/libiscsi.c
index 120fc520f27a,dd314d2b1111..000000000000
--- a/drivers/scsi/libiscsi.c
+++ b/drivers/scsi/libiscsi.c
@@@ -961,8 -914,8 +961,13 @@@ iscsi_data_in_rsp(struct iscsi_conn *co
  
  		if (res_count > 0 &&
  		    (rhdr->flags & ISCSI_FLAG_CMD_OVERFLOW ||
++<<<<<<< HEAD
 +		     res_count <= scsi_in(sc)->length))
 +			scsi_in(sc)->resid = res_count;
++=======
+ 		     res_count <= sc->sdb.length))
+ 			scsi_set_resid(sc, res_count);
++>>>>>>> 960bf87a4fbe (scsi: libiscsi: Use scsi_[gs]et_resid() where appropriate)
  		else
  			sc->result = (DID_BAD_TARGET << 16) | rhdr->cmd_status;
  	}
* Unmerged path drivers/scsi/libiscsi.c
