tipc: eliminate checking netns if node established

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-240.el8
commit-author Hoang Le <hoang.h.le@dektech.com.au>
commit d408bef4bfa60bac665b6e7239269570039a968b
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-240.el8/d408bef4.failed

Currently, we scan over all network namespaces at each received
discovery message in order to check if the sending peer might be
present in a host local namespaces.

This is unnecessary since we can assume that a peer will not change its
location during an established session.

We now improve the condition for this testing so that we don't perform
any redundant scans.

Fixes: f73b12812a3d ("tipc: improve throughput between nodes in netns")
	Acked-by: Jon Maloy <jon.maloy@ericsson.com>
	Signed-off-by: Hoang Le <hoang.h.le@dektech.com.au>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit d408bef4bfa60bac665b6e7239269570039a968b)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/tipc/node.c
diff --cc net/tipc/node.c
index 3fe27ab13350,b66d2f67b1dd..000000000000
--- a/net/tipc/node.c
+++ b/net/tipc/node.c
@@@ -1353,7 -1397,8 +1356,12 @@@ static void node_lost_contact(struct ti
  
  	/* Notify publications from this node */
  	n->action_flags |= TIPC_NOTIFY_NODE_DOWN;
++<<<<<<< HEAD
 +
++=======
+ 	n->peer_net = NULL;
+ 	n->peer_hash_mix = 0;
++>>>>>>> d408bef4bfa6 (tipc: eliminate checking netns if node established)
  	/* Notify sockets connected to node */
  	list_for_each_entry_safe(conn, safe, conns, list) {
  		skb = tipc_msg_create(TIPC_CRITICAL_IMPORTANCE, TIPC_CONN_MSG,
* Unmerged path net/tipc/node.c
