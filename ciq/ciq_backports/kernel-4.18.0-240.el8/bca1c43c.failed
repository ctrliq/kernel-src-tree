null_blk: remove unused variable warning on !CONFIG_BLK_DEV_ZONED

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-240.el8
commit-author Jens Axboe <axboe@kernel.dk>
commit bca1c43cb2dbe4212aea0793bfd91aeb4c2d184d
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-240.el8/bca1c43c.failed

If BLK_DEV_ZONED isn't set, 'ret' isn't used. This makes gcc complain,
rightfully. Move ret where it is used.

Fixes: 979d54475e0b ("null_blk: cleanup null_gendisk_register")
	Signed-off-by: Jens Axboe <axboe@kernel.dk>
(cherry picked from commit bca1c43cb2dbe4212aea0793bfd91aeb4c2d184d)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/block/null_blk_main.c
diff --cc drivers/block/null_blk_main.c
index 88b01572e8f5,ae8d4bc532b0..000000000000
--- a/drivers/block/null_blk_main.c
+++ b/drivers/block/null_blk_main.c
@@@ -1556,11 -1575,19 +1555,23 @@@ static int null_gendisk_register(struc
  	disk->queue		= nullb->q;
  	strncpy(disk->disk_name, nullb->disk_name, DISK_NAME_LEN);
  
 -#ifdef CONFIG_BLK_DEV_ZONED
  	if (nullb->dev->zoned) {
++<<<<<<< HEAD
 +		ret = blk_revalidate_disk_zones(disk);
 +		if (ret)
 +			return ret;
++=======
+ 		if (queue_is_mq(nullb->q)) {
+ 			int ret = blk_revalidate_disk_zones(disk);
+ 			if (ret)
+ 				return ret;
+ 		} else {
+ 			blk_queue_chunk_sectors(nullb->q,
+ 					nullb->dev->zone_size_sects);
+ 			nullb->q->nr_zones = blkdev_nr_zones(disk);
+ 		}
++>>>>>>> bca1c43cb2db (null_blk: remove unused variable warning on !CONFIG_BLK_DEV_ZONED)
  	}
 -#endif
  
  	add_disk(disk);
  	return 0;
* Unmerged path drivers/block/null_blk_main.c
