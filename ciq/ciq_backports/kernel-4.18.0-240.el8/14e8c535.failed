gpio: fix kernel-doc for of_gpio_need_valid_mask()

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-240.el8
commit-author Randy Dunlap <rdunlap@infradead.org>
commit 14e8c535ff684876d1be9dae475fe666b97c04a9
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-240.el8/14e8c535.failed

Fix kernel-doc for of_gpio_need_valid_mask().
Fixes this warning and uses correct Return: format.

../drivers/gpio/gpiolib-of.c:92: warning: Excess function parameter 'dev' description in 'of_gpio_need_valid_mask'

Fixes: f626d6dfb709 ("gpio: of: Break out OF-only code")
	Signed-off-by: Randy Dunlap <rdunlap@infradead.org>
	Cc: Linus Walleij <linus.walleij@linaro.org>
	Cc: Bartosz Golaszewski <bgolaszewski@baylibre.com>
	Cc: linux-gpio@vger.kernel.org
Link: https://lore.kernel.org/r/6c5d22c8-6e27-3314-9c46-701d932b11a6@infradead.org
	Signed-off-by: Linus Walleij <linus.walleij@linaro.org>
(cherry picked from commit 14e8c535ff684876d1be9dae475fe666b97c04a9)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/gpio/gpiolib-of.c
diff --cc drivers/gpio/gpiolib-of.c
index 85809bb5b516,3f50c433b7b1..000000000000
--- a/drivers/gpio/gpiolib-of.c
+++ b/drivers/gpio/gpiolib-of.c
@@@ -53,6 -81,24 +53,27 @@@ static struct gpio_desc *of_xlate_and_g
  	return gpiochip_get_desc(chip, ret);
  }
  
++<<<<<<< HEAD
++=======
+ /**
+  * of_gpio_need_valid_mask() - figure out if the OF GPIO driver needs
+  * to set the .valid_mask
+  * @gc: the target gpio_chip
+  *
+  * Return: true if the valid mask needs to be set
+  */
+ bool of_gpio_need_valid_mask(const struct gpio_chip *gc)
+ {
+ 	int size;
+ 	struct device_node *np = gc->of_node;
+ 
+ 	size = of_property_count_u32_elems(np,  "gpio-reserved-ranges");
+ 	if (size > 0 && size % 2 == 0)
+ 		return true;
+ 	return false;
+ }
+ 
++>>>>>>> 14e8c535ff68 (gpio: fix kernel-doc for of_gpio_need_valid_mask())
  static void of_gpio_flags_quirks(struct device_node *np,
  				 const char *propname,
  				 enum of_gpio_flags *flags,
* Unmerged path drivers/gpio/gpiolib-of.c
