usb: host: ohci-tmio: init genalloc for local memory

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-240.el8
commit-author Laurentiu Tudor <laurentiu.tudor@nxp.com>
commit 7d99532955d4649ba1417fb5a80252e0ce40f1e2
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-240.el8/7d995329.failed

In preparation for dropping the existing "coherent" dma mem declaration
APIs, replace the current dma_declare_coherent_memory() based mechanism
with the creation of a genalloc pool that will be used in the OHCI
subsystem as replacement for the DMA APIs.

	Signed-off-by: Laurentiu Tudor <laurentiu.tudor@nxp.com>
	Reviewed-by: Greg Kroah-Hartman <gregkh@linuxfoundation.org>
	Signed-off-by: Christoph Hellwig <hch@lst.de>
(cherry picked from commit 7d99532955d4649ba1417fb5a80252e0ce40f1e2)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/usb/host/ohci-tmio.c
diff --cc drivers/usb/host/ohci-tmio.c
index a631dbb369d7,3b84ce0c3f29..000000000000
--- a/drivers/usb/host/ohci-tmio.c
+++ b/drivers/usb/host/ohci-tmio.c
@@@ -224,11 -224,6 +224,14 @@@ static int ohci_hcd_tmio_drv_probe(stru
  		goto err_ioremap_regs;
  	}
  
++<<<<<<< HEAD
 +	ret = dma_declare_coherent_memory(&dev->dev, sram->start, sram->start,
 +				resource_size(sram), DMA_MEMORY_EXCLUSIVE);
 +	if (ret)
 +		goto err_dma_declare;
 +
++=======
++>>>>>>> 7d99532955d4 (usb: host: ohci-tmio: init genalloc for local memory)
  	if (cell->enable) {
  		ret = cell->enable(dev);
  		if (ret)
* Unmerged path drivers/usb/host/ohci-tmio.c
