net/mlx5: Define fdb tc levels per prio

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-240.el8
commit-author Paul Blakey <paulb@mellanox.com>
commit 4db7b98e943225dc2a7435811767e44f63640462
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-240.el8/4db7b98e.failed

Define FDB_TC_LEVELS_PER_PRIO instead of magic number 2.
This is the number of levels used by each tc prio table in the fdb.

	Signed-off-by: Paul Blakey <paulb@mellanox.com>
	Reviewed-by: Mark Bloch <markb@mellanox.com>
	Acked-by: Pablo Neira Ayuso <pablo@netfilter.org>
	Signed-off-by: Saeed Mahameed <saeedm@mellanox.com>
(cherry picked from commit 4db7b98e943225dc2a7435811767e44f63640462)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/mellanox/mlx5/core/eswitch.h
#	drivers/net/ethernet/mellanox/mlx5/core/fs_core.c
diff --cc drivers/net/ethernet/mellanox/mlx5/core/eswitch.h
index 38a4321bf0aa,8c9d8dc85861..000000000000
--- a/drivers/net/ethernet/mellanox/mlx5/core/eswitch.h
+++ b/drivers/net/ethernet/mellanox/mlx5/core/eswitch.h
@@@ -43,9 -43,10 +43,16 @@@
  #include <linux/mlx5/fs.h>
  #include "lib/mpfs.h"
  
++<<<<<<< HEAD
 +#define FDB_MAX_CHAIN 3
 +#define FDB_SLOW_PATH_CHAIN (FDB_MAX_CHAIN + 1)
 +#define FDB_MAX_PRIO 16
++=======
+ #define FDB_TC_MAX_CHAIN 3
+ #define FDB_TC_SLOW_PATH_CHAIN (FDB_TC_MAX_CHAIN + 1)
+ #define FDB_TC_MAX_PRIO 16
+ #define FDB_TC_LEVELS_PER_PRIO 2
++>>>>>>> 4db7b98e9432 (net/mlx5: Define fdb tc levels per prio)
  
  #ifdef CONFIG_MLX5_ESWITCH
  
@@@ -168,7 -173,7 +174,11 @@@ struct mlx5_eswitch_fdb 
  			struct {
  				struct mlx5_flow_table *fdb;
  				u32 num_rules;
++<<<<<<< HEAD
 +			} fdb_prio[FDB_MAX_CHAIN + 1][FDB_MAX_PRIO + 1][PRIO_LEVELS];
++=======
+ 			} fdb_prio[FDB_TC_MAX_CHAIN + 1][FDB_TC_MAX_PRIO + 1][FDB_TC_LEVELS_PER_PRIO];
++>>>>>>> 4db7b98e9432 (net/mlx5: Define fdb tc levels per prio)
  			/* Protects fdb_prio table */
  			struct mutex fdb_prio_lock;
  
diff --cc drivers/net/ethernet/mellanox/mlx5/core/fs_core.c
index d64993b85979,190c5c71b534..000000000000
--- a/drivers/net/ethernet/mellanox/mlx5/core/fs_core.c
+++ b/drivers/net/ethernet/mellanox/mlx5/core/fs_core.c
@@@ -2578,7 -2576,8 +2578,12 @@@ static int init_fdb_root_ns(struct mlx5
  		goto out_err;
  	}
  
++<<<<<<< HEAD
 +	levels = 2 * FDB_MAX_PRIO * (FDB_MAX_CHAIN + 1);
++=======
+ 	levels = FDB_TC_LEVELS_PER_PRIO *
+ 		 FDB_TC_MAX_PRIO * (FDB_TC_MAX_CHAIN + 1);
++>>>>>>> 4db7b98e9432 (net/mlx5: Define fdb tc levels per prio)
  	maj_prio = fs_create_prio_chained(&steering->fdb_root_ns->ns,
  					  FDB_FAST_PATH,
  					  levels);
@@@ -2594,8 -2593,9 +2599,14 @@@
  			goto out_err;
  		}
  
++<<<<<<< HEAD
 +		for (prio = 0; prio < FDB_MAX_PRIO * (chain + 1); prio++) {
 +			min_prio = fs_create_prio(ns, prio, 2);
++=======
+ 		for (prio = 0; prio < FDB_TC_MAX_PRIO * (chain + 1); prio++) {
+ 			min_prio = fs_create_prio(ns, prio,
+ 						  FDB_TC_LEVELS_PER_PRIO);
++>>>>>>> 4db7b98e9432 (net/mlx5: Define fdb tc levels per prio)
  			if (IS_ERR(min_prio)) {
  				err = PTR_ERR(min_prio);
  				goto out_err;
* Unmerged path drivers/net/ethernet/mellanox/mlx5/core/eswitch.h
* Unmerged path drivers/net/ethernet/mellanox/mlx5/core/fs_core.c
