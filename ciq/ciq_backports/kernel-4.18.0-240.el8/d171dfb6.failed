Bluetooth: btrtl: Fix an issue for the incorrect error return code.

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-240.el8
Rebuild_CHGLOG: - [bluetooth] Bluetooth: btrtl: Fix an issue for the incorrect error return code (Gopal Tiwari) [1838924]
Rebuild_FUZZ: 99.25%
commit-author Max Chou <max.chou@realtek.com>
commit d171dfb621240ee6c77cb354c0767b6dd41f83fe
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-240.el8/d171dfb6.failed

It does not need the '-' for PTR_ERR(skb) because PTR_ERR(skb) will
return the negative value during errors.

	Signed-off-by: Max Chou <max.chou@realtek.com>
	Signed-off-by: Marcel Holtmann <marcel@holtmann.org>
(cherry picked from commit d171dfb621240ee6c77cb354c0767b6dd41f83fe)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/bluetooth/btrtl.c
diff --cc drivers/bluetooth/btrtl.c
index 8307e5845311,ae9a2047f242..000000000000
--- a/drivers/bluetooth/btrtl.c
+++ b/drivers/bluetooth/btrtl.c
@@@ -305,9 -416,9 +305,15 @@@ static int rtl_download_firmware(struc
  		skb = __hci_cmd_sync(hdev, 0xfc20, frag_len + 1, dl_cmd,
  				     HCI_INIT_TIMEOUT);
  		if (IS_ERR(skb)) {
++<<<<<<< HEAD
 +			BT_ERR("%s: download fw command failed (%ld)",
 +			       hdev->name, PTR_ERR(skb));
 +			ret = -PTR_ERR(skb);
++=======
+ 			rtl_dev_err(hdev, "download fw command failed (%ld)",
+ 				    PTR_ERR(skb));
+ 			ret = PTR_ERR(skb);
++>>>>>>> d171dfb62124 (Bluetooth: btrtl: Fix an issue for the incorrect error return code.)
  			goto out;
  		}
  
* Unmerged path drivers/bluetooth/btrtl.c
