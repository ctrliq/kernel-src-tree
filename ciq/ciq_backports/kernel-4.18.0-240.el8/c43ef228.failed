mlxsw: spectrum_router: Do not assume RTNL is taken during nexthop init

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-240.el8
commit-author Ido Schimmel <idosch@mellanox.com>
commit c43ef22843db4a415d43854eaae4b08b9dcae394
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-240.el8/c43ef228.failed

RTNL is going to be removed from route insertion path, so use
__in_dev_get_rcu() from an RCU read-side critical section instead of
__in_dev_get_rtnl() which assumes RTNL is taken.

	Signed-off-by: Ido Schimmel <idosch@mellanox.com>
	Acked-by: Jiri Pirko <jiri@mellanox.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit c43ef22843db4a415d43854eaae4b08b9dcae394)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/mellanox/mlxsw/spectrum_router.c
diff --cc drivers/net/ethernet/mellanox/mlxsw/spectrum_router.c
index e11e167d054c,28dca7aa4ce0..000000000000
--- a/drivers/net/ethernet/mellanox/mlxsw/spectrum_router.c
+++ b/drivers/net/ethernet/mellanox/mlxsw/spectrum_router.c
@@@ -3848,10 -3840,14 +3848,18 @@@ static int mlxsw_sp_nexthop4_init(struc
  	if (!dev)
  		return 0;
  
- 	in_dev = __in_dev_get_rtnl(dev);
+ 	rcu_read_lock();
+ 	in_dev = __in_dev_get_rcu(dev);
  	if (in_dev && IN_DEV_IGNORE_ROUTES_WITH_LINKDOWN(in_dev) &&
++<<<<<<< HEAD
 +	    fib_nh->nh_flags & RTNH_F_LINKDOWN)
++=======
+ 	    fib_nh->fib_nh_flags & RTNH_F_LINKDOWN) {
+ 		rcu_read_unlock();
++>>>>>>> c43ef22843db (mlxsw: spectrum_router: Do not assume RTNL is taken during nexthop init)
  		return 0;
+ 	}
+ 	rcu_read_unlock();
  
  	err = mlxsw_sp_nexthop4_type_init(mlxsw_sp, nh, fib_nh);
  	if (err)
* Unmerged path drivers/net/ethernet/mellanox/mlxsw/spectrum_router.c
