flow_dissector: add meaningful comments

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-240.el8
commit-author Matteo Croce <mcroce@redhat.com>
commit 98298e6ca6d5908f96e529e70a254a4d5bf754e7
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-240.el8/98298e6c.failed

Documents two piece of code which can't be understood at a glance.

	Signed-off-by: Matteo Croce <mcroce@redhat.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 98298e6ca6d5908f96e529e70a254a4d5bf754e7)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/core/flow_dissector.c
diff --cc net/core/flow_dissector.c
index 7abf2e67e8a1,bc22b384ac6c..000000000000
--- a/net/core/flow_dissector.c
+++ b/net/core/flow_dissector.c
@@@ -1377,6 -1407,10 +1377,13 @@@ static const void *flow_keys_hash_start
  static inline size_t flow_keys_hash_length(const struct flow_keys *flow)
  {
  	size_t diff = FLOW_KEYS_HASH_OFFSET + sizeof(flow->addrs);
++<<<<<<< HEAD
++=======
+ 	BUILD_BUG_ON((sizeof(*flow) - FLOW_KEYS_HASH_OFFSET) % sizeof(u32));
+ 	/* flow.addrs MUST be the last member in struct flow_keys because
+ 	 * different L3 protocols have different address length
+ 	 */
++>>>>>>> 98298e6ca6d5 (flow_dissector: add meaningful comments)
  	BUILD_BUG_ON(offsetof(typeof(*flow), addrs) !=
  		     sizeof(*flow) - sizeof(flow->addrs));
  
diff --git a/include/net/flow_dissector.h b/include/net/flow_dissector.h
index 93bf96a1987b..25f7638cf850 100644
--- a/include/net/flow_dissector.h
+++ b/include/net/flow_dissector.h
@@ -283,6 +283,7 @@ struct flow_keys {
 	struct flow_dissector_key_vlan cvlan;
 	struct flow_dissector_key_keyid keyid;
 	struct flow_dissector_key_ports ports;
+	/* 'addrs' must be the last member */
 	struct flow_dissector_key_addrs addrs;
 };
 
* Unmerged path net/core/flow_dissector.c
