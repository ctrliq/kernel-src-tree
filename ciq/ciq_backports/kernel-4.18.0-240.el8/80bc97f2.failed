KVM: Fix the indentation to match coding style

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-240.el8
commit-author Haiwei Li <lihaiwei@tencent.com>
commit 80bc97f2d84dbec6df6b36d6bbc0715c14f80c61
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-240.el8/80bc97f2.failed

There is a bad indentation in next&queue branch. The patch looks like
fixes nothing though it fixes the indentation.

Before fixing:

                 if (!handle_fastpath_set_x2apic_icr_irqoff(vcpu, data)) {
                         kvm_skip_emulated_instruction(vcpu);
                         ret = EXIT_FASTPATH_EXIT_HANDLED;
                }
                 break;
         case MSR_IA32_TSCDEADLINE:

After fixing:

                 if (!handle_fastpath_set_x2apic_icr_irqoff(vcpu, data)) {
                         kvm_skip_emulated_instruction(vcpu);
                         ret = EXIT_FASTPATH_EXIT_HANDLED;
                 }
                 break;
         case MSR_IA32_TSCDEADLINE:

	Signed-off-by: Haiwei Li <lihaiwei@tencent.com>
Message-Id: <2f78457e-f3a7-3bc9-e237-3132ee87f71e@gmail.com>
	Signed-off-by: Paolo Bonzini <pbonzini@redhat.com>
(cherry picked from commit 80bc97f2d84dbec6df6b36d6bbc0715c14f80c61)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	arch/x86/kvm/x86.c
diff --cc arch/x86/kvm/x86.c
index c6d2c69f3cd9,375d3fc0a4a6..000000000000
--- a/arch/x86/kvm/x86.c
+++ b/arch/x86/kvm/x86.c
@@@ -1615,18 -1628,26 +1615,32 @@@ enum exit_fastpath_completion handle_fa
  	switch (msr) {
  	case APIC_BASE_MSR + (APIC_ICR >> 4):
  		data = kvm_read_edx_eax(vcpu);
++<<<<<<< HEAD
 +		ret = handle_fastpath_set_x2apic_icr_irqoff(vcpu, data);
++=======
+ 		if (!handle_fastpath_set_x2apic_icr_irqoff(vcpu, data)) {
+ 			kvm_skip_emulated_instruction(vcpu);
+ 			ret = EXIT_FASTPATH_EXIT_HANDLED;
+ 		}
+ 		break;
+ 	case MSR_IA32_TSCDEADLINE:
+ 		data = kvm_read_edx_eax(vcpu);
+ 		if (!handle_fastpath_set_tscdeadline(vcpu, data)) {
+ 			kvm_skip_emulated_instruction(vcpu);
+ 			ret = EXIT_FASTPATH_REENTER_GUEST;
+ 		}
++>>>>>>> 80bc97f2d84d (KVM: Fix the indentation to match coding style)
  		break;
  	default:
 -		break;
 +		return EXIT_FASTPATH_NONE;
  	}
  
 -	if (ret != EXIT_FASTPATH_NONE)
 +	if (!ret) {
  		trace_kvm_msr_write(msr, data);
 +		return EXIT_FASTPATH_SKIP_EMUL_INS;
 +	}
  
 -	return ret;
 +	return EXIT_FASTPATH_NONE;
  }
  EXPORT_SYMBOL_GPL(handle_fastpath_set_msr_irqoff);
  
* Unmerged path arch/x86/kvm/x86.c
