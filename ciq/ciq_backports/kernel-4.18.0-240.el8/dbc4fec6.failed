NFSv4.0 allow nconnect for v4.0

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-240.el8
commit-author Olga Kornievskaia <kolga@netapp.com>
commit dbc4fec6b6dd2d23e161b250d51cbd28bd9c8497
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-240.el8/dbc4fec6.failed

It looks like this "else" is just a typo.  It turns off nconnect for
NFSv4.0 even though it works for every other version.

	Signed-off-by: Olga Kornievskaia <kolga@netapp.com>
	Signed-off-by: J. Bruce Fields <bfields@redhat.com>
	Signed-off-by: Trond Myklebust <trond.myklebust@hammerspace.com>
(cherry picked from commit dbc4fec6b6dd2d23e161b250d51cbd28bd9c8497)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/nfs/nfs4client.c
diff --cc fs/nfs/nfs4client.c
index 43ea8e59787e,daacc78a3d48..000000000000
--- a/fs/nfs/nfs4client.c
+++ b/fs/nfs/nfs4client.c
@@@ -878,8 -878,11 +878,14 @@@ static int nfs4_set_client(struct nfs_s
  	};
  	struct nfs_client *clp;
  
++<<<<<<< HEAD
 +	if (minorversion > 0 && proto == XPRT_TRANSPORT_TCP)
++=======
+ 	if (minorversion == 0)
+ 		__set_bit(NFS_CS_REUSEPORT, &cl_init.init_flags);
+ 	if (proto == XPRT_TRANSPORT_TCP)
++>>>>>>> dbc4fec6b6dd (NFSv4.0 allow nconnect for v4.0)
  		cl_init.nconnect = nconnect;
 -
  	if (server->flags & NFS_MOUNT_NORESVPORT)
  		__set_bit(NFS_CS_NORESVPORT, &cl_init.init_flags);
  	if (server->options & NFS_OPTION_MIGRATION)
* Unmerged path fs/nfs/nfs4client.c
