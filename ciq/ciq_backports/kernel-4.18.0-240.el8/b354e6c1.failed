Bluetooth: L2CAP: Fix a condition in l2cap_sock_recvmsg()

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-240.el8
commit-author Dan Carpenter <dan.carpenter@oracle.com>
commit b354e6c10eaf97a1f5aff4839795bd32ad32f0f9
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-240.el8/b354e6c1.failed

Smatch complains about the indenting:

    net/bluetooth/l2cap_sock.c:1027 l2cap_sock_recvmsg()
    warn: inconsistent indenting

It looks like this is supposed to be an "else if" condition.

Fixes: 15f02b910562 ("Bluetooth: L2CAP: Add initial code for Enhanced Credit Based Mode")
	Signed-off-by: Dan Carpenter <dan.carpenter@oracle.com>
	Signed-off-by: Marcel Holtmann <marcel@holtmann.org>
(cherry picked from commit b354e6c10eaf97a1f5aff4839795bd32ad32f0f9)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/bluetooth/l2cap_sock.c
diff --cc net/bluetooth/l2cap_sock.c
index e980d3fce548,40fb10b591bd..000000000000
--- a/net/bluetooth/l2cap_sock.c
+++ b/net/bluetooth/l2cap_sock.c
@@@ -1001,7 -1020,11 +1001,15 @@@ static int l2cap_sock_recvmsg(struct so
  
  	if (sk->sk_state == BT_CONNECT2 && test_bit(BT_SK_DEFER_SETUP,
  						    &bt_sk(sk)->flags)) {
++<<<<<<< HEAD
 +		if (bdaddr_type_is_le(pi->chan->src_type)) {
++=======
+ 		if (pi->chan->mode == L2CAP_MODE_EXT_FLOWCTL) {
+ 			sk->sk_state = BT_CONNECTED;
+ 			pi->chan->state = BT_CONNECTED;
+ 			__l2cap_ecred_conn_rsp_defer(pi->chan);
+ 		} else if (bdaddr_type_is_le(pi->chan->src_type)) {
++>>>>>>> b354e6c10eaf (Bluetooth: L2CAP: Fix a condition in l2cap_sock_recvmsg())
  			sk->sk_state = BT_CONNECTED;
  			pi->chan->state = BT_CONNECTED;
  			__l2cap_le_connect_rsp_defer(pi->chan);
* Unmerged path net/bluetooth/l2cap_sock.c
