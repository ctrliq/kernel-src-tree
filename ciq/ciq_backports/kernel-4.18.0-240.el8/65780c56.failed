veth: move xdp stats in a dedicated structure

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-240.el8
commit-author Lorenzo Bianconi <lorenzo@kernel.org>
commit 65780c5627a26d788f10e2b1a37125779a556a58
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-240.el8/65780c56.failed

Move xdp stats in veth_stats data structure. This is a preliminary patch
to align xdp statistics to mlx5, ixgbe and mvneta drivers

	Signed-off-by: Lorenzo Bianconi <lorenzo@kernel.org>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 65780c5627a26d788f10e2b1a37125779a556a58)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/veth.c
diff --cc drivers/net/veth.c
index 013947869d96,33e23bbde5bf..000000000000
--- a/drivers/net/veth.c
+++ b/drivers/net/veth.c
@@@ -333,12 -317,12 +337,17 @@@ static void veth_get_stats64(struct net
  {
  	struct veth_priv *priv = netdev_priv(dev);
  	struct net_device *peer;
++<<<<<<< HEAD
 +	struct veth_rq_stats rx;
 +	struct pcpu_lstats tx;
++=======
+ 	struct veth_stats rx;
+ 	u64 packets, bytes;
++>>>>>>> 65780c5627a2 (veth: move xdp stats in a dedicated structure)
  
 -	tot->tx_dropped = veth_stats_tx(dev, &packets, &bytes);
 -	tot->tx_bytes = bytes;
 -	tot->tx_packets = packets;
 +	tot->tx_dropped = veth_stats_tx(&tx, dev);
 +	tot->tx_bytes = tx.bytes;
 +	tot->tx_packets = tx.packets;
  
  	veth_stats_rx(&rx, dev);
  	tot->rx_dropped = rx.xdp_drops;
* Unmerged path drivers/net/veth.c
