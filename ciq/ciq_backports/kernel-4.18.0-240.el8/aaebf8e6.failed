Bluetooth: Fix incorrect type for window and interval

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-240.el8
commit-author Abhishek Pandit-Subedi <abhishekpandit@chromium.org>
commit aaebf8e6088270e45d30314031b5d9a88a589cb9
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-240.el8/aaebf8e6.failed

The types for window and interval should be uint16, not uint8.

	Signed-off-by: Abhishek Pandit-Subedi <abhishekpandit@chromium.org>
	Signed-off-by: Marcel Holtmann <marcel@holtmann.org>
(cherry picked from commit aaebf8e6088270e45d30314031b5d9a88a589cb9)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/bluetooth/hci_request.c
diff --cc net/bluetooth/hci_request.c
index bc53cb509df7,f6870e98faab..000000000000
--- a/net/bluetooth/hci_request.c
+++ b/net/bluetooth/hci_request.c
@@@ -774,6 -890,12 +774,15 @@@ void hci_req_add_le_passive_scan(struc
  	struct hci_dev *hdev = req->hdev;
  	u8 own_addr_type;
  	u8 filter_policy;
++<<<<<<< HEAD
++=======
+ 	u16 window, interval;
+ 
+ 	if (hdev->scanning_paused) {
+ 		bt_dev_dbg(hdev, "Scanning is paused for suspend");
+ 		return;
+ 	}
++>>>>>>> aaebf8e60882 (Bluetooth: Fix incorrect type for window and interval)
  
  	/* Set require_privacy to false since no SCAN_REQ are send
  	 * during passive scanning. Not using an non-resolvable address
* Unmerged path net/bluetooth/hci_request.c
