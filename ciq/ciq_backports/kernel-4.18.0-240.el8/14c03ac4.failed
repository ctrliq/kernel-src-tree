net: devlink: remove unused devlink_port_get_phys_port_name() function

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-240.el8
Rebuild_CHGLOG: - [net] devlink: remove unused devlink_port_get_phys_port_name() function (Petr Oros) [1809496]
Rebuild_FUZZ: 96.30%
commit-author Jiri Pirko <jiri@mellanox.com>
commit 14c03ac4c100e4b81ec4747f5ec861701ff52de2
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-240.el8/14c03ac4.failed

Now it is unused, remove it.

	Signed-off-by: Jiri Pirko <jiri@mellanox.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 14c03ac4c100e4b81ec4747f5ec861701ff52de2)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	include/net/devlink.h
diff --cc include/net/devlink.h
index fca90b027029,31d5cec4d06b..000000000000
--- a/include/net/devlink.h
+++ b/include/net/devlink.h
@@@ -609,18 -582,7 +609,22 @@@ void devlink_port_type_clear(struct dev
  void devlink_port_attrs_set(struct devlink_port *devlink_port,
  			    enum devlink_port_flavour flavour,
  			    u32 port_number, bool split,
++<<<<<<< HEAD
 +			    u32 split_subport_number,
 +			    const unsigned char *switch_id,
 +			    unsigned char switch_id_len);
 +int devlink_port_get_phys_port_name(struct devlink_port *devlink_port,
 +				    char *name, size_t len);
 +void devlink_port_attrs_pci_pf_set(struct devlink_port *devlink_port,
 +				   const unsigned char *switch_id,
 +				   unsigned char switch_id_len, u16 pf);
 +void devlink_port_attrs_pci_vf_set(struct devlink_port *devlink_port,
 +				   const unsigned char *switch_id,
 +				   unsigned char switch_id_len,
 +				   u16 pf, u16 vf);
++=======
+ 			    u32 split_subport_number);
++>>>>>>> 14c03ac4c100 (net: devlink: remove unused devlink_port_get_phys_port_name() function)
  int devlink_sb_register(struct devlink *devlink, unsigned int sb_index,
  			u32 size, u16 ingress_pools_count,
  			u16 egress_pools_count, u16 ingress_tc_count,
* Unmerged path include/net/devlink.h
diff --git a/net/core/devlink.c b/net/core/devlink.c
index ac5bbc974636..dadd36163b6a 100644
--- a/net/core/devlink.c
+++ b/net/core/devlink.c
@@ -5872,13 +5872,6 @@ static int __devlink_port_phys_port_name_get(struct devlink_port *devlink_port,
 	return 0;
 }
 
-int devlink_port_get_phys_port_name(struct devlink_port *devlink_port,
-				    char *name, size_t len)
-{
-	return __devlink_port_phys_port_name_get(devlink_port, name, len);
-}
-EXPORT_SYMBOL_GPL(devlink_port_get_phys_port_name);
-
 int devlink_sb_register(struct devlink *devlink, unsigned int sb_index,
 			u32 size, u16 ingress_pools_count,
 			u16 egress_pools_count, u16 ingress_tc_count,
