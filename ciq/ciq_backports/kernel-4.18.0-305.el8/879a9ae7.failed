radix tree test suite: Remove multiorder benchmarking

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-305.el8
commit-author Matthew Wilcox <willy@infradead.org>
commit 879a9ae7b5bc046f195a725d62bbc96258e5d0c8
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-305.el8/879a9ae7.failed

The multiorder radix tree code is being removed, so remove the
benchmarking of its performance.

	Signed-off-by: Matthew Wilcox <willy@infradead.org>
(cherry picked from commit 879a9ae7b5bc046f195a725d62bbc96258e5d0c8)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	tools/testing/radix-tree/benchmark.c
diff --cc tools/testing/radix-tree/benchmark.c
index 99c40f3ed133,7e195ed8e92d..000000000000
--- a/tools/testing/radix-tree/benchmark.c
+++ b/tools/testing/radix-tree/benchmark.c
@@@ -242,16 -154,5 +238,20 @@@ void benchmark(void
  
  	for (c = 0; size[c]; c++)
  		for (s = 0; step[s]; s++)
++<<<<<<< HEAD
 +			benchmark_size(size[c], step[s], 0);
 +
 +	for (c = 0; size[c]; c++)
 +		for (s = 0; step[s]; s++)
 +			benchmark_size(size[c], step[s] << 9, 9);
 +
 +	for (c = 0; size[c]; c++)
 +		for (s = 0; step[s]; s++)
 +			benchmark_split(size[c], step[s]);
 +
 +	for (s = 0; step[s]; s++)
 +		benchmark_join(step[s]);
++=======
+ 			benchmark_size(size[c], step[s]);
++>>>>>>> 879a9ae7b5bc (radix tree test suite: Remove multiorder benchmarking)
  }
* Unmerged path tools/testing/radix-tree/benchmark.c
