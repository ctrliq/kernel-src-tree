dlm: add new RELEASE_RECOVER uevent attribute for release_lockspace

jira KERNEL-878
Rebuild_History Non-Buildable kernel-4.18.0-553.120.1.el8_10
commit-author Alexander Aring <aahringo@redhat.com>
commit 5665374c7246159dbca1a80a0b54ad27e3379bcb
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-553.120.1.el8_10/5665374c.failed

RELEASE_RECOVER=0|1 is passed to user space as part of the OFFLINE
uevent for leaving a lockspace, and used by subsequent patches.
RELEASE_RECOVER=1 tells user space that the release_lockspace/leave
should be handled by the remaining lockspace members as if the
leaving node had failed.

	Signed-off-by: Alexander Aring <aahringo@redhat.com>
	Signed-off-by: David Teigland <teigland@redhat.com>
(cherry picked from commit 5665374c7246159dbca1a80a0b54ad27e3379bcb)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/dlm/lockspace.c
diff --cc fs/dlm/lockspace.c
index 8bc309c01dca,6ff666a511c7..000000000000
--- a/fs/dlm/lockspace.c
+++ b/fs/dlm/lockspace.c
@@@ -784,10 -731,14 +789,16 @@@ static int release_lockspace(struct dlm
  		return rv;
  	}
  
 -	if (ls_count == 1)
 -		dlm_midcomms_version_wait();
 -
  	dlm_device_deregister(ls);
  
++<<<<<<< HEAD
 +	if (force != 3 && dlm_user_daemon_available())
 +		do_uevent(ls, 0);
++=======
+ 	if (release_option != DLM_RELEASE_NO_EVENT &&
+ 	    dlm_user_daemon_available())
+ 		do_uevent(ls, 0, 0);
++>>>>>>> 5665374c7246 (dlm: add new RELEASE_RECOVER uevent attribute for release_lockspace)
  
  	dlm_recoverd_stop(ls);
  
* Unmerged path fs/dlm/lockspace.c
