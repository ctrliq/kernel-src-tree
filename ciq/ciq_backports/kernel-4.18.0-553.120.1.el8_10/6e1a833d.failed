gfs2: bufdata allocation race

jira KERNEL-878
Rebuild_History Non-Buildable kernel-4.18.0-553.120.1.el8_10
commit-author Andreas Gruenbacher <agruenba@redhat.com>
commit 6e1a833df9524e05889cf0fe02879b02d85776fd
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-553.120.1.el8_10/6e1a833d.failed

The locking in gfs2_trans_add_data() and gfs2_trans_add_meta() doesn't
follow the usual coding pattern of checking bh->b_private under lock,
allocating a new bufdata object with the locks dropped, and re-checking
once the lock has been reacquired.  Both functions set bh->b_private
without holding the buffer lock.  Fix that.

Also, in gfs2_trans_add_meta(), taking the folio lock during the
allocation doesn't actually do anything useful.

	Signed-off-by: Andreas Gruenbacher <agruenba@redhat.com>
(cherry picked from commit 6e1a833df9524e05889cf0fe02879b02d85776fd)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/gfs2/trans.c
diff --cc fs/gfs2/trans.c
index 84b9f776183b,65cbe06e301a..000000000000
--- a/fs/gfs2/trans.c
+++ b/fs/gfs2/trans.c
@@@ -203,17 -204,20 +202,24 @@@ void gfs2_trans_add_data(struct gfs2_gl
  		set_bit(TR_TOUCHED, &tr->tr_flags);
  		goto out;
  	}
 -	spin_lock(&sdp->sd_log_lock);
 +	gfs2_log_lock(sdp);
  	bd = bh->b_private;
  	if (bd == NULL) {
 -		spin_unlock(&sdp->sd_log_lock);
 +		gfs2_log_unlock(sdp);
  		unlock_buffer(bh);
- 		if (bh->b_private == NULL)
- 			bd = gfs2_alloc_bufdata(gl, bh);
- 		else
- 			bd = bh->b_private;
+ 		bd = gfs2_alloc_bufdata(gl, bh);
  		lock_buffer(bh);
++<<<<<<< HEAD
 +		gfs2_log_lock(sdp);
++=======
+ 		spin_lock(&sdp->sd_log_lock);
+ 		if (bh->b_private) {
+ 			kmem_cache_free(gfs2_bufdata_cachep, bd);
+ 			bd = bh->b_private;
+ 		} else {
+ 			bh->b_private = bd;
+ 		}
++>>>>>>> 6e1a833df952 (gfs2: bufdata allocation race)
  	}
  	gfs2_assert(sdp, bd->bd_gl == gl);
  	set_bit(TR_TOUCHED, &tr->tr_flags);
@@@ -244,19 -268,20 +250,31 @@@ void gfs2_trans_add_meta(struct gfs2_gl
  		set_bit(TR_TOUCHED, &tr->tr_flags);
  		goto out;
  	}
 -	spin_lock(&sdp->sd_log_lock);
 +	gfs2_log_lock(sdp);
  	bd = bh->b_private;
  	if (bd == NULL) {
 -		spin_unlock(&sdp->sd_log_lock);
 +		gfs2_log_unlock(sdp);
  		unlock_buffer(bh);
++<<<<<<< HEAD
 +		lock_page(bh->b_page);
 +		if (bh->b_private == NULL)
 +			bd = gfs2_alloc_bufdata(gl, bh);
 +		else
 +			bd = bh->b_private;
 +		unlock_page(bh->b_page);
 +		lock_buffer(bh);
 +		gfs2_log_lock(sdp);
++=======
+ 		bd = gfs2_alloc_bufdata(gl, bh);
+ 		lock_buffer(bh);
+ 		spin_lock(&sdp->sd_log_lock);
+ 		if (bh->b_private) {
+ 			kmem_cache_free(gfs2_bufdata_cachep, bd);
+ 			bd = bh->b_private;
+ 		} else {
+ 			bh->b_private = bd;
+ 		}
++>>>>>>> 6e1a833df952 (gfs2: bufdata allocation race)
  	}
  	gfs2_assert(sdp, bd->bd_gl == gl);
  	set_bit(TR_TOUCHED, &tr->tr_flags);
* Unmerged path fs/gfs2/trans.c
