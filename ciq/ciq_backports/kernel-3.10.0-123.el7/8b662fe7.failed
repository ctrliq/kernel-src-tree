net/cxgb4: Fix referencing freed adapter

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-123.el7
Rebuild_CHGLOG: - [ethernet] cxgb4: Fix referencing freed adapter (Steve Best) [1063483]
Rebuild_FUZZ: 94.74%
commit-author Gavin Shan <shangw@linux.vnet.ibm.com>
commit 8b662fe70c68282f78482dc272df0c4f355e49f5
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-123.el7/8b662fe7.failed

The adapter is freed before we check its flags. It was caused
by commit 144be3d ("net/cxgb4: Avoid disabling PCI device for
towice"). The problem was reported by Intel's "0-day" tool.

The patch fixes it to avoid reverting commit 144be3d.

	Reported-by: Dan Carpenter <dan.carpenter@oracle.com>
	Signed-off-by: Gavin Shan <shangw@linux.vnet.ibm.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 8b662fe70c68282f78482dc272df0c4f355e49f5)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/chelsio/cxgb4/cxgb4_main.c
diff --cc drivers/net/ethernet/chelsio/cxgb4/cxgb4_main.c
index 5aafe28a9bff,43ab35fea48d..000000000000
--- a/drivers/net/ethernet/chelsio/cxgb4/cxgb4_main.c
+++ b/drivers/net/ethernet/chelsio/cxgb4/cxgb4_main.c
@@@ -5832,16 -6161,15 +5832,19 @@@ static void remove_one(struct pci_dev *
  
  		free_some_resources(adapter);
  		iounmap(adapter->regs);
 -		if (!is_t4(adapter->params.chip))
 +		if (!is_t4(adapter->chip))
  			iounmap(adapter->bar2);
- 		kfree(adapter);
  		pci_disable_pcie_error_reporting(pdev);
  		if ((adapter->flags & DEV_ENABLED)) {
  			pci_disable_device(pdev);
  			adapter->flags &= ~DEV_ENABLED;
  		}
  		pci_release_regions(pdev);
++<<<<<<< HEAD
 +		pci_set_drvdata(pdev, NULL);
++=======
+ 		kfree(adapter);
++>>>>>>> 8b662fe70c68 (net/cxgb4: Fix referencing freed adapter)
  	} else
  		pci_release_regions(pdev);
  }
* Unmerged path drivers/net/ethernet/chelsio/cxgb4/cxgb4_main.c
