PCI: Simplify IOV implementation and fix reference count races

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-123.el7
Rebuild_CHGLOG: - [pci] Simplify IOV implementation and fix reference count races (Myron Stowe) [984759]
Rebuild_FUZZ: 95.80%
commit-author Jiang Liu <liuj97@gmail.com>
commit dc087f2f6a2925e81831f3016b9cbb6e470e7423
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-123.el7/dc087f2f.failed

Trivial changes to IOV:

  1) use new PCI interfaces to simplify IOV implementation
  2) fix some reference count related race windows

[bhelgaas: fix virtfn_add() add bus/alloc dev error paths]
	Signed-off-by: Jiang Liu <jiang.liu@huawei.com>
	Signed-off-by: Bjorn Helgaas <bhelgaas@google.com>
	Cc: Donald Dutile <ddutile@redhat.com>
	Cc: Yinghai Lu <yinghai@kernel.org>
	Cc: Ram Pai <linuxram@us.ibm.com>
(cherry picked from commit dc087f2f6a2925e81831f3016b9cbb6e470e7423)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/pci/iov.c
diff --cc drivers/pci/iov.c
index ff062266a293,5fffca995a91..000000000000
--- a/drivers/pci/iov.c
+++ b/drivers/pci/iov.c
@@@ -74,18 -66,17 +66,30 @@@ static int virtfn_add(struct pci_dev *d
  	struct pci_dev *virtfn;
  	struct resource *res;
  	struct pci_sriov *iov = dev->sriov;
 -	struct pci_bus *bus;
 +
++<<<<<<< HEAD
 +	virtfn = alloc_pci_dev();
 +	if (!virtfn)
 +		return -ENOMEM;
  
 +	mutex_lock(&iov->dev->sriov->lock);
 +	virtfn->bus = virtfn_add_bus(dev->bus, virtfn_bus(dev, id));
 +	if (!virtfn->bus) {
 +		kfree(virtfn);
 +		mutex_unlock(&iov->dev->sriov->lock);
 +		return -ENOMEM;
 +	}
++=======
+ 	mutex_lock(&iov->dev->sriov->lock);
+ 	bus = virtfn_add_bus(dev->bus, virtfn_bus(dev, id));
+ 	if (!bus)
+ 		goto failed;
+ 
+ 	virtfn = pci_alloc_dev(bus);
+ 	if (!virtfn)
+ 		goto failed0;
+ 
++>>>>>>> dc087f2f6a29 (PCI: Simplify IOV implementation and fix reference count races)
  	virtfn->devfn = virtfn_devfn(dev, id);
  	virtfn->vendor = dev->vendor;
  	pci_read_config_word(dev, iov->pos + PCI_SRIOV_VF_DID, &virtfn->device);
* Unmerged path drivers/pci/iov.c
