NFS: Make nfs_readdir revalidate less often

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-123.el7
commit-author Scott Mayhew <smayhew@redhat.com>
commit 07b5ce8ef2d87f1914054804720d6facbaa3f4ce
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-123.el7/07b5ce8e.failed

Make nfs_readdir revalidate only when we're at the beginning of the directory or
if the cached attributes have expired.

	Signed-off-by: Scott Mayhew <smayhew@redhat.com>
	Signed-off-by: Trond Myklebust <Trond.Myklebust@netapp.com>
(cherry picked from commit 07b5ce8ef2d87f1914054804720d6facbaa3f4ce)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/nfs/dir.c
diff --cc fs/nfs/dir.c
index c662ff6befd6,e474ca2b2bfe..000000000000
--- a/fs/nfs/dir.c
+++ b/fs/nfs/dir.c
@@@ -806,8 -817,8 +806,13 @@@ static int nfs_readdir(struct file *fil
  	struct inode	*inode = dentry->d_inode;
  	nfs_readdir_descriptor_t my_desc,
  			*desc = &my_desc;
++<<<<<<< HEAD
 +	struct nfs_open_dir_context *dir_ctx = filp->private_data;
 +	int res;
++=======
+ 	struct nfs_open_dir_context *dir_ctx = file->private_data;
+ 	int res = 0;
++>>>>>>> 07b5ce8ef2d8 (NFS: Make nfs_readdir revalidate less often)
  
  	dfprintk(FILE, "NFS: readdir(%s/%s) starting at cookie %llu\n",
  			dentry->d_parent->d_name.name, dentry->d_name.name,
@@@ -822,13 -833,15 +827,18 @@@
  	 */
  	memset(desc, 0, sizeof(*desc));
  
 -	desc->file = file;
 -	desc->ctx = ctx;
 +	desc->file = filp;
  	desc->dir_cookie = &dir_ctx->dir_cookie;
  	desc->decode = NFS_PROTO(inode)->decode_dirent;
 -	desc->plus = nfs_use_readdirplus(inode, ctx) ? 1 : 0;
 +	desc->plus = nfs_use_readdirplus(inode, filp) ? 1 : 0;
  
  	nfs_block_sillyrename(dentry);
++<<<<<<< HEAD
 +	res = nfs_revalidate_mapping(inode, filp->f_mapping);
++=======
+ 	if (ctx->pos == 0 || nfs_attribute_cache_expired(inode))
+ 		res = nfs_revalidate_mapping(inode, file->f_mapping);
++>>>>>>> 07b5ce8ef2d8 (NFS: Make nfs_readdir revalidate less often)
  	if (res < 0)
  		goto out;
  
* Unmerged path fs/nfs/dir.c
