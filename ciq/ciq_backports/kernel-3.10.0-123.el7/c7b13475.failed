USB: console: remove unnecessary operations test

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-123.el7
Rebuild_CHGLOG: - [usb] console: remove unnecessary operations test (Don Zickus) [895641]
Rebuild_FUZZ: 94.51%
commit-author Johan Hovold <jhovold@gmail.com>
commit c7b13475b4466adcdcd8378f12b4ee416ef9b98b
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-123.el7/c7b13475.failed

Remove unnecessary tests for open and write operations as these are set
to the generic implementations by usb-serial core if left unset by a
subdriver.

	Signed-off-by: Johan Hovold <jhovold@gmail.com>
	Signed-off-by: Greg Kroah-Hartman <gregkh@linuxfoundation.org>
(cherry picked from commit c7b13475b4466adcdcd8378f12b4ee416ef9b98b)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/usb/serial/console.c
diff --cc drivers/usb/serial/console.c
index 5f3bcd31e204,c69bb50d4663..000000000000
--- a/drivers/usb/serial/console.c
+++ b/drivers/usb/serial/console.c
@@@ -230,21 -226,14 +226,32 @@@ static void usb_console_write(struct co
  		}
  		/* pass on to the driver specific version of this function if
  		   it is available */
++<<<<<<< HEAD
 +		if (serial->type->write)
 +			retval = serial->type->write(NULL, port, buf, i);
 +		else
 +			retval = usb_serial_generic_write(NULL, port, buf, i);
 +		pr_debug("%s - return value : %d\n", __func__, retval);
 +		if (lf) {
 +			/* append CR after LF */
 +			unsigned char cr = 13;
 +			if (serial->type->write)
 +				retval = serial->type->write(NULL,
 +								port, &cr, 1);
 +			else
 +				retval = usb_serial_generic_write(NULL,
 +								port, &cr, 1);
 +			pr_debug("%s - return value : %d\n", __func__, retval);
++=======
+ 		retval = serial->type->write(NULL, port, buf, i);
+ 		dev_dbg(&port->dev, "%s - write: %d\n", __func__, retval);
+ 		if (lf) {
+ 			/* append CR after LF */
+ 			unsigned char cr = 13;
+ 			retval = serial->type->write(NULL, port, &cr, 1);
+ 			dev_dbg(&port->dev, "%s - write cr: %d\n",
+ 							__func__, retval);
++>>>>>>> c7b13475b446 (USB: console: remove unnecessary operations test)
  		}
  		buf += i;
  		count -= i;
* Unmerged path drivers/usb/serial/console.c
