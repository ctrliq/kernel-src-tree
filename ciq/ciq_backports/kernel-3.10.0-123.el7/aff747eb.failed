ALSA: hda - Get rid of AMD HDMI exception in hdmi_present_sense()

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-123.el7
Rebuild_CHGLOG: - [alsa] hda: Get rid of AMD HDMI exception in hdmi_present_sense() (Jaroslav Kysela) [1044022]
Rebuild_FUZZ: 92.68%
commit-author Takashi Iwai <tiwai@suse.de>
commit aff747ebbf68c6efc4d091d54058af89b916f82d
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-123.el7/aff747eb.failed

Since the recent fake ELD patches, we can remove the check for AMD
HDMI in hdmi_present_sense() and decide the return value from
eld_valid value.

Suggested by Anssi Hannula.

	Signed-off-by: Takashi Iwai <tiwai@suse.de>
(cherry picked from commit aff747ebbf68c6efc4d091d54058af89b916f82d)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	sound/pci/hda/patch_hdmi.c
diff --cc sound/pci/hda/patch_hdmi.c
index f36275527fe5,e68792311bb2..000000000000
--- a/sound/pci/hda/patch_hdmi.c
+++ b/sound/pci/hda/patch_hdmi.c
@@@ -1366,6 -1560,15 +1366,18 @@@ static void hdmi_present_sense(struct h
  		snd_ctl_notify(codec->bus->card,
  			       SNDRV_CTL_EVENT_MASK_VALUE | SNDRV_CTL_EVENT_MASK_INFO,
  			       &per_pin->eld_ctl->id);
++<<<<<<< HEAD
++=======
+  unlock:
+ 	ret = !repoll || !pin_eld->monitor_present || pin_eld->eld_valid;
+ 
+ 	jack = snd_hda_jack_tbl_get(codec, pin_nid);
+ 	if (jack)
+ 		jack->block_report = !ret;
+ 
+ 	mutex_unlock(&per_pin->lock);
+ 	return ret;
++>>>>>>> aff747ebbf68 (ALSA: hda - Get rid of AMD HDMI exception in hdmi_present_sense())
  }
  
  static void hdmi_repoll_eld(struct work_struct *work)
* Unmerged path sound/pci/hda/patch_hdmi.c
