radeon/i2c: do not count reg index in number of i2c byte we are writing.

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-123.el7
Rebuild_CHGLOG: - [drm] radeon/i2c: do not count reg index in number of i2c byte we are writing (Rob Clark) [1054409]
Rebuild_FUZZ: 99.30%
commit-author Jerome Glisse <jglisse@redhat.com>
commit fae009d15a44e5f1d938340facf4b8bc7dc69a09
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-123.el7/fae009d1.failed

Useless to count the register index in number of bytes we are writing.

Fixes a regression with hw i2c enabled.

	Signed-off-by: Jerome Glisse <jglisse@redhat.com>
	Signed-off-by: Alex Deucher <alexander.deucher@amd.com>
	Cc: stable@vger.kernel.org
(cherry picked from commit fae009d15a44e5f1d938340facf4b8bc7dc69a09)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/gpu/drm/radeon/atombios_i2c.c
diff --cc drivers/gpu/drm/radeon/atombios_i2c.c
index 082338df708a,0652ee0a2098..000000000000
--- a/drivers/gpu/drm/radeon/atombios_i2c.c
+++ b/drivers/gpu/drm/radeon/atombios_i2c.c
@@@ -50,10 -52,14 +50,18 @@@ static int radeon_process_i2c_ch(struc
  
  	if (flags & HW_I2C_WRITE) {
  		if (num > ATOM_MAX_HW_I2C_WRITE) {
 -			DRM_ERROR("hw i2c: tried to write too many bytes (%d vs 3)\n", num);
 +			DRM_ERROR("hw i2c: tried to write too many bytes (%d vs 2)\n", num);
  			return -EINVAL;
  		}
++<<<<<<< HEAD
 +		memcpy(&out, buf, num);
++=======
+ 		args.ucRegIndex = buf[0];
+ 		if (num > 1) {
+ 			num--;
+ 			memcpy(&out, &buf[1], num);
+ 		}
++>>>>>>> fae009d15a44 (radeon/i2c: do not count reg index in number of i2c byte we are writing.)
  		args.lpI2CDataOut = cpu_to_le16(out);
  	} else {
  		if (num > ATOM_MAX_HW_I2C_READ) {
* Unmerged path drivers/gpu/drm/radeon/atombios_i2c.c
