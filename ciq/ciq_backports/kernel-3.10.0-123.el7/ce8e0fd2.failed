ALSA: hda/analog - Handle inverted EAPD properly in vmaster hook

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-123.el7
Rebuild_CHGLOG: - [alsa] hda/analog: Handle inverted EAPD properly in vmaster hook (Jaroslav Kysela) [1044022]
Rebuild_FUZZ: 92.56%
commit-author Takashi Iwai <tiwai@suse.de>
commit ce8e0fd239e411e08a0cd83868898cd3f573d7cf
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-123.el7/ce8e0fd2.failed

ad_vmaster_eapd_hook() needs to handle the inverted EAPD case
properly, too.  Otherwise the output gets broken on Lenovo N100 with
AD1986A codec.

Bugzilla: https://bugzilla.kernel.org/show_bug.cgi?id=64971
	Signed-off-by: Takashi Iwai <tiwai@suse.de>
(cherry picked from commit ce8e0fd239e411e08a0cd83868898cd3f573d7cf)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	sound/pci/hda/patch_analog.c
diff --cc sound/pci/hda/patch_analog.c
index dd94b3ac9e08,f6351b8e0f5d..000000000000
--- a/sound/pci/hda/patch_analog.c
+++ b/sound/pci/hda/patch_analog.c
@@@ -139,6 -139,20 +139,23 @@@ static int ad198x_suspend(struct hda_co
  }
  #endif
  
++<<<<<<< HEAD
++=======
+ /* follow EAPD via vmaster hook */
+ static void ad_vmaster_eapd_hook(void *private_data, int enabled)
+ {
+ 	struct hda_codec *codec = private_data;
+ 	struct ad198x_spec *spec = codec->spec;
+ 
+ 	if (!spec->eapd_nid)
+ 		return;
+ 	if (codec->inv_eapd)
+ 		enabled = !enabled;
+ 	snd_hda_codec_update_cache(codec, spec->eapd_nid, 0,
+ 				   AC_VERB_SET_EAPD_BTLENABLE,
+ 				   enabled ? 0x02 : 0x00);
+ }
++>>>>>>> ce8e0fd239e4 (ALSA: hda/analog - Handle inverted EAPD properly in vmaster hook)
  
  /*
   * Automatic parse of I/O pins from the BIOS configuration
* Unmerged path sound/pci/hda/patch_analog.c
