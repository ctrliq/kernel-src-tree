powerpc/dma: dma_set_coherent_mask() should not be GPL only

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-327.el7
Rebuild_CHGLOG: - [powerpc] dma: dma_set_coherent_mask() should not be GPL only (Gustavo Duarte) [1275976]
Rebuild_FUZZ: 92.73%
commit-author Benjamin Herrenschmidt <benh@kernel.crashing.org>
commit 977bf062bba3eb8d03f66d5b4e227e5d7ebc1e08
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-327.el7/977bf062.failed

When turning this from inline to an exported function I was a bit
over-eager and made it GPL only. This prevents the use of pretty much
all non-GPL PCI driver which is a bit over the top. Let's bring it
back in line with other architecture.

Fixes: 817820b0226a ("powerpc/iommu: Support "hybrid" iommu/direct DMA ops for coherent_mask < dma_mask")
	Signed-off-by: Benjamin Herrenschmidt <benh@kernel.crashing.org>
	Signed-off-by: Michael Ellerman <mpe@ellerman.id.au>
(cherry picked from commit 977bf062bba3eb8d03f66d5b4e227e5d7ebc1e08)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	arch/powerpc/kernel/dma.c
diff --cc arch/powerpc/kernel/dma.c
index ee78f6e49d64,3f1472a78f39..000000000000
--- a/arch/powerpc/kernel/dma.c
+++ b/arch/powerpc/kernel/dma.c
@@@ -189,6 -286,25 +189,28 @@@ struct dma_map_ops dma_direct_ops = 
  };
  EXPORT_SYMBOL(dma_direct_ops);
  
++<<<<<<< HEAD
++=======
+ int dma_set_coherent_mask(struct device *dev, u64 mask)
+ {
+ 	if (!dma_supported(dev, mask)) {
+ 		/*
+ 		 * We need to special case the direct DMA ops which can
+ 		 * support a fallback for coherent allocations. There
+ 		 * is no dma_op->set_coherent_mask() so we have to do
+ 		 * things the hard way:
+ 		 */
+ 		if (get_dma_ops(dev) != &dma_direct_ops ||
+ 		    get_iommu_table_base(dev) == NULL ||
+ 		    !dma_iommu_dma_supported(dev, mask))
+ 			return -EIO;
+ 	}
+ 	dev->coherent_dma_mask = mask;
+ 	return 0;
+ }
+ EXPORT_SYMBOL(dma_set_coherent_mask);
+ 
++>>>>>>> 977bf062bba3 (powerpc/dma: dma_set_coherent_mask() should not be GPL only)
  #define PREALLOC_DMA_DEBUG_ENTRIES (1 << 16)
  
  int __dma_set_mask(struct device *dev, u64 dma_mask)
* Unmerged path arch/powerpc/kernel/dma.c
