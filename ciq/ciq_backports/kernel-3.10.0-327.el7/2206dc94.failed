ALSA: hda/generic - Check power state cap at updating the widget power

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-327.el7
Rebuild_CHGLOG: - [alsa] hda/generic - Check power state cap at updating the widget power (Jaroslav Kysela) [1197064]
Rebuild_FUZZ: 95.52%
commit-author Takashi Iwai <tiwai@suse.de>
commit 2206dc949286fe0010c69213d3d8b4c53e6a2295
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-327.el7/2206dc94.failed

The new widget power-saving tries to apply the power change no matter
whether the node has a power cap or not.  It's bad (although most of
codecs chip just ignore it).  Check the capability properly
beforehand.

	Signed-off-by: Takashi Iwai <tiwai@suse.de>
(cherry picked from commit 2206dc949286fe0010c69213d3d8b4c53e6a2295)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	sound/pci/hda/hda_generic.c
diff --cc sound/pci/hda/hda_generic.c
index 0ef2459cd05f,afc6b1b0898c..000000000000
--- a/sound/pci/hda/hda_generic.c
+++ b/sound/pci/hda/hda_generic.c
@@@ -832,7 -832,9 +832,13 @@@ static hda_nid_t path_power_update(stru
  
  	for (i = 0; i < path->depth; i++) {
  		nid = path->path[i];
++<<<<<<< HEAD
 +		if (nid == codec->afg)
++=======
+ 		if (!(get_wcaps(codec, nid) & AC_WCAP_POWER))
+ 			continue;
+ 		if (nid == codec->core.afg)
++>>>>>>> 2206dc949286 (ALSA: hda/generic - Check power state cap at updating the widget power)
  			continue;
  		if (!allow_powerdown || is_active_nid_for_any(codec, nid))
  			state = AC_PWRST_D0;
* Unmerged path sound/pci/hda/hda_generic.c
