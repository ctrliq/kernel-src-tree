cxgb4: Don't call t4_slow_intr_handler when we're not the Master PF

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-327.el7
commit-author Hariprasad Shenai <hariprasad@chelsio.com>
commit c3c7b1217f557f9585f3805803d2126c2d385592
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-327.el7/c3c7b121.failed

	Signed-off-by: Hariprasad Shenai <hariprasad@chelsio.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit c3c7b1217f557f9585f3805803d2126c2d385592)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/chelsio/cxgb4/sge.c
diff --cc drivers/net/ethernet/chelsio/cxgb4/sge.c
index af3e7dc260f3,0d2eddab04ef..000000000000
--- a/drivers/net/ethernet/chelsio/cxgb4/sge.c
+++ b/drivers/net/ethernet/chelsio/cxgb4/sge.c
@@@ -2120,8 -2253,9 +2121,14 @@@ static irqreturn_t t4_intr_intx(int irq
  {
  	struct adapter *adap = cookie;
  
++<<<<<<< HEAD
 +	t4_write_reg(adap, MYPF_REG(PCIE_PF_CLI), 0);
 +	if (t4_slow_intr_handler(adap) | process_intrq(adap))
++=======
+ 	t4_write_reg(adap, MYPF_REG(PCIE_PF_CLI_A), 0);
+ 	if (((adap->flags & MASTER_PF) && t4_slow_intr_handler(adap)) |
+ 	    process_intrq(adap))
++>>>>>>> c3c7b1217f55 (cxgb4: Don't call t4_slow_intr_handler when we're not the Master PF)
  		return IRQ_HANDLED;
  	return IRQ_NONE;             /* probably shared interrupt */
  }
diff --git a/drivers/net/ethernet/chelsio/cxgb4/cxgb4_main.c b/drivers/net/ethernet/chelsio/cxgb4/cxgb4_main.c
index 1b2d97e91707..953ad108bd39 100644
--- a/drivers/net/ethernet/chelsio/cxgb4/cxgb4_main.c
+++ b/drivers/net/ethernet/chelsio/cxgb4/cxgb4_main.c
@@ -806,7 +806,8 @@ static irqreturn_t t4_nondata_intr(int irq, void *cookie)
 		adap->swintr = 1;
 		t4_write_reg(adap, MYPF_REG(PL_PF_INT_CAUSE), v);
 	}
-	t4_slow_intr_handler(adap);
+	if (adap->flags & MASTER_PF)
+		t4_slow_intr_handler(adap);
 	return IRQ_HANDLED;
 }
 
* Unmerged path drivers/net/ethernet/chelsio/cxgb4/sge.c
