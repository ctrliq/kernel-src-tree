thinkpad_acpi: support new BIOS version string pattern

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-327.el7
commit-author Adam Lee <adam.lee@canonical.com>
commit 1b0eb5bc241354aa854671fdf02132d2d1452bdf
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-327.el7/1b0eb5bc.failed

Latest ThinkPad models use a new string pattern of BIOS version,
thinkpad_acpi won't be loaded automatically without this fix.

	Signed-off-by: Adam Lee <adam.lee@canonical.com>

Intentatation cleanup.

	Signed-off-by: Darren Hart <dvhart@linux.intel.com>
(cherry picked from commit 1b0eb5bc241354aa854671fdf02132d2d1452bdf)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/platform/x86/thinkpad_acpi.c
diff --cc drivers/platform/x86/thinkpad_acpi.c
index a884609631b8,3b8ceee7c5cb..000000000000
--- a/drivers/platform/x86/thinkpad_acpi.c
+++ b/drivers/platform/x86/thinkpad_acpi.c
@@@ -8777,11 -8885,14 +8777,19 @@@ static bool __pure __init tpacpi_is_fw_
  	return (c >= '0' && c <= '9') || (c >= 'A' && c <= 'Z');
  }
  
++<<<<<<< HEAD
 +/* Most models: xxyTkkWW (#.##c); Ancient 570/600 and -SL lacks (#.##c) */
 +static bool __pure __init tpacpi_is_valid_fw_id(const char* const s,
++=======
+ static bool __pure __init tpacpi_is_valid_fw_id(const char * const s,
++>>>>>>> 1b0eb5bc2413 (thinkpad_acpi: support new BIOS version string pattern)
  						const char t)
  {
- 	return s && strlen(s) >= 8 &&
+ 	/*
+ 	 * Most models: xxyTkkWW (#.##c)
+ 	 * Ancient 570/600 and -SL lacks (#.##c)
+ 	 */
+ 	if (s && strlen(s) >= 8 &&
  		tpacpi_is_fw_digit(s[0]) &&
  		tpacpi_is_fw_digit(s[1]) &&
  		s[2] == t &&
* Unmerged path drivers/platform/x86/thinkpad_acpi.c
