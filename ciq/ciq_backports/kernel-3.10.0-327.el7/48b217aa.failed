ALSA: pcm: Fix document for snd_pcm_stop_xrun()

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-327.el7
Rebuild_CHGLOG: - [alsa] pcm: Fix document for snd_pcm_stop_xrun() (Jaroslav Kysela) [1197064]
Rebuild_FUZZ: 93.18%
commit-author Takashi Iwai <tiwai@suse.de>
commit 48b217aa43abc8c3545bb9b4d7a5b525b71d6ac2
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-327.el7/48b217aa.failed

Fix a copy & paste error:
 Warning(sound/core/pcm_native.c:1112): Excess function parameter 'state' description in 'snd_pcm_stop_xrun'

The state argument was dropped from snd_pcm_stop_xrun().

	Reported-by: kbuild test robot <fengguang.wu@intel.com>
	Signed-off-by: Takashi Iwai <tiwai@suse.de>
(cherry picked from commit 48b217aa43abc8c3545bb9b4d7a5b525b71d6ac2)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	sound/core/pcm_native.c
diff --cc sound/core/pcm_native.c
index f2b67f92c031,095d9572ad2b..000000000000
--- a/sound/core/pcm_native.c
+++ b/sound/core/pcm_native.c
@@@ -1098,6 -1098,28 +1098,31 @@@ int snd_pcm_drain_done(struct snd_pcm_s
  				     SNDRV_PCM_STATE_SETUP);
  }
  
++<<<<<<< HEAD
++=======
+ /**
+  * snd_pcm_stop_xrun - stop the running streams as XRUN
+  * @substream: the PCM substream instance
+  *
+  * This stops the given running substream (and all linked substreams) as XRUN.
+  * Unlike snd_pcm_stop(), this function takes the substream lock by itself.
+  *
+  * Return: Zero if successful, or a negative error code.
+  */
+ int snd_pcm_stop_xrun(struct snd_pcm_substream *substream)
+ {
+ 	unsigned long flags;
+ 	int ret = 0;
+ 
+ 	snd_pcm_stream_lock_irqsave(substream, flags);
+ 	if (snd_pcm_running(substream))
+ 		ret = snd_pcm_stop(substream, SNDRV_PCM_STATE_XRUN);
+ 	snd_pcm_stream_unlock_irqrestore(substream, flags);
+ 	return ret;
+ }
+ EXPORT_SYMBOL_GPL(snd_pcm_stop_xrun);
+ 
++>>>>>>> 48b217aa43ab (ALSA: pcm: Fix document for snd_pcm_stop_xrun())
  /*
   * pause callbacks
   */
* Unmerged path sound/core/pcm_native.c
