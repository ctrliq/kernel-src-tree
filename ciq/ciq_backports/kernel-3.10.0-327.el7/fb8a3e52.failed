CIFS: Improve indentation in cifs_user_read()

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-327.el7
commit-author Pavel Shilovsky <pshilovsky@samba.org>
commit fb8a3e52559ad52829c6838d304f5b75c140b97a
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-327.el7/fb8a3e52.failed

	Signed-off-by: Pavel Shilovsky <pshilovsky@samba.org>
	Signed-off-by: Steve French <smfrench@gmail.com>
(cherry picked from commit fb8a3e52559ad52829c6838d304f5b75c140b97a)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/cifs/file.c
diff --cc fs/cifs/file.c
index aef9c17411b5,e17012817d9d..000000000000
--- a/fs/cifs/file.c
+++ b/fs/cifs/file.c
@@@ -2969,15 -3038,16 +2967,26 @@@ again
  						rdata->bytes, rdata->cfile,
  						cifs_sb, &tmp_list);
  
- 					list_splice(&tmp_list, &rdata_list);
+ 				list_splice(&tmp_list, &rdata_list);
  
++<<<<<<< HEAD
 +					kref_put(&rdata->refcount,
 +						cifs_uncached_readdata_release);
 +					goto again;
 +				}
 +			} else {
 +				rc = cifs_readdata_to_iov(rdata, &to);
 +			}
++=======
+ 				kref_put(&rdata->refcount,
+ 					 cifs_uncached_readdata_release);
+ 				goto again;
+ 			} else if (rdata->result)
+ 				rc = rdata->result;
+ 			else
+ 				rc = cifs_readdata_to_iov(rdata, to);
+ 
++>>>>>>> fb8a3e52559a (CIFS: Improve indentation in cifs_user_read())
  			/* if there was a short read -- discard anything left */
  			if (rdata->got_bytes && rdata->got_bytes < rdata->bytes)
  				rc = -ENODATA;
* Unmerged path fs/cifs/file.c
