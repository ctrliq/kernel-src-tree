bsg: fix potential error pointer dereference

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-327.el7
commit-author Jens Axboe <axboe@fb.com>
commit 55872c5a3c01f0fe7b5298d19e24e237f5b5ff06
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-327.el7/55872c5a.failed

Dan writes:

block/bsg.c:327 bsg_map_hdr() error: 'next_rq' dereferencing possible
ERR_PTR().

Fix this by setting next_rq to NULL, for the case where it can be
!= NULL but an error pointer.

	Reported-by: Dan Carpenter <dan.carpenter@oracle.com>
	Signed-off-by: Jens Axboe <axboe@fb.com>
(cherry picked from commit 55872c5a3c01f0fe7b5298d19e24e237f5b5ff06)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	block/bsg.c
diff --cc block/bsg.c
index 992c00b4b8e4,276e869e686c..000000000000
--- a/block/bsg.c
+++ b/block/bsg.c
@@@ -285,8 -285,9 +285,14 @@@ bsg_map_hdr(struct bsg_device *bd, stru
  		}
  
  		next_rq = blk_get_request(q, READ, GFP_KERNEL);
++<<<<<<< HEAD
 +		if (!next_rq) {
 +			ret = -ENOMEM;
++=======
+ 		if (IS_ERR(next_rq)) {
+ 			ret = PTR_ERR(next_rq);
+ 			next_rq = NULL;
++>>>>>>> 55872c5a3c01 (bsg: fix potential error pointer dereference)
  			goto out;
  		}
  		rq->next_rq = next_rq;
* Unmerged path block/bsg.c
