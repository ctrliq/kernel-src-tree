rtnl: fix error path when adding an iface with a link net

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-327.el7
commit-author Nicolas Dichtel <nicolas.dichtel@6wind.com>
commit bdef279b993b3a112c493447d5b3d8f28c229d88
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-327.el7/bdef279b.failed

If an error occurs when the netdevice is moved to the link netns, a full cleanup
must be done.

Fixes: 317f4810e45e ("rtnl: allow to create device with IFLA_LINK_NETNSID set")
	Signed-off-by: Nicolas Dichtel <nicolas.dichtel@6wind.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit bdef279b993b3a112c493447d5b3d8f28c229d88)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/core/rtnetlink.c
diff --cc net/core/rtnetlink.c
index 187818c8949a,07447d1665e6..000000000000
--- a/net/core/rtnetlink.c
+++ b/net/core/rtnetlink.c
@@@ -1919,20 -2147,39 +1919,28 @@@ replay
  
  		dev->ifindex = ifm->ifi_index;
  
 -		if (ops->newlink) {
 +		if (ops->newlink)
  			err = ops->newlink(net, dev, tb, data);
 -			/* Drivers should call free_netdev() in ->destructor
 -			 * and unregister it on failure after registration
 -			 * so that device could be finally freed in rtnl_unlock.
 -			 */
 -			if (err < 0) {
 -				/* If device is not registered at all, free it now */
 -				if (dev->reg_state == NETREG_UNINITIALIZED)
 -					free_netdev(dev);
 -				goto out;
 -			}
 -		} else {
 +		else
  			err = register_netdevice(dev);
 -			if (err < 0) {
 -				free_netdev(dev);
 -				goto out;
 -			}
 -		}
 -		err = rtnl_configure_link(dev, ifm);
 -		if (err < 0) {
 -			unregister_netdevice(dev);
 +
++<<<<<<< HEAD
 +		if (err < 0 && !IS_ERR(dev))
 +			free_netdev(dev);
 +		if (err < 0)
  			goto out;
 -		}
  
 +		err = rtnl_configure_link(dev, ifm);
 +		if (err < 0)
 +			unregister_netdevice(dev);
++=======
+ 		if (link_net) {
+ 			err = dev_change_net_namespace(dev, dest_net, ifname);
+ 			if (err < 0)
+ 				unregister_netdevice(dev);
+ 		}
++>>>>>>> bdef279b993b (rtnl: fix error path when adding an iface with a link net)
  out:
 -		if (link_net)
 -			put_net(link_net);
  		put_net(dest_net);
  		return err;
  	}
* Unmerged path net/core/rtnetlink.c
