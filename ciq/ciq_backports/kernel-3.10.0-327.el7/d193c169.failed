HID: whitespace cleanup

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-327.el7
Rebuild_CHGLOG: - [pci] Whitespace cleanup (Myron Stowe) [1110898]
Rebuild_FUZZ: 87.80%
commit-author Dennis Chen <barracks510@gmail.com>
commit d193c169149abb1c54e4e969e78600ae75141e36
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-327.el7/d193c169.failed

Removes trailing whitespace in hid-core.c and usbhid/hid-quirks.c

	Signed-off-by: Dennis Chen <barracks510@gmail.com>
	Reviewed-by: Benjamin Tissoires <benjamin.tissoires@redhat.com>
	Signed-off-by: Jiri Kosina <jkosina@suse.cz>
(cherry picked from commit d193c169149abb1c54e4e969e78600ae75141e36)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/hid/hid-core.c
diff --cc drivers/hid/hid-core.c
index c4e5cdfa5d24,0ab9b0eb56a9..000000000000
--- a/drivers/hid/hid-core.c
+++ b/drivers/hid/hid-core.c
@@@ -2006,7 -2297,12 +2006,11 @@@ static struct device_attribute hid_dev_
  
  static int hid_uevent(struct device *dev, struct kobj_uevent_env *env)
  {
++<<<<<<< HEAD
 +	struct hid_device *hdev = container_of(dev, struct hid_device, dev);
++=======
+ 	struct hid_device *hdev = to_hid_device(dev);
++>>>>>>> d193c169149a (HID: whitespace cleanup)
  
  	if (add_uevent_var(env, "HID_ID=%04X:%08X:%08X",
  			hdev->bus, hdev->vendor, hdev->product))
* Unmerged path drivers/hid/hid-core.c
diff --git a/drivers/hid/usbhid/hid-quirks.c b/drivers/hid/usbhid/hid-quirks.c
index 0db9a67278ba..fde8af051191 100644
--- a/drivers/hid/usbhid/hid-quirks.c
+++ b/drivers/hid/usbhid/hid-quirks.c
@@ -244,7 +244,7 @@ static void usbhid_remove_all_dquirks(void)
 
 }
 
-/** 
+/**
  * usbhid_quirks_init: apply USB HID quirks specified at module load time
  */
 int usbhid_quirks_init(char **quirks_param)
@@ -307,7 +307,7 @@ static const struct hid_blacklist *usbhid_exists_squirk(const u16 idVendor,
 
 	if (bl_entry != NULL)
 		dbg_hid("Found squirk 0x%x for USB HID vendor 0x%hx prod 0x%hx\n",
-				bl_entry->quirks, bl_entry->idVendor, 
+				bl_entry->quirks, bl_entry->idVendor,
 				bl_entry->idProduct);
 	return bl_entry;
 }
