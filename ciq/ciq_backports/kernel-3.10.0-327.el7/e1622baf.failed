dev: set iflink to 0 for virtual interfaces

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-327.el7
commit-author Nicolas Dichtel <nicolas.dichtel@6wind.com>
commit e1622baf54df8cc958bf29d71de5ad545ea7d93c
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-327.el7/e1622baf.failed

Virtual interfaces are supposed to set an iflink value != of their ifindex.
It was not the case for some of them, like vxlan, bond or bridge.
Let's set iflink to 0 when dev->rtnl_link_ops is set.

	Signed-off-by: Nicolas Dichtel <nicolas.dichtel@6wind.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit e1622baf54df8cc958bf29d71de5ad545ea7d93c)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/core/dev.c
diff --cc net/core/dev.c
index 1924c9647d47,26622d614f81..000000000000
--- a/net/core/dev.c
+++ b/net/core/dev.c
@@@ -650,6 -660,27 +650,30 @@@ __setup("netdev=", netdev_boot_setup)
  *******************************************************************************/
  
  /**
++<<<<<<< HEAD
++=======
+  *	dev_get_iflink	- get 'iflink' value of a interface
+  *	@dev: targeted interface
+  *
+  *	Indicates the ifindex the interface is linked to.
+  *	Physical interfaces have the same 'ifindex' and 'iflink' values.
+  */
+ 
+ int dev_get_iflink(const struct net_device *dev)
+ {
+ 	if (dev->netdev_ops && dev->netdev_ops->ndo_get_iflink)
+ 		return dev->netdev_ops->ndo_get_iflink(dev);
+ 
+ 	/* If dev->rtnl_link_ops is set, it's a virtual interface. */
+ 	if (dev->rtnl_link_ops)
+ 		return 0;
+ 
+ 	return dev->ifindex;
+ }
+ EXPORT_SYMBOL(dev_get_iflink);
+ 
+ /**
++>>>>>>> e1622baf54df (dev: set iflink to 0 for virtual interfaces)
   *	__dev_get_by_name	- find a device by its name
   *	@net: the applicable net namespace
   *	@name: name to find
* Unmerged path net/core/dev.c
