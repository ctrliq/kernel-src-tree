virtio_net: drop config_enable

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-327.el7
Rebuild_CHGLOG: - [virtio] virtio-net: drop config_enable (Jason Wang) [1227339]
Rebuild_FUZZ: 96.67%
commit-author Michael S. Tsirkin <mst@redhat.com>
commit 102a2786c9df756cffdbcfd11096124e4dc6c311
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-327.el7/102a2786.failed

Now that virtio core ensures config changes don't arrive during probing,
drop config_enable flag in virtio net.
On removal, flush is now sufficient to guarantee that no change work is
queued.

This help simplify the driver, and will allow setting DRIVER_OK earlier
without losing config change notifications.

	Signed-off-by: Michael S. Tsirkin <mst@redhat.com>
	Reviewed-by: Cornelia Huck <cornelia.huck@de.ibm.com>
	Signed-off-by: Rusty Russell <rusty@rustcorp.com.au>
(cherry picked from commit 102a2786c9df756cffdbcfd11096124e4dc6c311)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/virtio_net.c
diff --cc drivers/net/virtio_net.c
index 54b3d59391dd,743fb04a4df9..000000000000
--- a/drivers/net/virtio_net.c
+++ b/drivers/net/virtio_net.c
@@@ -1618,12 -1744,14 +1612,11 @@@ static int virtnet_probe(struct virtio_
  	if (vi->stats == NULL)
  		goto free;
  
 -	for_each_possible_cpu(i) {
 -		struct virtnet_stats *virtnet_stats;
 -		virtnet_stats = per_cpu_ptr(vi->stats, i);
 -		u64_stats_init(&virtnet_stats->tx_syncp);
 -		u64_stats_init(&virtnet_stats->rx_syncp);
 -	}
 +	vi->vq_index = alloc_percpu(int);
 +	if (vi->vq_index == NULL)
 +		goto free_stats;
  
  	mutex_init(&vi->config_lock);
- 	vi->config_enable = true;
  	INIT_WORK(&vi->config_work, virtnet_config_changed_work);
  
  	/* If we can receive ANY GSO packets, we must allocate large ones. */
@@@ -1735,9 -1875,6 +1726,12 @@@ static void virtnet_remove(struct virti
  
  	remove_vq_common(vi);
  
++<<<<<<< HEAD
 +	flush_work(&vi->config_work);
 +
 +	free_percpu(vi->vq_index);
++=======
++>>>>>>> 102a2786c9df (virtio_net: drop config_enable)
  	free_percpu(vi->stats);
  	free_netdev(vi->dev);
  }
* Unmerged path drivers/net/virtio_net.c
