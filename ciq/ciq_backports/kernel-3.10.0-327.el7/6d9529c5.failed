crypto: hash - initialize entry len for null input in crypto hash sg list walk

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-327.el7
Rebuild_CHGLOG: - [crypto] ahash: initialize entry len for null input in crypto hash sg list walk (Herbert Xu) [1173756]
Rebuild_FUZZ: 91.89%
commit-author Tim Chen <tim.c.chen@linux.intel.com>
commit 6d9529c586dbf0d356916d113f72f36397e71cdb
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-327.el7/6d9529c5.failed

For the special case when we have a null input string, we want
to initialize the entry len to 0 for the hash/ahash walk, so
cyrpto_hash_walk_last will return the correct result indicating
that we have completed the scatter list walk.  Otherwise we may
keep walking the sg list and access bogus memory address.

	Signed-off-by: Tim Chen <tim.c.chen@linux.intel.com>
	Signed-off-by: Herbert Xu <herbert@gondor.apana.org.au>
(cherry picked from commit 6d9529c586dbf0d356916d113f72f36397e71cdb)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	crypto/ahash.c
diff --cc crypto/ahash.c
index 793a27f2493e,f6a36a52d738..000000000000
--- a/crypto/ahash.c
+++ b/crypto/ahash.c
@@@ -130,6 -144,27 +132,30 @@@ int crypto_hash_walk_first(struct ahash
  }
  EXPORT_SYMBOL_GPL(crypto_hash_walk_first);
  
++<<<<<<< HEAD
++=======
+ int crypto_ahash_walk_first(struct ahash_request *req,
+ 			    struct crypto_hash_walk *walk)
+ {
+ 	walk->total = req->nbytes;
+ 
+ 	if (!walk->total) {
+ 		walk->entrylen = 0;
+ 		return 0;
+ 	}
+ 
+ 	walk->alignmask = crypto_ahash_alignmask(crypto_ahash_reqtfm(req));
+ 	walk->sg = req->src;
+ 	walk->flags = req->base.flags & CRYPTO_TFM_REQ_MASK;
+ 	walk->flags |= CRYPTO_ALG_ASYNC;
+ 
+ 	BUILD_BUG_ON(CRYPTO_TFM_REQ_MASK & CRYPTO_ALG_ASYNC);
+ 
+ 	return hash_walk_new_entry(walk);
+ }
+ EXPORT_SYMBOL_GPL(crypto_ahash_walk_first);
+ 
++>>>>>>> 6d9529c586db (crypto: hash - initialize entry len for null input in crypto hash sg list walk)
  int crypto_hash_walk_first_compat(struct hash_desc *hdesc,
  				  struct crypto_hash_walk *walk,
  				  struct scatterlist *sg, unsigned int len)
* Unmerged path crypto/ahash.c
