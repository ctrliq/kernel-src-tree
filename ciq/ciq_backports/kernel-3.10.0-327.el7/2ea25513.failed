net: Create xmit_one() helper for dev_hard_start_xmit()

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-327.el7
Rebuild_CHGLOG: - [net] Create xmit_one() helper for dev_hard_start_xmit() (Alexander Duyck) [1205266]
Rebuild_FUZZ: 95.24%
commit-author David S. Miller <davem@davemloft.net>
commit 2ea255137555052655c6a646c4e48ea7481494c7
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-327.el7/2ea25513.failed

Hopefully making the code a bit easier to read and digest.

	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 2ea255137555052655c6a646c4e48ea7481494c7)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/core/dev.c
diff --cc net/core/dev.c
index 1924c9647d47,0fde7d2153db..000000000000
--- a/net/core/dev.c
+++ b/net/core/dev.c
@@@ -2488,9 -2619,7 +2505,8 @@@ static int xmit_one(struct sk_buff *skb
  int dev_hard_start_xmit(struct sk_buff *skb, struct net_device *dev,
  			struct netdev_queue *txq)
  {
 +	const struct net_device_ops *ops = dev->netdev_ops;
  	int rc = NETDEV_TX_OK;
- 	unsigned int skb_len;
  
  	if (likely(!skb->next)) {
  		netdev_features_t features;
@@@ -2548,15 -2677,7 +2564,19 @@@
  			}
  		}
  
++<<<<<<< HEAD
 +		if (!list_empty(&ptype_all))
 +			dev_queue_xmit_nit(skb, dev);
 +
 +		skb_len = skb->len;
 +		rc = ops->ndo_start_xmit(skb, dev);
 +		trace_net_dev_xmit(skb, rc, dev, skb_len);
 +		if (rc == NETDEV_TX_OK)
 +			txq_trans_update(txq);
 +		return rc;
++=======
+ 		return xmit_one(skb, dev, txq);
++>>>>>>> 2ea255137555 (net: Create xmit_one() helper for dev_hard_start_xmit())
  	}
  
  gso:
@@@ -2566,12 -2687,7 +2586,16 @@@
  		skb->next = nskb->next;
  		nskb->next = NULL;
  
++<<<<<<< HEAD
 +		if (!list_empty(&ptype_all))
 +			dev_queue_xmit_nit(nskb, dev);
 +
 +		skb_len = nskb->len;
 +		rc = ops->ndo_start_xmit(nskb, dev);
 +		trace_net_dev_xmit(nskb, rc, dev, skb_len);
++=======
+ 		rc = xmit_one(nskb, dev, txq);
++>>>>>>> 2ea255137555 (net: Create xmit_one() helper for dev_hard_start_xmit())
  		if (unlikely(rc != NETDEV_TX_OK)) {
  			if (rc & ~NETDEV_TX_MASK)
  				goto out_kfree_gso_skb;
* Unmerged path net/core/dev.c
