cxgb4: remove bond->lock

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-327.el7
commit-author Nikolay Aleksandrov <nikolay@redhat.com>
commit 51752afa735ab28b6023fcf22ec63955107f8a65
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-327.el7/51752afa.failed

RTNL should be already held in the notifier call so the slave list can
be traversed without a problem, remove the unnecessary bond->lock.

CC: Hariprasad S <hariprasad@chelsio.com>
	Signed-off-by: Nikolay Aleksandrov <nikolay@redhat.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 51752afa735ab28b6023fcf22ec63955107f8a65)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/chelsio/cxgb4/cxgb4_main.c
diff --cc drivers/net/ethernet/chelsio/cxgb4/cxgb4_main.c
index 9df6656ca8da,d6a9a0bc94f3..000000000000
--- a/drivers/net/ethernet/chelsio/cxgb4/cxgb4_main.c
+++ b/drivers/net/ethernet/chelsio/cxgb4/cxgb4_main.c
@@@ -4272,8 -4390,7 +4272,12 @@@ static int cxgb4_inet6addr_handler(stru
  		 * bond. We need to find such different adapters and add clip
  		 * in all of them only once.
  		 */
++<<<<<<< HEAD
 +		read_lock(&bond->lock);
 +		bond_for_each_slave(bond, slave, cnt) {
++=======
+ 		bond_for_each_slave(bond, slave, iter) {
++>>>>>>> 51752afa735a (cxgb4: remove bond->lock)
  			if (!first_pdev) {
  				ret = clip_add(slave->dev, ifa, event);
  				/* If clip_add is success then only initialize
* Unmerged path drivers/net/ethernet/chelsio/cxgb4/cxgb4_main.c
