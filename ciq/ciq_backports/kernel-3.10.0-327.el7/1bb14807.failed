net: fib6: reduce identation in ip6_convert_metrics

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-327.el7
Rebuild_CHGLOG: - [net] fib6: reduce identation in ip6_convert_metrics (Florian Westphal) [1254839]
Rebuild_FUZZ: 94.85%
commit-author Daniel Borkmann <daniel@iogearbox.net>
commit 1bb14807bc761a88bb9d319e7bf519eebf4c82ec
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-327.el7/1bb14807.failed

Reduce the identation a bit, there's no need to artificically have
it increased.

	Signed-off-by: Daniel Borkmann <daniel@iogearbox.net>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 1bb14807bc761a88bb9d319e7bf519eebf4c82ec)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/ipv6/route.c
diff --cc net/ipv6/route.c
index 40dd7c53aa88,0261b721b34b..000000000000
--- a/net/ipv6/route.c
+++ b/net/ipv6/route.c
@@@ -1452,9 -1695,51 +1452,57 @@@ out
  	return entries > rt_max_size;
  }
  
++<<<<<<< HEAD
 +/*
 + *
 + */
++=======
+ static int ip6_convert_metrics(struct mx6_config *mxc,
+ 			       const struct fib6_config *cfg)
+ {
+ 	struct nlattr *nla;
+ 	int remaining;
+ 	u32 *mp;
+ 
+ 	if (!cfg->fc_mx)
+ 		return 0;
+ 
+ 	mp = kzalloc(sizeof(u32) * RTAX_MAX, GFP_KERNEL);
+ 	if (unlikely(!mp))
+ 		return -ENOMEM;
+ 
+ 	nla_for_each_attr(nla, cfg->fc_mx, cfg->fc_mx_len, remaining) {
+ 		int type = nla_type(nla);
+ 		u32 val;
+ 
+ 		if (!type)
+ 			continue;
+ 		if (unlikely(type > RTAX_MAX))
+ 			goto err;
+ 
+ 		if (type == RTAX_CC_ALGO) {
+ 			char tmp[TCP_CA_NAME_MAX];
+ 
+ 			nla_strlcpy(tmp, nla, sizeof(tmp));
+ 			val = tcp_ca_get_key_by_name(tmp);
+ 			if (val == TCP_CA_UNSPEC)
+ 				goto err;
+ 		} else {
+ 			val = nla_get_u32(nla);
+ 		}
+ 
+ 		mp[type - 1] = val;
+ 		__set_bit(type - 1, mxc->mx_valid);
+ 	}
+ 
+ 	mxc->mx = mp;
+ 
+ 	return 0;
+  err:
+ 	kfree(mp);
+ 	return -EINVAL;
+ }
++>>>>>>> 1bb14807bc76 (net: fib6: reduce identation in ip6_convert_metrics)
  
  int ip6_route_add(struct fib6_config *cfg)
  {
* Unmerged path net/ipv6/route.c
