ALSA: line6: Remove superfluous NULL checks in each driver

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-327.el7
Rebuild_CHGLOG: - [alsa] line6: Remove superfluous NULL checks in each driver (Jaroslav Kysela) [1197064]
Rebuild_FUZZ: 94.55%
commit-author Takashi Iwai <tiwai@suse.de>
commit 8a3b7c086a509d90c312ebab6d8e5a9bf48c6cf5
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-327.el7/8a3b7c08.failed

The interface and driver objects are always set when callbacks are
called.  Drop such superfluous NULL checks in init and disconnect
calls of each driver.

	Tested-by: Chris Rorvick <chris@rorvick.com>
	Signed-off-by: Takashi Iwai <tiwai@suse.de>
(cherry picked from commit 8a3b7c086a509d90c312ebab6d8e5a9bf48c6cf5)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/staging/line6/pod.c
#	drivers/staging/line6/toneport.c
#	drivers/staging/line6/variax.c
#	sound/usb/line6/podhd.c
diff --cc drivers/staging/line6/pod.c
index 711d2c74641b,26ecf26a33ec..000000000000
--- a/drivers/staging/line6/pod.c
+++ b/drivers/staging/line6/pod.c
@@@ -323,17 -397,19 +323,28 @@@ static struct snd_kcontrol_new pod_cont
  };
  
  /*
 -	POD device disconnected.
 +	POD destructor.
  */
 -static void line6_pod_disconnect(struct usb_interface *interface)
 +static void pod_destruct(struct usb_interface *interface)
  {
  	struct usb_line6_pod *pod = usb_get_intfdata(interface);
++<<<<<<< HEAD:drivers/staging/line6/pod.c
 +
 +	if (pod == NULL)
 +		return;
 +	line6_cleanup_audio(&pod->line6);
 +
 +	del_timer(&pod->startup_timer);
++=======
+ 	struct device *dev = &interface->dev;
+ 
+ 	/* remove sysfs entries: */
+ 	device_remove_file(dev, &dev_attr_device_id);
+ 	device_remove_file(dev, &dev_attr_firmware_version);
+ 	device_remove_file(dev, &dev_attr_serial_number);
+ 
+ 	del_timer_sync(&pod->startup_timer);
++>>>>>>> 8a3b7c086a50 (ALSA: line6: Remove superfluous NULL checks in each driver):sound/usb/line6/pod.c
  	cancel_work_sync(&pod->startup_work);
  }
  
diff --cc drivers/staging/line6/toneport.c
index 4f8fc09cde1e,e5669bd3233a..000000000000
--- a/drivers/staging/line6/toneport.c
+++ b/drivers/staging/line6/toneport.c
@@@ -321,22 -363,34 +321,48 @@@ static void toneport_setup(struct usb_l
  }
  
  /*
++<<<<<<< HEAD:drivers/staging/line6/toneport.c
++=======
+ 	Toneport device disconnected.
+ */
+ static void line6_toneport_disconnect(struct usb_interface *interface)
+ {
+ 	struct usb_line6_toneport *toneport;
+ 	u16 idProduct;
+ 
+ 	toneport = usb_get_intfdata(interface);
+ 	del_timer_sync(&toneport->timer);
+ 	idProduct = le16_to_cpu(toneport->line6.usbdev->descriptor.idProduct);
+ 
+ 	if (toneport_has_led(idProduct)) {
+ 		device_remove_file(&interface->dev, &dev_attr_led_red);
+ 		device_remove_file(&interface->dev, &dev_attr_led_green);
+ 	}
+ }
+ 
+ 
+ /*
++>>>>>>> 8a3b7c086a50 (ALSA: line6: Remove superfluous NULL checks in each driver):sound/usb/line6/toneport.c
  	 Try to init Toneport device.
  */
 -static int toneport_init(struct usb_interface *interface,
 -			 struct usb_line6 *line6)
 +static int toneport_try_init(struct usb_interface *interface,
 +			     struct usb_line6_toneport *toneport)
  {
  	int err;
 -	struct usb_line6_toneport *toneport =  (struct usb_line6_toneport *) line6;
 +	struct usb_line6 *line6 = &toneport->line6;
 +	struct usb_device *usbdev = line6->usbdev;
 +
++<<<<<<< HEAD:drivers/staging/line6/toneport.c
 +	if ((interface == NULL) || (toneport == NULL))
 +		return -ENODEV;
  
 +	/* initialize audio system: */
 +	err = line6_init_audio(line6);
 +	if (err < 0)
 +		return err;
++=======
+ 	line6->disconnect = line6_toneport_disconnect;
++>>>>>>> 8a3b7c086a50 (ALSA: line6: Remove superfluous NULL checks in each driver):sound/usb/line6/toneport.c
  
  	/* initialize PCM subsystem: */
  	err = line6_init_pcm(line6, &toneport_pcm_properties);
diff --cc drivers/staging/line6/variax.c
index bd0f694fa8d8,cd3adeffde02..000000000000
--- a/drivers/staging/line6/variax.c
+++ b/drivers/staging/line6/variax.c
@@@ -153,13 -210,11 +153,17 @@@ void line6_variax_process_message(struc
  /*
  	Variax destructor.
  */
 -static void line6_variax_disconnect(struct usb_interface *interface)
 +static void variax_destruct(struct usb_interface *interface)
  {
 -	struct usb_line6_variax *variax;
 +	struct usb_line6_variax *variax = usb_get_intfdata(interface);
  
++<<<<<<< HEAD:drivers/staging/line6/variax.c
 +	if (variax == NULL)
 +		return;
 +	line6_cleanup_audio(&variax->line6);
++=======
+ 	variax = usb_get_intfdata(interface);
++>>>>>>> 8a3b7c086a50 (ALSA: line6: Remove superfluous NULL checks in each driver):sound/usb/line6/variax.c
  
  	del_timer(&variax->startup_timer1);
  	del_timer(&variax->startup_timer2);
* Unmerged path sound/usb/line6/podhd.c
* Unmerged path drivers/staging/line6/pod.c
* Unmerged path drivers/staging/line6/toneport.c
* Unmerged path drivers/staging/line6/variax.c
* Unmerged path sound/usb/line6/podhd.c
