bonding: slight optimizztion for bond_slave_override()

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-327.el7
commit-author dingtianhong <dingtianhong@huawei.com>
commit 3900f29021f0bc7fe9815aa32f1a993b7dfdd402
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-327.el7/3900f290.failed

When the skb is xmit by the function bond_slave_override(),
it will have duplicate judgement for slave state, and I think it
will consumes a little performance, maybe it is negligible,
so I simplify the function and remove the unwanted judgement.

	Signed-off-by: Ding Tianhong <dingtianhong@huawei.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 3900f29021f0bc7fe9815aa32f1a993b7dfdd402)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/bonding/bond_main.c
diff --cc drivers/net/bonding/bond_main.c
index ab67050d61a5,9efccefbfb68..000000000000
--- a/drivers/net/bonding/bond_main.c
+++ b/drivers/net/bonding/bond_main.c
@@@ -3996,17 -3672,20 +3996,31 @@@ static void bond_set_xmit_hash_policy(s
  static inline int bond_slave_override(struct bonding *bond,
  				      struct sk_buff *skb)
  {
 +	int i, res = 1;
  	struct slave *slave = NULL;
++<<<<<<< HEAD
 +	struct slave *check_slave;
++=======
+ 	struct list_head *iter;
++>>>>>>> 3900f29021f0 (bonding: slight optimizztion for bond_slave_override())
  
  	if (!skb->queue_mapping)
  		return 1;
  
  	/* Find out if any slaves have the same mapping as this skb. */
++<<<<<<< HEAD
 +	bond_for_each_slave(bond, check_slave, i) {
 +		if (check_slave->queue_id == skb->queue_mapping) {
 +			slave = check_slave;
++=======
+ 	bond_for_each_slave_rcu(bond, slave, iter) {
+ 		if (slave->queue_id == skb->queue_mapping) {
+ 			if (slave_can_tx(slave)) {
+ 				bond_dev_queue_xmit(bond, skb, slave->dev);
+ 				return 0;
+ 			}
+ 			/* If the slave isn't UP, use default transmit policy. */
++>>>>>>> 3900f29021f0 (bonding: slight optimizztion for bond_slave_override())
  			break;
  		}
  	}
* Unmerged path drivers/net/bonding/bond_main.c
