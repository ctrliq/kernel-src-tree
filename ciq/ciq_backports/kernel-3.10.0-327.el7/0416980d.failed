ALSA: line6: Fix volume calculation for big-endian

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-327.el7
Rebuild_CHGLOG: - [alsa] line6: Fix volume calculation for big-endian (Jaroslav Kysela) [1197064]
Rebuild_FUZZ: 93.62%
commit-author Takashi Iwai <tiwai@suse.de>
commit 0416980d0a2bc10acff1c2329159577036e5666b
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-327.el7/0416980d.failed

	Signed-off-by: Takashi Iwai <tiwai@suse.de>
(cherry picked from commit 0416980d0a2bc10acff1c2329159577036e5666b)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/staging/line6/playback.c
diff --cc drivers/staging/line6/playback.c
index f97584b88a0d,ae41124c351e..000000000000
--- a/drivers/staging/line6/playback.c
+++ b/drivers/staging/line6/playback.c
@@@ -33,8 -31,9 +33,14 @@@ static void change_volume(struct urb *u
  		return;		/* maximum volume - no change */
  
  	if (bytes_per_frame == 4) {
++<<<<<<< HEAD:drivers/staging/line6/playback.c
 +		short *p, *buf_end;
 +		p = (short *)urb_out->transfer_buffer;
++=======
+ 		__le16 *p, *buf_end;
+ 
+ 		p = (__le16 *)urb_out->transfer_buffer;
++>>>>>>> 0416980d0a2b (ALSA: line6: Fix volume calculation for big-endian):sound/usb/line6/playback.c
  		buf_end = p + urb_out->transfer_buffer_length / sizeof(*p);
  
  		for (; p < buf_end; ++p) {
@@@ -117,9 -116,10 +125,16 @@@ static void add_monitor_signal(struct u
  		return;		/* zero volume - no change */
  
  	if (bytes_per_frame == 4) {
++<<<<<<< HEAD:drivers/staging/line6/playback.c
 +		short *pi, *po, *buf_end;
 +		pi = (short *)signal;
 +		po = (short *)urb_out->transfer_buffer;
++=======
+ 		__le16 *pi, *po, *buf_end;
+ 
+ 		pi = (__le16 *)signal;
+ 		po = (__le16 *)urb_out->transfer_buffer;
++>>>>>>> 0416980d0a2b (ALSA: line6: Fix volume calculation for big-endian):sound/usb/line6/playback.c
  		buf_end = po + urb_out->transfer_buffer_length / sizeof(*po);
  
  		for (; po < buf_end; ++pi, ++po) {
* Unmerged path drivers/staging/line6/playback.c
