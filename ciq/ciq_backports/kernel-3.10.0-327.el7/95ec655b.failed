Revert "dev: set iflink to 0 for virtual interfaces"

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-327.el7
commit-author Nicolas Dichtel <nicolas.dichtel@6wind.com>
commit 95ec655bc465ccb2a3329d4aff9a45e3c8188db5
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-327.el7/95ec655b.failed

This reverts commit e1622baf54df8cc958bf29d71de5ad545ea7d93c.

The side effect of this commit is to add a '@NONE' after each virtual
interface name with a 'ip link'. It may break existing scripts.

	Reported-by: Olivier Hartkopp <socketcan@hartkopp.net>
	Signed-off-by: Nicolas Dichtel <nicolas.dichtel@6wind.com>
	Tested-by: Oliver Hartkopp <socketcan@hartkopp.net>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 95ec655bc465ccb2a3329d4aff9a45e3c8188db5)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/core/dev.c
diff --cc net/core/dev.c
index c33def8a553a,1e57efda055b..000000000000
--- a/net/core/dev.c
+++ b/net/core/dev.c
@@@ -651,6 -665,23 +651,26 @@@ __setup("netdev=", netdev_boot_setup)
  *******************************************************************************/
  
  /**
++<<<<<<< HEAD
++=======
+  *	dev_get_iflink	- get 'iflink' value of a interface
+  *	@dev: targeted interface
+  *
+  *	Indicates the ifindex the interface is linked to.
+  *	Physical interfaces have the same 'ifindex' and 'iflink' values.
+  */
+ 
+ int dev_get_iflink(const struct net_device *dev)
+ {
+ 	if (dev->netdev_ops && dev->netdev_ops->ndo_get_iflink)
+ 		return dev->netdev_ops->ndo_get_iflink(dev);
+ 
+ 	return dev->ifindex;
+ }
+ EXPORT_SYMBOL(dev_get_iflink);
+ 
+ /**
++>>>>>>> 95ec655bc465 (Revert "dev: set iflink to 0 for virtual interfaces")
   *	__dev_get_by_name	- find a device by its name
   *	@net: the applicable net namespace
   *	@name: name to find
* Unmerged path net/core/dev.c
