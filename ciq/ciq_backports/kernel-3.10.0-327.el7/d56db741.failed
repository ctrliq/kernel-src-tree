ALSA: hda - Release resources in device release callback

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-327.el7
Rebuild_CHGLOG: - [alsa] hda - Release resources in device release callback (Jaroslav Kysela) [1197064]
Rebuild_FUZZ: 94.34%
commit-author Takashi Iwai <tiwai@suse.de>
commit d56db741b8e688a0b9d4d5bb9caa11dfcb7c0b08
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-327.el7/d56db741.failed

Move the destructor code to device release callback for the codec
object instead.  This is a safer place to release the resources than
dev_free callback in general.

	Signed-off-by: Takashi Iwai <tiwai@suse.de>
(cherry picked from commit d56db741b8e688a0b9d4d5bb9caa11dfcb7c0b08)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	sound/pci/hda/hda_codec.c
diff --cc sound/pci/hda/hda_codec.c
index b23b865bd99f,6fecf57c8d7c..000000000000
--- a/sound/pci/hda/hda_codec.c
+++ b/sound/pci/hda/hda_codec.c
@@@ -1205,28 -1191,6 +1205,31 @@@ void snd_hda_codec_cleanup_for_unbind(s
  	remove_conn_list(codec);
  }
  
++<<<<<<< HEAD
 +static void snd_hda_codec_free(struct hda_codec *codec)
 +{
 +	if (!codec)
 +		return;
 +	codec->in_freeing = 1;
 +	if (device_is_registered(hda_codec_dev(codec)))
 +		device_del(hda_codec_dev(codec));
 +	free_init_pincfgs(codec);
 +	list_del(&codec->list);
 +	codec->bus->caddr_tbl[codec->addr] = NULL;
 +	hda_call_pm_notify(codec, false); /* cancel leftover refcounts */
 +	snd_hda_sysfs_clear(codec);
 +	free_hda_cache(&codec->amp_cache);
 +	free_hda_cache(&codec->cmd_cache);
 +	kfree(codec->vendor_name);
 +	kfree(codec->chip_name);
 +	kfree(codec->modelname);
 +	kfree(codec->wcaps);
 +	codec->bus->num_codecs--;
 +	put_device(hda_codec_dev(codec));
 +}
 +
++=======
++>>>>>>> d56db741b8e6 (ALSA: hda - Release resources in device release callback)
  static bool snd_hda_codec_get_supported_ps(struct hda_codec *codec,
  				hda_nid_t fg, unsigned int power_state);
  
* Unmerged path sound/pci/hda/hda_codec.c
