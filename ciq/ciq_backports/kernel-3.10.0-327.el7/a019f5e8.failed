ALSA: line6: Remove superfluous out-of-memory error messages

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-327.el7
Rebuild_CHGLOG: - [alsa] line6: Remove superfluous out-of-memory error messages (Jaroslav Kysela) [1197064]
Rebuild_FUZZ: 94.74%
commit-author Takashi Iwai <tiwai@suse.de>
commit a019f5e8c522f5b5f8b3419a1e56d142ea4c7621
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-327.el7/a019f5e8.failed

Kernel already shows the error in the common path.

	Tested-by: Chris Rorvick <chris@rorvick.com>
	Signed-off-by: Takashi Iwai <tiwai@suse.de>
(cherry picked from commit a019f5e8c522f5b5f8b3419a1e56d142ea4c7621)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/staging/line6/capture.c
#	drivers/staging/line6/driver.c
#	drivers/staging/line6/playback.c
diff --cc drivers/staging/line6/capture.c
index 93bd813139f8,727b31876d48..000000000000
--- a/drivers/staging/line6/capture.c
+++ b/drivers/staging/line6/capture.c
@@@ -402,15 -402,13 +402,18 @@@ int line6_create_audio_in_urbs(struct s
  		urb = line6pcm->urb_audio_in[i] =
  		    usb_alloc_urb(LINE6_ISO_PACKETS, GFP_KERNEL);
  
++<<<<<<< HEAD:drivers/staging/line6/capture.c
 +		if (urb == NULL) {
 +			dev_err(line6pcm->line6->ifcdev, "Out of memory\n");
++=======
+ 		if (urb == NULL)
++>>>>>>> a019f5e8c522 (ALSA: line6: Remove superfluous out-of-memory error messages):sound/usb/line6/capture.c
  			return -ENOMEM;
- 		}
  
 -		urb->dev = line6->usbdev;
 +		urb->dev = line6pcm->line6->usbdev;
  		urb->pipe =
 -		    usb_rcvisocpipe(line6->usbdev,
 -				    line6->properties->ep_audio_r &
 +		    usb_rcvisocpipe(line6pcm->line6->usbdev,
 +				    line6pcm->ep_audio_read &
  				    USB_ENDPOINT_NUMBER_MASK);
  		urb->transfer_flags = URB_ISO_ASAP;
  		urb->start_frame = -1;
diff --cc drivers/staging/line6/driver.c
index bd624c90f1ad,b8f5134dcec2..000000000000
--- a/drivers/staging/line6/driver.c
+++ b/drivers/staging/line6/driver.c
@@@ -876,8 -590,6 +873,11 @@@ static int line6_probe(struct usb_inter
  		line6->urb_listen = usb_alloc_urb(0, GFP_KERNEL);
  
  		if (line6->urb_listen == NULL) {
++<<<<<<< HEAD:drivers/staging/line6/driver.c
 +			dev_err(&interface->dev, "Out of memory\n");
 +			line6_destruct(interface);
++=======
++>>>>>>> a019f5e8c522 (ALSA: line6: Remove superfluous out-of-memory error messages):sound/usb/line6/driver.c
  			ret = -ENOMEM;
  			goto err_destruct;
  		}
diff --cc drivers/staging/line6/playback.c
index feb51c49e316,5a7fe409a3b9..000000000000
--- a/drivers/staging/line6/playback.c
+++ b/drivers/staging/line6/playback.c
@@@ -556,15 -558,13 +556,18 @@@ int line6_create_audio_out_urbs(struct 
  		urb = line6pcm->urb_audio_out[i] =
  		    usb_alloc_urb(LINE6_ISO_PACKETS, GFP_KERNEL);
  
++<<<<<<< HEAD:drivers/staging/line6/playback.c
 +		if (urb == NULL) {
 +			dev_err(line6pcm->line6->ifcdev, "Out of memory\n");
++=======
+ 		if (urb == NULL)
++>>>>>>> a019f5e8c522 (ALSA: line6: Remove superfluous out-of-memory error messages):sound/usb/line6/playback.c
  			return -ENOMEM;
- 		}
  
 -		urb->dev = line6->usbdev;
 +		urb->dev = line6pcm->line6->usbdev;
  		urb->pipe =
 -		    usb_sndisocpipe(line6->usbdev,
 -				    line6->properties->ep_audio_w &
 +		    usb_sndisocpipe(line6pcm->line6->usbdev,
 +				    line6pcm->ep_audio_write &
  				    USB_ENDPOINT_NUMBER_MASK);
  		urb->transfer_flags = URB_ISO_ASAP;
  		urb->start_frame = -1;
* Unmerged path drivers/staging/line6/capture.c
* Unmerged path drivers/staging/line6/driver.c
diff --git a/drivers/staging/line6/midi.c b/drivers/staging/line6/midi.c
index e3f9a53dbd96..1724759296fd 100644
--- a/drivers/staging/line6/midi.c
+++ b/drivers/staging/line6/midi.c
@@ -120,16 +120,13 @@ static int send_midi_async(struct usb_line6 *line6, unsigned char *data,
 
 	urb = usb_alloc_urb(0, GFP_ATOMIC);
 
-	if (urb == NULL) {
-		dev_err(line6->ifcdev, "Out of memory\n");
+	if (urb == NULL)
 		return -ENOMEM;
-	}
 
 	transfer_buffer = kmemdup(data, length, GFP_ATOMIC);
 
 	if (transfer_buffer == NULL) {
 		usb_free_urb(urb);
-		dev_err(line6->ifcdev, "Out of memory\n");
 		return -ENOMEM;
 	}
 
* Unmerged path drivers/staging/line6/playback.c
diff --git a/drivers/staging/line6/variax.c b/drivers/staging/line6/variax.c
index bd0f694fa8d8..607580355fed 100644
--- a/drivers/staging/line6/variax.c
+++ b/drivers/staging/line6/variax.c
@@ -187,10 +187,8 @@ static int variax_try_init(struct usb_interface *interface,
 	variax->buffer_activate = kmemdup(variax_activate,
 					  sizeof(variax_activate), GFP_KERNEL);
 
-	if (variax->buffer_activate == NULL) {
-		dev_err(&interface->dev, "Out of memory\n");
+	if (variax->buffer_activate == NULL)
 		return -ENOMEM;
-	}
 
 	/* initialize audio system: */
 	err = line6_init_audio(&variax->line6);
