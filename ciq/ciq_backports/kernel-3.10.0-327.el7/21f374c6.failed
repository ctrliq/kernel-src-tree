bonding: Invert test

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-327.el7
commit-author Joe Perches <joe@perches.com>
commit 21f374c6cfbf98c1efceee0b528ce784d215935c
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-327.el7/21f374c6.failed

Make the error case return early.
Make the normal return at the bottom of the function.
Reduces indent for readability.

	Signed-off-by: Joe Perches <joe@perches.com>
	Reviewed-by: Ding Tianhong <dingtianhong@huawei.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 21f374c6cfbf98c1efceee0b528ce784d215935c)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/bonding/bond_3ad.c
diff --cc drivers/net/bonding/bond_3ad.c
index dc508c87249d,f04f3625e944..000000000000
--- a/drivers/net/bonding/bond_3ad.c
+++ b/drivers/net/bonding/bond_3ad.c
@@@ -2409,16 -2379,16 +2409,27 @@@ int __bond_3ad_get_active_agg_info(stru
  		}
  	}
  
++<<<<<<< HEAD
 +	if (aggregator) {
 +		ad_info->aggregator_id = aggregator->aggregator_identifier;
 +		ad_info->ports = aggregator->num_of_ports;
 +		ad_info->actor_key = aggregator->actor_oper_aggregator_key;
 +		ad_info->partner_key = aggregator->partner_oper_aggregator_key;
 +		memcpy(ad_info->partner_system, aggregator->partner_system.mac_addr_value, ETH_ALEN);
 +		return 0;
 +	}
- 
- 	return -1;
++=======
+ 	if (!aggregator)
+ 		return -1;
++>>>>>>> 21f374c6cfbf (bonding: Invert test)
+ 
+ 	ad_info->aggregator_id = aggregator->aggregator_identifier;
+ 	ad_info->ports = aggregator->num_of_ports;
+ 	ad_info->actor_key = aggregator->actor_oper_aggregator_key;
+ 	ad_info->partner_key = aggregator->partner_oper_aggregator_key;
+ 	ether_addr_copy(ad_info->partner_system,
+ 			aggregator->partner_system.mac_addr_value);
+ 	return 0;
  }
  
  /* Wrapper used to hold bond->lock so no slave manipulation can occur */
* Unmerged path drivers/net/bonding/bond_3ad.c
