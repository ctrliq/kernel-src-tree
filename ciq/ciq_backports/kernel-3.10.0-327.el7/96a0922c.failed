bonding: use the correct ether type for alb

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-327.el7
commit-author Veaceslav Falico <vfalico@redhat.com>
commit 96a0922c2349ccfbf5583708c3602945a755c874
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-327.el7/96a0922c.failed

Currently it's using the wrong ETH_P_LOOP type, which is sometimes treated
as packet length instead of ether type (because it's 0x0060).

Use the new ETH_P_LOOPBACK type.

CC: Jay Vosburgh <fubar@us.ibm.com>
CC: Andy Gospodarek <andy@greyhouse.net>
	Signed-off-by: Veaceslav Falico <vfalico@redhat.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 96a0922c2349ccfbf5583708c3602945a755c874)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/bonding/bond_alb.c
diff --cc drivers/net/bonding/bond_alb.c
index d3256b311be3,060b6117aeac..000000000000
--- a/drivers/net/bonding/bond_alb.c
+++ b/drivers/net/bonding/bond_alb.c
@@@ -973,9 -1003,9 +973,15 @@@ static void alb_send_lp_vid(struct slav
  	char *data;
  
  	memset(&pkt, 0, size);
++<<<<<<< HEAD
 +	memcpy(pkt.mac_dst, mac_addr, ETH_ALEN);
 +	memcpy(pkt.mac_src, mac_addr, ETH_ALEN);
 +	pkt.type = cpu_to_be16(ETH_P_LOOP);
++=======
+ 	ether_addr_copy(pkt.mac_dst, mac_addr);
+ 	ether_addr_copy(pkt.mac_src, mac_addr);
+ 	pkt.type = cpu_to_be16(ETH_P_LOOPBACK);
++>>>>>>> 96a0922c2349 (bonding: use the correct ether type for alb)
  
  	skb = dev_alloc_skb(size);
  	if (!skb)
* Unmerged path drivers/net/bonding/bond_alb.c
