ACPI / video: trivial style cleanups

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-327.el7
Rebuild_CHGLOG: - [acpi] video: trivial style cleanups (Benjamin Tissoires) [1218354]
Rebuild_FUZZ: 89.23%
commit-author Felipe Contreras <felipe.contreras@gmail.com>
commit 915ea7e41439efa7793814cdf4338cb6b003538a
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-327.el7/915ea7e4.failed

Fix several coding style defects in drivers/acpi/video.c:
 - Initialization of static variables.
 - Whitespace in expressions, variable definitions, function
   headers, etc.
 - Positioning of labels.
 - Braces around single statements.

[rjw: Changelog]
	Signed-off-by: Felipe Contreras <felipe.contreras@gmail.com>
	Signed-off-by: Rafael J. Wysocki <rafael.j.wysocki@intel.com>
(cherry picked from commit 915ea7e41439efa7793814cdf4338cb6b003538a)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/acpi/video.c
diff --cc drivers/acpi/video.c
index 8f1d29b3475e,fff5bb3ffda8..000000000000
--- a/drivers/acpi/video.c
+++ b/drivers/acpi/video.c
@@@ -881,20 -890,23 +881,36 @@@ out
  
  static void acpi_video_device_find_cap(struct acpi_video_device *device)
  {
++<<<<<<< HEAD
 +	if (acpi_has_method(device->dev->handle, "_ADR"))
 +		device->cap._ADR = 1;
 +	if (acpi_has_method(device->dev->handle, "_BCL"))
 +		device->cap._BCL = 1;
 +	if (acpi_has_method(device->dev->handle, "_BCM"))
 +		device->cap._BCM = 1;
 +	if (acpi_has_method(device->dev->handle, "_BQC")) {
++=======
+ 	acpi_handle h_dummy1;
+ 
+ 	if (ACPI_SUCCESS(acpi_get_handle(device->dev->handle, "_ADR", &h_dummy1)))
+ 		device->cap._ADR = 1;
+ 	if (ACPI_SUCCESS(acpi_get_handle(device->dev->handle, "_BCL", &h_dummy1)))
+ 		device->cap._BCL = 1;
+ 	if (ACPI_SUCCESS(acpi_get_handle(device->dev->handle, "_BCM", &h_dummy1)))
+ 		device->cap._BCM = 1;
+ 	if (ACPI_SUCCESS(acpi_get_handle(device->dev->handle, "_BQC", &h_dummy1)))
++>>>>>>> 915ea7e41439 (ACPI / video: trivial style cleanups)
  		device->cap._BQC = 1;
 -	else if (ACPI_SUCCESS(acpi_get_handle(device->dev->handle, "_BCQ",
 -				&h_dummy1))) {
 +	} else if (acpi_has_method(device->dev->handle, "_BCQ")) {
  		printk(KERN_WARNING FW_BUG "_BCQ is used instead of _BQC\n");
  		device->cap._BCQ = 1;
  	}
  
++<<<<<<< HEAD
 +	if (acpi_has_method(device->dev->handle, "_DDC"))
++=======
+ 	if (ACPI_SUCCESS(acpi_get_handle(device->dev->handle, "_DDC", &h_dummy1)))
++>>>>>>> 915ea7e41439 (ACPI / video: trivial style cleanups)
  		device->cap._DDC = 1;
  
  	if (acpi_video_init_brightness(device))
@@@ -988,17 -1000,19 +1004,33 @@@
  
  static void acpi_video_bus_find_cap(struct acpi_video_bus *video)
  {
++<<<<<<< HEAD
 +	if (acpi_has_method(video->device->handle, "_DOS"))
 +		video->cap._DOS = 1;
 +	if (acpi_has_method(video->device->handle, "_DOD"))
 +		video->cap._DOD = 1;
 +	if (acpi_has_method(video->device->handle, "_ROM"))
 +		video->cap._ROM = 1;
 +	if (acpi_has_method(video->device->handle, "_GPD"))
 +		video->cap._GPD = 1;
 +	if (acpi_has_method(video->device->handle, "_SPD"))
 +		video->cap._SPD = 1;
 +	if (acpi_has_method(video->device->handle, "_VPO"))
++=======
+ 	acpi_handle h_dummy1;
+ 
+ 	if (ACPI_SUCCESS(acpi_get_handle(video->device->handle, "_DOS", &h_dummy1)))
+ 		video->cap._DOS = 1;
+ 	if (ACPI_SUCCESS(acpi_get_handle(video->device->handle, "_DOD", &h_dummy1)))
+ 		video->cap._DOD = 1;
+ 	if (ACPI_SUCCESS(acpi_get_handle(video->device->handle, "_ROM", &h_dummy1)))
+ 		video->cap._ROM = 1;
+ 	if (ACPI_SUCCESS(acpi_get_handle(video->device->handle, "_GPD", &h_dummy1)))
+ 		video->cap._GPD = 1;
+ 	if (ACPI_SUCCESS(acpi_get_handle(video->device->handle, "_SPD", &h_dummy1)))
+ 		video->cap._SPD = 1;
+ 	if (ACPI_SUCCESS(acpi_get_handle(video->device->handle, "_VPO", &h_dummy1)))
++>>>>>>> 915ea7e41439 (ACPI / video: trivial style cleanups)
  		video->cap._VPO = 1;
  }
  
* Unmerged path drivers/acpi/video.c
