ALSA: line6/toneport: Fix wrong argument for toneport_has_led()

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-327.el7
Rebuild_CHGLOG: - [alsa] line6/toneport: Fix wrong argument for toneport_has_led() (Jaroslav Kysela) [1197064]
Rebuild_FUZZ: 95.00%
commit-author Takashi Iwai <tiwai@suse.de>
commit bf115fcf957f080a1766bb785e23980c1440d06e
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-327.el7/bf115fcf.failed

	Tested-by: Chris Rorvick <chris@rorvick.com>
	Signed-off-by: Takashi Iwai <tiwai@suse.de>
(cherry picked from commit bf115fcf957f080a1766bb785e23980c1440d06e)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/staging/line6/toneport.c
diff --cc drivers/staging/line6/toneport.c
index 4f8fc09cde1e,9a4f5403569e..000000000000
--- a/drivers/staging/line6/toneport.c
+++ b/drivers/staging/line6/toneport.c
@@@ -321,22 -363,37 +321,42 @@@ static void toneport_setup(struct usb_l
  }
  
  /*
++<<<<<<< HEAD:drivers/staging/line6/toneport.c
++=======
+ 	Toneport device disconnected.
+ */
+ static void line6_toneport_disconnect(struct usb_interface *interface)
+ {
+ 	struct usb_line6_toneport *toneport;
+ 
+ 	toneport = usb_get_intfdata(interface);
+ 	del_timer_sync(&toneport->timer);
+ 
+ 	if (toneport_has_led(toneport->type)) {
+ 		device_remove_file(&interface->dev, &dev_attr_led_red);
+ 		device_remove_file(&interface->dev, &dev_attr_led_green);
+ 	}
+ }
+ 
+ 
+ /*
++>>>>>>> bf115fcf957f (ALSA: line6/toneport: Fix wrong argument for toneport_has_led()):sound/usb/line6/toneport.c
  	 Try to init Toneport device.
  */
 -static int toneport_init(struct usb_interface *interface,
 -			 struct usb_line6 *line6)
 +static int toneport_try_init(struct usb_interface *interface,
 +			     struct usb_line6_toneport *toneport)
  {
  	int err;
 -	struct usb_line6_toneport *toneport =  (struct usb_line6_toneport *) line6;
 +	struct usb_line6 *line6 = &toneport->line6;
 +	struct usb_device *usbdev = line6->usbdev;
  
 -	setup_timer(&toneport->timer, toneport_start_pcm,
 -		    (unsigned long)toneport);
 +	if ((interface == NULL) || (toneport == NULL))
 +		return -ENODEV;
  
 -	line6->disconnect = line6_toneport_disconnect;
 +	/* initialize audio system: */
 +	err = line6_init_audio(line6);
 +	if (err < 0)
 +		return err;
  
  	/* initialize PCM subsystem: */
  	err = line6_init_pcm(line6, &toneport_pcm_properties);
* Unmerged path drivers/staging/line6/toneport.c
