crypto: tcrypt - Add new mode for sha512_mb

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-693.el7
Rebuild_CHGLOG: - [crypto] tcrypt - Add new mode for sha512_mb (Herbert Xu) [1379518]
Rebuild_FUZZ: 89.74%
commit-author Megha Dey <megha.dey@linux.intel.com>
commit 14009c4bde13d191206d58d0513a9179568a0125
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-693.el7/14009c4b.failed

Add a new mode to calculate the speed of the sha512_mb algorithm

	Signed-off-by: Megha Dey <megha.dey@linux.intel.com>
	Reviewed-by: Fenghua Yu <fenghua.yu@intel.com>
	Reviewed-by: Tim Chen <tim.c.chen@linux.intel.com>
	Signed-off-by: Herbert Xu <herbert@gondor.apana.org.au>
(cherry picked from commit 14009c4bde13d191206d58d0513a9179568a0125)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	crypto/tcrypt.c
diff --cc crypto/tcrypt.c
index 41684883a4b3,158c164cbddf..000000000000
--- a/crypto/tcrypt.c
+++ b/crypto/tcrypt.c
@@@ -1859,6 -1914,35 +1859,38 @@@ static int do_test(int m
  		test_ahash_speed("rmd320", sec, generic_hash_speed_template);
  		if (mode > 400 && mode < 500) break;
  
++<<<<<<< HEAD
++=======
+ 	case 418:
+ 		test_ahash_speed("sha3-224", sec, generic_hash_speed_template);
+ 		if (mode > 400 && mode < 500) break;
+ 
+ 	case 419:
+ 		test_ahash_speed("sha3-256", sec, generic_hash_speed_template);
+ 		if (mode > 400 && mode < 500) break;
+ 
+ 	case 420:
+ 		test_ahash_speed("sha3-384", sec, generic_hash_speed_template);
+ 		if (mode > 400 && mode < 500) break;
+ 
+ 
+ 	case 421:
+ 		test_ahash_speed("sha3-512", sec, generic_hash_speed_template);
+ 		if (mode > 400 && mode < 500) break;
+ 
+ 	case 422:
+ 		test_mb_ahash_speed("sha1", sec, generic_hash_speed_template);
+ 		if (mode > 400 && mode < 500) break;
+ 
+ 	case 423:
+ 		test_mb_ahash_speed("sha256", sec, generic_hash_speed_template);
+ 		if (mode > 400 && mode < 500) break;
+ 
+ 	case 424:
+ 		test_mb_ahash_speed("sha512", sec, generic_hash_speed_template);
+ 		if (mode > 400 && mode < 500) break;
+ 
++>>>>>>> 14009c4bde13 (crypto: tcrypt - Add new mode for sha512_mb)
  	case 499:
  		break;
  
* Unmerged path crypto/tcrypt.c
