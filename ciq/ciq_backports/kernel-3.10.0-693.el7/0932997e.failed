br_multicast: Replace rcu_assign_pointer() with RCU_INIT_POINTER()

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-693.el7
commit-author Andreea-Cristina Bernat <bernat.ada@gmail.com>
commit 0932997e34bad52353c25756c55ccf97c522ae7c
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-693.el7/0932997e.failed

The use of "rcu_assign_pointer()" is NULLing out the pointer.
According to RCU_INIT_POINTER()'s block comment:
"1.   This use of RCU_INIT_POINTER() is NULLing out the pointer"
it is better to use it instead of rcu_assign_pointer() because it has a
smaller overhead.

The following Coccinelle semantic patch was used:
@@
@@

- rcu_assign_pointer
+ RCU_INIT_POINTER
  (..., NULL)

	Signed-off-by: Andreea-Cristina Bernat <bernat.ada@gmail.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 0932997e34bad52353c25756c55ccf97c522ae7c)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/bridge/br_multicast.c
diff --cc net/bridge/br_multicast.c
index 251bf0711bce,648d79ccf462..000000000000
--- a/net/bridge/br_multicast.c
+++ b/net/bridge/br_multicast.c
@@@ -1749,6 -1822,7 +1749,10 @@@ static void br_multicast_query_expired(
  	if (query->startup_sent < br->multicast_startup_query_count)
  		query->startup_sent++;
  
++<<<<<<< HEAD
++=======
+ 	RCU_INIT_POINTER(querier, NULL);
++>>>>>>> 0932997e34ba (br_multicast: Replace rcu_assign_pointer() with RCU_INIT_POINTER())
  	br_multicast_send_query(br, NULL, query);
  	spin_unlock(&br->multicast_lock);
  }
* Unmerged path net/bridge/br_multicast.c
