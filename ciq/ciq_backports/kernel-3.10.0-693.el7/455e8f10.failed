autofs - constify misc struct path instances

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-693.el7
Rebuild_CHGLOG: - [fs] autofs: constify misc struct path instances (Ian Kent) [1320588]
Rebuild_FUZZ: 96.55%
commit-author Ian Kent <ikent@redhat.com>
commit 455e8f1030de82b68ee4e82f71516f3692f5e626
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-693.el7/455e8f10.failed

	Signed-off-by: Ian Kent <raven@themaw.net>
	Cc: Al Viro <viro@ZenIV.linux.org.uk>
	Cc: Eric W. Biederman <ebiederm@xmission.com>
	Cc: Omar Sandoval <osandov@osandov.com>
	Signed-off-by: Al Viro <viro@zeniv.linux.org.uk>
(cherry picked from commit 455e8f1030de82b68ee4e82f71516f3692f5e626)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/autofs4/expire.c
diff --cc fs/autofs4/expire.c
index 5b220e9acb0a,13178bf2c431..000000000000
--- a/fs/autofs4/expire.c
+++ b/fs/autofs4/expire.c
@@@ -582,10 -593,12 +582,14 @@@ int autofs4_do_expire_multi(struct supe
  
  	if (dentry) {
  		struct autofs_info *ino = autofs4_dentry_ino(dentry);
++<<<<<<< HEAD
++=======
+ 		const struct path path = { .mnt = mnt, .dentry = dentry };
++>>>>>>> 455e8f1030de (autofs - constify misc struct path instances)
  
  		/* This is synchronous because it makes the daemon a
 -		 * little easier
 -		 */
 -		ret = autofs4_wait(sbi, &path, NFY_EXPIRE);
 +                   little easier */
 +		ret = autofs4_wait(sbi, dentry, NFY_EXPIRE);
  
  		spin_lock(&sbi->fs_lock);
  		/* avoid rapid-fire expire attempts if expiry fails */
* Unmerged path fs/autofs4/expire.c
diff --git a/fs/autofs4/root.c b/fs/autofs4/root.c
index b538a83d1c8b..0711c2b710ad 100644
--- a/fs/autofs4/root.c
+++ b/fs/autofs4/root.c
@@ -299,7 +299,7 @@ static int do_expire_wait(const struct path *path, bool rcu_walk)
 	if (!expiring)
 		return autofs4_expire_wait(path, rcu_walk);
 	else {
-		struct path this = { .mnt = path->mnt, .dentry = expiring };
+		const struct path this = { .mnt = path->mnt, .dentry = expiring };
 		/*
 		 * If we are racing with expire the request might not
 		 * be quite complete, but the directory has been removed
