acct: serialize acct_on()

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-693.el7
commit-author Al Viro <viro@zeniv.linux.org.uk>
commit 9df7fa16ee956bf0cdf4a711eac827be92d584bc
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-693.el7/9df7fa16.failed

brute-force - on a global mutex that isn't nested into anything.

	Signed-off-by: Al Viro <viro@zeniv.linux.org.uk>
(cherry picked from commit 9df7fa16ee956bf0cdf4a711eac827be92d584bc)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	kernel/acct.c
diff --cc kernel/acct.c
index ae373e4b635d,08963a292878..000000000000
--- a/kernel/acct.c
+++ b/kernel/acct.c
@@@ -262,8 -264,10 +264,14 @@@ SYSCALL_DEFINE1(acct, const char __use
  	if (name) {
  		struct filename *tmp = getname(name);
  		if (IS_ERR(tmp))
++<<<<<<< HEAD
 +			return (PTR_ERR(tmp));
++=======
+ 			return PTR_ERR(tmp);
+ 		mutex_lock(&acct_on_mutex);
++>>>>>>> 9df7fa16ee95 (acct: serialize acct_on())
  		error = acct_on(tmp);
+ 		mutex_unlock(&acct_on_mutex);
  		putname(tmp);
  	} else {
  		struct bsd_acct_struct *acct;
* Unmerged path kernel/acct.c
