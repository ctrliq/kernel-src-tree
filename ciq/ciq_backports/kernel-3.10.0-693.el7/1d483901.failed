qla2xxx: small cleanup in qla2x00_wait_for_hba_ready()

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-693.el7
commit-author Dan Carpenter <dan.carpenter@oracle.com>
commit 1d48390117c7df9839cf4d43ec33346e7868a53c
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-693.el7/1d483901.failed

The "if (test_bit(UNLOADING..." line was indented one tab more than it
should have been.  There was an extra parenthesis around the
qla2x00_reset_active() function call.  I lined up the conditions a bit
so that it shows how they group together.

	Signed-off-by: Dan Carpenter <dan.carpenter@oracle.com>
	Acked-by: Himanshu Madhani <himanshu.madhani@qlogic.com>
	Signed-off-by: Martin K. Petersen <martin.petersen@oracle.com>
(cherry picked from commit 1d48390117c7df9839cf4d43ec33346e7868a53c)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/scsi/qla2xxx/qla_os.c
diff --cc drivers/scsi/qla2xxx/qla_os.c
index 83f9b8499e95,1dd865028edd..000000000000
--- a/drivers/scsi/qla2xxx/qla_os.c
+++ b/drivers/scsi/qla2xxx/qla_os.c
@@@ -886,12 -897,16 +886,21 @@@ static voi
  qla2x00_wait_for_hba_ready(scsi_qla_host_t *vha)
  {
  	struct qla_hw_data *ha = vha->hw;
 -	scsi_qla_host_t *base_vha = pci_get_drvdata(ha->pdev);
  
++<<<<<<< HEAD
 +	while (((qla2x00_reset_active(vha)) || ha->dpc_active ||
 +	    ha->flags.mbox_busy) ||
 +		test_bit(FX00_RESET_RECOVERY, &vha->dpc_flags) ||
 +		test_bit(FX00_TARGET_SCAN, &vha->dpc_flags))
++=======
+ 	while ((qla2x00_reset_active(vha) || ha->dpc_active ||
+ 		ha->flags.mbox_busy) ||
+ 	       test_bit(FX00_RESET_RECOVERY, &vha->dpc_flags) ||
+ 	       test_bit(FX00_TARGET_SCAN, &vha->dpc_flags)) {
+ 		if (test_bit(UNLOADING, &base_vha->dpc_flags))
+ 			break;
++>>>>>>> 1d48390117c7 (qla2xxx: small cleanup in qla2x00_wait_for_hba_ready())
  		msleep(1000);
 -	}
  }
  
  int
* Unmerged path drivers/scsi/qla2xxx/qla_os.c
