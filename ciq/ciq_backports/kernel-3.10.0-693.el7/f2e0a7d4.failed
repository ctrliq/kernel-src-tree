Input: wacom - only register once the MODULE_* macros

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-693.el7
commit-author Benjamin Tissoires <benjamin.tissoires@redhat.com>
commit f2e0a7d4a0b94b4274e3b7e15bf650d45a866f3c
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-693.el7/f2e0a7d4.failed

Putting the various MODULE_* makes them appear several times in modinfo
because wacom.h is used both in wacom_sys.c and wacom_wac.h.
Having the macros near the module declaration makes them appear only once.

Add also MODULE_VERSION(DRIVER_VERSION) to export the current version
number.

	Signed-off-by: Benjamin Tissoires <benjamin.tissoires@redhat.com>
	Signed-off-by: Dmitry Torokhov <dmitry.torokhov@gmail.com>
(cherry picked from commit f2e0a7d4a0b94b4274e3b7e15bf650d45a866f3c)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/hid/wacom_sys.c
diff --cc drivers/hid/wacom_sys.c
index 527bf559d1db,3e388ec31da8..000000000000
--- a/drivers/hid/wacom_sys.c
+++ b/drivers/hid/wacom_sys.c
@@@ -1493,11 -1441,16 +1493,20 @@@ static struct usb_driver wacom_driver 
  	.name =		"wacom",
  	.id_table =	wacom_ids,
  	.probe =	wacom_probe,
 -	.remove =	wacom_remove,
 -#ifdef CONFIG_PM
 +	.disconnect =	wacom_disconnect,
 +	.suspend =	wacom_suspend,
  	.resume =	wacom_resume,
  	.reset_resume =	wacom_reset_resume,
 -#endif
 -	.raw_event =	wacom_raw_event,
 +	.supports_autosuspend = 1,
  };
++<<<<<<< HEAD
 +
 +module_usb_driver(wacom_driver);
++=======
+ module_hid_driver(wacom_driver);
+ 
+ MODULE_VERSION(DRIVER_VERSION);
+ MODULE_AUTHOR(DRIVER_AUTHOR);
+ MODULE_DESCRIPTION(DRIVER_DESC);
+ MODULE_LICENSE(DRIVER_LICENSE);
++>>>>>>> f2e0a7d4a0b9 (Input: wacom - only register once the MODULE_* macros)
diff --git a/drivers/hid/wacom.h b/drivers/hid/wacom.h
index 87d2c48e3adc..7e1fc9cb91a3 100644
--- a/drivers/hid/wacom.h
+++ b/drivers/hid/wacom.h
@@ -102,10 +102,6 @@
 #define DRIVER_DESC "USB Wacom tablet driver"
 #define DRIVER_LICENSE "GPL"
 
-MODULE_AUTHOR(DRIVER_AUTHOR);
-MODULE_DESCRIPTION(DRIVER_DESC);
-MODULE_LICENSE(DRIVER_LICENSE);
-
 #define USB_VENDOR_ID_WACOM	0x056a
 #define USB_VENDOR_ID_LENOVO	0x17ef
 
* Unmerged path drivers/hid/wacom_sys.c
