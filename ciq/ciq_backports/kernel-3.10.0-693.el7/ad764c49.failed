usb: Kconfig: move ulpi bus support out of host

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-693.el7
Rebuild_CHGLOG: - [usb] kconfig: move ulpi bus support out of host (Torez Smith) [1383823]
Rebuild_FUZZ: 94.38%
commit-author Peter Chen <peter.chen@nxp.com>
commit ad764c49f65ac171e493e6baf39bc8ba296ef376
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-693.el7/ad764c49.failed

The ULPI bus is not only for host, but for device mode too, so move
it out from host's Kconfig.

	Signed-off-by: Peter Chen <peter.chen@nxp.com>
	Acked-by: Heikki Krogerus <heikki.krogerus@linux.intel.com>
	Signed-off-by: Greg Kroah-Hartman <gregkh@linuxfoundation.org>
(cherry picked from commit ad764c49f65ac171e493e6baf39bc8ba296ef376)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/usb/core/Kconfig
diff --cc drivers/usb/core/Kconfig
index a99c89e78126,d6e43ce96799..000000000000
--- a/drivers/usb/core/Kconfig
+++ b/drivers/usb/core/Kconfig
@@@ -83,24 -82,3 +83,27 @@@ config USB_OTG_FS
  	help
  	  Implements OTG Finite State Machine as specified in On-The-Go
  	  and Embedded Host Supplement to the USB Revision 2.0 Specification.
++<<<<<<< HEAD
 +
 +config USB_ULPI_BUS
 +	tristate "USB ULPI PHY interface support"
 +	depends on USB_SUPPORT
 +	help
 +	  UTMI+ Low Pin Interface (ULPI) is specification for a commonly used
 +	  USB 2.0 PHY interface. The ULPI specification defines a standard set
 +	  of registers that can be used to detect the vendor and product which
 +	  allows ULPI to be handled as a bus. This module is the driver for that
 +	  bus.
 +
 +	  The ULPI interfaces (the buses) are registered by the drivers for USB
 +	  controllers which support ULPI register access and have ULPI PHY
 +	  attached to them. The ULPI PHY drivers themselves are normal PHY
 +	  drivers.
 +
 +	  ULPI PHYs provide often functions such as ADP sensing/probing (OTG
 +	  protocol) and USB charger detection.
 +
 +	  To compile this driver as a module, choose M here: the module will
 +	  be called ulpi.
++=======
++>>>>>>> ad764c49f65a (usb: Kconfig: move ulpi bus support out of host)
diff --git a/drivers/usb/Kconfig b/drivers/usb/Kconfig
index 310e22c5e3ff..d654160c6cd6 100644
--- a/drivers/usb/Kconfig
+++ b/drivers/usb/Kconfig
@@ -155,4 +155,24 @@ config USB_LED_TRIG
 	  LEDs and you want to use them as activity indicators for USB host or
 	  gadget.
 
+config USB_ULPI_BUS
+	tristate "USB ULPI PHY interface support"
+	help
+	  UTMI+ Low Pin Interface (ULPI) is specification for a commonly used
+	  USB 2.0 PHY interface. The ULPI specification defines a standard set
+	  of registers that can be used to detect the vendor and product which
+	  allows ULPI to be handled as a bus. This module is the driver for that
+	  bus.
+
+	  The ULPI interfaces (the buses) are registered by the drivers for USB
+	  controllers which support ULPI register access and have ULPI PHY
+	  attached to them. The ULPI PHY drivers themselves are normal PHY
+	  drivers.
+
+	  ULPI PHYs provide often functions such as ADP sensing/probing (OTG
+	  protocol) and USB charger detection.
+
+	  To compile this driver as a module, choose M here: the module will
+	  be called ulpi.
+
 endif # USB_SUPPORT
* Unmerged path drivers/usb/core/Kconfig
