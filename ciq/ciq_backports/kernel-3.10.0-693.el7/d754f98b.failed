net: add phys ID compare helper to test if two IDs are the same

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-693.el7
Rebuild_CHGLOG: - [net] add phys ID compare helper to test if two IDs are the same (Ivan Vecera) [1275772 1297841 1331748]
Rebuild_FUZZ: 95.87%
commit-author Scott Feldman <sfeldma@gmail.com>
commit d754f98b502ad9a8c7570d494e1eaa0e6bc0350c
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-693.el7/d754f98b.failed

	Signed-off-by: Scott Feldman <sfeldma@gmail.com>
	Acked-by: Jiri Pirko <jiri@resnulli.us>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit d754f98b502ad9a8c7570d494e1eaa0e6bc0350c)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/switchdev/switchdev.c
* Unmerged path net/switchdev/switchdev.c
diff --git a/include/linux/netdevice.h b/include/linux/netdevice.h
index b6150f910ad9..ef2808be46bc 100644
--- a/include/linux/netdevice.h
+++ b/include/linux/netdevice.h
@@ -789,6 +789,13 @@ struct netdev_phys_port_id {
 };
 #define netdev_phys_item_id netdev_phys_port_id
 
+static inline bool netdev_phys_item_id_same(struct netdev_phys_item_id *a,
+					    struct netdev_phys_item_id *b)
+{
+	return a->id_len == b->id_len &&
+	       memcmp(a->id, b->id, a->id_len) == 0;
+}
+
 typedef u16 (*select_queue_fallback_t)(struct net_device *dev,
 				       struct sk_buff *skb);
 
* Unmerged path net/switchdev/switchdev.c
