NTB: Do not advance transport RX on link down

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-693.el7
Rebuild_CHGLOG: - [ntb] Do not advance transport RX on link down (Suravee Suthikulpanit) [1303727]
Rebuild_FUZZ: 94.12%
commit-author Allen Hubbe <Allen.Hubbe@emc.com>
commit c0900b33d1c8c24246eaedb9553f078024c332fc
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-693.el7/c0900b33.failed

On link down, don't advance RX index to the next entry.  The next entry
should never be valid after receiving the link down flag.

	Signed-off-by: Allen Hubbe <Allen.Hubbe@emc.com>
	Signed-off-by: Jon Mason <jdmason@kudzu.us>
(cherry picked from commit c0900b33d1c8c24246eaedb9553f078024c332fc)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/ntb/ntb_transport.c
diff --cc drivers/ntb/ntb_transport.c
index bf77f3a0b404,0f86b60a0c0e..000000000000
--- a/drivers/ntb/ntb_transport.c
+++ b/drivers/ntb/ntb_transport.c
@@@ -1163,12 -1208,17 +1163,26 @@@ static int ntb_process_rxc(struct ntb_t
  		return -EAGAIN;
  	}
  
++<<<<<<< HEAD
 +	if (hdr->ver != (u32) qp->rx_pkts) {
 +		dev_dbg(&ntb_query_pdev(qp->ndev)->dev,
 +			"qp %d: version mismatch, expected %llu - got %u\n",
 +			qp->qp_num, qp->rx_pkts, hdr->ver);
 +		ntb_list_add(&qp->ntb_rx_pend_q_lock, &entry->entry,
 +			     &qp->rx_pend_q);
++=======
+ 	if (hdr->flags & LINK_DOWN_FLAG) {
+ 		dev_dbg(&qp->ndev->pdev->dev, "link down flag set\n");
+ 		ntb_qp_link_down(qp);
+ 		hdr->flags = 0;
+ 		return -EAGAIN;
+ 	}
+ 
+ 	if (hdr->ver != (u32)qp->rx_pkts) {
+ 		dev_dbg(&qp->ndev->pdev->dev,
+ 			"version mismatch, expected %llu - got %u\n",
+ 			qp->rx_pkts, hdr->ver);
++>>>>>>> c0900b33d1c8 (NTB: Do not advance transport RX on link down)
  		qp->rx_err_ver++;
  		return -EIO;
  	}
* Unmerged path drivers/ntb/ntb_transport.c
