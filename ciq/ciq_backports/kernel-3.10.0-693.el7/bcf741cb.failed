crypto: testmgr - Reenable sha1/aes in FIPS mode

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-693.el7
Rebuild_CHGLOG: - [crypto] testmgr - Reenable sha1/aes in FIPS mode (Herbert Xu) [1465234]
Rebuild_FUZZ: 90.91%
commit-author Herbert Xu <herbert@gondor.apana.org.au>
commit bcf741cb779283081db47853264cc94854e7ad83
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-693.el7/bcf741cb.failed

The combination of sha1 and aes was disabled in FIPS Mode
accidentally.  This patch reenables it.

Fixes: 284a0f6e87b0 ("crypto: testmgr - Disable fips-allowed for...")
	Signed-off-by: Herbert Xu <herbert@gondor.apana.org.au>
	Acked-by: Stephan Müller <smueller@chronox.de>
(cherry picked from commit bcf741cb779283081db47853264cc94854e7ad83)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	crypto/testmgr.c
diff --cc crypto/testmgr.c
index cc134d1b6ccc,7125ba3880af..000000000000
--- a/crypto/testmgr.c
+++ b/crypto/testmgr.c
@@@ -1985,8 -2424,25 +1985,28 @@@ static const struct alg_test_desc alg_t
  			}
  		}
  	}, {
 -		.alg = "authenc(hmac(sha256),cbc(aes))",
 +		.alg = "authenc(hmac(sha1),cbc(aes))",
  		.test = alg_test_aead,
++<<<<<<< HEAD
++=======
+ 		.fips_allowed = 1,
+ 		.suite = {
+ 			.aead = {
 -				.enc = __VECS(hmac_sha256_aes_cbc_enc_tv_temp)
++				.enc = __VECS(hmac_sha1_aes_cbc_enc_tv_temp)
+ 			}
+ 		}
+ 	}, {
 -		.alg = "authenc(hmac(sha256),cbc(des))",
++		.alg = "authenc(hmac(sha1),cbc(des))",
+ 		.test = alg_test_aead,
+ 		.suite = {
+ 			.aead = {
 -				.enc = __VECS(hmac_sha256_des_cbc_enc_tv_temp)
++				.enc = __VECS(hmac_sha1_des_cbc_enc_tv_temp)
+ 			}
+ 		}
+ 	}, {
 -		.alg = "authenc(hmac(sha256),cbc(des3_ede))",
++		.alg = "authenc(hmac(sha1),cbc(des3_ede))",
+ 		.test = alg_test_aead,
++>>>>>>> bcf741cb7792 (crypto: testmgr - Reenable sha1/aes in FIPS mode)
  		.fips_allowed = 1,
  		.suite = {
  			.aead = {
* Unmerged path crypto/testmgr.c
