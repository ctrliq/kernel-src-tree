Bluetooth: Put led_trigger field behind CONFIG_BT_LEDS

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-693.el7
commit-author Marcel Holtmann <marcel@holtmann.org>
commit 53f863a66904542b03204f2b115d050b04c11ba5
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-693.el7/53f863a6.failed

The led_trigger field in hci_dev should be conditional based on if
CONFIG_BT_LEDS is set or not.

	Signed-off-by: Marcel Holtmann <marcel@holtmann.org>
	Signed-off-by: Johan Hedberg <johan.hedberg@intel.com>
(cherry picked from commit 53f863a66904542b03204f2b115d050b04c11ba5)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	include/net/bluetooth/hci_core.h
diff --cc include/net/bluetooth/hci_core.h
index f9fcacb85020,b8d43bd9c71b..000000000000
--- a/include/net/bluetooth/hci_core.h
+++ b/include/net/bluetooth/hci_core.h
@@@ -497,8 -398,18 +497,20 @@@ struct hci_dev 
  	__u32			rpa_timeout;
  	struct delayed_work	rpa_expired;
  	bdaddr_t		rpa;
++<<<<<<< HEAD
++=======
+ 
+ #if IS_ENABLED(CONFIG_BT_LEDS)
+ 	struct led_trigger	*power_led;
+ #endif
+ 
+ 	int (*open)(struct hci_dev *hdev);
+ 	int (*close)(struct hci_dev *hdev);
+ 	int (*flush)(struct hci_dev *hdev);
+ 	int (*setup)(struct hci_dev *hdev);
++>>>>>>> 53f863a66904 (Bluetooth: Put led_trigger field behind CONFIG_BT_LEDS)
  	int (*shutdown)(struct hci_dev *hdev);
  	int (*send)(struct hci_dev *hdev, struct sk_buff *skb);
 -	void (*notify)(struct hci_dev *hdev, unsigned int evt);
  	void (*hw_error)(struct hci_dev *hdev, u8 code);
  	int (*post_init)(struct hci_dev *hdev);
  	int (*set_diag)(struct hci_dev *hdev, bool enable);
* Unmerged path include/net/bluetooth/hci_core.h
