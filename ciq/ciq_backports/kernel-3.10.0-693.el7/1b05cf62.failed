qeth: Include error message for "OS Mismatch"

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-693.el7
commit-author Eugene Crosser <Eugene.Crosser@ru.ibm.com>
commit 1b05cf6285c108c8c2acfec47dd02ed50d973823
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-693.el7/1b05cf62.failed

Having understood the semantics of BRIDGEPORT error code 0x0010,
we can introduce a meaningful error message.

	Signed-off-by: Eugene Crosser <Eugene.Crosser@ru.ibm.com>
	Signed-off-by: Ursula Braun <ubraun@linux.vnet.ibm.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 1b05cf6285c108c8c2acfec47dd02ed50d973823)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/s390/net/qeth_l2_main.c
diff --cc drivers/s390/net/qeth_l2_main.c
index 87b4499b880d,faab0b669ca4..000000000000
--- a/drivers/s390/net/qeth_l2_main.c
+++ b/drivers/s390/net/qeth_l2_main.c
@@@ -1670,8 -1797,16 +1670,18 @@@ static int qeth_bridgeport_makerc(struc
  		case 0x000C: /* Not configured as bridge Port */
  			rc = -ENODEV; /* maybe not the best code here? */
  			dev_err(&card->gdev->dev,
 -	"The device is not configured as a Bridge Port\n");
 +	"The HiperSockets device is not configured as a Bridge Port\n");
  			break;
++<<<<<<< HEAD
++=======
+ 		case 0x2B10:
+ 		case 0x0010: /* OS mismatch */
+ 			rc = -EPERM;
+ 			dev_err(&card->gdev->dev,
+ 	"A Bridge Port is already configured by a different operating system\n");
+ 			break;
+ 		case 0x2B14:
++>>>>>>> 1b05cf6285c1 (qeth: Include error message for "OS Mismatch")
  		case 0x0014: /* Another device is Primary */
  			switch (setcmd) {
  			case IPA_SBP_SET_PRIMARY_BRIDGE_PORT:
* Unmerged path drivers/s390/net/qeth_l2_main.c
