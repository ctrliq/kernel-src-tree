drivers/net: fixup comments after "Future-proof tunnel offload handlers"

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-693.el7
Rebuild_CHGLOG: - [netdrv] ixgbe: fixup comments after "Future-proof tunnel offload handlers" (Ken Cox) [1383524]
Rebuild_FUZZ: 91.30%
commit-author Sabrina Dubroca <sd@queasysnail.net>
commit e5de25dce9243a3d29b5ebc131cc9d59008f39f7
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-693.el7/e5de25dc.failed

Some comments weren't updated to reflect the renaming of ndo's and the
change of arguments.

	Signed-off-by: Sabrina Dubroca <sd@queasysnail.net>
	Acked-by: Alexander Duyck <aduyck@mirantis.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit e5de25dce9243a3d29b5ebc131cc9d59008f39f7)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/intel/fm10k/fm10k_netdev.c
#	drivers/net/ethernet/intel/ixgbe/ixgbe_main.c
diff --cc drivers/net/ethernet/intel/fm10k/fm10k_netdev.c
index a7e084e1d610,20a5bbe3f536..000000000000
--- a/drivers/net/ethernet/intel/fm10k/fm10k_netdev.c
+++ b/drivers/net/ethernet/intel/fm10k/fm10k_netdev.c
@@@ -437,8 -432,7 +437,12 @@@ static void fm10k_restore_vxlan_port(st
  /**
   * fm10k_add_vxlan_port
   * @netdev: network interface device structure
++<<<<<<< HEAD
 + * @sa_family: Address family of new port
 + * @port: port number used for VXLAN
++=======
+  * @ti: Tunnel endpoint information
++>>>>>>> e5de25dce924 (drivers/net: fixup comments after "Future-proof tunnel offload handlers")
   *
   * This function is called when a new VXLAN interface has added a new port
   * number to the range that is currently in use for VXLAN.  The new port
@@@ -481,8 -478,7 +485,12 @@@ insert_tail
  /**
   * fm10k_del_vxlan_port
   * @netdev: network interface device structure
++<<<<<<< HEAD
 + * @sa_family: Address family of freed port
 + * @port: port number used for VXLAN
++=======
+  * @ti: Tunnel endpoint information
++>>>>>>> e5de25dce924 (drivers/net: fixup comments after "Future-proof tunnel offload handlers")
   *
   * This function is called when a new VXLAN interface has freed a port
   * number from the range that is currently in use for VXLAN.  The freed
diff --cc drivers/net/ethernet/intel/ixgbe/ixgbe_main.c
index 42cb8cd466de,918b94baca81..000000000000
--- a/drivers/net/ethernet/intel/ixgbe/ixgbe_main.c
+++ b/drivers/net/ethernet/intel/ixgbe/ixgbe_main.c
@@@ -8409,11 -8851,10 +8409,15 @@@ static int ixgbe_set_features(struct ne
  /**
   * ixgbe_add_vxlan_port - Get notifications about VXLAN ports that come up
   * @dev: The port's netdev
++<<<<<<< HEAD
 + * @sa_family: Socket Family that VXLAN is notifiying us about
 + * @port: New UDP port number that VXLAN started listening to
++=======
+  * @ti: Tunnel endpoint information
++>>>>>>> e5de25dce924 (drivers/net: fixup comments after "Future-proof tunnel offload handlers")
   **/
 -static void ixgbe_add_vxlan_port(struct net_device *dev,
 -				 struct udp_tunnel_info *ti)
 +static void ixgbe_add_vxlan_port(struct net_device *dev, sa_family_t sa_family,
 +				 __be16 port)
  {
  	struct ixgbe_adapter *adapter = netdev_priv(dev);
  	struct ixgbe_hw *hw = &adapter->hw;
@@@ -8441,11 -8886,10 +8445,15 @@@
  /**
   * ixgbe_del_vxlan_port - Get notifications about VXLAN ports that go away
   * @dev: The port's netdev
++<<<<<<< HEAD
 + * @sa_family: Socket Family that VXLAN is notifying us about
 + * @port: UDP port number that VXLAN stopped listening to
++=======
+  * @ti: Tunnel endpoint information
++>>>>>>> e5de25dce924 (drivers/net: fixup comments after "Future-proof tunnel offload handlers")
   **/
 -static void ixgbe_del_vxlan_port(struct net_device *dev,
 -				 struct udp_tunnel_info *ti)
 +static void ixgbe_del_vxlan_port(struct net_device *dev, sa_family_t sa_family,
 +				 __be16 port)
  {
  	struct ixgbe_adapter *adapter = netdev_priv(dev);
  
* Unmerged path drivers/net/ethernet/intel/fm10k/fm10k_netdev.c
* Unmerged path drivers/net/ethernet/intel/ixgbe/ixgbe_main.c
diff --git a/drivers/net/geneve.c b/drivers/net/geneve.c
index d6abacdaaf40..491fbe22f3f2 100644
--- a/drivers/net/geneve.c
+++ b/drivers/net/geneve.c
@@ -1143,9 +1143,9 @@ static struct device_type geneve_type = {
 	.name = "geneve",
 };
 
-/* Calls the ndo_add_udp_enc_port of the caller in order to
+/* Calls the ndo_udp_tunnel_add of the caller in order to
  * supply the listening GENEVE udp ports. Callers are expected
- * to implement the ndo_add_udp_enc_port.
+ * to implement the ndo_udp_tunnel_add.
  */
 static void geneve_push_rx_ports(struct net_device *dev)
 {
diff --git a/drivers/net/vxlan.c b/drivers/net/vxlan.c
index 3b2c9ff3617e..9c8601b5f9cf 100644
--- a/drivers/net/vxlan.c
+++ b/drivers/net/vxlan.c
@@ -2494,9 +2494,9 @@ static struct device_type vxlan_type = {
 	.name = "vxlan",
 };
 
-/* Calls the ndo_add_udp_enc_port of the caller in order to
+/* Calls the ndo_udp_tunnel_add of the caller in order to
  * supply the listening VXLAN udp ports. Callers are expected
- * to implement the ndo_add_udp_enc_port.
+ * to implement the ndo_udp_tunnel_add.
  */
 static void vxlan_push_rx_ports(struct net_device *dev)
 {
