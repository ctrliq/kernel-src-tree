scsi: qla2xxx: Fix apparent cut-n-paste error.

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-693.el7
Rebuild_CHGLOG: - [scsi] qla2xxx: Fix apparent cut-n-paste error (Chad Dupuis) [1414957]
Rebuild_FUZZ: 91.76%
commit-author Dave Jones <davej@codemonkey.org.uk>
commit c3c4239465e11b2cc25fcf375c7909a342bcf4dc
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-693.el7/c3c42394.failed

Commit 093df73771ba ("scsi: qla2xxx: Fix Target mode handling with
Multiqueue changes.") introduces two bodies of code that look similar
but with s/req/rsp/ in the second instance.  But in one case, it looks
like this conversion was missed.

	Signed-off-by: Dave Jones <davej@codemonkey.org.uk>
	Reviewed-by: Laurence Oberman <loberman@redhat.com>
	Acked-by: Quinn Tran <Quinn.Tran@cavium.com>
	Signed-off-by: Martin K. Petersen <martin.petersen@oracle.com>
(cherry picked from commit c3c4239465e11b2cc25fcf375c7909a342bcf4dc)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/scsi/qla2xxx/qla_os.c
diff --cc drivers/scsi/qla2xxx/qla_os.c
index f1f989d1fce1,ad4edc13ebcf..000000000000
--- a/drivers/scsi/qla2xxx/qla_os.c
+++ b/drivers/scsi/qla2xxx/qla_os.c
@@@ -415,10 -457,23 +415,16 @@@ static void qla2x00_free_queues(struct 
  	kfree(ha->req_q_map);
  	ha->req_q_map = NULL;
  
 -
 -	spin_lock_irqsave(&ha->hardware_lock, flags);
  	for (cnt = 0; cnt < ha->max_rsp_queues; cnt++) {
 -		if (!test_bit(cnt, ha->rsp_qid_map))
 -			continue;
 -
  		rsp = ha->rsp_q_map[cnt];
++<<<<<<< HEAD
++=======
+ 		clear_bit(cnt, ha->rsp_qid_map);
+ 		ha->rsp_q_map[cnt] =  NULL;
+ 		spin_unlock_irqrestore(&ha->hardware_lock, flags);
++>>>>>>> c3c4239465e1 (scsi: qla2xxx: Fix apparent cut-n-paste error.)
  		qla2x00_free_rsp_que(ha, rsp);
 -		spin_lock_irqsave(&ha->hardware_lock, flags);
  	}
 -	spin_unlock_irqrestore(&ha->hardware_lock, flags);
 -
  	kfree(ha->rsp_q_map);
  	ha->rsp_q_map = NULL;
  }
* Unmerged path drivers/scsi/qla2xxx/qla_os.c
