flow_dissector: remove bogus return in tipc section

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-693.el7
commit-author Jiri Pirko <jiri@resnulli.us>
commit 74b80e841b4da2f83d8f548f32cfe421fde85aca
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-693.el7/74b80e84.failed

Fixes: 06635a35d13d42b9 ("flow_dissect: use programable dissector in skb_flow_dissect and friends")
	Reported-by: Dan Carpenter <dan.carpenter@oracle.com>
	Signed-off-by: Jiri Pirko <jiri@resnulli.us>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 74b80e841b4da2f83d8f548f32cfe421fde85aca)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/core/flow_dissector.c
diff --cc net/core/flow_dissector.c
index fbf58a98e819,703d05916a80..000000000000
--- a/net/core/flow_dissector.c
+++ b/net/core/flow_dissector.c
@@@ -213,10 -274,17 +213,24 @@@ ipv6
  		hdr = __skb_header_pointer(skb, nhoff, sizeof(_hdr), data, hlen, &_hdr);
  		if (!hdr)
  			return false;
++<<<<<<< HEAD
 +		flow->src = hdr->srcnode;
 +		flow->dst = 0;
 +		flow->n_proto = proto;
 +		flow->thoff = (u16)nhoff;
++=======
+ 		key_basic->n_proto = proto;
+ 		key_basic->thoff = (u16)nhoff;
+ 
+ 		if (skb_flow_dissector_uses_key(flow_dissector,
+ 						FLOW_DISSECTOR_KEY_IPV6_HASH_ADDRS)) {
+ 			key_addrs = skb_flow_dissector_target(flow_dissector,
+ 							      FLOW_DISSECTOR_KEY_IPV6_HASH_ADDRS,
+ 							      target_container);
+ 			key_addrs->src = hdr->srcnode;
+ 			key_addrs->dst = 0;
+ 		}
++>>>>>>> 74b80e841b4d (flow_dissector: remove bogus return in tipc section)
  		return true;
  	}
  	case htons(ETH_P_FCOE):
* Unmerged path net/core/flow_dissector.c
