bridge: vlan: Use rcu_dereference instead of rtnl_dereference

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-693.el7
commit-author Ido Schimmel <idosch@mellanox.com>
commit eca1e006cf6f6f66a1a90c055a8a6d393475c3f9
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-693.el7/eca1e006.failed

br_should_learn() is protected by RCU and not by RTNL, so use correct
flavor of nbp_vlan_group().

Fixes: 907b1e6e83ed ("bridge: vlan: use proper rcu for the vlgrp
member")
	Signed-off-by: Ido Schimmel <idosch@mellanox.com>
	Acked-by: Nikolay Aleksandrov <nikolay@cumulusnetworks.com>
	Acked-by: Jiri Pirko <jiri@mellanox.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit eca1e006cf6f6f66a1a90c055a8a6d393475c3f9)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/bridge/br_vlan.c
diff --cc net/bridge/br_vlan.c
index 1122c9d717ea,1394da63614a..000000000000
--- a/net/bridge/br_vlan.c
+++ b/net/bridge/br_vlan.c
@@@ -281,8 -481,9 +281,13 @@@ bool br_should_learn(struct net_bridge_
  	if (!br->vlan_enabled)
  		return true;
  
++<<<<<<< HEAD
 +	v = rcu_dereference(p->vlan_info);
 +	if (!v)
++=======
+ 	vg = nbp_vlan_group_rcu(p);
+ 	if (!vg || !vg->num_vlans)
++>>>>>>> eca1e006cf6f (bridge: vlan: Use rcu_dereference instead of rtnl_dereference)
  		return false;
  
  	if (!br_vlan_get_tag(skb, vid) && skb->vlan_proto != br->vlan_proto)
* Unmerged path net/bridge/br_vlan.c
