PCI: hv: Allocate interrupt descriptors with GFP_ATOMIC

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-693.el7
Rebuild_CHGLOG: - [pci] hv: Allocate interrupt descriptors with GFP_ATOMIC (Vitaly Kuznetsov) [1442282]
Rebuild_FUZZ: 95.24%
commit-author K. Y. Srinivasan <kys@microsoft.com>
commit 59c58ceeea9cdc6144d7b0303753e6bd26d87455
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-693.el7/59c58cee.failed

The memory allocation here needs to be non-blocking.  Fix the issue.

	Signed-off-by: K. Y. Srinivasan <kys@microsoft.com>
	Signed-off-by: Bjorn Helgaas <bhelgaas@google.com>
	Reviewed-by: Long Li <longli@microsoft.com>
	Cc: <stable@vger.kernel.org>
(cherry picked from commit 59c58ceeea9cdc6144d7b0303753e6bd26d87455)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/pci/pci-hyperv.c
diff --cc drivers/pci/pci-hyperv.c
index 38242335bf2b,e73880c5d979..000000000000
--- a/drivers/pci/pci-hyperv.c
+++ b/drivers/pci/pci-hyperv.c
@@@ -817,7 -871,14 +817,18 @@@ static void hv_compose_msi_msg(struct p
  	if (!hpdev)
  		goto return_null_message;
  
++<<<<<<< HEAD:drivers/pci/pci-hyperv.c
 +	int_desc = kzalloc(sizeof(*int_desc), GFP_KERNEL);
++=======
+ 	/* Free any previous message that might have already been composed. */
+ 	if (data->chip_data) {
+ 		int_desc = data->chip_data;
+ 		data->chip_data = NULL;
+ 		hv_int_desc_free(hpdev, int_desc);
+ 	}
+ 
+ 	int_desc = kzalloc(sizeof(*int_desc), GFP_ATOMIC);
++>>>>>>> 59c58ceeea9c (PCI: hv: Allocate interrupt descriptors with GFP_ATOMIC):drivers/pci/host/pci-hyperv.c
  	if (!int_desc)
  		goto drop_reference;
  
* Unmerged path drivers/pci/pci-hyperv.c
