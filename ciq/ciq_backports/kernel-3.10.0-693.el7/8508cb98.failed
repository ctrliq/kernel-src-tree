mmc: queue: remove BUG_ON for bounce_sg

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-693.el7
Rebuild_CHGLOG: - [mmc] queue: remove BUG_ON for bounce_sg (Don Zickus) [1430497]
Rebuild_FUZZ: 93.15%
commit-author Shawn Lin <shawn.lin@rock-chips.com>
commit 8508cb9868015cad4f10d818f32f4030c75e88e0
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-693.el7/8508cb98.failed

bounce_sg for mqrq_cur and mqrq_pre are proper
allocated when initializing the queue and will not
be freed before explicitly cleaning the queue. So from
the code itself it should be quite confident to remove
this check. If that BUG_ON take effects, it is mostly
likely the memory is randomly oopsing.

	Signed-off-by: Shawn Lin <shawn.lin@rock-chips.com>
	Signed-off-by: Ulf Hansson <ulf.hansson@linaro.org>
(cherry picked from commit 8508cb9868015cad4f10d818f32f4030c75e88e0)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/mmc/card/queue.c
diff --cc drivers/mmc/card/queue.c
index e41c15ee6b06,cf29809f69e4..000000000000
--- a/drivers/mmc/card/queue.c
+++ b/drivers/mmc/card/queue.c
@@@ -464,26 -440,12 +464,30 @@@ unsigned int mmc_queue_map_sg(struct mm
  	unsigned int sg_len;
  	size_t buflen;
  	struct scatterlist *sg;
 +	enum mmc_packed_type cmd_type;
  	int i;
  
 -	if (!mqrq->bounce_buf)
 -		return blk_rq_map_sg(mq->queue, mqrq->req, mqrq->sg);
 +	cmd_type = mqrq->cmd_type;
 +
 +	if (!mqrq->bounce_buf) {
 +		if (mmc_packed_cmd(cmd_type))
 +			return mmc_queue_packed_map_sg(mq, mqrq->packed,
 +						       mqrq->sg, cmd_type);
 +		else
 +			return blk_rq_map_sg(mq->queue, mqrq->req, mqrq->sg);
 +	}
 +
++<<<<<<< HEAD
 +	BUG_ON(!mqrq->bounce_sg);
  
 +	if (mmc_packed_cmd(cmd_type))
 +		sg_len = mmc_queue_packed_map_sg(mq, mqrq->packed,
 +						 mqrq->bounce_sg, cmd_type);
 +	else
 +		sg_len = blk_rq_map_sg(mq->queue, mqrq->req, mqrq->bounce_sg);
++=======
+ 	sg_len = blk_rq_map_sg(mq->queue, mqrq->req, mqrq->bounce_sg);
++>>>>>>> 8508cb986801 (mmc: queue: remove BUG_ON for bounce_sg)
  
  	mqrq->bounce_sg_len = sg_len;
  
* Unmerged path drivers/mmc/card/queue.c
