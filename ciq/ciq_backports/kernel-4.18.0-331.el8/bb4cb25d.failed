gfs2: Remove unused gfs2_iomap_alloc argument

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-331.el8
commit-author Andreas Gruenbacher <agruenba@redhat.com>
commit bb4cb25dd319fa5630cc304c5bfa926266736935
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-331.el8/bb4cb25d.failed

Remove the unused flags argument of gfs2_iomap_alloc.

	Signed-off-by: Andreas Gruenbacher <agruenba@redhat.com>
(cherry picked from commit bb4cb25dd319fa5630cc304c5bfa926266736935)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/gfs2/bmap.c
diff --cc fs/gfs2/bmap.c
index ea56f612166e,bd0b0cd38dbe..000000000000
--- a/fs/gfs2/bmap.c
+++ b/fs/gfs2/bmap.c
@@@ -1285,10 -1228,15 +1284,22 @@@ int gfs2_block_map(struct inode *inode
  	clear_buffer_boundary(bh_map);
  	trace_gfs2_bmap(ip, bh_map, lblock, create, 1);
  
++<<<<<<< HEAD
 +	ret = gfs2_iomap_get(inode, pos, length, flags, &iomap, &mp);
 +	if (create && !ret && iomap.type == IOMAP_HOLE)
 +		ret = gfs2_iomap_alloc(inode, &iomap, IOMAP_WRITE, &mp);
 +	release_metapath(&mp);
++=======
+ 	if (create) {
+ 		ret = gfs2_iomap_get(inode, pos, length, IOMAP_WRITE, &iomap, &mp);
+ 		if (!ret && iomap.type == IOMAP_HOLE)
+ 			ret = gfs2_iomap_alloc(inode, &iomap, &mp);
+ 		release_metapath(&mp);
+ 	} else {
+ 		ret = gfs2_iomap_get(inode, pos, length, 0, &iomap, &mp);
+ 		release_metapath(&mp);
+ 	}
++>>>>>>> bb4cb25dd319 (gfs2: Remove unused gfs2_iomap_alloc argument)
  	if (ret)
  		goto out;
  
* Unmerged path fs/gfs2/bmap.c
