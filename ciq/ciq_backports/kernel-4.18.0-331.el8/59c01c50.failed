gfs2: merge gfs2_writepage_common into gfs2_writepage

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-331.el8
commit-author Christoph Hellwig <hch@lst.de>
commit 59c01c5046cc0cf567d58cc7b59a704a36a06899
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-331.el8/59c01c50.failed

There is no need to keep these two functions separate.

	Signed-off-by: Christoph Hellwig <hch@lst.de>
	Signed-off-by: Andreas Gruenbacher <agruenba@redhat.com>
(cherry picked from commit 59c01c5046cc0cf567d58cc7b59a704a36a06899)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/gfs2/aops.c
diff --cc fs/gfs2/aops.c
index f53009f23f8a,4edf917752af..000000000000
--- a/fs/gfs2/aops.c
+++ b/fs/gfs2/aops.c
@@@ -120,34 -115,11 +118,38 @@@ out
  	return 0;
  }
  
++<<<<<<< HEAD
 +/**
 + * gfs2_writepage - Write page for writeback mappings
 + * @page: The page
 + * @wbc: The writeback control
 + *
 + */
 +
 +static int gfs2_writepage(struct page *page, struct writeback_control *wbc)
 +{
 +	int ret;
 +
 +	ret = gfs2_writepage_common(page, wbc);
 +	if (ret <= 0)
 +		return ret;
 +
 +	return nobh_writepage(page, gfs2_get_block_noalloc, wbc);
 +}
 +
 +/**
 + * gfs2_write_jdata_page - gfs2 jdata-specific version of block_write_full_page
 + * @page: The page to write
 + * @wbc: The writeback control
 + *
 + * This is the same as calling block_write_full_page, but it also
++=======
+ /* This is the same as calling block_write_full_page, but it also
++>>>>>>> 59c01c5046cc (gfs2: merge gfs2_writepage_common into gfs2_writepage)
   * writes pages outside of i_size
   */
 -static int gfs2_write_full_page(struct page *page, get_block_t *get_block,
 -				struct writeback_control *wbc)
 +static int gfs2_write_jdata_page(struct page *page,
 +				 struct writeback_control *wbc)
  {
  	struct inode * const inode = page->mapping->host;
  	loff_t i_size = i_size_read(inode);
* Unmerged path fs/gfs2/aops.c
