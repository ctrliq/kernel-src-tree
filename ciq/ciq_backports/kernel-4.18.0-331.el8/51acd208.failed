cifs: remove ctx argument from cifs_setup_cifs_sb

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-331.el8
commit-author Ronnie Sahlberg <lsahlber@redhat.com>
commit 51acd208bd57c82eaa2fda3cab03d62436c6a2ae
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-331.el8/51acd208.failed

	Signed-off-by: Ronnie Sahlberg <lsahlber@redhat.com>
	Signed-off-by: Steve French <stfrench@microsoft.com>
(cherry picked from commit 51acd208bd57c82eaa2fda3cab03d62436c6a2ae)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/cifs/cifsfs.c
diff --cc fs/cifs/cifsfs.c
index a344700860d4,4c385eeecc05..000000000000
--- a/fs/cifs/cifsfs.c
+++ b/fs/cifs/cifsfs.c
@@@ -798,22 -790,33 +798,32 @@@ cifs_smb3_do_mount(struct file_system_t
  	cifs_sb = kzalloc(sizeof(struct cifs_sb_info), GFP_KERNEL);
  	if (cifs_sb == NULL) {
  		root = ERR_PTR(-ENOMEM);
 -		goto out;
 +		goto out_nls;
  	}
  
 -	cifs_sb->ctx = kzalloc(sizeof(struct smb3_fs_context), GFP_KERNEL);
 -	if (!cifs_sb->ctx) {
 +	cifs_sb->mountdata = kstrndup(data, PAGE_SIZE, GFP_KERNEL);
 +	if (cifs_sb->mountdata == NULL) {
  		root = ERR_PTR(-ENOMEM);
 -		goto out;
 -	}
 -	rc = smb3_fs_context_dup(cifs_sb->ctx, old_ctx);
 -	if (rc) {
 -		root = ERR_PTR(rc);
 -		goto out;
 +		goto out_free;
  	}
  
 -	rc = cifs_setup_volume_info(cifs_sb->ctx);
 +	rc = cifs_setup_cifs_sb(ctx, cifs_sb);
  	if (rc) {
  		root = ERR_PTR(rc);
 -		goto out;
 +		goto out_free;
  	}
  
++<<<<<<< HEAD
 +	rc = cifs_mount(cifs_sb, ctx);
++=======
+ 	rc = cifs_setup_cifs_sb(cifs_sb);
+ 	if (rc) {
+ 		root = ERR_PTR(rc);
+ 		goto out;
+ 	}
+ 
+ 	rc = cifs_mount(cifs_sb, cifs_sb->ctx);
++>>>>>>> 51acd208bd57 (cifs: remove ctx argument from cifs_setup_cifs_sb)
  	if (rc) {
  		if (!(flags & SB_SILENT))
  			cifs_dbg(VFS, "cifs_mount failed w/return code = %d\n",
* Unmerged path fs/cifs/cifsfs.c
diff --git a/fs/cifs/cifsproto.h b/fs/cifs/cifsproto.h
index aa66a6b9aaf5..33275efa626b 100644
--- a/fs/cifs/cifsproto.h
+++ b/fs/cifs/cifsproto.h
@@ -235,8 +235,7 @@ extern int cifs_read_page_from_socket(struct TCP_Server_Info *server,
 					struct page *page,
 					unsigned int page_offset,
 					unsigned int to_read);
-extern int cifs_setup_cifs_sb(struct smb3_fs_context *ctx,
-			       struct cifs_sb_info *cifs_sb);
+extern int cifs_setup_cifs_sb(struct cifs_sb_info *cifs_sb);
 extern int cifs_match_super(struct super_block *, void *);
 extern void cifs_cleanup_volume_info(struct smb3_fs_context *ctx);
 extern struct smb3_fs_context *cifs_get_volume_info(char *mount_data,
diff --git a/fs/cifs/connect.c b/fs/cifs/connect.c
index fad8e79e9678..722b91f793e3 100644
--- a/fs/cifs/connect.c
+++ b/fs/cifs/connect.c
@@ -3774,9 +3774,10 @@ void reset_cifs_unix_caps(unsigned int xid, struct cifs_tcon *tcon,
 	}
 }
 
-int cifs_setup_cifs_sb(struct smb3_fs_context *ctx,
-			struct cifs_sb_info *cifs_sb)
+int cifs_setup_cifs_sb(struct cifs_sb_info *cifs_sb)
 {
+	struct smb3_fs_context *ctx = cifs_sb->ctx;
+
 	INIT_DELAYED_WORK(&cifs_sb->prune_tlinks, cifs_prune_tlinks);
 
 	spin_lock_init(&cifs_sb->tlink_tree_lock);
@@ -3791,7 +3792,7 @@ int cifs_setup_cifs_sb(struct smb3_fs_context *ctx,
 	cifs_sb->wsize = ctx->wsize;
 
 	cifs_dbg(FYI, "file mode: %04ho  dir mode: %04ho\n",
-		 cifs_sb->ctx->file_mode, cifs_sb->ctx->dir_mode);
+		 ctx->file_mode, ctx->dir_mode);
 
 	cifs_sb->local_nls = ctx->local_nls;
 
