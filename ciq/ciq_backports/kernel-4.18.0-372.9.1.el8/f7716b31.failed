gre: Don't accidentally set RTO_ONLINK in gre_fill_metadata_dst()

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-372.9.1.el8
commit-author Guillaume Nault <gnault@redhat.com>
commit f7716b318568b22fbf0e3be99279a979e217cf71
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-372.9.1.el8/f7716b31.failed

Mask the ECN bits before initialising ->flowi4_tos. The tunnel key may
have the last ECN bit set, which will interfere with the route lookup
process as ip_route_output_key_hash() interpretes this bit specially
(to restrict the route scope).

Found by code inspection, compile tested only.

Fixes: 962924fa2b7a ("ip_gre: Refactor collect metatdata mode tunnel xmit to ip_md_tunnel_xmit")
	Signed-off-by: Guillaume Nault <gnault@redhat.com>
	Signed-off-by: Jakub Kicinski <kuba@kernel.org>
(cherry picked from commit f7716b318568b22fbf0e3be99279a979e217cf71)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/ipv4/ip_gre.c
diff --cc net/ipv4/ip_gre.c
index 3d3141eb6f7c,99db2e41ed10..000000000000
--- a/net/ipv4/ip_gre.c
+++ b/net/ipv4/ip_gre.c
@@@ -587,8 -604,9 +587,14 @@@ static int gre_fill_metadata_dst(struc
  
  	key = &info->key;
  	ip_tunnel_init_flow(&fl4, IPPROTO_GRE, key->u.ipv4.dst, key->u.ipv4.src,
++<<<<<<< HEAD
 +			    tunnel_id_to_key32(key->tun_id), key->tos, 0,
 +			    skb->mark);
++=======
+ 			    tunnel_id_to_key32(key->tun_id),
+ 			    key->tos & ~INET_ECN_MASK, 0, skb->mark,
+ 			    skb_get_hash(skb));
++>>>>>>> f7716b318568 (gre: Don't accidentally set RTO_ONLINK in gre_fill_metadata_dst())
  	rt = ip_route_output_key(dev_net(dev), &fl4);
  	if (IS_ERR(rt))
  		return PTR_ERR(rt);
* Unmerged path net/ipv4/ip_gre.c
