ALSA: msnd: Allocate resources with device-managed APIs

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-372.9.1.el8
commit-author Takashi Iwai <tiwai@suse.de>
commit 098493041a2ce8a35eada728e136e5a5edfe24ad
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-372.9.1.el8/09849304.failed

This patch converts the resource management in ISA msnd drivers with
devres as a clean up.  Each manual resource management is converted
with the corresponding devres helper.  The remove callback became
superfluous and dropped.

This should give no user-visible functional changes.

Link: https://lore.kernel.org/r/20210715075941.23332-67-tiwai@suse.de
	Signed-off-by: Takashi Iwai <tiwai@suse.de>
(cherry picked from commit 098493041a2ce8a35eada728e136e5a5edfe24ad)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	sound/isa/msnd/msnd_pinnacle.c
diff --cc sound/isa/msnd/msnd_pinnacle.c
index 24b34ecf5e5b,4433a92f08e7..000000000000
--- a/sound/isa/msnd/msnd_pinnacle.c
+++ b/sound/isa/msnd/msnd_pinnacle.c
@@@ -1040,19 -1001,6 +1001,22 @@@ static int snd_msnd_isa_probe(struct de
  	dev_set_drvdata(pdev, card);
  
  	return 0;
++<<<<<<< HEAD
 +
 +#ifndef MSND_CLASSIC
 +cfg_error:
 +	release_region(cfg[idx], 2);
 +	snd_card_free(card);
 +	return err;
 +#endif
 +}
 +
 +static int snd_msnd_isa_remove(struct device *pdev, unsigned int dev)
 +{
 +	snd_msnd_unload(dev_get_drvdata(pdev));
 +	return 0;
++=======
++>>>>>>> 098493041a2c (ALSA: msnd: Allocate resources with device-managed APIs)
  }
  
  static struct isa_driver snd_msnd_driver = {
* Unmerged path sound/isa/msnd/msnd_pinnacle.c
