ASoC: topology: Fix using uninitialized pointer

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-372.9.1.el8
commit-author Jaska Uimonen <jaska.uimonen@linux.intel.com>
commit b9c035aa43b8c074b3bcfdaaa8bea2537d85b7c3
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-372.9.1.el8/b9c035aa.failed

The original patch changed kcontrol_type to a pointer. In some goto
cases the pointer is assigned into a struct member as uninitialized and
this will cause a runtime error with UBSan even if it isn't a real bug.
So initialize the pointer to NULL.

	Reported-by: Dan Carpenter <dan.carpenter@oracle.com>
Fixes: d29d41e28eea ("ASoC: topology: Add support for multiple kcontrol types to a widget")
	Signed-off-by: Jaska Uimonen <jaska.uimonen@linux.intel.com>
	Reviewed-by: Kai Vehmanen <kai.vehmanen@linux.intel.com>
Link: https://lore.kernel.org/r/20210519100713.879958-1-jaska.uimonen@linux.intel.com
	Signed-off-by: Mark Brown <broonie@kernel.org>
(cherry picked from commit b9c035aa43b8c074b3bcfdaaa8bea2537d85b7c3)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	sound/soc/soc-topology.c
diff --cc sound/soc/soc-topology.c
index 8aaf5d0beaff,5e65b72910e9..000000000000
--- a/sound/soc/soc-topology.c
+++ b/sound/soc/soc-topology.c
@@@ -1455,8 -1422,13 +1455,16 @@@ static int soc_tplg_dapm_widget_create(
  	struct snd_soc_dapm_widget template, *widget;
  	struct snd_soc_tplg_ctl_hdr *control_hdr;
  	struct snd_soc_card *card = tplg->comp->card;
++<<<<<<< HEAD
 +	unsigned int kcontrol_type;
++=======
+ 	unsigned int *kcontrol_type = NULL;
+ 	struct snd_kcontrol_new *kc;
+ 	int mixer_count = 0;
+ 	int bytes_count = 0;
+ 	int enum_count = 0;
++>>>>>>> b9c035aa43b8 (ASoC: topology: Fix using uninitialized pointer)
  	int ret = 0;
 -	int i;
  
  	if (strnlen(w->name, SNDRV_CTL_ELEM_ID_NAME_MAXLEN) ==
  		SNDRV_CTL_ELEM_ID_NAME_MAXLEN)
* Unmerged path sound/soc/soc-topology.c
