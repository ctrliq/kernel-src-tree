ASoC: Intel: bytcr_rt5651: use devm_clk_get_optional() for mclk

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-372.9.1.el8
commit-author Andy Shevchenko <andriy.shevchenko@linux.intel.com>
commit a8627df5491e00e23d4f2e648ff796adbfa23cc5
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-372.9.1.el8/a8627df5.failed

The devm_clk_get_optional() helper returns NULL when devm_clk_get()
returns -ENOENT. This makes things slightly cleaner. The added benefit
is mostly cosmetic.

	Signed-off-by: Andy Shevchenko <andriy.shevchenko@linux.intel.com>
	Reviewed-by: Hans de Goede <hdegoede@redhat.com>
Link: https://lore.kernel.org/r/20211007170250.27997-4-andriy.shevchenko@linux.intel.com
	Signed-off-by: Mark Brown <broonie@kernel.org>
(cherry picked from commit a8627df5491e00e23d4f2e648ff796adbfa23cc5)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	sound/soc/intel/boards/bytcr_rt5651.c
diff --cc sound/soc/intel/boards/bytcr_rt5651.c
index cdbd973658b5,28c561302e69..000000000000
--- a/sound/soc/intel/boards/bytcr_rt5651.c
+++ b/sound/soc/intel/boards/bytcr_rt5651.c
@@@ -1055,21 -1056,20 +1047,24 @@@ static int snd_byt_rt5651_mc_probe(stru
  		byt_rt5651_dais[dai_index].cpus->dai_name = "ssp0-port";
  
  	if (byt_rt5651_quirk & BYT_RT5651_MCLK_EN) {
++<<<<<<< HEAD
 +		priv->mclk = devm_clk_get(&pdev->dev, "pmc_plt_clk_3");
++=======
+ 		priv->mclk = devm_clk_get_optional(dev, "pmc_plt_clk_3");
++>>>>>>> a8627df5491e (ASoC: Intel: bytcr_rt5651: use devm_clk_get_optional() for mclk)
  		if (IS_ERR(priv->mclk)) {
  			ret_val = PTR_ERR(priv->mclk);
 -			dev_err(dev,
 +			dev_err(&pdev->dev,
  				"Failed to get MCLK from pmc_plt_clk_3: %d\n",
  				ret_val);
- 			/*
- 			 * Fall back to bit clock usage for -ENOENT (clock not
- 			 * available likely due to missing dependencies), bail
- 			 * for all other errors, including -EPROBE_DEFER
- 			 */
- 			if (ret_val != -ENOENT)
- 				goto err;
- 			byt_rt5651_quirk &= ~BYT_RT5651_MCLK_EN;
+ 			goto err;
  		}
+ 		/*
+ 		 * Fall back to bit clock usage when clock is not
+ 		 * available likely due to missing dependencies.
+ 		 */
+ 		if (!priv->mclk)
+ 			byt_rt5651_quirk &= ~BYT_RT5651_MCLK_EN;
  	}
  
  	snprintf(byt_rt5651_components, sizeof(byt_rt5651_components),
* Unmerged path sound/soc/intel/boards/bytcr_rt5651.c
