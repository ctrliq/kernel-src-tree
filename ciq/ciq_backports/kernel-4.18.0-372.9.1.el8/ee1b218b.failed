xfs: cleanup the read/write helper naming

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-372.9.1.el8
commit-author Christoph Hellwig <hch@lst.de>
commit ee1b218b09560982010e4dfffa8f9d4f05f62220
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-372.9.1.el8/ee1b218b.failed

Drop a few pointless aio_ prefixes.

	Signed-off-by: Christoph Hellwig <hch@lst.de>
	Reviewed-by: Dave Chinner <dchinner@redhat.com>
	Reviewed-by: Brian Foster <bfoster@redhat.com>
	Reviewed-by: Darrick J. Wong <djwong@kernel.org>
	Signed-off-by: Darrick J. Wong <djwong@kernel.org>
(cherry picked from commit ee1b218b09560982010e4dfffa8f9d4f05f62220)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/xfs/xfs_file.c
diff --cc fs/xfs/xfs_file.c
index 57eb6b8f3be3,3dc5f8ca64ec..000000000000
--- a/fs/xfs/xfs_file.c
+++ b/fs/xfs/xfs_file.c
@@@ -223,8 -197,25 +223,8 @@@ xfs_file_fsync
  	return error;
  }
  
 -static int
 -xfs_ilock_iocb(
 -	struct kiocb		*iocb,
 -	unsigned int		lock_mode)
 -{
 -	struct xfs_inode	*ip = XFS_I(file_inode(iocb->ki_filp));
 -
 -	if (iocb->ki_flags & IOCB_NOWAIT) {
 -		if (!xfs_ilock_nowait(ip, lock_mode))
 -			return -EAGAIN;
 -	} else {
 -		xfs_ilock(ip, lock_mode);
 -	}
 -
 -	return 0;
 -}
 -
  STATIC ssize_t
- xfs_file_dio_aio_read(
+ xfs_file_dio_read(
  	struct kiocb		*iocb,
  	struct iov_iter		*to)
  {
@@@ -644,14 -630,10 +644,21 @@@ xfs_file_dax_write
  	size_t			count;
  	loff_t			pos;
  
++<<<<<<< HEAD
 +	if (iocb->ki_flags & IOCB_NOWAIT) {
 +		if (!xfs_ilock_nowait(ip, iolock))
 +			return -EAGAIN;
 +	} else {
 +		xfs_ilock(ip, iolock);
 +	}
 +
 +	ret = xfs_file_aio_write_checks(iocb, from, &iolock);
++=======
+ 	ret = xfs_ilock_iocb(iocb, iolock);
+ 	if (ret)
+ 		return ret;
+ 	ret = xfs_file_write_checks(iocb, from, &iolock);
++>>>>>>> ee1b218b0956 (xfs: cleanup the read/write helper naming)
  	if (ret)
  		goto out;
  
* Unmerged path fs/xfs/xfs_file.c
