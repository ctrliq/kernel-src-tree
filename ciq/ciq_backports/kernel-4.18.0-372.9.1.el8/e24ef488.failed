ALSA: opti9xx: fix missing { } around an if block

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-372.9.1.el8
commit-author Colin Ian King <colin.king@canonical.com>
commit e24ef4881bdd11c13d35235a410571c0d0aa0e5d
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-372.9.1.el8/e24ef488.failed

Currently the { } braces are missing around an if block causing subsequent
code after the return to become unreachable. Fix this by adding the
missing { }.

Addresses-Coverity: ("Structurally dead code")
Fixes: 2973ee4a5b54 ("ALSA: opti9xx: Allocate resources with device-managed APIs")
	Signed-off-by: Colin Ian King <colin.king@canonical.com>
Link: https://lore.kernel.org/r/20210720153741.73230-1-colin.king@canonical.com
	Signed-off-by: Takashi Iwai <tiwai@suse.de>
(cherry picked from commit e24ef4881bdd11c13d35235a410571c0d0aa0e5d)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	sound/isa/opti9xx/opti92x-ad1848.c
diff --cc sound/isa/opti9xx/opti92x-ad1848.c
index 50d92f6536a9,4beeb32fe2a7..000000000000
--- a/sound/isa/opti9xx/opti92x-ad1848.c
+++ b/sound/isa/opti9xx/opti92x-ad1848.c
@@@ -1126,14 -1093,11 +1126,18 @@@ static int snd_opti9xx_pnp_probe(struc
  	}
  
  	error = snd_opti9xx_init(chip, hw);
 -	if (error)
 +	if (error) {
 +		snd_card_free(card);
  		return error;
++<<<<<<< HEAD
 +	}
 +	error = snd_opti9xx_read_check(chip);
++=======
+ 	error = snd_opti9xx_read_check(card, chip);
++>>>>>>> e24ef4881bdd (ALSA: opti9xx: fix missing { } around an if block)
  	if (error) {
  		snd_printk(KERN_ERR "OPTI chip not found\n");
 +		snd_card_free(card);
  		return error;
  	}
  	error = snd_opti9xx_probe(card);
* Unmerged path sound/isa/opti9xx/opti92x-ad1848.c
