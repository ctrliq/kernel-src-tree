drm/amd/pm: Remove artificial freq level on Navi1x

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-372.9.1.el8
commit-author Lijo Lazar <lijo.lazar@amd.com>
commit be83a5676767c99c2417083c29d42aa1e109a69d
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-372.9.1.el8/be83a567.failed

Print Navi1x fine grained clocks in a consistent manner with other SOCs.
Don't show aritificial DPM level when the current clock equals min or max.

	Signed-off-by: Lijo Lazar <lijo.lazar@amd.com>
	Reviewed-by: Evan Quan <evan.quan@amd.com>
	Acked-by: Alex Deucher <alexander.deucher@amd.com>
	Signed-off-by: Alex Deucher <alexander.deucher@amd.com>
(cherry picked from commit be83a5676767c99c2417083c29d42aa1e109a69d)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/gpu/drm/amd/pm/swsmu/smu11/navi10_ppt.c
diff --cc drivers/gpu/drm/amd/pm/swsmu/smu11/navi10_ppt.c
index 23ada41351ad,60a557068ea4..000000000000
--- a/drivers/gpu/drm/amd/pm/swsmu/smu11/navi10_ppt.c
+++ b/drivers/gpu/drm/amd/pm/swsmu/smu11/navi10_ppt.c
@@@ -1317,14 -1319,17 +1317,22 @@@ static int navi10_print_clk_levels(stru
  			freq_values[1] = cur_value;
  			mark_index = cur_value == freq_values[0] ? 0 :
  				     cur_value == freq_values[2] ? 2 : 1;
- 			if (mark_index != 1)
- 				freq_values[1] = (freq_values[0] + freq_values[2]) / 2;
  
++<<<<<<< HEAD
 +			for (i = 0; i < 3; i++) {
 +				size += sprintf(buf + size, "%d: %uMhz %s\n", i, freq_values[i],
- 						i == mark_index ? "*" : "");
++=======
+ 			levels = 3;
+ 			if (mark_index != 1) {
+ 				levels = 2;
+ 				freq_values[1] = freq_values[2];
  			}
  
+ 			for (i = 0; i < levels; i++) {
+ 				size += sysfs_emit_at(buf, size, "%d: %uMhz %s\n", i, freq_values[i],
++>>>>>>> be83a5676767 (drm/amd/pm: Remove artificial freq level on Navi1x)
+ 						i == mark_index ? "*" : "");
+ 			}
  		}
  		break;
  	case SMU_PCIE:
* Unmerged path drivers/gpu/drm/amd/pm/swsmu/smu11/navi10_ppt.c
