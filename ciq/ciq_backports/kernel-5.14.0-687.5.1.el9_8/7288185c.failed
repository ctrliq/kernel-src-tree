gfs2: less aggressive low-memory log flushing

jira KERNEL-1035
Rebuild_History Non-Buildable kernel-5.14.0-687.5.1.el9_8
commit-author Andreas Gruenbacher <agruenba@redhat.com>
commit 7288185ce87ec70133b7bc3b694b0f74bf46a0ee
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-5.14.0-687.5.1.el9_8/7288185c.failed

It turns out that for some workloads, the fix in commit b74cd55aa9a9d
("gfs2: low-memory forced flush fixes") causes the number of forced log
flushes to increase to a degree that the overall filesystem performance
drops significantly.  Address that by forcing a log flush only when
gfs2_writepages cannot make any progress rather than when it cannot make
"enough" progress.

Fixes: b74cd55aa9a9d ("gfs2: low-memory forced flush fixes")
	Signed-off-by: Andreas Gruenbacher <agruenba@redhat.com>
(cherry picked from commit 7288185ce87ec70133b7bc3b694b0f74bf46a0ee)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/gfs2/aops.c
diff --cc fs/gfs2/aops.c
index f090ea410c56,6a6ded7a61d2..000000000000
--- a/fs/gfs2/aops.c
+++ b/fs/gfs2/aops.c
@@@ -179,7 -158,12 +179,16 @@@ static int gfs2_writepages(struct addre
  			   struct writeback_control *wbc)
  {
  	struct gfs2_sbd *sdp = gfs2_mapping2sbd(mapping);
++<<<<<<< HEAD
 +	struct iomap_writepage_ctx wpc = { };
++=======
+ 	long initial_nr_to_write = wbc->nr_to_write;
+ 	struct iomap_writepage_ctx wpc = {
+ 		.inode		= mapping->host,
+ 		.wbc		= wbc,
+ 		.ops		= &gfs2_writeback_ops,
+ 	};
++>>>>>>> 7288185ce87e (gfs2: less aggressive low-memory log flushing)
  	int ret;
  
  	/*
@@@ -188,8 -172,8 +197,13 @@@
  	 * want balance_dirty_pages() to loop indefinitely trying to write out
  	 * pages held in the ail that it can't find.
  	 */
++<<<<<<< HEAD
 +	ret = iomap_writepages(mapping, wbc, &wpc, &gfs2_writeback_ops);
 +	if (ret == 0 && wbc->nr_to_write > 0)
++=======
+ 	ret = iomap_writepages(&wpc);
+ 	if (ret == 0 && wbc->nr_to_write == initial_nr_to_write)
++>>>>>>> 7288185ce87e (gfs2: less aggressive low-memory log flushing)
  		set_bit(SDF_FORCE_AIL_FLUSH, &sdp->sd_flags);
  	return ret;
  }
* Unmerged path fs/gfs2/aops.c
