gfs2: gfs2_glock_hold cleanup

jira KERNEL-1574
Rebuild_History Non-Buildable kernel-4.18.0-553.162.1.el8_10
commit-author Andreas Gruenbacher <agruenba@redhat.com>
commit d3b39fcb39574780fffec4357412ce0460339e9f
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-553.162.1.el8_10/d3b39fcb.failed

Use lockref_get_not_dead() instead of an unguarded __lockref_is_dead()
check.

	Signed-off-by: Andreas Gruenbacher <agruenba@redhat.com>
(cherry picked from commit d3b39fcb39574780fffec4357412ce0460339e9f)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/gfs2/glock.c
diff --cc fs/gfs2/glock.c
index 8cc089e815cd,bba415511920..000000000000
--- a/fs/gfs2/glock.c
+++ b/fs/gfs2/glock.c
@@@ -211,33 -183,13 +211,39 @@@ static void gfs2_free_dead_glocks(struc
   *
   */
  
 -struct gfs2_glock *gfs2_glock_hold(struct gfs2_glock *gl)
 +void gfs2_glock_hold(struct gfs2_glock *gl)
  {
++<<<<<<< HEAD
 +	GLOCK_BUG_ON(gl, __lockref_is_dead(&gl->gl_lockref));
 +	lockref_get(&gl->gl_lockref);
++=======
+ 	if (!lockref_get_not_dead(&gl->gl_lockref))
+ 		GLOCK_BUG_ON(gl, 1);
+ 	return gl;
++>>>>>>> d3b39fcb3957 (gfs2: gfs2_glock_hold cleanup)
  }
  
 +/**
 + * demote_ok - Check to see if it's ok to unlock a glock
 + * @gl: the glock
 + *
 + * Returns: 1 if it's ok
 + */
 +
 +static int demote_ok(const struct gfs2_glock *gl)
 +{
 +	const struct gfs2_glock_operations *glops = gl->gl_ops;
 +
 +	if (gl->gl_state == LM_ST_UNLOCKED)
 +		return 0;
 +	if (!list_empty(&gl->gl_holders))
 +		return 0;
 +	if (glops->go_demote_ok)
 +		return glops->go_demote_ok(gl);
 +	return 1;
 +}
 +
 +
  static void gfs2_glock_add_to_lru(struct gfs2_glock *gl)
  {
  	spin_lock(&lru_lock);
* Unmerged path fs/gfs2/glock.c
