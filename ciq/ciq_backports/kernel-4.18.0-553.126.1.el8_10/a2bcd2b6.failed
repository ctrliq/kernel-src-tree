Bluetooth: hci_sync: Avoid use-after-free in dbg for hci_add_adv_monitor()

jira KERNEL-1078
Rebuild_History Non-Buildable kernel-4.18.0-553.126.1.el8_10
commit-author Manish Mandlik <mmandlik@google.com>
commit a2bcd2b63271a93a695fabbfbf459c603d956d48
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-553.126.1.el8_10/a2bcd2b6.failed

KSAN reports use-after-free in hci_add_adv_monitor().

While adding an adv monitor,
    hci_add_adv_monitor() calls ->
    msft_add_monitor_pattern() calls ->
    msft_add_monitor_sync() calls ->
    msft_le_monitor_advertisement_cb() calls in an error case ->
    hci_free_adv_monitor() which frees the *moniter.

This is referenced by bt_dev_dbg() in hci_add_adv_monitor().

Fix the bt_dev_dbg() by using handle instead of monitor->handle.

Fixes: b747a83690c8 ("Bluetooth: hci_sync: Refactor add Adv Monitor")
	Signed-off-by: Manish Mandlik <mmandlik@google.com>
	Signed-off-by: Luiz Augusto von Dentz <luiz.von.dentz@intel.com>
(cherry picked from commit a2bcd2b63271a93a695fabbfbf459c603d956d48)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/bluetooth/hci_core.c
diff --cc net/bluetooth/hci_core.c
index 3fb0d6b800db,a5992f1b3c9b..000000000000
--- a/net/bluetooth/hci_core.c
+++ b/net/bluetooth/hci_core.c
@@@ -1938,14 -1949,15 +1938,20 @@@ bool hci_add_adv_monitor(struct hci_de
  
  	switch (hci_get_adv_monitor_offload_ext(hdev)) {
  	case HCI_ADV_MONITOR_EXT_NONE:
 -		bt_dev_dbg(hdev, "add monitor %d status %d",
 -			   monitor->handle, status);
 +		hci_update_passive_scan(hdev);
 +		bt_dev_dbg(hdev, "%s add monitor status %d", hdev->name, *err);
  		/* Message was not forwarded to controller - not an error */
 -		break;
 -
 +		return false;
  	case HCI_ADV_MONITOR_EXT_MSFT:
++<<<<<<< HEAD
 +		*err = msft_add_monitor_pattern(hdev, monitor);
 +		bt_dev_dbg(hdev, "%s add monitor msft status %d", hdev->name,
 +			   *err);
++=======
+ 		status = msft_add_monitor_pattern(hdev, monitor);
+ 		bt_dev_dbg(hdev, "add monitor %d msft status %d",
+ 			   handle, status);
++>>>>>>> a2bcd2b63271 (Bluetooth: hci_sync: Avoid use-after-free in dbg for hci_add_adv_monitor())
  		break;
  	}
  
* Unmerged path net/bluetooth/hci_core.c
