crypto: algif_aead - Do not set MAY_BACKLOG on the async path

jira KERNEL-1078
Rebuild_History Non-Buildable kernel-4.18.0-553.126.1.el8_10
commit-author Herbert Xu <herbert@gondor.apana.org.au>
commit cbdad1f246dd98e6c9c32a6e5212337f542aa7e0
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-553.126.1.el8_10/cbdad1f2.failed

The async path cannot use MAY_BACKLOG because it is not meant to
block, which is what MAY_BACKLOG does.  On the other hand, both
the sync and async paths can make use of MAY_SLEEP.

Fixes: 83094e5e9e49 ("crypto: af_alg - add async support to...")
	Cc: <stable@vger.kernel.org>
	Signed-off-by: Herbert Xu <herbert@gondor.apana.org.au>
(cherry picked from commit cbdad1f246dd98e6c9c32a6e5212337f542aa7e0)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	crypto/algif_aead.c
diff --cc crypto/algif_aead.c
index 198fc4cdbc50,42493b4d8ce4..000000000000
--- a/crypto/algif_aead.c
+++ b/crypto/algif_aead.c
@@@ -79,10 -75,10 +79,15 @@@ static int crypto_aead_copy_sgl(struct 
  				struct scatterlist *src,
  				struct scatterlist *dst, unsigned int len)
  {
 -	SYNC_SKCIPHER_REQUEST_ON_STACK(skreq, null_tfm);
 +	SKCIPHER_REQUEST_ON_STACK(skreq, null_tfm);
  
++<<<<<<< HEAD
 +	skcipher_request_set_tfm(skreq, null_tfm);
 +	skcipher_request_set_callback(skreq, CRYPTO_TFM_REQ_MAY_BACKLOG,
++=======
+ 	skcipher_request_set_sync_tfm(skreq, null_tfm);
+ 	skcipher_request_set_callback(skreq, CRYPTO_TFM_REQ_MAY_SLEEP,
++>>>>>>> cbdad1f246dd (crypto: algif_aead - Do not set MAY_BACKLOG on the async path)
  				      NULL, NULL);
  	skcipher_request_set_crypt(skreq, src, dst, len, NULL);
  
* Unmerged path crypto/algif_aead.c
