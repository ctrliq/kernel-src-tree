smb: common: change the data type of num_aces to le16

jira KERNEL-1078
Rebuild_History Non-Buildable kernel-4.18.0-553.126.1.el8_10
commit-author Namjae Jeon <linkinjeon@kernel.org>
commit 62e7dd0a39c2d0d7ff03274c36df971f1b3d2d0d
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-553.126.1.el8_10/62e7dd0a.failed

2.4.5 in [MS-DTYP].pdf describe the data type of num_aces as le16.

AceCount (2 bytes): An unsigned 16-bit integer that specifies the count
of the number of ACE records in the ACL.

Change it to le16 and add reserved field to smb_acl struct.

	Reported-by: Igor Leite Ladessa <igor-ladessa@hotmail.com>
	Tested-by: Igor Leite Ladessa <igor-ladessa@hotmail.com>
	Signed-off-by: Namjae Jeon <linkinjeon@kernel.org>
	Signed-off-by: Steve French <stfrench@microsoft.com>
(cherry picked from commit 62e7dd0a39c2d0d7ff03274c36df971f1b3d2d0d)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/cifs/cifsacl.c
#	fs/smb/common/smbacl.h
#	fs/smb/server/smbacl.c
#	fs/smb/server/smbacl.h
diff --cc fs/cifs/cifsacl.c
index 0af6988cb2f4,7d953208046a..000000000000
--- a/fs/cifs/cifsacl.c
+++ b/fs/cifs/cifsacl.c
@@@ -758,10 -763,10 +758,10 @@@ static void parse_dacl(struct cifs_acl 
  		       struct cifs_fattr *fattr, bool mode_from_special_sid)
  {
  	int i;
- 	int num_aces = 0;
+ 	u16 num_aces = 0;
  	int acl_size;
  	char *acl_base;
 -	struct smb_ace **ppace;
 +	struct cifs_ace **ppace;
  
  	/* BB need to add parm so we can store the SID BB */
  
@@@ -788,9 -793,9 +788,9 @@@
  	fattr->cf_mode &= ~(0777);
  
  	acl_base = (char *)pdacl;
 -	acl_size = sizeof(struct smb_acl);
 +	acl_size = sizeof(struct cifs_acl);
  
- 	num_aces = le32_to_cpu(pdacl->num_aces);
+ 	num_aces = le16_to_cpu(pdacl->num_aces);
  	if (num_aces > 0) {
  		umode_t denied_mode = 0;
  
@@@ -925,13 -935,14 +925,21 @@@ unsigned int setup_special_user_owner_A
  }
  
  static void populate_new_aces(char *nacl_base,
++<<<<<<< HEAD:fs/cifs/cifsacl.c
 +		struct cifs_sid *pownersid,
 +		struct cifs_sid *pgrpsid,
 +		__u64 *pnmode, u32 *pnum_aces, u16 *pnsize,
 +		bool modefromsid)
++=======
+ 		struct smb_sid *pownersid,
+ 		struct smb_sid *pgrpsid,
+ 		__u64 *pnmode, u16 *pnum_aces, u16 *pnsize,
+ 		bool modefromsid,
+ 		bool posix)
++>>>>>>> 62e7dd0a39c2 (smb: common: change the data type of num_aces to le16):fs/smb/client/cifsacl.c
  {
  	__u64 nmode;
- 	u32 num_aces = 0;
+ 	u16 num_aces = 0;
  	u16 nsize = 0;
  	__u64 user_mode;
  	__u64 group_mode;
@@@ -1035,20 -1048,20 +1043,25 @@@ static __u16 replace_sids_and_copy_aces
  {
  	int i;
  	u16 size = 0;
 -	struct smb_ace *pntace = NULL;
 +	struct cifs_ace *pntace = NULL;
  	char *acl_base = NULL;
- 	u32 src_num_aces = 0;
+ 	u16 src_num_aces = 0;
  	u16 nsize = 0;
 -	struct smb_ace *pnntace = NULL;
 +	struct cifs_ace *pnntace = NULL;
  	char *nacl_base = NULL;
  	u16 ace_size = 0;
  
  	acl_base = (char *)pdacl;
++<<<<<<< HEAD:fs/cifs/cifsacl.c
 +	size = sizeof(struct cifs_acl);
 +	src_num_aces = le32_to_cpu(pdacl->num_aces);
++=======
+ 	size = sizeof(struct smb_acl);
+ 	src_num_aces = le16_to_cpu(pdacl->num_aces);
++>>>>>>> 62e7dd0a39c2 (smb: common: change the data type of num_aces to le16):fs/smb/client/cifsacl.c
  
  	nacl_base = (char *)pndacl;
 -	nsize = sizeof(struct smb_acl);
 +	nsize = sizeof(struct cifs_acl);
  
  	/* Go through all the ACEs */
  	for (i = 0; i < src_num_aces; ++i) {
@@@ -1075,14 -1088,13 +1088,18 @@@ static int set_chmod_dacl(struct cifs_a
  {
  	int i;
  	u16 size = 0;
 -	struct smb_ace *pntace = NULL;
 +	struct cifs_ace *pntace = NULL;
  	char *acl_base = NULL;
- 	u32 src_num_aces = 0;
+ 	u16 src_num_aces = 0;
  	u16 nsize = 0;
 -	struct smb_ace *pnntace = NULL;
 +	struct cifs_ace *pnntace = NULL;
  	char *nacl_base = NULL;
++<<<<<<< HEAD:fs/cifs/cifsacl.c
 +	u32 num_aces = 0;
 +	__u64 nmode;
++=======
+ 	u16 num_aces = 0;
++>>>>>>> 62e7dd0a39c2 (smb: common: change the data type of num_aces to le16):fs/smb/client/cifsacl.c
  	bool new_aces_set = false;
  
  	/* Assuming that pndacl and pnmode are never NULL */
@@@ -1100,8 -1111,8 +1117,13 @@@
  	}
  
  	acl_base = (char *)pdacl;
++<<<<<<< HEAD:fs/cifs/cifsacl.c
 +	size = sizeof(struct cifs_acl);
 +	src_num_aces = le32_to_cpu(pdacl->num_aces);
++=======
+ 	size = sizeof(struct smb_acl);
+ 	src_num_aces = le16_to_cpu(pdacl->num_aces);
++>>>>>>> 62e7dd0a39c2 (smb: common: change the data type of num_aces to le16):fs/smb/client/cifsacl.c
  
  	/* Retain old ACEs which we can retain */
  	for (i = 0; i < src_num_aces; ++i) {
@@@ -1282,10 -1293,10 +1304,10 @@@ static int build_sec_desc(struct cifs_n
  			dacloffset ? dacl_ptr->revision : cpu_to_le16(ACL_REVISION);
  
  		ndacl_ptr->size = cpu_to_le16(0);
- 		ndacl_ptr->num_aces = cpu_to_le32(0);
+ 		ndacl_ptr->num_aces = cpu_to_le16(0);
  
  		rc = set_chmod_dacl(dacl_ptr, ndacl_ptr, owner_sid_ptr, group_sid_ptr,
 -				    pnmode, mode_from_sid, posix);
 +				    pnmode, mode_from_sid);
  
  		sidsoffset = ndacloffset + le16_to_cpu(ndacl_ptr->size);
  		/* copy the non-dacl portion of secdesc */
@@@ -1617,21 -1638,22 +1639,25 @@@ id_mode_to_cifs_acl(struct inode *inode
  		id_from_sid = false;
  
  	/* Potentially, five new ACEs can be added to the ACL for U,G,O mapping */
 +	nsecdesclen = secdesclen;
  	if (pnmode && *pnmode != NO_CHANGE_64) { /* chmod */
 -		if (posix)
 -			nsecdesclen = 1 * sizeof(struct smb_ace);
 -		else if (mode_from_sid)
 -			nsecdesclen = secdesclen + (2 * sizeof(struct smb_ace));
 +		if (mode_from_sid)
 +			nsecdesclen += 2 * sizeof(struct cifs_ace);
  		else /* cifsacl */
 -			nsecdesclen = secdesclen + (5 * sizeof(struct smb_ace));
 +			nsecdesclen += 5 * sizeof(struct cifs_ace);
  	} else { /* chown */
  		/* When ownership changes, changes new owner sid length could be different */
 -		nsecdesclen = sizeof(struct smb_ntsd) + (sizeof(struct smb_sid) * 2);
 +		nsecdesclen = sizeof(struct cifs_ntsd) + (sizeof(struct cifs_sid) * 2);
  		dacloffset = le32_to_cpu(pntsd->dacloffset);
  		if (dacloffset) {
 -			dacl_ptr = (struct smb_acl *)((char *)pntsd + dacloffset);
 +			dacl_ptr = (struct cifs_acl *)((char *)pntsd + dacloffset);
  			if (mode_from_sid)
  				nsecdesclen +=
++<<<<<<< HEAD:fs/cifs/cifsacl.c
 +					le32_to_cpu(dacl_ptr->num_aces) * sizeof(struct cifs_ace);
++=======
+ 					le16_to_cpu(dacl_ptr->num_aces) * sizeof(struct smb_ace);
++>>>>>>> 62e7dd0a39c2 (smb: common: change the data type of num_aces to le16):fs/smb/client/cifsacl.c
  			else /* cifsacl */
  				nsecdesclen += le16_to_cpu(dacl_ptr->size);
  		}
* Unmerged path fs/smb/common/smbacl.h
* Unmerged path fs/smb/server/smbacl.c
* Unmerged path fs/smb/server/smbacl.h
* Unmerged path fs/cifs/cifsacl.c
* Unmerged path fs/smb/common/smbacl.h
* Unmerged path fs/smb/server/smbacl.c
* Unmerged path fs/smb/server/smbacl.h
