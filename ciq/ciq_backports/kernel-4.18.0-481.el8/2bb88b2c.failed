net: devlink: remove redundant net_eq() check from sb_pool_get_dumpit()

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-481.el8
commit-author Jiri Pirko <jiri@nvidia.com>
commit 2bb88b2c4f7334bd91c734f3983492a133250edb
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-481.el8/2bb88b2c.failed

The net_eq() check is already performed inside
devlinks_xa_for_each_registered_get() helper, so remove the redundant
appearance.

	Signed-off-by: Jiri Pirko <jiri@nvidia.com>
Link: https://lore.kernel.org/r/20220727055912.568391-1-jiri@resnulli.us
	Signed-off-by: Jakub Kicinski <kuba@kernel.org>
(cherry picked from commit 2bb88b2c4f7334bd91c734f3983492a133250edb)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/core/devlink.c
diff --cc net/core/devlink.c
index 571e4c3d21fd,ca4c9939d569..000000000000
--- a/net/core/devlink.c
+++ b/net/core/devlink.c
@@@ -2196,12 -2641,8 +2196,17 @@@ static int devlink_nl_cmd_sb_pool_get_d
  	int err = 0;
  
  	mutex_lock(&devlink_mutex);
++<<<<<<< HEAD
 +	xa_for_each_marked(&devlinks, index, devlink, DEVLINK_REGISTERED) {
 +		if (!devlink_try_get(devlink))
 +			continue;
 +
 +		if (!net_eq(devlink_net(devlink), sock_net(msg->sk)) ||
 +		    !devlink->ops->sb_pool_get)
++=======
+ 	devlinks_xa_for_each_registered_get(sock_net(msg->sk), index, devlink) {
+ 		if (!devlink->ops->sb_pool_get)
++>>>>>>> 2bb88b2c4f73 (net: devlink: remove redundant net_eq() check from sb_pool_get_dumpit())
  			goto retry;
  
  		devl_lock(devlink);
* Unmerged path net/core/devlink.c
