netfilter: nft_tproxy: restrict to prerouting hook

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-481.el8
commit-author Florian Westphal <fw@strlen.de>
commit 18bbc3213383a82b05383827f4b1b882e3f0a5a5
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-481.el8/18bbc321.failed

TPROXY is only allowed from prerouting, but nft_tproxy doesn't check this.
This fixes a crash (null dereference) when using tproxy from e.g. output.

Fixes: 4ed8eb6570a4 ("netfilter: nf_tables: Add native tproxy support")
	Reported-by: Shell Chen <xierch@gmail.com>
	Signed-off-by: Florian Westphal <fw@strlen.de>
(cherry picked from commit 18bbc3213383a82b05383827f4b1b882e3f0a5a5)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/netfilter/nft_tproxy.c
diff --cc net/netfilter/nft_tproxy.c
index b97ab1198b03,62da25ad264b..000000000000
--- a/net/netfilter/nft_tproxy.c
+++ b/net/netfilter/nft_tproxy.c
@@@ -295,7 -325,10 +302,12 @@@ static const struct nft_expr_ops nft_tp
  	.size		= NFT_EXPR_SIZE(sizeof(struct nft_tproxy)),
  	.eval		= nft_tproxy_eval,
  	.init		= nft_tproxy_init,
 -	.destroy	= nft_tproxy_destroy,
  	.dump		= nft_tproxy_dump,
++<<<<<<< HEAD
++=======
+ 	.reduce		= NFT_REDUCE_READONLY,
+ 	.validate	= nft_tproxy_validate,
++>>>>>>> 18bbc3213383 (netfilter: nft_tproxy: restrict to prerouting hook)
  };
  
  static struct nft_expr_type nft_tproxy_type __read_mostly = {
* Unmerged path net/netfilter/nft_tproxy.c
