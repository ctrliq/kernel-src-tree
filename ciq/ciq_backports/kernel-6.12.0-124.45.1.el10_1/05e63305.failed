sched_ext: Fix scx_kick_pseqs corruption on concurrent scheduler loads

jira KERNEL-755
Rebuild_History Non-Buildable kernel-6.12.0-124.45.1.el10_1
commit-author Andrea Righi <arighi@nvidia.com>
commit 05e63305c85c88141500f0a2fb02afcfba9396e1
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-6.12.0-124.45.1.el10_1/05e63305.failed

If we load a BPF scheduler while another scheduler is already running,
alloc_kick_pseqs() would be called again, overwriting the previously
allocated arrays.

Fix by moving the alloc_kick_pseqs() call after the scx_enable_state()
check, ensuring that the arrays are only allocated when a scheduler can
actually be loaded.

Fixes: 14c1da3895a11 ("sched_ext: Allocate scx_kick_cpus_pnt_seqs lazily using kvzalloc()")
	Signed-off-by: Andrea Righi <arighi@nvidia.com>
	Signed-off-by: Tejun Heo <tj@kernel.org>
(cherry picked from commit 05e63305c85c88141500f0a2fb02afcfba9396e1)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	kernel/sched/ext.c
diff --cc kernel/sched/ext.c
index e1daa0de872a,12c9c3595692..000000000000
--- a/kernel/sched/ext.c
+++ b/kernel/sched/ext.c
@@@ -5562,65 -4575,21 +5562,80 @@@ static int scx_ops_enable(struct sched_
  		return -EINVAL;
  	}
  
 -	mutex_lock(&scx_enable_mutex);
 +	mutex_lock(&scx_ops_enable_mutex);
 +
++<<<<<<< HEAD
 +	if (!scx_ops_helper) {
 +		WRITE_ONCE(scx_ops_helper,
 +			   scx_create_rt_helper("sched_ext_ops_helper"));
 +		if (!scx_ops_helper) {
 +			ret = -ENOMEM;
 +			goto err_unlock;
 +		}
 +	}
 +
 +	if (!global_dsqs) {
 +		struct scx_dispatch_q **dsqs;
  
 +		dsqs = kcalloc(nr_node_ids, sizeof(dsqs[0]), GFP_KERNEL);
 +		if (!dsqs) {
 +			ret = -ENOMEM;
 +			goto err_unlock;
 +		}
 +
 +		for_each_node_state(node, N_POSSIBLE) {
 +			struct scx_dispatch_q *dsq;
 +
 +			dsq = kzalloc_node(sizeof(*dsq), GFP_KERNEL, node);
 +			if (!dsq) {
 +				for_each_node_state(node, N_POSSIBLE)
 +					kfree(dsqs[node]);
 +				kfree(dsqs);
 +				ret = -ENOMEM;
 +				goto err_unlock;
 +			}
 +
 +			init_dsq(dsq, SCX_DSQ_GLOBAL);
 +			dsqs[node] = dsq;
 +		}
 +
 +		global_dsqs = dsqs;
 +	}
 +
 +	if (scx_ops_enable_state() != SCX_OPS_DISABLED) {
++=======
+ 	if (scx_enable_state() != SCX_DISABLED) {
++>>>>>>> 05e63305c85c (sched_ext: Fix scx_kick_pseqs corruption on concurrent scheduler loads)
  		ret = -EBUSY;
  		goto err_unlock;
  	}
  
++<<<<<<< HEAD
 +	scx_root_kobj = kzalloc(sizeof(*scx_root_kobj), GFP_KERNEL);
 +	if (!scx_root_kobj) {
 +		ret = -ENOMEM;
 +		goto err_unlock;
 +	}
 +
 +	scx_root_kobj->kset = scx_kset;
 +	ret = kobject_init_and_add(scx_root_kobj, &scx_ktype, NULL, "root");
 +	if (ret < 0)
 +		goto err;
 +
 +	scx_exit_info = alloc_exit_info(ops->exit_dump_len);
 +	if (!scx_exit_info) {
 +		ret = -ENOMEM;
 +		goto err_del;
++=======
+ 	ret = alloc_kick_pseqs();
+ 	if (ret)
+ 		goto err_unlock;
+ 
+ 	sch = scx_alloc_and_add_sched(ops);
+ 	if (IS_ERR(sch)) {
+ 		ret = PTR_ERR(sch);
+ 		goto err_free_pseqs;
++>>>>>>> 05e63305c85c (sched_ext: Fix scx_kick_pseqs corruption on concurrent scheduler loads)
  	}
  
  	/*
* Unmerged path kernel/sched/ext.c
