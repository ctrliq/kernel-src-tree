netfilter: conntrack: Fix gre tunneling over ipv6

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-519.el8
commit-author Ludovic Senecaux <linuxludo@free.fr>
commit 8b2030b4305951f44afef80225f1475618e25a73
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-519.el8/8b2030b4.failed

This fix permits gre connections to be tracked within ip6tables rules

	Signed-off-by: Ludovic Senecaux <linuxludo@free.fr>
	Acked-by: Florian Westphal <fw@strlen.de>
	Signed-off-by: Pablo Neira Ayuso <pablo@netfilter.org>
(cherry picked from commit 8b2030b4305951f44afef80225f1475618e25a73)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/netfilter/nf_conntrack_proto_gre.c
diff --cc net/netfilter/nf_conntrack_proto_gre.c
index 7740299a4824,db11e403d818..000000000000
--- a/net/netfilter/nf_conntrack_proto_gre.c
+++ b/net/netfilter/nf_conntrack_proto_gre.c
@@@ -219,11 -218,6 +219,14 @@@ int nf_conntrack_gre_packet(struct nf_c
  			    enum ip_conntrack_info ctinfo,
  			    const struct nf_hook_state *state)
  {
++<<<<<<< HEAD
 +	unsigned long status;
 +
 +	if (state->pf != NFPROTO_IPV4)
 +		return -NF_ACCEPT;
 +
++=======
++>>>>>>> 8b2030b43059 (netfilter: conntrack: Fix gre tunneling over ipv6)
  	if (!nf_ct_is_confirmed(ct)) {
  		unsigned int *timeouts = nf_ct_timeout_lookup(ct);
  
* Unmerged path net/netfilter/nf_conntrack_proto_gre.c
