bpf, udp: Remove some pointless comments

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-365.el8
commit-author Cong Wang <cong.wang@bytedance.com>
commit 928dc406802dc4547b5ef84c3075fe144d4cbcf1
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-365.el8/928dc406.failed

These comments in udp_bpf_update_proto() are copied from the
original TCP code and apparently do not apply to UDP. Just
remove them.

	Reported-by: Jakub Sitnicki <jakub@cloudflare.com>
	Signed-off-by: Cong Wang <cong.wang@bytedance.com>
	Signed-off-by: Daniel Borkmann <daniel@iogearbox.net>
	Acked-by: John Fastabend <john.fastabend@gmail.com>
Link: https://lore.kernel.org/bpf/20210403052715.13854-1-xiyou.wangcong@gmail.com
(cherry picked from commit 928dc406802dc4547b5ef84c3075fe144d4cbcf1)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/ipv4/udp_bpf.c
diff --cc net/ipv4/udp_bpf.c
index 51c5328e9dc1,4a7e38c5d842..000000000000
--- a/net/ipv4/udp_bpf.c
+++ b/net/ipv4/udp_bpf.c
@@@ -101,14 -101,23 +101,29 @@@ static int __init udp_bpf_v4_build_prot
  	udp_bpf_rebuild_protos(&udp_bpf_prots[UDP_BPF_IPV4], &udp_prot);
  	return 0;
  }
 -core_initcall(udp_bpf_v4_build_proto);
 +late_initcall(udp_bpf_v4_build_proto);
  
 -int udp_bpf_update_proto(struct sock *sk, bool restore)
 +struct proto *udp_bpf_get_proto(struct sock *sk, struct sk_psock *psock)
  {
  	int family = sk->sk_family == AF_INET ? UDP_BPF_IPV4 : UDP_BPF_IPV6;
++<<<<<<< HEAD
++=======
+ 	struct sk_psock *psock = sk_psock(sk);
+ 
+ 	if (restore) {
+ 		sk->sk_write_space = psock->saved_write_space;
+ 		WRITE_ONCE(sk->sk_prot, psock->sk_proto);
+ 		return 0;
+ 	}
++>>>>>>> 928dc406802d (bpf, udp: Remove some pointless comments)
  
  	if (sk->sk_family == AF_INET6)
  		udp_bpf_check_v6_needs_rebuild(psock->sk_proto);
  
++<<<<<<< HEAD
 +	return &udp_bpf_prots[family];
++=======
+ 	WRITE_ONCE(sk->sk_prot, &udp_bpf_prots[family]);
+ 	return 0;
++>>>>>>> 928dc406802d (bpf, udp: Remove some pointless comments)
  }
 -EXPORT_SYMBOL_GPL(udp_bpf_update_proto);
* Unmerged path net/ipv4/udp_bpf.c
