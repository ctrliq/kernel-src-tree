drm/i915/dp: fix DG1 and RKL max source rates

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-365.el8
commit-author Jani Nikula <jani.nikula@intel.com>
commit bc41f059a080e487c235b539f1e5cdbf605aba9f
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-365.el8/bc41f059.failed

Combo phy is limited to 5.4 GHz on low-voltage SKUs, but both eDP and DP
can do 8.1 GHz on combo phy.

Bspec: 49182, 49205, 49202

	Cc: Imre Deak <imre.deak@intel.com>
	Signed-off-by: Jani Nikula <jani.nikula@intel.com>
	Signed-off-by: Animesh Manna <animesh.manna@intel.com>
	Reviewed-by: Imre Deak <imre.deak@intel.com>
	Signed-off-by: Imre Deak <imre.deak@intel.com>
Link: https://patchwork.freedesktop.org/patch/msgid/20210901160402.24816-5-animesh.manna@intel.com
(cherry picked from commit bc41f059a080e487c235b539f1e5cdbf605aba9f)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/gpu/drm/i915/display/intel_dp.c
diff --cc drivers/gpu/drm/i915/display/intel_dp.c
index aefe3cd87e91,3fe1158706d5..000000000000
--- a/drivers/gpu/drm/i915/display/intel_dp.c
+++ b/drivers/gpu/drm/i915/display/intel_dp.c
@@@ -353,11 -367,13 +365,21 @@@ intel_dp_set_source_rates(struct intel_
  	drm_WARN_ON(&dev_priv->drm,
  		    intel_dp->source_rates || intel_dp->num_source_rates);
  
++<<<<<<< HEAD
 +	if (INTEL_GEN(dev_priv) >= 10) {
 +		source_rates = cnl_rates;
 +		size = ARRAY_SIZE(cnl_rates);
 +		if (IS_GEN(dev_priv, 10))
 +			max_rate = cnl_max_source_rate(intel_dp);
++=======
+ 	if (DISPLAY_VER(dev_priv) >= 11) {
+ 		source_rates = icl_rates;
+ 		size = ARRAY_SIZE(icl_rates);
+ 		if (IS_DG2(dev_priv))
+ 			max_rate = dg2_max_source_rate(intel_dp);
+ 		else if (IS_DG1(dev_priv) || IS_ROCKETLAKE(dev_priv))
+ 			max_rate = dg1_max_source_rate(intel_dp);
++>>>>>>> bc41f059a080 (drm/i915/dp: fix DG1 and RKL max source rates)
  		else if (IS_JSL_EHL(dev_priv))
  			max_rate = ehl_max_source_rate(intel_dp);
  		else
* Unmerged path drivers/gpu/drm/i915/display/intel_dp.c
