ACPI: power: Use dev_dbg() to print some messages

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-365.el8
commit-author Rafael J. Wysocki <rafael.j.wysocki@intel.com>
commit db9b6d87a8d4552c691c9f008a233985f41a9e4d
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-365.el8/db9b6d87.failed

The messages printed by acpi_resume_power_resources() and
acpi_turn_off_unused_power_resources() are not important enough to be
printed with pr_info(), so use dev_dbg() instead of it to get rid of
some noise in the kernel log.

	Signed-off-by: Rafael J. Wysocki <rafael.j.wysocki@intel.com>
	Acked-by: Borislav Petkov <bp@suse.de>
(cherry picked from commit db9b6d87a8d4552c691c9f008a233985f41a9e4d)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/acpi/power.c
diff --cc drivers/acpi/power.c
index 3fdfde3a851a,eba7785047ca..000000000000
--- a/drivers/acpi/power.c
+++ b/drivers/acpi/power.c
@@@ -1069,7 -1027,16 +1069,20 @@@ void acpi_turn_off_unused_power_resourc
  	list_for_each_entry_reverse(resource, &acpi_power_resource_list, list_node) {
  		mutex_lock(&resource->resource_lock);
  
++<<<<<<< HEAD
 +		acpi_power_turn_off_if_unused(resource, init);
++=======
+ 		/*
+ 		 * Turn off power resources in an unknown state too, because the
+ 		 * platform firmware on some system expects the OS to turn off
+ 		 * power resources without any users unconditionally.
+ 		 */
+ 		if (!resource->ref_count &&
+ 		    resource->state != ACPI_POWER_RESOURCE_STATE_OFF) {
+ 			dev_dbg(&resource->device.dev, "Turning OFF\n");
+ 			__acpi_power_off(resource);
+ 		}
++>>>>>>> db9b6d87a8d4 (ACPI: power: Use dev_dbg() to print some messages)
  
  		mutex_unlock(&resource->resource_lock);
  	}
* Unmerged path drivers/acpi/power.c
