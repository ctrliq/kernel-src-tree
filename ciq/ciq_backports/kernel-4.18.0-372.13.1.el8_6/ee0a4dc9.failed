Revert "netfilter: conntrack: tag conntracks picked up in local out hook"

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-372.13.1.el8_6
commit-author Florian Westphal <fw@strlen.de>
commit ee0a4dc9f317fb9a97f20037d219802ca8de939b
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-372.13.1.el8_6/ee0a4dc9.failed

This was a prerequisite for the ill-fated
"netfilter: nat: force port remap to prevent shadowing well-known ports".

As this has been reverted, this change can be backed out too.

	Signed-off-by: Florian Westphal <fw@strlen.de>
(cherry picked from commit ee0a4dc9f317fb9a97f20037d219802ca8de939b)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	include/net/netfilter/nf_conntrack.h
diff --cc include/net/netfilter/nf_conntrack.h
index 6ee25e6d1eff,b08b70989d2c..000000000000
--- a/include/net/netfilter/nf_conntrack.h
+++ b/include/net/netfilter/nf_conntrack.h
@@@ -81,10 -96,7 +81,14 @@@ struct nf_conn 
  	/* Have we seen traffic both ways yet? (bitset) */
  	unsigned long status;
  
++<<<<<<< HEAD
 +	/* jiffies32 when this ct is considered dead */
 +	u32 timeout;
 +	u16		local_origin:1;
 +
++=======
+ 	u16		cpu;
++>>>>>>> ee0a4dc9f317 (Revert "netfilter: conntrack: tag conntracks picked up in local out hook")
  	possible_net_t ct_net;
  
  #if IS_ENABLED(CONFIG_NF_NAT)
* Unmerged path include/net/netfilter/nf_conntrack.h
diff --git a/net/netfilter/nf_conntrack_core.c b/net/netfilter/nf_conntrack_core.c
index fec53c37620d..e25b144a9f56 100644
--- a/net/netfilter/nf_conntrack_core.c
+++ b/net/netfilter/nf_conntrack_core.c
@@ -1668,9 +1668,6 @@ resolve_normal_ct(struct nf_conn *tmpl,
 			return 0;
 		if (IS_ERR(h))
 			return PTR_ERR(h);
-
-		ct = nf_ct_tuplehash_to_ctrack(h);
-		ct->local_origin = state->hook == NF_INET_LOCAL_OUT;
 	}
 	ct = nf_ct_tuplehash_to_ctrack(h);
 
