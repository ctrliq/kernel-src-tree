cifs: fix SMB1 error path in cifs_get_file_info_unix

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-553.16.1.el8_10
commit-author Steve French <stfrench@microsoft.com>
commit e39df24169a2ceb0d359eb3a05ff982711f2eb32
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-553.16.1.el8_10/e39df241.failed

We were trying to fill in uninitialized file attributes in the error case.

Addresses-Coverity: 139689 ("Uninitialized variables")
	Signed-off-by: Steve French <stfrench@microsoft.com>
(cherry picked from commit e39df24169a2ceb0d359eb3a05ff982711f2eb32)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/cifs/inode.c
diff --cc fs/cifs/inode.c
index 3c6cd383548e,b96b253e7635..000000000000
--- a/fs/cifs/inode.c
+++ b/fs/cifs/inode.c
@@@ -360,9 -355,12 +360,16 @@@ cifs_get_file_info_unix(struct file *fi
  	} else if (rc == -EREMOTE) {
  		cifs_create_dfs_fattr(&fattr, inode->i_sb);
  		rc = 0;
- 	}
+ 	} else
+ 		goto cifs_gfiunix_out;
  
++<<<<<<< HEAD
 +	cifs_fattr_to_inode(inode, &fattr);
++=======
+ 	rc = cifs_fattr_to_inode(inode, &fattr);
+ 
+ cifs_gfiunix_out:
++>>>>>>> e39df24169a2 (cifs: fix SMB1 error path in cifs_get_file_info_unix)
  	free_xid(xid);
  	return rc;
  }
* Unmerged path fs/cifs/inode.c
