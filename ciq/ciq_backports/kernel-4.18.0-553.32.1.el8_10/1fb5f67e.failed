gfs2: Rename GLF_FROZEN to GLF_HAVE_FROZEN_REPLY

jira LE-2169
Rebuild_History Non-Buildable kernel-4.18.0-553.32.1.el8_10
commit-author Andreas Gruenbacher <agruenba@redhat.com>
commit 1fb5f67e21805333daca4b0c96416254de96400e
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-553.32.1.el8_10/1fb5f67e.failed

The GLF_FROZEN flag indicates that a reply to a DLM locking request has
been received, but should not be processed at this time.  To clarify
that meaning, rename the flag to GLF_HAVE_FROZEN_REPLY.

	Signed-off-by: Andreas Gruenbacher <agruenba@redhat.com>
(cherry picked from commit 1fb5f67e21805333daca4b0c96416254de96400e)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/gfs2/glock.c
diff --cc fs/gfs2/glock.c
index a9684efd290d,bac7078e4870..000000000000
--- a/fs/gfs2/glock.c
+++ b/fs/gfs2/glock.c
@@@ -1542,10 -1641,10 +1542,15 @@@ int gfs2_glock_nq(struct gfs2_holder *g
  	spin_lock(&gl->gl_lockref.lock);
  	add_to_queue(gh);
  	if (unlikely((LM_FLAG_NOEXP & gh->gh_flags) &&
++<<<<<<< HEAD
 +		     test_and_clear_bit(GLF_FROZEN, &gl->gl_flags))) {
 +		set_bit(GLF_REPLY_PENDING, &gl->gl_flags);
++=======
+ 		     test_and_clear_bit(GLF_HAVE_FROZEN_REPLY, &gl->gl_flags))) {
+ 		set_bit(GLF_HAVE_REPLY, &gl->gl_flags);
++>>>>>>> 1fb5f67e2180 (gfs2: Rename GLF_FROZEN to GLF_HAVE_FROZEN_REPLY)
  		gl->gl_lockref.count++;
 -		gfs2_glock_queue_work(gl, 0);
 +		__gfs2_glock_queue_work(gl, 0);
  	}
  	run_queue(gl, 1);
  	spin_unlock(&gl->gl_lockref.lock);
* Unmerged path fs/gfs2/glock.c
diff --git a/fs/gfs2/incore.h b/fs/gfs2/incore.h
index bce238196547..f7634194b5dd 100644
--- a/fs/gfs2/incore.h
+++ b/fs/gfs2/incore.h
@@ -327,7 +327,7 @@ enum {
 	GLF_INVALIDATE_IN_PROGRESS	= 8,
 	GLF_REPLY_PENDING		= 9,
 	GLF_INITIAL			= 10,
-	GLF_FROZEN			= 11,
+	GLF_HAVE_FROZEN_REPLY		= 11,
 	GLF_INSTANTIATE_IN_PROG		= 12, /* instantiate happening now */
 	GLF_LRU				= 13,
 	GLF_OBJECT			= 14, /* Used only for tracing */
diff --git a/fs/gfs2/trace_gfs2.h b/fs/gfs2/trace_gfs2.h
index a5deb9f86831..87b0a738cc81 100644
--- a/fs/gfs2/trace_gfs2.h
+++ b/fs/gfs2/trace_gfs2.h
@@ -55,7 +55,7 @@
 	{(1UL << GLF_INVALIDATE_IN_PROGRESS),	"i" },		\
 	{(1UL << GLF_REPLY_PENDING),		"r" },		\
 	{(1UL << GLF_INITIAL),			"I" },		\
-	{(1UL << GLF_FROZEN),			"F" },		\
+	{(1UL << GLF_HAVE_FROZEN_REPLY),	"F" },		\
 	{(1UL << GLF_LRU),			"L" },		\
 	{(1UL << GLF_OBJECT),			"o" },		\
 	{(1UL << GLF_BLOCKING),			"b" })
