dm cache: use dm_submit_bio_remap

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-408.el8
commit-author Mike Snitzer <snitzer@redhat.com>
commit 69596f555b81942b41b10669e5faefc6ce883abb
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-408.el8/69596f55.failed

	Signed-off-by: Mike Snitzer <snitzer@redhat.com>
(cherry picked from commit 69596f555b81942b41b10669e5faefc6ce883abb)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/md/dm-cache-target.c
diff --cc drivers/md/dm-cache-target.c
index 9160c6d318d0,780a61bc6cc0..000000000000
--- a/drivers/md/dm-cache-target.c
+++ b/drivers/md/dm-cache-target.c
@@@ -805,7 -803,7 +805,11 @@@ static void accounted_complete(struct c
  static void accounted_request(struct cache *cache, struct bio *bio)
  {
  	accounted_begin(cache, bio);
++<<<<<<< HEAD
 +	generic_make_request(bio);
++=======
+ 	dm_submit_bio_remap(bio, NULL);
++>>>>>>> 69596f555b81 (dm cache: use dm_submit_bio_remap)
  }
  
  static void issue_op(struct bio *bio, void *context)
@@@ -1710,7 -1708,7 +1714,11 @@@ static bool process_bio(struct cache *c
  	bool commit_needed;
  
  	if (map_bio(cache, bio, get_bio_block(cache, bio), &commit_needed) == DM_MAPIO_REMAPPED)
++<<<<<<< HEAD
 +		generic_make_request(bio);
++=======
+ 		dm_submit_bio_remap(bio, NULL);
++>>>>>>> 69596f555b81 (dm cache: use dm_submit_bio_remap)
  
  	return commit_needed;
  }
@@@ -1776,7 -1774,7 +1784,11 @@@ static bool process_discard_bio(struct 
  
  	if (cache->features.discard_passdown) {
  		remap_to_origin(cache, bio);
++<<<<<<< HEAD
 +		generic_make_request(bio);
++=======
+ 		dm_submit_bio_remap(bio, NULL);
++>>>>>>> 69596f555b81 (dm cache: use dm_submit_bio_remap)
  	} else
  		bio_endio(bio);
  
* Unmerged path drivers/md/dm-cache-target.c
