audit: remove trailing spaces and tabs

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-408.el8
commit-author Zhen Lei <thunder.leizhen@huawei.com>
commit 6ddb5680085a3eefe0c6267e3514060045a13c95
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-408.el8/6ddb5680.failed

Run the following command to find and remove the trailing spaces and tabs:

sed -r -i 's/[ \t]+$//' <audit_files>

The files to be checked are as follows:
kernel/audit*
include/linux/audit.h
include/uapi/linux/audit.h

	Signed-off-by: Zhen Lei <thunder.leizhen@huawei.com>
	Acked-by: Richard Guy Briggs <rgb@redhat.com>
	Signed-off-by: Paul Moore <paul@paul-moore.com>
(cherry picked from commit 6ddb5680085a3eefe0c6267e3514060045a13c95)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	kernel/audit.h
diff --cc kernel/audit.h
index b6a48746db57,b565ea16c0a5..000000000000
--- a/kernel/audit.h
+++ b/kernel/audit.h
@@@ -1,4 -1,5 +1,9 @@@
++<<<<<<< HEAD
 +/* audit -- definition of audit_context structure and supporting types 
++=======
+ /* SPDX-License-Identifier: GPL-2.0-or-later */
+ /* audit -- definition of audit_context structure and supporting types
++>>>>>>> 6ddb5680085a (audit: remove trailing spaces and tabs)
   *
   * Copyright 2003-2004 Red Hat, Inc.
   * Copyright 2005 Hewlett-Packard Development Company, L.P.
diff --git a/include/uapi/linux/audit.h b/include/uapi/linux/audit.h
index 706508ea7d25..2aa89c3e4808 100644
--- a/include/uapi/linux/audit.h
+++ b/include/uapi/linux/audit.h
@@ -48,7 +48,7 @@
  * 2500 - 2999 future user space (maybe integrity labels and related events)
  *
  * Messages from 1000-1199 are bi-directional. 1200-1299 & 2100 - 2999 are
- * exclusively user space. 1300-2099 is kernel --> user space 
+ * exclusively user space. 1300-2099 is kernel --> user space
  * communication.
  */
 #define AUDIT_GET		1000	/* Get status */
@@ -78,7 +78,7 @@
 #define AUDIT_LAST_USER_MSG	1199
 #define AUDIT_FIRST_USER_MSG2	2100	/* More user space messages */
 #define AUDIT_LAST_USER_MSG2	2999
- 
+
 #define AUDIT_DAEMON_START      1200    /* Daemon startup record */
 #define AUDIT_DAEMON_END        1201    /* Daemon normal stop record */
 #define AUDIT_DAEMON_ABORT      1202    /* Daemon error stop record */
* Unmerged path kernel/audit.h
diff --git a/kernel/auditsc.c b/kernel/auditsc.c
index 279cdbd78b94..9bbd6332a205 100644
--- a/kernel/auditsc.c
+++ b/kernel/auditsc.c
@@ -346,13 +346,13 @@ static int audit_compare_uid(kuid_t uid,
 {
 	struct audit_names *n;
 	int rc;
- 
+
 	if (name) {
 		rc = audit_uid_comparator(uid, f->op, name->uid);
 		if (rc)
 			return rc;
 	}
- 
+
 	if (ctx) {
 		list_for_each_entry(n, &ctx->names_list, list) {
 			rc = audit_uid_comparator(uid, f->op, n->uid);
@@ -370,13 +370,13 @@ static int audit_compare_gid(kgid_t gid,
 {
 	struct audit_names *n;
 	int rc;
- 
+
 	if (name) {
 		rc = audit_gid_comparator(gid, f->op, name->gid);
 		if (rc)
 			return rc;
 	}
- 
+
 	if (ctx) {
 		list_for_each_entry(n, &ctx->names_list, list) {
 			rc = audit_gid_comparator(gid, f->op, n->gid);
