ionic: simplify rx skb alloc

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-326.el8
commit-author Shannon Nelson <snelson@pensando.io>
commit 89e572e7369fd9ad9c6b07b023bc6e5e9abcd4de
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-326.el8/89e572e7.failed

Remove an unnecessary layer over rx skb allocation.

	Signed-off-by: Shannon Nelson <snelson@pensando.io>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 89e572e7369fd9ad9c6b07b023bc6e5e9abcd4de)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/pensando/ionic/ionic_txrx.c
diff --cc drivers/net/ethernet/pensando/ionic/ionic_txrx.c
index 8369e1e143a1,cd2540ff9251..000000000000
--- a/drivers/net/ethernet/pensando/ionic/ionic_txrx.c
+++ b/drivers/net/ethernet/pensando/ionic/ionic_txrx.c
@@@ -76,13 -44,11 +44,16 @@@ static void ionic_rx_buf_reset(struct i
  static int ionic_rx_page_alloc(struct ionic_queue *q,
  			       struct ionic_buf_info *buf_info)
  {
- 	struct ionic_lif *lif = q->lif;
+ 	struct net_device *netdev = q->lif->netdev;
  	struct ionic_rx_stats *stats;
- 	struct net_device *netdev;
  	struct device *dev;
  
++<<<<<<< HEAD
 +	netdev = lif->netdev;
 +	dev = lif->ionic->dev;
++=======
+ 	dev = q->dev;
++>>>>>>> 89e572e7369f (ionic: simplify rx skb alloc)
  	stats = q_to_rx_stats(q);
  
  	if (unlikely(!buf_info)) {
@@@ -162,8 -128,10 +133,15 @@@ static struct sk_buff *ionic_rx_frags(s
  				      struct ionic_cq_info *cq_info)
  {
  	struct ionic_rxq_comp *comp = cq_info->cq_desc;
++<<<<<<< HEAD
 +	struct device *dev = q->lif->ionic->dev;
 +	struct ionic_buf_info *buf_info;
++=======
+ 	struct net_device *netdev = q->lif->netdev;
+ 	struct ionic_buf_info *buf_info;
+ 	struct ionic_rx_stats *stats;
+ 	struct device *dev = q->dev;
++>>>>>>> 89e572e7369f (ionic: simplify rx skb alloc)
  	struct sk_buff *skb;
  	unsigned int i;
  	u16 frag_len;
@@@ -218,8 -192,10 +202,15 @@@ static struct sk_buff *ionic_rx_copybre
  					  struct ionic_cq_info *cq_info)
  {
  	struct ionic_rxq_comp *comp = cq_info->cq_desc;
++<<<<<<< HEAD
 +	struct device *dev = q->lif->ionic->dev;
 +	struct ionic_buf_info *buf_info;
++=======
+ 	struct net_device *netdev = q->lif->netdev;
+ 	struct ionic_buf_info *buf_info;
+ 	struct ionic_rx_stats *stats;
+ 	struct device *dev = q->dev;
++>>>>>>> 89e572e7369f (ionic: simplify rx skb alloc)
  	struct sk_buff *skb;
  	u16 len;
  
* Unmerged path drivers/net/ethernet/pensando/ionic/ionic_txrx.c
