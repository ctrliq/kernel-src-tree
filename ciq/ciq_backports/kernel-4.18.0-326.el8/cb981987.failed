drm/qxl: fix monitors object vmap

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-326.el8
commit-author Gerd Hoffmann <kraxel@redhat.com>
commit cb981987b401ff18e4bd6fc08a91e187ac043da0
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-326.el8/cb981987.failed

Use the correct vmap variant.  We don't hold a reservation here,
so we can't use the _locked variant.  We can drop the pin because
qxl_bo_vmap will do that for us.

	Signed-off-by: Gerd Hoffmann <kraxel@redhat.com>
	Acked-by: Thomas Zimmermann <tzimmermann@suse.de>
Link: http://patchwork.freedesktop.org/patch/msgid/20210217123213.2199186-9-kraxel@redhat.com
(cherry picked from commit cb981987b401ff18e4bd6fc08a91e187ac043da0)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/gpu/drm/qxl/qxl_display.c
diff --cc drivers/gpu/drm/qxl/qxl_display.c
index 59ef1f0c985d,f106da917863..000000000000
--- a/drivers/gpu/drm/qxl/qxl_display.c
+++ b/drivers/gpu/drm/qxl/qxl_display.c
@@@ -1154,8 -1163,6 +1154,11 @@@ int qxl_create_monitors_object(struct q
  	if (ret)
  		return ret;
  
++<<<<<<< HEAD
 +	qxl_bo_kmap(qdev->monitors_config_bo, NULL);
 +
++=======
++>>>>>>> cb981987b401 (drm/qxl: fix monitors object vmap)
  	qdev->monitors_config = qdev->monitors_config_bo->kptr;
  	qdev->ram_header->monitors_config =
  		qxl_bo_physical_address(qdev, qdev->monitors_config_bo, 0);
@@@ -1180,8 -1187,7 +1183,12 @@@ int qxl_destroy_monitors_object(struct 
  	qdev->monitors_config = NULL;
  	qdev->ram_header->monitors_config = 0;
  
++<<<<<<< HEAD
 +	qxl_bo_kunmap(qdev->monitors_config_bo);
 +	ret = qxl_bo_unpin(qdev->monitors_config_bo);
++=======
+ 	ret = qxl_bo_vunmap(qdev->monitors_config_bo);
++>>>>>>> cb981987b401 (drm/qxl: fix monitors object vmap)
  	if (ret)
  		return ret;
  
* Unmerged path drivers/gpu/drm/qxl/qxl_display.c
