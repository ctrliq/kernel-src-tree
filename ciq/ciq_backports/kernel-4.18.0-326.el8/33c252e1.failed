ionic: fix up a couple of code style nits

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-326.el8
commit-author Shannon Nelson <snelson@pensando.io>
commit 33c252e1ba8b9ba00831bc520f7017c816429179
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-326.el8/33c252e1.failed

Clean up variable declarations.

	Signed-off-by: Shannon Nelson <snelson@pensando.io>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 33c252e1ba8b9ba00831bc520f7017c816429179)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/pensando/ionic/ionic_lif.c
diff --cc drivers/net/ethernet/pensando/ionic/ionic_lif.c
index ff3f4b510503,4e22e50922cd..000000000000
--- a/drivers/net/ethernet/pensando/ionic/ionic_lif.c
+++ b/drivers/net/ethernet/pensando/ionic/ionic_lif.c
@@@ -1719,11 -2015,14 +1719,16 @@@ static void ionic_txrx_free(struct ioni
  
  static int ionic_txrx_alloc(struct ionic_lif *lif)
  {
++<<<<<<< HEAD
 +	unsigned int sg_desc_sz;
 +	unsigned int flags;
 +	unsigned int i;
++=======
+ 	unsigned int comp_sz, desc_sz, num_desc, sg_desc_sz;
+ 	unsigned int flags, i;
++>>>>>>> 33c252e1ba8b (ionic: fix up a couple of code style nits)
  	int err = 0;
  
 -	num_desc = lif->ntxq_descs;
 -	desc_sz = sizeof(struct ionic_txq_desc);
 -	comp_sz = sizeof(struct ionic_txq_comp);
 -
  	if (lif->qtype_info[IONIC_QTYPE_TXQ].version >= 1 &&
  	    lif->qtype_info[IONIC_QTYPE_TXQ].sg_desc_sz ==
  					  sizeof(struct ionic_txq_sg_desc_v1))
@@@ -2243,12 -2583,11 +2248,18 @@@ static void ionic_swap_queues(struct io
  int ionic_reconfigure_queues(struct ionic_lif *lif,
  			     struct ionic_queue_params *qparam)
  {
++<<<<<<< HEAD
 +	struct ionic_qcq **tx_qcqs = NULL;
 +	struct ionic_qcq **rx_qcqs = NULL;
 +	unsigned int sg_desc_sz;
 +	unsigned int flags;
++=======
+ 	unsigned int comp_sz, desc_sz, num_desc, sg_desc_sz;
+ 	struct ionic_qcq **tx_qcqs = NULL;
+ 	struct ionic_qcq **rx_qcqs = NULL;
+ 	unsigned int flags, i;
++>>>>>>> 33c252e1ba8b (ionic: fix up a couple of code style nits)
  	int err = -ENOMEM;
- 	unsigned int i;
  
  	/* allocate temporary qcq arrays to hold new queue structs */
  	if (qparam->nxqs != lif->nxqs || qparam->ntxq_descs != lif->ntxq_descs) {
* Unmerged path drivers/net/ethernet/pensando/ionic/ionic_lif.c
