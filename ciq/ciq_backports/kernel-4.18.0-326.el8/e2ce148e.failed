ionic: check for valid tx_mode on SKBTX_HW_TSTAMP xmit

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-326.el8
commit-author Shannon Nelson <snelson@pensando.io>
commit e2ce148e948e7cf6267351ea407ac45a6b561a54
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-326.el8/e2ce148e.failed

Make sure the device is in a Tx offload mode before calling the
hwstamp offload xmit.

	Signed-off-by: Shannon Nelson <snelson@pensando.io>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit e2ce148e948e7cf6267351ea407ac45a6b561a54)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/pensando/ionic/ionic_txrx.c
diff --cc drivers/net/ethernet/pensando/ionic/ionic_txrx.c
index c718ad9db767,765050a5f7a8..000000000000
--- a/drivers/net/ethernet/pensando/ionic/ionic_txrx.c
+++ b/drivers/net/ethernet/pensando/ionic/ionic_txrx.c
@@@ -1198,6 -1232,10 +1198,13 @@@ netdev_tx_t ionic_start_xmit(struct sk_
  		return NETDEV_TX_OK;
  	}
  
++<<<<<<< HEAD
++=======
+ 	if (unlikely(skb_shinfo(skb)->tx_flags & SKBTX_HW_TSTAMP))
+ 		if (lif->hwstamp_txq && lif->phc->ts_config_tx_mode)
+ 			return ionic_start_hwstamp_xmit(skb, netdev);
+ 
++>>>>>>> e2ce148e948e (ionic: check for valid tx_mode on SKBTX_HW_TSTAMP xmit)
  	if (unlikely(queue_index >= lif->nxqs))
  		queue_index = 0;
  	q = &lif->txqcqs[queue_index]->q;
* Unmerged path drivers/net/ethernet/pensando/ionic/ionic_txrx.c
