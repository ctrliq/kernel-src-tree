ALSA: usb-audio: Correct document for snd_usb_endpoint_free_all()

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-326.el8
commit-author Takashi Iwai <tiwai@suse.de>
commit 036f90dd92bb0aac66fdeec8386401dd396c6079
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-326.el8/036f90dd.failed

The kerndoc comment for the new function snd_usb_endpoint_free_all()
had a typo wrt the argument name.  Fix it.

Fixes: 00272c61827e ("ALSA: usb-audio: Avoid unnecessary interface re-setup")
	Reported-by: Pierre-Louis Bossart <pierre-louis.bossart@linux.intel.com>
	Cc: <stable@vger.kernel.org>
Link: https://lore.kernel.org/r/20210205082837.6327-1-tiwai@suse.de
	Signed-off-by: Takashi Iwai <tiwai@suse.de>
(cherry picked from commit 036f90dd92bb0aac66fdeec8386401dd396c6079)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	sound/usb/endpoint.c
diff --cc sound/usb/endpoint.c
index 208ac2aff462,4d1c678a0d80..000000000000
--- a/sound/usb/endpoint.c
+++ b/sound/usb/endpoint.c
@@@ -1362,18 -1450,24 +1362,23 @@@ void snd_usb_endpoint_release(struct sn
  }
  
  /**
++<<<<<<< HEAD
 + * snd_usb_endpoint_free: Free the resources of an snd_usb_endpoint
++=======
+  * snd_usb_endpoint_free_all: Free the resources of an snd_usb_endpoint
+  * @chip: The chip
++>>>>>>> 036f90dd92bb (ALSA: usb-audio: Correct document for snd_usb_endpoint_free_all())
 + *
 + * @ep: the endpoint to free
   *
 - * This free all endpoints and those resources
 + * This free all resources of the given ep.
   */
 -void snd_usb_endpoint_free_all(struct snd_usb_audio *chip)
 +void snd_usb_endpoint_free(struct snd_usb_endpoint *ep)
  {
 -	struct snd_usb_endpoint *ep, *en;
 -	struct snd_usb_iface_ref *ip, *in;
 -
 -	list_for_each_entry_safe(ep, en, &chip->ep_list, list)
 -		kfree(ep);
 -
 -	list_for_each_entry_safe(ip, in, &chip->iface_ref_list, list)
 -		kfree(ip);
 +	kfree(ep);
  }
  
 -/*
 +/**
   * snd_usb_handle_sync_urb: parse an USB sync packet
   *
   * @ep: the endpoint to handle the packet
* Unmerged path sound/usb/endpoint.c
