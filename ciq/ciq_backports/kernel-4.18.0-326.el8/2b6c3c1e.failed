net/mlx5e: Fix error return code in mlx5e_tc_esw_init()

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-326.el8
commit-author Wei Yongjun <weiyongjun1@huawei.com>
commit 2b6c3c1e74e53f370074eb03769c9499aa30c55a
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-326.el8/2b6c3c1e.failed

Fix to return negative error code from the mlx5e_tc_tun_init() error
handling case instead of 0, as done elsewhere in this function.

This commit also using 0 instead of 'ret' when success since it is
always equal to 0.

Fixes: 8914add2c9e5 ("net/mlx5e: Handle FIB events to update tunnel endpoint device")
	Reported-by: Hulk Robot <hulkci@huawei.com>
	Signed-off-by: Wei Yongjun <weiyongjun1@huawei.com>
	Signed-off-by: Saeed Mahameed <saeedm@nvidia.com>
(cherry picked from commit 2b6c3c1e74e53f370074eb03769c9499aa30c55a)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/mellanox/mlx5/core/en_tc.c
diff --cc drivers/net/ethernet/mellanox/mlx5/core/en_tc.c
index 00c2a6fd4471,9f126054d371..000000000000
--- a/drivers/net/ethernet/mellanox/mlx5/core/en_tc.c
+++ b/drivers/net/ethernet/mellanox/mlx5/core/en_tc.c
@@@ -5281,8 -4749,16 +5281,18 @@@ int mlx5e_tc_esw_init(struct rhashtabl
  
  	lockdep_set_class(&tc_ht->mutex, &tc_ht_lock_key);
  
++<<<<<<< HEAD
 +	return err;
++=======
+ 	uplink_priv->encap = mlx5e_tc_tun_init(priv);
+ 	if (IS_ERR(uplink_priv->encap)) {
+ 		err = PTR_ERR(uplink_priv->encap);
+ 		goto err_register_fib_notifier;
+ 	}
+ 
+ 	return 0;
++>>>>>>> 2b6c3c1e74e5 (net/mlx5e: Fix error return code in mlx5e_tc_esw_init())
  
 -err_register_fib_notifier:
 -	rhashtable_destroy(tc_ht);
  err_ht_init:
  	mapping_destroy(uplink_priv->tunnel_enc_opts_mapping);
  err_enc_opts_mapping:
* Unmerged path drivers/net/ethernet/mellanox/mlx5/core/en_tc.c
