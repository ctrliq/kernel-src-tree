ionic: advertise support for hardware timestamps

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-326.el8
commit-author Shannon Nelson <snelson@pensando.io>
commit afeefec6773607552c450ad2eeb43f39173c2d5c
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-326.el8/afeefec6.failed

Let the network stack know we've got support for timestamping
the packets.

	Signed-off-by: Allen Hubbe <allenbh@pensando.io>
	Signed-off-by: Shannon Nelson <snelson@pensando.io>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit afeefec6773607552c450ad2eeb43f39173c2d5c)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/pensando/ionic/ionic_lif.c
diff --cc drivers/net/ethernet/pensando/ionic/ionic_lif.c
index ff3f4b510503,ee56fed12e07..000000000000
--- a/drivers/net/ethernet/pensando/ionic/ionic_lif.c
+++ b/drivers/net/ethernet/pensando/ionic/ionic_lif.c
@@@ -1278,6 -1539,10 +1278,13 @@@ static int ionic_set_nic_features(struc
  	int err;
  
  	ctx.cmd.lif_setattr.features = ionic_netdev_features_to_nic(features);
++<<<<<<< HEAD
++=======
+ 
+ 	if (lif->phc)
+ 		ctx.cmd.lif_setattr.features |= cpu_to_le64(IONIC_ETH_HW_TIMESTAMP);
+ 
++>>>>>>> afeefec67736 (ionic: advertise support for hardware timestamps)
  	err = ionic_adminq_post_wait(lif, &ctx);
  	if (err)
  		return err;
@@@ -3025,6 -3386,9 +3052,12 @@@ void ionic_lif_unregister(struct ionic_
  
  	if (lif->netdev->reg_state == NETREG_REGISTERED)
  		unregister_netdev(lif->netdev);
++<<<<<<< HEAD
++=======
+ 
+ 	ionic_lif_unregister_phc(lif);
+ 
++>>>>>>> afeefec67736 (ionic: advertise support for hardware timestamps)
  	lif->registered = false;
  }
  
* Unmerged path drivers/net/ethernet/pensando/ionic/ionic_lif.c
