ibmvnic: Use 'skb_frag_address()' instead of hand coding it

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-326.el8
commit-author Christophe JAILLET <christophe.jaillet@wanadoo.fr>
commit c3105f8485775943b2ccde09c7163510c161c965
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-326.el8/c3105f84.failed

'page_address(skb_frag_page()) + skb_frag_off()' can be replaced by an
equivalent 'skb_frag_address()' which is less verbose.

	Signed-off-by: Christophe JAILLET <christophe.jaillet@wanadoo.fr>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit c3105f8485775943b2ccde09c7163510c161c965)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/ibm/ibmvnic.c
diff --cc drivers/net/ethernet/ibm/ibmvnic.c
index c3c11936a552,473411542911..000000000000
--- a/drivers/net/ethernet/ibm/ibmvnic.c
+++ b/drivers/net/ethernet/ibm/ibmvnic.c
@@@ -1722,9 -1678,8 +1722,14 @@@ static netdev_tx_t ibmvnic_xmit(struct 
  		for (i = 0; i < skb_shinfo(skb)->nr_frags; i++) {
  			const skb_frag_t *frag = &skb_shinfo(skb)->frags[i];
  
++<<<<<<< HEAD
 +			memcpy(dst + cur,
 +			       page_address(skb_frag_page(frag)) +
 +			       frag->page_offset, skb_frag_size(frag));
++=======
+ 			memcpy(dst + cur, skb_frag_address(frag),
+ 			       skb_frag_size(frag));
++>>>>>>> c3105f848577 (ibmvnic: Use 'skb_frag_address()' instead of hand coding it)
  			cur += skb_frag_size(frag);
  		}
  	} else {
* Unmerged path drivers/net/ethernet/ibm/ibmvnic.c
