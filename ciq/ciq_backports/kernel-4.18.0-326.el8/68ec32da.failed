net/mlx5: fix error return code in mlx5e_tc_nic_init()

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-326.el8
commit-author Wang Hai <wanghai38@huawei.com>
commit 68ec32daf7d50a9f7425f8607a7402c13aa0c587
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-326.el8/68ec32da.failed

Fix to return a negative error code from the error handling
case instead of 0, as done elsewhere in this function.

Fixes: aedd133d17bc ("net/mlx5e: Support CT offload for tc nic flows")
	Reported-by: Hulk Robot <hulkci@huawei.com>
	Signed-off-by: Wang Hai <wanghai38@huawei.com>
	Signed-off-by: Saeed Mahameed <saeedm@nvidia.com>
(cherry picked from commit 68ec32daf7d50a9f7425f8607a7402c13aa0c587)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/mellanox/mlx5/core/en_tc.c
diff --cc drivers/net/ethernet/mellanox/mlx5/core/en_tc.c
index cf12ebb59fe3,ce710f22b1ff..000000000000
--- a/drivers/net/ethernet/mellanox/mlx5/core/en_tc.c
+++ b/drivers/net/ethernet/mellanox/mlx5/core/en_tc.c
@@@ -5190,6 -5227,13 +5190,16 @@@ int mlx5e_tc_nic_init(struct mlx5e_pri
  		goto err_chains;
  	}
  
++<<<<<<< HEAD
++=======
+ 	tc->ct = mlx5_tc_ct_init(priv, tc->chains, &priv->fs.tc.mod_hdr,
+ 				 MLX5_FLOW_NAMESPACE_KERNEL);
+ 	if (IS_ERR(tc->ct)) {
+ 		err = PTR_ERR(tc->ct);
+ 		goto err_ct;
+ 	}
+ 
++>>>>>>> 68ec32daf7d5 (net/mlx5: fix error return code in mlx5e_tc_nic_init())
  	tc->netdevice_nb.notifier_call = mlx5e_tc_netdev_event;
  	err = register_netdevice_notifier_dev_net(priv->netdev,
  						  &tc->netdevice_nb,
* Unmerged path drivers/net/ethernet/mellanox/mlx5/core/en_tc.c
