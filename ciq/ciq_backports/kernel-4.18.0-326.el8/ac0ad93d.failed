mptcp: use SHA256_BLOCK_SIZE, not SHA_MESSAGE_BYTES

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-326.el8
commit-author Eric Biggers <ebiggers@google.com>
commit ac0ad93df7136e27d2a9c5ec554547695f581d0e
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-326.el8/ac0ad93d.failed

In preparation for naming the SHA-1 stuff in <linux/cryptohash.h>
properly and moving it to a more appropriate header, fix the HMAC-SHA256
code in mptcp_crypto_hmac_sha() to use SHA256_BLOCK_SIZE instead of
"SHA_MESSAGE_BYTES" which is actually the SHA-1 block size.
(Fortunately these are both 64 bytes, so this wasn't a "real" bug...)

	Cc: Paolo Abeni <pabeni@redhat.com>
	Cc: mptcp@lists.01.org
	Signed-off-by: Eric Biggers <ebiggers@google.com>
	Reviewed-by: Matthieu Baerts <matthieu.baerts@tessares.net>
	Signed-off-by: Herbert Xu <herbert@gondor.apana.org.au>
(cherry picked from commit ac0ad93df7136e27d2a9c5ec554547695f581d0e)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/mptcp/crypto.c
diff --cc net/mptcp/crypto.c
index e0a95b1dcf25,81b06d875f92..000000000000
--- a/net/mptcp/crypto.c
+++ b/net/mptcp/crypto.c
@@@ -59,7 -61,7 +59,11 @@@ void mptcp_crypto_hmac_sha(u64 key1, u6
  	put_unaligned_be64(key2, key2be);
  
  	/* Generate key xored with ipad */
++<<<<<<< HEAD
 +	memset(input, 0x36, SHA1_BLOCK_SIZE);
++=======
+ 	memset(input, 0x36, SHA256_BLOCK_SIZE);
++>>>>>>> ac0ad93df713 (mptcp: use SHA256_BLOCK_SIZE, not SHA_MESSAGE_BYTES)
  	for (i = 0; i < 8; i++)
  		input[i] ^= key1be[i];
  	for (i = 0; i < 8; i++)
@@@ -76,7 -78,7 +80,11 @@@
  	sha256_final(&state, &input[SHA256_BLOCK_SIZE]);
  
  	/* Prepare second part of hmac */
++<<<<<<< HEAD
 +	memset(input, 0x5C, SHA1_BLOCK_SIZE);
++=======
+ 	memset(input, 0x5C, SHA256_BLOCK_SIZE);
++>>>>>>> ac0ad93df713 (mptcp: use SHA256_BLOCK_SIZE, not SHA_MESSAGE_BYTES)
  	for (i = 0; i < 8; i++)
  		input[i] ^= key1be[i];
  	for (i = 0; i < 8; i++)
* Unmerged path net/mptcp/crypto.c
