xfrm: input: hold netns during deferred transport reinjection

jira KERNEL-1623
cve CVE-2026-63919
Rebuild_History Non-Buildable kernel-4.18.0-553.164.1.el8_10
commit-author Zhengchuan Liang <zcliangcn@gmail.com>
commit c16f74dc1d75d0e2e7670076d5375deda110ebeb
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-553.164.1.el8_10/c16f74dc.failed

Transport-mode reinjection stores a struct net pointer in skb->cb and
uses it later from xfrm_trans_reinject(). That pointer must stay valid
until the deferred callback runs.

Take a netns reference when queueing deferred reinjection work and drop
it after the callback completes. Use maybe_get_net() so the queueing
path does not revive a namespace that is already being torn down.

This keeps the existing workqueue design and fixes the netns lifetime
handling in one place for all users of xfrm_trans_queue_net().

Fixes: 7b3801927e52 ("xfrm: introduce xfrm_trans_queue_net")
	Cc: stable@kernel.org
	Reported-by: Yuan Tan <yuantan098@gmail.com>
	Reported-by: Xin Liu <bird@lzu.edu.cn>
Co-developed-by: Luxing Yin <tr0jan@lzu.edu.cn>
	Signed-off-by: Luxing Yin <tr0jan@lzu.edu.cn>
	Signed-off-by: Zhengchuan Liang <zcliangcn@gmail.com>
	Signed-off-by: Ren Wei <n05ec@lzu.edu.cn>
Assisted-by: Codex:gpt-5.4
	Signed-off-by: Steffen Klassert <steffen.klassert@secunet.com>
(cherry picked from commit c16f74dc1d75d0e2e7670076d5375deda110ebeb)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/xfrm/xfrm_input.c
diff --cc net/xfrm/xfrm_input.c
index 6ceac35d61fa,e4c2cd24936d..000000000000
--- a/net/xfrm/xfrm_input.c
+++ b/net/xfrm/xfrm_input.c
@@@ -793,11 -792,18 +793,22 @@@ static void xfrm_trans_reinject(unsigne
  	struct sk_buff *skb;
  
  	__skb_queue_head_init(&queue);
 -	spin_lock_bh(&trans->queue_lock);
  	skb_queue_splice_init(&trans->queue, &queue);
 -	spin_unlock_bh(&trans->queue_lock);
  
++<<<<<<< HEAD
 +	while ((skb = __skb_dequeue(&queue)))
 +		XFRM_TRANS_SKB_CB(skb)->finish(XFRM_TRANS_SKB_CB(skb)->net,
 +					       NULL, skb);
++=======
+ 	local_bh_disable();
+ 	while ((skb = __skb_dequeue(&queue))) {
+ 		struct net *net = XFRM_TRANS_SKB_CB(skb)->net;
+ 
+ 		XFRM_TRANS_SKB_CB(skb)->finish(net, NULL, skb);
+ 		put_net(net);
+ 	}
+ 	local_bh_enable();
++>>>>>>> c16f74dc1d75 (xfrm: input: hold netns during deferred transport reinjection)
  }
  
  int xfrm_trans_queue_net(struct net *net, struct sk_buff *skb,
@@@ -813,10 -820,16 +825,19 @@@
  
  	BUILD_BUG_ON(sizeof(struct xfrm_trans_cb) > sizeof(skb->cb));
  
+ 	hold_net = maybe_get_net(net);
+ 	if (!hold_net)
+ 		return -ENODEV;
+ 
  	XFRM_TRANS_SKB_CB(skb)->finish = finish;
++<<<<<<< HEAD
 +	XFRM_TRANS_SKB_CB(skb)->net = net;
++=======
+ 	XFRM_TRANS_SKB_CB(skb)->net = hold_net;
+ 	spin_lock_bh(&trans->queue_lock);
++>>>>>>> c16f74dc1d75 (xfrm: input: hold netns during deferred transport reinjection)
  	__skb_queue_tail(&trans->queue, skb);
 -	spin_unlock_bh(&trans->queue_lock);
 -	schedule_work(&trans->work);
 +	tasklet_schedule(&trans->tasklet);
  	return 0;
  }
  EXPORT_SYMBOL(xfrm_trans_queue_net);
* Unmerged path net/xfrm/xfrm_input.c
