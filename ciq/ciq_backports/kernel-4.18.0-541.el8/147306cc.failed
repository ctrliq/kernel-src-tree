Bluetooth: hci_event: Use of a function table to handle Command Status

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-541.el8
commit-author Luiz Augusto von Dentz <luiz.von.dentz@intel.com>
commit 147306ccbbba23e89481980141d11637948e345d
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-541.el8/147306cc.failed

This change the use of switch statement to a function table which is
easier to extend.

	Signed-off-by: Luiz Augusto von Dentz <luiz.von.dentz@intel.com>
	Signed-off-by: Marcel Holtmann <marcel@holtmann.org>
(cherry picked from commit 147306ccbbba23e89481980141d11637948e345d)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/bluetooth/hci_event.c
diff --cc net/bluetooth/hci_event.c
index b86024e8f1bb,df1aca5f28da..000000000000
--- a/net/bluetooth/hci_event.c
+++ b/net/bluetooth/hci_event.c
@@@ -2665,9 -2665,16 +2665,18 @@@ static void hci_cs_exit_sniff_mode(stru
  static void hci_cs_disconnect(struct hci_dev *hdev, u8 status)
  {
  	struct hci_cp_disconnect *cp;
 -	struct hci_conn_params *params;
  	struct hci_conn *conn;
 -	bool mgmt_conn;
  
++<<<<<<< HEAD
 +	if (!status)
++=======
+ 	bt_dev_dbg(hdev, "status 0x%2.2x", status);
+ 
+ 	/* Wait for HCI_EV_DISCONN_COMPLETE if status 0x00 and not suspended
+ 	 * otherwise cleanup the connection immediately.
+ 	 */
+ 	if (!status && !hdev->suspended)
++>>>>>>> 147306ccbbba (Bluetooth: hci_event: Use of a function table to handle Command Status)
  		return;
  
  	cp = hci_sent_cmd_data(hdev, HCI_OP_DISCONNECT);
* Unmerged path net/bluetooth/hci_event.c
