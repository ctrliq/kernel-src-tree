fs: create helper file_user_path() for user displayed mapped file path

jira KERNEL-1208
Rebuild_History Non-Buildable kernel-4.18.0-553.137.1.el8_10
commit-author Amir Goldstein <amir73il@gmail.com>
commit 08582d678fcf11fc86188f0b92239d3d49667d8e
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-553.137.1.el8_10/08582d67.failed

Overlayfs uses backing files with "fake" overlayfs f_path and "real"
underlying f_inode, in order to use underlying inode aops for mapped
files and to display the overlayfs path in /proc/<pid>/maps.

In preparation for storing the overlayfs "fake" path instead of the
underlying "real" path in struct backing_file, define a noop helper
file_user_path() that returns f_path for now.

Use the new helper in procfs and kernel logs whenever a path of a
mapped file is displayed to users.

	Signed-off-by: Amir Goldstein <amir73il@gmail.com>
Link: https://lore.kernel.org/r/20231009153712.1566422-3-amir73il@gmail.com
	Signed-off-by: Christian Brauner <brauner@kernel.org>
(cherry picked from commit 08582d678fcf11fc86188f0b92239d3d49667d8e)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	arch/arc/kernel/troubleshoot.c
#	fs/proc/task_mmu.c
#	fs/proc/task_nommu.c
#	include/linux/fs.h
diff --cc arch/arc/kernel/troubleshoot.c
index 783b20354f8b,c380d8c30704..000000000000
--- a/arch/arc/kernel/troubleshoot.c
+++ b/arch/arc/kernel/troubleshoot.c
@@@ -92,22 -82,24 +92,35 @@@ static void show_faulting_vma(unsigned 
  	/* can't use print_vma_addr() yet as it doesn't check for
  	 * non-inclusive vma
  	 */
 -	mmap_read_lock(active_mm);
 -	vma = vma_lookup(active_mm, address);
 +	down_read(&active_mm->mmap_sem);
 +	vma = find_vma(active_mm, address);
  
 -	/* Lookup the vma at the address and report if the container VMA is not
 -	 * found
 +	/* check against the find_vma( ) behaviour which returns the next VMA
 +	 * if the container VMA is not found
  	 */
++<<<<<<< HEAD
 +	if (vma && (vma->vm_start <= address)) {
 +		struct file *file = vma->vm_file;
 +		if (file) {
 +			nm = file_path(file, buf, PAGE_SIZE - 1);
 +			inode = file_inode(vma->vm_file);
 +			dev = inode->i_sb->s_dev;
 +			ino = inode->i_ino;
++=======
+ 	if (vma) {
+ 		char buf[ARC_PATH_MAX];
+ 		char *nm = "anon";
+ 
+ 		if (vma->vm_file) {
+ 			/* XXX: can we use %pD below and get rid of buf? */
+ 			nm = d_path(file_user_path(vma->vm_file), buf,
+ 				    ARC_PATH_MAX-1);
+ 			if (IS_ERR(nm))
+ 				nm = "?";
++>>>>>>> 08582d678fcf (fs: create helper file_user_path() for user displayed mapped file path)
  		}
 -		pr_info("  @off 0x%lx in [%s]  VMA: 0x%08lx to 0x%08lx\n",
 +		pr_info("    @off 0x%lx in [%s]\n"
 +			"    VMA: 0x%08lx to 0x%08lx\n",
  			vma->vm_start < TASK_UNMAPPED_BASE ?
  				address : address - vma->vm_start,
  			nm, vma->vm_start, vma->vm_end);
diff --cc fs/proc/task_mmu.c
index ec52e808eb98,1593940ca01e..000000000000
--- a/fs/proc/task_mmu.c
+++ b/fs/proc/task_mmu.c
@@@ -321,7 -289,14 +321,18 @@@ show_map_vma(struct seq_file *m, struc
  	 */
  	if (file) {
  		seq_pad(m, ' ');
++<<<<<<< HEAD
 +		seq_file_path(m, file, "\n");
++=======
+ 		/*
+ 		 * If user named this anon shared memory via
+ 		 * prctl(PR_SET_VMA ..., use the provided name.
+ 		 */
+ 		if (anon_name)
+ 			seq_printf(m, "[anon_shmem:%s]", anon_name->name);
+ 		else
+ 			seq_path(m, file_user_path(file), "\n");
++>>>>>>> 08582d678fcf (fs: create helper file_user_path() for user displayed mapped file path)
  		goto done;
  	}
  
@@@ -1802,10 -1967,10 +1813,15 @@@ static int show_numa_map(struct seq_fil
  
  	if (file) {
  		seq_puts(m, " file=");
++<<<<<<< HEAD
 +		seq_file_path(m, file, "\n\t= ");
 +	} else if (vma->vm_start <= mm->brk && vma->vm_end >= mm->start_brk) {
++=======
+ 		seq_path(m, file_user_path(file), "\n\t= ");
+ 	} else if (vma_is_initial_heap(vma)) {
++>>>>>>> 08582d678fcf (fs: create helper file_user_path() for user displayed mapped file path)
  		seq_puts(m, " heap");
 -	} else if (vma_is_initial_stack(vma)) {
 +	} else if (is_stack(vma)) {
  		seq_puts(m, " stack");
  	}
  
diff --cc fs/proc/task_nommu.c
index af63696b5001,2d5862c13399..000000000000
--- a/fs/proc/task_nommu.c
+++ b/fs/proc/task_nommu.c
@@@ -175,10 -157,10 +175,15 @@@ static int nommu_vma_show(struct seq_fi
  
  	if (file) {
  		seq_pad(m, ' ');
++<<<<<<< HEAD
 +		seq_file_path(m, file, "");
 +	} else if (mm && is_stack(vma)) {
++=======
+ 		seq_path(m, file_user_path(file), "");
+ 	} else if (mm && vma_is_initial_stack(vma)) {
++>>>>>>> 08582d678fcf (fs: create helper file_user_path() for user displayed mapped file path)
  		seq_pad(m, ' ');
 -		seq_puts(m, "[stack]");
 +		seq_printf(m, "[stack]");
  	}
  
  	seq_putc(m, '\n');
diff --cc include/linux/fs.h
index 7f7a0ab8527b,057a3bbd4d27..000000000000
--- a/include/linux/fs.h
+++ b/include/linux/fs.h
@@@ -2611,11 -2480,53 +2611,59 @@@ extern long do_sys_open(int dfd, const 
  			umode_t mode);
  extern struct file *file_open_name(struct filename *, int, umode_t);
  extern struct file *filp_open(const char *, int, umode_t);
 -extern struct file *file_open_root(const struct path *,
 +extern struct file *file_open_root(struct dentry *, struct vfsmount *,
  				   const char *, int, umode_t);
++<<<<<<< HEAD
 +extern struct file * dentry_open(const struct path *, int, const struct cred *);
 +extern struct file * open_with_fake_path(const struct path *, int,
 +					 struct inode*, const struct cred *);
++=======
+ static inline struct file *file_open_root_mnt(struct vfsmount *mnt,
+ 				   const char *name, int flags, umode_t mode)
+ {
+ 	return file_open_root(&(struct path){.mnt = mnt, .dentry = mnt->mnt_root},
+ 			      name, flags, mode);
+ }
+ struct file *dentry_open(const struct path *path, int flags,
+ 			 const struct cred *creds);
+ struct file *dentry_create(const struct path *path, int flags, umode_t mode,
+ 			   const struct cred *cred);
+ struct file *backing_file_open(const struct path *path, int flags,
+ 			       const struct path *real_path,
+ 			       const struct cred *cred);
+ struct path *backing_file_real_path(struct file *f);
+ 
+ /*
+  * file_real_path - get the path corresponding to f_inode
+  *
+  * When opening a backing file for a stackable filesystem (e.g.,
+  * overlayfs) f_path may be on the stackable filesystem and f_inode on
+  * the underlying filesystem.  When the path associated with f_inode is
+  * needed, this helper should be used instead of accessing f_path
+  * directly.
+ */
+ static inline const struct path *file_real_path(struct file *f)
+ {
+ 	if (unlikely(f->f_mode & FMODE_BACKING))
+ 		return backing_file_real_path(f);
+ 	return &f->f_path;
+ }
+ 
+ /*
+  * file_user_path - get the path to display for memory mapped file
+  *
+  * When mmapping a file on a stackable filesystem (e.g., overlayfs), the file
+  * stored in ->vm_file is a backing file whose f_inode is on the underlying
+  * filesystem.  When the mapped file path is displayed to user (e.g. via
+  * /proc/<pid>/maps), this helper should be used to get the path to display
+  * to the user, which is the path of the fd that user has requested to map.
+  */
+ static inline const struct path *file_user_path(struct file *f)
+ {
+ 	return &f->f_path;
+ }
+ 
++>>>>>>> 08582d678fcf (fs: create helper file_user_path() for user displayed mapped file path)
  static inline struct file *file_clone_open(struct file *file)
  {
  	return dentry_open(&file->f_path, file->f_flags, file->f_cred);
* Unmerged path arch/arc/kernel/troubleshoot.c
diff --git a/fs/proc/base.c b/fs/proc/base.c
index 0f9e194a6bd4..aeb69b5f3af5 100644
--- a/fs/proc/base.c
+++ b/fs/proc/base.c
@@ -2151,7 +2151,7 @@ static int map_files_get_link(struct dentry *dentry, struct path *path)
 	rc = -ENOENT;
 	vma = find_exact_vma(mm, vm_start, vm_end);
 	if (vma && vma->vm_file) {
-		*path = vma->vm_file->f_path;
+		*path = *file_user_path(vma->vm_file);
 		path_get(path);
 		rc = 0;
 	}
diff --git a/fs/proc/nommu.c b/fs/proc/nommu.c
index 3b63be64e436..975b832999d8 100644
--- a/fs/proc/nommu.c
+++ b/fs/proc/nommu.c
@@ -64,7 +64,7 @@ static int nommu_region_show(struct seq_file *m, struct vm_region *region)
 
 	if (file) {
 		seq_pad(m, ' ');
-		seq_file_path(m, file, "");
+		seq_path(m, file_user_path(file), "");
 	}
 
 	seq_putc(m, '\n');
* Unmerged path fs/proc/task_mmu.c
* Unmerged path fs/proc/task_nommu.c
* Unmerged path include/linux/fs.h
diff --git a/kernel/trace/trace_output.c b/kernel/trace/trace_output.c
index 4dc651f34f90..8a181d770707 100644
--- a/kernel/trace/trace_output.c
+++ b/kernel/trace/trace_output.c
@@ -418,7 +418,7 @@ static int seq_print_user_ip(struct trace_seq *s, struct mm_struct *mm,
 			vmstart = vma->vm_start;
 		}
 		if (file) {
-			ret = trace_seq_path(s, &file->f_path);
+			ret = trace_seq_path(s, file_user_path(file));
 			if (ret)
 				trace_seq_printf(s, "[+0x%lx]",
 						 ip - vmstart);
