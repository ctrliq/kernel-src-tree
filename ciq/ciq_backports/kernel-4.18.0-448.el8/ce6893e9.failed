wifi: mac80211: ethtool: use deflink for now

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-448.el8
commit-author Johannes Berg <johannes.berg@intel.com>
commit ce6893e917862cae669af52a440d086f416c4cc6
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-448.el8/ce6893e9.failed

Hardcoding link[0] is more confusing than using deflink,
so use deflink for now, which requires making the macro
a bit safer.

	Signed-off-by: Johannes Berg <johannes.berg@intel.com>
(cherry picked from commit ce6893e917862cae669af52a440d086f416c4cc6)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/mac80211/ethtool.c
diff --cc net/mac80211/ethtool.c
index 9f3d36bdf5ae,c2b38370bfb1..000000000000
--- a/net/mac80211/ethtool.c
+++ b/net/mac80211/ethtool.c
@@@ -114,7 -114,7 +114,11 @@@ static void ieee80211_get_stats(struct 
  		sta_set_sinfo(sta, &sinfo, false);
  
  		i = 0;
++<<<<<<< HEAD
 +		ADD_STA_STATS(sta);
++=======
+ 		ADD_STA_STATS(&sta->deflink);
++>>>>>>> ce6893e91786 (wifi: mac80211: ethtool: use deflink for now)
  
  		data[i++] = sta->sta_state;
  
@@@ -140,7 -140,7 +144,11 @@@
  			memset(&sinfo, 0, sizeof(sinfo));
  			sta_set_sinfo(sta, &sinfo, false);
  			i = 0;
++<<<<<<< HEAD
 +			ADD_STA_STATS(sta);
++=======
+ 			ADD_STA_STATS(&sta->deflink);
++>>>>>>> ce6893e91786 (wifi: mac80211: ethtool: use deflink for now)
  		}
  	}
  
* Unmerged path net/mac80211/ethtool.c
