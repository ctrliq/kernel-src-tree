scsi: mpi3mr: Remove unnecessary cast

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-448.el8
commit-author Jules Irenge <jbi.octave@gmail.com>
commit c863a2dcb9b0894d9ac6030c4d1ab2e7867dabcb
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-448.el8/c863a2dc.failed

coccinelle reports a warning:

  WARNING: casting value returned by memory allocation function to (struct
  mpi3mr_throttle_group_info *) is useless

To fix this, the unnecessary cast is removed.

Link: https://lore.kernel.org/r/Yx+kp8NxHvDHs7dv@playground
	Signed-off-by: Jules Irenge <jbi.octave@gmail.com>
	Signed-off-by: Martin K. Petersen <martin.petersen@oracle.com>
(cherry picked from commit c863a2dcb9b0894d9ac6030c4d1ab2e7867dabcb)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/scsi/mpi3mr/mpi3mr_fw.c
diff --cc drivers/scsi/mpi3mr/mpi3mr_fw.c
index 308ad473ffd6,0c4aabaefdcc..000000000000
--- a/drivers/scsi/mpi3mr/mpi3mr_fw.c
+++ b/drivers/scsi/mpi3mr/mpi3mr_fw.c
@@@ -3772,6 -3891,14 +3772,17 @@@ retry_init
  		}
  	}
  
++<<<<<<< HEAD
++=======
+ 	if (!mrioc->throttle_groups && mrioc->num_io_throttle_group) {
+ 		dprint_init(mrioc, "allocating memory for throttle groups\n");
+ 		sz = sizeof(struct mpi3mr_throttle_group_info);
+ 		mrioc->throttle_groups = kcalloc(mrioc->num_io_throttle_group, sz, GFP_KERNEL);
+ 		if (!mrioc->throttle_groups)
+ 			goto out_failed_noretry;
+ 	}
+ 
++>>>>>>> c863a2dcb9b0 (scsi: mpi3mr: Remove unnecessary cast)
  	retval = mpi3mr_enable_events(mrioc);
  	if (retval) {
  		ioc_err(mrioc, "failed to enable events %d\n",
* Unmerged path drivers/scsi/mpi3mr/mpi3mr_fw.c
