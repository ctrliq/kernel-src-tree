ACPI: PM: Unify debug messages in acpi_device_set_power()

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-448.el8
commit-author Rafael J. Wysocki <rafael.j.wysocki@intel.com>
commit f4f3548dc8d53d683770b058fdafa01fd7c07669
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-448.el8/f4f3548d.failed

Convert all of the debug messages printed by acpi_device_set_power()
to acpi_handle_debug() and adjust them slightly for consistency with
acpi_device_get_power() and other acpi_device_set_power() debug
messages.

	Signed-off-by: Rafael J. Wysocki <rafael.j.wysocki@intel.com>
(cherry picked from commit f4f3548dc8d53d683770b058fdafa01fd7c07669)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/acpi/device_pm.c
diff --cc drivers/acpi/device_pm.c
index 42798962dc60,fef309fcd944..000000000000
--- a/drivers/acpi/device_pm.c
+++ b/drivers/acpi/device_pm.c
@@@ -183,9 -174,8 +183,14 @@@ int acpi_device_set_power(struct acpi_d
  
  	/* There is a special case for D0 addressed below. */
  	if (state > ACPI_STATE_D0 && state == device->power.state) {
++<<<<<<< HEAD
 +		ACPI_DEBUG_PRINT((ACPI_DB_INFO, "Device [%s] already in %s\n",
 +				  device->pnp.bus_id,
 +				  acpi_power_state_string(state)));
++=======
+ 		acpi_handle_debug(device->handle, "Already in %s\n",
+ 				  acpi_power_state_string(state));
++>>>>>>> f4f3548dc8d5 (ACPI: PM: Unify debug messages in acpi_device_set_power())
  		return 0;
  	}
  
@@@ -281,14 -272,13 +287,20 @@@
  
   end:
  	if (result) {
- 		dev_warn(&device->dev, "Failed to change power state to %s\n",
- 			 acpi_power_state_string(target_state));
+ 		acpi_handle_debug(device->handle,
+ 				  "Failed to change power state to %s\n",
+ 				  acpi_power_state_string(target_state));
  	} else {
  		device->power.state = target_state;
++<<<<<<< HEAD
 +		ACPI_DEBUG_PRINT((ACPI_DB_INFO,
 +				  "Device [%s] transitioned to %s\n",
 +				  device->pnp.bus_id,
 +				  acpi_power_state_string(target_state)));
++=======
+ 		acpi_handle_debug(device->handle, "Power state changed to %s\n",
+ 				  acpi_power_state_string(target_state));
++>>>>>>> f4f3548dc8d5 (ACPI: PM: Unify debug messages in acpi_device_set_power())
  	}
  
  	return result;
* Unmerged path drivers/acpi/device_pm.c
