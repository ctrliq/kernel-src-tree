mt76: mt7921: enable HW beacon filter not depending on PM flag

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-448.el8
commit-author Sean Wang <sean.wang@mediatek.com>
commit abdb2b524b32292b78cb3aa41e9e64659e8a3a2f
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-448.el8/abdb2b52.failed

The hardware beacon filter works whether or not the PM flag is set, so we
drop redundant dependency checks on PM flags.

Additionally, the patch implicitly allow the MT7921[E, S, U] to have a
consistent configuration of the hardware beacon filter.

	Tested-by: Deren Wu <deren.wu@mediatek.com>
	Signed-off-by: Sean Wang <sean.wang@mediatek.com>
	Signed-off-by: Felix Fietkau <nbd@nbd.name>
(cherry picked from commit abdb2b524b32292b78cb3aa41e9e64659e8a3a2f)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/wireless/mediatek/mt76/mt7921/main.c
diff --cc drivers/net/wireless/mediatek/mt76/mt7921/main.c
index dbd7ff121712,2ea20e9306fd..000000000000
--- a/drivers/net/wireless/mediatek/mt76/mt7921/main.c
+++ b/drivers/net/wireless/mediatek/mt76/mt7921/main.c
@@@ -668,8 -668,7 +668,12 @@@ static void mt7921_bss_info_changed(str
  	if (changed & BSS_CHANGED_ASSOC) {
  		mt7921_mcu_sta_update(dev, NULL, vif, true,
  				      MT76_STA_INFO_STATE_ASSOC);
++<<<<<<< HEAD
 +		if (dev->pm.enable)
 +			mt7921_mcu_set_beacon_filter(dev, vif, info->assoc);
++=======
+ 		mt7921_mcu_set_beacon_filter(dev, vif, vif->cfg.assoc);
++>>>>>>> abdb2b524b32 (mt76: mt7921: enable HW beacon filter not depending on PM flag)
  	}
  
  	if (changed & BSS_CHANGED_ARP_FILTER) {
* Unmerged path drivers/net/wireless/mediatek/mt76/mt7921/main.c
