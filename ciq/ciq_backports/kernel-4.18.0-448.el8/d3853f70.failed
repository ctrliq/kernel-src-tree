wifi: mac80211: mlme: remove sta argument from ieee80211_config_bw

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-448.el8
commit-author Johannes Berg <johannes.berg@intel.com>
commit d3853f700ce621f7f90f3a119a1b706f829fc55f
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-448.el8/d3853f70.failed

The argument is unused except for NULL checking, but we already
do that anyway, so it's not needed. Remove the argument.

	Signed-off-by: Johannes Berg <johannes.berg@intel.com>
(cherry picked from commit d3853f700ce621f7f90f3a119a1b706f829fc55f)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/mac80211/mlme.c
diff --cc net/mac80211/mlme.c
index fc4128b322c2,3c59cac9ab85..000000000000
--- a/net/mac80211/mlme.c
+++ b/net/mac80211/mlme.c
@@@ -402,8 -404,7 +402,12 @@@ out
  	return ret;
  }
  
++<<<<<<< HEAD
 +static int ieee80211_config_bw(struct ieee80211_sub_if_data *sdata,
 +			       struct sta_info *sta,
++=======
+ static int ieee80211_config_bw(struct ieee80211_link_data *link,
++>>>>>>> d3853f700ce6 (wifi: mac80211: mlme: remove sta argument from ieee80211_config_bw)
  			       const struct ieee80211_ht_cap *ht_cap,
  			       const struct ieee80211_vht_cap *vht_cap,
  			       const struct ieee80211_ht_operation *ht_oper,
@@@ -4385,11 -4431,11 +4386,15 @@@ static void ieee80211_rx_mgmt_beacon(st
  				erp_valid, erp_value);
  
  	mutex_lock(&local->sta_mtx);
 -	sta = sta_info_get(sdata, sdata->vif.cfg.ap_addr);
 +	sta = sta_info_get(sdata, bssid);
  
 -	changed |= ieee80211_recalc_twt_req(link, sta, elems);
 +	changed |= ieee80211_recalc_twt_req(sdata, sta, elems);
  
++<<<<<<< HEAD
 +	if (ieee80211_config_bw(sdata, sta, elems->ht_cap_elem,
++=======
+ 	if (ieee80211_config_bw(link, elems->ht_cap_elem,
++>>>>>>> d3853f700ce6 (wifi: mac80211: mlme: remove sta argument from ieee80211_config_bw)
  				elems->vht_cap_elem, elems->ht_operation,
  				elems->vht_operation, elems->he_operation,
  				elems->eht_operation,
* Unmerged path net/mac80211/mlme.c
