swiotlb: remove a useless return in swiotlb_init

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-448.el8
commit-author Dongli Zhang <dongli.zhang@oracle.com>
commit 466298c65678e7a2bd7bd268fd3776de73e96a4b
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-448.el8/466298c6.failed

Both swiotlb_init_remap() and swiotlb_init() have return type void.

	Signed-off-by: Dongli Zhang <dongli.zhang@oracle.com>
	Signed-off-by: Christoph Hellwig <hch@lst.de>
(cherry picked from commit 466298c65678e7a2bd7bd268fd3776de73e96a4b)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	kernel/dma/swiotlb.c
diff --cc kernel/dma/swiotlb.c
index afdd6d1eaf34,fd21f4162f4b..000000000000
--- a/kernel/dma/swiotlb.c
+++ b/kernel/dma/swiotlb.c
@@@ -246,38 -274,15 +246,42 @@@ int __init swiotlb_init_with_tbl(char *
  		panic("%s: Failed to allocate %zu bytes align=0x%lx\n",
  		      __func__, alloc_size, PAGE_SIZE);
  
 -	swiotlb_init_io_tlb_mem(mem, __pa(tlb), nslabs, flags, false);
 +	swiotlb_init_io_tlb_mem(mem, __pa(tlb), nslabs, false);
  
 -	if (flags & SWIOTLB_VERBOSE)
 +	if (verbose)
  		swiotlb_print_info();
 +	return 0;
  }
  
 -void __init swiotlb_init(bool addressing_limit, unsigned int flags)
 +/*
 + * Statically reserve bounce buffer space and initialize bounce buffer data
 + * structures for the software IO TLB used to implement the DMA API.
 + */
 +void  __init
 +swiotlb_init(int verbose)
  {
++<<<<<<< HEAD
 +	size_t bytes = PAGE_ALIGN(default_nslabs << IO_TLB_SHIFT);
 +	void *tlb;
 +
 +	if (swiotlb_force == SWIOTLB_NO_FORCE)
 +		return;
 +
 +	/* Get IO TLB memory from the low pages */
 +	tlb = memblock_alloc_low_nopanic(bytes, PAGE_SIZE);
 +	if (!tlb)
 +		goto fail;
 +	if (swiotlb_init_with_tbl(tlb, default_nslabs, verbose))
 +		goto fail_free_mem;
 +	return;
 +
 +fail_free_mem:
 +	memblock_free_early(__pa(tlb), bytes);
 +fail:
 +	pr_warn("Cannot allocate buffer");
++=======
+ 	swiotlb_init_remap(addressing_limit, flags, NULL);
++>>>>>>> 466298c65678 (swiotlb: remove a useless return in swiotlb_init)
  }
  
  /*
* Unmerged path kernel/dma/swiotlb.c
