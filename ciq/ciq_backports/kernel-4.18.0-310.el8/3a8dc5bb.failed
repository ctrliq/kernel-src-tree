scsi: core: Remove scsi_init_cmd_errh

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-310.el8
commit-author Christoph Hellwig <hch@lst.de>
commit 3a8dc5bbc8c04db2646ed0842118b0f66659a3db
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-310.el8/3a8dc5bb.failed

There is no good reason to keep this functionality as a separate function,
just merge it into the only caller.

Link: https://lore.kernel.org/r/20201005084130.143273-3-hch@lst.de
	Reviewed-by: Hannes Reinecke <hare@suse.de>
	Signed-off-by: Christoph Hellwig <hch@lst.de>
	Signed-off-by: Martin K. Petersen <martin.petersen@oracle.com>
(cherry picked from commit 3a8dc5bbc8c04db2646ed0842118b0f66659a3db)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/scsi/scsi_lib.c
diff --cc drivers/scsi/scsi_lib.c
index 90e2c0bdccdc,b515560eaf02..000000000000
--- a/drivers/scsi/scsi_lib.c
+++ b/drivers/scsi/scsi_lib.c
@@@ -293,22 -293,6 +293,25 @@@ int __scsi_execute(struct scsi_device *
  }
  EXPORT_SYMBOL(__scsi_execute);
  
++<<<<<<< HEAD
 +/**
 + * scsi_init_cmd_errh - Initialize cmd fields related to error handling.
 + * @cmd:  command that is ready to be queued.
 + *
 + * This function has the job of initializing a number of fields related to error
 + * handling. Typically this will be called once for each command, as required.
 + */
 +static void scsi_init_cmd_errh(struct scsi_cmnd *cmd)
 +{
 +	cmd->serial_number = 0;
 +	scsi_set_resid(cmd, 0);
 +	memset(cmd->sense_buffer, 0, SCSI_SENSE_BUFFERSIZE);
 +	if (cmd->cmd_len == 0)
 +		cmd->cmd_len = scsi_command_size(cmd->cmnd);
 +}
 +
++=======
++>>>>>>> 3a8dc5bbc8c0 (scsi: core: Remove scsi_init_cmd_errh)
  /*
   * Wake up the error handler if necessary. Avoid as follows that the error
   * handler is not woken up if host in-flight requests number ==
* Unmerged path drivers/scsi/scsi_lib.c
