nvme: remove unnecessary return values

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-310.el8
commit-author Javier González <javier.gonz@samsung.com>
commit e1aaf5cacba9d994d825a87a33bdd33343477f16
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-310.el8/e1aaf5ca.failed

Cleanup unnecessary ret values that are not checked or used in
nvme_alloc_ns().

	Signed-off-by: Javier González <javier.gonz@samsung.com>
	Reviewed-by: Minwoo Im <minwoo.im.dev@gmail.com>
	Signed-off-by: Christoph Hellwig <hch@lst.de>
(cherry picked from commit e1aaf5cacba9d994d825a87a33bdd33343477f16)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/nvme/host/core.c
diff --cc drivers/nvme/host/core.c
index 63ea79cfd1d0,6d0c902034af..000000000000
--- a/drivers/nvme/host/core.c
+++ b/drivers/nvme/host/core.c
@@@ -3961,9 -3872,9 +3961,9 @@@ static void nvme_alloc_ns(struct nvme_c
  	struct gendisk *disk;
  	struct nvme_id_ns *id;
  	char disk_name[DISK_NAME_LEN];
- 	int node = ctrl->numa_node, flags = GENHD_FL_EXT_DEVT, ret;
+ 	int node = ctrl->numa_node, flags = GENHD_FL_EXT_DEVT;
  
 -	if (nvme_identify_ns(ctrl, nsid, ids, &id))
 +	if (nvme_identify_ns(ctrl, nsid, &id))
  		return;
  
  	ns = kzalloc_node(sizeof(*ns), GFP_KERNEL, node);
@@@ -3984,15 -3894,9 +3984,19 @@@
  
  	ns->queue->queuedata = ns;
  	ns->ctrl = ctrl;
 +
++<<<<<<< HEAD
  	kref_init(&ns->kref);
 +	ns->lba_shift = 9; /* set to a default value for 512 until disk is validated */
  
 +	blk_queue_logical_block_size(ns->queue, 1 << ns->lba_shift);
 +	nvme_set_queue_limits(ctrl, ns->queue);
 +
 +	ret = nvme_init_ns_head(ns, nsid, id);
 +	if (ret)
++=======
+ 	if (nvme_init_ns_head(ns, nsid, ids, id->nmic & NVME_NS_NMIC_SHARED))
++>>>>>>> e1aaf5cacba9 (nvme: remove unnecessary return values)
  		goto out_free_queue;
  	nvme_set_disk_name(disk_name, ns, ctrl, &flags);
  
* Unmerged path drivers/nvme/host/core.c
