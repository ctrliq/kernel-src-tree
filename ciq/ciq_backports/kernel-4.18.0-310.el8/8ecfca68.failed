RDMA: Lift ibdev_to_node from rds to common code

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-310.el8
commit-author Christoph Hellwig <hch@lst.de>
commit 8ecfca68dc4cbee1272a0161e3f2fb9387dc6930
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-310.el8/8ecfca68.failed

Lift the ibdev_to_node from rds to common code and document it.

Link: https://lore.kernel.org/r/20201106181941.1878556-4-hch@lst.de
	Signed-off-by: Christoph Hellwig <hch@lst.de>
	Signed-off-by: Jason Gunthorpe <jgg@nvidia.com>
(cherry picked from commit 8ecfca68dc4cbee1272a0161e3f2fb9387dc6930)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/rds/ib.h
diff --cc net/rds/ib.h
index fd9333d81e78,c23a11d9ad36..000000000000
--- a/net/rds/ib.h
+++ b/net/rds/ib.h
@@@ -232,7 -264,6 +232,10 @@@ struct rds_ib_device 
  	int			*vector_load;
  };
  
++<<<<<<< HEAD
 +#define ibdev_to_node(ibdev) dev_to_node((ibdev)->dev.parent)
++=======
++>>>>>>> 8ecfca68dc4c (RDMA: Lift ibdev_to_node from rds to common code)
  #define rdsibdev_to_node(rdsibdev) ibdev_to_node(rdsibdev->dev)
  
  /* bits for i_ack_flags */
diff --git a/include/rdma/ib_verbs.h b/include/rdma/ib_verbs.h
index 4f2a3eeb85db..54bc7fd87f4e 100644
--- a/include/rdma/ib_verbs.h
+++ b/include/rdma/ib_verbs.h
@@ -4605,6 +4605,19 @@ static inline struct ib_device *rdma_device_to_ibdev(struct device *device)
 	return coredev->owner;
 }
 
+/**
+ * ibdev_to_node - return the NUMA node for a given ib_device
+ * @dev:	device to get the NUMA node for.
+ */
+static inline int ibdev_to_node(struct ib_device *ibdev)
+{
+	struct device *parent = ibdev->dev.parent;
+
+	if (!parent)
+		return NUMA_NO_NODE;
+	return dev_to_node(parent);
+}
+
 /**
  * rdma_device_to_drv_device - Helper macro to reach back to driver's
  *			       ib_device holder structure from device pointer.
* Unmerged path net/rds/ib.h
