fuse: move rb_erase() before tree_insert()

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-310.el8
commit-author Miklos Szeredi <mszeredi@redhat.com>
commit 69a6487ac0ea1bbc8d8b654cb5a4541f2d922f91
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-310.el8/69a6487a.failed

In fuse_writepage_end() the old writepages entry needs to be removed from
the rbtree before inserting the new one, otherwise tree_insert() would
fail.  This is a very rare codepath and no reproducer exists.

	Signed-off-by: Miklos Szeredi <mszeredi@redhat.com>
(cherry picked from commit 69a6487ac0ea1bbc8d8b654cb5a4541f2d922f91)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/fuse/file.c
diff --cc fs/fuse/file.c
index 1b617c09a013,7247c02adb32..000000000000
--- a/fs/fuse/file.c
+++ b/fs/fuse/file.c
@@@ -1662,7 -1586,6 +1662,10 @@@ static void fuse_writepage_finish(struc
  	struct backing_dev_info *bdi = inode_to_bdi(inode);
  	int i;
  
++<<<<<<< HEAD
 +	list_del(&wpa->writepages_entry);
++=======
++>>>>>>> 69a6487ac0ea (fuse: move rb_erase() before tree_insert())
  	for (i = 0; i < ap->num_pages; i++) {
  		dec_wb_stat(&bdi->wb, WB_WRITEBACK);
  		dec_node_page_state(ap->pages[i], NR_WRITEBACK_TEMP);
* Unmerged path fs/fuse/file.c
