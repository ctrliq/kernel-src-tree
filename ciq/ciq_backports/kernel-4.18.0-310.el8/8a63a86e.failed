block: use bd_partno in bdevname

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-310.el8
commit-author Christoph Hellwig <hch@lst.de>
commit 8a63a86e1f933dc7f114497408c50c58a633b8bc
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-310.el8/8a63a86e.failed

No need to go through the hd_struct to find the partition number.

	Signed-off-by: Christoph Hellwig <hch@lst.de>
	Signed-off-by: Jens Axboe <axboe@kernel.dk>
(cherry picked from commit 8a63a86e1f933dc7f114497408c50c58a633b8bc)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	block/genhd.c
diff --cc block/genhd.c
index e5a406eac921,e5f17f022ec7..000000000000
--- a/block/genhd.c
+++ b/block/genhd.c
@@@ -87,7 -83,12 +87,16 @@@ char *disk_name(struct gendisk *hd, in
  	return buf;
  }
  
++<<<<<<< HEAD
 +#ifdef CONFIG_SMP
++=======
+ const char *bdevname(struct block_device *bdev, char *buf)
+ {
+ 	return disk_name(bdev->bd_disk, bdev->bd_partno, buf);
+ }
+ EXPORT_SYMBOL(bdevname);
+ 
++>>>>>>> 8a63a86e1f93 (block: use bd_partno in bdevname)
  static void part_stat_read_all(struct hd_struct *part, struct disk_stats *stat)
  {
  	int cpu;
* Unmerged path block/genhd.c
