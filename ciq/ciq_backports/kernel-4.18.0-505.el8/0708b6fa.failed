ice: remove dead store on XSK hotpath

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-505.el8
commit-author Alexander Lobakin <alexandr.lobakin@intel.com>
commit 0708b6facb4d165ef22bccddf2dc3e1eb9a12d03
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-505.el8/0708b6fa.failed

The 'if (ntu == rx_ring->count)' block in ice_alloc_rx_buffers_zc()
was previously residing in the loop, but after introducing the
batched interface it is used only to wrap-around the NTU descriptor,
thus no more need to assign 'xdp'.

Fixes: db804cfc21e9 ("ice: Use the xsk batched rx allocation interface")
	Signed-off-by: Alexander Lobakin <alexandr.lobakin@intel.com>
	Acked-by: Maciej Fijalkowski <maciej.fijalkowski@intel.com>
	Tested-by: Kiran Bhandare <kiranx.bhandare@intel.com>
	Signed-off-by: Tony Nguyen <anthony.l.nguyen@intel.com>
(cherry picked from commit 0708b6facb4d165ef22bccddf2dc3e1eb9a12d03)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/intel/ice/ice_xsk.c
diff --cc drivers/net/ethernet/intel/ice/ice_xsk.c
index 71d9a26e480a,27f5f64dcbd6..000000000000
--- a/drivers/net/ethernet/intel/ice/ice_xsk.c
+++ b/drivers/net/ethernet/intel/ice/ice_xsk.c
@@@ -460,22 -392,19 +460,36 @@@ bool ice_alloc_rx_bufs_zc(struct ice_rx
  
  		rx_desc++;
  		xdp++;
 +		ntu++;
 +
 +		if (unlikely(ntu == rx_ring->count)) {
 +			rx_desc = ICE_RX_DESC(rx_ring, 0);
 +			xdp = rx_ring->xdp_buf;
 +			ntu = 0;
 +		}
 +	} while (--count);
 +
 +	if (rx_ring->next_to_use != ntu) {
 +		/* clear the status bits for the next_to_use descriptor */
 +		rx_desc->wb.status_error0 = 0;
 +		ice_release_rx_desc(rx_ring, ntu);
  	}
  
++<<<<<<< HEAD
 +	return ok;
++=======
+ 	ntu += nb_buffs;
+ 	if (ntu == rx_ring->count) {
+ 		rx_desc = ICE_RX_DESC(rx_ring, 0);
+ 		ntu = 0;
+ 	}
+ 
+ 	/* clear the status bits for the next_to_use descriptor */
+ 	rx_desc->wb.status_error0 = 0;
+ 	ice_release_rx_desc(rx_ring, ntu);
+ 
+ 	return count == nb_buffs;
++>>>>>>> 0708b6facb4d (ice: remove dead store on XSK hotpath)
  }
  
  /**
* Unmerged path drivers/net/ethernet/intel/ice/ice_xsk.c
