r8152: remove some bit operations

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-502.el8
commit-author Hayes Wang <hayeswang@realtek.com>
commit 9c68011bd7e477ee8d03824c8cb40eab9c64027d
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-502.el8/9c68011b.failed

Remove DELL_TB_RX_AGG_BUG and LENOVO_MACPASSTHRU flags of rtl8152_flags.
They are only set when initializing and wouldn't be change. It is enough
to record them with variables.

	Signed-off-by: Hayes Wang <hayeswang@realtek.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 9c68011bd7e477ee8d03824c8cb40eab9c64027d)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/usb/r8152.c
diff --cc drivers/net/usb/r8152.c
index e84ea7c904d3,136ea06540ff..000000000000
--- a/drivers/net/usb/r8152.c
+++ b/drivers/net/usb/r8152.c
@@@ -842,7 -929,11 +840,13 @@@ struct r8152 
  	u32 rx_buf_sz;
  	u32 rx_copybreak;
  	u32 rx_pending;
 -	u32 fc_pause_on, fc_pause_off;
  
++<<<<<<< HEAD
++=======
+ 	u32 support_2500full:1;
+ 	u32 lenovo_macpassthru:1;
+ 	u32 dell_tb_rx_agg_bug:1;
++>>>>>>> 9c68011bd7e4 (r8152: remove some bit operations)
  	u16 ocp_base;
  	u16 speed;
  	u16 eee_adv;
@@@ -5516,10 -7967,11 +5520,10 @@@ static void r8153_init(struct r8152 *tp
  	/* rx aggregation */
  	ocp_data = ocp_read_word(tp, MCU_TYPE_USB, USB_USB_CTRL);
  	ocp_data &= ~(RX_AGG_DISABLE | RX_ZERO_EN);
- 	if (test_bit(DELL_TB_RX_AGG_BUG, &tp->flags))
 -	ocp_write_word(tp, MCU_TYPE_USB, USB_USB_CTRL, ocp_data);
++	if (tp->dell_tb_rx_agg_bug)
 +		ocp_data |= RX_AGG_DISABLE;
  
 -	ocp_data = ocp_read_byte(tp, MCU_TYPE_USB, USB_BMU_CONFIG);
 -	ocp_data |= ACT_ODMA;
 -	ocp_write_byte(tp, MCU_TYPE_USB, USB_BMU_CONFIG, ocp_data);
 +	ocp_write_word(tp, MCU_TYPE_USB, USB_USB_CTRL, ocp_data);
  
  	rtl_tally_reset(tp);
  
* Unmerged path drivers/net/usb/r8152.c
