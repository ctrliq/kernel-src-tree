ionic: Move devlink registration to be last devlink command

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-394.el8
commit-author Leon Romanovsky <leonro@nvidia.com>
commit 7911c8bd546fa85e408544426646dd1f1a684286
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-394.el8/7911c8bd.failed

This change prevents from users to access device before devlink is
fully configured.

	Signed-off-by: Leon Romanovsky <leonro@nvidia.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 7911c8bd546fa85e408544426646dd1f1a684286)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/pensando/ionic/ionic_devlink.c
diff --cc drivers/net/ethernet/pensando/ionic/ionic_devlink.c
index 9423673b59c9,2267da95640b..000000000000
--- a/drivers/net/ethernet/pensando/ionic/ionic_devlink.c
+++ b/drivers/net/ethernet/pensando/ionic/ionic_devlink.c
@@@ -82,12 -82,6 +82,15 @@@ int ionic_devlink_register(struct ioni
  	struct devlink_port_attrs attrs = {};
  	int err;
  
++<<<<<<< HEAD
 +	err = devlink_register(dl, ionic->dev);
 +	if (err) {
 +		dev_warn(ionic->dev, "devlink_register failed: %d\n", err);
 +		return err;
 +	}
 +
++=======
++>>>>>>> 7911c8bd546f (ionic: Move devlink registration to be last devlink command)
  	attrs.flavour = DEVLINK_PORT_FLAVOUR_PHYSICAL;
  	devlink_port_attrs_set(&ionic->dl_port, &attrs);
  	err = devlink_port_register(dl, &ionic->dl_port, 0);
* Unmerged path drivers/net/ethernet/pensando/ionic/ionic_devlink.c
