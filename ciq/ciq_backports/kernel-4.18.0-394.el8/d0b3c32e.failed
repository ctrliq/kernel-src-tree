arm64: memory: Cosmetic cleanups

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-394.el8
commit-author Will Deacon <will@kernel.org>
commit d0b3c32ed9220616548ff63808751cf2f6608df1
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-394.el8/d0b3c32e.failed

Cleanup memory.h so that the indentation is consistent, remove pointless
line-wrapping and use consistent parameter names for different versions
of the same macro.

	Reviewed-by: Steve Capper <steve.capper@arm.com>
	Acked-by: Catalin Marinas <catalin.marinas@arm.com>
	Reviewed-by: Mark Rutland <mark.rutland@arm.com>
	Signed-off-by: Will Deacon <will@kernel.org>
(cherry picked from commit d0b3c32ed9220616548ff63808751cf2f6608df1)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	arch/arm64/include/asm/memory.h
diff --cc arch/arm64/include/asm/memory.h
index 6d1182b5d477,d69c2865ae40..000000000000
--- a/arch/arm64/include/asm/memory.h
+++ b/arch/arm64/include/asm/memory.h
@@@ -26,7 -16,6 +27,10 @@@
  #include <linux/types.h>
  #include <asm/bug.h>
  #include <asm/page-def.h>
++<<<<<<< HEAD
 +#include <asm/sizes.h>
++=======
++>>>>>>> d0b3c32ed922 (arm64: memory: Cosmetic cleanups)
  
  /*
   * Size of the PCI I/O space. This must remain a power of two so that
@@@ -82,11 -63,18 +86,11 @@@
  #else
  #define VA_BITS_MIN		(VA_BITS)
  #endif
 -
 -#define _VA_START(va)		(-(UL(1) << ((va) - 1)))
 +#define _PAGE_END(va)		(-(UL(1) << ((va) - 1)))
  
- #define KERNEL_START      _text
- #define KERNEL_END        _end
+ #define KERNEL_START		_text
+ #define KERNEL_END		_end
  
 -#ifdef CONFIG_ARM64_VA_BITS_52
 -#define MAX_USER_VA_BITS	52
 -#else
 -#define MAX_USER_VA_BITS	VA_BITS
 -#endif
 -
  /*
   * Generic and tag-based KASAN require 1/8th and 1/16th of the kernel virtual
   * address space for the shadow region respectively. They can bloat the stack
* Unmerged path arch/arm64/include/asm/memory.h
