NFSv4: nfs4_inc/dec_nlink_locked should also invalidate ctime

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-394.el8
commit-author Trond Myklebust <trond.myklebust@hammerspace.com>
commit 82eae5a432cb2d50d7ea51428dcad7894e4165fe
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-394.el8/82eae5a4.failed

If the nlink changes, then so will the ctime.

	Signed-off-by: Trond Myklebust <trond.myklebust@hammerspace.com>
(cherry picked from commit 82eae5a432cb2d50d7ea51428dcad7894e4165fe)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/nfs/nfs4proc.c
diff --cc fs/nfs/nfs4proc.c
index 2b64b461b599,d05f4ca5d9c0..000000000000
--- a/fs/nfs/nfs4proc.c
+++ b/fs/nfs/nfs4proc.c
@@@ -1171,14 -1169,18 +1171,26 @@@ int nfs4_call_sync(struct rpc_clnt *cln
  static void
  nfs4_inc_nlink_locked(struct inode *inode)
  {
++<<<<<<< HEAD
 +	nfs_set_cache_invalid(inode, NFS_INO_INVALID_OTHER);
++=======
+ 	nfs_set_cache_invalid(inode, NFS_INO_INVALID_CHANGE |
+ 					     NFS_INO_INVALID_CTIME |
+ 					     NFS_INO_INVALID_NLINK);
++>>>>>>> 82eae5a432cb (NFSv4: nfs4_inc/dec_nlink_locked should also invalidate ctime)
  	inc_nlink(inode);
  }
  
  static void
  nfs4_dec_nlink_locked(struct inode *inode)
  {
++<<<<<<< HEAD
 +	nfs_set_cache_invalid(inode, NFS_INO_INVALID_OTHER);
++=======
+ 	nfs_set_cache_invalid(inode, NFS_INO_INVALID_CHANGE |
+ 					     NFS_INO_INVALID_CTIME |
+ 					     NFS_INO_INVALID_NLINK);
++>>>>>>> 82eae5a432cb (NFSv4: nfs4_inc/dec_nlink_locked should also invalidate ctime)
  	drop_nlink(inode);
  }
  
* Unmerged path fs/nfs/nfs4proc.c
