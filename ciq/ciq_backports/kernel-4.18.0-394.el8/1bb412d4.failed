net_tstamp: define new flag HWTSTAMP_FLAG_BONDED_PHC_INDEX

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-394.el8
commit-author Hangbin Liu <liuhangbin@gmail.com>
commit 1bb412d46ca9df90a3fe4b704f5385f03621455d
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-394.el8/1bb412d4.failed

As we defined the new hwtstamp_config flag HWTSTAMP_FLAG_BONDED_PHC_INDEX
as enum, it's not easy for userspace program to check if the flag is
supported when build.

Let's define the new flag so user space could build it on old kernel with
ifdef check.

Fixes: 9c9211a3fc7a ("net_tstamp: add new flag HWTSTAMP_FLAG_BONDED_PHC_INDEX")
	Signed-off-by: Hangbin Liu <liuhangbin@gmail.com>
	Acked-by: Richard Cochran <richardcochran@gmail.com>
	Signed-off-by: Jakub Kicinski <kuba@kernel.org>
(cherry picked from commit 1bb412d46ca9df90a3fe4b704f5385f03621455d)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	include/uapi/linux/net_tstamp.h
diff --cc include/uapi/linux/net_tstamp.h
index 7ed0b3d1c00a,55501e5e7ac8..000000000000
--- a/include/uapi/linux/net_tstamp.h
+++ b/include/uapi/linux/net_tstamp.h
@@@ -65,6 -78,21 +65,24 @@@ struct hwtstamp_config 
  	int rx_filter;
  };
  
++<<<<<<< HEAD
++=======
+ /* possible values for hwtstamp_config->flags */
+ enum hwtstamp_flags {
+ 	/*
+ 	 * With this flag, the user could get bond active interface's
+ 	 * PHC index. Note this PHC index is not stable as when there
+ 	 * is a failover, the bond active interface will be changed, so
+ 	 * will be the PHC index.
+ 	 */
+ 	HWTSTAMP_FLAG_BONDED_PHC_INDEX = (1<<0),
+ #define HWTSTAMP_FLAG_BONDED_PHC_INDEX	HWTSTAMP_FLAG_BONDED_PHC_INDEX
+ 
+ 	HWTSTAMP_FLAG_LAST = HWTSTAMP_FLAG_BONDED_PHC_INDEX,
+ 	HWTSTAMP_FLAG_MASK = (HWTSTAMP_FLAG_LAST - 1) | HWTSTAMP_FLAG_LAST
+ };
+ 
++>>>>>>> 1bb412d46ca9 (net_tstamp: define new flag HWTSTAMP_FLAG_BONDED_PHC_INDEX)
  /* possible values for hwtstamp_config->tx_type */
  enum hwtstamp_tx_types {
  	/*
* Unmerged path include/uapi/linux/net_tstamp.h
