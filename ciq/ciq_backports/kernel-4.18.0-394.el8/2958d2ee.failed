NFSD: Add a couple more nfsd_clid_expired call sites

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-394.el8
commit-author Chuck Lever <chuck.lever@oracle.com>
commit 2958d2ee71021b6c44212ec6c2a39cc71d9cd4a9
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-394.el8/2958d2ee.failed

Improve observation of NFSv4 lease expiry.

	Signed-off-by: Chuck Lever <chuck.lever@oracle.com>
	Signed-off-by: J. Bruce Fields <bfields@redhat.com>
(cherry picked from commit 2958d2ee71021b6c44212ec6c2a39cc71d9cd4a9)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/nfsd/nfs4state.c
#	fs/nfsd/trace.h
diff --cc fs/nfsd/nfs4state.c
index 968845cb8307,2e7bbaa92684..000000000000
--- a/fs/nfsd/nfs4state.c
+++ b/fs/nfsd/nfs4state.c
@@@ -2623,9 -2665,11 +2623,15 @@@ static void force_expire_client(struct 
  	struct nfsd_net *nn = net_generic(clp->net, nfsd_net_id);
  	bool already_expired;
  
++<<<<<<< HEAD
 +	spin_lock(&nn->client_lock);
++=======
+ 	trace_nfsd_clid_admin_expired(&clp->cl_clientid);
+ 
+ 	spin_lock(&clp->cl_lock);
++>>>>>>> 2958d2ee7102 (NFSD: Add a couple more nfsd_clid_expired call sites)
  	clp->cl_time = 0;
 -	spin_unlock(&clp->cl_lock);
 +	spin_unlock(&nn->client_lock);
  
  	wait_event(expiry_wq, atomic_read(&clp->cl_rpc_users) == 0);
  	spin_lock(&nn->client_lock);
diff --cc fs/nfsd/trace.h
index 2c7ada5dbc70,ac96416b4b33..000000000000
--- a/fs/nfsd/trace.h
+++ b/fs/nfsd/trace.h
@@@ -492,7 -511,11 +492,15 @@@ DEFINE_EVENT(nfsd_clientid_class, nfsd_
  	TP_PROTO(const clientid_t *clid), \
  	TP_ARGS(clid))
  
++<<<<<<< HEAD
 +DEFINE_CLIENTID_EVENT(expired);
++=======
+ DEFINE_CLIENTID_EVENT(reclaim_complete);
+ DEFINE_CLIENTID_EVENT(confirmed);
+ DEFINE_CLIENTID_EVENT(destroyed);
+ DEFINE_CLIENTID_EVENT(admin_expired);
+ DEFINE_CLIENTID_EVENT(replaced);
++>>>>>>> 2958d2ee7102 (NFSD: Add a couple more nfsd_clid_expired call sites)
  DEFINE_CLIENTID_EVENT(purged);
  DEFINE_CLIENTID_EVENT(renew);
  DEFINE_CLIENTID_EVENT(stale);
* Unmerged path fs/nfsd/nfs4state.c
* Unmerged path fs/nfsd/trace.h
