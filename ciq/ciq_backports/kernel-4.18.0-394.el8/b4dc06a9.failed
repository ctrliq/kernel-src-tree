scsi: smartpqi: Fix a name typo and cleanup code

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-394.el8
commit-author Kevin Barnett <kevin.barnett@microchip.com>
commit b4dc06a9070e3ca9d18a33fe649df594832dde1a
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-394.el8/b4dc06a9.failed

Rename the function pqi_is_io_high_priority() to pqi_is_io_high_priority().
Remove 2 unnecessary lines from the function, and adjusted the white space.

Link: https://lore.kernel.org/r/164375209818.440833.10908948825731635853.stgit@brunhilda.pdev.net
	Reviewed-by: Mike McGowen <mike.mcgowen@microchip.com>
	Reviewed-by: Scott Benesh <scott.benesh@microchip.com>
	Reviewed-by: Scott Teel <scott.teel@microchip.com>
	Signed-off-by: Kevin Barnett <kevin.barnett@microchip.com>
	Signed-off-by: Don Brace <don.brace@microchip.com>
	Signed-off-by: Martin K. Petersen <martin.petersen@oracle.com>
(cherry picked from commit b4dc06a9070e3ca9d18a33fe649df594832dde1a)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/scsi/smartpqi/smartpqi_init.c
diff --cc drivers/scsi/smartpqi/smartpqi_init.c
index b59ebf12bc05,8ff38e3ecd09..000000000000
--- a/drivers/scsi/smartpqi/smartpqi_init.c
+++ b/drivers/scsi/smartpqi/smartpqi_init.c
@@@ -5578,12 -5581,48 +5578,53 @@@ static void pqi_aio_io_complete(struct 
  	pqi_scsi_done(scmd);
  }
  
++<<<<<<< HEAD
++=======
+ static inline bool pqi_is_io_high_priority(struct pqi_ctrl_info *ctrl_info,
+ 	struct pqi_scsi_dev *device, struct scsi_cmnd *scmd)
+ {
+ 	bool io_high_prio;
+ 	int priority_class;
+ 
+ 	io_high_prio = false;
+ 
+ 	if (device->ncq_prio_enable) {
+ 		priority_class =
+ 			IOPRIO_PRIO_CLASS(req_get_ioprio(scsi_cmd_to_rq(scmd)));
+ 		if (priority_class == IOPRIO_CLASS_RT) {
+ 			/* Set NCQ priority for read/write commands. */
+ 			switch (scmd->cmnd[0]) {
+ 			case WRITE_16:
+ 			case READ_16:
+ 			case WRITE_12:
+ 			case READ_12:
+ 			case WRITE_10:
+ 			case READ_10:
+ 			case WRITE_6:
+ 			case READ_6:
+ 				io_high_prio = true;
+ 				break;
+ 			}
+ 		}
+ 	}
+ 
+ 	return io_high_prio;
+ }
+ 
++>>>>>>> b4dc06a9070e (scsi: smartpqi: Fix a name typo and cleanup code)
  static inline int pqi_aio_submit_scsi_cmd(struct pqi_ctrl_info *ctrl_info,
  	struct pqi_scsi_dev *device, struct scsi_cmnd *scmd,
  	struct pqi_queue_group *queue_group)
  {
++<<<<<<< HEAD
++=======
+ 	bool io_high_prio;
+ 
+ 	io_high_prio = pqi_is_io_high_priority(ctrl_info, device, scmd);
+ 
++>>>>>>> b4dc06a9070e (scsi: smartpqi: Fix a name typo and cleanup code)
  	return pqi_aio_submit_io(ctrl_info, scmd, device->aio_handle,
 -		scmd->cmnd, scmd->cmd_len, queue_group, NULL,
 -		false, io_high_prio);
 +		scmd->cmnd, scmd->cmd_len, queue_group, NULL, false);
  }
  
  static int pqi_aio_submit_io(struct pqi_ctrl_info *ctrl_info,
* Unmerged path drivers/scsi/smartpqi/smartpqi_init.c
