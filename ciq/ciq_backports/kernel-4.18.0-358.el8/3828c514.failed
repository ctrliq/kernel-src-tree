mptcp: Make mptcp_pm_nl_mp_prio_send_ack() static

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-358.el8
commit-author Mat Martineau <mathew.j.martineau@linux.intel.com>
commit 3828c514726fce7d97063155c4749eafefd9fbd2
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-358.el8/3828c514.failed

This function is only used within pm_netlink.c now.

Fixes: 067065422fcd ("mptcp: add the outgoing MP_PRIO support")
	Reviewed-by: Matthieu Baerts <matthieu.baerts@tessares.net>
	Signed-off-by: Mat Martineau <mathew.j.martineau@linux.intel.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 3828c514726fce7d97063155c4749eafefd9fbd2)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/mptcp/protocol.h
diff --cc net/mptcp/protocol.h
index b81cfed9207e,284fdcec067e..000000000000
--- a/net/mptcp/protocol.h
+++ b/net/mptcp/protocol.h
@@@ -683,9 -736,7 +683,13 @@@ void mptcp_pm_nl_addr_send_ack(struct m
  void mptcp_pm_rm_addr_received(struct mptcp_sock *msk,
  			       const struct mptcp_rm_list *rm_list);
  void mptcp_pm_mp_prio_received(struct sock *sk, u8 bkup);
++<<<<<<< HEAD
 +int mptcp_pm_nl_mp_prio_send_ack(struct mptcp_sock *msk,
 +				 struct mptcp_addr_info *addr,
 +				 u8 bkup);
++=======
+ void mptcp_pm_mp_fail_received(struct sock *sk, u64 fail_seq);
++>>>>>>> 3828c514726f (mptcp: Make mptcp_pm_nl_mp_prio_send_ack() static)
  void mptcp_pm_free_anno_list(struct mptcp_sock *msk);
  bool mptcp_pm_sport_in_anno_list(struct mptcp_sock *msk, const struct sock *sk);
  struct mptcp_pm_add_entry *
diff --git a/net/mptcp/pm_netlink.c b/net/mptcp/pm_netlink.c
index 41d1ed7a0e8c..54de1fa7d020 100644
--- a/net/mptcp/pm_netlink.c
+++ b/net/mptcp/pm_netlink.c
@@ -552,9 +552,9 @@ void mptcp_pm_nl_addr_send_ack(struct mptcp_sock *msk)
 	}
 }
 
-int mptcp_pm_nl_mp_prio_send_ack(struct mptcp_sock *msk,
-				 struct mptcp_addr_info *addr,
-				 u8 bkup)
+static int mptcp_pm_nl_mp_prio_send_ack(struct mptcp_sock *msk,
+					struct mptcp_addr_info *addr,
+					u8 bkup)
 {
 	struct mptcp_subflow_context *subflow;
 
* Unmerged path net/mptcp/protocol.h
