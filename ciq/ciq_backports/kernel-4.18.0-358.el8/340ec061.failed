iommu/amd: Remove redundant assignment of err

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-358.el8
commit-author Shaokun Zhang <zhangshaokun@hisilicon.com>
commit 340ec061f76502befea92d9913886991a60cf15e
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-358.el8/340ec061.failed

'err' will be initialized and cleanup the redundant initialization.

	Cc: Joerg Roedel <joro@8bytes.org>
	Signed-off-by: Shaokun Zhang <zhangshaokun@hisilicon.com>
Link: https://lore.kernel.org/r/1621395447-34738-1-git-send-email-zhangshaokun@hisilicon.com
	Signed-off-by: Joerg Roedel <jroedel@suse.de>
(cherry picked from commit 340ec061f76502befea92d9913886991a60cf15e)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/iommu/amd/iommu.c
diff --cc drivers/iommu/amd/iommu.c
index 0377ee5135b7,b1fbf2c83df5..000000000000
--- a/drivers/iommu/amd/iommu.c
+++ b/drivers/iommu/amd/iommu.c
@@@ -1791,13 -1772,22 +1791,17 @@@ void amd_iommu_domain_update(struct pro
  	amd_iommu_domain_flush_complete(domain);
  }
  
 -static void __init amd_iommu_init_dma_ops(void)
 -{
 -	swiotlb = (iommu_default_passthrough() || sme_me_mask) ? 1 : 0;
 -
 -	if (amd_iommu_unmap_flush)
 -		pr_info("IO/TLB flush on unmap enabled\n");
 -	else
 -		pr_info("Lazy IO/TLB flushing enabled\n");
 -	iommu_set_dma_strict(amd_iommu_unmap_flush);
 -}
 -
  int __init amd_iommu_init_api(void)
  {
++<<<<<<< HEAD
 +	int ret, err = 0;
++=======
+ 	int err;
++>>>>>>> 340ec061f765 (iommu/amd: Remove redundant assignment of err)
  
 -	amd_iommu_init_dma_ops();
 +	ret = iova_cache_get();
 +	if (ret)
 +		return ret;
  
  	err = bus_set_iommu(&pci_bus_type, &amd_iommu_ops);
  	if (err)
* Unmerged path drivers/iommu/amd/iommu.c
