Bluetooth: Replace BT_DBG with bt_dev_dbg in HCI request

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-358.el8
commit-author Howard Chung <howardchung@google.com>
commit 22fbcfc54705213fa358bcc1349db8bbdf398ddc
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-358.el8/22fbcfc5.failed

This replaces the BT_DBG function to bt_dev_dbg as it is cleaner to show
the controller index in the debug message.

	Signed-off-by: Howard Chung <howardchung@google.com>
	Signed-off-by: Marcel Holtmann <marcel@holtmann.org>
(cherry picked from commit 22fbcfc54705213fa358bcc1349db8bbdf398ddc)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/bluetooth/hci_request.c
diff --cc net/bluetooth/hci_request.c
index c5e7fb593754,048d4db9d4ea..000000000000
--- a/net/bluetooth/hci_request.c
+++ b/net/bluetooth/hci_request.c
@@@ -417,11 -413,15 +417,17 @@@ static void __hci_update_background_sca
  	 */
  	hci_discovery_filter_clear(hdev);
  
++<<<<<<< HEAD
++=======
+ 	bt_dev_dbg(hdev, "ADV monitoring is %s",
+ 		   hci_is_adv_monitoring(hdev) ? "on" : "off");
+ 
++>>>>>>> 22fbcfc54705 (Bluetooth: Replace BT_DBG with bt_dev_dbg in HCI request)
  	if (list_empty(&hdev->pend_le_conns) &&
 -	    list_empty(&hdev->pend_le_reports) &&
 -	    !hci_is_adv_monitoring(hdev)) {
 +	    list_empty(&hdev->pend_le_reports)) {
  		/* If there is no pending LE connections or devices
 -		 * to be scanned for or no ADV monitors, we should stop the
 -		 * background scanning.
 +		 * to be scanned for, we should stop the background
 +		 * scanning.
  		 */
  
  		/* If controller is not scanning we are done. */
* Unmerged path net/bluetooth/hci_request.c
