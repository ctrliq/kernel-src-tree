bridge: Use PTR_ERR_OR_ZERO instead if(IS_ERR(...)) + PTR_ERR

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-358.el8
commit-author Jiapeng Zhong <abaci-bugfix@linux.alibaba.com>
commit 8d21c882aba8004bb996270589c17ba71e100a16
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-358.el8/8d21c882.failed

coccicheck suggested using PTR_ERR_OR_ZERO() and looking at the code.

Fix the following coccicheck warnings:

./net/bridge/br_multicast.c:1295:7-13: WARNING: PTR_ERR_OR_ZERO can be
used.

	Reported-by: Abaci <abaci@linux.alibaba.com>
	Signed-off-by: Jiapeng Zhong <abaci-bugfix@linux.alibaba.com>
	Acked-by: Nikolay Aleksandrov <nikolay@nvidia.com>
Link: https://lore.kernel.org/r/1611542381-91178-1-git-send-email-abaci-bugfix@linux.alibaba.com
	Signed-off-by: Jakub Kicinski <kuba@kernel.org>
(cherry picked from commit 8d21c882aba8004bb996270589c17ba71e100a16)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/bridge/br_multicast.c
diff --cc net/bridge/br_multicast.c
index 4eaac3b4a6ea,3aa2833f60c7..000000000000
--- a/net/bridge/br_multicast.c
+++ b/net/bridge/br_multicast.c
@@@ -892,10 -1283,26 +892,28 @@@ found
  		mod_timer(&p->timer, now + br->multicast_membership_interval);
  
  out:
 -	return p;
 -}
 +	err = 0;
  
++<<<<<<< HEAD
 +err:
++=======
+ static int br_multicast_add_group(struct net_bridge *br,
+ 				  struct net_bridge_port *port,
+ 				  struct br_ip *group,
+ 				  const unsigned char *src,
+ 				  u8 filter_mode,
+ 				  bool igmpv2_mldv1)
+ {
+ 	struct net_bridge_port_group *pg;
+ 	int err;
+ 
+ 	spin_lock(&br->multicast_lock);
+ 	pg = __br_multicast_add_group(br, port, group, src, filter_mode,
+ 				      igmpv2_mldv1, false);
+ 	/* NULL is considered valid for host joined groups */
+ 	err = PTR_ERR_OR_ZERO(pg);
++>>>>>>> 8d21c882aba8 (bridge: Use PTR_ERR_OR_ZERO instead if(IS_ERR(...)) + PTR_ERR)
  	spin_unlock(&br->multicast_lock);
 -
  	return err;
  }
  
* Unmerged path net/bridge/br_multicast.c
