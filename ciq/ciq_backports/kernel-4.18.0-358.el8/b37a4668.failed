netdevice: add the case if dev is NULL

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-358.el8
commit-author Yajun Deng <yajun.deng@linux.dev>
commit b37a466837393af72fe8bcb8f1436410f3f173f3
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-358.el8/b37a4668.failed

Add the case if dev is NULL in dev_{put, hold}, so the caller doesn't
need to care whether dev is NULL or not.

	Signed-off-by: Yajun Deng <yajun.deng@linux.dev>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit b37a466837393af72fe8bcb8f1436410f3f173f3)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	include/linux/netdevice.h
diff --cc include/linux/netdevice.h
index 9aa82973cdf2,135c943699d0..000000000000
--- a/include/linux/netdevice.h
+++ b/include/linux/netdevice.h
@@@ -4100,7 -4143,13 +4100,17 @@@ void netdev_run_todo(void)
   */
  static inline void dev_put(struct net_device *dev)
  {
++<<<<<<< HEAD
 +	this_cpu_dec(*dev->pcpu_refcnt);
++=======
+ 	if (dev) {
+ #ifdef CONFIG_PCPU_DEV_REFCNT
+ 		this_cpu_dec(*dev->pcpu_refcnt);
+ #else
+ 		refcount_dec(&dev->dev_refcnt);
+ #endif
+ 	}
++>>>>>>> b37a46683739 (netdevice: add the case if dev is NULL)
  }
  
  /**
@@@ -4111,7 -4160,13 +4121,17 @@@
   */
  static inline void dev_hold(struct net_device *dev)
  {
++<<<<<<< HEAD
 +	this_cpu_inc(*dev->pcpu_refcnt);
++=======
+ 	if (dev) {
+ #ifdef CONFIG_PCPU_DEV_REFCNT
+ 		this_cpu_inc(*dev->pcpu_refcnt);
+ #else
+ 		refcount_inc(&dev->dev_refcnt);
+ #endif
+ 	}
++>>>>>>> b37a46683739 (netdevice: add the case if dev is NULL)
  }
  
  /* Carrier loss detection, dial on demand. The functions netif_carrier_on
* Unmerged path include/linux/netdevice.h
