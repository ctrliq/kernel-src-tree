nfsd: refactor set_client

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-358.el8
commit-author J. Bruce Fields <bfields@redhat.com>
commit 7950b5316e40d99dcb85ab81a2d1dbb913d7c1c8
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-358.el8/7950b531.failed

This'll be useful elsewhere.

	Signed-off-by: J. Bruce Fields <bfields@redhat.com>
	Signed-off-by: Chuck Lever <chuck.lever@oracle.com>
(cherry picked from commit 7950b5316e40d99dcb85ab81a2d1dbb913d7c1c8)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/nfsd/nfs4state.c
diff --cc fs/nfsd/nfs4state.c
index 1988369d20ec,c74bf3b5b0de..000000000000
--- a/fs/nfsd/nfs4state.c
+++ b/fs/nfsd/nfs4state.c
@@@ -4580,15 -4633,26 +4580,29 @@@ static __be32 nfsd4_check_seqid(struct 
  	return nfserr_bad_seqid;
  }
  
++<<<<<<< HEAD
 +static __be32 lookup_clientid(clientid_t *clid,
- 		struct nfsd4_compound_state *cstate,
- 		struct nfsd_net *nn)
++=======
+ static struct nfs4_client *lookup_clientid(clientid_t *clid, bool sessions,
+ 						struct nfsd_net *nn)
  {
  	struct nfs4_client *found;
  
+ 	spin_lock(&nn->client_lock);
+ 	found = find_confirmed_client(clid, sessions, nn);
+ 	if (found)
+ 		atomic_inc(&found->cl_rpc_users);
+ 	spin_unlock(&nn->client_lock);
+ 	return found;
+ }
+ 
+ static __be32 set_client(clientid_t *clid,
++>>>>>>> 7950b5316e40 (nfsd: refactor set_client)
+ 		struct nfsd4_compound_state *cstate,
 -		struct nfsd_net *nn,
 -		bool sessions)
++		struct nfsd_net *nn)
+ {
  	if (cstate->clp) {
- 		found = cstate->clp;
- 		if (!same_clid(&found->cl_clientid, clid))
+ 		if (!same_clid(&cstate->clp->cl_clientid, clid))
  			return nfserr_stale_clientid;
  		return nfs_ok;
  	}
@@@ -4602,17 -4664,9 +4614,16 @@@
  	 * will be false.
  	 */
  	WARN_ON_ONCE(cstate->session);
++<<<<<<< HEAD
 +	spin_lock(&nn->client_lock);
 +	found = find_confirmed_client(clid, false, nn);
 +	if (!found) {
 +		spin_unlock(&nn->client_lock);
++=======
+ 	cstate->clp = lookup_clientid(clid, sessions, nn);
+ 	if (!cstate->clp)
++>>>>>>> 7950b5316e40 (nfsd: refactor set_client)
  		return nfserr_expired;
- 	}
- 	atomic_inc(&found->cl_rpc_users);
- 	spin_unlock(&nn->client_lock);
- 
- 	/* Cache the nfs4_client in cstate! */
- 	cstate->clp = found;
  	return nfs_ok;
  }
  
* Unmerged path fs/nfsd/nfs4state.c
