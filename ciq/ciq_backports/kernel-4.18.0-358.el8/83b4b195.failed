Bluetooth: smp: Use the correct print format

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-358.el8
commit-author Kai Ye <yekai13@huawei.com>
commit 83b4b19551411c83bbcf677718ab5d9f60d982f6
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-358.el8/83b4b195.failed

According to Documentation/core-api/printk-formats.rst,
Use the correct print format. Printing an unsigned int value should use %u
instead of %d. Otherwise printk() might end up displaying negative numbers.

	Signed-off-by: Kai Ye <yekai13@huawei.com>
	Signed-off-by: Marcel Holtmann <marcel@holtmann.org>
(cherry picked from commit 83b4b19551411c83bbcf677718ab5d9f60d982f6)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/bluetooth/smp.c
diff --cc net/bluetooth/smp.c
index ca95c370f679,b9413a78993a..000000000000
--- a/net/bluetooth/smp.c
+++ b/net/bluetooth/smp.c
@@@ -876,7 -859,8 +876,12 @@@ static int tk_request(struct l2cap_con
  	memset(smp->tk, 0, sizeof(smp->tk));
  	clear_bit(SMP_FLAG_TK_VALID, &smp->flags);
  
++<<<<<<< HEAD
 +	BT_DBG("tk_request: auth:%d lcl:%d rem:%d", auth, local_io, remote_io);
++=======
+ 	bt_dev_dbg(hcon->hdev, "auth:%u lcl:%u rem:%u", auth, local_io,
+ 		   remote_io);
++>>>>>>> 83b4b1955141 (Bluetooth: smp: Use the correct print format)
  
  	/* If neither side wants MITM, either "just" confirm an incoming
  	 * request or use just-works for outgoing ones. The JUST_CFM
@@@ -941,7 -925,7 +946,11 @@@
  		get_random_bytes(&passkey, sizeof(passkey));
  		passkey %= 1000000;
  		put_unaligned_le32(passkey, smp->tk);
++<<<<<<< HEAD
 +		BT_DBG("PassKey: %d", passkey);
++=======
+ 		bt_dev_dbg(hcon->hdev, "PassKey: %u", passkey);
++>>>>>>> 83b4b1955141 (Bluetooth: smp: Use the correct print format)
  		set_bit(SMP_FLAG_TK_VALID, &smp->flags);
  	}
  
@@@ -1679,7 -1655,7 +1688,11 @@@ int smp_user_confirm_reply(struct hci_c
  	case MGMT_OP_USER_PASSKEY_REPLY:
  		value = le32_to_cpu(passkey);
  		memset(smp->tk, 0, sizeof(smp->tk));
++<<<<<<< HEAD
 +		BT_DBG("PassKey: %d", value);
++=======
+ 		bt_dev_dbg(conn->hcon->hdev, "PassKey: %u", value);
++>>>>>>> 83b4b1955141 (Bluetooth: smp: Use the correct print format)
  		put_unaligned_le32(value, smp->tk);
  		fallthrough;
  	case MGMT_OP_USER_CONFIRM_REPLY:
* Unmerged path net/bluetooth/smp.c
