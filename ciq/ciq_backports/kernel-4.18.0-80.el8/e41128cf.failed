block: remove set but not used variable 'et'

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-80.el8
commit-author YueHaibing <yuehaibing@huawei.com>
commit e41128cfd9389b889e91097b8ca61574d1e71482
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-80.el8/e41128cf.failed

Fixes gcc '-Wunused-but-set-variable' warning:

block/blk-ioc.c: In function 'put_io_context_active':
block/blk-ioc.c:174:24: warning:
 variable 'et' set but not used [-Wunused-but-set-variable]

It not used any more after commit
a1ce35fa4985 ("block: remove dead elevator code")

	Signed-off-by: YueHaibing <yuehaibing@huawei.com>
	Signed-off-by: Jens Axboe <axboe@kernel.dk>
(cherry picked from commit e41128cfd9389b889e91097b8ca61574d1e71482)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	block/blk-ioc.c
diff --cc block/blk-ioc.c
index 01580f88fcb3,56755ad5ac88..000000000000
--- a/block/blk-ioc.c
+++ b/block/blk-ioc.c
@@@ -193,19 -189,7 +192,23 @@@ retry
  		if (icq->flags & ICQ_EXITED)
  			continue;
  
++<<<<<<< HEAD
 +		et = icq->q->elevator->type;
 +		if (et->uses_mq) {
 +			ioc_exit_icq(icq);
 +		} else {
 +			if (spin_trylock(icq->q->queue_lock)) {
 +				ioc_exit_icq(icq);
 +				spin_unlock(icq->q->queue_lock);
 +			} else {
 +				spin_unlock_irqrestore(&ioc->lock, flags);
 +				cpu_relax();
 +				goto retry;
 +			}
 +		}
++=======
+ 		ioc_exit_icq(icq);
++>>>>>>> e41128cfd938 (block: remove set but not used variable 'et')
  	}
  	spin_unlock_irqrestore(&ioc->lock, flags);
  
* Unmerged path block/blk-ioc.c
