xfs: remove [cm]time update from reflink calls

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-80.el8
commit-author Darrick J. Wong <darrick.wong@oracle.com>
commit bf4a1fcf0bc18d52cf0fce6571d6f327ab5eaf22
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-80.el8/bf4a1fcf.failed

Now that the vfs remap helper dirties the inode [cm]time for us, xfs no
longer needs to do that on its own.

	Signed-off-by: Darrick J. Wong <darrick.wong@oracle.com>
	Reviewed-by: Dave Chinner <dchinner@redhat.com>
	Signed-off-by: Dave Chinner <david@fromorbit.com>

(cherry picked from commit bf4a1fcf0bc18d52cf0fce6571d6f327ab5eaf22)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/xfs/xfs_reflink.c
diff --cc fs/xfs/xfs_reflink.c
index c17089911875,e72218477bf2..000000000000
--- a/fs/xfs/xfs_reflink.c
+++ b/fs/xfs/xfs_reflink.c
@@@ -927,7 -927,7 +927,11 @@@ xfs_reflink_update_dest
  	struct xfs_trans	*tp;
  	int			error;
  
++<<<<<<< HEAD
 +	if (is_dedupe && newlen <= i_size_read(VFS_I(dest)) && cowextsize == 0)
++=======
+ 	if (newlen <= i_size_read(VFS_I(dest)) && cowextsize == 0)
++>>>>>>> bf4a1fcf0bc1 (xfs: remove [cm]time update from reflink calls)
  		return 0;
  
  	error = xfs_trans_alloc(mp, &M_RES(mp)->tr_ichange, 0, 0, 0, &tp);
@@@ -948,10 -948,6 +952,13 @@@
  		dest->i_d.di_flags2 |= XFS_DIFLAG2_COWEXTSIZE;
  	}
  
++<<<<<<< HEAD
 +	if (!is_dedupe) {
 +		xfs_trans_ichgtime(tp, dest,
 +				   XFS_ICHGTIME_MOD | XFS_ICHGTIME_CHG);
 +	}
++=======
++>>>>>>> bf4a1fcf0bc1 (xfs: remove [cm]time update from reflink calls)
  	xfs_trans_log_inode(tp, dest, XFS_ILOG_CORE);
  
  	error = xfs_trans_commit(tp);
* Unmerged path fs/xfs/xfs_reflink.c
