net/sched: act_police: use per-cpu counters

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-80.el8
Rebuild_CHGLOG: - [net] sched: act_police: use per-cpu counters (Ivan Vecera) [1638022]
Rebuild_FUZZ: 95.12%
commit-author Davide Caratti <dcaratti@redhat.com>
commit 93be42f9173bbc133bd72915b658394308437d00
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-80.el8/93be42f9.failed

use per-CPU counters, instead of sharing a single set of stats with all
cores. This removes the need of using spinlock when statistics are read
or updated.

	Signed-off-by: Davide Caratti <dcaratti@redhat.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 93be42f9173bbc133bd72915b658394308437d00)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/sched/act_police.c
diff --cc net/sched/act_police.c
index 0b82775575a6,965a48d3ec35..000000000000
--- a/net/sched/act_police.c
+++ b/net/sched/act_police.c
@@@ -107,14 -110,15 +107,20 @@@ static int tcf_police_init(struct net *
  
  	if (!exists) {
  		ret = tcf_idr_create(tn, parm->index, NULL, a,
++<<<<<<< HEAD
 +				     &act_police_ops, bind, false);
 +		if (ret)
++=======
+ 				     &act_police_ops, bind, true);
+ 		if (ret) {
+ 			tcf_idr_cleanup(tn, parm->index);
++>>>>>>> 93be42f9173b (net/sched: act_police: use per-cpu counters)
  			return ret;
 -		}
  		ret = ACT_P_CREATED;
 -	} else if (!ovr) {
 +	} else {
  		tcf_idr_release(*a, bind);
 -		return -EEXIST;
 +		if (!ovr)
 +			return -EEXIST;
  	}
  
  	police = to_police(*a);
* Unmerged path net/sched/act_police.c
