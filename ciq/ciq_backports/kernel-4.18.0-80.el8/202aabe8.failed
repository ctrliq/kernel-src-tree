xdp: fix uninitialized 'err' variable

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-80.el8
commit-author Jakub Kicinski <jakub.kicinski@netronome.com>
commit 202aabe84a8fd809e8f401bc05e20f35a5102ece
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-80.el8/202aabe8.failed

Smatch caught an uninitialized variable error which GCC seems
to miss.

Fixes: a25717d2b604 ("xdp: support simultaneous driver and hw XDP attachment")
	Signed-off-by: Jakub Kicinski <jakub.kicinski@netronome.com>
	Acked-by: Daniel Borkmann <daniel@iogearbox.net>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 202aabe84a8fd809e8f401bc05e20f35a5102ece)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/core/rtnetlink.c
diff --cc net/core/rtnetlink.c
index 9dc7e359ad09,92b6fa5d5f6e..000000000000
--- a/net/core/rtnetlink.c
+++ b/net/core/rtnetlink.c
@@@ -1392,7 -1412,21 +1392,25 @@@ static int rtnl_xdp_fill(struct sk_buf
  	if (!xdp)
  		return -EMSGSIZE;
  
++<<<<<<< HEAD
 +	mode = rtnl_xdp_attached_mode(dev, &prog_id);
++=======
+ 	prog_id = 0;
+ 	mode = XDP_ATTACHED_NONE;
+ 	err = rtnl_xdp_report_one(skb, dev, &prog_id, &mode, XDP_ATTACHED_SKB,
+ 				  IFLA_XDP_SKB_PROG_ID, rtnl_xdp_prog_skb);
+ 	if (err)
+ 		goto err_cancel;
+ 	err = rtnl_xdp_report_one(skb, dev, &prog_id, &mode, XDP_ATTACHED_DRV,
+ 				  IFLA_XDP_DRV_PROG_ID, rtnl_xdp_prog_drv);
+ 	if (err)
+ 		goto err_cancel;
+ 	err = rtnl_xdp_report_one(skb, dev, &prog_id, &mode, XDP_ATTACHED_HW,
+ 				  IFLA_XDP_HW_PROG_ID, rtnl_xdp_prog_hw);
+ 	if (err)
+ 		goto err_cancel;
+ 
++>>>>>>> 202aabe84a8f (xdp: fix uninitialized 'err' variable)
  	err = nla_put_u8(skb, IFLA_XDP_ATTACHED, mode);
  	if (err)
  		goto err_cancel;
* Unmerged path net/core/rtnetlink.c
