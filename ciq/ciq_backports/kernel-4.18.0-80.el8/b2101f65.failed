ide: don't acquire queue lock in ide_pm_execute_rq

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-80.el8
commit-author Christoph Hellwig <hch@lst.de>
commit b2101f655f8f6bd510154364e24d1cdba037133f
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-80.el8/b2101f65.failed

There is nothing we can synchronize against over a call to
blk_queue_dying.

	Reviewed-by: Omar Sandoval <osandov@fb.com>
	Signed-off-by: Christoph Hellwig <hch@lst.de>
	Signed-off-by: Jens Axboe <axboe@kernel.dk>
(cherry picked from commit b2101f655f8f6bd510154364e24d1cdba037133f)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/ide/ide-pm.c
diff --cc drivers/ide/ide-pm.c
index a8c53c98252d,56690f523100..000000000000
--- a/drivers/ide/ide-pm.c
+++ b/drivers/ide/ide-pm.c
@@@ -44,7 -44,6 +44,8 @@@ static int ide_pm_execute_rq(struct req
  {
  	struct request_queue *q = rq->q;
  
++<<<<<<< HEAD
 +	spin_lock_irq(q->queue_lock);
  	if (unlikely(blk_queue_dying(q))) {
  		rq->rq_flags |= RQF_QUIET;
  		scsi_req(rq)->result = -ENXIO;
@@@ -52,7 -50,6 +53,15 @@@
  		blk_mq_end_request(rq, BLK_STS_OK);
  		return -ENXIO;
  	}
 +	spin_unlock_irq(q->queue_lock);
++=======
++	if (unlikely(blk_queue_dying(q))) {
++		rq->rq_flags |= RQF_QUIET;
++		scsi_req(rq)->result = -ENXIO;
++		blk_mq_end_request(rq, BLK_STS_OK);
++		return -ENXIO;
++	}
++>>>>>>> b2101f655f8f (ide: don't acquire queue lock in ide_pm_execute_rq)
  	blk_execute_rq(q, NULL, rq, true);
  
  	return scsi_req(rq)->result ? -EIO : 0;
* Unmerged path drivers/ide/ide-pm.c
