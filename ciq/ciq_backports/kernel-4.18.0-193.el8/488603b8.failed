sched/core: Don't skip remote tick for idle CPUs

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-193.el8
commit-author Scott Wood <swood@redhat.com>
commit 488603b815a7514c7009e6fc339d74ed4a30f343
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-193.el8/488603b8.failed

This will be used in the next patch to get a loadavg update from
nohz cpus.  The delta check is skipped because idle_sched_class
doesn't update se.exec_start.

	Signed-off-by: Scott Wood <swood@redhat.com>
	Signed-off-by: Peter Zijlstra (Intel) <peterz@infradead.org>
	Signed-off-by: Ingo Molnar <mingo@kernel.org>
Link: https://lkml.kernel.org/r/1578736419-14628-2-git-send-email-swood@redhat.com
(cherry picked from commit 488603b815a7514c7009e6fc339d74ed4a30f343)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	kernel/sched/core.c
diff --cc kernel/sched/core.c
index aa8fe44c1242,cf8b33dc4513..000000000000
--- a/kernel/sched/core.c
+++ b/kernel/sched/core.c
@@@ -3084,7 -3674,7 +3084,11 @@@ static void sched_tick_remote(struct wo
  
  	rq_lock_irq(rq, &rf);
  	curr = rq->curr;
++<<<<<<< HEAD
 +	if (is_idle_task(curr))
++=======
+ 	if (cpu_is_offline(cpu))
++>>>>>>> 488603b815a7 (sched/core: Don't skip remote tick for idle CPUs)
  		goto out_unlock;
  
  	update_rq_clock(rq);
* Unmerged path kernel/sched/core.c
