cpuidle-haltpoll: do not set an owner to allow modunload

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-193.el8
commit-author Joao Martins <joao.m.martins@oracle.com>
commit 472f263660832b90e53bede2020f68cd14f8b76c
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-193.el8/472f2636.failed

cpuidle-haltpoll can be built as a module to allow optional late load.
Given we are setting @owner to THIS_MODULE, cpuidle will attempt to grab a
module reference every time a cpuidle_device is registered -- so
essentially all online cpus get a reference.

This prevents for the module to be unloaded later, which makes the
module_exit callback entirely unused. Thus remove the @owner and allow
module to be unloaded.

Fixes: fa86ee90eb11 ("add cpuidle-haltpoll driver")
	Signed-off-by: Joao Martins <joao.m.martins@oracle.com>
	Signed-off-by: Rafael J. Wysocki <rafael.j.wysocki@intel.com>
(cherry picked from commit 472f263660832b90e53bede2020f68cd14f8b76c)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/cpuidle/cpuidle-haltpoll.c
diff --cc drivers/cpuidle/cpuidle-haltpoll.c
index 0a34979b43b6,49a65c6fe91e..000000000000
--- a/drivers/cpuidle/cpuidle-haltpoll.c
+++ b/drivers/cpuidle/cpuidle-haltpoll.c
@@@ -29,7 -34,7 +29,11 @@@ static int default_enter_idle(struct cp
  
  static struct cpuidle_driver haltpoll_driver = {
  	.name = "haltpoll",
++<<<<<<< HEAD
 +	.owner = THIS_MODULE,
++=======
+ 	.governor = "haltpoll",
++>>>>>>> 472f26366083 (cpuidle-haltpoll: do not set an owner to allow modunload)
  	.states = {
  		{ /* entry 0 is for polling */ },
  		{
* Unmerged path drivers/cpuidle/cpuidle-haltpoll.c
