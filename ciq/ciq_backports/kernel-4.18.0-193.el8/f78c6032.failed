net: fix two coding style issues

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-193.el8
Rebuild_CHGLOG: - [net] fix two coding style issues (Petr Oros) [1751273]
Rebuild_FUZZ: 91.53%
commit-author Michal Kubecek <mkubecek@suse.cz>
commit f78c6032c4cb89b408190afd4feb61ff4461a114
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-193.el8/f78c6032.failed

This is a simple cleanup addressing two coding style issues found by
checkpatch.pl in an earlier patch. It's submitted as a separate patch to
keep the original patch as it was generated by spatch.

	Signed-off-by: Michal Kubecek <mkubecek@suse.cz>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit f78c6032c4cb89b408190afd4feb61ff4461a114)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/bridge/br_netlink.c
#	net/decnet/dn_table.c
diff --cc net/bridge/br_netlink.c
index 0b3ccef34ba8,348ddb6d09bb..000000000000
--- a/net/bridge/br_netlink.c
+++ b/net/bridge/br_netlink.c
@@@ -403,9 -413,9 +403,14 @@@ static int br_fill_ifinfo(struct sk_buf
  		goto nla_put_failure;
  
  	if (event == RTM_NEWLINK && port) {
++<<<<<<< HEAD
 +		struct nlattr *nest
 +			= nla_nest_start(skb, IFLA_PROTINFO | NLA_F_NESTED);
++=======
+ 		struct nlattr *nest;
++>>>>>>> f78c6032c4cb (net: fix two coding style issues)
  
+ 		nest = nla_nest_start(skb, IFLA_PROTINFO);
  		if (nest == NULL || br_port_fill_attrs(skb, port) < 0)
  			goto nla_put_failure;
  		nla_nest_end(skb, nest);
diff --cc net/decnet/dn_table.c
index f0710b5d037d,33fefb0aebca..000000000000
--- a/net/decnet/dn_table.c
+++ b/net/decnet/dn_table.c
@@@ -348,7 -348,8 +348,12 @@@ static int dn_fib_dump_info(struct sk_b
  		struct rtnexthop *nhp;
  		struct nlattr *mp_head;
  
++<<<<<<< HEAD
 +		if (!(mp_head = nla_nest_start(skb, RTA_MULTIPATH)))
++=======
+ 		mp_head = nla_nest_start_noflag(skb, RTA_MULTIPATH);
+ 		if (!mp_head)
++>>>>>>> f78c6032c4cb (net: fix two coding style issues)
  			goto errout;
  
  		for_nexthops(fi) {
* Unmerged path net/bridge/br_netlink.c
* Unmerged path net/decnet/dn_table.c
