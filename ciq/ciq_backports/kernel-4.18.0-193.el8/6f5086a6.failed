iommu/amd: Remove leading 0s in error log messages

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-193.el8
Rebuild_CHGLOG: - [iommu] amd: Remove leading 0s in error log messages (Jerry Snitselaar) [1742234]
Rebuild_FUZZ: 93.62%
commit-author Joerg Roedel <jroedel@suse.de>
commit 6f5086a6e3383ae6a1b08a88836fcfda7d171ca4
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-193.el8/6f5086a6.failed

Remove the leading 0s in the address field of the error log
messages.

	Signed-off-by: Joerg Roedel <jroedel@suse.de>
(cherry picked from commit 6f5086a6e3383ae6a1b08a88836fcfda7d171ca4)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/iommu/amd_iommu.c
diff --cc drivers/iommu/amd_iommu.c
index e65d230ea62c,c5d78c87248a..000000000000
--- a/drivers/iommu/amd_iommu.c
+++ b/drivers/iommu/amd_iommu.c
@@@ -535,10 -540,10 +535,14 @@@ static void amd_iommu_report_page_fault
  		dev_data = get_dev_data(&pdev->dev);
  
  	if (dev_data && __ratelimit(&dev_data->rs)) {
- 		dev_err(&pdev->dev, "Event logged [IO_PAGE_FAULT domain=0x%04x address=0x%016llx flags=0x%04x]\n",
+ 		dev_err(&pdev->dev, "Event logged [IO_PAGE_FAULT domain=0x%04x address=0x%llx flags=0x%04x]\n",
  			domain_id, address, flags);
  	} else if (printk_ratelimit()) {
++<<<<<<< HEAD
 +		pr_err("AMD-Vi: Event logged [IO_PAGE_FAULT device=%02x:%02x.%x domain=0x%04x address=0x%016llx flags=0x%04x]\n",
++=======
+ 		pr_err("Event logged [IO_PAGE_FAULT device=%02x:%02x.%x domain=0x%04x address=0x%llx flags=0x%04x]\n",
++>>>>>>> 6f5086a6e338 (iommu/amd: Remove leading 0s in error log messages)
  			PCI_BUS_NUM(devid), PCI_SLOT(devid), PCI_FUNC(devid),
  			domain_id, address, flags);
  	}
* Unmerged path drivers/iommu/amd_iommu.c
