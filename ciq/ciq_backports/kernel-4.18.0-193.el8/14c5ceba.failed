memremap: move from kernel/ to mm/

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-193.el8
commit-author Christoph Hellwig <hch@lst.de>
commit 14c5cebad510c2875ca525f36605b47058769670
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-193.el8/14c5ceba.failed

memremap.c implements MM functionality for ZONE_DEVICE, so it really
should be in the mm/ directory, not the kernel/ one.

Link: http://lkml.kernel.org/r/20190722094143.18387-1-hch@lst.de
	Signed-off-by: Christoph Hellwig <hch@lst.de>
	Reviewed-by: Anshuman Khandual <anshuman.khandual@arm.com>
	Acked-by: Dan Williams <dan.j.williams@intel.com>
	Signed-off-by: Andrew Morton <akpm@linux-foundation.org>
	Signed-off-by: Linus Torvalds <torvalds@linux-foundation.org>
(cherry picked from commit 14c5cebad510c2875ca525f36605b47058769670)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	mm/Makefile
diff --cc mm/Makefile
index 26ef77a3883b,d0b295c3b764..000000000000
--- a/mm/Makefile
+++ b/mm/Makefile
@@@ -103,5 -102,6 +103,10 @@@ obj-$(CONFIG_FRAME_VECTOR) += frame_vec
  obj-$(CONFIG_DEBUG_PAGE_REF) += debug_page_ref.o
  obj-$(CONFIG_HARDENED_USERCOPY) += usercopy.o
  obj-$(CONFIG_PERCPU_STATS) += percpu-stats.o
++<<<<<<< HEAD
 +obj-$(CONFIG_HMM) += hmm.o
++=======
+ obj-$(CONFIG_ZONE_DEVICE) += memremap.o
+ obj-$(CONFIG_HMM_MIRROR) += hmm.o
++>>>>>>> 14c5cebad510 (memremap: move from kernel/ to mm/)
  obj-$(CONFIG_MEMFD_CREATE) += memfd.o
diff --git a/kernel/Makefile b/kernel/Makefile
index aebf6a889edd..bccfbb9c4428 100644
--- a/kernel/Makefile
+++ b/kernel/Makefile
@@ -115,7 +115,6 @@ obj-$(CONFIG_CONTEXT_TRACKING) += context_tracking.o
 obj-$(CONFIG_TORTURE_TEST) += torture.o
 
 obj-$(CONFIG_HAS_IOMEM) += iomem.o
-obj-$(CONFIG_ZONE_DEVICE) += memremap.o
 obj-$(CONFIG_RSEQ) += rseq.o
 
 $(obj)/configs.o: $(obj)/config_data.h
* Unmerged path mm/Makefile
diff --git a/kernel/memremap.c b/mm/memremap.c
similarity index 100%
rename from kernel/memremap.c
rename to mm/memremap.c
