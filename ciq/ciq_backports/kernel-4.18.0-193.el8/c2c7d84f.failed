NFS: remove unnecessary test for IS_ERR(cred)

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-193.el8
commit-author NeilBrown <neilb@suse.com>
commit c2c7d84fd173aff121ffd45cf15277eff43348d3
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-193.el8/c2c7d84f.failed

As gte_current_cred() cannot return an error,
this test is not necessary.
It hasn't been necessary for years, but it wasn't so obvious
before.

	Reported-by: Dan Carpenter <dan.carpenter@oracle.com>
	Signed-off-by: NeilBrown <neilb@suse.com>
	Signed-off-by: Anna Schumaker <Anna.Schumaker@Netapp.com>
(cherry picked from commit c2c7d84fd173aff121ffd45cf15277eff43348d3)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/nfs/unlink.c
diff --cc fs/nfs/unlink.c
index fd61bf0fce63,79b97b3c4427..000000000000
--- a/fs/nfs/unlink.c
+++ b/fs/nfs/unlink.c
@@@ -352,12 -347,7 +352,16 @@@ nfs_async_rename(struct inode *old_dir
  		return ERR_PTR(-ENOMEM);
  	task_setup_data.callback_data = data;
  
++<<<<<<< HEAD
 +	data->cred = rpc_lookup_cred();
 +	if (IS_ERR(data->cred)) {
 +		struct rpc_task *task = ERR_CAST(data->cred);
 +		kfree(data);
 +		return task;
 +	}
++=======
+ 	data->cred = get_current_cred();
++>>>>>>> c2c7d84fd173 (NFS: remove unnecessary test for IS_ERR(cred))
  
  	msg.rpc_argp = &data->args;
  	msg.rpc_resp = &data->res;
* Unmerged path fs/nfs/unlink.c
