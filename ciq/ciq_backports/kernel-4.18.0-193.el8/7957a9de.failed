mac80211: Don't access sk_queue_head->next directly.

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-193.el8
Rebuild_CHGLOG: - [net] mac80211: Don't access sk_queue_head->next directly (Jarod Wilson) [1776606]
Rebuild_FUZZ: 99.03%
commit-author David S. Miller <davem@davemloft.net>
commit 7957a9dea8bf58b02e6ebf28f91c4bce74b5c91c
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-193.el8/7957a9de.failed

Use __skb_peek() instead.

	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 7957a9dea8bf58b02e6ebf28f91c4bce74b5c91c)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/mac80211/rx.c
diff --cc net/mac80211/rx.c
index 910263452859,a0ca27aeb732..000000000000
--- a/net/mac80211/rx.c
+++ b/net/mac80211/rx.c
@@@ -2133,11 -2089,7 +2133,15 @@@ ieee80211_reassemble_find(struct ieee80
  		    entry->last_frag + 1 != frag)
  			continue;
  
++<<<<<<< HEAD
 +#if 0 /* Not in RHEL */
  		f_skb = __skb_peek(&entry->skb_list);
 +#else
 +		f_skb = entry->skb_list.next;
 +#endif
++=======
++		f_skb = __skb_peek(&entry->skb_list);
++>>>>>>> 7957a9dea8bf (mac80211: Don't access sk_queue_head->next directly.)
  		f_hdr = (struct ieee80211_hdr *) f_skb->data;
  
  		/*
* Unmerged path net/mac80211/rx.c
