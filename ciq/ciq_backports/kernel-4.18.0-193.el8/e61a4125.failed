cpufreq: dev_pm_qos_update_request() can return 1 on success

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-193.el8
Rebuild_CHGLOG: - [cpufreq] dev_pm_qos_update_request() can return 1 on success (Al Stone) [1739729 1725581]
Rebuild_FUZZ: 91.89%
commit-author Viresh Kumar <viresh.kumar@linaro.org>
commit e61a41256edf9f425039129757af4a80b5ed8162
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-193.el8/e61a4125.failed

dev_pm_qos_update_request() can return 1 on success, so don't treat
it as an error.

Fixes: 18c49926c4bf ("cpufreq: Add QoS requests for userspace constraints")
	Signed-off-by: Viresh Kumar <viresh.kumar@linaro.org>
	Signed-off-by: Rafael J. Wysocki <rafael.j.wysocki@intel.com>
(cherry picked from commit e61a41256edf9f425039129757af4a80b5ed8162)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/cpufreq/cpufreq.c
diff --cc drivers/cpufreq/cpufreq.c
index 3bb6013f5a6f,c28ebf2810f1..000000000000
--- a/drivers/cpufreq/cpufreq.c
+++ b/drivers/cpufreq/cpufreq.c
@@@ -2437,10 -2527,9 +2437,16 @@@ static int cpufreq_boost_set_sw(int sta
  			break;
  		}
  
++<<<<<<< HEAD
 +		down_write(&policy->rwsem);
 +		policy->user_policy.max = policy->max;
 +		cpufreq_governor_limits(policy);
 +		up_write(&policy->rwsem);
++=======
+ 		ret = dev_pm_qos_update_request(policy->max_freq_req, policy->max);
+ 		if (ret < 0)
+ 			break;
++>>>>>>> e61a41256edf (cpufreq: dev_pm_qos_update_request() can return 1 on success)
  	}
  
  	return ret;
* Unmerged path drivers/cpufreq/cpufreq.c
