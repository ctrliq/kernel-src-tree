drm/amd/display: add default clocks if not able to fetch them

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-193.el8
commit-author Alex Deucher <alexander.deucher@amd.com>
commit ee9ea6d88593509f2bbfb8bd40482b21b6a7502c
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-193.el8/ee9ea6d8.failed

dm_pp_get_clock_levels_by_type needs to add the default clocks
to the powerplay case as well.  This was accidently dropped.

Fixes: b3ea88fef321de ("drm/amd/powerplay: add get_clock_by_type interface for display")
Bug: https://gitlab.freedesktop.org/drm/amd/issues/906
	Reviewed-by: Nicholas Kazlauskas <nicholas.kazlauskas@amd.com>
	Signed-off-by: Alex Deucher <alexander.deucher@amd.com>
(cherry picked from commit ee9ea6d88593509f2bbfb8bd40482b21b6a7502c)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/gpu/drm/amd/display/amdgpu_dm/amdgpu_dm_pp_smu.c
diff --cc drivers/gpu/drm/amd/display/amdgpu_dm/amdgpu_dm_pp_smu.c
index a114954d6a5b,a2e1a73f66b8..000000000000
--- a/drivers/gpu/drm/amd/display/amdgpu_dm/amdgpu_dm_pp_smu.c
+++ b/drivers/gpu/drm/amd/display/amdgpu_dm/amdgpu_dm_pp_smu.c
@@@ -307,7 -342,14 +307,18 @@@ bool dm_pp_get_clock_levels_by_type
  	if (adev->powerplay.pp_funcs && adev->powerplay.pp_funcs->get_clock_by_type) {
  		if (adev->powerplay.pp_funcs->get_clock_by_type(pp_handle,
  			dc_to_pp_clock_type(clk_type), &pp_clks)) {
++<<<<<<< HEAD
 +		/* Error in pplib. Provide default values. */
++=======
+ 			/* Error in pplib. Provide default values. */
+ 			get_default_clock_levels(clk_type, dc_clks);
+ 			return true;
+ 		}
+ 	} else if (adev->smu.ppt_funcs && adev->smu.ppt_funcs->get_clock_by_type) {
+ 		if (smu_get_clock_by_type(&adev->smu,
+ 					  dc_to_pp_clock_type(clk_type),
+ 					  &pp_clks)) {
++>>>>>>> ee9ea6d88593 (drm/amd/display: add default clocks if not able to fetch them)
  			get_default_clock_levels(clk_type, dc_clks);
  			return true;
  		}
* Unmerged path drivers/gpu/drm/amd/display/amdgpu_dm/amdgpu_dm_pp_smu.c
