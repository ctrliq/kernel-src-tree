sched/fair: Remove unneeded prototype of capacity_of()

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-193.el8
commit-author Valentin Schneider <valentin.schneider@arm.com>
commit e2abb398115e9c33f3d1e25bf6d1d08badc58b13
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-193.el8/e2abb398.failed

The prototype of that function was already hoisted up in:

  commit 3b1baa6496e6 ("sched/fair: Add 'group_misfit_task' load-balance type")

but that seems to have been missed. Get rid of the extra prototype.

	Signed-off-by: Valentin Schneider <valentin.schneider@arm.com>
	Acked-by: Quentin Perret <quentin.perret@arm.com>
	Cc: Dietmar.Eggemann@arm.com
	Cc: Linus Torvalds <torvalds@linux-foundation.org>
	Cc: Peter Zijlstra <peterz@infradead.org>
	Cc: Thomas Gleixner <tglx@linutronix.de>
	Cc: morten.rasmussen@arm.com
Fixes: 2802bf3cd936 ("sched/fair: Add over-utilization/tipping point indicator")
Link: http://lkml.kernel.org/r/20190416140621.19884-1-valentin.schneider@arm.com
	Signed-off-by: Ingo Molnar <mingo@kernel.org>
(cherry picked from commit e2abb398115e9c33f3d1e25bf6d1d08badc58b13)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	kernel/sched/fair.c
diff --cc kernel/sched/fair.c
index 6f6374cf5117,b6cc0703b850..000000000000
--- a/kernel/sched/fair.c
+++ b/kernel/sched/fair.c
@@@ -5126,6 -5114,23 +5126,26 @@@ static inline void hrtick_update(struc
  }
  #endif
  
++<<<<<<< HEAD
++=======
+ #ifdef CONFIG_SMP
+ static inline unsigned long cpu_util(int cpu);
+ 
+ static inline bool cpu_overutilized(int cpu)
+ {
+ 	return (capacity_of(cpu) * 1024) < (cpu_util(cpu) * capacity_margin);
+ }
+ 
+ static inline void update_overutilized_status(struct rq *rq)
+ {
+ 	if (!READ_ONCE(rq->rd->overutilized) && cpu_overutilized(rq->cpu))
+ 		WRITE_ONCE(rq->rd->overutilized, SG_OVERUTILIZED);
+ }
+ #else
+ static inline void update_overutilized_status(struct rq *rq) { }
+ #endif
+ 
++>>>>>>> e2abb398115e (sched/fair: Remove unneeded prototype of capacity_of())
  /*
   * The enqueue_task method is called before nr_running is
   * increased. Here we update the fair scheduling stats and
* Unmerged path kernel/sched/fair.c
