IB/mlx4: Delete unused func arg

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-193.el8
commit-author Yuval Shaia <yuval.shaia@oracle.com>
commit 8ce0048f76542a1f4d68b647c5d4a4d35e7547cf
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-193.el8/8ce0048f.failed

The function argument virt_addr is not in use - delete it.

	Signed-off-by: Yuval Shaia <yuval.shaia@oracle.com>
	Reviewed-by: Majd Dibbiny <majd@mellanox.com>
	Signed-off-by: Jason Gunthorpe <jgg@mellanox.com>
(cherry picked from commit 8ce0048f76542a1f4d68b647c5d4a4d35e7547cf)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/infiniband/hw/mlx4/mr.c
diff --cc drivers/infiniband/hw/mlx4/mr.c
index c7c85c22e4e3,753479285ce9..000000000000
--- a/drivers/infiniband/hw/mlx4/mr.c
+++ b/drivers/infiniband/hw/mlx4/mr.c
@@@ -367,9 -367,8 +367,14 @@@ end
  	return block_shift;
  }
  
++<<<<<<< HEAD
 +static struct ib_umem *mlx4_get_umem_mr(struct ib_ucontext *context, u64 start,
 +					u64 length, u64 virt_addr,
 +					int access_flags)
++=======
+ static struct ib_umem *mlx4_get_umem_mr(struct ib_udata *udata, u64 start,
+ 					u64 length, int access_flags)
++>>>>>>> 8ce0048f7654 (IB/mlx4: Delete unused func arg)
  {
  	/*
  	 * Force registering the memory as writable if the underlying pages
@@@ -415,8 -414,7 +420,12 @@@ struct ib_mr *mlx4_ib_reg_user_mr(struc
  	if (!mr)
  		return ERR_PTR(-ENOMEM);
  
++<<<<<<< HEAD
 +	mr->umem = mlx4_get_umem_mr(pd->uobject->context, start, length,
 +				    virt_addr, access_flags);
++=======
+ 	mr->umem = mlx4_get_umem_mr(udata, start, length, access_flags);
++>>>>>>> 8ce0048f7654 (IB/mlx4: Delete unused func arg)
  	if (IS_ERR(mr->umem)) {
  		err = PTR_ERR(mr->umem);
  		goto err_free;
@@@ -505,9 -503,8 +514,14 @@@ int mlx4_ib_rereg_user_mr(struct ib_mr 
  
  		mlx4_mr_rereg_mem_cleanup(dev->dev, &mmr->mmr);
  		ib_umem_release(mmr->umem);
++<<<<<<< HEAD
 +		mmr->umem =
 +			mlx4_get_umem_mr(mr->uobject->context, start, length,
 +					 virt_addr, mr_access_flags);
++=======
+ 		mmr->umem = mlx4_get_umem_mr(udata, start, length,
+ 					     mr_access_flags);
++>>>>>>> 8ce0048f7654 (IB/mlx4: Delete unused func arg)
  		if (IS_ERR(mmr->umem)) {
  			err = PTR_ERR(mmr->umem);
  			/* Prevent mlx4_ib_dereg_mr from free'ing invalid pointer */
* Unmerged path drivers/infiniband/hw/mlx4/mr.c
