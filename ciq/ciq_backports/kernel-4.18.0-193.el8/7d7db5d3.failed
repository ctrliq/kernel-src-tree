ASoC: soc-core: move soc_find_component()

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-193.el8
Rebuild_CHGLOG: - [sound] ALSA: ASoC: soc-core: move soc_find_component() (Jaroslav Kysela) [1738610]
Rebuild_FUZZ: 93.18%
commit-author Kuninori Morimoto <kuninori.morimoto.gx@renesas.com>
commit 7d7db5d3c3d849c2f877be3b4c9afa7f5b5257c2
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-193.el8/7d7db5d3.failed

move soc_find_component() next to snd_soc_is_matching_component().
This is prepare for soc_find_component() cleanup

	Signed-off-by: Kuninori Morimoto <kuninori.morimoto.gx@renesas.com>
	Signed-off-by: Mark Brown <broonie@kernel.org>
(cherry picked from commit 7d7db5d3c3d849c2f877be3b4c9afa7f5b5257c2)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	sound/soc/soc-core.c
diff --cc sound/soc/soc-core.c
index 06cc21a02d68,1b94119cfb0d..000000000000
--- a/sound/soc/soc-core.c
+++ b/sound/soc/soc-core.c
@@@ -782,22 -801,6 +801,25 @@@ static struct snd_soc_component *soc_fi
  	return NULL;
  }
  
++<<<<<<< HEAD
 +static int snd_soc_is_matching_component(
 +	const struct snd_soc_dai_link_component *dlc,
 +	struct snd_soc_component *component)
 +{
 +	struct device_node *component_of_node;
 +
 +	component_of_node = soc_component_to_node(component);
 +
 +	if (dlc->of_node && component_of_node != dlc->of_node)
 +		return 0;
 +	if (dlc->name && strcmp(component->name, dlc->name))
 +		return 0;
 +
 +	return 1;
 +}
 +
++=======
++>>>>>>> 7d7db5d3c3d8 (ASoC: soc-core: move soc_find_component())
  /**
   * snd_soc_find_dai - Find a registered DAI
   *
* Unmerged path sound/soc/soc-core.c
