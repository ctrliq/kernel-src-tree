ASoC: soc-component: fix a couple missing error assignments

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-193.el8
Rebuild_CHGLOG: - [sound] ALSA: ASoC: soc-component: fix a couple missing error assignments (Jaroslav Kysela) [1710702]
Rebuild_FUZZ: 95.16%
commit-author Dan Carpenter <dan.carpenter@oracle.com>
commit 901e822b2e365dac4727e0ddffb444a2554b0a89
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-193.el8/901e822b.failed

There were a couple places where the return value wasn't assigned so the
error handling wouldn't trigger.

Fixes: 5c0769af4caf ("ASoC: soc-dai: add snd_soc_dai_bespoke_trigger()")
Fixes: 95aef3553384 ("ASoC: soc-dai: add snd_soc_dai_trigger()")
	Signed-off-by: Dan Carpenter <dan.carpenter@oracle.com>
Link: https://lore.kernel.org/r/20190923142257.GB31251@mwanda
	Signed-off-by: Mark Brown <broonie@kernel.org>
(cherry picked from commit 901e822b2e365dac4727e0ddffb444a2554b0a89)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	sound/soc/soc-pcm.c
diff --cc sound/soc/soc-pcm.c
index 53198362776d,a1b99ac57d9e..000000000000
--- a/sound/soc/soc-pcm.c
+++ b/sound/soc/soc-pcm.c
@@@ -1153,11 -1070,9 +1153,17 @@@ static int soc_pcm_trigger(struct snd_p
  			return ret;
  	}
  
++<<<<<<< HEAD
 +	if (cpu_dai->driver->ops->trigger) {
 +		ret = cpu_dai->driver->ops->trigger(substream, cmd, cpu_dai);
 +		if (ret < 0)
 +			return ret;
 +	}
++=======
+ 	ret = snd_soc_dai_trigger(cpu_dai, substream, cmd);
+ 	if (ret < 0)
+ 		return ret;
++>>>>>>> 901e822b2e36 (ASoC: soc-component: fix a couple missing error assignments)
  
  	if (rtd->dai_link->ops->trigger) {
  		ret = rtd->dai_link->ops->trigger(substream, cmd);
@@@ -1190,6 -1096,11 +1196,14 @@@ static int soc_pcm_bespoke_trigger(stru
  		if (ret < 0)
  			return ret;
  	}
++<<<<<<< HEAD
++=======
+ 
+ 	ret = snd_soc_dai_bespoke_trigger(cpu_dai, substream, cmd);
+ 	if (ret < 0)
+ 		return ret;
+ 
++>>>>>>> 901e822b2e36 (ASoC: soc-component: fix a couple missing error assignments)
  	return 0;
  }
  /*
* Unmerged path sound/soc/soc-pcm.c
