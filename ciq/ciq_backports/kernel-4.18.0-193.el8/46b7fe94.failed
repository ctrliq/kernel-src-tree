PM / Domains: Improve warn for multiple states but no governor

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-193.el8
commit-author Aisheng Dong <aisheng.dong@nxp.com>
commit 46b7fe94995fd166786a301dbba919046e70e702
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-193.el8/46b7fe94.failed

It's possible a PM domain defines only one state and it does not need
a governor to work. For such case, a warning actually is not necessary.

	Signed-off-by: Dong Aisheng <aisheng.dong@nxp.com>
	Reviewed-by: Ulf Hansson <ulf.hansson@linaro.org>
	Signed-off-by: Rafael J. Wysocki <rafael.j.wysocki@intel.com>
(cherry picked from commit 46b7fe94995fd166786a301dbba919046e70e702)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/base/power/domain.c
diff --cc drivers/base/power/domain.c
index 764e0f28979e,394f9daa3983..000000000000
--- a/drivers/base/power/domain.c
+++ b/drivers/base/power/domain.c
@@@ -1768,8 -1766,8 +1768,13 @@@ int pm_genpd_init(struct generic_pm_dom
  		ret = genpd_set_default_power_state(genpd);
  		if (ret)
  			return ret;
++<<<<<<< HEAD
 +	} else if (!gov) {
 +		pr_warn("%s: no governor for states\n", genpd->name);
++=======
+ 	} else if (!gov && genpd->state_count > 1) {
+ 		pr_warn("%s : no governor for states\n", genpd->name);
++>>>>>>> 46b7fe94995f (PM / Domains: Improve warn for multiple states but no governor)
  	}
  
  	device_initialize(&genpd->dev);
* Unmerged path drivers/base/power/domain.c
