net/mlx5: Fix rtable reference leak

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-193.el8
Rebuild_CHGLOG: - [netdrv] mlx5: Fix rtable reference leak (Mohammad Heib) [1776921]
Rebuild_FUZZ: 93.94%
commit-author Parav Pandit <parav@mellanox.com>
commit 2347cee83b2bd868bde2d283db0fac89f22be4e0
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-193.el8/2347cee8.failed

If the rt entry gateway family is not AF_INET for multipath device,
rtable reference is leaked.
Hence, fix it by releasing the reference.

Fixes: 5fb091e8130b ("net/mlx5e: Use hint to resolve route when in HW multipath mode")
Fixes: e32ee6c78efa ("net/mlx5e: Support tunnel encap over tagged Ethernet")
	Signed-off-by: Parav Pandit <parav@mellanox.com>
	Signed-off-by: Saeed Mahameed <saeedm@mellanox.com>
(cherry picked from commit 2347cee83b2bd868bde2d283db0fac89f22be4e0)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/mellanox/mlx5/core/en/tc_tun.c
diff --cc drivers/net/ethernet/mellanox/mlx5/core/en/tc_tun.c
index e0d4d2f5935e,13af72556987..000000000000
--- a/drivers/net/ethernet/mellanox/mlx5/core/en/tc_tun.c
+++ b/drivers/net/ethernet/mellanox/mlx5/core/en/tc_tun.c
@@@ -99,8 -97,10 +99,14 @@@ static int mlx5e_route_lookup_ipv4(stru
  	if (ret)
  		return ret;
  
++<<<<<<< HEAD
 +	if (mlx5_lag_is_multipath(mdev) && !rt->rt_gateway)
++=======
+ 	if (mlx5_lag_is_multipath(mdev) && rt->rt_gw_family != AF_INET) {
+ 		ip_rt_put(rt);
++>>>>>>> 2347cee83b2b (net/mlx5: Fix rtable reference leak)
  		return -ENETUNREACH;
+ 	}
  #else
  	return -EOPNOTSUPP;
  #endif
* Unmerged path drivers/net/ethernet/mellanox/mlx5/core/en/tc_tun.c
