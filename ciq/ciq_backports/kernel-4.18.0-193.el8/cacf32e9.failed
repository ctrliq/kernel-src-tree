mlx5: Return -EINVAL when WARN_ON_ONCE triggers in mlx5e_tls_resync().

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-193.el8
commit-author David S. Miller <davem@davemloft.net>
commit cacf32e99788e91a3eaefafb0976deadf84f37d5
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-193.el8/cacf32e9.failed

Return value was changes to 'int' from void but this return statement
was not updated, or it slipped in via a merge.

Fixes: b5d9a834f4fd ("net/tls: don't clear TX resync flag on error")
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit cacf32e99788e91a3eaefafb0976deadf84f37d5)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/mellanox/mlx5/core/en_accel/tls.c
diff --cc drivers/net/ethernet/mellanox/mlx5/core/en_accel/tls.c
index e88340e196f7,fba561ffe1d4..000000000000
--- a/drivers/net/ethernet/mellanox/mlx5/core/en_accel/tls.c
+++ b/drivers/net/ethernet/mellanox/mlx5/core/en_accel/tls.c
@@@ -166,7 -167,10 +166,12 @@@ static void mlx5e_tls_resync_rx(struct 
  	struct tls_context *tls_ctx = tls_get_ctx(sk);
  	struct mlx5e_priv *priv = netdev_priv(netdev);
  	struct mlx5e_tls_offload_context_rx *rx_ctx;
 -	u64 rcd_sn = *(u64 *)rcd_sn_data;
  
++<<<<<<< HEAD
++=======
+ 	if (WARN_ON_ONCE(direction != TLS_OFFLOAD_CTX_DIR_RX))
+ 		return -EINVAL;
++>>>>>>> cacf32e99788 (mlx5: Return -EINVAL when WARN_ON_ONCE triggers in mlx5e_tls_resync().)
  	rx_ctx = mlx5e_get_tls_rx_context(tls_ctx);
  
  	netdev_info(netdev, "resyncing seq %d rcd %lld\n", seq,
* Unmerged path drivers/net/ethernet/mellanox/mlx5/core/en_accel/tls.c
