ACPI: debug: Clean up acpi_aml_init()

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-193.el8
commit-author Rafael J. Wysocki <rafael.j.wysocki@intel.com>
commit 6010ce3f2e57a4a30327abba528876bd7c8cbd36
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-193.el8/6010ce3f.failed

The err_exit label in acpi_aml_init() is not used any more after
commit 9ec6dbfbdc0a ("ACPI: no need to check return value of
debugfs_create functions"), but the other label in there is not
necessary too, so rearrange the code to get rid of them both.

No intentional functional impact.

Fixes: 9ec6dbfbdc0a ("ACPI: no need to check return value of debugfs_create functions")
	Reported-by: Stephen Rothwell <sfr@canb.auug.org.au>
	Reviewed-by: Greg Kroah-Hartman <gregkh@linuxfoundation.org>
	Signed-off-by: Rafael J. Wysocki <rafael.j.wysocki@intel.com>
(cherry picked from commit 6010ce3f2e57a4a30327abba528876bd7c8cbd36)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/acpi/acpi_dbg.c
diff --cc drivers/acpi/acpi_dbg.c
index a2dcd62ea32f,4a434c23a196..000000000000
--- a/drivers/acpi/acpi_dbg.c
+++ b/drivers/acpi/acpi_dbg.c
@@@ -750,13 -750,8 +750,13 @@@ static const struct acpi_debugger_ops a
  
  int __init acpi_aml_init(void)
  {
- 	int ret = 0;
+ 	int ret;
  
 +	if (!acpi_debugfs_dir) {
 +		ret = -ENOENT;
 +		goto err_exit;
 +	}
 +
  	/* Initialize AML IO interface */
  	mutex_init(&acpi_aml_io.lock);
  	init_waitqueue_head(&acpi_aml_io.wait);
@@@ -766,16 -762,8 +767,19 @@@
  					      S_IFREG | S_IRUGO | S_IWUSR,
  					      acpi_debugfs_dir, NULL,
  					      &acpi_aml_operations);
++<<<<<<< HEAD
 +	if (acpi_aml_dentry == NULL) {
 +		ret = -ENODEV;
 +		goto err_exit;
 +	}
 +	ret = acpi_register_debugger(THIS_MODULE, &acpi_aml_debugger);
 +	if (ret)
 +		goto err_fs;
 +	acpi_aml_initialized = true;
++=======
++>>>>>>> 6010ce3f2e57 (ACPI: debug: Clean up acpi_aml_init())
  
- err_fs:
+ 	ret = acpi_register_debugger(THIS_MODULE, &acpi_aml_debugger);
  	if (ret) {
  		debugfs_remove(acpi_aml_dentry);
  		acpi_aml_dentry = NULL;
* Unmerged path drivers/acpi/acpi_dbg.c
