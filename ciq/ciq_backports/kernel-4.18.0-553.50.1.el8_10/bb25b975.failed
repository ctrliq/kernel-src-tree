gfs2: remove dead code in add_to_queue

jira LE-2815
Rebuild_History Non-Buildable kernel-4.18.0-553.50.1.el8_10
commit-author Su Hui <suhui@nfschina.com>
commit bb25b97562e52b2b5808b348db32568b1f5394b5
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-553.50.1.el8_10/bb25b975.failed

clang static analyzer complains that value stored to 'gh' is never read.
The code of this line is useless after commit 0b93bac2271e
("gfs2: Remove LM_FLAG_PRIORITY flag"). Remove this code to save space.

	Signed-off-by: Su Hui <suhui@nfschina.com>
	Signed-off-by: Andreas Gruenbacher <agruenba@redhat.com>
(cherry picked from commit bb25b97562e52b2b5808b348db32568b1f5394b5)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/gfs2/glock.c
diff --cc fs/gfs2/glock.c
index 5dc035ea3dca,b290f8d172d7..000000000000
--- a/fs/gfs2/glock.c
+++ b/fs/gfs2/glock.c
@@@ -1518,14 -1524,10 +1518,21 @@@ fail
  		return;
  	}
  	list_add_tail(&gh->gh_list, insert_pt);
++<<<<<<< HEAD
 +do_cancel:
 +	gh = list_first_entry(&gl->gl_holders, struct gfs2_holder, gh_list);
 +	if (!(gh->gh_flags & LM_FLAG_PRIORITY)) {
 +		spin_unlock(&gl->gl_lockref.lock);
 +		if (sdp->sd_lockstruct.ls_ops->lm_cancel)
 +			sdp->sd_lockstruct.ls_ops->lm_cancel(gl);
 +		spin_lock(&gl->gl_lockref.lock);
 +	}
++=======
+ 	spin_unlock(&gl->gl_lockref.lock);
+ 	if (sdp->sd_lockstruct.ls_ops->lm_cancel)
+ 		sdp->sd_lockstruct.ls_ops->lm_cancel(gl);
+ 	spin_lock(&gl->gl_lockref.lock);
++>>>>>>> bb25b97562e5 (gfs2: remove dead code in add_to_queue)
  	return;
  
  trap_recursive:
* Unmerged path fs/gfs2/glock.c
