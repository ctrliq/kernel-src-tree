fs: prepare for adding LSM blob to backing_file

jira KERNEL-1233
cve CVE-2026-46054
Rebuild_History Non-Buildable kernel-5.14.0-687.20.1.el9_8
commit-author Amir Goldstein <amir73il@gmail.com>
commit 880bd496ec72a6dcb00cb70c430ef752ba242ae7
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-5.14.0-687.20.1.el9_8/880bd496.failed

In preparation to adding LSM blob to backing_file struct, factor out
helpers init_backing_file() and backing_file_free().

	Cc: stable@vger.kernel.org
	Cc: linux-fsdevel@vger.kernel.org
	Cc: linux-unionfs@vger.kernel.org
	Cc: linux-erofs@lists.ozlabs.org
	Signed-off-by: Amir Goldstein <amir73il@gmail.com>
	Reviewed-by: Serge Hallyn <serge@hallyn.com>
[PM: use the term "LSM blob", fix comment style to match file]
	Signed-off-by: Paul Moore <paul@paul-moore.com>
(cherry picked from commit 880bd496ec72a6dcb00cb70c430ef752ba242ae7)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/file_table.c
diff --cc fs/file_table.c
index 845c741dc518,3b3792903185..000000000000
--- a/fs/file_table.c
+++ b/fs/file_table.c
@@@ -44,20 -43,46 +44,35 @@@ static struct kmem_cache *filp_cachep _
  
  static struct percpu_counter nr_files __cacheline_aligned_in_smp;
  
 -/* Container for backing file with optional user path */
 -struct backing_file {
 -	struct file file;
 -	union {
 -		struct path user_path;
 -		freeptr_t bf_freeptr;
 -	};
 -};
 -
 -#define backing_file(f) container_of(f, struct backing_file, file)
 -
 -const struct path *backing_file_user_path(const struct file *f)
 +static void file_free_rcu(struct rcu_head *head)
  {
 -	return &backing_file(f)->user_path;
 -}
 -EXPORT_SYMBOL_GPL(backing_file_user_path);
 +	struct file *f = container_of(head, struct file, f_u.fu_rcuhead);
  
 -void backing_file_set_user_path(struct file *f, const struct path *path)
 -{
 -	backing_file(f)->user_path = *path;
 +	put_cred(f->f_cred);
 +	kmem_cache_free(filp_cachep, f);
  }
 -EXPORT_SYMBOL_GPL(backing_file_set_user_path);
  
+ static inline void backing_file_free(struct backing_file *ff)
+ {
+ 	path_put(&ff->user_path);
+ 	kmem_cache_free(bfilp_cachep, ff);
+ }
+ 
  static inline void file_free(struct file *f)
  {
  	security_file_free(f);
 -	if (likely(!(f->f_mode & FMODE_NOACCOUNT)))
 +	if (!(f->f_mode & FMODE_NOACCOUNT))
  		percpu_counter_dec(&nr_files);
++<<<<<<< HEAD
 +	call_rcu(&f->f_u.fu_rcuhead, file_free_rcu);
++=======
+ 	put_cred(f->f_cred);
+ 	if (unlikely(f->f_mode & FMODE_BACKING)) {
+ 		backing_file_free(backing_file(f));
+ 	} else {
+ 		kmem_cache_free(filp_cachep, f);
+ 	}
++>>>>>>> 880bd496ec72 (fs: prepare for adding LSM blob to backing_file)
  }
  
  /*
@@@ -215,6 -288,46 +230,49 @@@ struct file *alloc_empty_file_noaccount
  	return f;
  }
  
++<<<<<<< HEAD
++=======
+ static int init_backing_file(struct backing_file *ff)
+ {
+ 	memset(&ff->user_path, 0, sizeof(ff->user_path));
+ 	return 0;
+ }
+ 
+ /*
+  * Variant of alloc_empty_file() that allocates a backing_file container
+  * and doesn't check and modify nr_files.
+  *
+  * This is only for kernel internal use, and the allocate file must not be
+  * installed into file tables or such.
+  */
+ struct file *alloc_empty_backing_file(int flags, const struct cred *cred)
+ {
+ 	struct backing_file *ff;
+ 	int error;
+ 
+ 	ff = kmem_cache_alloc(bfilp_cachep, GFP_KERNEL);
+ 	if (unlikely(!ff))
+ 		return ERR_PTR(-ENOMEM);
+ 
+ 	error = init_file(&ff->file, flags, cred);
+ 	if (unlikely(error)) {
+ 		kmem_cache_free(bfilp_cachep, ff);
+ 		return ERR_PTR(error);
+ 	}
+ 
+ 	/* The f_mode flags must be set before fput(). */
+ 	ff->file.f_mode |= FMODE_BACKING | FMODE_NOACCOUNT;
+ 	error = init_backing_file(ff);
+ 	if (unlikely(error)) {
+ 		fput(&ff->file);
+ 		return ERR_PTR(error);
+ 	}
+ 
+ 	return &ff->file;
+ }
+ EXPORT_SYMBOL_GPL(alloc_empty_backing_file);
+ 
++>>>>>>> 880bd496ec72 (fs: prepare for adding LSM blob to backing_file)
  /**
   * file_init_path - initialize a 'struct file' based on path
   *
* Unmerged path fs/file_table.c
