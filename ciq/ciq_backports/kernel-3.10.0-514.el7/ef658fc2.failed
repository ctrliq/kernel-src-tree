NVMe: initialize error to '0'

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-514.el7
commit-author Jens Axboe <axboe@fb.com>
commit ef658fc2a6809b42dd7002229fd174a9a1645707
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-514.el7/ef658fc2.failed

	Reported-by: Keith Busch <keith.busch@intel.com>
Fixes: 1951feae88c5 ("nvme: use an integer value to Linux errno values")
	Signed-off-by: Jens Axboe <axboe@fb.com>
(cherry picked from commit ef658fc2a6809b42dd7002229fd174a9a1645707)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/block/nvme-core.c
diff --cc drivers/block/nvme-core.c
index f5099f908371,22d83752ae87..000000000000
--- a/drivers/block/nvme-core.c
+++ b/drivers/block/nvme-core.c
@@@ -513,8 -606,8 +513,12 @@@ static void req_completion(struct nvme_
  	struct nvme_iod *iod = ctx;
  	struct request *req = iod_get_private(iod);
  	struct nvme_cmd_info *cmd_rq = blk_mq_rq_to_pdu(req);
 +
  	u16 status = le16_to_cpup(&cqe->status) >> 1;
++<<<<<<< HEAD:drivers/block/nvme-core.c
++=======
+ 	int error = 0;
++>>>>>>> ef658fc2a680 (NVMe: initialize error to '0'):drivers/nvme/host/pci.c
  
  	if (unlikely(status)) {
  		if (!(status & NVME_SC_DNR || blk_noretry_request(req))
* Unmerged path drivers/block/nvme-core.c
