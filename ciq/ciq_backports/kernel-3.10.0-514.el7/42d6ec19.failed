IB/hfi1: Add the break statement that was removed in an earlier patch

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-514.el7
commit-author Harish Chegondi <harish.chegondi@intel.com>
commit 42d6ec19c918cb5bc6d14769e24240dce8f81687
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-514.el7/42d6ec19.failed

The break statement was unintentionally removed in this patch
commit 41ca419abc0ca7ee65d765408cdc1a7fed2897a3
("staging/rdma/hfi1: Remove hfi1 MR and hfi1 specific qp type")

	Reviewed-by: Mike Marciniszyn <mike.marciniszyn@intel.com>
	Signed-off-by: Harish Chegondi <harish.chegondi@intel.com>
	Signed-off-by: Doug Ledford <dledford@redhat.com>
(cherry picked from commit 42d6ec19c918cb5bc6d14769e24240dce8f81687)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/staging/hfi1/ruc.c
diff --cc drivers/staging/hfi1/ruc.c
index c4280b6f47d4,aa53859503ee..000000000000
--- a/drivers/staging/hfi1/ruc.c
+++ b/drivers/staging/hfi1/ruc.c
@@@ -456,16 -462,19 +456,23 @@@ again
  			goto op_err;
  		if (!ret)
  			goto rnr_nak;
 -		/* skip copy_last set and qp_access_flags recheck */
 -		goto do_write;
 +		/* FALLTHROUGH */
  	case IB_WR_RDMA_WRITE:
 -		copy_last = ibpd_to_rvtpd(qp->ibqp.pd)->user;
  		if (unlikely(!(qp->qp_access_flags & IB_ACCESS_REMOTE_WRITE)))
  			goto inv_err;
 -do_write:
  		if (wqe->length == 0)
  			break;
++<<<<<<< HEAD:drivers/staging/hfi1/ruc.c
 +		if (unlikely(!hfi1_rkey_ok(qp, &qp->r_sge.sge, wqe->length,
 +					   wqe->wr.wr.rdma.remote_addr,
 +					   wqe->wr.wr.rdma.rkey,
 +					   IB_ACCESS_REMOTE_WRITE)))
++=======
+ 		if (unlikely(!rvt_rkey_ok(qp, &qp->r_sge.sge, wqe->length,
+ 					  wqe->rdma_wr.remote_addr,
+ 					  wqe->rdma_wr.rkey,
+ 					  IB_ACCESS_REMOTE_WRITE)))
++>>>>>>> 42d6ec19c918 (IB/hfi1: Add the break statement that was removed in an earlier patch):drivers/staging/rdma/hfi1/ruc.c
  			goto acc_err;
  		qp->r_sge.sg_list = NULL;
  		qp->r_sge.num_sge = 1;
* Unmerged path drivers/staging/hfi1/ruc.c
