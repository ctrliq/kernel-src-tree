virt: Add virt directory to the top Makefile

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-514.el7
Rebuild_CHGLOG: - [virt] Add virt directory to the top Makefile (Paolo Bonzini) [1301888]
Rebuild_FUZZ: 92.68%
commit-author Feng Wu <feng.wu@intel.com>
commit 37d9fe4783ffcaddcc4afe67626691e62c5ab30e
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-514.el7/37d9fe47.failed

We need to build files in virt/lib/, which are now used by
KVM and VFIO, so add virt directory to the top Makefile.

	Signed-off-by: Feng Wu <feng.wu@intel.com>
	Acked-by: Michal Marek <mmarek@suse.com>
	Signed-off-by: Paolo Bonzini <pbonzini@redhat.com>
(cherry picked from commit 37d9fe4783ffcaddcc4afe67626691e62c5ab30e)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	Makefile
diff --cc Makefile
index 3dadade7b979,7c6c9769bd1c..000000000000
--- a/Makefile
+++ b/Makefile
@@@ -772,12 -883,10 +773,16 @@@ core-y		+= kernel/ mm/ fs/ ipc/ securit
  
  vmlinux-dirs	:= $(patsubst %/,%,$(filter %/, $(init-y) $(init-m) \
  		     $(core-y) $(core-m) $(drivers-y) $(drivers-m) \
- 		     $(net-y) $(net-m) $(libs-y) $(libs-m)))
+ 		     $(net-y) $(net-m) $(libs-y) $(libs-m) $(virt-y)))
  
  vmlinux-alldirs	:= $(sort $(vmlinux-dirs) $(patsubst %/,%,$(filter %/, \
++<<<<<<< HEAD
 +		     $(init-n) $(init-) \
 +		     $(core-n) $(core-) $(drivers-n) $(drivers-) \
 +		     $(net-n)  $(net-)  $(libs-n)    $(libs-))))
++=======
+ 		     $(init-) $(core-) $(drivers-) $(net-) $(libs-) $(virt-))))
++>>>>>>> 37d9fe4783ff (virt: Add virt directory to the top Makefile)
  
  init-y		:= $(patsubst %/, %/built-in.o, $(init-y))
  core-y		:= $(patsubst %/, %/built-in.o, $(core-y))
* Unmerged path Makefile
diff --git a/virt/Makefile b/virt/Makefile
new file mode 100644
index 000000000000..be783472ac81
--- /dev/null
+++ b/virt/Makefile
@@ -0,0 +1 @@
+obj-y	+= lib/
