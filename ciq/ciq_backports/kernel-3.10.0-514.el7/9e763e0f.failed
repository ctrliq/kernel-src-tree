mm: use %*pb[l] to print bitmaps including cpumasks and nodemasks

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-514.el7
Rebuild_CHGLOG: - [usb] use *pb[l] to print bitmaps including cpumasks and nodemasks (Don Zickus) [1303209]
Rebuild_FUZZ: 92.56%
commit-author Tejun Heo <tj@kernel.org>
commit 9e763e0f4f94d5f9ac8928867a4399ec3b3c7e33
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-514.el7/9e763e0f.failed

printk and friends can now format bitmaps using '%*pb[l]'.  cpumask
and nodemask also provide cpumask_pr_args() and nodemask_pr_args()
respectively which can be used to generate the two printf arguments
necessary to format the specified cpu/nodemask.

	Signed-off-by: Tejun Heo <tj@kernel.org>
	Signed-off-by: Andrew Morton <akpm@linux-foundation.org>
	Signed-off-by: Linus Torvalds <torvalds@linux-foundation.org>
(cherry picked from commit 9e763e0f4f94d5f9ac8928867a4399ec3b3c7e33)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	mm/mempolicy.c
diff --cc mm/mempolicy.c
index a75f5f10e1aa,4721046a134a..000000000000
--- a/mm/mempolicy.c
+++ b/mm/mempolicy.c
@@@ -2899,11 -2817,7 +2899,17 @@@ int mpol_to_str(char *buffer, int maxle
  			p += snprintf(p, buffer + maxlen - p, "relative");
  	}
  
++<<<<<<< HEAD
 +	if (!nodes_empty(nodes)) {
 +		if (buffer + maxlen < p + 2)
 +			return -ENOSPC;
 +		*p++ = ':';
 +	 	p += nodelist_scnprintf(p, buffer + maxlen - p, nodes);
 +	}
 +	return p - buffer;
++=======
+ 	if (!nodes_empty(nodes))
+ 		p += scnprintf(p, buffer + maxlen - p, ":%*pbl",
+ 			       nodemask_pr_args(&nodes));
++>>>>>>> 9e763e0f4f94 (mm: use %*pb[l] to print bitmaps including cpumasks and nodemasks)
  }
* Unmerged path mm/mempolicy.c
