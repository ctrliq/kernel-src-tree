HID: input: hid-input remove hid_output_raw_report call

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-514.el7
Rebuild_CHGLOG: - [hid] input: hid-input remove hid_output_raw_report call (Benjamin Tissoires) [1311883]
Rebuild_FUZZ: 95.24%
commit-author Benjamin Tissoires <benjamin.tissoires@redhat.com>
commit 2ebaebcf31096f83401c850393e93bc8f28db0e9
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-514.el7/2ebaebcf.failed

hid_output_raw_report() is not a ll_driver callback and should not be used.
To keep the same code path than before, we are forced to play with the
different hid_hw_* calls: if the usb or i2c device does not support
direct output reports, then we will rely on the SET_REPORT HID call.

	Signed-off-by: Benjamin Tissoires <benjamin.tissoires@redhat.com>
	Reviewed-by: David Herrmann <dh.herrmann@gmail.com>
	Signed-off-by: Jiri Kosina <jkosina@suse.cz>
(cherry picked from commit 2ebaebcf31096f83401c850393e93bc8f28db0e9)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/hid/hid-input.c
diff --cc drivers/hid/hid-input.c
index 802311fb961a,310b96779e8e..000000000000
--- a/drivers/hid/hid-input.c
+++ b/drivers/hid/hid-input.c
@@@ -1184,7 -1184,10 +1184,14 @@@ static void hidinput_led_worker(struct 
  
  	hid_output_report(report, buf);
  	/* synchronous output report */
++<<<<<<< HEAD
 +	hid->hid_output_raw_report(hid, buf, len, HID_OUTPUT_REPORT);
++=======
+ 	ret = hid_hw_output_report(hid, buf, len);
+ 	if (ret == -ENOSYS)
+ 		hid_hw_raw_request(hid, report->id, buf, len, HID_OUTPUT_REPORT,
+ 				HID_REQ_SET_REPORT);
++>>>>>>> 2ebaebcf3109 (HID: input: hid-input remove hid_output_raw_report call)
  	kfree(buf);
  }
  
* Unmerged path drivers/hid/hid-input.c
