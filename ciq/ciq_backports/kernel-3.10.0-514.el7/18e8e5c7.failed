mm: Drop unlikely before IS_ERR(_OR_NULL)

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-514.el7
Rebuild_CHGLOG: - [net] Drop unlikely before IS_ERR(_OR_NULL) (Eric Garver) [1297476]
Rebuild_FUZZ: 94.87%
commit-author Viresh Kumar <viresh.kumar@linaro.org>
commit 18e8e5c7a9f9aef1b45e0729dc340989d5a954d0
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-514.el7/18e8e5c7.failed

IS_ERR(_OR_NULL) already contain an 'unlikely' compiler flag and there
is no need to do that again from its callers. Drop it.

	Acked-by: Kirill A. Shutemov <kirill.shutemov@linux.intel.com>
	Signed-off-by: Viresh Kumar <viresh.kumar@linaro.org>
	Signed-off-by: Jiri Kosina <jkosina@suse.cz>
(cherry picked from commit 18e8e5c7a9f9aef1b45e0729dc340989d5a954d0)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	mm/huge_memory.c
diff --cc mm/huge_memory.c
index 43c4703f033a,e14652480c59..000000000000
--- a/mm/huge_memory.c
+++ b/mm/huge_memory.c
@@@ -144,11 -149,11 +144,16 @@@ static int start_khugepaged(void
  		if (!khugepaged_thread)
  			khugepaged_thread = kthread_run(khugepaged, NULL,
  							"khugepaged");
++<<<<<<< HEAD
 +		if (unlikely(IS_ERR(khugepaged_thread))) {
 +			printk(KERN_ERR
 +			       "khugepaged: kthread_run(khugepaged) failed\n");
++=======
+ 		if (IS_ERR(khugepaged_thread)) {
+ 			pr_err("khugepaged: kthread_run(khugepaged) failed\n");
++>>>>>>> 18e8e5c7a9f9 (mm: Drop unlikely before IS_ERR(_OR_NULL))
  			err = PTR_ERR(khugepaged_thread);
  			khugepaged_thread = NULL;
 -			goto fail;
  		}
  
  		if (!list_empty(&khugepaged_scan.mm_head))
* Unmerged path mm/huge_memory.c
