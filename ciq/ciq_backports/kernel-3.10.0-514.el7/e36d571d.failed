ceph: no need to get parent inode in ceph_open

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-514.el7
commit-author Jianpeng Ma <jianpeng.ma@intel.com>
commit e36d571d70c7f46b20c28d81025fd5fc044a8e22
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-514.el7/e36d571d.failed

parent inode is needed in creating new inode case.  For ceph_open,
the target inode already exists.

	Signed-off-by: Jianpeng Ma <jianpeng.ma@intel.com>
	Signed-off-by: Yan, Zheng <zyan@redhat.com>
(cherry picked from commit e36d571d70c7f46b20c28d81025fd5fc044a8e22)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/ceph/file.c
diff --cc fs/ceph/file.c
index 0c3070bb755c,90ec110b8400..000000000000
--- a/fs/ceph/file.c
+++ b/fs/ceph/file.c
@@@ -192,11 -207,9 +191,15 @@@ int ceph_open(struct inode *inode, stru
  	}
  	req->r_inode = inode;
  	ihold(inode);
 -
  	req->r_num_caps = 1;
++<<<<<<< HEAD
 +	if (flags & O_CREAT)
 +		parent_inode = ceph_get_dentry_parent_inode(file->f_dentry);
 +	err = ceph_mdsc_do_request(mdsc, parent_inode, req);
 +	iput(parent_inode);
++=======
+ 	err = ceph_mdsc_do_request(mdsc, NULL, req);
++>>>>>>> e36d571d70c7 (ceph: no need to get parent inode in ceph_open)
  	if (!err)
  		err = ceph_init_file(inode, file, req->r_fmode);
  	ceph_mdsc_put_request(req);
* Unmerged path fs/ceph/file.c
