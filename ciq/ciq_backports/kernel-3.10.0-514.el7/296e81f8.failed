net: cdc_ncm: improve bind error debug messages

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-514.el7
commit-author Bjørn Mork <bjorn@mork.no>
commit 296e81f84c7ef7272cca1636386d55acc1af3238
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-514.el7/296e81f8.failed

Make it a bit easier for users to figure out what goes
wrong when bind fails.

	Cc: Alexey Orishko <alexey.orishko@gmail.com>
	Signed-off-by: Bjørn Mork <bjorn@mork.no>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 296e81f84c7ef7272cca1636386d55acc1af3238)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/usb/cdc_ncm.c
diff --cc drivers/net/usb/cdc_ncm.c
index ce6f0ca110d1,4531f38fc0e5..000000000000
--- a/drivers/net/usb/cdc_ncm.c
+++ b/drivers/net/usb/cdc_ncm.c
@@@ -445,24 -436,30 +451,38 @@@ advance
  
  	/* reset data interface */
  	temp = usb_set_interface(dev->udev, iface_no, 0);
- 	if (temp)
+ 	if (temp) {
+ 		dev_dbg(&intf->dev, "set interface failed\n");
  		goto error2;
+ 	}
  
 +	/* initialize data interface */
 +	if (cdc_ncm_setup(dev))
 +		goto error2;
 +
  	/* configure data interface */
  	temp = usb_set_interface(dev->udev, iface_no, data_altsetting);
- 	if (temp)
+ 	if (temp) {
+ 		dev_dbg(&intf->dev, "set interface failed\n");
  		goto error2;
+ 	}
  
  	cdc_ncm_find_endpoints(dev, ctx->data);
  	cdc_ncm_find_endpoints(dev, ctx->control);
- 	if (!dev->in || !dev->out || !dev->status)
+ 	if (!dev->in || !dev->out || !dev->status) {
+ 		dev_dbg(&intf->dev, "failed to collect endpoints\n");
  		goto error2;
+ 	}
  
++<<<<<<< HEAD
 +	dev->net->ethtool_ops = &cdc_ncm_ethtool_ops;
++=======
+ 	/* initialize data interface */
+ 	if (cdc_ncm_setup(dev))	{
+ 		dev_dbg(&intf->dev, "cdc_ncm_setup() failed\n");
+ 		goto error2;
+ 	}
++>>>>>>> 296e81f84c7e (net: cdc_ncm: improve bind error debug messages)
  
  	usb_set_intfdata(ctx->data, dev);
  	usb_set_intfdata(ctx->control, dev);
* Unmerged path drivers/net/usb/cdc_ncm.c
