ALSA: hda: Delete an unnecessary check before the function call "kobject_put"

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-514.el7
commit-author Markus Elfring <elfring@users.sourceforge.net>
commit 24d9b755ab2b44ec56723cd8e6b578985a1f1c76
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-514.el7/24d9b755.failed

The kobject_put() function tests whether its argument is NULL and then
returns immediately. Thus the test around the call is not needed.

This issue was detected by using the Coccinelle software.

	Signed-off-by: Markus Elfring <elfring@users.sourceforge.net>
	Signed-off-by: Takashi Iwai <tiwai@suse.de>
(cherry picked from commit 24d9b755ab2b44ec56723cd8e6b578985a1f1c76)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	sound/hda/hdac_sysfs.c
diff --cc sound/hda/hdac_sysfs.c
index b358d5157802,089b35f6f108..000000000000
--- a/sound/hda/hdac_sysfs.c
+++ b/sound/hda/hdac_sysfs.c
@@@ -318,9 -321,7 +318,13 @@@ static void widget_tree_free(struct hda
  			free_widget_node(*p, &widget_node_group);
  		kfree(tree->nodes);
  	}
++<<<<<<< HEAD
 +	free_widget_node(tree->afg, &widget_afg_group);
 +	if (tree->root)
 +		kobject_put(tree->root);
++=======
+ 	kobject_put(tree->root);
++>>>>>>> 24d9b755ab2b (ALSA: hda: Delete an unnecessary check before the function call "kobject_put")
  	kfree(tree);
  	codec->widgets = NULL;
  }
* Unmerged path sound/hda/hdac_sysfs.c
