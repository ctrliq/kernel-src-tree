rhashtable: initialize atomic nelems variable

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-514.el7
commit-author Ying Xue <ying.xue@windriver.com>
commit 545a148e43bed67618cc90b66f9864fba0878890
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-514.el7/545a148e.failed

	Signed-off-by: Ying Xue <ying.xue@windriver.com>
	Cc: Thomas Graf <tgraf@suug.ch>
	Acked-by: Thomas Graf <tgraf@suug.ch>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 545a148e43bed67618cc90b66f9864fba0878890)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	lib/rhashtable.c
diff --cc lib/rhashtable.c
index be20e9720492,8023b554905c..000000000000
--- a/lib/rhashtable.c
+++ b/lib/rhashtable.c
@@@ -529,10 -847,10 +529,15 @@@ int rhashtable_init(struct rhashtable *
  	if (tbl == NULL)
  		return -ENOMEM;
  
++<<<<<<< HEAD
 +	memset(ht, 0, sizeof(*ht));
 +	ht->shift = ilog2(tbl->size);
 +	memcpy(&ht->p, params, sizeof(*params));
++=======
+ 	atomic_set(&ht->nelems, 0);
+ 	atomic_set(&ht->shift, ilog2(tbl->size));
++>>>>>>> 545a148e43be (rhashtable: initialize atomic nelems variable)
  	RCU_INIT_POINTER(ht->tbl, tbl);
 -	RCU_INIT_POINTER(ht->future_tbl, tbl);
  
  	if (!ht->p.hash_rnd)
  		get_random_bytes(&ht->p.hash_rnd, sizeof(ht->p.hash_rnd));
* Unmerged path lib/rhashtable.c
