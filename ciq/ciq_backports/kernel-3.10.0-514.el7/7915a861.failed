ext4: print ext4 mount option data_err=abort correctly

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-514.el7
commit-author Ales Novak <alnovak@suse.cz>
commit 7915a861c01839a05eb7346023741742c4d2135e
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-514.el7/7915a861.failed

If data_err=abort option is specified for an ext3/ext4 mount,
/proc/mounts does show it as "(null)". This is caused by token2str()
returning NULL for Opt_data_err_abort (due to its pattern containing
'=').

	Signed-off-by: Ales Novak <alnovak@suse.cz>
	Signed-off-by: Theodore Ts'o <tytso@mit.edu>
(cherry picked from commit 7915a861c01839a05eb7346023741742c4d2135e)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/ext4/super.c
diff --cc fs/ext4/super.c
index 5d3aa4bc8d65,99996e9a8f57..000000000000
--- a/fs/ext4/super.c
+++ b/fs/ext4/super.c
@@@ -1643,6 -1693,19 +1643,22 @@@ static int handle_mount_opt(struct supe
  		}
  		sbi->s_jquota_fmt = m->mount_opt;
  #endif
++<<<<<<< HEAD
++=======
+ 	} else if (token == Opt_dax) {
+ #ifdef CONFIG_FS_DAX
+ 		ext4_msg(sb, KERN_WARNING,
+ 		"DAX enabled. Warning: EXPERIMENTAL, use at your own risk");
+ 			sbi->s_mount_opt |= m->mount_opt;
+ #else
+ 		ext4_msg(sb, KERN_INFO, "dax option not supported");
+ 		return -1;
+ #endif
+ 	} else if (token == Opt_data_err_abort) {
+ 		sbi->s_mount_opt |= m->mount_opt;
+ 	} else if (token == Opt_data_err_ignore) {
+ 		sbi->s_mount_opt &= ~m->mount_opt;
++>>>>>>> 7915a861c018 (ext4: print ext4 mount option data_err=abort correctly)
  	} else {
  		if (!args->from)
  			arg = 1;
* Unmerged path fs/ext4/super.c
