cxl: Set psl_fir_cntl to production environment value

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-514.el7
commit-author Frederic Barrat <fbarrat@linux.vnet.ibm.com>
commit c6d2ee09c2fffd3efdd31be2b2811d081a45bb99
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-514.el7/c6d2ee09.failed

Switch the setting of psl_fir_cntl from debug to production
environment recommended value. It mostly affects the PSL behavior when
an error is raised in psl_fir1/2.

Tested with cxlflash.

	Signed-off-by: Frederic Barrat <fbarrat@linux.vnet.ibm.com>
	Reviewed-by: Uma Krishnan <ukrishn@linux.vnet.ibm.com>
	Signed-off-by: Michael Ellerman <mpe@ellerman.id.au>
(cherry picked from commit c6d2ee09c2fffd3efdd31be2b2811d081a45bb99)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/misc/cxl/pci.c
diff --cc drivers/misc/cxl/pci.c
index a95e8da73872,6f0c4ac4b649..000000000000
--- a/drivers/misc/cxl/pci.c
+++ b/drivers/misc/cxl/pci.c
@@@ -375,6 -374,20 +375,23 @@@ static int init_implementation_adapter_
  		return -ENODEV;
  	}
  
++<<<<<<< HEAD
++=======
+ 	return 0;
+ }
+ 
+ static int init_implementation_adapter_psl_regs(struct cxl *adapter, struct pci_dev *dev)
+ {
+ 	u64 psl_dsnctl, psl_fircntl;
+ 	u64 chipid;
+ 	u64 capp_unit_id;
+ 	int rc;
+ 
+ 	rc = calc_capp_routing(dev, &chipid, &capp_unit_id);
+ 	if (rc)
+ 		return rc;
+ 
++>>>>>>> c6d2ee09c2ff (cxl: Set psl_fir_cntl to production environment value)
  	psl_dsnctl = 0x0000900000000000ULL; /* pteupd ttype, scdone */
  	psl_dsnctl |= (0x2ULL << (63-38)); /* MMIO hang pulse: 256 us */
  	/* Tell PSL where to route data to */
* Unmerged path drivers/misc/cxl/pci.c
