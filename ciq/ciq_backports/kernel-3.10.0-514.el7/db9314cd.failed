IB/core: Replace memset with eth_zero_addr

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-514.el7
commit-author Amitoj Kaur Chawla <amitoj1606@gmail.com>
commit db9314cd351ebd734970c7afc2995a743e34aa02
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-514.el7/db9314cd.failed

Use eth_zero_addr to assign the zero address to the given address
array instead of memset when second argument is address of zero.

The Coccinelle semantic patch used to make this change is as follows:

// <smpl>
@eth_zero_addr@
expression e;
@@

-memset(e,0x00,ETH_ALEN);
+eth_zero_addr(e);
// </smpl>

	Signed-off-by: Amitoj Kaur Chawla <amitoj1606@gmail.com>
	Signed-off-by: Doug Ledford <dledford@redhat.com>
(cherry picked from commit db9314cd351ebd734970c7afc2995a743e34aa02)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/infiniband/core/sa_query.c
diff --cc drivers/infiniband/core/sa_query.c
index f94744352956,4b3ac765d08d..000000000000
--- a/drivers/infiniband/core/sa_query.c
+++ b/drivers/infiniband/core/sa_query.c
@@@ -1152,9 -1218,10 +1152,16 @@@ static void ib_sa_path_rec_callback(str
  
  		ib_unpack(path_rec_table, ARRAY_SIZE(path_rec_table),
  			  mad->data, &rec);
++<<<<<<< HEAD
 +		rec.vlan_id = 0xffff;
 +		memset(rec.dmac, 0, ETH_ALEN);
 +		memset(rec.smac, 0, ETH_ALEN);
++=======
+ 		rec.net = NULL;
+ 		rec.ifindex = 0;
+ 		rec.gid_type = IB_GID_TYPE_IB;
+ 		eth_zero_addr(rec.dmac);
++>>>>>>> db9314cd351e (IB/core: Replace memset with eth_zero_addr)
  		query->callback(status, &rec, query->context);
  	} else
  		query->callback(status, NULL, query->context);
* Unmerged path drivers/infiniband/core/sa_query.c
