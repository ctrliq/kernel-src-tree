xfs: Remove duplicate jumps to the same label

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-514.el7
commit-author Jan Kara <jack@suse.com>
commit d6077aa339d6580d12bd1089231eea2940383e32
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-514.el7/d6077aa3.failed

xfs_create() and xfs_create_tmpfile() have useless jumps to identical
labels. Simplify them.

	Signed-off-by: Jan Kara <jack@suse.com>
	Reviewed-by: Dave Chinner <dchinner@redhat.com>
	Signed-off-by: Dave Chinner <david@fromorbit.com>


(cherry picked from commit d6077aa339d6580d12bd1089231eea2940383e32)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/xfs/xfs_inode.c
diff --cc fs/xfs/xfs_inode.c
index e92152b360c6,d22a984d8470..000000000000
--- a/fs/xfs/xfs_inode.c
+++ b/fs/xfs/xfs_inode.c
@@@ -1180,11 -1175,8 +1180,16 @@@ xfs_create
  	 */
  	error = xfs_dir_ialloc(&tp, dp, mode, is_dir ? 2 : 1, rdev,
  			       prid, resblks > 0, &ip, &committed);
++<<<<<<< HEAD
 +	if (error) {
 +		if (error == -ENOSPC)
 +			goto out_trans_cancel;
 +		goto out_trans_abort;
 +	}
++=======
+ 	if (error)
+ 		goto out_trans_cancel;
++>>>>>>> d6077aa339d6 (xfs: Remove duplicate jumps to the same label)
  
  	/*
  	 * Now we join the directory inode to the transaction.  We do not do it
@@@ -1328,11 -1315,8 +1333,16 @@@ xfs_create_tmpfile
  
  	error = xfs_dir_ialloc(&tp, dp, mode, 1, 0,
  				prid, resblks > 0, &ip, NULL);
++<<<<<<< HEAD
 +	if (error) {
 +		if (error == -ENOSPC)
 +			goto out_trans_cancel;
 +		goto out_trans_abort;
 +	}
++=======
+ 	if (error)
+ 		goto out_trans_cancel;
++>>>>>>> d6077aa339d6 (xfs: Remove duplicate jumps to the same label)
  
  	if (mp->m_flags & XFS_MOUNT_WSYNC)
  		xfs_trans_set_sync(tp);
* Unmerged path fs/xfs/xfs_inode.c
