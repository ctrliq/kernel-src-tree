mmc: dw_mmc: remove unused member variable.

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-514.el7
Rebuild_CHGLOG: - [include] mmc: dw_mmc: remove unused member variable (Don Zickus) [1127975 1277866 1280133 1286932 1297039]
Rebuild_FUZZ: 98.82%
commit-author Jaehoon Chung <jh80.chung@samsung.com>
commit 907abd51012b9b0b0b687e97d5ebadbddbc682fe
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-514.el7/907abd51.failed

Since using the device-tree, didn't use the callback pointer.
So removed the unused callback pointer.
When the set_power callback is used, it should be added in future.

	Signed-off-by: Jaehoon Chung <jh80.chung@samsung.com>
	Signed-off-by: Ulf Hansson <ulf.hansson@linaro.org>
	Signed-off-by: Chris Ball <chris@printf.net>
(cherry picked from commit 907abd51012b9b0b0b687e97d5ebadbddbc682fe)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/mmc/host/dw_mmc.c
diff --cc drivers/mmc/host/dw_mmc.c
index 53089ade7270,81991eca5671..000000000000
--- a/drivers/mmc/host/dw_mmc.c
+++ b/drivers/mmc/host/dw_mmc.c
@@@ -968,8 -1028,8 +957,13 @@@ static int dw_mci_get_cd(struct mmc_hos
  	/* Use platform get_cd function, else try onboard card detect */
  	if (brd->quirks & DW_MCI_QUIRK_BROKEN_CARD_DETECTION)
  		present = 1;
++<<<<<<< HEAD
 +	else if (brd->get_cd)
 +		present = !brd->get_cd(slot->id);
++=======
+ 	else if (!IS_ERR_VALUE(gpio_cd))
+ 		present = gpio_cd;
++>>>>>>> 907abd51012b (mmc: dw_mmc: remove unused member variable.)
  	else
  		present = (mci_readl(slot->host, CDETECT) & (1 << slot->id))
  			== 0 ? 1 : 0;
@@@ -2009,20 -2116,16 +2003,10 @@@ static int dw_mci_init_slot(struct dw_m
  	slot->quirks = dw_mci_of_get_slot_quirks(host->dev, slot->id);
  
  	mmc->ops = &dw_mci_ops;
 -	if (of_property_read_u32_array(host->dev->of_node,
 -				       "clock-freq-min-max", freq, 2)) {
 -		mmc->f_min = DW_MCI_FREQ_MIN;
 -		mmc->f_max = DW_MCI_FREQ_MAX;
 -	} else {
 -		mmc->f_min = freq[0];
 -		mmc->f_max = freq[1];
 -	}
 +	mmc->f_min = DIV_ROUND_UP(host->bus_hz, 510);
 +	mmc->f_max = host->bus_hz;
  
- 	if (host->pdata->get_ocr)
- 		mmc->ocr_avail = host->pdata->get_ocr(id);
- 	else
- 		mmc->ocr_avail = MMC_VDD_32_33 | MMC_VDD_33_34;
- 
- 	/*
- 	 * Start with slot power disabled, it will be enabled when a card
- 	 * is detected.
- 	 */
- 	if (host->pdata->setpower)
- 		host->pdata->setpower(id, 0);
+ 	mmc->ocr_avail = MMC_VDD_32_33 | MMC_VDD_33_34;
  
  	if (host->pdata->caps)
  		mmc->caps = host->pdata->caps;
* Unmerged path drivers/mmc/host/dw_mmc.c
diff --git a/include/linux/mmc/dw_mmc.h b/include/linux/mmc/dw_mmc.h
index a829f7ee28c8..59c427d451a8 100644
--- a/include/linux/mmc/dw_mmc.h
+++ b/include/linux/mmc/dw_mmc.h
@@ -246,20 +246,6 @@ struct dw_mci_board {
 	/* delay in mS before detecting cards after interrupt */
 	u32 detect_delay_ms;
 
-	int (*init)(u32 slot_id, irq_handler_t , void *);
-	int (*get_ro)(u32 slot_id);
-	int (*get_cd)(u32 slot_id);
-	int (*get_ocr)(u32 slot_id);
-	int (*get_bus_wd)(u32 slot_id);
-	/*
-	 * Enable power to selected slot and set voltage to desired level.
-	 * Voltage levels are specified using MMC_VDD_xxx defines defined
-	 * in linux/mmc/host.h file.
-	 */
-	void (*setpower)(u32 slot_id, u32 volt);
-	void (*exit)(u32 slot_id);
-	void (*select_slot)(u32 slot_id);
-
 	struct dw_mci_dma_ops *dma_ops;
 	struct dma_pdata *data;
 	struct block_settings *blk_settings;
