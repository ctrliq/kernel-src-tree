nl80211: Move ACL parsing later to avoid a possible memory leak

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-514.el7
commit-author Purushottam Kushwaha <pkushwah@qti.qualcomm.com>
commit 6e8ef842223b90a33efd570128bb566a9ae6f5ad
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-514.el7/6e8ef842.failed

No support for pbss results in a memory leak for the acl_data
(if parse_acl_data succeeds). Fix this by moving the ACL parsing later.

	Cc: stable@vger.kernel.org
Fixes: 34d505193bd10 ("cfg80211: basic support for PBSS network type")
	Signed-off-by: Purushottam Kushwaha <pkushwah@qti.qualcomm.com>
	Signed-off-by: Johannes Berg <johannes@sipsolutions.net>
(cherry picked from commit 6e8ef842223b90a33efd570128bb566a9ae6f5ad)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/wireless/nl80211.c
diff --cc net/wireless/nl80211.c
index eb6f33911b58,7d72283901a3..000000000000
--- a/net/wireless/nl80211.c
+++ b/net/wireless/nl80211.c
@@@ -3440,6 -3487,16 +3440,19 @@@ static int nl80211_start_ap(struct sk_b
  		params.smps_mode = NL80211_SMPS_OFF;
  	}
  
++<<<<<<< HEAD
++=======
+ 	params.pbss = nla_get_flag(info->attrs[NL80211_ATTR_PBSS]);
+ 	if (params.pbss && !rdev->wiphy.bands[NL80211_BAND_60GHZ])
+ 		return -EOPNOTSUPP;
+ 
+ 	if (info->attrs[NL80211_ATTR_ACL_POLICY]) {
+ 		params.acl = parse_acl_data(&rdev->wiphy, info);
+ 		if (IS_ERR(params.acl))
+ 			return PTR_ERR(params.acl);
+ 	}
+ 
++>>>>>>> 6e8ef842223b (nl80211: Move ACL parsing later to avoid a possible memory leak)
  	wdev_lock(wdev);
  	err = rdev_start_ap(rdev, dev, &params);
  	if (!err) {
* Unmerged path net/wireless/nl80211.c
