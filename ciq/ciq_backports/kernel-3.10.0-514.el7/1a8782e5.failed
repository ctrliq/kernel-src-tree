fm10k: fold fm10k_pull_tail into fm10k_add_rx_frag

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-514.el7
commit-author Alexander Duyck <alexander.h.duyck@redhat.com>
commit 1a8782e59ff9bb0ed4b75d6a346a2ed212bd2031
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-514.el7/1a8782e5.failed

This change folds the fm10k_pull_tail call into fm10k_add_rx_frag.  The
advantage to doing this is that the fragment doesn't have to be modified
after it is added to the skb.

	Signed-off-by: Alexander Duyck <alexander.h.duyck@redhat.com>
	Tested-by: Krishneil Singh <Krishneil.k.singh@intel.com>
	Signed-off-by: Jeff Kirsher <jeffrey.t.kirsher@intel.com>
(cherry picked from commit 1a8782e59ff9bb0ed4b75d6a346a2ed212bd2031)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/intel/fm10k/fm10k_main.c
diff --cc drivers/net/ethernet/intel/fm10k/fm10k_main.c
index 4c37553631ff,982fdcdc795b..000000000000
--- a/drivers/net/ethernet/intel/fm10k/fm10k_main.c
+++ b/drivers/net/ethernet/intel/fm10k/fm10k_main.c
@@@ -580,18 -558,9 +558,24 @@@ static bool fm10k_cleanup_headers(struc
  		return true;
  	}
  
++<<<<<<< HEAD
 +	/* place header in linear portion of buffer */
 +	if (skb_is_nonlinear(skb))
 +		fm10k_pull_tail(skb);
 +
 +	/* if skb_pad returns an error the skb was freed */
 +	if (unlikely(skb->len < 60)) {
 +		int pad_len = 60 - skb->len;
 +
 +		if (skb_pad(skb, pad_len))
 +			return true;
 +		__skb_put(skb, pad_len);
 +	}
++=======
+ 	/* if eth_skb_pad returns an error the skb was freed */
+ 	if (eth_skb_pad(skb))
+ 		return true;
++>>>>>>> 1a8782e59ff9 (fm10k: fold fm10k_pull_tail into fm10k_add_rx_frag)
  
  	return false;
  }
* Unmerged path drivers/net/ethernet/intel/fm10k/fm10k_main.c
