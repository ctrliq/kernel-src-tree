sctp: remove the local_bh_disable/enable in sctp_endpoint_lookup_assoc

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-514.el7
commit-author Xin Long <lucien.xin@gmail.com>
commit c79c0666915418f9c0f01a6d0e93179416fb0c9e
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-514.el7/c79c0666.failed

sctp_endpoint_lookup_assoc is called in the protection of sock lock
there is no need to call local_bh_disable in this function. so remove
them.

	Signed-off-by: Xin Long <lucien.xin@gmail.com>
	Signed-off-by: Marcelo Ricardo Leitner <marcelo.leitner@gmail.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit c79c0666915418f9c0f01a6d0e93179416fb0c9e)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/sctp/endpointola.c
diff --cc net/sctp/endpointola.c
index e09f906514db,52838eaa1582..000000000000
--- a/net/sctp/endpointola.c
+++ b/net/sctp/endpointola.c
@@@ -316,10 -314,10 +316,10 @@@ struct sctp_endpoint *sctp_endpoint_is_
  }
  
  /* Find the association that goes with this chunk.
 - * We lookup the transport from hashtable at first, then get association
 - * through t->assoc.
 + * We do a linear search of the associations for this endpoint.
 + * We return the matching transport address too.
   */
- static struct sctp_association *__sctp_endpoint_lookup_assoc(
+ struct sctp_association *sctp_endpoint_lookup_assoc(
  	const struct sctp_endpoint *ep,
  	const union sctp_addr *paddr,
  	struct sctp_transport **transport)
@@@ -363,21 -342,6 +363,24 @@@ out
  	return asoc;
  }
  
++<<<<<<< HEAD
 +/* Lookup association on an endpoint based on a peer address.  BH-safe.  */
 +struct sctp_association *sctp_endpoint_lookup_assoc(
 +	const struct sctp_endpoint *ep,
 +	const union sctp_addr *paddr,
 +	struct sctp_transport **transport)
 +{
 +	struct sctp_association *asoc;
 +
 +	sctp_local_bh_disable();
 +	asoc = __sctp_endpoint_lookup_assoc(ep, paddr, transport);
 +	sctp_local_bh_enable();
 +
 +	return asoc;
 +}
 +
++=======
++>>>>>>> c79c06669154 (sctp: remove the local_bh_disable/enable in sctp_endpoint_lookup_assoc)
  /* Look for any peeled off association from the endpoint that matches the
   * given peer address.
   */
* Unmerged path net/sctp/endpointola.c
