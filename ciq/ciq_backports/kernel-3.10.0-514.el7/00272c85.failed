dm linear: remove redundant target name from error messages

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-514.el7
commit-author Tomohiro Kusumi <kusumi.tomohiro@gmail.com>
commit 00272c854ee17b804ce81ef706f611dac17f4f89
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-514.el7/00272c85.failed

Commit 72d94861 back in 2006 should have consistently removed
"dm-linear: " from all error messages.

	Signed-off-by: Tomohiro Kusumi <kusumi.tomohiro@gmail.com>
	Signed-off-by: Mike Snitzer <snitzer@redhat.com>
(cherry picked from commit 00272c854ee17b804ce81ef706f611dac17f4f89)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/md/dm-linear.c
diff --cc drivers/md/dm-linear.c
index 4f99d267340c,05c35aacb3aa..000000000000
--- a/drivers/md/dm-linear.c
+++ b/drivers/md/dm-linear.c
@@@ -42,14 -43,16 +42,20 @@@ static int linear_ctr(struct dm_target 
  		return -ENOMEM;
  	}
  
 -	ret = -EINVAL;
  	if (sscanf(argv[1], "%llu%c", &tmp, &dummy) != 1) {
- 		ti->error = "dm-linear: Invalid device sector";
+ 		ti->error = "Invalid device sector";
  		goto bad;
  	}
  	lc->start = tmp;
  
++<<<<<<< HEAD
 +	if (dm_get_device(ti, argv[0], dm_table_get_mode(ti->table), &lc->dev)) {
 +		ti->error = "dm-linear: Device lookup failed";
++=======
+ 	ret = dm_get_device(ti, argv[0], dm_table_get_mode(ti->table), &lc->dev);
+ 	if (ret) {
+ 		ti->error = "Device lookup failed";
++>>>>>>> 00272c854ee1 (dm linear: remove redundant target name from error messages)
  		goto bad;
  	}
  
* Unmerged path drivers/md/dm-linear.c
