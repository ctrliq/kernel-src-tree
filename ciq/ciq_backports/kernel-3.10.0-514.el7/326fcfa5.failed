net: remove unnecessary semicolon in netdev_alloc_pcpu_stats()

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-514.el7
Rebuild_CHGLOG: - [net] remove unnecessary semicolon in netdev_alloc_pcpu_stats() (Paolo Abeni) [1331757]
Rebuild_FUZZ: 95.80%
commit-author Felix Fietkau <nbd@openwrt.org>
commit 326fcfa5acca446b3f71e99f6d19881145556e5c
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-514.el7/326fcfa5.failed

This semicolon causes a build error if the function call is wrapped in
parentheses.

Fixes: aabc92bbe3cf ("net: add __netdev_alloc_pcpu_stats() to indicate gfp flags")
	Reported-by: Imre Kaloz <kaloz@openwrt.org>
	Signed-off-by: Felix Fietkau <nbd@openwrt.org>
	Acked-by: Pablo Neira Ayuso <pablo@netfilter.org>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 326fcfa5acca446b3f71e99f6d19881145556e5c)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	include/linux/netdevice.h
diff --cc include/linux/netdevice.h
index 95e53059b81a,3143c847bddb..000000000000
--- a/include/linux/netdevice.h
+++ b/include/linux/netdevice.h
@@@ -1897,20 -2069,23 +1897,26 @@@ struct pcpu_sw_netstats 
  	struct u64_stats_sync   syncp;
  };
  
 -#define __netdev_alloc_pcpu_stats(type, gfp)				\
 -({									\
 -	typeof(type) __percpu *pcpu_stats = alloc_percpu_gfp(type, gfp);\
 -	if (pcpu_stats)	{						\
 -		int __cpu;						\
 -		for_each_possible_cpu(__cpu) {				\
 -			typeof(type) *stat;				\
 -			stat = per_cpu_ptr(pcpu_stats, __cpu);		\
 -			u64_stats_init(&stat->syncp);			\
 -		}							\
 -	}								\
 -	pcpu_stats;							\
 +#define netdev_alloc_pcpu_stats(type)				\
 +({								\
 +	typeof(type) *pcpu_stats = alloc_percpu(type);		\
 +	if (pcpu_stats)	{					\
 +		int i;						\
 +		for_each_possible_cpu(i) {			\
 +			typeof(type) *stat;			\
 +			stat = per_cpu_ptr(pcpu_stats, i);	\
 +			u64_stats_init(&stat->syncp);		\
 +		}						\
 +	}							\
 +	pcpu_stats;						\
  })
  
++<<<<<<< HEAD
++=======
+ #define netdev_alloc_pcpu_stats(type)					\
+ 	__netdev_alloc_pcpu_stats(type, GFP_KERNEL)
+ 
++>>>>>>> 326fcfa5acca (net: remove unnecessary semicolon in netdev_alloc_pcpu_stats())
  #include <linux/notifier.h>
  
  /* netdevice notifier chain. Please remember to update the rtnetlink
* Unmerged path include/linux/netdevice.h
