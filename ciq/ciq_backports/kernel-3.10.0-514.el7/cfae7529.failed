dm: remove unused mapped_device argument from free_tio()

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-514.el7
commit-author Mike Snitzer <snitzer@redhat.com>
commit cfae7529b525c3fa86deb71cf2036659240a865e
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-514.el7/cfae7529.failed

	Signed-off-by: Mike Snitzer <snitzer@redhat.com>
(cherry picked from commit cfae7529b525c3fa86deb71cf2036659240a865e)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/md/dm.c
diff --cc drivers/md/dm.c
index a6b44461a701,1b2f96205361..000000000000
--- a/drivers/md/dm.c
+++ b/drivers/md/dm.c
@@@ -1609,13 -1657,17 +1607,23 @@@ static void __clone_and_map_data_bio(st
  		num_target_bios = ti->num_write_bios(ti, bio);
  
  	for (target_bio_nr = 0; target_bio_nr < num_target_bios; target_bio_nr++) {
++<<<<<<< HEAD
 +		tio = alloc_tio(ci, ti, nr_iovecs, target_bio_nr);
 +		if (split_bvec)
 +			clone_split_bio(tio, bio, sector, idx, offset, len);
 +		else
 +			clone_bio(tio, bio, sector, idx, bv_count, len);
++=======
+ 		tio = alloc_tio(ci, ti, target_bio_nr);
+ 		tio->len_ptr = len;
+ 		r = clone_bio(tio, bio, sector, *len);
+ 		if (r < 0) {
+ 			free_tio(tio);
+ 			break;
+ 		}
++>>>>>>> cfae7529b525 (dm: remove unused mapped_device argument from free_tio())
  		__map_bio(tio);
  	}
 -
 -	return r;
  }
  
  typedef unsigned (*get_num_bios_fn)(struct dm_target *ti);
* Unmerged path drivers/md/dm.c
