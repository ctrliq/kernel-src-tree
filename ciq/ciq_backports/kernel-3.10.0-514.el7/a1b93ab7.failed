Revert "usb: interface authorization: Use a flag for the default device authorization"

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-514.el7
commit-author Greg Kroah-Hartman <gregkh@linuxfoundation.org>
commit a1b93ab71587b8b44d45d114937cb4e75f9a5f27
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-514.el7/a1b93ab7.failed

This reverts commit 3cf1fc80655d3af7083ea4b3615e5f8532543be7 as the
signed-off-by address is invalid.

	Cc: Stefan Koch <stefan.koch10@gmail.com>
	Signed-off-by: Greg Kroah-Hartman <gregkh@linuxfoundation.org>
(cherry picked from commit a1b93ab71587b8b44d45d114937cb4e75f9a5f27)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	include/linux/usb/hcd.h
diff --cc include/linux/usb/hcd.h
index d9104aa3f832,36ce3d215cad..000000000000
--- a/include/linux/usb/hcd.h
+++ b/include/linux/usb/hcd.h
@@@ -119,6 -120,7 +119,10 @@@ struct usb_hcd 
  #define HCD_FLAG_WAKEUP_PENDING		4	/* root hub is resuming? */
  #define HCD_FLAG_RH_RUNNING		5	/* root hub is running? */
  #define HCD_FLAG_DEAD			6	/* controller has died? */
++<<<<<<< HEAD
++=======
+ #define HCD_FLAG_INTF_AUTHORIZED	7	/* authorize interfaces? */
++>>>>>>> a1b93ab71587 (Revert "usb: interface authorization: Use a flag for the default device authorization")
  
  	/* The flags can be tested using these macros; they are likely to
  	 * be slightly faster than test_bit().
@@@ -130,6 -132,14 +134,17 @@@
  #define HCD_RH_RUNNING(hcd)	((hcd)->flags & (1U << HCD_FLAG_RH_RUNNING))
  #define HCD_DEAD(hcd)		((hcd)->flags & (1U << HCD_FLAG_DEAD))
  
++<<<<<<< HEAD
++=======
+ 	/*
+ 	 * Specifies if interfaces are authorized by default
+ 	 * or they require explicit user space authorization; this bit is
+ 	 * settable through /sys/class/usb_host/X/interface_authorized_default
+ 	 */
+ #define HCD_INTF_AUTHORIZED(hcd) \
+ 	((hcd)->flags & (1U << HCD_FLAG_INTF_AUTHORIZED))
+ 
++>>>>>>> a1b93ab71587 (Revert "usb: interface authorization: Use a flag for the default device authorization")
  	/* Flags that get set only during HCD registration or removal. */
  	unsigned		rh_registered:1;/* is root hub registered? */
  	unsigned		rh_pollable:1;	/* may we poll the root hub? */
* Unmerged path include/linux/usb/hcd.h
