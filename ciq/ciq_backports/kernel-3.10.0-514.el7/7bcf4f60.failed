r8152: adjust rtl_start_rx

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-514.el7
commit-author hayeswang <hayeswang@realtek.com>
commit 7bcf4f605b4592452fba1e421b930909f7842310
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-514.el7/7bcf4f60.failed

If there is a error for r8152_submit_rx(), add the remaining rx
buffers to the list. Then the remaining rx buffers could be
submitted later.

	Signed-off-by: Hayes Wang <hayeswang@realtek.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 7bcf4f605b4592452fba1e421b930909f7842310)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/usb/r8152.c
diff --cc drivers/net/usb/r8152.c
index 74303b429023,4a9ece01def6..000000000000
--- a/drivers/net/usb/r8152.c
+++ b/drivers/net/usb/r8152.c
@@@ -1951,6 -1989,50 +1951,53 @@@ static void rxdy_gated_en(struct r8152 
  	ocp_write_word(tp, MCU_TYPE_PLA, PLA_MISC_1, ocp_data);
  }
  
++<<<<<<< HEAD
++=======
+ static int rtl_start_rx(struct r8152 *tp)
+ {
+ 	int i, ret = 0;
+ 
+ 	INIT_LIST_HEAD(&tp->rx_done);
+ 	for (i = 0; i < RTL8152_MAX_RX; i++) {
+ 		INIT_LIST_HEAD(&tp->rx_info[i].list);
+ 		ret = r8152_submit_rx(tp, &tp->rx_info[i], GFP_KERNEL);
+ 		if (ret)
+ 			break;
+ 	}
+ 
+ 	if (ret && ++i < RTL8152_MAX_RX) {
+ 		struct list_head rx_queue;
+ 		unsigned long flags;
+ 
+ 		INIT_LIST_HEAD(&rx_queue);
+ 
+ 		do {
+ 			struct rx_agg *agg = &tp->rx_info[i++];
+ 			struct urb *urb = agg->urb;
+ 
+ 			urb->actual_length = 0;
+ 			list_add_tail(&agg->list, &rx_queue);
+ 		} while (i < RTL8152_MAX_RX);
+ 
+ 		spin_lock_irqsave(&tp->rx_lock, flags);
+ 		list_splice_tail(&rx_queue, &tp->rx_done);
+ 		spin_unlock_irqrestore(&tp->rx_lock, flags);
+ 	}
+ 
+ 	return ret;
+ }
+ 
+ static int rtl_stop_rx(struct r8152 *tp)
+ {
+ 	int i;
+ 
+ 	for (i = 0; i < RTL8152_MAX_RX; i++)
+ 		usb_kill_urb(tp->rx_info[i].urb);
+ 
+ 	return 0;
+ }
+ 
++>>>>>>> 7bcf4f605b45 (r8152: adjust rtl_start_rx)
  static int rtl_enable(struct r8152 *tp)
  {
  	u32 ocp_data;
* Unmerged path drivers/net/usb/r8152.c
