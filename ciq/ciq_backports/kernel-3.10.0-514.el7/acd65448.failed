i40e: rename rss_size to alloc_rss_size in i40e_pf

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-514.el7
commit-author Helin Zhang <helin.zhang@intel.com>
commit acd65448f2e6b3407b8bb28b57d711ca417ea679
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-514.el7/acd65448.failed

This patch renames rss_size to alloc_rss_size in i40e_pf, which is
clearer and avoids confusion. It also adds comments to the other
related structure members to help clarify usage.

Change-ID: Ia90090609d006ab589cb639975bb8a0af795d16f
	Signed-off-by: Helin Zhang <helin.zhang@intel.com>
	Tested-by: Andrew Bowers <andrewx.bowers@intel.com>
	Signed-off-by: Jeff Kirsher <jeffrey.t.kirsher@intel.com>
(cherry picked from commit acd65448f2e6b3407b8bb28b57d711ca417ea679)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/intel/i40e/i40e_main.c
diff --cc drivers/net/ethernet/intel/i40e/i40e_main.c
index 7551867973c5,491c82fe6467..000000000000
--- a/drivers/net/ethernet/intel/i40e/i40e_main.c
+++ b/drivers/net/ethernet/intel/i40e/i40e_main.c
@@@ -10225,8 -10239,15 +10229,18 @@@ static void i40e_determine_queue_usage(
  	}
  
  	pf->queues_left = queues_left;
++<<<<<<< HEAD
++=======
+ 	dev_dbg(&pf->pdev->dev,
+ 		"qs_avail=%d FD SB=%d lan_qs=%d lan_tc0=%d vf=%d*%d vmdq=%d*%d, remaining=%d\n",
+ 		pf->hw.func_caps.num_tx_qp,
+ 		!!(pf->flags & I40E_FLAG_FD_SB_ENABLED),
+ 		pf->num_lan_qps, pf->alloc_rss_size, pf->num_req_vfs,
+ 		pf->num_vf_qps, pf->num_vmdq_vsis, pf->num_vmdq_qps,
+ 		queues_left);
++>>>>>>> acd65448f2e6 (i40e: rename rss_size to alloc_rss_size in i40e_pf)
  #ifdef I40E_FCOE
 -	dev_dbg(&pf->pdev->dev, "fcoe queues = %d\n", pf->num_fcoe_qps);
 +	dev_info(&pf->pdev->dev, "fcoe queues = %d\n", pf->num_fcoe_qps);
  #endif
  }
  
diff --git a/drivers/net/ethernet/intel/i40e/i40e.h b/drivers/net/ethernet/intel/i40e/i40e.h
index 63fe91a5d13f..5796c4fec95a 100644
--- a/drivers/net/ethernet/intel/i40e/i40e.h
+++ b/drivers/net/ethernet/intel/i40e/i40e.h
@@ -266,7 +266,7 @@ struct i40e_pf {
 	u16 num_lan_qps;           /* num lan queues this PF has set up */
 	u16 num_lan_msix;          /* num queue vectors for the base PF vsi */
 	int queues_left;           /* queues left unclaimed */
-	u16 rss_size;              /* num queues in the RSS array */
+	u16 alloc_rss_size;        /* allocated RSS queues */
 	u16 rss_size_max;          /* HW defined max RSS queues */
 	u16 fdir_pf_filter_count;  /* num of guaranteed filters for this PF */
 	u16 num_alloc_vsi;         /* num VSIs this driver supports */
@@ -414,7 +414,7 @@ struct i40e_pf {
 	u32 rx_hwtstamp_cleared;
 	bool ptp_tx;
 	bool ptp_rx;
-	u16 rss_table_size;
+	u16 rss_table_size; /* HW RSS table size */
 	/* These are only valid in NPAR modes */
 	u32 npar_max_bw;
 	u32 npar_min_bw;
@@ -504,8 +504,8 @@ struct i40e_vsi {
 	u16 rx_itr_setting;
 	u16 tx_itr_setting;
 
-	u16 rss_table_size;
-	u16 rss_size;
+	u16 rss_table_size; /* HW RSS table size */
+	u16 rss_size;       /* Allocated RSS queues */
 	u8  *rss_hkey_user; /* User configured hash keys */
 	u8  *rss_lut_user;  /* User configured lookup table entries */
 
* Unmerged path drivers/net/ethernet/intel/i40e/i40e_main.c
