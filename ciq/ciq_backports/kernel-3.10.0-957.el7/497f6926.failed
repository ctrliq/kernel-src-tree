ext4: Simplify error handling in ext4_dax_huge_fault()

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-957.el7
commit-author Jan Kara <jack@suse.cz>
commit 497f6926d880c57f65bf7c3f1086526fa774c55e
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-957.el7/497f6926.failed

If transaction starting fails, just bail out of the function immediately
instead of checking for that condition throughout the function.

	Reviewed-by: Ross Zwisler <ross.zwisler@linux.intel.com>
	Signed-off-by: Jan Kara <jack@suse.cz>
	Signed-off-by: Dan Williams <dan.j.williams@intel.com>
(cherry picked from commit 497f6926d880c57f65bf7c3f1086526fa774c55e)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/ext4/file.c
diff --cc fs/ext4/file.c
index feb7f54458b2,208adfc3e673..000000000000
--- a/fs/ext4/file.c
+++ b/fs/ext4/file.c
@@@ -300,13 -310,9 +305,16 @@@ static int ext4_dax_huge_fault(struct v
  	} else {
  		down_read(&EXT4_I(inode)->i_mmap_sem);
  	}
++<<<<<<< HEAD
 +	if (!IS_ERR(handle))
 +		result = dax_iomap_fault(vmf, pe_size, &ext4_iomap_ops);
 +	else
 +		result = VM_FAULT_SIGBUS;
++=======
+ 	result = dax_iomap_fault(vmf, pe_size, NULL, &ext4_iomap_ops);
++>>>>>>> 497f6926d880 (ext4: Simplify error handling in ext4_dax_huge_fault())
  	if (write) {
- 		if (!IS_ERR(handle))
- 			ext4_journal_stop(handle);
+ 		ext4_journal_stop(handle);
  		up_read(&EXT4_I(inode)->i_mmap_sem);
  		sb_end_pagefault(sb);
  	} else {
* Unmerged path fs/ext4/file.c
