HID: hid-sensor-hub: change kmalloc + memcpy by kmemdup

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-957.el7
Rebuild_CHGLOG: - [hid] hid-sensor-hub: change kmalloc + memcpy by kmemdup (Tony Camuso) [1559170]
Rebuild_FUZZ: 95.24%
commit-author Andy Shevchenko <andriy.shevchenko@linux.intel.com>
commit 7b0692f1c60a9551f8ad5fe706b79a23720a196c
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-957.el7/7b0692f1.failed

The patch substitutes kmemdup for kmalloc followed by memcpy.

	Signed-off-by: Andy Shevchenko <andriy.shevchenko@linux.intel.com>
	Acked-by: Srinivas Pandruvada <srinivas.pandruvada@linux.intel.com>
	Signed-off-by: Jiri Kosina <jkosina@suse.cz>
(cherry picked from commit 7b0692f1c60a9551f8ad5fe706b79a23720a196c)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/hid/hid-sensor-hub.c
diff --cc drivers/hid/hid-sensor-hub.c
index 59301072e347,e46e0134b0f9..000000000000
--- a/drivers/hid/hid-sensor-hub.c
+++ b/drivers/hid/hid-sensor-hub.c
@@@ -465,11 -430,10 +465,18 @@@ static int sensor_hub_raw_event(struct 
  		if (pdata->pending.status && pdata->pending.attr_usage_id ==
  				report->field[i]->usage->hid) {
  			hid_dbg(hdev, "data was pending ...\n");
++<<<<<<< HEAD
 +			pdata->pending.raw_data = kmalloc(sz, GFP_ATOMIC);
 +			if (pdata->pending.raw_data) {
 +				memcpy(pdata->pending.raw_data, ptr, sz);
 +				pdata->pending.raw_size  = sz;
 +			} else
++=======
+ 			pdata->pending.raw_data = kmemdup(ptr, sz, GFP_ATOMIC);
+ 			if (pdata->pending.raw_data)
+ 				pdata->pending.raw_size = sz;
+ 			else
++>>>>>>> 7b0692f1c60a (HID: hid-sensor-hub: change kmalloc + memcpy by kmemdup)
  				pdata->pending.raw_size = 0;
  			complete(&pdata->pending.ready);
  		}
* Unmerged path drivers/hid/hid-sensor-hub.c
