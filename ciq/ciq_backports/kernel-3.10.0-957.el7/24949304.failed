cxgb4: IPv6 filter takes 2 tids

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-957.el7
commit-author Ganesh Goudar <ganeshgr@chelsio.com>
commit 24949304c9ed7a409ac42ec63e19835519ffc44a
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-957.el7/24949304.failed

on T6, IPv6 filter would occupy 2 tids instead of 4.

	Signed-off-by: Kumar Sanghvi <kumaras@chelsio.com>
	Signed-off-by: Ganesh Goudar <ganeshgr@chelsio.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 24949304c9ed7a409ac42ec63e19835519ffc44a)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/chelsio/cxgb4/cxgb4_filter.c
diff --cc drivers/net/ethernet/chelsio/cxgb4/cxgb4_filter.c
index d415417d85d3,3177b0c9bd2d..000000000000
--- a/drivers/net/ethernet/chelsio/cxgb4/cxgb4_filter.c
+++ b/drivers/net/ethernet/chelsio/cxgb4/cxgb4_filter.c
@@@ -670,6 -1341,17 +713,20 @@@ int __cxgb4_set_filter(struct net_devic
  	if (f->valid)
  		clear_filter(adapter, f);
  
++<<<<<<< HEAD
++=======
+ 	if (is_t6(adapter->params.chip) && fs->type &&
+ 	    ipv6_addr_type((const struct in6_addr *)fs->val.lip) !=
+ 	    IPV6_ADDR_ANY) {
+ 		ret = cxgb4_clip_get(dev, (const u32 *)&fs->val.lip, 1);
+ 		if (ret) {
+ 			cxgb4_clear_ftid(&adapter->tids, filter_id, PF_INET6,
+ 					 chip_ver);
+ 			return ret;
+ 		}
+ 	}
+ 
++>>>>>>> 24949304c9ed (cxgb4: IPv6 filter takes 2 tids)
  	/* Convert the filter specification into our internal format.
  	 * We copy the PF/VF specification into the Outer VLAN field
  	 * here so the rest of the code -- including the interface to
* Unmerged path drivers/net/ethernet/chelsio/cxgb4/cxgb4_filter.c
