powerpc/fadump: Return error when fadump registration fails

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-957.el7
Rebuild_CHGLOG: - [powerpc] fadump: Return error when fadump registration fails (Gustavo Duarte) [1621969]
Rebuild_FUZZ: 92.73%
commit-author Michal Suchanek <msuchanek@suse.de>
commit 98b8cd7f75643e0a442d7a4c1cef2c9d53b7e92b
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-957.el7/98b8cd7f.failed

 - log an error message when registration fails and no error code listed
   in the switch is returned
 - translate the hv error code to posix error code and return it from
   fw_register
 - return the posix error code from fw_register to the process writing
   to sysfs
 - return EEXIST on re-registration
 - return success on deregistration when fadump is not registered
 - return ENODEV when no memory is reserved for fadump

	Signed-off-by: Michal Suchanek <msuchanek@suse.de>
	Tested-by: Hari Bathini <hbathini@linux.vnet.ibm.com>
[mpe: Use pr_err() to shrink the error print]
	Signed-off-by: Michael Ellerman <mpe@ellerman.id.au>
(cherry picked from commit 98b8cd7f75643e0a442d7a4c1cef2c9d53b7e92b)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	arch/powerpc/kernel/fadump.c
diff --cc arch/powerpc/kernel/fadump.c
index 2b42e52c61ab,8a5d0e029b93..000000000000
--- a/arch/powerpc/kernel/fadump.c
+++ b/arch/powerpc/kernel/fadump.c
@@@ -460,22 -377,9 +460,26 @@@ static int __init early_fadump_param(ch
  }
  early_param("fadump", early_fadump_param);
  
++<<<<<<< HEAD
 +/*
 + * Look for fadump_reserve_mem= cmdline option
 + * TODO: Remove references to 'fadump_reserve_mem=' parameter,
 + *       the sooner 'crashkernel=' parameter is accustomed to.
 + */
 +static int __init early_fadump_reserve_mem(char *p)
 +{
 +	if (p)
 +		fw_dump.reserve_bootvar = memparse(p, &p);
 +	return 0;
 +}
 +early_param("fadump_reserve_mem", early_fadump_reserve_mem);
 +
 +static void register_fw_dump(struct fadump_mem_struct *fdm)
++=======
+ static int register_fw_dump(struct fadump_mem_struct *fdm)
++>>>>>>> 98b8cd7f7564 (powerpc/fadump: Return error when fadump registration fails)
  {
- 	int rc;
+ 	int rc, err;
  	unsigned int wait_time;
  
  	pr_debug("Registering for firmware-assisted kernel dump...\n");
@@@ -498,11 -406,9 +506,12 @@@
  			" dump. Hardware Error(%d).\n", rc);
  		break;
  	case -3:
 +                if (!is_boot_memory_area_contiguous())
 +                        pr_err("Can't have holes in boot memory area while "
 +                               "registering fadump\n");
  		printk(KERN_ERR "Failed to register firmware-assisted kernel"
  			" dump. Parameter Error(%d).\n", rc);
+ 		err = -EINVAL;
  		break;
  	case -9:
  		printk(KERN_ERR "firmware-assisted kernel dump is already "
* Unmerged path arch/powerpc/kernel/fadump.c
