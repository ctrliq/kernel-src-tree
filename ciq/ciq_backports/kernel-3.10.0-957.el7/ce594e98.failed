r8152: simply the arguments

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-957.el7
commit-author hayeswang <hayeswang@realtek.com>
commit ce594e9824ab6ed41ac86f672203f1832b87de79
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-957.el7/ce594e98.failed

Replace &tp->napi with napi and tp->netdev with netdev.

	Signed-off-by: Hayes Wang <hayeswang@realtek.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit ce594e9824ab6ed41ac86f672203f1832b87de79)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/usb/r8152.c
diff --cc drivers/net/usb/r8152.c
index d12800506be2,4b85e95ab754..000000000000
--- a/drivers/net/usb/r8152.c
+++ b/drivers/net/usb/r8152.c
@@@ -3699,27 -3697,19 +3706,35 @@@ static int rtl8152_resume(struct usb_in
  	if (!test_bit(SELECTIVE_SUSPEND, &tp->flags)) {
  		tp->rtl_ops.init(tp);
  		queue_delayed_work(system_long_wq, &tp->hw_phy_work, 0);
- 		netif_device_attach(tp->netdev);
+ 		netif_device_attach(netdev);
  	}
  
- 	if (netif_running(tp->netdev) && tp->netdev->flags & IFF_UP) {
+ 	if (netif_running(netdev) && netdev->flags & IFF_UP) {
  		if (test_bit(SELECTIVE_SUSPEND, &tp->flags)) {
+ 			struct napi_struct *napi = &tp->napi;
+ 
  			tp->rtl_ops.autosuspend_en(tp, false);
- 			napi_disable(&tp->napi);
+ 			napi_disable(napi);
  			set_bit(WORK_ENABLE, &tp->flags);
++<<<<<<< HEAD
 +
 +			if (netif_carrier_ok(tp->netdev)) {
 +				if (rtl8152_get_speed(tp) & LINK_STATUS) {
 +					rtl_start_rx(tp);
 +				} else {
 +					netif_carrier_off(tp->netdev);
 +					tp->rtl_ops.disable(tp);
 +					netif_info(tp, link, tp->netdev,
 +						   "linking down\n");
 +				}
 +			}
 +
 +			napi_enable(&tp->napi);
++=======
+ 			if (netif_carrier_ok(netdev))
+ 				rtl_start_rx(tp);
+ 			napi_enable(napi);
++>>>>>>> ce594e9824ab (r8152: simply the arguments)
  			clear_bit(SELECTIVE_SUSPEND, &tp->flags);
  			smp_mb__after_atomic();
  			if (!list_empty(&tp->rx_done))
* Unmerged path drivers/net/usb/r8152.c
