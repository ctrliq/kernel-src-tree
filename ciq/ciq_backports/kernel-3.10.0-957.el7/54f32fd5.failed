HID: Make report_descriptor available for all devices

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-957.el7
Rebuild_CHGLOG: - [hid] Make report_descriptor available for all devices (Tony Camuso) [1559170]
Rebuild_FUZZ: 95.05%
commit-author Andy Lutomirski <luto@kernel.org>
commit 54f32fd5dff96a903f838de1efcdfb9831c5b207
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-957.el7/54f32fd5.failed

Currently the sysfs report_descriptor attribute is only available if the device
is claimed.  We have the descriptor before we even create the device node, so
just instantiate report_descriptor statically.

	Signed-off-by: Andy Lutomirski <luto@kernel.org>
	Signed-off-by: Jiri Kosina <jkosina@suse.cz>
(cherry picked from commit 54f32fd5dff96a903f838de1efcdfb9831c5b207)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/hid/hid-core.c
diff --cc drivers/hid/hid-core.c
index 02c64a2e9327,d5ddb7573691..000000000000
--- a/drivers/hid/hid-core.c
+++ b/drivers/hid/hid-core.c
@@@ -2194,11 -2224,21 +2188,23 @@@ static ssize_t modalias_show(struct dev
  
  	return (len >= PAGE_SIZE) ? (PAGE_SIZE - 1) : len;
  }
 -static DEVICE_ATTR_RO(modalias);
  
 -static struct attribute *hid_dev_attrs[] = {
 -	&dev_attr_modalias.attr,
 -	NULL,
 +static struct device_attribute hid_dev_attrs[] = {
 +	__ATTR_RO(modalias),
 +	__ATTR_NULL,
  };
++<<<<<<< HEAD
++=======
+ static struct bin_attribute *hid_dev_bin_attrs[] = {
+ 	&dev_bin_attr_report_desc,
+ 	NULL
+ };
+ static const struct attribute_group hid_dev_group = {
+ 	.attrs = hid_dev_attrs,
+ 	.bin_attrs = hid_dev_bin_attrs,
+ };
+ __ATTRIBUTE_GROUPS(hid_dev);
++>>>>>>> 54f32fd5dff9 (HID: Make report_descriptor available for all devices)
  
  static int hid_uevent(struct device *dev, struct kobj_uevent_env *env)
  {
* Unmerged path drivers/hid/hid-core.c
