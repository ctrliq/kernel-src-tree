dax: Create local variable for vmf->flags & FAULT_FLAG_WRITE test

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-957.el7
commit-author Jan Kara <jack@suse.cz>
commit d2c43ef13327478b299db0f58193495367c6d4ae
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-957.el7/d2c43ef1.failed

There are already two users and more are coming.

	Reviewed-by: Christoph Hellwig <hch@lst.de>
	Reviewed-by: Ross Zwisler <ross.zwisler@linux.intel.com>
	Signed-off-by: Jan Kara <jack@suse.cz>
	Signed-off-by: Dan Williams <dan.j.williams@intel.com>
(cherry picked from commit d2c43ef13327478b299db0f58193495367c6d4ae)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/dax.c
diff --cc fs/dax.c
index 679214f8898b,5ea71381dba0..000000000000
--- a/fs/dax.c
+++ b/fs/dax.c
@@@ -1282,8 -1208,8 +1283,13 @@@ static int dax_iomap_pte_fault(struct v
  		break;
  	case IOMAP_UNWRITTEN:
  	case IOMAP_HOLE:
++<<<<<<< HEAD
 +		if (!(vmf->flags & FAULT_FLAG_WRITE)) {
 +			vmf_ret = dax_load_hole(mapping, &entry, vmf);
++=======
+ 		if (!write) {
+ 			vmf_ret = dax_load_hole(mapping, entry, vmf);
++>>>>>>> d2c43ef13327 (dax: Create local variable for vmf->flags & FAULT_FLAG_WRITE test)
  			goto finish_iomap;
  		}
  		/*FALLTHRU*/
* Unmerged path fs/dax.c
