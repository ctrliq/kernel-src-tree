drm/nouveau: Remove duplicate poll_enable() in pmops_runtime_suspend()

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-957.el7
commit-author Lyude Paul <lyude@redhat.com>
commit 611ce855420a6e8b9ff47af5f47431d52c7709f8
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-957.el7/611ce855.failed

Since actual hotplug notifications don't get disabled until
nouveau_display_fini() is called, all this will do is cause any hotplugs
that happen between this drm_kms_helper_poll_disable() call and the
actual hotplug disablement to potentially be dropped if ACPI isn't
around to help us.

	Signed-off-by: Lyude Paul <lyude@redhat.com>
	Acked-by: Karol Herbst <kherbst@redhat.com>
	Acked-by: Daniel Vetter <daniel@ffwll.ch>
	Cc: stable@vger.kernel.org
	Cc: Lukas Wunner <lukas@wunner.de>
	Signed-off-by: Ben Skeggs <bskeggs@redhat.com>
(cherry picked from commit 611ce855420a6e8b9ff47af5f47431d52c7709f8)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/gpu/drm/nouveau/nouveau_drm.c
diff --cc drivers/gpu/drm/nouveau/nouveau_drm.c
index 7cbd6aca3c10,5fdc1fbe2ee5..000000000000
--- a/drivers/gpu/drm/nouveau/nouveau_drm.c
+++ b/drivers/gpu/drm/nouveau/nouveau_drm.c
@@@ -748,8 -835,6 +748,11 @@@ nouveau_pmops_runtime_suspend(struct de
  		return -EBUSY;
  	}
  
++<<<<<<< HEAD
 +	drm_kms_helper_poll_disable(drm_dev);
 +	vga_switcheroo_set_dynamic_switch(pdev, VGA_SWITCHEROO_OFF);
++=======
++>>>>>>> 611ce855420a (drm/nouveau: Remove duplicate poll_enable() in pmops_runtime_suspend())
  	nouveau_switcheroo_optimus_dsm();
  	ret = nouveau_do_suspend(drm_dev, true);
  	pci_save_state(pdev);
* Unmerged path drivers/gpu/drm/nouveau/nouveau_drm.c
