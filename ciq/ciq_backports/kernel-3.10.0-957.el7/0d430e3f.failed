x86/LDT: Print the real LDT base address

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-957.el7
Rebuild_CHGLOG: - [x86] ldt: Print the real LDT base address (Gopal Tiwari) [1456572]
Rebuild_FUZZ: 94.74%
commit-author Jan Beulich <JBeulich@suse.com>
commit 0d430e3fb3f7cdc13c0d22078b820f682821b45a
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-957.el7/0d430e3f.failed

This was meant to print base address and entry count; make it do so
again.

Fixes: 37868fe113ff "x86/ldt: Make modify_ldt synchronous"
	Signed-off-by: Jan Beulich <jbeulich@suse.com>
	Acked-by: Andy Lutomirski <luto@kernel.org>
Link: http://lkml.kernel.org/r/56797D8402000078000C24F0@prv-mh.provo.novell.com
	Signed-off-by: Thomas Gleixner <tglx@linutronix.de>
(cherry picked from commit 0d430e3fb3f7cdc13c0d22078b820f682821b45a)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	arch/x86/kernel/process_64.c
diff --cc arch/x86/kernel/process_64.c
index fb066fe1e019,b9d99e0f82c4..000000000000
--- a/arch/x86/kernel/process_64.c
+++ b/arch/x86/kernel/process_64.c
@@@ -126,13 -121,15 +126,18 @@@ void __show_regs(struct pt_regs *regs, 
  void release_thread(struct task_struct *dead_task)
  {
  	if (dead_task->mm) {
 -#ifdef CONFIG_MODIFY_LDT_SYSCALL
 -		if (dead_task->mm->context.ldt) {
 +		if (dead_task->mm->context.size) {
  			pr_warn("WARNING: dead process %s still has LDT? <%p/%d>\n",
  				dead_task->comm,
++<<<<<<< HEAD
 +				dead_task->mm->context.ldt,
 +				dead_task->mm->context.size);
++=======
+ 				dead_task->mm->context.ldt->entries,
+ 				dead_task->mm->context.ldt->size);
++>>>>>>> 0d430e3fb3f7 (x86/LDT: Print the real LDT base address)
  			BUG();
  		}
 -#endif
  	}
  }
  
* Unmerged path arch/x86/kernel/process_64.c
