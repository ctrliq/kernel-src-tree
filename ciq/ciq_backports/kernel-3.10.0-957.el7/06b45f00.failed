ALSA: firewire: Convert to snd_card_new() with a device pointer

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-957.el7
commit-author Takashi Iwai <tiwai@suse.de>
commit 06b45f00a46621e0766e37a02a9676b9143261a2
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-957.el7/06b45f00.failed

Also remove superfluous snd_card_set_dev() calls.

	Reviewed-by: Takashi Sakamoto <o-takashi@sakamocchi.jp>
	Signed-off-by: Takashi Iwai <tiwai@suse.de>
(cherry picked from commit 06b45f00a46621e0766e37a02a9676b9143261a2)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	sound/firewire/dice.c
#	sound/firewire/isight.c
#	sound/firewire/scs1x.c
#	sound/firewire/speakers.c
diff --cc sound/firewire/isight.c
index 36693001467f,7ac94439e758..000000000000
--- a/sound/firewire/isight.c
+++ b/sound/firewire/isight.c
@@@ -628,10 -631,10 +628,14 @@@ static int isight_probe(struct device *
  	struct isight *isight;
  	int err;
  
- 	err = snd_card_create(-1, NULL, THIS_MODULE, sizeof(*isight), &card);
+ 	err = snd_card_new(&unit->device, -1, NULL, THIS_MODULE,
+ 			   sizeof(*isight), &card);
  	if (err < 0)
  		return err;
++<<<<<<< HEAD
 +	snd_card_set_dev(card, unit_dev);
++=======
++>>>>>>> 06b45f00a466 (ALSA: firewire: Convert to snd_card_new() with a device pointer)
  
  	isight = card->private_data;
  	isight->card = card;
diff --cc sound/firewire/scs1x.c
index b252c21b6d13,2dba848a781f..000000000000
--- a/sound/firewire/scs1x.c
+++ b/sound/firewire/scs1x.c
@@@ -392,10 -391,10 +392,14 @@@ static int scs_probe(struct device *uni
  	struct scs *scs;
  	int err;
  
- 	err = snd_card_create(-16, NULL, THIS_MODULE, sizeof(*scs), &card);
+ 	err = snd_card_new(&unit->device, -16, NULL, THIS_MODULE,
+ 			   sizeof(*scs), &card);
  	if (err < 0)
  		return err;
++<<<<<<< HEAD
 +	snd_card_set_dev(card, unit_dev);
++=======
++>>>>>>> 06b45f00a466 (ALSA: firewire: Convert to snd_card_new() with a device pointer)
  
  	scs = card->private_data;
  	scs->card = card;
diff --cc sound/firewire/speakers.c
index 0f1e5d88ed15,9f7ef219b109..000000000000
--- a/sound/firewire/speakers.c
+++ b/sound/firewire/speakers.c
@@@ -706,10 -668,10 +706,14 @@@ static int fwspk_probe(struct device *u
  	u32 firmware;
  	int err;
  
- 	err = snd_card_create(-1, NULL, THIS_MODULE, sizeof(*fwspk), &card);
+ 	err = snd_card_new(&unit->device, -1, NULL, THIS_MODULE,
+ 			   sizeof(*fwspk), &card);
  	if (err < 0)
  		return err;
++<<<<<<< HEAD
 +	snd_card_set_dev(card, unit_dev);
++=======
++>>>>>>> 06b45f00a466 (ALSA: firewire: Convert to snd_card_new() with a device pointer)
  
  	fwspk = card->private_data;
  	fwspk->card = card;
* Unmerged path sound/firewire/dice.c
* Unmerged path sound/firewire/dice.c
* Unmerged path sound/firewire/isight.c
* Unmerged path sound/firewire/scs1x.c
* Unmerged path sound/firewire/speakers.c
