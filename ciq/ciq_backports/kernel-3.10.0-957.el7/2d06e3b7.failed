md: raid10: avoid direct access to bvec table in handle_reshape_read_error

jira LE-1907
Rebuild_History Non-Buildable kernel-3.10.0-957.el7
Rebuild_CHGLOG: - [md] raid10: avoid direct access to bvec table in handle_reshape_read_error (Nigel Croxon) [1494474]
Rebuild_FUZZ: 97.22%
commit-author Ming Lei <tom.leiming@gmail.com>
commit 2d06e3b7145bb08705615e6e7400024d8e36a5c0
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-3.10.0-957.el7/2d06e3b7.failed

All reshape I/O share pages from 1st copy device, so just use that pages
for avoiding direct access to bvec table in handle_reshape_read_error.

	Signed-off-by: Ming Lei <tom.leiming@gmail.com>
	Signed-off-by: Shaohua Li <shli@fb.com>
(cherry picked from commit 2d06e3b7145bb08705615e6e7400024d8e36a5c0)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/md/raid10.c
diff --cc drivers/md/raid10.c
index e568b64df05f,0f13d57ef646..000000000000
--- a/drivers/md/raid10.c
+++ b/drivers/md/raid10.c
@@@ -4764,8 -4685,8 +4767,13 @@@ static int handle_reshape_read_error(st
  			success = sync_page_io(rdev,
  					       addr,
  					       s << 9,
++<<<<<<< HEAD
 +					       bvec[idx].bv_page,
 +					       READ, false);
++=======
+ 					       pages[idx],
+ 					       REQ_OP_READ, 0, false);
++>>>>>>> 2d06e3b7145b (md: raid10: avoid direct access to bvec table in handle_reshape_read_error)
  			rdev_dec_pending(rdev, mddev);
  			rcu_read_lock();
  			if (success)
* Unmerged path drivers/md/raid10.c
