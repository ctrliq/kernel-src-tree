netfilter: nft_set_pipapo: merge pipapo_get/lookup

jira KERNEL-1646
Rebuild_History Non-Buildable kernel-5.14.0-687.51.1.el9_8
commit-author Florian Westphal <fw@strlen.de>
commit d8d871a35ca9ee4881d34995444ed1cb826d01db
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-5.14.0-687.51.1.el9_8/d8d871a3.failed

The matching algorithm has implemented thrice:
1. data path lookup, generic version
2. data path lookup, avx2 version
3. control plane lookup

Merge 1 and 3 by refactoring pipapo_get as a common helper, then make
nft_pipapo_lookup and nft_pipapo_get both call the common helper.

Aside from the code savings this has the benefit that we no longer allocate
temporary scratch maps for each control plane get and insertion operation.

	Signed-off-by: Florian Westphal <fw@strlen.de>
	Reviewed-by: Stefano Brivio <sbrivio@redhat.com>
	Signed-off-by: Pablo Neira Ayuso <pablo@netfilter.org>
(cherry picked from commit d8d871a35ca9ee4881d34995444ed1cb826d01db)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/netfilter/nft_set_pipapo.c
diff --cc net/netfilter/nft_set_pipapo.c
index 5a2984c5ef6d,28e67c4d7132..000000000000
--- a/net/netfilter/nft_set_pipapo.c
+++ b/net/netfilter/nft_set_pipapo.c
@@@ -1333,16 -1298,10 +1267,23 @@@ static int nft_pipapo_insert(const stru
  		return -ENOTEMPTY;
  	}
  
++<<<<<<< HEAD
 +	if (PTR_ERR(dup) == -ENOENT) {
 +		/* Look for partially overlapping entries */
 +		dup = pipapo_get(m, end, nft_genmask_next(net), tstamp,
 +				 GFP_KERNEL);
 +	}
 +
 +	if (PTR_ERR(dup) != -ENOENT) {
 +		if (IS_ERR(dup))
 +			return PTR_ERR(dup);
 +		*ext2 = &dup->ext;
++=======
+ 	/* Look for partially overlapping entries */
+ 	dup = pipapo_get(m, end, nft_genmask_next(net), tstamp);
+ 	if (dup) {
+ 		*elem_priv = &dup->priv;
++>>>>>>> d8d871a35ca9 (netfilter: nft_set_pipapo: merge pipapo_get/lookup)
  		return -ENOTEMPTY;
  	}
  
* Unmerged path net/netfilter/nft_set_pipapo.c
