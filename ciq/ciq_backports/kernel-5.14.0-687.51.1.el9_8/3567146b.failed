netfilter: nf_tables: avoid false-positive lockdep splats with basechain hook

jira KERNEL-1646
Rebuild_History Non-Buildable kernel-5.14.0-687.51.1.el9_8
commit-author Florian Westphal <fw@strlen.de>
commit 3567146b94afcd69d4916c880eb5b1b0e3797397
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-5.14.0-687.51.1.el9_8/3567146b.failed

Like previous patches: iteration is ok if the list cannot be altered in
parallel.

	Signed-off-by: Florian Westphal <fw@strlen.de>
	Signed-off-by: Pablo Neira Ayuso <pablo@netfilter.org>
(cherry picked from commit 3567146b94afcd69d4916c880eb5b1b0e3797397)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/netfilter/nf_tables_api.c
diff --cc net/netfilter/nf_tables_api.c
index 6308130a4a21,3b5154f2dd79..000000000000
--- a/net/netfilter/nf_tables_api.c
+++ b/net/netfilter/nf_tables_api.c
@@@ -1717,8 -1824,10 +1717,15 @@@ nla_put_failure
  	return -ENOSPC;
  }
  
++<<<<<<< HEAD
 +static int nft_dump_basechain_hook(struct sk_buff *skb, int family,
 +				   const struct nft_base_chain *basechain)
++=======
+ static int nft_dump_basechain_hook(struct sk_buff *skb,
+ 				   const struct net *net, int family,
+ 				   const struct nft_base_chain *basechain,
+ 				   const struct list_head *hook_list)
++>>>>>>> 3567146b94af (netfilter: nf_tables: avoid false-positive lockdep splats with basechain hook)
  {
  	const struct nf_hook_ops *ops = &basechain->ops;
  	struct nft_hook *hook, *first = NULL;
@@@ -1739,7 -1847,11 +1746,15 @@@
  		if (!nest_devs)
  			goto nla_put_failure;
  
++<<<<<<< HEAD
 +		list_for_each_entry(hook, &basechain->hook_list, list) {
++=======
+ 		if (!hook_list)
+ 			hook_list = &basechain->hook_list;
+ 
+ 		list_for_each_entry_rcu(hook, hook_list, list,
+ 					lockdep_commit_lock_is_held(net)) {
++>>>>>>> 3567146b94af (netfilter: nf_tables: avoid false-positive lockdep splats with basechain hook)
  			if (!first)
  				first = hook;
  
@@@ -1786,7 -1902,7 +1801,11 @@@ static int nf_tables_fill_chain_info(st
  		const struct nft_base_chain *basechain = nft_base_chain(chain);
  		struct nft_stats __percpu *stats;
  
++<<<<<<< HEAD
 +		if (nft_dump_basechain_hook(skb, family, basechain))
++=======
+ 		if (nft_dump_basechain_hook(skb, net, family, basechain, hook_list))
++>>>>>>> 3567146b94af (netfilter: nf_tables: avoid false-positive lockdep splats with basechain hook)
  			goto nla_put_failure;
  
  		if (nla_put_be32(skb, NFTA_CHAIN_POLICY,
* Unmerged path net/netfilter/nf_tables_api.c
