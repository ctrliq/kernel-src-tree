netfilter: nf_tables: adjust lockdep assertions handling

jira KERNEL-1646
Rebuild_History Non-Buildable kernel-5.14.0-687.51.1.el9_8
commit-author Fedor Pchelkin <pchelkin@ispras.ru>
commit 8df1b40de76979bb8e975201d07b71103d5de820
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-5.14.0-687.51.1.el9_8/8df1b40d.failed

It's needed to check the return value of lockdep_commit_lock_is_held(),
otherwise there's no point in this assertion as it doesn't print any
debug information on itself.

Found by Linux Verification Center (linuxtesting.org) with Svace static
analysis tool.

Fixes: b04df3da1b5c ("netfilter: nf_tables: do not defer rule destruction via call_rcu")
	Reported-by: Alexey Khoroshilov <khoroshilov@ispras.ru>
	Signed-off-by: Fedor Pchelkin <pchelkin@ispras.ru>
	Acked-by: Florian Westphal <fw@strlen.de>
	Signed-off-by: Pablo Neira Ayuso <pablo@netfilter.org>
(cherry picked from commit 8df1b40de76979bb8e975201d07b71103d5de820)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/netfilter/nf_tables_api.c
diff --cc net/netfilter/nf_tables_api.c
index 283cf07e8650,620824a56a55..000000000000
--- a/net/netfilter/nf_tables_api.c
+++ b/net/netfilter/nf_tables_api.c
@@@ -3569,8 -4039,11 +3569,13 @@@ void nf_tables_rule_destroy(const struc
  	kfree(rule);
  }
  
 -/* can only be used if rule is no longer visible to dumps */
  static void nf_tables_rule_release(const struct nft_ctx *ctx, struct nft_rule *rule)
  {
++<<<<<<< HEAD
++=======
+ 	WARN_ON_ONCE(!lockdep_commit_lock_is_held(ctx->net));
+ 
++>>>>>>> 8df1b40de769 (netfilter: nf_tables: adjust lockdep assertions handling)
  	nft_rule_expr_deactivate(ctx, rule, NFT_TRANS_RELEASE);
  	nf_tables_rule_destroy(ctx, rule);
  }
@@@ -5367,6 -5864,8 +5372,11 @@@ void nf_tables_deactivate_set(const str
  			      struct nft_set_binding *binding,
  			      enum nft_trans_phase phase)
  {
++<<<<<<< HEAD
++=======
+ 	WARN_ON_ONCE(!lockdep_commit_lock_is_held(ctx->net));
+ 
++>>>>>>> 8df1b40de769 (netfilter: nf_tables: adjust lockdep assertions handling)
  	switch (phase) {
  	case NFT_TRANS_PREPARE_ERROR:
  		nft_set_trans_unbind(ctx, set);
* Unmerged path net/netfilter/nf_tables_api.c
