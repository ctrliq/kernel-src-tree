netfilter: nft_ct: expectation timeouts are passed in milliseconds

jira KERNEL-1646
Rebuild_History Non-Buildable kernel-5.14.0-687.51.1.el9_8
commit-author Florian Westphal <fw@strlen.de>
commit 6fb421bd07f156cdf0cdede062d31f1c21def326
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-5.14.0-687.51.1.el9_8/6fb421bd.failed

Userspace passes '5000' in case user asks for 5 seconds.

Allowing for sub-second expectation lifetimes makes sense to me. so
fix up the kernel side instead of munging nft to send a value rounded
up to next second.

Also note that this violates nft convention of passing integers in
network byte order, but we can't change this anymore.

Fixes: 857b46027d6f ("netfilter: nft_ct: add ct expectations support")
	Signed-off-by: Florian Westphal <fw@strlen.de>
	Signed-off-by: Pablo Neira Ayuso <pablo@netfilter.org>
(cherry picked from commit 6fb421bd07f156cdf0cdede062d31f1c21def326)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/netfilter/nft_ct.c
diff --cc net/netfilter/nft_ct.c
index 2b45d2058afe,03a88c77e0f0..000000000000
--- a/net/netfilter/nft_ct.c
+++ b/net/netfilter/nft_ct.c
@@@ -1295,7 -1340,7 +1310,11 @@@ static void nft_ct_expect_obj_eval(stru
  		          &ct->tuplehash[!dir].tuple.src.u3,
  		          &ct->tuplehash[!dir].tuple.dst.u3,
  		          priv->l4proto, NULL, &priv->dport);
++<<<<<<< HEAD
 +	exp->timeout.expires = jiffies + priv->timeout * HZ;
++=======
+ 	exp->timeout += priv->timeout;
++>>>>>>> 6fb421bd07f1 (netfilter: nft_ct: expectation timeouts are passed in milliseconds)
  
  	if (nf_ct_expect_related(exp, 0) != 0)
  		regs->verdict.code = NF_DROP;
* Unmerged path net/netfilter/nft_ct.c
