nf_tables: nft_dynset: fix possible stateful expression memleak in error path

jira KERNEL-1646
cve CVE-2026-23399
Rebuild_History Non-Buildable kernel-5.14.0-687.51.1.el9_8
commit-author Pablo Neira Ayuso <pablo@netfilter.org>
commit 0548a13b5a145b16e4da0628b5936baf35f51b43
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-5.14.0-687.51.1.el9_8/0548a13b.failed

If cloning the second stateful expression in the element via GFP_ATOMIC
fails, then the first stateful expression remains in place without being
released.

   unreferenced object (percpu) 0x607b97e9cab8 (size 16):
     comm "softirq", pid 0, jiffies 4294931867
     hex dump (first 16 bytes on cpu 3):
       00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00
     backtrace (crc 0):
       pcpu_alloc_noprof+0x453/0xd80
       nft_counter_clone+0x9c/0x190 [nf_tables]
       nft_expr_clone+0x8f/0x1b0 [nf_tables]
       nft_dynset_new+0x2cb/0x5f0 [nf_tables]
       nft_rhash_update+0x236/0x11c0 [nf_tables]
       nft_dynset_eval+0x11f/0x670 [nf_tables]
       nft_do_chain+0x253/0x1700 [nf_tables]
       nft_do_chain_ipv4+0x18d/0x270 [nf_tables]
       nf_hook_slow+0xaa/0x1e0
       ip_local_deliver+0x209/0x330

Fixes: 563125a73ac3 ("netfilter: nftables: generalize set extension to support for several expressions")
	Reported-by: Gurpreet Shergill <giki.shergill@proton.me>
	Signed-off-by: Pablo Neira Ayuso <pablo@netfilter.org>
	Signed-off-by: Florian Westphal <fw@strlen.de>
(cherry picked from commit 0548a13b5a145b16e4da0628b5936baf35f51b43)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/netfilter/nft_dynset.c
diff --cc net/netfilter/nft_dynset.c
index a6fc1cbfa88d,9123277be03c..000000000000
--- a/net/netfilter/nft_dynset.c
+++ b/net/netfilter/nft_dynset.c
@@@ -35,18 -39,22 +39,27 @@@ static int nft_dynset_expr_setup(const 
  
  	for (i = 0; i < priv->num_exprs; i++) {
  		expr = nft_setelem_expr_at(elem_expr, elem_expr->size);
++<<<<<<< HEAD
 +		if (nft_expr_clone(expr, priv->expr_array[i]) < 0)
 +			return -1;
++=======
+ 		if (nft_expr_clone(expr, priv->expr_array[i], GFP_ATOMIC) < 0)
+ 			goto err_out;
++>>>>>>> 0548a13b5a14 (nf_tables: nft_dynset: fix possible stateful expression memleak in error path)
  
  		elem_expr->size += priv->expr_array[i]->ops->size;
  	}
  
  	return 0;
+ err_out:
+ 	nft_set_elem_expr_destroy(&ctx, elem_expr);
+ 
+ 	return -1;
  }
  
 -struct nft_elem_priv *nft_dynset_new(struct nft_set *set,
 -				     const struct nft_expr *expr,
 -				     struct nft_regs *regs)
 +static struct nft_elem_priv *nft_dynset_new(struct nft_set *set,
 +					    const struct nft_expr *expr,
 +					    struct nft_regs *regs)
  {
  	const struct nft_dynset *priv = nft_expr_priv(expr);
  	struct nft_set_ext *ext;
diff --git a/include/net/netfilter/nf_tables.h b/include/net/netfilter/nf_tables.h
index d5f74e99a208..8c672e13f243 100644
--- a/include/net/netfilter/nf_tables.h
+++ b/include/net/netfilter/nf_tables.h
@@ -839,6 +839,8 @@ struct nft_elem_priv *nft_set_elem_init(const struct nft_set *set,
 					u64 timeout, u64 expiration, gfp_t gfp);
 int nft_set_elem_expr_clone(const struct nft_ctx *ctx, struct nft_set *set,
 			    struct nft_expr *expr_array[]);
+void nft_set_elem_expr_destroy(const struct nft_ctx *ctx,
+			       struct nft_set_elem_expr *elem_expr);
 void nft_set_elem_destroy(const struct nft_set *set,
 			  const struct nft_elem_priv *elem_priv,
 			  bool destroy_expr);
diff --git a/net/netfilter/nf_tables_api.c b/net/netfilter/nf_tables_api.c
index 6308130a4a21..27f6453289f4 100644
--- a/net/netfilter/nf_tables_api.c
+++ b/net/netfilter/nf_tables_api.c
@@ -6181,8 +6181,8 @@ static void __nft_set_elem_expr_destroy(const struct nft_ctx *ctx,
 	}
 }
 
-static void nft_set_elem_expr_destroy(const struct nft_ctx *ctx,
-				      struct nft_set_elem_expr *elem_expr)
+void nft_set_elem_expr_destroy(const struct nft_ctx *ctx,
+			       struct nft_set_elem_expr *elem_expr)
 {
 	struct nft_expr *expr;
 	u32 size;
* Unmerged path net/netfilter/nft_dynset.c
