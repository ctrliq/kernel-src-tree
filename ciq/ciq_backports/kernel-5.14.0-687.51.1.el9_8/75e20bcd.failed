netfilter: nf_tables: Introduce functions freeing nft_hook objects

jira KERNEL-1646
Rebuild_History Non-Buildable kernel-5.14.0-687.51.1.el9_8
commit-author Phil Sutter <phil@nwl.cc>
commit 75e20bcdce24b34e9bf71a7a731f9294fb2c633c
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-5.14.0-687.51.1.el9_8/75e20bcd.failed

Pointless wrappers around kfree() for now, prep work for an embedded
list of nf_hook_ops.

	Signed-off-by: Phil Sutter <phil@nwl.cc>
	Signed-off-by: Pablo Neira Ayuso <pablo@netfilter.org>
(cherry picked from commit 75e20bcdce24b34e9bf71a7a731f9294fb2c633c)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/netfilter/nf_tables_api.c
diff --cc net/netfilter/nf_tables_api.c
index 2c8921ae4a05,9998fcf44a38..000000000000
--- a/net/netfilter/nf_tables_api.c
+++ b/net/netfilter/nf_tables_api.c
@@@ -2119,7 -2354,8 +2129,12 @@@ static int nf_tables_parse_netdev_hooks
  			goto err_hook;
  		}
  		if (nft_hook_list_find(hook_list, hook)) {
++<<<<<<< HEAD
 +			kfree(hook);
++=======
+ 			NL_SET_BAD_ATTR(extack, tmp);
+ 			nft_netdev_hook_free(hook);
++>>>>>>> 75e20bcdce24 (netfilter: nf_tables: Introduce functions freeing nft_hook objects)
  			err = -EEXIST;
  			goto err_hook;
  		}
@@@ -2539,12 -2795,18 +2554,27 @@@ static int nf_tables_updchain(struct nf
  			return -EEXIST;
  		}
  
++<<<<<<< HEAD
 +		if (nft_base_chain_netdev(ctx->family, hook.num)) {
 +			if (!nft_hook_list_equal(&basechain->hook_list,
 +						 &hook.list)) {
 +				nft_chain_release_hook(&hook);
 +				NL_SET_BAD_ATTR(extack, attr);
 +				return -EEXIST;
++=======
+ 		if (nft_base_chain_netdev(ctx->family, basechain->ops.hooknum)) {
+ 			list_for_each_entry_safe(h, next, &hook.list, list) {
+ 				h->ops.pf	= basechain->ops.pf;
+ 				h->ops.hooknum	= basechain->ops.hooknum;
+ 				h->ops.priority	= basechain->ops.priority;
+ 				h->ops.priv	= basechain->ops.priv;
+ 				h->ops.hook	= basechain->ops.hook;
+ 
+ 				if (nft_hook_list_find(&basechain->hook_list, h)) {
+ 					list_del(&h->list);
+ 					nft_netdev_hook_free(h);
+ 				}
++>>>>>>> 75e20bcdce24 (netfilter: nf_tables: Introduce functions freeing nft_hook objects)
  			}
  		} else {
  			ops = &basechain->ops;
@@@ -2622,9 -2909,28 +2652,23 @@@
  	nft_trans_commit_list_add_tail(ctx->net, trans);
  
  	return 0;
 -
 -err_trans:
 +err:
  	free_percpu(stats);
  	kfree(trans);
++<<<<<<< HEAD
++=======
+ err_hooks:
+ 	if (nla[NFTA_CHAIN_HOOK]) {
+ 		list_for_each_entry_safe(h, next, &hook.list, list) {
+ 			if (unregister)
+ 				nf_unregister_net_hook(ctx->net, &h->ops);
+ 			list_del(&h->list);
+ 			nft_netdev_hook_free_rcu(h);
+ 		}
+ 		module_put(hook.type->owner);
+ 	}
+ 
++>>>>>>> 75e20bcdce24 (netfilter: nf_tables: Introduce functions freeing nft_hook objects)
  	return err;
  }
  
@@@ -8235,21 -9011,7 +8279,25 @@@ static int nft_flowtable_update(struct 
  	list_for_each_entry_safe(hook, next, &flowtable_hook.list, list) {
  		if (nft_hook_list_find(&flowtable->hook_list, hook)) {
  			list_del(&hook->list);
++<<<<<<< HEAD
 +			kfree(hook);
 +			continue;
 +		}
 +
 +		nft_net = nft_pernet(ctx->net);
 +		list_for_each_entry(trans, &nft_net->commit_list, list) {
 +			if (trans->msg_type != NFT_MSG_NEWFLOWTABLE ||
 +			    trans->ctx.table != ctx->table ||
 +			    !nft_trans_flowtable_update(trans))
 +				continue;
 +
 +			if (nft_hook_list_find(&nft_trans_flowtable_hooks(trans), hook)) {
 +				err = -EEXIST;
 +				goto err_flowtable_update_hook;
 +			}
++=======
+ 			nft_netdev_hook_free(hook);
++>>>>>>> 75e20bcdce24 (netfilter: nf_tables: Introduce functions freeing nft_hook objects)
  		}
  	}
  
* Unmerged path net/netfilter/nf_tables_api.c
