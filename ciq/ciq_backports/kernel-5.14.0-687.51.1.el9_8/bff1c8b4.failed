netfilter: nft_meta_bridge: add validate callback for get operations

jira KERNEL-1646
Rebuild_History Non-Buildable kernel-5.14.0-687.51.1.el9_8
commit-author Florian Westphal <fw@strlen.de>
commit bff1c8b49a9cb5c04af20f4e7d43bf4af5863bc6
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-5.14.0-687.51.1.el9_8/bff1c8b4.failed

Blamed commit added NFT_META_BRI_IIFHWADDR to the set validate callback,
yet this is a get operation.

Add a get validate callback and move the NFT_META_BRI_IIFHWADDR key
there.

AFAICS this is harmless, NFT_META_BRI_IIFHWADDR can deal with a NULL
input device and the set handler ignores a NFT_META_BRI_IIFHWADDR
operation, but it allows to read 4 bytes off bridge skb->cb[].

Fixes: cbd2257dc96e ("netfilter: nft_meta_bridge: introduce NFT_META_BRI_IIFHWADDR support")
	Signed-off-by: Florian Westphal <fw@strlen.de>
	Reviewed-by: Fernando Fernandez Mancera <fmancera@suse.de>
	Signed-off-by: Pablo Neira Ayuso <pablo@netfilter.org>
(cherry picked from commit bff1c8b49a9cb5c04af20f4e7d43bf4af5863bc6)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/bridge/netfilter/nft_meta_bridge.c
#	net/netfilter/nft_meta.c
diff --cc net/bridge/netfilter/nft_meta_bridge.c
index 1967fd063cfb,3d95f68e0906..000000000000
--- a/net/bridge/netfilter/nft_meta_bridge.c
+++ b/net/bridge/netfilter/nft_meta_bridge.c
@@@ -100,6 -134,67 +118,70 @@@ static const struct nft_expr_ops nft_me
  	.dump		= nft_meta_get_dump,
  };
  
++<<<<<<< HEAD
++=======
+ static void nft_meta_bridge_set_eval(const struct nft_expr *expr,
+ 				     struct nft_regs *regs,
+ 				     const struct nft_pktinfo *pkt)
+ {
+ 	const struct nft_meta *meta = nft_expr_priv(expr);
+ 	u32 *sreg = &regs->data[meta->sreg];
+ 	struct sk_buff *skb = pkt->skb;
+ 	u8 value8;
+ 
+ 	switch (meta->key) {
+ 	case NFT_META_BRI_BROUTE:
+ 		value8 = nft_reg_load8(sreg);
+ 		BR_INPUT_SKB_CB(skb)->br_netfilter_broute = !!value8;
+ 		break;
+ 	default:
+ 		nft_meta_set_eval(expr, regs, pkt);
+ 	}
+ }
+ 
+ static int nft_meta_bridge_set_init(const struct nft_ctx *ctx,
+ 				    const struct nft_expr *expr,
+ 				    const struct nlattr * const tb[])
+ {
+ 	struct nft_meta *priv = nft_expr_priv(expr);
+ 	unsigned int len;
+ 	int err;
+ 
+ 	priv->key = ntohl(nla_get_be32(tb[NFTA_META_KEY]));
+ 	switch (priv->key) {
+ 	case NFT_META_BRI_BROUTE:
+ 		len = sizeof(u8);
+ 		break;
+ 	default:
+ 		return nft_meta_set_init(ctx, expr, tb);
+ 	}
+ 
+ 	priv->len = len;
+ 	err = nft_parse_register_load(ctx, tb[NFTA_META_SREG], &priv->sreg, len);
+ 	if (err < 0)
+ 		return err;
+ 
+ 	return 0;
+ }
+ 
+ static int nft_meta_bridge_set_validate(const struct nft_ctx *ctx,
+ 					const struct nft_expr *expr)
+ {
+ 	struct nft_meta *priv = nft_expr_priv(expr);
+ 	unsigned int hooks;
+ 
+ 	switch (priv->key) {
+ 	case NFT_META_BRI_BROUTE:
+ 		hooks = 1 << NF_BR_PRE_ROUTING;
+ 		break;
+ 	default:
+ 		return nft_meta_set_validate(ctx, expr);
+ 	}
+ 
+ 	return nft_chain_validate_hooks(ctx->chain, hooks);
+ }
+ 
++>>>>>>> bff1c8b49a9c (netfilter: nft_meta_bridge: add validate callback for get operations)
  static const struct nft_expr_ops nft_meta_bridge_set_ops = {
  	.type		= &nft_meta_bridge_type,
  	.size		= NFT_EXPR_SIZE(sizeof(struct nft_meta)),
diff --cc net/netfilter/nft_meta.c
index af8f709a4e79,0a43e0787a68..000000000000
--- a/net/netfilter/nft_meta.c
+++ b/net/netfilter/nft_meta.c
@@@ -579,9 -635,8 +579,14 @@@ static int nft_meta_get_validate_xfrm(c
  #endif
  }
  
++<<<<<<< HEAD
 +static int nft_meta_get_validate(const struct nft_ctx *ctx,
 +				 const struct nft_expr *expr,
 +				 const struct nft_data **data)
++=======
+ int nft_meta_get_validate(const struct nft_ctx *ctx,
+ 			  const struct nft_expr *expr)
++>>>>>>> bff1c8b49a9c (netfilter: nft_meta_bridge: add validate callback for get operations)
  {
  	const struct nft_meta *priv = nft_expr_priv(expr);
  
@@@ -597,10 -652,10 +602,11 @@@
  
  	return 0;
  }
+ EXPORT_SYMBOL_GPL(nft_meta_get_validate);
  
  int nft_meta_set_validate(const struct nft_ctx *ctx,
 -			  const struct nft_expr *expr)
 +			  const struct nft_expr *expr,
 +			  const struct nft_data **data)
  {
  	struct nft_meta *priv = nft_expr_priv(expr);
  	unsigned int hooks;
diff --git a/include/net/netfilter/nft_meta.h b/include/net/netfilter/nft_meta.h
index 690f6245026c..e0004bedbf78 100644
--- a/include/net/netfilter/nft_meta.h
+++ b/include/net/netfilter/nft_meta.h
@@ -40,6 +40,8 @@ void nft_meta_set_eval(const struct nft_expr *expr,
 void nft_meta_set_destroy(const struct nft_ctx *ctx,
 			  const struct nft_expr *expr);
 
+int nft_meta_get_validate(const struct nft_ctx *ctx,
+			  const struct nft_expr *expr);
 int nft_meta_set_validate(const struct nft_ctx *ctx,
 			  const struct nft_expr *expr,
 			  const struct nft_data **data);
* Unmerged path net/bridge/netfilter/nft_meta_bridge.c
* Unmerged path net/netfilter/nft_meta.c
