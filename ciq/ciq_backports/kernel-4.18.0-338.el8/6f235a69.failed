ch_ktls: fix enum-conversion warning

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-338.el8
commit-author Arnd Bergmann <arnd@arndb.de>
commit 6f235a69e59484e382dc31952025b0308efedc17
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-338.el8/6f235a69.failed

gcc points out an incorrect enum assignment:

drivers/net/ethernet/chelsio/inline_crypto/ch_ktls/chcr_ktls.c: In function 'chcr_ktls_cpl_set_tcb_rpl':
drivers/net/ethernet/chelsio/inline_crypto/ch_ktls/chcr_ktls.c:684:22: warning: implicit conversion from 'enum <anonymous>' to 'enum ch_ktls_open_state' [-Wenum-conversion]

This appears harmless, and should apparently use 'CH_KTLS_OPEN_SUCCESS'
instead of 'false', with the same value '0'.

Fixes: efca3878a5fb ("ch_ktls: Issue if connection offload fails")
	Reviewed-by: Andrew Lunn <andrew@lunn.ch>
	Signed-off-by: Arnd Bergmann <arnd@arndb.de>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 6f235a69e59484e382dc31952025b0308efedc17)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/crypto/chelsio/chcr_ktls.c
diff --cc drivers/crypto/chelsio/chcr_ktls.c
index 887ecc2060e8,1115b8f9ea4e..000000000000
--- a/drivers/crypto/chelsio/chcr_ktls.c
+++ b/drivers/crypto/chelsio/chcr_ktls.c
@@@ -650,12 -710,22 +650,26 @@@ int chcr_ktls_cpl_set_tcb_rpl(struct ad
  
  	t = &adap->tids;
  	tx_info = lookup_tid(t, tid);
 -
  	if (!tx_info || tx_info->tid != tid) {
 -		pr_err("%s: incorrect tx_info or tid\n", __func__);
 +		pr_err("tx_info or atid is not correct\n");
  		return -1;
  	}
++<<<<<<< HEAD:drivers/crypto/chelsio/chcr_ktls.c
 +	/* update the connection state */
 +	chcr_ktls_update_connection_state(tx_info, KTLS_CONN_SET_TCB_RPL);
++=======
+ 
+ 	spin_lock(&tx_info->lock);
+ 	if (tx_info->pending_close) {
+ 		spin_unlock(&tx_info->lock);
+ 		kvfree(tx_info);
+ 		return 0;
+ 	}
+ 	tx_info->open_state = CH_KTLS_OPEN_SUCCESS;
+ 	spin_unlock(&tx_info->lock);
+ 
+ 	complete(&tx_info->completion);
++>>>>>>> 6f235a69e594 (ch_ktls: fix enum-conversion warning):drivers/net/ethernet/chelsio/inline_crypto/ch_ktls/chcr_ktls.c
  	return 0;
  }
  
* Unmerged path drivers/crypto/chelsio/chcr_ktls.c
