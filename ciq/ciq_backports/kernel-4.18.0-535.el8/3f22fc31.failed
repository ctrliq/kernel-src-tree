ice: remove unnecessary &array[0] and just use array

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-535.el8
Rebuild_CHGLOG: - ice: remove unnecessary &array[0] and just use array (Petr Oros) [RHEL-15678]
Rebuild_FUZZ: 96.00%
commit-author Jacob Keller <jacob.e.keller@intel.com>
commit 3f22fc3131b814f0d5d1ce6d94fb8239e6df1754
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-535.el8/3f22fc31.failed

In ice_is_malicious_vf we print the VF MAC address using %pM by passing the
address of the first element of vf->dev_lan_addr. This is equivalent to
just passing vf->dev_lan_addr, so do that.

	Signed-off-by: Jacob Keller <jacob.e.keller@intel.com>
	Reviewed-by: Michal Swiatkowski <michal.swiatkowski@linux.intel.com>
	Tested-by: Marek Szlosek <marek.szlosek@intel.com>
	Signed-off-by: Tony Nguyen <anthony.l.nguyen@intel.com>
(cherry picked from commit 3f22fc3131b814f0d5d1ce6d94fb8239e6df1754)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/intel/ice/ice_sriov.c
diff --cc drivers/net/ethernet/intel/ice/ice_sriov.c
index 33fbbf188926,938be486721e..000000000000
--- a/drivers/net/ethernet/intel/ice/ice_sriov.c
+++ b/drivers/net/ethernet/intel/ice/ice_sriov.c
@@@ -1813,25 -1815,13 +1813,31 @@@ ice_is_malicious_vf(struct ice_pf *pf, 
  	if (status)
  		goto out_put_vf;
  
 -	if (report_malvf) {
 -		struct ice_vsi *pf_vsi = ice_get_main_vsi(pf);
 -		u8 zero_addr[ETH_ALEN] = {};
 +	if (malvf) {
 +		bool report_vf = false;
  
++<<<<<<< HEAD
 +		/* if the VF is malicious and we haven't let the user
 +		 * know about it, then let them know now
 +		 */
 +		status = ice_mbx_report_malvf(&pf->hw, &vf->mbx_info,
 +					      &report_vf);
 +		if (status)
 +			dev_dbg(dev, "Error reporting malicious VF\n");
 +
 +		if (report_vf) {
 +			struct ice_vsi *pf_vsi = ice_get_main_vsi(pf);
 +
 +			if (pf_vsi)
 +				dev_warn(dev, "VF MAC %pM on PF MAC %pM is generating asynchronous messages and may be overflowing the PF message queue. Please see the Adapter User Guide for more information\n",
 +					 &vf->dev_lan_addr[0],
 +					 pf_vsi->netdev->dev_addr);
 +		}
++=======
+ 		dev_warn(dev, "VF MAC %pM on PF MAC %pM is generating asynchronous messages and may be overflowing the PF message queue. Please see the Adapter User Guide for more information\n",
+ 			 vf->dev_lan_addr,
+ 			 pf_vsi ? pf_vsi->netdev->dev_addr : zero_addr);
++>>>>>>> 3f22fc3131b8 (ice: remove unnecessary &array[0] and just use array)
  	}
  
  out_put_vf:
* Unmerged path drivers/net/ethernet/intel/ice/ice_sriov.c
