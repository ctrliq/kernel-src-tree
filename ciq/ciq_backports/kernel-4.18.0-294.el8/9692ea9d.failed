smb3: remove overly noisy debug line in signing errors

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-294.el8
commit-author Steve French <stfrench@microsoft.com>
commit 9692ea9d3288a201df762868a52552b2e07e1c55
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-294.el8/9692ea9d.failed

A dump_stack call for signature related errors can be too noisy
and not of much value in debugging such problems.

	Signed-off-by: Steve French <stfrench@microsoft.com>
	Reviewed-by: Shyam Prasad N <nspmangalore@gmail.com>
(cherry picked from commit 9692ea9d3288a201df762868a52552b2e07e1c55)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	fs/cifs/smb2transport.c
diff --cc fs/cifs/smb2transport.c
index 3f3760b3a0d5,c0348e3b1695..000000000000
--- a/fs/cifs/smb2transport.c
+++ b/fs/cifs/smb2transport.c
@@@ -659,9 -659,11 +659,15 @@@ smb2_verify_signature(struct smb_rqst *
  	if (rc)
  		return rc;
  
++<<<<<<< HEAD
 +	if (memcmp(server_response_sig, shdr->Signature, SMB2_SIGNATURE_SIZE))
++=======
+ 	if (memcmp(server_response_sig, shdr->Signature, SMB2_SIGNATURE_SIZE)) {
+ 		cifs_dbg(VFS, "sign fail cmd 0x%x message id 0x%llx\n",
+ 			shdr->Command, shdr->MessageId);
++>>>>>>> 9692ea9d3288 (smb3: remove overly noisy debug line in signing errors)
  		return -EACCES;
 -	} else
 +	else
  		return 0;
  }
  
* Unmerged path fs/cifs/smb2transport.c
