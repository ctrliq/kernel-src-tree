net_sched: sch_fq: no longer use skb_is_tcp_pure_ack()

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-294.el8
commit-author Eric Dumazet <edumazet@google.com>
commit 7baf33bdac37da65ddce3adf4daa8c7805802174
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-294.el8/7baf33bd.failed

With the new EDT model, sch_fq no longer has to special
case TCP pure acks, since their skb->tstamp will allow them
being sent without pacing delay.

	Signed-off-by: Eric Dumazet <edumazet@google.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 7baf33bdac37da65ddce3adf4daa8c7805802174)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/sched/sch_fq.c
diff --cc net/sched/sch_fq.c
index 520fc183e0d7,4b1af706896c..000000000000
--- a/net/sched/sch_fq.c
+++ b/net/sched/sch_fq.c
@@@ -452,10 -444,13 +452,17 @@@ begin
  	}
  
  	skb = f->head;
++<<<<<<< HEAD
 +	if (skb && !skb_is_tcp_pure_ack(skb)) {
 +		u64 time_next_packet = f->time_next_packet;
++=======
+ 	if (skb) {
+ 		u64 time_next_packet = max_t(u64, ktime_to_ns(skb->tstamp),
+ 					     f->time_next_packet);
+ 
++>>>>>>> 7baf33bdac37 (net_sched: sch_fq: no longer use skb_is_tcp_pure_ack())
  		if (now < time_next_packet) {
  			head->first = f->next;
 -			f->time_next_packet = time_next_packet;
  			fq_flow_set_throttled(q, f);
  			goto begin;
  		}
* Unmerged path net/sched/sch_fq.c
