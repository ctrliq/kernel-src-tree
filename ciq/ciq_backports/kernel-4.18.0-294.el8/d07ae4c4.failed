dma-mapping: DMA_COHERENT_POOL should select GENERIC_ALLOCATOR

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-294.el8
commit-author Christoph Hellwig <hch@lst.de>
commit d07ae4c486908615ab336b987c7c367d132fd844
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-294.el8/d07ae4c4.failed

The dma coherent pool code needs genalloc.  Move the select over
from DMA_REMAP, which doesn't actually need it.

Fixes: dbed452a078d ("dma-pool: decouple DMA_REMAP from DMA_COHERENT_POOL")
	Reported-by: kernel test robot <lkp@intel.com>
	Signed-off-by: Christoph Hellwig <hch@lst.de>
	Acked-by: David Rientjes <rientjes@google.com>
(cherry picked from commit d07ae4c486908615ab336b987c7c367d132fd844)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	kernel/dma/Kconfig
diff --cc kernel/dma/Kconfig
index 1f8c9d04062a,1da3f44f2565..000000000000
--- a/kernel/dma/Kconfig
+++ b/kernel/dma/Kconfig
@@@ -70,11 -74,14 +70,17 @@@ config DMA_NONCOHERENT_MMA
  	default y if !MMU
  	bool
  
++<<<<<<< HEAD
++=======
+ config DMA_COHERENT_POOL
+ 	select GENERIC_ALLOCATOR
+ 	bool
+ 
++>>>>>>> d07ae4c48690 (dma-mapping: DMA_COHERENT_POOL should select GENERIC_ALLOCATOR)
  config DMA_REMAP
 -	bool
  	depends on MMU
- 	select GENERIC_ALLOCATOR
  	select DMA_NONCOHERENT_MMAP
 +	bool
  
  config DMA_DIRECT_REMAP
  	bool
* Unmerged path kernel/dma/Kconfig
