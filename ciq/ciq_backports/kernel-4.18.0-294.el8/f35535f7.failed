qede: reformat several structures in "qede.h"

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-294.el8
commit-author Alexander Lobakin <alobakin@marvell.com>
commit f35535f73c1c7538c26e3468562ca08ecfce9735
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-294.el8/f35535f7.failed

Make the file more readable and easier for adding new fields.

Misc: use IFNAMSIZ and netdev_name() instead of sizeof_field()
and direct net_device::name dereferencing.

	Signed-off-by: Alexander Lobakin <alobakin@marvell.com>
	Signed-off-by: Igor Russkikh <irusskikh@marvell.com>
	Signed-off-by: Michal Kalderon <michal.kalderon@marvell.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit f35535f73c1c7538c26e3468562ca08ecfce9735)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/qlogic/qede/qede.h
diff --cc drivers/net/ethernet/qlogic/qede/qede.h
index 2f666109bc6a,e8ed0bb94ee0..000000000000
--- a/drivers/net/ethernet/qlogic/qede/qede.h
+++ b/drivers/net/ethernet/qlogic/qede/qede.h
@@@ -434,22 -444,25 +443,30 @@@ struct qede_tx_queue 
  #define BD_UNMAP_LEN(bd)		(le16_to_cpu((bd)->nbytes))
  
  struct qede_fastpath {
- 	struct qede_dev	*edev;
- #define QEDE_FASTPATH_TX	BIT(0)
- #define QEDE_FASTPATH_RX	BIT(1)
- #define QEDE_FASTPATH_XDP	BIT(2)
- #define QEDE_FASTPATH_COMBINED	(QEDE_FASTPATH_TX | QEDE_FASTPATH_RX)
- 	u8			type;
- 	u8			id;
- 	u8			xdp_xmit;
- 	struct napi_struct	napi;
- 	struct qed_sb_info	*sb_info;
- 	struct qede_rx_queue	*rxq;
- 	struct qede_tx_queue	*txq;
- 	struct qede_tx_queue	*xdp_tx;
+ 	struct qede_dev			*edev;
  
++<<<<<<< HEAD
 +#define VEC_NAME_SIZE  (FIELD_SIZEOF(struct net_device, name) + 8)
 +	char	name[VEC_NAME_SIZE];
++=======
+ 	u8				type;
+ #define QEDE_FASTPATH_TX		BIT(0)
+ #define QEDE_FASTPATH_RX		BIT(1)
+ #define QEDE_FASTPATH_XDP		BIT(2)
+ #define QEDE_FASTPATH_COMBINED		(QEDE_FASTPATH_TX | QEDE_FASTPATH_RX)
+ 
+ 	u8				id;
+ 
+ 	u8				xdp_xmit;
+ 
+ 	struct napi_struct		napi;
+ 	struct qed_sb_info		*sb_info;
+ 	struct qede_rx_queue		*rxq;
+ 	struct qede_tx_queue		*txq;
+ 	struct qede_tx_queue		*xdp_tx;
+ 
+ 	char				name[IFNAMSIZ + 8];
++>>>>>>> f35535f73c1c (qede: reformat several structures in "qede.h")
  };
  
  /* Debug print definitions */
* Unmerged path drivers/net/ethernet/qlogic/qede/qede.h
