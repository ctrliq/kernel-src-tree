ASoC: soc-pcm: remove unneeded dev_err() for snd_soc_dai_startup()

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-294.el8
Rebuild_CHGLOG: - [sound] ALSA: ASoC: soc-pcm: remove unneeded dev_err() for snd_soc_dai_startup() (Jaroslav Kysela) [1783462]
Rebuild_FUZZ: 95.65%
commit-author Kuninori Morimoto <kuninori.morimoto.gx@renesas.com>
commit ce820145a9ec04797a417fcb01b8ff02dcfd9846
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-294.el8/ce820145.failed

snd_soc_dai_startup() itself will indicate error message,
thus, soc_pcm_open() don't need to handle it.
This patch removes it.

	Signed-off-by: Kuninori Morimoto <kuninori.morimoto.gx@renesas.com>
Link: https://lore.kernel.org/r/87eemmbwmy.wl-kuninori.morimoto.gx@renesas.com
	Signed-off-by: Mark Brown <broonie@kernel.org>
(cherry picked from commit ce820145a9ec04797a417fcb01b8ff02dcfd9846)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	sound/soc/soc-pcm.c
diff --cc sound/soc/soc-pcm.c
index 82d806ac486a,070fb71fc6f6..000000000000
--- a/sound/soc/soc-pcm.c
+++ b/sound/soc/soc-pcm.c
@@@ -620,12 -729,8 +620,17 @@@ static int soc_pcm_open(struct snd_pcm_
  	/* startup the audio subsystem */
  	for_each_rtd_dais(rtd, i, dai) {
  		ret = snd_soc_dai_startup(dai, substream);
++<<<<<<< HEAD
 +		if (ret < 0) {
 +			dev_err(dai->dev,
 +				"ASoC: can't open DAI %s: %d\n",
 +				dai->name, ret);
 +			goto config_err;
 +		}
++=======
+ 		if (ret < 0)
+ 			goto err;
++>>>>>>> ce820145a9ec (ASoC: soc-pcm: remove unneeded dev_err() for snd_soc_dai_startup())
  
  		if (substream->stream == SNDRV_PCM_STREAM_PLAYBACK)
  			dai->tx_mask = 0;
* Unmerged path sound/soc/soc-pcm.c
