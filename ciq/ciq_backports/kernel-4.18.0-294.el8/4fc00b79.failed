bpf: Add missing newline characters in verifier error messages

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-294.el8
commit-author Yonghong Song <yhs@fb.com>
commit 4fc00b79b85d4c34bef06ad49f109ad7cd9e5d83
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-294.el8/4fc00b79.failed

Newline characters are added in two verifier error messages,
refactored in Commit afbf21dce668 ("bpf: Support readonly/readwrite
buffers in verifier"). This way, they do not mix with
messages afterwards.

	Signed-off-by: Yonghong Song <yhs@fb.com>
	Signed-off-by: Daniel Borkmann <daniel@iogearbox.net>
	Acked-by: Song Liu <songliubraving@fb.com>
Link: https://lore.kernel.org/bpf/20200728221801.1090349-1-yhs@fb.com
(cherry picked from commit 4fc00b79b85d4c34bef06ad49f109ad7cd9e5d83)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	kernel/bpf/verifier.c
diff --cc kernel/bpf/verifier.c
index b8aec186e303,b6ccfce3bf4c..000000000000
--- a/kernel/bpf/verifier.c
+++ b/kernel/bpf/verifier.c
@@@ -3025,8 -3069,8 +3025,13 @@@ static int check_tp_buffer_access(struc
  {
  	if (off < 0) {
  		verbose(env,
++<<<<<<< HEAD
 +			"R%d invalid tracepoint buffer access: off=%d, size=%d",
 +			regno, off, size);
++=======
+ 			"R%d invalid %s buffer access: off=%d, size=%d\n",
+ 			regno, buf_info, off, size);
++>>>>>>> 4fc00b79b85d (bpf: Add missing newline characters in verifier error messages)
  		return -EACCES;
  	}
  	if (!tnum_is_const(reg->var_off) || reg->var_off.value) {
* Unmerged path kernel/bpf/verifier.c
