vfio/pci: Remove redundant declaration of vfio_pci_driver

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-294.el8
commit-author Zenghui Yu <yuzenghui@huawei.com>
commit eac7cc21c4e49ab7dfeef22e29abdee8432face9
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-294.el8/eac7cc21.failed

It was added by commit 137e5531351d ("vfio/pci: Add sriov_configure
support") but duplicates a forward declaration earlier in the file.

Remove it.

	Signed-off-by: Zenghui Yu <yuzenghui@huawei.com>
	Reviewed-by: Cornelia Huck <cohuck@redhat.com>
	Signed-off-by: Alex Williamson <alex.williamson@redhat.com>
(cherry picked from commit eac7cc21c4e49ab7dfeef22e29abdee8432face9)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/vfio/pci/vfio_pci.c
diff --cc drivers/vfio/pci/vfio_pci.c
index 00cc893e8bdd,da68e2f86622..000000000000
--- a/drivers/vfio/pci/vfio_pci.c
+++ b/drivers/vfio/pci/vfio_pci.c
@@@ -1644,6 -1862,34 +1644,37 @@@ static const struct vfio_device_ops vfi
  
  static int vfio_pci_reflck_attach(struct vfio_pci_device *vdev);
  static void vfio_pci_reflck_put(struct vfio_pci_reflck *reflck);
++<<<<<<< HEAD
++=======
+ 
+ static int vfio_pci_bus_notifier(struct notifier_block *nb,
+ 				 unsigned long action, void *data)
+ {
+ 	struct vfio_pci_device *vdev = container_of(nb,
+ 						    struct vfio_pci_device, nb);
+ 	struct device *dev = data;
+ 	struct pci_dev *pdev = to_pci_dev(dev);
+ 	struct pci_dev *physfn = pci_physfn(pdev);
+ 
+ 	if (action == BUS_NOTIFY_ADD_DEVICE &&
+ 	    pdev->is_virtfn && physfn == vdev->pdev) {
+ 		pci_info(vdev->pdev, "Captured SR-IOV VF %s driver_override\n",
+ 			 pci_name(pdev));
+ 		pdev->driver_override = kasprintf(GFP_KERNEL, "%s",
+ 						  vfio_pci_ops.name);
+ 	} else if (action == BUS_NOTIFY_BOUND_DRIVER &&
+ 		   pdev->is_virtfn && physfn == vdev->pdev) {
+ 		struct pci_driver *drv = pci_dev_driver(pdev);
+ 
+ 		if (drv && drv != &vfio_pci_driver)
+ 			pci_warn(vdev->pdev,
+ 				 "VF %s bound to driver %s while PF bound to vfio-pci\n",
+ 				 pci_name(pdev), drv->name);
+ 	}
+ 
+ 	return 0;
+ }
++>>>>>>> eac7cc21c4e4 (vfio/pci: Remove redundant declaration of vfio_pci_driver)
  
  static int vfio_pci_probe(struct pci_dev *pdev, const struct pci_device_id *id)
  {
* Unmerged path drivers/vfio/pci/vfio_pci.c
