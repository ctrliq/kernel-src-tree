net: stmmac: xgmac: Fix RSS not writing all Keys to HW

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-294.el8
commit-author Jose Abreu <Jose.Abreu@synopsys.com>
commit 3c72d4d33059cf84a23492e827731f12c023ab00
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-294.el8/3c72d4d3.failed

The sizeof(cfg->key) is != ARRAY_SIZE(cfg->key). Fix it. This warning is
triggered when running with cc flag -Wsizeof-array-div.

	Reported-by: kbuild test robot <lkp@intel.com>
	Reported-by: Nick Desaulniers <ndesaulniers@google.com>
	Reported-by: Nathan Chancellor <natechancellor@gmail.com>
Fixes: 76067459c686 ("net: stmmac: Implement RSS and enable it in XGMAC core")
	Reviewed-by: Nick Desaulniers <ndesaulniers@google.com>
	Signed-off-by: Jose Abreu <Jose.Abreu@synopsys.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 3c72d4d33059cf84a23492e827731f12c023ab00)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/stmicro/stmmac/dwxgmac2_core.c
diff --cc drivers/net/ethernet/stmicro/stmmac/dwxgmac2_core.c
index 416a4a204a6f,4a1f52474dbc..000000000000
--- a/drivers/net/ethernet/stmicro/stmmac/dwxgmac2_core.c
+++ b/drivers/net/ethernet/stmicro/stmmac/dwxgmac2_core.c
@@@ -534,8 -533,8 +534,13 @@@ static int dwxgmac2_rss_configure(struc
  		return 0;
  	}
  
++<<<<<<< HEAD
 +	for (i = 0; i < (sizeof(cfg->key) / sizeof(u32)); i++) {
 +		ret = dwxgmac2_rss_write_reg(ioaddr, true, i, *key++);
++=======
+ 	for (i = 0; i < (ARRAY_SIZE(cfg->key) / sizeof(u32)); i++) {
+ 		ret = dwxgmac2_rss_write_reg(ioaddr, true, i, cfg->key[i]);
++>>>>>>> 3c72d4d33059 (net: stmmac: xgmac: Fix RSS not writing all Keys to HW)
  		if (ret)
  			return ret;
  	}
* Unmerged path drivers/net/ethernet/stmicro/stmmac/dwxgmac2_core.c
