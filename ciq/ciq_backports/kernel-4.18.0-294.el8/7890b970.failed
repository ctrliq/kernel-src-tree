nvme-multipath: stop using ->queuedata

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-294.el8
commit-author Christoph Hellwig <hch@lst.de>
commit 7890b9701b792a4d75b4adef4abe325383ccfca4
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-294.el8/7890b970.failed

nvme-multipath already uses the gendisk private data, not need to
also set up the request_queue queuedata and use it in one place only.

	Signed-off-by: Christoph Hellwig <hch@lst.de>
	Reviewed-by: Sagi Grimberg <sagi@grimberg.me>
	Signed-off-by: Jens Axboe <axboe@kernel.dk>
(cherry picked from commit 7890b9701b792a4d75b4adef4abe325383ccfca4)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/nvme/host/multipath.c
diff --cc drivers/nvme/host/multipath.c
index 8b9db3d66ae9,5f5537d0a024..000000000000
--- a/drivers/nvme/host/multipath.c
+++ b/drivers/nvme/host/multipath.c
@@@ -380,11 -375,9 +380,14 @@@ int nvme_mpath_alloc_disk(struct nvme_c
  	if (!(ctrl->subsys->cmic & (1 << 1)) || !multipath)
  		return 0;
  
 -	q = blk_alloc_queue(nvme_ns_head_make_request, ctrl->numa_node);
 +	q = blk_alloc_queue_node(GFP_KERNEL, ctrl->numa_node);
  	if (!q)
  		goto out;
++<<<<<<< HEAD
 +	q->queuedata = head;
 +	blk_queue_make_request(q, nvme_ns_head_make_request);
++=======
++>>>>>>> 7890b9701b79 (nvme-multipath: stop using ->queuedata)
  	blk_queue_flag_set(QUEUE_FLAG_NONROT, q);
  	/* set to a default value for 512 until disk is validated */
  	blk_queue_logical_block_size(q, 512);
* Unmerged path drivers/nvme/host/multipath.c
