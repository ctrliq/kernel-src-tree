net: Removed the device type check to add mpls support for devices

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-294.el8
Rebuild_CHGLOG: - [net] Removed the device type check to add mpls support for devices (Guillaume Nault) [1839788]
Rebuild_FUZZ: 96.06%
commit-author Martin Varghese <martin.varghese@nokia.com>
commit 350e7ab92da8420fe97e1edf040db8c9bab750f8
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-294.el8/350e7ab9.failed

MPLS has no dependency with the device type of underlying devices.
Hence the device type check to add mpls support for devices can be
avoided.

	Signed-off-by: Martin Varghese <martin.varghese@nokia.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 350e7ab92da8420fe97e1edf040db8c9bab750f8)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/mpls/af_mpls.c
diff --cc net/mpls/af_mpls.c
index 6d8199472765,6fdd0c9f865a..000000000000
--- a/net/mpls/af_mpls.c
+++ b/net/mpls/af_mpls.c
@@@ -1586,16 -1584,10 +1586,23 @@@ static int mpls_dev_notify(struct notif
  	unsigned int flags;
  
  	if (event == NETDEV_REGISTER) {
++<<<<<<< HEAD
 +		/* For now just support Ethernet, IPGRE, SIT and IPIP devices */
 +		if (dev->type == ARPHRD_ETHER ||
 +		    dev->type == ARPHRD_LOOPBACK ||
 +		    dev->type == ARPHRD_IPGRE ||
 +		    dev->type == ARPHRD_SIT ||
 +		    dev->type == ARPHRD_TUNNEL) {
 +			mdev = mpls_add_dev(dev);
 +			if (IS_ERR(mdev))
 +				return notifier_from_errno(PTR_ERR(mdev));
 +		}
++=======
+ 		mdev = mpls_add_dev(dev);
+ 		if (IS_ERR(mdev))
+ 			return notifier_from_errno(PTR_ERR(mdev));
+ 
++>>>>>>> 350e7ab92da8 (net: Removed the device type check to add mpls support for devices)
  		return NOTIFY_OK;
  	}
  
* Unmerged path net/mpls/af_mpls.c
