net: stmmac: Fix VLAN filtering when HW does not support it

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-294.el8
commit-author Jose Abreu <Jose.Abreu@synopsys.com>
commit dd6a4998e64a7806b54c3eba1e5e7bf6c81ccf8c
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-294.el8/dd6a4998.failed

If we don't have any filters available we can't rely upon the return
code of stmmac_add_hw_vlan_rx_fltr() / stmmac_del_hw_vlan_rx_fltr(). Add
a check for this.

Fixes: ed64639bc1e0 ("net: stmmac: Add support for VLAN Rx filtering")
	Signed-off-by: Jose Abreu <Jose.Abreu@synopsys.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit dd6a4998e64a7806b54c3eba1e5e7bf6c81ccf8c)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/stmicro/stmmac/stmmac_main.c
diff --cc drivers/net/ethernet/stmicro/stmmac/stmmac_main.c
index 55e88ad23227,e6898fd5223f..000000000000
--- a/drivers/net/ethernet/stmicro/stmmac/stmmac_main.c
+++ b/drivers/net/ethernet/stmicro/stmmac/stmmac_main.c
@@@ -4391,7 -4566,13 +4391,17 @@@ static int stmmac_vlan_rx_add_vid(struc
  		return ret;
  	}
  
++<<<<<<< HEAD
 +	return ret;
++=======
+ 	if (priv->hw->num_vlan) {
+ 		ret = stmmac_add_hw_vlan_rx_fltr(priv, ndev, priv->hw, proto, vid);
+ 		if (ret)
+ 			return ret;
+ 	}
+ 
+ 	return 0;
++>>>>>>> dd6a4998e64a (net: stmmac: Fix VLAN filtering when HW does not support it)
  }
  
  static int stmmac_vlan_rx_kill_vid(struct net_device *ndev, __be16 proto, u16 vid)
@@@ -4403,6 -4585,13 +4413,16 @@@
  		is_double = true;
  
  	clear_bit(vid, priv->active_vlans);
++<<<<<<< HEAD
++=======
+ 
+ 	if (priv->hw->num_vlan) {
+ 		ret = stmmac_del_hw_vlan_rx_fltr(priv, ndev, priv->hw, proto, vid);
+ 		if (ret)
+ 			return ret;
+ 	}
+ 
++>>>>>>> dd6a4998e64a (net: stmmac: Fix VLAN filtering when HW does not support it)
  	return stmmac_vlan_update(priv, is_double);
  }
  
* Unmerged path drivers/net/ethernet/stmicro/stmmac/stmmac_main.c
