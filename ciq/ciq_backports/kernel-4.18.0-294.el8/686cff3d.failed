net: stmmac: Fix incorrect location to set real_num_rx|tx_queues

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-294.el8
commit-author Aashish Verma <aashishx.verma@intel.com>
commit 686cff3d7022ddf35d7e38bc80191eb92de5989a
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-294.el8/686cff3d.failed

netif_set_real_num_tx_queues() & netif_set_real_num_rx_queues() should be
used to inform network stack about the real Tx & Rx queue (active) number
in both stmmac_open() and stmmac_resume(), therefore, we move the code
from stmmac_dvr_probe() to stmmac_hw_setup().

Fixes: c02b7a914551 net: stmmac: use netif_set_real_num_{rx,tx}_queues

	Signed-off-by: Aashish Verma <aashishx.verma@intel.com>
	Signed-off-by: Ong Boon Leong <boon.leong.ong@intel.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 686cff3d7022ddf35d7e38bc80191eb92de5989a)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/stmicro/stmmac/stmmac_main.c
diff --cc drivers/net/ethernet/stmicro/stmmac/stmmac_main.c
index 1060fe24c01a,178aa33844fd..000000000000
--- a/drivers/net/ethernet/stmicro/stmmac/stmmac_main.c
+++ b/drivers/net/ethernet/stmicro/stmmac/stmmac_main.c
@@@ -2662,6 -2724,18 +2662,21 @@@ static int stmmac_hw_setup(struct net_d
  	if (priv->dma_cap.vlins)
  		stmmac_enable_vlan(priv, priv->hw, STMMAC_VLAN_INSERT);
  
++<<<<<<< HEAD
++=======
+ 	/* TBS */
+ 	for (chan = 0; chan < tx_cnt; chan++) {
+ 		struct stmmac_tx_queue *tx_q = &priv->tx_queue[chan];
+ 		int enable = tx_q->tbs & STMMAC_TBS_AVAIL;
+ 
+ 		stmmac_enable_tbs(priv, priv->ioaddr, enable, chan);
+ 	}
+ 
+ 	/* Configure real RX and TX queues */
+ 	netif_set_real_num_rx_queues(dev, priv->plat->rx_queues_to_use);
+ 	netif_set_real_num_tx_queues(dev, priv->plat->tx_queues_to_use);
+ 
++>>>>>>> 686cff3d7022 (net: stmmac: Fix incorrect location to set real_num_rx|tx_queues)
  	/* Start the ball rolling... */
  	stmmac_start_all_dma(priv);
  
* Unmerged path drivers/net/ethernet/stmicro/stmmac/stmmac_main.c
