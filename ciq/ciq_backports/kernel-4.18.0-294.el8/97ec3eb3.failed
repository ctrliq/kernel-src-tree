tcp_bbr: fix typo in bbr_pacing_margin_percent

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-294.el8
commit-author Neal Cardwell <ncardwell@google.com>
commit 97ec3eb33dd300764ca5fa8923e5430dc4dbac13
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-294.el8/97ec3eb3.failed

There was a typo in this parameter name.

	Signed-off-by: Neal Cardwell <ncardwell@google.com>
	Signed-off-by: Eric Dumazet <edumazet@google.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 97ec3eb33dd300764ca5fa8923e5430dc4dbac13)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/ipv4/tcp_bbr.c
diff --cc net/ipv4/tcp_bbr.c
index c75c19bf9668,b88081285fd1..000000000000
--- a/net/ipv4/tcp_bbr.c
+++ b/net/ipv4/tcp_bbr.c
@@@ -128,6 -128,9 +128,12 @@@ static const u32 bbr_probe_rtt_mode_ms 
  /* Skip TSO below the following bandwidth (bits/sec): */
  static const int bbr_min_tso_rate = 1200000;
  
++<<<<<<< HEAD
++=======
+ /* Pace at ~1% below estimated bw, on average, to reduce queue at bottleneck. */
+ static const int bbr_pacing_margin_percent = 1;
+ 
++>>>>>>> 97ec3eb33dd3 (tcp_bbr: fix typo in bbr_pacing_margin_percent)
  /* We use a high_gain value of 2/ln(2) because it's the smallest pacing gain
   * that will allow a smoothly increasing pacing rate that will double each RTT
   * and send the same number of packets per RTT that an un-paced, slow-starting
@@@ -213,7 -214,7 +219,11 @@@ static u64 bbr_rate_bytes_per_sec(struc
  	rate *= mss;
  	rate *= gain;
  	rate >>= BBR_SCALE;
++<<<<<<< HEAD
 +	rate *= USEC_PER_SEC;
++=======
+ 	rate *= USEC_PER_SEC / 100 * (100 - bbr_pacing_margin_percent);
++>>>>>>> 97ec3eb33dd3 (tcp_bbr: fix typo in bbr_pacing_margin_percent)
  	return rate >> BW_SCALE;
  }
  
* Unmerged path net/ipv4/tcp_bbr.c
