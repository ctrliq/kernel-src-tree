driver core: Change delimiter in devlink device's name to "--"

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-294.el8
commit-author Saravana Kannan <saravanak@google.com>
commit 90b109d50da09ddaa179732c01ccba7f759c125d
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-294.el8/90b109d5.failed

The devlink device name is of the form "supplier:consumer". But ":" is
fairly common in device names and makes it visually hard to distinguish
supplier and consumer. So, replace it with "--" to make it easier.

	Signed-off-by: Saravana Kannan <saravanak@google.com>
Link: https://lore.kernel.org/r/20200724180523.1393383-1-saravanak@google.com
	Signed-off-by: Greg Kroah-Hartman <gregkh@linuxfoundation.org>
(cherry picked from commit 90b109d50da09ddaa179732c01ccba7f759c125d)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	Documentation/ABI/testing/sysfs-class-devlink
#	drivers/base/core.c
diff --cc drivers/base/core.c
index 7ffd5023ca95,4d05868d9356..000000000000
--- a/drivers/base/core.c
+++ b/drivers/base/core.c
@@@ -402,6 -621,25 +402,28 @@@ struct device_link *device_link_add(str
  	link->flags = flags;
  	kref_init(&link->kref);
  
++<<<<<<< HEAD
++=======
+ 	link->link_dev.class = &devlink_class;
+ 	device_set_pm_not_required(&link->link_dev);
+ 	dev_set_name(&link->link_dev, "%s--%s",
+ 		     dev_name(supplier), dev_name(consumer));
+ 	if (device_register(&link->link_dev)) {
+ 		put_device(consumer);
+ 		put_device(supplier);
+ 		kfree(link);
+ 		link = NULL;
+ 		goto out;
+ 	}
+ 
+ 	if (flags & DL_FLAG_PM_RUNTIME) {
+ 		if (flags & DL_FLAG_RPM_ACTIVE)
+ 			refcount_inc(&link->rpm_active);
+ 
+ 		pm_runtime_new_link(consumer);
+ 	}
+ 
++>>>>>>> 90b109d50da0 (driver core: Change delimiter in devlink device's name to "--")
  	/* Determine the initial link state. */
  	if (flags & DL_FLAG_STATELESS)
  		link->status = DL_STATE_NONE;
* Unmerged path Documentation/ABI/testing/sysfs-class-devlink
* Unmerged path Documentation/ABI/testing/sysfs-class-devlink
* Unmerged path drivers/base/core.c
