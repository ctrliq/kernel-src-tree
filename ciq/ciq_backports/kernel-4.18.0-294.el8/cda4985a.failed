net: stmmac: Fix the packet count in stmmac_rx()

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-294.el8
commit-author Jose Abreu <Jose.Abreu@synopsys.com>
commit cda4985a3ebb95f14fe3a9dc905820417baf79a5
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-294.el8/cda4985a.failed

Currently, stmmac_rx() is counting the number of descriptors but it
should count the number of packets as specified by the NAPI limit.

Fix this.

Fixes: ec222003bd94 ("net: stmmac: Prepare to add Split Header support")
	Signed-off-by: Jose Abreu <Jose.Abreu@synopsys.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit cda4985a3ebb95f14fe3a9dc905820417baf79a5)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/net/ethernet/stmicro/stmmac/stmmac_main.c
diff --cc drivers/net/ethernet/stmicro/stmmac/stmmac_main.c
index 074dfac20958,1ab1eea1556a..000000000000
--- a/drivers/net/ethernet/stmicro/stmmac/stmmac_main.c
+++ b/drivers/net/ethernet/stmicro/stmmac/stmmac_main.c
@@@ -3551,8 -3531,8 +3549,13 @@@ read_again
  		if (unlikely(error && (status & rx_not_ls)))
  			goto read_again;
  		if (unlikely(error)) {
++<<<<<<< HEAD
 +			if (skb)
 +				dev_kfree_skb(skb);
++=======
+ 			dev_kfree_skb(skb);
+ 			count++;
++>>>>>>> cda4985a3ebb (net: stmmac: Fix the packet count in stmmac_rx())
  			continue;
  		}
  
* Unmerged path drivers/net/ethernet/stmicro/stmmac/stmmac_main.c
