ASoC: soc-card: add snd_soc_card_add_dai_link()

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-294.el8
Rebuild_CHGLOG: - [sound] ALSA: ASoC: soc-card: add snd_soc_card_add_dai_link() (Jaroslav Kysela) [1869536]
Rebuild_FUZZ: 94.00%
commit-author Kuninori Morimoto <kuninori.morimoto.gx@renesas.com>
commit cbc7a6b5a87a129084c643455d16699ea490fd2f
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-294.el8/cbc7a6b5.failed

Card related function should be implemented at soc-card now.
This patch adds it.

This patch adds missing return when error case.

	Signed-off-by: Kuninori Morimoto <kuninori.morimoto.gx@renesas.com>
	Reviewed-by: Ranjani Sridharan <ranjani.sridharan@linux.intel.com>
Link: https://lore.kernel.org/r/87o8q8zv2m.wl-kuninori.morimoto.gx@renesas.com
	Signed-off-by: Mark Brown <broonie@kernel.org>
(cherry picked from commit cbc7a6b5a87a129084c643455d16699ea490fd2f)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	include/sound/soc-card.h
#	sound/soc/soc-card.c
* Unmerged path include/sound/soc-card.h
* Unmerged path sound/soc/soc-card.c
* Unmerged path include/sound/soc-card.h
* Unmerged path sound/soc/soc-card.c
diff --git a/sound/soc/soc-core.c b/sound/soc/soc-core.c
index 4411bbd54478..53a640ed7ee3 100644
--- a/sound/soc/soc-core.c
+++ b/sound/soc/soc-core.c
@@ -1007,8 +1007,9 @@ int snd_soc_add_pcm_runtime(struct snd_soc_card *card,
 	/*
 	 * Notify the machine driver for extra initialization
 	 */
-	if (card->add_dai_link)
-		card->add_dai_link(card, dai_link);
+	ret = snd_soc_card_add_dai_link(card, dai_link);
+	if (ret < 0)
+		return ret;
 
 	if (dai_link->ignore)
 		return 0;
