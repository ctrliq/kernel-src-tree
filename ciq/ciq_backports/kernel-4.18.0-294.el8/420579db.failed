s390/qeth: don't warn for napi with 0 budget

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-294.el8
commit-author Julian Wiedmann <jwi@linux.ibm.com>
commit 420579dba126c6111b5a3dea062f21a7e4e647c6
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-294.el8/420579db.failed

Calling napi->poll() with 0 budget is a legitimate use by netpoll.

Fixes: a1c3ed4c9ca0 ("qeth: NAPI support for l2 and l3 discipline")
	Signed-off-by: Julian Wiedmann <jwi@linux.ibm.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit 420579dba126c6111b5a3dea062f21a7e4e647c6)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	drivers/s390/net/qeth_core_main.c
diff --cc drivers/s390/net/qeth_core_main.c
index d195a992cf72,2264c6619def..000000000000
--- a/drivers/s390/net/qeth_core_main.c
+++ b/drivers/s390/net/qeth_core_main.c
@@@ -5282,14 -5437,31 +5282,38 @@@ walk_packet
  
  	*__element = element;
  	*__offset = offset;
++<<<<<<< HEAD
 +	if (use_rx_sg) {
 +		QETH_CARD_STAT_INC(card, rx_sg_skbs);
 +		QETH_CARD_STAT_ADD(card, rx_sg_frags,
 +				   skb_shinfo(skb)->nr_frags);
++=======
+ 
+ 	qeth_receive_skb(card, skb, hdr, uses_frags);
+ 	return 0;
+ }
+ 
+ static int qeth_extract_skbs(struct qeth_card *card, int budget,
+ 			     struct qeth_qdio_buffer *buf, bool *done)
+ {
+ 	int work_done = 0;
+ 
+ 	*done = false;
+ 
+ 	while (budget) {
+ 		if (qeth_extract_skb(card, buf, &card->rx.b_element,
+ 				     &card->rx.e_offset)) {
+ 			*done = true;
+ 			break;
+ 		}
+ 
+ 		work_done++;
+ 		budget--;
++>>>>>>> 420579dba126 (s390/qeth: don't warn for napi with 0 budget)
  	}
 -
 -	return work_done;
 +	return skb;
  }
 +EXPORT_SYMBOL_GPL(qeth_core_get_next_skb);
  
  int qeth_poll(struct napi_struct *napi, int budget)
  {
* Unmerged path drivers/s390/net/qeth_core_main.c
