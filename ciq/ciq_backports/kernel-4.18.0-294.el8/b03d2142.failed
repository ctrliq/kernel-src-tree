tcp: move ipv6_specific declaration to remove a warning

jira LE-1907
Rebuild_History Non-Buildable kernel-4.18.0-294.el8
commit-author Eric Dumazet <edumazet@google.com>
commit b03d2142bea8cf7407a0a668ce8f5f115bd226c4
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-4.18.0-294.el8/b03d2142.failed

ipv6_specific should be declared in tcp include files,
not mptcp.

This removes the following warning :
  CHECK   net/ipv6/tcp_ipv6.c
net/ipv6/tcp_ipv6.c:78:42: warning: symbol 'ipv6_specific' was not declared. Should it be static?

	Signed-off-by: Eric Dumazet <edumazet@google.com>
	Signed-off-by: David S. Miller <davem@davemloft.net>
(cherry picked from commit b03d2142bea8cf7407a0a668ce8f5f115bd226c4)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	include/net/tcp.h
diff --cc include/net/tcp.h
index 40b3155f95cb,e6920ae0765c..000000000000
--- a/include/net/tcp.h
+++ b/include/net/tcp.h
@@@ -930,6 -932,13 +930,16 @@@ static inline int tcp_v6_sdif(const str
  #endif
  	return 0;
  }
++<<<<<<< HEAD
++=======
+ 
+ extern const struct inet_connection_sock_af_ops ipv6_specific;
+ 
+ INDIRECT_CALLABLE_DECLARE(void tcp_v6_send_check(struct sock *sk, struct sk_buff *skb));
+ INDIRECT_CALLABLE_DECLARE(int tcp_v6_rcv(struct sk_buff *skb));
+ INDIRECT_CALLABLE_DECLARE(void tcp_v6_early_demux(struct sk_buff *skb));
+ 
++>>>>>>> b03d2142bea8 (tcp: move ipv6_specific declaration to remove a warning)
  #endif
  
  static inline bool inet_exact_dif_match(struct net *net, struct sk_buff *skb)
* Unmerged path include/net/tcp.h
diff --git a/net/mptcp/protocol.h b/net/mptcp/protocol.h
index d755af8e07e9..d9440cc6beb9 100644
--- a/net/mptcp/protocol.h
+++ b/net/mptcp/protocol.h
@@ -363,9 +363,6 @@ static inline void mptcp_subflow_tcp_fallback(struct sock *sk,
 }
 
 extern const struct inet_connection_sock_af_ops ipv4_specific;
-#if IS_ENABLED(CONFIG_MPTCP_IPV6)
-extern const struct inet_connection_sock_af_ops ipv6_specific;
-#endif
 
 void mptcp_proto_init(void);
 #if IS_ENABLED(CONFIG_MPTCP_IPV6)
