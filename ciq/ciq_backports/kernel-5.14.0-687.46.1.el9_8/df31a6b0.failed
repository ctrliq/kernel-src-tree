Revert "net/smc: Introduce TCP ULP support"

jira KERNEL-1580
cve CVE-2026-46330
Rebuild_History Non-Buildable kernel-5.14.0-687.46.1.el9_8
commit-author D. Wythe <alibuda@linux.alibaba.com>
commit df31a6b0a3057e66994ad6ccf5d95b9b9514f033
Empty-Commit: Cherry-Pick Conflicts during history rebuild.
Will be included in final tarball splat. Ref for failed cherry-pick at:
ciq/ciq_backports/kernel-5.14.0-687.46.1.el9_8/df31a6b0.failed

This reverts commit d7cd421da9da2cc7b4d25b8537f66db5c8331c40.

As reported by Al Viro, the TCP ULP support for SMC is fundamentally
broken. The implementation attempts to convert an active TCP socket
into an SMC socket by modifying the underlying `struct file`, dentry,
and inode in-place, which violates core VFS invariants that assume
these structures are immutable for an open file, creating a risk of
use after free errors and general system instability.

Given the severity of this design flaw and the fact that cleaner
alternatives (e.g., LD_PRELOAD, BPF) exist for legacy application
transparency, the correct course of action is to remove this feature
entirely.

Fixes: d7cd421da9da ("net/smc: Introduce TCP ULP support")
Link: https://lore.kernel.org/netdev/Yus1SycZxcd+wHwz@ZenIV/
	Reported-by: Al Viro <viro@zeniv.linux.org.uk>
	Signed-off-by: D. Wythe <alibuda@linux.alibaba.com>
	Reviewed-by: Tony Lu <tonylu@linux.alibaba.com>
	Reviewed-by: Dust Li <dust.li@linux.alibaba.com>
	Reviewed-by: Simon Horman <horms@kernel.org>
Link: https://patch.msgid.link/20260128055452.98251-1-alibuda@linux.alibaba.com
	Signed-off-by: Jakub Kicinski <kuba@kernel.org>
(cherry picked from commit df31a6b0a3057e66994ad6ccf5d95b9b9514f033)
	Signed-off-by: Jonathan Maple <jmaple@ciq.com>

# Conflicts:
#	net/smc/af_smc.c
diff --cc net/smc/af_smc.c
index b691b7fc1260,d8201eb3ac5f..000000000000
--- a/net/smc/af_smc.c
+++ b/net/smc/af_smc.c
@@@ -3407,17 -3379,7 +3406,21 @@@ static int smc_create(struct net *net, 
  	if (!sk)
  		goto out;
  
++<<<<<<< HEAD
 +	/* create internal TCP socket for CLC handshake and fallback */
 +	smc = smc_sk(sk);
 +
 +	rc = 0;
 +	if (clcsock)
 +		smc->clcsock = clcsock;
 +	else
 +		rc = smc_create_clcsk(net, sk, family);
 +	smc->sk.sk_sndbuf = max(smc->clcsock->sk->sk_sndbuf, SMC_BUF_MIN_SIZE);
 +	smc->sk.sk_rcvbuf = max(smc->clcsock->sk->sk_rcvbuf, SMC_BUF_MIN_SIZE);
 +
++=======
+ 	rc = smc_create_clcsk(net, sk, family);
++>>>>>>> df31a6b0a305 (Revert "net/smc: Introduce TCP ULP support")
  	if (rc) {
  		sk_common_release(sk);
  		sock->sk = NULL;
@@@ -3618,29 -3516,21 +3557,37 @@@ static int __init smc_init(void
  		pr_err("%s: ib_register fails with %d\n", __func__, rc);
  		goto out_sock;
  	}
 -	rc = smc_inet_init();
++<<<<<<< HEAD
 +
 +	rc = smc_loopback_init();
  	if (rc) {
 -		pr_err("%s: smc_inet_init fails with %d\n", __func__, rc);
 +		pr_err("%s: smc_loopback_init fails with %d\n", __func__, rc);
  		goto out_ib;
  	}
 -	rc = bpf_smc_hs_ctrl_init();
 +
 +	rc = tcp_register_ulp(&smc_ulp_ops);
  	if (rc) {
 -		pr_err("%s: bpf_smc_hs_ctrl_init fails with %d\n", __func__,
 -		       rc);
 -		goto out_inet;
 +		pr_err("%s: tcp_ulp_register fails with %d\n", __func__, rc);
 +		goto out_lo;
 +	}
++=======
++>>>>>>> df31a6b0a305 (Revert "net/smc: Introduce TCP ULP support")
 +	rc = smc_inet_init();
 +	if (rc) {
 +		pr_err("%s: smc_inet_init fails with %d\n", __func__, rc);
- 		goto out_ulp;
++		goto out_ib;
  	}
  	static_branch_enable(&tcp_have_smc);
  	return 0;
++<<<<<<< HEAD
 +out_ulp:
 +	tcp_unregister_ulp(&smc_ulp_ops);
 +out_lo:
 +	smc_loopback_exit();
++=======
+ out_inet:
+ 	smc_inet_exit();
++>>>>>>> df31a6b0a305 (Revert "net/smc: Introduce TCP ULP support")
  out_ib:
  	smc_ib_unregister_client();
  out_sock:
@@@ -3676,10 -3566,8 +3623,9 @@@ static void __exit smc_exit(void
  {
  	static_branch_disable(&tcp_have_smc);
  	smc_inet_exit();
- 	tcp_unregister_ulp(&smc_ulp_ops);
  	sock_unregister(PF_SMC);
  	smc_core_exit();
 +	smc_loopback_exit();
  	smc_ib_unregister_client();
  	smc_ism_exit();
  	destroy_workqueue(smc_close_wq);
* Unmerged path net/smc/af_smc.c
