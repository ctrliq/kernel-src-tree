# Symbol: PWM_GPIO [=n]
# Type  : tristate
# Defined at drivers/pwm/Kconfig:239
#   Prompt: GPIO PWM support
#   Depends on: PWM [=y] && GPIOLIB [=y] && HIGH_RES_TIMERS [=y]
#   Location:
#     -> Device Drivers
#       -> Pulse-Width Modulation (PWM) Support (PWM [=y])
#         -> GPIO PWM support (PWM_GPIO [=n])
# 
# 
# 
# Commit: 7f61257cd6e1 (pwm: Add GPIO PWM driver)
# CONFIG_PWM_GPIO is not set
