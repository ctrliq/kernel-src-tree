# Symbol: ZRAM_BACKEND_DEFLATE [=n]
# Type  : bool
# Defined at drivers/block/zram/Kconfig:35
#   Prompt: deflate compression support
#   Depends on: BLK_DEV [=y] && ZRAM [=m]
#   Location:
#     -> Device Drivers
#       -> Block devices (BLK_DEV [=y])
#         -> Compressed RAM block device support (ZRAM [=m])
#           -> deflate compression support (ZRAM_BACKEND_DEFLATE [=n])
# Selects: ZLIB_DEFLATE [=y] && ZLIB_INFLATE [=y]
# 
# 
# 
# Commit: 84112e314f69 (zram: add zlib compression backend support)
# CONFIG_ZRAM_BACKEND_DEFLATE is not set
