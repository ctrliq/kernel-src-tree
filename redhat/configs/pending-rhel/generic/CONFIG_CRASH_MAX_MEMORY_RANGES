# Symbol: CRASH_MAX_MEMORY_RANGES [=8192]
# Type  : integer
# Defined at kernel/Kconfig.kexec:129
#   Prompt: Specify the maximum number of memory regions for the elfcorehdr
#   Depends on: CRASH_HOTPLUG [=y]
#   Location:
#     -> General setup
#       -> Kexec and crash features
#         -> kernel crash dumps (CRASH_DUMP [=y])
#           -> Update the crash elfcorehdr on system configuration changes (CRASH_HOTPLUG [=y])
#             -> Specify the maximum number of memory regions for the elfcorehdr (CRASH_MAX_MEMORY_RANGES [=8192])
# 
# 
# 
CONFIG_CRASH_MAX_MEMORY_RANGES=8192
