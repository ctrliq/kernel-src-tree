# Symbol: ZSWAP_SHRINKER_DEFAULT_ON [=n]
# Type  : bool
# Defined at mm/Kconfig:64
#   Prompt: Shrink the zswap pool on memory pressure
#   Depends on: ZSWAP [=y]
#   Location:
#     -> Memory Management options
#       -> Support for paging of anonymous memory (swap) (SWAP [=y])
#         -> Compressed cache for swap pages (ZSWAP [=y])
#           -> Shrink the zswap pool on memory pressure (ZSWAP_SHRINKER_DEFAULT_ON [=n])
# 
# 
# 
# CONFIG_ZSWAP_SHRINKER_DEFAULT_ON is not set
