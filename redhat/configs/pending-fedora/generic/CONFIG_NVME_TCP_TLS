# Symbol: NVME_TCP_TLS [=n]
# Type  : bool
# Defined at drivers/nvme/host/Kconfig:95
#   Prompt: NVMe over Fabrics TCP TLS encryption support
#   Depends on: NVME_TCP [=m]
#   Location:
#     -> Device Drivers
#       -> NVME Support
#         -> NVM Express over Fabrics TCP host driver (NVME_TCP [=m])
#           -> NVMe over Fabrics TCP TLS encryption support (NVME_TCP_TLS [=n])
# Selects: NVME_COMMON [=m] && NVME_KEYRING [=n] && NET_HANDSHAKE [=y] && KEYS [=y]
# 
# 
# 
# CONFIG_NVME_TCP_TLS is not set
